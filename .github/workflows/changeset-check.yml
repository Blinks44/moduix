name: Changeset Check

on:
  pull_request:

permissions:
  contents: read

concurrency:
  group: changeset-check-${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  changeset-check:
    name: Require changeset for releasable UI changes
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Validate changeset
        shell: bash
        run: |
          set -euo pipefail

          base_ref="${{ github.base_ref }}"
          git fetch origin "${base_ref}" --depth=1

          changed_files="$(git diff --name-only "origin/${base_ref}"...HEAD)"

          if [[ -z "${changed_files}" ]]; then
            echo "No changed files detected."
            exit 0
          fi

          echo "Changed files:"
          echo "${changed_files}"

          releasable_files="$(
            printf '%s\n' "${changed_files}" \
              | grep -E '^(packages/ui/package\.json|packages/ui/tsdown\.config\.ts|packages/ui/tsconfig\.app\.json|packages/ui/tsconfig\.json|packages/ui/src/)' \
              | grep -Ev '(^packages/ui/src/.*\.stories\.(ts|tsx|css)$|^packages/ui/src/components/.+/[^/]+\.md$)' \
              || true
          )"

          if [[ -z "${releasable_files}" ]]; then
            echo "No releasable UI changes detected. Skipping changeset requirement."
            exit 0
          fi

          echo "Releasable UI files:"
          echo "${releasable_files}"

          if grep -Eq '^\.changeset/[^/]+\.md$' <<< "${changed_files}"; then
            echo "Changeset file found. Check passed."
            exit 0
          fi

          echo "::error::This PR changes releasable files in packages/ui but does not include a changeset file in .changeset/*.md"
          echo "Run 'npm run changeset' and commit the generated file."
          exit 1