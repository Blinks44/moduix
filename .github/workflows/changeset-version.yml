name: Changeset Version

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

concurrency:
  group: changeset-version-${{ github.workflow }}
  cancel-in-progress: true

jobs:
  changeset-version:
    name: Update changeset version with PR
    runs-on: ubuntu-latest
    if: github.actor != 'github-actions[bot]' && !startsWith(github.ref, 'refs/heads/changeset-release/')

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Create or update release PR
        uses: changesets/action@v1
        with:
          version: npm run changeset:version
          commit: 'chore(release): version packages'
          title: 'chore(release): version packages'
          setupGitUser: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}