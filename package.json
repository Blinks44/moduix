{
  "name": "moduix-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "build:docs": "turbo run build -F moduix-docs",
    "build:ui": "turbo run build -F moduix",
    "changeset": "changeset",
    "changeset:version": "changeset version",
    "changeset:release": "turbo run build -F moduix && changeset publish",
    "deploy:docs": "turbo run deploy -F moduix-docs",
    "dev": "turbo run dev",
    "fmt:check": "oxfmt --check",
    "fmt:fix": "oxfmt",
    "lint:check": "oxlint",
    "lint:fix": "oxlint --fix",
    "prepare": "husky",
    "publish:ui": "npm publish -w moduix",
    "sync:base-ui": "node scripts/update-base-ui-docs.mjs",
    "tsc:check": "turbo run tsc:check"
  },
  "dependencies": {
    "rollup-plugin-copy": "^3.5.0"
  },
  "devDependencies": {
    "@changesets/cli": "^2.31.0",
    "@storybook/react-vite": "^10.4.0",
    "husky": "^9.1.7",
    "lint-staged": "^17.0.5",
    "oxfmt": "^0.51.0",
    "oxlint": "^1.66.0",
    "storybook": "^10.4.0",
    "turbo": "^2.9.14"
  },
  "packageManager": "npm@11.14.1"
}