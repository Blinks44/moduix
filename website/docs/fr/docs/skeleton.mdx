---
title: Skeleton
component: skeleton
description: Un espace réservé pour le chargement de textes, cartes, médias et sections personnalisées en React et Solid.
---

import * as SkeletonDocs from '@/components/examples/skeleton/css-properties';
import SkeletonBasic from '@/components/examples/skeleton/basic.tsx';
import SkeletonCard from '@/components/examples/skeleton/card.tsx';
import SkeletonCustomHost from '@/components/examples/skeleton/custom-host.tsx';
import SkeletonLoadedContent from '@/components/examples/skeleton/loaded-content.tsx';
import SkeletonMediaObject from '@/components/examples/skeleton/media-object.tsx';
import SkeletonResponsiveLayout from '@/components/examples/skeleton/responsive-layout.tsx';
import SkeletonVariants from '@/components/examples/skeleton/variants.tsx';
import skeletonSolidBasic from '../../../snippets/skeleton/solid/basic.tsx?raw';
import skeletonSolidCard from '../../../snippets/skeleton/solid/card.tsx?raw';
import skeletonSolidCustomHost from '../../../snippets/skeleton/solid/custom-host.tsx?raw';
import skeletonSolidLoadedContent from '../../../snippets/skeleton/solid/loaded-content.tsx?raw';
import skeletonSolidMediaObject from '../../../snippets/skeleton/solid/media-object.tsx?raw';
import skeletonSolidResponsiveLayout from '../../../snippets/skeleton/solid/responsive-layout.tsx?raw';
import skeletonSolidVariants from '../../../snippets/skeleton/solid/variants.tsx?raw';

## Référence de l’API

Ark UI ne fournit pas de primitive `Skeleton` dédiée. moduix construit `Skeleton` à partir de la
[fabrique Ark](https://ark-ui.com/docs/guides/composition#the-ark-factory) officielle et suit les recommandations
d’Ark pour la [composition](https://ark-ui.com/docs/guides/composition), les
[styles](https://ark-ui.com/docs/guides/styling) et les [refs](https://ark-ui.com/docs/guides/ref).

## Exemple de base


Utilisez `Skeleton` pour les lignes de texte, les cartes, les avatars et les espaces réservés aux médias. La
mise en page environnante gère les espacements et la composition responsive. Consultez le [guide de démarrage](/docs/quick-start)
pour l’installation des packages, la configuration du registre et le choix du mode de style.

<ExampleFrame variant="component">
  <SkeletonBasic />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/skeleton/basic.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={skeletonSolidBasic} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/skeleton/skeleton-basic.module.css"
```
</div>

</ExampleCode>

## Installation avec shadcn

Consultez le [Démarrage rapide](/docs/quick-start) pour la configuration complète du paquet, du registre et des styles.

<Tabs groupId="framework">
  <Tab label="React" value="react">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-react/skeleton' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-react-tailwind/skeleton' },
  ]}
/>

  </Tab>
  <Tab label="Solid" value="solid">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-solid/skeleton' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-solid-tailwind/skeleton' },
  ]}
/>

  </Tab>
</Tabs>

## Anatomie

`Skeleton` ne comporte qu’une seule partie visuelle. La mise en page se fait dans le balisage qui l’entoure.

```text
Skeleton.Root
└─ children (facultatifs, affichés lorsque loading={false})
```

| Partie                        | Rôle                                                                                                                  |
| ----------------------------- | --------------------------------------------------------------------------------------------------------------------- |
| `Skeleton` / `Skeleton.Root` | Bloc d’attente. Rend par défaut un élément de la fabrique Ark ; avec `asChild`, un autre élément sémantique peut devenir la racine. |

## Composition

`Skeleton` ne gère pas l’état de chargement. La zone de données ou de page qui porte la requête transmet `loading`
et doit porter `aria-busy` ; l’espace réservé est décoratif. Définissez `loading={false}` pour afficher les enfants
dans le même élément racine.

`Skeleton` ne comporte qu’une seule partie visuelle : composez les cartes, les objets média et les mises en page
responsives dans votre propre conteneur. `Stack` est exporté depuis chaque package, de sorte que les exemples
ci-dessous pourraient l’utiliser dans les deux variantes ; les exemples Tailwind utilisent simplement les
utilitaires flex et grid natifs. Utilisez `asChild` uniquement lorsqu’un autre élément
sémantique doit devenir la racine ; en Solid, `asChild` reçoit une fonction de rendu.

## Exemples

### Carte

Utilisez un grand bloc et des lignes de texte plus courtes pour préserver la mise en page d’une carte pendant le chargement.

<ExampleFrame variant="component">
  <SkeletonCard />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/skeleton/card.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={skeletonSolidCard} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/skeleton/skeleton-card.module.css"
```
</div>

</ExampleCode>

### Contenu chargé

Utilisez `loading` pour conserver le même élément racine lors du remplacement de l’espace réservé par le contenu réel.

<ExampleFrame variant="component">
  <SkeletonLoadedContent />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/skeleton/loaded-content.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={skeletonSolidLoadedContent} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/skeleton/skeleton-loaded-content.module.css"
```
</div>

</ExampleCode>

### Objet média

Utilisez `boxSize` avec un rayon complet pour l’espace réservé d’un avatar, puis placez les lignes de texte à côté.

<ExampleFrame variant="component">
  <SkeletonMediaObject />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/skeleton/media-object.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={skeletonSolidMediaObject} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/skeleton/skeleton-media-object.module.css"
```
</div>

</ExampleCode>

### Mise en page responsive

Laissez la logique de mise en page responsive dans le conteneur et utilisez chaque `Skeleton` comme un bloc visuel.

<ExampleFrame variant="component">
  <SkeletonResponsiveLayout />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/skeleton/responsive-layout.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={skeletonSolidResponsiveLayout} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/skeleton/skeleton-responsive-layout.module.css"
```
</div>

</ExampleCode>

### Variantes

Utilisez `variant` pour choisir un espace réservé animé par pulsation ou statique.

<ExampleFrame variant="component">
  <SkeletonVariants />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/skeleton/variants.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={skeletonSolidVariants} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/skeleton/skeleton-variants.module.css"
```
</div>

</ExampleCode>

### Personnalisation avancée

Utilisez `asChild` pour qu’un élément sémantique tel que `section` devienne la racine DOM à la place du `div` par défaut.

<ExampleFrame variant="component">
  <SkeletonCustomHost />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/skeleton/custom-host.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={skeletonSolidCustomHost} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/skeleton/skeleton-custom-host.module.css"
```
</div>

</ExampleCode>

## Styles

Utilisez `className` en React ou `class` en Solid pour les styles structurels et les classes utilitaires de votre
application. Les variantes Tailwind utilisent les utilitaires natifs et les classes du consommateur ; les variantes
CSS Modules exposent les variables ci-dessous. Les attributs d’état et les hooks `data-slot` sont communs aux deux
modes de style.

### Variables CSS

<div className="docs-reference">
  <Tabs
    values={[{ label: 'Variables CSS', value: 'Variables CSS' }]}
    className="docs-reference-tabs"
    contentItemClassName="docs-reference-panel"
  >
    <Tab>
      <SkeletonDocs.SkeletonCssPropertiesPanel />
    </Tab>
  </Tabs>
</div>

### Sélecteurs et attributs de style

- `[data-scope="skeleton"][data-part="root"]` cible l’élément racine exporté.
- `data-slot="skeleton-root"` est le sélecteur stable de moduix pour cet élément.
- `data-state="loading"` et `data-state="loaded"` reflètent la propriété `loading`.
- `data-variant="pulse"` et `data-variant="none"` reflètent la variante choisie.
- `data-loading` n’est présent que lorsque les styles d’attente sont actifs.
