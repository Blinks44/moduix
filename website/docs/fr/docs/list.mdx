---
title: List
component: list
description: Liste sémantique avec espacement, typographie, tonalité et marqueurs natifs personnalisables.
---

import * as ListDocs from '@/components/examples/list/css-properties';
import ListBasic from '@/components/examples/list/basic.tsx';
import ListCustomComposition from '@/components/examples/list/custom-composition.tsx';
import ListMarkerless from '@/components/examples/list/markerless.tsx';
import ListNativeItems from '@/components/examples/list/native-items.tsx';
import ListOrdered from '@/components/examples/list/ordered.tsx';
import ListRootAsChild from '@/components/examples/list/root-as-child.tsx';
import ListTone from '@/components/examples/list/tone.tsx';
import listSolidBasic from '../../../snippets/list/solid/basic.tsx?raw';
import listSolidCustomComposition from '../../../snippets/list/solid/custom-composition.tsx?raw';
import listSolidMarkerless from '../../../snippets/list/solid/markerless.tsx?raw';
import listSolidNativeItems from '../../../snippets/list/solid/native-items.tsx?raw';
import listSolidOrdered from '../../../snippets/list/solid/ordered.tsx?raw';
import listSolidRootAsChild from '../../../snippets/list/solid/root-as-child.tsx?raw';
import listSolidTone from '../../../snippets/list/solid/tone.tsx?raw';

## Référence de l’API

Ark UI ne fournit pas de primitive dédiée pour les listes. moduix s’appuie sur l’[Ark factory](https://ark-ui.com/docs/guides/composition#the-ark-factory)
pour composer des éléments natifs `ul`, `ol` et `li`. Consultez les guides d’Ark sur la
[composition](https://ark-ui.com/docs/guides/composition), les [styles](https://ark-ui.com/docs/guides/styling) et
la [transmission des refs](https://ark-ui.com/docs/guides/ref) pour connaître le modèle sous-jacent.

## Choisir le bon composant

Utilisez `List` pour du contenu statique regroupé, par exemple des étapes, des caractéristiques ou des métadonnées.
Le composant conserve la sémantique native de `ul` et `ol`, tout en gérant l’espacement, la typographie, la tonalité
et les marqueurs.

Si l’utilisateur doit parcourir la liste au clavier et sélectionner une ou plusieurs options, utilisez
[Listbox](/docs/listbox). `Listbox` gère le focus, la collection et la sélection.

## Exemple de base


`List` rend un élément `<ul>` sémantique par défaut. Utilisez `List.Item` si vous avez besoin de sa partie stable et
de ses points d’accroche CSS, ou passez directement des éléments `<li>` natifs si ce wrapper ne vous est pas utile.
La racine `List` gère l’espacement, la typographie, la tonalité et les marqueurs. Laissez `marker` absent pour le
mode sémantique par défaut, ou définissez explicitement `disc`, `decimal` ou `none`.

<ExampleFrame variant="component">
  <ListBasic />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/list/basic.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={listSolidBasic} />

    </Tab>
  </Tabs>
</div>
</ExampleCode>

## Installation avec shadcn

Consultez le [Démarrage rapide](/docs/quick-start) pour la configuration complète du paquet, du registre et des styles.

<Tabs groupId="framework">
  <Tab label="React" value="react">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-react/list' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-react-tailwind/list' },
  ]}
/>

  </Tab>
  <Tab label="Solid" value="solid">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-solid/list' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-solid-tailwind/list' },
  ]}
/>

  </Tab>
</Tabs>

## Anatomie

```text
List / List.Root
└─ List.Item | li
```

| Partie               | Rôle                                                                                                  |
| -------------------- | ----------------------------------------------------------------------------------------------------- |
| `List` / `List.Root` | Racine sémantique de la liste, qui rend `ul` ou `ol` et contrôle les marqueurs, l’espacement, la taille et la tonalité. |
| `List.Item`          | Partie facultative qui rend `li`, transmet les attributs natifs et la ref, et prend en charge `asChild`.             |

## Composition

Utilisez `List` ou `List.Root` comme racine. Ses enfants directs doivent être `List.Item` ou des éléments `li` natifs,
car l’espacement de la racine et le style des marqueurs ciblent les éléments directs de la liste. Utilisez `List.Item`
si vous avez besoin de sa partie stable, de `className`/`class` ou de `asChild` ; sinon, de simples éléments `li`
suffisent. Utilisez `asChild` uniquement lorsqu’un composant de l’application rend un seul élément sémantique `ul`,
`ol` ou `li` et transmet les props et la ref reçues. En Solid, utilisez la forme d’`asChild` avec une fonction de rendu.
`List` ne fournit ni machine à états ni callbacks : la sélection au clavier relève de `Listbox`.

## Exemples

### Racine personnalisée

Utilisez `List asChild` lorsqu’un composant de votre application doit rendre lui-même l’élément sémantique de la liste.
En React, il doit transmettre les props et la ref reçues à un seul `<ul>` ou `<ol>` ; en Solid, utilisez la forme avec
fonction de rendu.

<ExampleFrame variant="component">
  <ListRootAsChild />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/list/root-as-child.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={listSolidRootAsChild} />

    </Tab>
  </Tabs>
</div>
</ExampleCode>

### Sans marqueurs

Utilisez `marker="none"` si vous avez besoin de la sémantique d’une liste sans marqueurs visibles. Dans ce mode,
`List` ajoute `role="list"` par défaut, sauf si vous fournissez une autre valeur pour `role`.

<ExampleFrame variant="component">
  <ListMarkerless />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/list/markerless.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={listSolidMarkerless} />

    </Tab>
  </Tabs>
</div>
</ExampleCode>

### Éléments natifs

Utilisez des éléments `li` natifs si vous n’avez pas besoin de la partie exportée `List.Item`.

<ExampleFrame variant="component">
  <ListNativeItems />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/list/native-items.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={listSolidNativeItems} />

    </Tab>
  </Tabs>
</div>
</ExampleCode>

### Liste ordonnée

Utilisez `as="ol"` pour rendre une liste ordonnée tout en conservant les styles appliqués par la racine moduix.
Les propriétés natives `start`, `reversed` et `type` sont transmises à la racine de la liste ordonnée.

<ExampleFrame variant="component">
  <ListOrdered />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/list/ordered.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={listSolidOrdered} />

    </Tab>
  </Tabs>
</div>
</ExampleCode>

### Tonalité

Utilisez `tone` pour appliquer une couleur sémantique au texte et aux marqueurs de la liste.

<ExampleFrame variant="component">
  <ListTone />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/list/tone.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={listSolidTone} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/list/list-tone.module.css"
```
</div>
</ExampleCode>

### Personnalisation avancée

Utilisez `List.Item asChild` lorsqu’un composant personnalisé doit rendre lui-même l’élément `<li>`. Il doit
transmettre les props et la ref reçues et conserver l’élément sémantique `<li>`. Dans les autres cas, utilisez
directement `List.Item className` ou `class`.

<ExampleFrame variant="component">
  <ListCustomComposition />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/list/custom-composition.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={listSolidCustomComposition} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/list/list-custom-composition.module.css"
```
</div>
</ExampleCode>

## Styles

Appliquez des styles à `List` avec la propriété de classe (`className` en React et `class` en Solid), les attributs
`data-*` alignés sur Ark, le sélecteur natif `li::marker` et les variables CSS publiques `--moduix-list-*`
répertoriées ci-dessous. Cette liste couvre l’API CSS publique.

### Propriétés CSS

<div className="docs-reference">
  <Tabs
    values={[{ label: 'Variables CSS', value: 'CSS Variables' }]}
    className="docs-reference-tabs"
    contentItemClassName="docs-reference-panel"
  >
    <Tab>
      <ListDocs.ListCssPropertiesPanel />
    </Tab>
  </Tabs>
</div>

### Points d’accroche CSS

`List` expose les points d’accroche CSS publics suivants :

- racine : `data-scope="list"`, `data-part="root"`, `data-slot="list-root"`
- élément : `data-scope="list"`, `data-part="item"`, `data-slot="list-item"`
- `data-gap="xs" | "sm" | "md" | "lg" | "xl" | "2xl"` pour cibler les styles liés à l’espacement
- `data-marker="auto" | "disc" | "decimal" | "none"` pour cibler les styles liés aux marqueurs
- `data-size="xs" | "sm" | "md" | "lg" | "xl"` pour cibler les styles liés à la taille typographique
- `data-tone="default" | "muted" | "subtle" | "primary" | "destructive"` pour les couleurs sémantiques
- le sélecteur natif `li::marker` pour remplacer la couleur ou l’épaisseur du marqueur
