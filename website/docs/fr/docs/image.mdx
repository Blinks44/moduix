---
title: Image
component: image
description: "Images responsives avec détection automatique des CDN. Unpic génère les attributs srcset et sizes."
---

import * as ImageDocs from '@/components/examples/image/css-properties';
import ImageArtDirection from '@/components/examples/image/art-direction.tsx';
import ImageBasic from '@/components/examples/image/basic.tsx';
import ImageFixed from '@/components/examples/image/fixed.tsx';
import ImageFullWidth from '@/components/examples/image/full-width.tsx';
import imageSolidArtDirection from '../../../snippets/image/solid/art-direction.tsx?raw';
import imageSolidBasic from '../../../snippets/image/solid/basic.tsx?raw';
import imageSolidFixed from '../../../snippets/image/solid/fixed.tsx?raw';
import imageSolidFullWidth from '../../../snippets/image/solid/full-width.tsx?raw';

## Référence de l’API

`Image` est un composant moduix léger qui s’appuie sur le composant `<img>` d’[Unpic React](https://unpic.pics/img/react/).
Ark UI ne propose pas de primitive dédiée aux images. `Image` utilise donc la composition native de `<img>` et de
`<picture>` plutôt qu’une partie Ark. Consultez le [guide de composition d’Ark](https://ark-ui.com/docs/guides/composition)
pour en savoir plus sur son modèle fondé sur les éléments natifs.

## Exemple de base

Utilisez `Image` avec l’URL d’une image provenant d’un CDN pris en charge ou avec une URL d’image classique.
Dans le mode `layout="constrained"`, indiquez `width` et `height`. Unpic génère un `srcset` et un attribut
`sizes` adaptés lorsqu’il reconnaît le fournisseur. Dans le cas contraire, il conserve l’URL source telle quelle.
Le navigateur réserve l’espace nécessaire avant le chargement de l’image.

<ExampleFrame variant="component">
  <ImageBasic />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/image/basic.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={imageSolidBasic} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

## Installation avec shadcn

Consultez le [Démarrage rapide](/docs/quick-start) pour la configuration complète du paquet, du registre et des styles.

<Tabs groupId="framework">
  <Tab label="React" value="react">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-react/image' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-react-tailwind/image' },
  ]}
/>

  </Tab>
  <Tab label="Solid" value="solid">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-solid/image' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-solid-tailwind/image' },
  ]}
/>

  </Tab>
</Tabs>

## Anatomie

```text
Image
└─ img

picture (native)
├─ ImageSource
└─ Image
```

| Partie                 | Rôle                                                                      |
| ---------------------- | ------------------------------------------------------------------------- |
| `Image`                | Élément `<img>` natif et adaptatif ; Unpic génère `srcset`, `sizes` et les styles. |
| `ImageSource`          | Élément `<source>` adaptatif utilisé dans un élément natif `<picture>`.          |

## Composition

`Image` est la racine et peut être rendu directement. Utilisez `ImageSource` uniquement comme enfant
d’un élément natif `<picture>` et placez `Image` en dernier afin qu’il reste l’image de secours si
aucune source n’est sélectionnée. Pour choisir un format, placez une source avec `type` avant une
source équivalente sans `type` ; le navigateur retient le premier format qu’il prend en charge.

## Exemples

### Cadrage selon l’écran

Utilisez `ImageSource` pour afficher une vue plus large d’un bureau sur les grands écrans et une image
au format portrait sur les petits écrans. La première source destinée aux grands écrans demande le format
AVIF. La suivante sert de solution de remplacement aux navigateurs qui ne prennent pas l’AVIF en charge.
`ImageSource` doit rester dans un élément natif `<picture>`.

<ExampleFrame variant="component">
  <ImageArtDirection />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/image/art-direction.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={imageSolidArtDirection} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/image/image-art-direction.module.css"
```
</div>

</ExampleCode>

### Taille fixe

Utilisez `layout="fixed"` lorsque l’image doit conserver des dimensions exactes en pixels CSS.
Vérifiez que son conteneur parent dispose de suffisamment d’espace : une image fixe ne se réduit pas
pour tenir dans un conteneur plus étroit.

<ExampleFrame variant="component">
  <ImageFixed />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/image/fixed.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={imageSolidFixed} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Pleine largeur

Utilisez `layout="fullWidth"` pour une image principale ou une section média qui doit occuper toute
la largeur de son conteneur. Indiquez `height` si la hauteur du cadrage doit rester fixe.

<ExampleFrame variant="component">
  <ImageFullWidth />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/image/full-width.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={imageSolidFullWidth} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

## Styles

Utilisez `className` en React ou `class` en Solid sur `Image` et `ImageSource` pour les styles locaux.
Les variantes Tailwind utilisent des utilitaires natifs et les classes du consommateur. La variable
`--moduix-image-radius` personnalise les variantes CSS Modules, tandis que les hooks `data-slot` sont
partagés par les deux modes de style.

### Propriétés CSS

<CssPropertiesSection properties={ImageDocs.imageCssProperties} />

### Points d’accroche CSS

- `data-slot="image-root"` permet de sélectionner l’élément `<img>` affiché en CSS.
- `data-slot="image-source"` permet de sélectionner le nœud `<source>` dans un élément natif `<picture>`.
- `className` en React et `class` en Solid sont les méthodes recommandées pour appliquer des styles locaux.
