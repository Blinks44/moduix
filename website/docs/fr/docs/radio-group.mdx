---
title: Radio Group
component: radio-group
description: Un groupe de boutons radio pour choisir une seule option parmi plusieurs.
---

import * as RadioGroupDocs from '@/components/examples/radio-group/radio-group';
import RadioGroupAdvancedCustomization from '../../en/docs/_snippets/radio-group/advanced-customization.tsx';
import RadioGroupAsChild from '../../en/docs/_snippets/radio-group/aschild.tsx';
import RadioGroupBasic from '../../en/docs/_snippets/radio-group/basic.tsx';
import RadioGroupControlled from '../../en/docs/_snippets/radio-group/controlled.tsx';
import RadioGroupDisabled from '../../en/docs/_snippets/radio-group/disabled.tsx';
import RadioGroupDisabledItem from '../../en/docs/_snippets/radio-group/item-disabled.tsx';
import RadioGroupForm from '../../en/docs/_snippets/radio-group/form.tsx';
import RadioGroupIndicator from '../../en/docs/_snippets/radio-group/indicator.tsx';
import RadioGroupInitialValue from '../../en/docs/_snippets/radio-group/initial-value.tsx';
import RadioGroupOrientation from '../../en/docs/_snippets/radio-group/orientation.tsx';
import RadioGroupRootProvider from '../../en/docs/_snippets/radio-group/root-provider.tsx';
import RadioGroupSizes from '../../en/docs/_snippets/radio-group/sizes.tsx';
import RadioGroupField from '../../en/docs/_snippets/radio-group/with-field.tsx';
import RadioGroupFieldset from '../../en/docs/_snippets/radio-group/with-fieldset.tsx';

Utilisez `RadioGroup` lorsqu’une personne doit choisir une seule option parmi plusieurs options
visibles, par exemple un framework, un forfait ou un type de compte. Utilisez `Checkbox` pour des
choix indépendants et `Switch` pour un réglage qui s’applique immédiatement.

## Référence de l’API

`RadioGroup` suit la primitive radio-group d’Ark UI et ajoute les styles moduix, des attributs
`data-slot` stables et le composant pratique `Option`. Le composant est exporté depuis
`@moduix/react/radio-group`.

<PrimitiveReference href="https://ark-ui.com/docs/components/radio-group" />

## Exemple de base

Pour la plupart des groupes, utilisez `RadioGroup.Option` pour afficher un élément avec libellé,
contrôle et texte visible. Chaque élément inclut automatiquement son champ natif masqué. Si le groupe
se trouve dans un formulaire natif, transmettez-lui `name`, comme dans l’exemple Formulaire.

<ExampleFrame variant="component">
  <RadioGroupBasic />
</ExampleFrame>

<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }]} className="docs-tabs">
    <Tab value="React">

```tsx file="../../en/docs/_snippets/radio-group/basic.tsx"
```

    </Tab>
  </Tabs>
</div>

## Installation avec shadcn

<ShadcnInstall packageName="radio-group" />

## Anatomie

```text
RadioGroup.Root
├─ RadioGroup.Label
├─ RadioGroup.Indicator
└─ RadioGroup.Item[value]
   ├─ RadioGroup.ItemControl
   ├─ RadioGroup.ItemText
   └─ <input> (automatic)

RadioGroup.RootProvider
└─ RadioGroup.* + useRadioGroup()

RadioGroup.Option
└─ RadioGroup.Item + ItemControl + ItemText + <input> (automatic)
```

| Partie                     | Rôle                                                        |
| ------------------------- | ----------------------------------------------------------- |
| `RadioGroup` / `.Root`    | Gère la valeur, la navigation au clavier, l’orientation et les formulaires. |
| `RadioGroup.RootProvider` | Relie les parties à l’état créé avec `useRadioGroup()`.    |
| `RadioGroup.Label`        | Libellé accessible du groupe.                              |
| `RadioGroup.Item`         | Une option sélectionnable ; rend un `label` par défaut.    |
| `RadioGroup.Option`       | Élément pratique avec contrôle, texte et champ masqué.     |
| `RadioGroup.ItemControl`  | Contrôle radio visuel ; accepte la prop moduix `size`.      |
| `RadioGroup.ItemText`     | Texte visible de l’option.                                 |
| `RadioGroup.Indicator`    | Indicateur facultatif de l’élément actif, mesuré par Ark.   |

## Composition

### Composition recommandée

`RadioGroup` est la forme courte de `RadioGroup.Root`. Utilisez `RadioGroup.Option` pour l’élément
avec libellé standard. Il accepte les props de l’élément Ark, la prop moduix `size` et `className`.
La classe est appliquée à l’`Item` sous-jacent, ce qui permet toujours de cibler les parties
imbriquées avec leurs attributs `data-slot`.

### Quand créer une composition personnalisée

Utilisez `RadioGroup.RootProvider` avec `useRadioGroup()` de `@moduix/react` lorsque l’état doit être
créé avant l’affichage du groupe, partagé avec des contrôles situés hors de l’arbre rendu ou lu par
une autre partie de l’interface. Passez l’état renvoyé à `RootProvider` et n’utilisez pas `Root` et
`RootProvider` pour la même instance d’état.

Utilisez l’arbre explicite d’`Item` lorsque les parties imbriquées ont besoin de leur propre
positionnement ou de leurs propres classes, ou lorsque `RadioGroup.Item asChild` doit laisser un autre
élément porter le nœud DOM de l’option. L’enfant direct d’un `Item` avec `asChild` doit rester un
`label` sémantique.

Pour lire l’état dans l’arbre, utilisez `RadioGroup.Context`, `RadioGroup.ItemContext`,
`useRadioGroupContext()` ou `useRadioGroupItemContext()` de `@moduix/react`.

### Propriétés par défaut

| Propriété                         | Par défaut | Notes                                                       |
| --------------------------------- | ---------- | ----------------------------------------------------------- |
| `RadioGroup` / `RadioGroup.Root`  | -          | Les valeurs Ark s’appliquent ; moduix n’ajoute pas de comportement par défaut au groupe. |
| `RadioGroup.Option size`          | `md`       | Diamètre du contrôle et taille du point indicateur.         |
| `RadioGroup.ItemControl size`     | `md`       | Diamètre du contrôle moduix et taille du point indicateur.  |

### Comportement

`RadioGroup.Root` accepte notamment les props Ark `value`, `defaultValue`,
`onValueChange(details)`, `name`, `form`, `orientation`, `disabled`, `invalid`, `readOnly`,
`required`, `ids` et `asChild`. Dans le callback, lisez la valeur sélectionnée dans `details.value`.

Chaque `RadioGroup.Item` affiche automatiquement son champ natif. Celui-ci participe à l’envoi, à la
validation et à la réinitialisation du formulaire ; vous n’avez donc pas besoin d’ajouter vous-même
une partie input. Définissez `name` sur le composant racine lorsque la valeur sélectionnée doit être
incluse dans `FormData`.

Ark gère l’état contrôlé ou non contrôlé, le comportement WAI-ARIA, le déplacement du focus et la
navigation au clavier. La touche <kbd>Tab</kbd> place le focus sur l’élément sélectionné ou sur le
premier élément si aucun choix n’est encore fait. <kbd>Espace</kbd> sélectionne l’élément qui a le
focus ; les touches fléchées déplacent le focus et sélectionnent l’option suivante ou précédente.

`Field.Root` fournit un libellé visible, un texte d’aide et un message d’erreur, mais ne transmet pas
son état à `RadioGroup`. Transmettez `disabled`, `invalid`, `readOnly` et `required` au groupe lui-même.
`Fieldset.Root` transmet aux groupes radio imbriqués ses états Ark `disabled` et `invalid`.

Chaque partie stylée transmet sa ref à la partie DOM Ark correspondante. Le champ natif automatique
reste interne et ne constitue pas une cible de ref publique distincte.

## Exemples

### asChild

Utilisez `RadioGroup.Item asChild` lorsqu’une carte ou une disposition personnalisée du libellé doit
porter le nœud DOM de l’option. Gardez un `label` sémantique comme enfant direct.

<ExampleFrame variant="component">
  <RadioGroupAsChild />
</ExampleFrame>

<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx file="../../en/docs/_snippets/radio-group/aschild.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/radio-group/radio-group-aschild.module.css"
```

    </Tab>
  </Tabs>
</div>

### État contrôlé

Utilisez `value` avec `onValueChange(details)` lorsque la valeur sélectionnée est gérée par l’état
React ou doit rester synchronisée avec d’autres parties de l’interface.

<ExampleFrame variant="component">
  <RadioGroupControlled />
</ExampleFrame>

<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx file="../../en/docs/_snippets/radio-group/controlled.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/radio-group/radio-group-controlled.module.css"
```

    </Tab>
  </Tabs>
</div>

### État désactivé

Définissez `disabled` sur la racine pour rendre toutes les options indisponibles tout en gardant la
valeur sélectionnée visible.

<ExampleFrame variant="component">
  <RadioGroupDisabled />
</ExampleFrame>

<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }]} className="docs-tabs">
    <Tab value="React">

```tsx file="../../en/docs/_snippets/radio-group/disabled.tsx"
```

    </Tab>
  </Tabs>
</div>

### Élément désactivé

Définissez `disabled` sur un `Item` ou une `Option` précis lorsque seule cette option est indisponible.

<ExampleFrame variant="component">
  <RadioGroupDisabledItem />
</ExampleFrame>

<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }]} className="docs-tabs">
    <Tab value="React">

```tsx file="../../en/docs/_snippets/radio-group/item-disabled.tsx"
```

    </Tab>
  </Tabs>
</div>

### Formulaire

Transmettez `name` à la racine lorsque la valeur sélectionnée doit être envoyée avec le formulaire.
Le champ natif créé automatiquement pour chaque élément participe à l’envoi et à la réinitialisation
du formulaire, sans qu’il soit nécessaire d’ajouter une partie input manuellement.

<ExampleFrame variant="component">
  <RadioGroupForm />
</ExampleFrame>

<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx file="../../en/docs/_snippets/radio-group/form.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/radio-group/radio-group-form.module.css"
```

    </Tab>
  </Tabs>
</div>

### Indicateur

Rendez `RadioGroup.Indicator` lorsque l’élément actif a besoin d’une mise en évidence supplémentaire
mesurée. Gardez l’indicateur et les boutons radio dans le même contexte de positionnement, et placez
le contenu sans rapport à l’extérieur, afin que l’indicateur suive la géométrie des éléments.

<ExampleFrame variant="component">
  <RadioGroupIndicator />
</ExampleFrame>

<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx file="../../en/docs/_snippets/radio-group/indicator.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/radio-group/radio-group-indicator.module.css"
```

    </Tab>
  </Tabs>
</div>

### Valeur initiale

Définissez `defaultValue` lorsque l’élément sélectionné au départ est connu et que le groupe doit
gérer lui-même les changements suivants.

<ExampleFrame variant="component">
  <RadioGroupInitialValue />
</ExampleFrame>

<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }]} className="docs-tabs">
    <Tab value="React">

```tsx file="../../en/docs/_snippets/radio-group/initial-value.tsx"
```

    </Tab>
  </Tabs>
</div>

### Orientation

Définissez `orientation="horizontal"` lorsque le comportement des touches fléchées doit suivre un
groupe horizontal. Disposez aussi les éléments sur une ligne avec un conteneur en ligne si la mise en
page doit être horizontale.

<ExampleFrame variant="component">
  <RadioGroupOrientation />
</ExampleFrame>

<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx file="../../en/docs/_snippets/radio-group/orientation.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/radio-group/radio-group-orientation.module.css"
```

    </Tab>
  </Tabs>
</div>

### Root Provider

Utilisez `RootProvider` lorsqu’un état créé par `useRadioGroup()` doit être partagé avec des contrôles situés
en dehors de l’arbre du groupe. L’exemple montre aussi comment modifier la valeur sélectionnée depuis
un contrôle externe.

<ExampleFrame variant="component">
  <RadioGroupRootProvider />
</ExampleFrame>

<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx file="../../en/docs/_snippets/radio-group/root-provider.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/radio-group/radio-group-root-provider.module.css"
```

    </Tab>
  </Tabs>
</div>

### Tailles

Utilisez la prop moduix `size` sur `ItemControl` ou `Option` lorsque le contrôle du bouton radio doit avoir un
diamètre différent. Les valeurs prises en charge sont `xs`, `sm`, `md`, `lg` et `xl`.

<ExampleFrame variant="component">
  <RadioGroupSizes />
</ExampleFrame>

<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }]} className="docs-tabs">
    <Tab value="React">

```tsx file="../../en/docs/_snippets/radio-group/sizes.tsx"
```

    </Tab>
  </Tabs>
</div>

### Avec Field

Utilisez `Field.Root` lorsqu’un groupe radio a besoin d’un libellé, d’un texte d’aide ou d’un message
d’erreur au niveau du champ. Transmettez aussi `invalid` à `RadioGroup`, car `Field.Root` ne définit
pas l’état de validation du groupe.

<ExampleFrame variant="component">
  <RadioGroupField />
</ExampleFrame>

<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx file="../../en/docs/_snippets/radio-group/with-field.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/radio-group/radio-group-with-field.module.css"
```

    </Tab>
  </Tabs>
</div>

### Avec Fieldset

Entourez le groupe d’un `Fieldset` lorsque plusieurs options partagent une légende et appartiennent à
la même section du formulaire. `Fieldset` peut aussi transmettre ses états `disabled` et `invalid` au
groupe imbriqué.

<ExampleFrame variant="component">
  <RadioGroupFieldset />
</ExampleFrame>

<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx file="../../en/docs/_snippets/radio-group/with-fieldset.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/radio-group/radio-group-with-fieldset.module.css"
```

    </Tab>
  </Tabs>
</div>

### Personnalisation avancée

N’utilisez pas `Option` lorsque les parties imbriquées ont besoin de props, d’un positionnement ou de
styles spécifiques. Composez l’arbre complet avec `Item`, `ItemControl` et `ItemText`.

<ExampleFrame variant="component">
  <RadioGroupAdvancedCustomization />
</ExampleFrame>

<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }]} className="docs-tabs">
    <Tab value="React">

```tsx file="../../en/docs/_snippets/radio-group/advanced-customization.tsx"
```

    </Tab>
  </Tabs>
</div>

## Styles

Personnalisez `RadioGroup` avec `className`, les attributs stables `data-slot="radio-group-*"`, les
attributs Ark `data-scope`, `data-part` et `data-state`, ainsi que les variables CSS publiques
`--moduix-radio-*` ci-dessous.

### Propriétés CSS

<div className="docs-reference">
  <Tabs
    values={[{ label: 'Variables CSS', value: 'CSS Variables' }]}
    className="docs-reference-tabs"
    contentItemClassName="docs-reference-panel"
  >
    <Tab>
      <RadioGroupDocs.RadioGroupCssPropertiesPanel />
    </Tab>
  </Tabs>
</div>

### Points d’accroche CSS

- Attributs d’anatomie Ark : `data-scope="radio-group"` et `data-part` sur root, label, item,
  item-control, item-text, item-hidden-input et indicator.
- Attributs d’état Ark : `data-state="checked|unchecked"`, `data-disabled`, `data-readonly`,
  `data-invalid`, `data-required`, `data-focus`, `data-focus-visible`, `data-hover`, `data-active`
  et `data-orientation`.
- Variables de l’indicateur Ark : `--transition-property`, `--left`, `--top`, `--width` et `--height`.
- Attributs moduix : `data-slot="radio-group-root"`, `radio-group-root-provider`, `radio-group-label`,
  `radio-group-item`, `radio-group-item-control`, `radio-group-item-text`, `radio-group-item-hidden-input` et
  `radio-group-indicator`.
