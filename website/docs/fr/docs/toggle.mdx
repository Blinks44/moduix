---
title: Toggle
component: toggle
description: Un bouton à deux états pour les actions compactes comme les favoris, la mise en forme et les barres d’outils.
---

import * as ToggleDocs from '@/components/examples/toggle/css-properties';
import ToggleAdvancedCustomization from '@/components/examples/toggle/advanced-customization.tsx';
import ToggleAsChild from '@/components/examples/toggle/aschild.tsx';
import ToggleBasic from '@/components/examples/toggle/basic.tsx';
import ToggleControlled from '@/components/examples/toggle/controlled.tsx';
import ToggleDisabled from '@/components/examples/toggle/disabled.tsx';
import ToggleIcons from '@/components/examples/toggle/icons.tsx';
import ToggleIndicator from '@/components/examples/toggle/indicator.tsx';
import ToggleSizes from '@/components/examples/toggle/sizes.tsx';
import ToggleVariants from '@/components/examples/toggle/variants.tsx';
import toggleSolidAdvancedCustomization from '../../../snippets/toggle/solid/advanced-customization.tsx?raw';
import toggleSolidAsChild from '../../../snippets/toggle/solid/aschild.tsx?raw';
import toggleSolidBasic from '../../../snippets/toggle/solid/basic.tsx?raw';
import toggleSolidControlled from '../../../snippets/toggle/solid/controlled.tsx?raw';
import toggleSolidDisabled from '../../../snippets/toggle/solid/disabled.tsx?raw';
import toggleSolidIcons from '../../../snippets/toggle/solid/icons.tsx?raw';
import toggleSolidIndicator from '../../../snippets/toggle/solid/indicator.tsx?raw';
import toggleSolidSizes from '../../../snippets/toggle/solid/sizes.tsx?raw';
import toggleSolidVariants from '../../../snippets/toggle/solid/variants.tsx?raw';

## Référence de l’API

`Toggle` enveloppe le composant primitif Toggle d’Ark UI et ajoute les propriétés visuelles moduix pour les variantes et les tailles.
Importez-le depuis `@moduix/react/toggle` ou `@moduix/solid/toggle`.

<PrimitiveReference href="https://ark-ui.com/docs/components/toggle" />

## Choisir le bon composant

Utilisez `Toggle` lorsque le contrôle doit se comporter comme un bouton et indiquer s’il est activé.

- Choisissez **`Toggle`** pour les favoris, l’enregistrement d’éléments, la mise en forme et les actions compactes d’une barre d’outils.
- Choisissez **`Switch`** pour les réglages qui s’activent ou se désactivent, présentés comme des préférences.
- Choisissez **`Checkbox`** pour les sélections envoyées avec une liste ou un formulaire.
- Choisissez **`ToggleGroup`** lorsque plusieurs toggles doivent partager un même modèle de sélection.

## Exemple de base


Utilisez `Toggle` dans le cas courant et conservez le comportement d’Ark lié à l’état activé.

<ExampleFrame variant="component">
  <ToggleBasic />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/toggle/basic.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={toggleSolidBasic} />

    </Tab>
  </Tabs>
</div>
</ExampleCode>

## Installation avec shadcn

Consultez le [Démarrage rapide](/docs/quick-start) pour la configuration complète du paquet, du registre et des styles.

<Tabs groupId="framework">
  <Tab label="React" value="react">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-react/toggle' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-react-tailwind/toggle' },
  ]}
/>

  </Tab>
  <Tab label="Solid" value="solid">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-solid/toggle' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-solid-tailwind/toggle' },
  ]}
/>

  </Tab>
</Tabs>

## Anatomie

```text
Toggle
└─ ToggleIndicator (facultatif)

ToggleContext (lecteur d’état, pas une partie du DOM)
```

| Partie | Rôle |
| ------ | --- |
| `Toggle` | Partie racine qui gère l’état activé, le focus, la désactivation et la composition `asChild`. |
| `ToggleIndicator` | Partie facultative qui affiche le contenu activé et son `fallback` lorsque le toggle est désactivé. |
| `ToggleContext` | Lit l’état du toggle le plus proche pour les descendants personnalisés ; il ne rend pas de partie du DOM. |

## Composition

Utilisez `defaultPressed` pour un toggle non contrôlé, ou passez `pressed` avec `onPressedChange` lorsque l’état
appartient à l’application. Utilisez `ToggleIndicator` lorsque
le contenu du bouton change selon son état. Pour les descendants personnalisés qui doivent lire cet état, utilisez
`ToggleContext` ou `useToggleContext()` depuis le package correspondant. Utilisez `asChild` lorsqu’un autre bouton
doit porter le comportement du toggle. Dans Solid, `asChild` reçoit une fonction de rendu ; dans React, il reçoit
un seul enfant compatible avec un bouton.

## Exemples

### asChild

Utilisez `asChild` lorsqu’un autre bouton doit porter le comportement du toggle. Gardez un seul enfant compatible
avec un bouton afin qu’Ark puisse lui transmettre les propriétés d’interaction et l’état d’accessibilité.

<ExampleFrame variant="component">
  <ToggleAsChild />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/toggle/aschild.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={toggleSolidAsChild} />

    </Tab>
  </Tabs>
</div>
</ExampleCode>

### État contrôlé

Contrôlez `pressed` lorsque le toggle fait partie de l’état de l’application, de l’analytique ou d’une autre interaction.

<ExampleFrame variant="component">
  <ToggleControlled />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/toggle/controlled.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={toggleSolidControlled} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/toggle/toggle-controlled.module.css"
```
</div>
</ExampleCode>

### État désactivé

Utilisez `disabled` pour bloquer les interactions tout en conservant l’état activé actuel.

<ExampleFrame variant="component">
  <ToggleDisabled />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/toggle/disabled.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={toggleSolidDisabled} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/toggle/toggle-disabled.module.css"
```
</div>
</ExampleCode>

### Icônes

Passez les icônes comme des enfants ordinaires. Les toggles avec une icône seule doivent avoir un nom accessible,
par exemple avec `aria-label`.

<ExampleFrame variant="component">
  <ToggleIcons />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/toggle/icons.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={toggleSolidIcons} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/toggle/toggle-icons.module.css"
```
</div>
</ExampleCode>

### Indicateur

Utilisez `ToggleIndicator` lorsque le contenu intégré doit différer selon que le toggle est activé ou non.

<ExampleFrame variant="component">
  <ToggleIndicator />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/toggle/indicator.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={toggleSolidIndicator} />

    </Tab>
  </Tabs>
</div>
</ExampleCode>

### Tailles

Utilisez les tailles de texte pour les toggles avec libellé et les tailles réservées aux boutons avec icône seule
lorsque l’espace est limité.

<ExampleFrame variant="component">
  <ToggleSizes />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/toggle/sizes.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={toggleSolidSizes} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/toggle/toggle-sizes.module.css"
```
</div>
</ExampleCode>

### Variantes

Définissez `variant` sur `default`, `outline` ou `ghost` pour l’adapter à la surface environnante.

<ExampleFrame variant="component">
  <ToggleVariants />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/toggle/variants.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={toggleSolidVariants} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/toggle/toggle-variants.module.css"
```
</div>
</ExampleCode>

### Personnalisation avancée

Utilisez le hook de contexte lorsqu’un descendant personnalisé doit connaître l’état activé, sans modifier le contenu
du bouton avec `ToggleIndicator`.

<ExampleFrame variant="component">
  <ToggleAdvancedCustomization />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/toggle/advanced-customization.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={toggleSolidAdvancedCustomization} />

    </Tab>
  </Tabs>
</div>
</ExampleCode>

## Styles

Utilisez `className` dans React ou `class` dans Solid sur les parties exportées pour les styles structurels. Les variantes
Tailwind utilisent les utilitaires natifs et les classes du consommateur. Les variantes CSS Modules exposent en plus les
tokens `--moduix-toggle-*` pour les variantes, les tailles, l’anneau de focus, l’espacement et la taille des icônes.

### Propriétés CSS

<div className="docs-reference">
  <Tabs
    values={[{ label: 'CSS Variables', value: 'CSS Variables' }]}
    className="docs-reference-tabs"
    contentItemClassName="docs-reference-panel"
  >
    <Tab>
      <ToggleDocs.ToggleCssPropertiesPanel />
    </Tab>
  </Tabs>
</div>

### Points d’accroche CSS

- Sélecteurs `data-slot` stables : `toggle-root` et `toggle-indicator`.
- Sélecteurs des parties Ark UI : `data-scope="toggle"` et `data-part="root" | "indicator"`.
- Sélecteurs d’état Ark UI : `data-state="on" | "off"`, `data-pressed` et `data-disabled`.
- Sélecteurs moduix : `data-variant` et `data-size`, notamment `icon-sm`, `icon-md` et `icon-lg`.
- Les icônes enfants directes et celles de `ToggleIndicator` utilisent
  `--moduix-toggle-icon-size` sur le chemin CSS Modules.
