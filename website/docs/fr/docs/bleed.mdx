---
title: Bleed
component: bleed
description: Composant de mise en page qui permet à certains contenus de dépasser les limites d’un conteneur.
---

import * as BleedDocs from '@/components/examples/bleed/css-properties';
import BleedBasic from '@/components/examples/bleed/basic.tsx';
import BleedBlockBleed from '@/components/examples/bleed/block-bleed.tsx';
import BleedCustomStyling from '@/components/examples/bleed/custom-styling.tsx';
import BleedInlineAmounts from '@/components/examples/bleed/inline-amounts.tsx';
import BleedSemanticElement from '@/components/examples/bleed/semantic-element.tsx';

import bleedSolidBasic from '../../../snippets/bleed/solid/basic.tsx?raw';
import bleedSolidBlockBleed from '../../../snippets/bleed/solid/block-bleed.tsx?raw';
import bleedSolidCustomStyling from '../../../snippets/bleed/solid/custom-styling.tsx?raw';
import bleedSolidInlineAmounts from '../../../snippets/bleed/solid/inline-amounts.tsx?raw';
import bleedSolidSemanticElement from '../../../snippets/bleed/solid/semantic-element.tsx?raw';

## Référence de l’API

Ark UI ne fournit pas de primitive `bleed` dédiée. Dans moduix, `Bleed` repose sur la
[fabrique Ark](https://ark-ui.com/docs/guides/composition#the-ark-factory) et suit les recommandations d’Ark pour la
[composition](https://ark-ui.com/docs/guides/composition), les [styles](https://ark-ui.com/docs/guides/styling) et les
[références](https://ark-ui.com/docs/guides/ref).

Utilisez [Container](/docs/container) pour conserver une colonne de contenu agréable à lire. Placez `Bleed` à
l’intérieur lorsque seul un bloc média, un arrière-plan, un séparateur ou une bannière doit dépasser les limites du
conteneur.


Les mêmes exports sont disponibles depuis `@moduix/solid/bleed` pour Solid.
## Exemple de base

La valeur par défaut `inline="full"` permet à un bloc média, une bannière ou un élément décoratif de section
d’atteindre les bords de la fenêtre tout en restant dans le flux normal du document.


<ExampleFrame variant="component">
  <BleedBasic />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/bleed/basic.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={bleedSolidBasic} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/bleed/bleed-basic.module.css"
```
</div>

</ExampleCode>

## Installation avec shadcn

Consultez le [Démarrage rapide](/docs/quick-start) pour la configuration complète du paquet, du registre et des styles.

<Tabs groupId="framework">
  <Tab label="React" value="react">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-react/bleed' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-react-tailwind/bleed' },
  ]}
/>

  </Tab>
  <Tab label="Solid" value="solid">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-solid/bleed' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-solid-tailwind/bleed' },
  ]}
/>

  </Tab>
</Tabs>

## Anatomie

`Bleed` rend un seul élément racine conforme aux conventions d’Ark. Il applique des marges négatives sur l’axe en
ligne et/ou l’axe de bloc, sans ajouter d’enveloppe interne.

```text
Bleed
└─ root[data-scope="bleed"][data-part="root"][data-slot="bleed-root"]
   └─ children
```

| Partie | Rôle                                                                                 |
| ------ | ------------------------------------------------------------------------------------ |
| `Bleed` | Composant racine qui permet au contenu de dépasser sur les axes en ligne et de bloc. |

## Composition

`Bleed` est un composant de présentation sans état. Il ne possède qu’une partie racine ; il n’y a donc aucune partie enfant à coordonner. Les valeurs par défaut sont `inline="full"`, `block="none"` et `asChild={false}`. Lorsque `asChild` doit confier la racine rendue à un élément sémantique, fournissez exactement un enfant capable de recevoir les propriétés DOM et une ref en React, ou utilisez la fonction de rendu Solid. Les refs React ciblent la racine ou l’enfant rendu ; la forme Solid ne transmet pas la ref du composant à l’hôte personnalisé.

Combinez `inline` et `block` lorsqu’une surface doit dépasser les marges internes sur les deux axes logiques. `inline="full"` est relatif à la fenêtre d’affichage ; dans une enveloppe imbriquée, il peut donc être nécessaire de redéfinir localement `--moduix-bleed-inline-full` et `--moduix-bleed-inline-full-size`.
## Exemples

### Débordement sur l’axe de bloc

Ajoutez `block` lorsque la surface doit dépasser l’espacement intérieur du conteneur également sur l’axe de bloc.

<ExampleFrame variant="component">
  <BleedBlockBleed />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/bleed/block-bleed.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={bleedSolidBlockBleed} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/bleed/bleed-block-bleed.module.css"
```
</div>

</ExampleCode>

### Styles personnalisés

Surchargez localement `--moduix-bleed-inline-full` et `--moduix-bleed-inline-full-size` lorsqu’une enveloppe imbriquée
doit s’adapter à sa propre zone de contenu plutôt qu’à la fenêtre.

<ExampleFrame variant="component">
  <BleedCustomStyling />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/bleed/custom-styling.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={bleedSolidCustomStyling} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/bleed/bleed-custom-styling.module.css"
```
</div>

</ExampleCode>

### Débordement sur l’axe en ligne

Utilisez `inline` pour contrôler le dépassement du contenu sur l’axe en ligne. L’exemple compare les valeurs fixes
`sm` et `lg` avec `full`, qui s’étend jusqu’à la fenêtre.

<ExampleFrame variant="component">
  <BleedInlineAmounts />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/bleed/inline-amounts.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={bleedSolidInlineAmounts} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/bleed/bleed-inline-amounts.module.css"
```
</div>

</ExampleCode>

### Élément sémantique

Utilisez `asChild` lorsque le contenu doit utiliser un élément sémantique plus précis. Dans cet exemple, l’élément
`figure` devient la racine de `Bleed`.

<ExampleFrame variant="component">
  <BleedSemanticElement />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/bleed/semantic-element.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={bleedSolidSemanticElement} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/bleed/bleed-semantic-element.module.css"
```
</div>

</ExampleCode>

## Styles

`Bleed` expose des variables CSS et des attributs de données stables sur son élément racine. Utilisez les variables pour
ajuster son extension et `className` pour styliser la surface elle-même.

### Propriétés CSS

<CssPropertiesSection properties={BleedDocs.bleedCssProperties} />

### Points d’accroche CSS

- `data-scope="bleed"` et `data-part="root"` fournissent des points d’accroche conformes à Ark sur l’élément racine exporté.
- `data-slot="bleed-root"` est un sélecteur moduix stable sur l’élément racine exporté.
- `data-inline` indique le mode de dépassement actuel sur l’axe en ligne : `none`, `xs`, `sm`, `md`, `lg`, `xl` ou `full`.
- `data-block` indique le mode de dépassement actuel sur l’axe de bloc : `none`, `xs`, `sm`, `md`, `lg` ou `xl`.
- L’API CSS publique `--moduix-bleed-*` couvre les valeurs de dépassement fondées sur l’échelle ainsi que la largeur
  calculée jusqu’aux bords de la fenêtre pour `inline="full"`.
