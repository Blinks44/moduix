---
title: Textarea
component: textarea
description: Zone de texte native multiligne, compatible avec Field et stylée par moduix.
---

import * as TextareaDocs from '@/components/examples/textarea/css-properties';
import TextareaAutoResize from '@/components/examples/textarea/auto-resize.tsx';
import TextareaBasic from '@/components/examples/textarea/basic.tsx';
import TextareaControlled from '@/components/examples/textarea/controlled.tsx';
import TextareaDisabledAndReadonly from '@/components/examples/textarea/disabled-and-readonly.tsx';
import TextareaFieldValidation from '@/components/examples/textarea/field-validation.tsx';
import TextareaNativeAttributes from '@/components/examples/textarea/native-attributes.tsx';
import textareaSolidAutoResize from '../../../snippets/textarea/solid/auto-resize.tsx?raw';
import textareaSolidBasic from '../../../snippets/textarea/solid/basic.tsx?raw';
import textareaSolidControlled from '../../../snippets/textarea/solid/controlled.tsx?raw';
import textareaSolidDisabledAndReadonly from '../../../snippets/textarea/solid/disabled-and-readonly.tsx?raw';
import textareaSolidFieldValidation from '../../../snippets/textarea/solid/field-validation.tsx?raw';
import textareaSolidNativeAttributes from '../../../snippets/textarea/solid/native-attributes.tsx?raw';

## Référence de l’API

Ark UI ne fournit pas de primitive `Textarea` autonome. Dans moduix, le composant s’appuie sur
[`FieldTextarea` d’Ark UI](https://ark-ui.com/docs/components/field) et conserve l’API native de
`textarea`. Il prend en charge `autoresize` et `asChild`, transfère la ref vers l’élément natif et
hérite de l’état de `Field`.

## Choisir le bon composant

Utilisez `Textarea` pour du texte libre qui peut s’étendre sur plusieurs lignes et doit conserver le
comportement natif d’une zone de texte dans le navigateur.

- Choisissez **`Input`** pour les valeurs courtes sur une ligne : noms, URL ou filtres compacts.
- Choisissez **`Textarea`** pour les notes, commentaires, descriptions et autres contenus longs saisis librement.
- Choisissez **`Editable`** pour un texte d’abord affiché, puis modifiable à la demande.
- Choisissez **`Field` + `Textarea`** lorsque le champ doit exposer de façon déclarative un libellé, une
  description, son caractère obligatoire ou sa validation.

## Utilisation de base

Placez `Textarea` dans `Field` lorsqu’il faut fournir au champ un libellé accessible ou un texte
complémentaire. C’est la configuration recommandée pour les commentaires, notes, descriptions de
tâches et contenus similaires.


<ExampleFrame variant="component">
  <TextareaBasic />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/textarea/basic.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={textareaSolidBasic} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/textarea/textarea-basic.module.css"
```
</div>

</ExampleCode>

## Installation avec shadcn

Consultez le [Démarrage rapide](/docs/quick-start) pour la configuration complète du paquet, du registre et des styles.

<Tabs groupId="framework">
  <Tab label="React" value="react">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-react/textarea' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-react-tailwind/textarea' },
  ]}
/>

  </Tab>
  <Tab label="Solid" value="solid">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-solid/textarea' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-solid-tailwind/textarea' },
  ]}
/>

  </Tab>
</Tabs>

## Anatomie

La structure publique se limite à une seule partie :

```text
Field (optional wrapper)
└─ Textarea
```

| Partie    | Rôle                                                              |
| --------- | ----------------------------------------------------------------- |
| `Textarea` | Élément `textarea` natif avec les styles moduix et l’état de `Field`. |

`Textarea` n’exporte ni conteneur supplémentaire, ni sous-composant, ni provider ou hook de contexte,
ni champ masqué, ni libellé ou compteur de caractères.

## Composition

Utilisez `defaultValue` pour une zone de texte non contrôlée. Lorsque l’application gère la valeur,
utilisez `value` avec le gestionnaire natif `onChange` et le mécanisme d’état du framework utilisé.

Placez `Textarea` dans `Field`, avec `FieldLabel`, `FieldHelperText` et `FieldErrorText`, lorsque
le contrôle doit fournir un libellé, un texte complémentaire ou une validation. En dehors de `Field`,
donnez-lui un nom accessible avec un `<label>` natif ou `aria-label`.

Activez `autoresize` seulement lorsque le contrôle doit grandir avec son contenu. Dans ce mode, Ark
désactive le redimensionnement manuel : `--moduix-textarea-resize` ne s’applique donc que lorsque
`autoresize` vaut `false`. Avec `asChild`, fournissez un seul élément textarea sémantique capable de
recevoir les propriétés fusionnées et la ref.

## Exemples

### Redimensionnement automatique

Définissez `autoresize` lorsque la zone de texte doit grandir au fil de la saisie. Ark désactive le
redimensionnement manuel lorsque cette option est active.

<ExampleFrame variant="component">
  <TextareaAutoResize />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/textarea/auto-resize.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={textareaSolidAutoResize} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/textarea/textarea-auto-resize.module.css"
```
</div>

</ExampleCode>

### Valeur contrôlée

Utilisez `value` et le gestionnaire natif `onChange` lorsque l’état de l’application doit piloter le contenu.

<ExampleFrame variant="component">
  <TextareaControlled />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/textarea/controlled.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={textareaSolidControlled} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/textarea/textarea-controlled.module.css"
```
</div>

</ExampleCode>

### Désactivé et en lecture seule

Utilisez `disabled` lorsque l’utilisateur ne doit pas interagir avec le contrôle. Utilisez `readOnly`
lorsqu’il peut lire et sélectionner la valeur, mais pas la modifier.

<ExampleFrame variant="component">
  <TextareaDisabledAndReadonly />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/textarea/disabled-and-readonly.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={textareaSolidDisabledAndReadonly} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/textarea/textarea-disabled-and-readonly.module.css"
```
</div>

</ExampleCode>

### Validation du champ

Définissez `invalid` sur `Field`. Ark ajoute `aria-invalid` et `data-invalid` à la zone de texte et
la relie à son message d’erreur.

<ExampleFrame variant="component">
  <TextareaFieldValidation />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/textarea/field-validation.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={textareaSolidFieldValidation} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/textarea/textarea-field-validation.module.css"
```
</div>

</ExampleCode>

### Attributs natifs et soumission du formulaire

Utilisez les propriétés natives de `textarea`, comme `rows`, `maxLength` et `spellCheck`, pour le
comportement du navigateur. Cet exemple soumet également le formulaire et lit la valeur actuelle via
`FormData`.

<ExampleFrame variant="component">
  <TextareaNativeAttributes />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/textarea/native-attributes.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={textareaSolidNativeAttributes} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/textarea/textarea-native-attributes.module.css"
```
</div>

</ExampleCode>

## Styles

Dans React, personnalisez le contrôle avec `className` ; dans Solid, utilisez `class`, avec
`data-slot="textarea-root"`. Les variantes Tailwind utilisent les utilitaires natifs et les classes
du consommateur. Les variables publiques `--moduix-textarea-*` ci-dessous concernent les variantes CSS Modules.

### Propriétés CSS

<div className="docs-reference">
<Tabs
    values={[{ label: 'Variables CSS', value: 'CSS Variables' }]}
    className="docs-reference-tabs"
    contentItemClassName="docs-reference-panel"
> <Tab> <TextareaDocs.TextareaCssPropertiesPanel /> </Tab> </Tabs>
</div>

### Points d’accroche CSS

Utilisez la racine `Textarea` pour l’apparence du contrôle et le `Field` environnant pour la mise en
page et le texte complémentaire.

- La racine `Textarea` pour la bordure, l’arrière-plan, le placeholder, le comportement de redimensionnement et l’anneau de focus.
- Le conteneur `Field` pour la largeur, la largeur maximale, l’espacement, la disposition du libellé, la description et le message de validation.
- `data-slot="textarea-root"` pour un ciblage stable dans les styles à portée limitée.
- `data-autoresize` lorsque `autoresize` est activé.
- `data-invalid`, `[aria-invalid='true']`, `[disabled]`, `data-disabled`, `:read-only` et `data-readonly` pour les styles tenant compte de l’état.
