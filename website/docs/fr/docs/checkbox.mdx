---
title: Checkbox
component: checkbox
description: Un composant de case à cocher pour les options individuelles et les groupes à sélection multiple.
---

import * as CheckboxDocs from '@/components/examples/checkbox/checkbox';
import CheckboxBasic from '../../en/docs/_snippets/checkbox/basic.tsx';
import CheckboxControlled from '../../en/docs/_snippets/checkbox/controlled.tsx';
import CheckboxControlledGroup from '../../en/docs/_snippets/checkbox/controlled-group.tsx';
import CheckboxCustomIndicator from '../../en/docs/_snippets/checkbox/custom-indicator.tsx';
import CheckboxDefaultChecked from '../../en/docs/_snippets/checkbox/default-checked.tsx';
import CheckboxDisabled from '../../en/docs/_snippets/checkbox/disabled.tsx';
import CheckboxGroup from '../../en/docs/_snippets/checkbox/group.tsx';
import CheckboxGroupWithFieldset from '../../en/docs/_snippets/checkbox/group-with-fieldset.tsx';
import CheckboxGroupWithForm from '../../en/docs/_snippets/checkbox/group-with-form.tsx';
import CheckboxIndeterminate from '../../en/docs/_snippets/checkbox/indeterminate.tsx';
import CheckboxInvalidGroup from '../../en/docs/_snippets/checkbox/invalid-group.tsx';
import CheckboxMaxSelected from '../../en/docs/_snippets/checkbox/max-selected.tsx';
import CheckboxReadOnly from '../../en/docs/_snippets/checkbox/read-only.tsx';
import CheckboxRootProvider from '../../en/docs/_snippets/checkbox/root-provider.tsx';
import CheckboxSelectAll from '../../en/docs/_snippets/checkbox/select-all.tsx';
import CheckboxSizes from '../../en/docs/_snippets/checkbox/sizes.tsx';
import CheckboxWithField from '../../en/docs/_snippets/checkbox/with-field.tsx';
import CheckboxWithForm from '../../en/docs/_snippets/checkbox/with-form.tsx';

Utilisez `Checkbox` lorsque l’utilisateur peut sélectionner une ou plusieurs options indépendantes
dans un formulaire, un panneau de réglages ou une liste de filtres. Utilisez `RadioGroup` lorsqu’un
seul choix est possible, et `Switch` lorsqu’un réglage doit s’appliquer immédiatement.

## Référence de l’API

<PrimitiveReference href="https://ark-ui.com/docs/components/checkbox" />

## Exemple de base

Composez une case à cocher avec les parties Ark. Dans la plupart des cas, laissez `Checkbox.Control`
vide pour utiliser les icônes intégrées de moduix pour les états coché et indéterminé. Ajoutez
`Checkbox.Label` pour afficher le libellé. moduix ajoute automatiquement le champ natif du formulaire.

<ExampleFrame variant="component">
  <CheckboxBasic />
</ExampleFrame>

<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }]} className="docs-tabs">
    <Tab value="React">

```tsx file="../../en/docs/_snippets/checkbox/basic.tsx"
```
    </Tab>
  </Tabs>
</div>

## Installation avec shadcn

<ShadcnInstall packageName="checkbox" />

## Anatomie

```text
Checkbox.Root
├─ Checkbox.Control
│  ├─ Checkbox.Indicator
│  └─ Checkbox.Indicator[indeterminate] (optional)
├─ Checkbox.Label
└─ native input (automatic)

Checkbox.RootProvider
└─ same part tree connected to useCheckbox()

Checkbox.Group
└─ Checkbox.Root[value]
   ├─ Checkbox.Control
   ├─ Checkbox.Label
   └─ the native form input
```

| Partie                        | Rôle                                                                                                      |
| ----------------------------- | --------------------------------------------------------------------------------------------------------- |
| `Checkbox` / `Checkbox.Root`  | Gère l’état d’une case à cocher et rend le `label` qui l’enveloppe.                                      |
| `Checkbox.RootProvider`       | Relie les parties à un état Ark créé en dehors de l’arbre rendu.                                          |
| `Checkbox.Control`            | Partie visible de la case à cocher. Vide, elle affiche les icônes d’état intégrées.                      |
| `Checkbox.Indicator`          | Emplacement facultatif pour une icône personnalisée. Passez `indeterminate` pour cet état.               |
| `Checkbox.Label`              | Libellé visible de la case à cocher.                                                                      |
| `Checkbox.Group`              | Gère les valeurs sélectionnées de plusieurs cases à cocher.                                              |

## Composition

### Composition recommandée

Conservez l’arborescence des parties Ark dans votre balisage. `Checkbox.Root` accepte les propriétés
usuelles du composant racine Ark, notamment `checked`, `defaultChecked`,
`onCheckedChange(details)`, `disabled`, `readOnly`, `invalid`, `required`, `name`, `value`, `form`,
`ids` et `asChild`. Les propriétés `checked` et `defaultChecked` acceptent `true`, `false` ou
`"indeterminate"`.

`Checkbox.Group` accepte `value`, `defaultValue`, `onValueChange(value)`, `disabled`, `readOnly`,
`invalid`, `maxSelectedValues` et `name`.

`Checkbox.Control` est le raccourci pratique proposé par moduix. Laissez-le vide pour utiliser les
icônes intégrées, ou transmettez des enfants pour fournir un indicateur personnalisé. `size` est la
seule propriété supplémentaire de moduix sur `Checkbox.Root` et `Checkbox.RootProvider`.

### Quand créer une composition personnalisée

Utilisez `Checkbox.RootProvider` lorsque l’état de la case à cocher doit être géré en dehors de
l’arbre rendu. moduix réexporte `useCheckbox()` depuis `@moduix/react/checkbox` pour ce scénario.
Passez l’état renvoyé à `Checkbox.RootProvider` via sa propriété `value`.
`Checkbox.Context`, `useCheckboxContext()`, `useCheckboxGroup()` et `useCheckboxGroupContext()`
exposent l’état Ark pour des compositions plus avancées. Pour gérer l’état d’un groupe en dehors de
l’arbre rendu, associez `useCheckboxGroup()` à `Checkbox.GroupProvider` d’Ark depuis
`@ark-ui/react/checkbox`. Rendez
`Checkbox.Indicator` explicitement uniquement si vous avez besoin d’une icône personnalisée ou
d’une disposition différente.

### Propriétés par défaut

| Élément                       | Valeur par défaut | Remarques                                                        |
| ----------------------------- | ----------------- | ---------------------------------------------------------------- |
| `Checkbox` / `Checkbox.Root` | -                 | Aucun comportement supplémentaire, les valeurs Ark s’appliquent. |
| `size`                       | `md`              | S’applique à `Checkbox.Root` et `Checkbox.RootProvider`.         |

### Comportement

`Checkbox.Root` et `Checkbox.RootProvider` rendent toujours un champ natif. La case à cocher peut
ainsi participer à la validation native, à la réinitialisation et à l’envoi du formulaire. Pour
inclure une case seule dans les données envoyées, transmettez `name` et `value` à `Checkbox.Root`.
Pour un groupe, transmettez `name` à `Checkbox.Group` et `value` à chaque `Checkbox.Root`.

Lors d’une migration depuis shadcn, `Checkbox.Control` inclut déjà l’icône par défaut.
`Checkbox.Control` et `Checkbox.Label` restent explicites dans l’arborescence, tandis que
le champ masqué reste interne au composant.

`Checkbox.Root` et `Checkbox.RootProvider` d’Ark rendent un `label` par défaut. Avec `asChild`,
l’enfant direct doit aussi être un `label` sémantique ; sinon, la case à cocher n’a plus de libellé de
formulaire valide.

Lorsque `readOnly` est défini, moduix reflète cet état avec `aria-readonly` sur le champ natif créé
automatiquement. Ark empêche toujours les interactions.

Une `ref` transmise à une partie pointe vers le nœud DOM Ark correspondant. Pour basculer une case à
cocher qui a le focus, appuyez sur <kbd>Espace</kbd>.

## Exemples

### État contrôlé

Utilisez une case à cocher contrôlée lorsque sa valeur est gérée par une autre partie de l’interface.

<ExampleFrame variant="component">
  <CheckboxControlled />
</ExampleFrame>

<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx file="../../en/docs/_snippets/checkbox/controlled.tsx"
```
    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/checkbox/checkbox-controlled.module.css"
```
    </Tab>
  </Tabs>
</div>

### Groupe contrôlé

Utilisez un groupe contrôlé lorsque les options sélectionnées influencent des filtres, des
récapitulatifs ou des actions.

<ExampleFrame variant="component">
  <CheckboxControlledGroup />
</ExampleFrame>

<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }]} className="docs-tabs">
    <Tab value="React">

```tsx file="../../en/docs/_snippets/checkbox/controlled-group.tsx"
```
    </Tab>
  </Tabs>
</div>

### Indicateur personnalisé

Remplacez la coche intégrée par votre propre icône tout en conservant le comportement et
l’accessibilité d’Ark.

<ExampleFrame variant="component">
  <CheckboxCustomIndicator />
</ExampleFrame>

<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }]} className="docs-tabs">
    <Tab value="React">

```tsx file="../../en/docs/_snippets/checkbox/custom-indicator.tsx"
```
    </Tab>
  </Tabs>
</div>

### Cochée par défaut

Utilisez `defaultChecked` lorsque la case à cocher doit avoir une valeur initiale, puis laissez-la
gérer elle-même son état.

<ExampleFrame variant="component">
  <CheckboxDefaultChecked />
</ExampleFrame>

<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }]} className="docs-tabs">
    <Tab value="React">

```tsx file="../../en/docs/_snippets/checkbox/default-checked.tsx"
```
    </Tab>
  </Tabs>
</div>

### État désactivé

Utilisez `disabled` pour une option indisponible avec laquelle l’utilisateur ne peut pas interagir.

<ExampleFrame variant="component">
  <CheckboxDisabled />
</ExampleFrame>

<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }]} className="docs-tabs">
    <Tab value="React">

```tsx file="../../en/docs/_snippets/checkbox/disabled.tsx"
```
    </Tab>
  </Tabs>
</div>

### Groupe

Utilisez `Checkbox.Group` lorsque plusieurs cases à cocher partagent un tableau de valeurs
sélectionnées.

<ExampleFrame variant="component">
  <CheckboxGroup />
</ExampleFrame>

<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }]} className="docs-tabs">
    <Tab value="React">

```tsx file="../../en/docs/_snippets/checkbox/group.tsx"
```
    </Tab>
  </Tabs>
</div>

### Groupe avec Fieldset

Utilisez `Fieldset` lorsque le groupe représente un seul champ de formulaire et a besoin d’une
légende commune.

<ExampleFrame variant="component">
  <CheckboxGroupWithFieldset />
</ExampleFrame>

<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }]} className="docs-tabs">
    <Tab value="React">

```tsx file="../../en/docs/_snippets/checkbox/group-with-fieldset.tsx"
```
    </Tab>
  </Tabs>
</div>

### Groupe dans un formulaire

Placez `Checkbox.Group` dans un formulaire lorsque vous devez récupérer toutes les valeurs
sélectionnées dans `FormData`. Lisez-les avec `FormData.getAll(name)`.

<ExampleFrame variant="component">
  <CheckboxGroupWithForm />
</ExampleFrame>

<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx file="../../en/docs/_snippets/checkbox/group-with-form.tsx"
```
    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/checkbox/checkbox-group-with-form.module.css"
```
    </Tab>
  </Tabs>
</div>

### État indéterminé

Utilisez `checked="indeterminate"` lorsqu’une case à cocher représente une sélection partielle. C’est
fréquent pour une commande « Tout sélectionner ».

<ExampleFrame variant="component">
  <CheckboxIndeterminate />
</ExampleFrame>

<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }]} className="docs-tabs">
    <Tab value="React">

```tsx file="../../en/docs/_snippets/checkbox/indeterminate.tsx"
```
    </Tab>
  </Tabs>
</div>

### Groupe invalide

Définissez `invalid` sur le groupe lorsque la validation porte sur l’ensemble de la sélection.

<ExampleFrame variant="component">
  <CheckboxInvalidGroup />
</ExampleFrame>

<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }]} className="docs-tabs">
    <Tab value="React">

```tsx file="../../en/docs/_snippets/checkbox/invalid-group.tsx"
```
    </Tab>
  </Tabs>
</div>

### Nombre maximal de sélections

Utilisez `maxSelectedValues` pour limiter le nombre d’options sélectionnées simultanément.

<ExampleFrame variant="component">
  <CheckboxMaxSelected />
</ExampleFrame>

<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }]} className="docs-tabs">
    <Tab value="React">

```tsx file="../../en/docs/_snippets/checkbox/max-selected.tsx"
```
    </Tab>
  </Tabs>
</div>

### Lecture seule

Utilisez `readOnly` pour afficher une valeur sans permettre à l’utilisateur de la modifier.

<ExampleFrame variant="component">
  <CheckboxReadOnly />
</ExampleFrame>

<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }]} className="docs-tabs">
    <Tab value="React">

```tsx file="../../en/docs/_snippets/checkbox/read-only.tsx"
```
    </Tab>
  </Tabs>
</div>

### Root Provider

Utilisez `Checkbox.RootProvider` lorsque l’état de la case à cocher doit être géré en dehors de
l’arbre rendu. Importez `useCheckbox` depuis `@moduix/react/checkbox`.

<ExampleFrame variant="component">
  <CheckboxRootProvider />
</ExampleFrame>

<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx file="../../en/docs/_snippets/checkbox/root-provider.tsx"
```
    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/checkbox/checkbox-root-provider.module.css"
```
    </Tab>
  </Tabs>
</div>

### Tout sélectionner

Associez une case à cocher parente à un groupe contrôlé pour permettre à un seul élément de
sélectionner ou de désélectionner toutes les options.

<ExampleFrame variant="component">
  <CheckboxSelectAll />
</ExampleFrame>

<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx file="../../en/docs/_snippets/checkbox/select-all.tsx"
```
    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/checkbox/checkbox-select-all.module.css"
```
    </Tab>
  </Tabs>
</div>

### Tailles

Utilisez la propriété `size` pour adapter la taille de la case à cocher au formulaire ou à l’écran de
réglages.

<ExampleFrame variant="component">
  <CheckboxSizes />
</ExampleFrame>

<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }]} className="docs-tabs">
    <Tab value="React">

```tsx file="../../en/docs/_snippets/checkbox/sizes.tsx"
```
    </Tab>
  </Tabs>
</div>

### Avec Field

Utilisez `Field` lorsqu’une case à cocher a besoin d’un texte d’aide, de messages de validation ou
d’un état de champ partagé.

<ExampleFrame variant="component">
  <CheckboxWithField />
</ExampleFrame>

<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }]} className="docs-tabs">
    <Tab value="React">

```tsx file="../../en/docs/_snippets/checkbox/with-field.tsx"
```
    </Tab>
  </Tabs>
</div>

### Dans un formulaire

Transmettez `name` et `value` à `Checkbox.Root` pour inclure son champ natif créé automatiquement
dans `FormData`.

<ExampleFrame variant="component">
  <CheckboxWithForm />
</ExampleFrame>

<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx file="../../en/docs/_snippets/checkbox/with-form.tsx"
```
    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/checkbox/checkbox-with-form.module.css"
```
    </Tab>
  </Tabs>
</div>

## Styles

### Propriétés CSS

<CssPropertiesSection properties={CheckboxDocs.checkboxOverrideCssProperties} />

### Points d’accroche CSS

- `Checkbox.Root` expose `data-slot="checkbox-root"`, `data-size` et les attributs d’état Ark tels que `data-state`, `data-active`, `data-disabled`, `data-readonly`, `data-invalid`, `data-required`, `data-hover`, `data-focus` et `data-focus-visible`.
- `Checkbox.RootProvider` expose `data-slot="checkbox-root-provider"` et le même contrat de style que le composant racine.
- `Checkbox.Control` expose `data-slot="checkbox-control"`, `data-state="checked" | "indeterminate" | "unchecked"` et `data-active`.
- `Checkbox.Indicator` expose `data-slot="checkbox-indicator"` et hérite des attributs d’état de la case à cocher d’Ark.
- `Checkbox.Label` expose `data-slot="checkbox-label"` et les attributs d’état de la case à cocher d’Ark.
- `Checkbox.Group` expose `data-slot="checkbox-group"`.
