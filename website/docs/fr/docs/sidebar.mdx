---
title: Sidebar
component: sidebar
description: Navigation d’application redimensionnable et repliable, basée sur le Splitter d’Ark UI.
---

import { SidebarCssPropertiesPanel } from '@/components/examples/sidebar/css-properties';
import SidebarBasic from '@/components/examples/sidebar/basic.tsx';
import SidebarCustomScrollArea from '@/components/examples/sidebar/custom-scroll-area.tsx';
import SidebarCustomSizes from '@/components/examples/sidebar/custom-sizes.tsx';
import SidebarNestedNavigation from '@/components/examples/sidebar/nested-navigation.tsx';
import SidebarNestedNavigationAndAccountMenu from '@/components/examples/sidebar/nested-navigation-and-account-menu.tsx';
import SidebarPersistedLayout from '@/components/examples/sidebar/persisted-layout.tsx';
import SidebarRightSidebar from '@/components/examples/sidebar/right-sidebar.tsx';
import SidebarWorkspaceSelect from '@/components/examples/sidebar/workspace-select.tsx';
import sidebarSolidBasic from '../../../snippets/sidebar/solid/basic.tsx?raw';
import sidebarSolidCustomScrollArea from '../../../snippets/sidebar/solid/custom-scroll-area.tsx?raw';
import sidebarSolidCustomSizes from '../../../snippets/sidebar/solid/custom-sizes.tsx?raw';
import sidebarSolidNestedNavigation from '../../../snippets/sidebar/solid/nested-navigation.tsx?raw';
import sidebarSolidNestedNavigationAndAccountMenu from '../../../snippets/sidebar/solid/nested-navigation-and-account-menu.tsx?raw';
import sidebarSolidPersistedLayout from '../../../snippets/sidebar/solid/persisted-layout.tsx?raw';
import sidebarSolidRightSidebar from '../../../snippets/sidebar/solid/right-sidebar.tsx?raw';
import sidebarSolidWorkspaceSelect from '../../../snippets/sidebar/solid/workspace-select.tsx?raw';

## Référence de l’API

`Sidebar` est la mise en page moduix dédiée à la navigation d’une application. Elle associe des
parties de navigation au composant [Ark UI Splitter](https://ark-ui.com/docs/components/splitter),
qui fournit le redimensionnement, l’état replié, les interactions au clavier et l’accessibilité.
L’état et les callbacks exposés suivent le contrat de `Splitter`.

<PrimitiveReference href="https://ark-ui.com/docs/components/splitter" />

## Choisir le bon composant

- Choisissez **`Sidebar`** pour une navigation qui doit être redimensionnable, se replier en barre
  d’icônes et rester à côté du contenu principal.
- Choisissez **`Splitter`** lorsque vous avez besoin de panneaux redimensionnables génériques, plutôt
  que d’un panneau de navigation et d’une zone de contenu principale.

## Exemple de base

`Sidebar` est disponible pour React et Solid dans les packages CSS Modules et Tailwind. L’aperçu
interactif utilise React et CSS Modules, tandis que les onglets affichent le code natif pour la même
composition.

Composez le panneau de navigation, la poignée de redimensionnement et la zone de contenu principale
comme enfants directs de la racine. Les identifiants et contraintes par défaut conviennent à la mise
en page classique avec une barre latérale à gauche. Enveloppez les boutons de navigation réduits à
une icône dans `Sidebar.Tooltip` pour conserver leurs libellés après le repli.

<ExampleFrame variant="component">
  <SidebarBasic />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/sidebar/basic.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={sidebarSolidBasic} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/sidebar/sidebar-basic.module.css"
```
</div>

</ExampleCode>

## Installation avec shadcn

<Tabs groupId="framework">
  <Tab label="React" value="react">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-react/sidebar' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-react-tailwind/sidebar' },
  ]}
/>

  </Tab>
  <Tab label="Solid" value="solid">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-solid/sidebar' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-solid-tailwind/sidebar' },
  ]}
/>

  </Tab>
</Tabs>

Consultez [Quick start](/docs/quick-start) pour configurer le registry et choisir l’installation du
package.

## Anatomie

```text
Sidebar / Sidebar.Root
├─ Sidebar.Panel[id="sidebar"]
│  ├─ Sidebar.Header
│  │  ├─ Sidebar.Label
│  │  └─ Sidebar.Input
│  ├─ Sidebar.Content
│  │  └─ Sidebar.Group
│  │     ├─ Sidebar.GroupLabel
│  │     ├─ Sidebar.GroupAction
│  │     └─ Sidebar.NavigationList
│  │        └─ Sidebar.NavigationItem
│  │           ├─ Sidebar.ExpandedContent
│  │           ├─ Sidebar.CollapsedContent
│  │           ├─ Sidebar.NavigationAction
│  │           ├─ Sidebar.NavigationBadge
│  │           └─ Sidebar.NavigationSubList
│  │              └─ Sidebar.NavigationSubItem
│  │                 └─ Sidebar.NavigationSubButton
│  └─ Sidebar.Footer
│     └─ Sidebar.Separator
├─ Sidebar.ResizeTrigger
├─ Sidebar.Trigger
└─ Sidebar.Inset[id="content"]
```

| Partie | Rôle |
| --- | --- |
| `Sidebar` / `Sidebar.Root` | Racine Ark Splitter avec les réglages d’une barre à deux panneaux. |
| `Sidebar.Panel` | Panneau de navigation repliable, avec l’identifiant `"sidebar"` par défaut. |
| `Sidebar.Inset` | Panneau de contenu principal, avec l’identifiant `"content"` par défaut. |
| `Sidebar.ResizeTrigger` | Ligne accessible pour le redimensionnement à la souris ou au clavier. |
| `Sidebar.Trigger` | Contrôle latéral pour replier ou déplier le panneau. |
| `Sidebar.Header` | Zone supérieure qui ne défile pas. |
| `Sidebar.Content` | Zone centrale défilante. |
| `Sidebar.Footer` | Zone inférieure qui ne défile pas. |
| `Sidebar.Group` | Section de navigation sémantique. |
| `Sidebar.NavigationButton` | Bouton ou lien `asChild` avec les états active et size. |
| `Sidebar.ExpandedContent` | Contenu affiché lorsque le panneau est déplié. |
| `Sidebar.CollapsedContent` | Contenu affiché lorsque le panneau est replié. |
| `Sidebar.Tooltip` | Aide de libellé repliée avec positionnement adapté au côté. |
| `Sidebar.NavigationSubList` | Liste de navigation imbriquée. |

## Composition

`Sidebar` possède l’état de mise en page du Splitter, tandis que `Collapsible`, `Menu` et
`Sidebar.Tooltip` possèdent chacun leur propre état. Gardez `Panel`, `ResizeTrigger`, `Trigger` et
`Inset` comme enfants directs, dans l’ordre visuel: `Panel` en premier à gauche, `Inset` en premier
à droite. `ExpandedContent` et `CollapsedContent` ne font que changer la branche visible. Ils servent
à associer des liens `Collapsible` dans la vue étendue et un `Menu` dans la barre repliée, sans
déplacer l’état dans Sidebar. Pour conserver la largeur, contrôlez `size`, mettez-le à jour dans
`onResize(details)` et enregistrez la valeur finale dans `onResizeEnd(details)`. Dans Solid, `asChild`
utilise une fonction de rendu qui reçoit les props fusionnées.

## Exemples

### Navigation imbriquée

Utilisez `ExpandedContent` pour les liens imbriqués affichés dans le panneau et `CollapsedContent`
pour les mêmes liens dans un menu contextuel. Les deux branches restent composées par l’application.

<ExampleFrame variant="component">
  <SidebarNestedNavigation />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs groupId="framework" values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]} className="docs-tabs">
    <Tab value="react">

```tsx pure file="../../../src/components/examples/sidebar/nested-navigation.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={sidebarSolidNestedNavigation} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/sidebar/sidebar-nested-navigation.module.css"
```
</div>

</ExampleCode>

### Zone de défilement personnalisée

Le défilement natif est utilisé par défaut. Placez `ScrollArea` dans `Sidebar.Content` pour ajouter
une barre de défilement stylisée ou un fondu sur le bord, et faites de son viewport le seul conteneur
de défilement.

<ExampleFrame variant="component">
  <SidebarCustomScrollArea />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs groupId="framework" values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]} className="docs-tabs">
    <Tab value="react">

```tsx pure file="../../../src/components/examples/sidebar/custom-scroll-area.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={sidebarSolidCustomScrollArea} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/sidebar/sidebar-custom-scroll-area.module.css"
```
</div>

</ExampleCode>

### Mise en page persistante sur ordinateur

Conservez la largeur de la barre latérale dans l’état de l’application ou dans le stockage. Contrôlez
`size` pendant le redimensionnement et enregistrez la valeur finale dans `onResizeEnd(details)`.

<ExampleFrame variant="component">
  <SidebarPersistedLayout />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs groupId="framework" values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]} className="docs-tabs">
    <Tab value="react">

```tsx pure file="../../../src/components/examples/sidebar/persisted-layout.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={sidebarSolidPersistedLayout} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/sidebar/sidebar-persisted-layout.module.css"
```
</div>

</ExampleCode>

### Structure complète de l’application

Combinez la recherche, les actions de groupe, les badges, la navigation imbriquée, le menu de la
barre repliée et le menu du compte dans une même structure. Chaque primitive imbriquée conserve son
propre état et sa gestion du focus.

<ExampleFrame variant="component">
  <SidebarNestedNavigationAndAccountMenu />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs groupId="framework" values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]} className="docs-tabs">
    <Tab value="react">

```tsx pure file="../../../src/components/examples/sidebar/nested-navigation-and-account-menu.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={sidebarSolidNestedNavigationAndAccountMenu} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/sidebar/sidebar-nested-navigation-and-account-menu.module.css"
```
</div>

</ExampleCode>

### Barre latérale à droite

Passez `side="right"` et rendez `Inset`, `Trigger`, `ResizeTrigger` et `Panel` dans cet ordre.
Sidebar ajuste automatiquement la configuration des panneaux, les identifiants, le placement et la
direction du chevron.

<ExampleFrame variant="component">
  <SidebarRightSidebar />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs groupId="framework" values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]} className="docs-tabs">
    <Tab value="react">

```tsx pure file="../../../src/components/examples/sidebar/right-sidebar.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={sidebarSolidRightSidebar} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/sidebar/sidebar-right-sidebar.module.css"
```
</div>

</ExampleCode>

### Sélection d’un espace de travail

Utilisez `Select.Trigger asChild` avec `Sidebar.NavigationButton`. Placez la valeur sélectionnée
dans `Sidebar.Label`, gardez `Select.Indicator` comme enfant direct du trigger et marquez l’ancre
visuelle compacte avec `data-sidebar-icon`.

<ExampleFrame variant="component">
  <SidebarWorkspaceSelect />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs groupId="framework" values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]} className="docs-tabs">
    <Tab value="react">

```tsx pure file="../../../src/components/examples/sidebar/workspace-select.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={sidebarSolidWorkspaceSelect} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/sidebar/sidebar-workspace-select.module.css"
```
</div>

</ExampleCode>

### Personnalisation avancée

Définissez une taille initiale explicite et composez d’autres primitives dans la structure lorsque
la largeur initiale par défaut ne convient pas.

<ExampleFrame variant="component">
  <SidebarCustomSizes />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs groupId="framework" values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]} className="docs-tabs">
    <Tab value="react">

```tsx pure file="../../../src/components/examples/sidebar/custom-sizes.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={sidebarSolidCustomSizes} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/sidebar/sidebar-custom-sizes.module.css"
```
</div>

</ExampleCode>

## Styles

Chaque partie sémantique accepte `className` en React et `class` en Solid et expose un hook stable
`data-slot="sidebar-*"`. `Sidebar.Panel` ajoute `data-state="expanded"` ou `"collapsed"`, tandis
que les parties de mise en page exposent `data-side`. `NavigationButton` et `NavigationSubButton`
exposent `data-active`, et `NavigationButton` expose aussi `data-size`.

Lorsque la barre est repliée, les libellés et titres de groupe sortent de la mise en page visuelle
tout en restant accessibles aux technologies d’assistance. Les listes imbriquées, champs, actions,
badges et indicateurs sont masqués pour garder la barre d’icônes compacte. Marquez les ancres
visuelles non-SVG, comme `Avatar` ou une marque, avec `data-sidebar-icon`.

La largeur des panneaux appartient à l’état d’Ark, pas au CSS visuel. Remplacez `defaultSize` ou
contrôlez `size` pour définir la largeur courante. Utilisez les variables publiques
`--moduix-sidebar-*` pour les espacements internes, les dimensions, les couleurs et le décalage du
trigger.

### Propriétés CSS

<div className="docs-reference">
  <Tabs
    values={[{ label: 'CSS Variables', value: 'CSS Variables' }]}
    className="docs-reference-tabs"
    contentItemClassName="docs-reference-panel"
  >
    <Tab>
      <SidebarCssPropertiesPanel />
    </Tab>
  </Tabs>
</div>

### Points d’accroche CSS

Les parties de la coque Sidebar exposent aussi `data-scope="sidebar"` et `data-part`. `Sidebar.Input`
et `Sidebar.Separator` conservent les scopes de leurs primitives, tandis que les parties basées sur
Ark conservent les attributs d’état du Splitter, notamment `data-focus`, `data-dragging`,
`data-disabled` et `data-orientation`.
