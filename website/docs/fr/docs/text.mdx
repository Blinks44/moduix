---
title: Text
component: text
description: Un composant typographique pour le texte courant ou en ligne, l’emphase sémantique et les aperçus tronqués.
---

import * as TextDocs from '@/components/examples/text/css-properties';
import TextAlignment from '@/components/examples/text/alignment.tsx';
import TextBasic from '@/components/examples/text/basic.tsx';
import TextCustomComposition from '@/components/examples/text/custom-composition.tsx';
import TextSemantics from '@/components/examples/text/semantics.tsx';
import TextSize from '@/components/examples/text/size.tsx';
import TextTone from '@/components/examples/text/tone.tsx';
import TextTruncation from '@/components/examples/text/truncation.tsx';
import TextWeight from '@/components/examples/text/weight.tsx';
import textSolidAlignment from '../../../snippets/text/solid/alignment.tsx?raw';
import textSolidBasic from '../../../snippets/text/solid/basic.tsx?raw';
import textSolidCustomComposition from '../../../snippets/text/solid/custom-composition.tsx?raw';
import textSolidSemantics from '../../../snippets/text/solid/semantics.tsx?raw';
import textSolidSize from '../../../snippets/text/solid/size.tsx?raw';
import textSolidTone from '../../../snippets/text/solid/tone.tsx?raw';
import textSolidTruncation from '../../../snippets/text/solid/truncation.tsx?raw';
import textSolidWeight from '../../../snippets/text/solid/weight.tsx?raw';

## Référence de l’API

Ark UI ne propose pas de composant `Text` dédié. moduix propose ce composant typographique, construit avec la
[fabrique Ark](https://ark-ui.com/docs/guides/composition#the-ark-factory) officielle, et suit les recommandations
d’Ark concernant la [composition](https://ark-ui.com/docs/guides/composition), les
[styles](https://ark-ui.com/docs/guides/styling) et les [refs](https://ark-ui.com/docs/guides/ref).

## Choisir le bon composant

Utilisez `Text` pour le texte courant et auxiliaire, l’emphase en ligne et les autres textes d’interface qui doivent
suivre l’échelle typographique de moduix.

- Choisissez **`Heading`** pour la structure du document et la hiérarchie des titres.
- Choisissez **`Text`** lorsque le contenu doit conserver la sémantique d’un paragraphe ou d’un texte en ligne tout en utilisant les styles du texte courant.
- Utilisez du CSS local lorsque le besoin concerne la mise en page du texte enrichi, l’espacement ou le dépliage d’un texte tronqué.

## Exemple de base


Utilisez l’élément `p` par défaut pour un paragraphe. Passez `as` lorsque le contenu doit utiliser une autre balise HTML
sémantique tout en conservant le style typographique de `Text`.

<ExampleFrame variant="component">
  <TextBasic />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/text/basic.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={textSolidBasic} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/text/text-basic.module.css"
```
</div>

</ExampleCode>

## Installation avec shadcn

Consultez le [Démarrage rapide](/docs/quick-start) pour la configuration complète du paquet, du registre et des styles.

<Tabs groupId="framework">
  <Tab label="React" value="react">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-react/text' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-react-tailwind/text' },
  ]}
/>

  </Tab>
  <Tab label="Solid" value="solid">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-solid/text' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-solid-tailwind/text' },
  ]}
/>

  </Tab>
</Tabs>

## Anatomie

`Text` rend un seul élément racine avec la fabrique Ark via l’export racine plat.

```text
Text
└─ texte ou contenu en ligne
```

| Partie                | Rôle                                                                  |
| --------------------- | --------------------------------------------------------------------- |
| `Text`              | Racine typographique avec sémantique HTML et attributs `data-*` stables. |

## Composition

`Text` se limite à un élément racine et ne possède ni état ni sous-parties. Utilisez `<Text>` pour l’élément racine,
`as` pour une balise sémantique prise en charge et `asChild` seulement lorsqu’un élément
existant doit recevoir les propriétés et la `ref` du texte. La troncature ne fait que limiter l’affichage ; l’état et le
contrôle du dépliage restent dans l’interface environnante.

## Exemples

### Alignement

Utilisez `align` lorsque l’alignement appartient au texte lui-même et non à son conteneur.

<ExampleFrame variant="component">
  <TextAlignment />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/text/alignment.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={textSolidAlignment} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/text/text-alignment.module.css"
```
</div>

</ExampleCode>

### Sémantique

Utilisez `as` pour choisir une balise HTML sémantique tout en conservant le style typographique de `Text`.

<ExampleFrame variant="component">
  <TextSemantics />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/text/semantics.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={textSolidSemantics} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/text/text-semantics.module.css"
```
</div>

</ExampleCode>

### Taille

Utilisez `size` pour modifier la hiérarchie visuelle sans changer de composant typographique.

<ExampleFrame variant="component">
  <TextSize />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/text/size.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={textSolidSize} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/text/text-size.module.css"
```
</div>

</ExampleCode>

### Tonalité

Utilisez `tone` pour indiquer le rôle du texte sans ajouter de classe de couleur locale.

<ExampleFrame variant="component">
  <TextTone />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/text/tone.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={textSolidTone} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/text/text-tone.module.css"
```
</div>

</ExampleCode>

### Troncature

Utilisez `truncate` pour une troncature sur une ligne avec points de suspension et `lineClamp` pour les aperçus
compacts sur plusieurs lignes. Quand les deux sont définis, `lineClamp` reste la contrainte effective et prend le
pas sur la troncature sur une ligne. Ajoutez le comportement de dépliage dans l’interface environnante.

<ExampleFrame variant="component">
  <TextTruncation />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/text/truncation.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={textSolidTruncation} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/text/text-truncation.module.css"
```
</div>

</ExampleCode>

### Graisse

Utilisez `weight` lorsque l’emphase doit rester dans le même élément sémantique.

<ExampleFrame variant="component">
  <TextWeight />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/text/weight.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={textSolidWeight} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/text/text-weight.module.css"
```
</div>

</ExampleCode>

### Personnalisation avancée

Utilisez `asChild` lorsque la racine doit être un élément personnalisé existant plutôt que l’une des valeurs prises en
charge par `as`. L’enfant doit être un élément unique capable d’accepter les propriétés transmises et la `ref`.

<ExampleFrame variant="component">
  <TextCustomComposition />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/text/custom-composition.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={textSolidCustomComposition} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

## Styles

Personnalisez `Text` avec `className`, les attributs de données de la racine et l’API CSS publique. Les packages
CSS Modules exposent les variables `--moduix-text-*` ci-dessous ; les packages Tailwind utilisent des classes utilitaires
et les remplacements de classes fournis par l’application.

### Propriétés CSS

<div className="not-prose docs-reference">
  <Tabs
    values={[{ label: 'Variables CSS', value: 'CSS Variables' }]}
    className="docs-reference-tabs"
    contentItemClassName="docs-reference-panel"
  >
    <Tab>
      <TextDocs.TextCssPropertiesPanel />
    </Tab>
  </Tabs>
</div>

### Points d’accroche CSS

- `data-scope="text"` et `data-part="root"` identifient la racine utilisée par Ark.
- `data-slot="text-root"` est le sélecteur moduix stable de la racine exportée.
- `data-size` reflète la taille visuelle calculée : `xs`, `sm`, `md`, `lg` ou `xl`.
- `data-weight` reflète la graisse calculée : `regular`, `medium`, `semibold` ou `bold`.
- `data-tone` reflète la tonalité active : `default`, `muted`, `subtle`, `primary` ou `destructive`.
- `data-align` apparaît uniquement lorsque `align` vaut explicitement `start`, `center`, `end`, `left`, `right` ou `justify`.
- `data-truncate` apparaît lorsque la troncature sur une ligne avec points de suspension est active.
- `data-line-clamp` apparaît lorsque `lineClamp` est un entier positif. Dans ce cas, `lineClamp` définit le nombre de
  lignes utilisé par défaut. Définissez `--moduix-text-line-clamp` pour remplacer cette valeur via l’API CSS publique.
