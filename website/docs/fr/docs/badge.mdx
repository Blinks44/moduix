---
title: Badge
component: badge
description: Un badge compact pour afficher de brèves informations comme un statut, une catégorie, une version ou un nombre.
---

import * as BadgeDocs from '@/components/examples/badge/css-properties';
import BadgeBasic from '@/components/examples/badge/basic.tsx';
import BadgeCustomStyling from '@/components/examples/badge/custom-styling.tsx';
import BadgeLink from '@/components/examples/badge/link.tsx';
import BadgeLongLabels from '@/components/examples/badge/long-labels.tsx';
import BadgeVariants from '@/components/examples/badge/variants.tsx';
import BadgeWithDot from '@/components/examples/badge/with-dot.tsx';
import BadgeWithIcon from '@/components/examples/badge/with-icon.tsx';

import badgeSolidBasic from '../../../snippets/badge/solid/basic.tsx?raw';
import badgeSolidCustomStyling from '../../../snippets/badge/solid/custom-styling.tsx?raw';
import badgeSolidLink from '../../../snippets/badge/solid/link.tsx?raw';
import badgeSolidLongLabels from '../../../snippets/badge/solid/long-labels.tsx?raw';
import badgeSolidVariants from '../../../snippets/badge/solid/variants.tsx?raw';
import badgeSolidWithDot from '../../../snippets/badge/solid/with-dot.tsx?raw';
import badgeSolidWithIcon from '../../../snippets/badge/solid/with-icon.tsx?raw';

## Référence de l’API

Ark UI ne fournit pas de primitive `Badge` dédiée. moduix construit `Badge` avec la [factory Ark](https://ark-ui.com/docs/guides/composition#the-ark-factory)
officielle et suit les recommandations d’Ark pour la [composition](https://ark-ui.com/docs/guides/composition), les
[styles](https://ark-ui.com/docs/guides/styling) et les [références](https://ark-ui.com/docs/guides/ref).

## Choisir le bon composant

Utilisez `Badge` pour afficher une information courte sur un élément. C’est un composant de présentation : il ne remplace
pas un contrôle qui permet à l’utilisateur de sélectionner ou de modifier une valeur.

- Choisissez **`Badge`** pour de courtes métadonnées comme un statut, une catégorie, une version ou un nombre.
- Choisissez **`Tag`** pour des valeurs sélectionnées, des filtres, des affectations ou des libellés qui font partie de l’état actuel de l’interface.


Les mêmes exports sont disponibles depuis `@moduix/solid/badge` pour Solid.
## Exemple de base

Commencez par un `<Badge>` simple. Par défaut, le composant rend un `span` de présentation. Définissez `variant` pour
choisir un style intégré et ajoutez `Badge.Dot` uniquement lorsqu’un statut a besoin d’un indicateur visuel.


Ce composant est disponible pour React et Solid, avec des variantes CSS Modules et Tailwind. L’aperçu utilise React avec CSS Modules ; les onglets affichent le code natif correspondant à chaque framework.
<ExampleFrame variant="component">
  <BadgeBasic />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/badge/basic.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={badgeSolidBasic} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

## Installation avec shadcn

Consultez le [Démarrage rapide](/docs/quick-start) pour la configuration complète du paquet, du registre et des styles.

<Tabs groupId="framework">
  <Tab label="React" value="react">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-react/badge' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-react-tailwind/badge' },
  ]}
/>

  </Tab>
  <Tab label="Solid" value="solid">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-solid/badge' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-solid-tailwind/badge' },
  ]}
/>

  </Tab>
</Tabs>

## Anatomie

`Badge` est le raccourci de `Badge.Root`. Son API publique reste volontairement réduite aux parties visibles.

```text
Badge / Badge.Root
├─ Badge.Label (facultatif ; à utiliser explicitement pour la troncature)
├─ Badge.Dot (facultatif)
└─ icône svg (facultative)
```

| Partie                 | Rôle                                                                                  |
| ---------------------- | ------------------------------------------------------------------------------------- |
| `Badge` / `Badge.Root` | Partie racine. Applique la variante, la bordure et les espacements.                    |
| `Badge.Label`          | Partie de texte explicite. Permet de tronquer correctement un badge composé.          |
| `Badge.Dot`            | Partie décorative. Affiche un marqueur circulaire qui reprend la couleur du texte.    |

## Composition

`Badge` est un composant de présentation et ne gère aucun état. Le texte direct reste inchangé ; utilisez `Badge.Label` lorsqu’un libellé contraint doit être tronqué ou lorsqu’il est combiné avec `Badge.Dot` ou une icône. Pour un lien ou un bouton sémantique, utilisez `asChild` avec exactement un enfant en React. En Solid, passez une fonction de rendu à `asChild` ; l’enfant rendu conserve ses sémantiques natives. `Badge.Root` est l’équivalent avec espace de noms de `Badge`.
## Exemples

### Styles personnalisés

Lorsqu’une variante intégrée est proche du résultat souhaité mais nécessite encore un léger ajustement, utilisez une classe
ou définissez des variables CSS directement sur le badge.

<ExampleFrame variant="component">
  <BadgeCustomStyling />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/badge/custom-styling.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={badgeSolidCustomStyling} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/badge/badge-custom-styling.module.css"
```
</div>

</ExampleCode>

### Lien

Utilisez `variant="link"` avec `asChild` lorsque le badge doit devenir un lien dans une ligne de métadonnées. L’élément
enfant reste un véritable lien et conserve sa sémantique native.

<ExampleFrame variant="component">
  <BadgeLink />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/badge/link.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={badgeSolidLink} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Libellés longs

Placez un libellé long dans `Badge.Label` pour le tronquer dans un badge de largeur contrainte. Ajoutez `title` pour
rendre le libellé complet visible au survol. Dans les interfaces tactiles, choisissez un libellé adapté à l’espace disponible.

<ExampleFrame variant="component">
  <BadgeLongLabels />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/badge/long-labels.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={badgeSolidLongLabels} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/badge/badge-long-labels.module.css"
```
</div>

</ExampleCode>

### Variantes

Utilisez les variantes intégrées pour les styles courants.

<ExampleFrame variant="component">
  <BadgeVariants />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/badge/variants.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={badgeSolidVariants} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/badge/badge-variants.module.css"
```
</div>

</ExampleCode>

### Avec un point

Ajoutez `Badge.Dot` lorsque le libellé a besoin d’un indicateur d’état qui reste visuellement associé. Le texte visible doit
porter le sens du statut, car le point est décoratif.

<ExampleFrame variant="component">
  <BadgeWithDot />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/badge/with-dot.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={badgeSolidWithDot} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/badge/badge-with-dot.module.css"
```
</div>

</ExampleCode>

### Avec une icône

Placez une icône SVG enfant directe à côté du libellé lorsque les métadonnées ont besoin d’un repère visuel supplémentaire.

<ExampleFrame variant="component">
  <BadgeWithIcon />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/badge/with-icon.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={badgeSolidWithIcon} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/badge/badge-with-icon.module.css"
```
</div>

</ExampleCode>

## Styles

Commencez avec une variante intégrée. Pour une modification ponctuelle de taille ou de style, définissez les variables
`--moduix-badge-*` publiques sur le badge via une classe ou un style en ligne. Utilisez les variables
`--moduix-badge-<variant>-*` pour personnaliser une variante dans toute l’application. Les variables générales
`--moduix-badge-bg`, `--moduix-badge-border-color` et `--moduix-badge-color` remplacent les valeurs propres à la variante
pour un badge donné.

### Propriétés CSS

<CssPropertiesSection properties={BadgeDocs.badgeCssProperties} />

### Points d’accroche CSS

- `data-slot="badge-root"`, `data-slot="badge-label"` et `data-slot="badge-dot"` sont des sélecteurs stables pour cibler les parties exportées en CSS.
- `Badge.Root` expose également `data-scope="badge"` et `data-part="root"`. `Badge.Label` et `Badge.Dot` exposent `data-part="label"` et `data-part="dot"`.
- `Badge.Root` indique la variante sélectionnée dans l’attribut `data-variant`.
- Les hôtes focalisables rendus avec `Badge asChild` ou `Badge.Root asChild` reçoivent le style de focus visible. Les racines de type lien reçoivent aussi le survol de la variante et le curseur pointeur ; les styles propres aux boutons restent gérés par l’application.
- Les icônes `svg` placées directement comme enfants héritent de `currentColor`, ne capturent pas les événements du pointeur et utilisent `--moduix-badge-icon-size`.
- `Badge` ne dispose ni d’une primitive Ark dédiée ni d’une machine de comportement ; il n’expose donc aucun état géré par Ark ni aucune variable CSS d’exécution.
