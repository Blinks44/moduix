---
title: Floating Panel
component: floating-panel
description: Une fenêtre non modale Ark UI que l'utilisateur peut déplacer, redimensionner, réduire et agrandir.
---

import * as FloatingPanelDocs from '@/components/examples/floating-panel/css-properties';
import FloatingPanelAdvancedCustomization from '@/components/examples/floating-panel/advanced-customization.tsx';
import FloatingPanelAnchorPosition from '@/components/examples/floating-panel/anchor-position.tsx';
import FloatingPanelBasic from '@/components/examples/floating-panel/basic.tsx';
import FloatingPanelBoundary from '@/components/examples/floating-panel/boundary.tsx';
import FloatingPanelContext from '@/components/examples/floating-panel/context.tsx';
import FloatingPanelControlledOpen from '@/components/examples/floating-panel/controlled-open.tsx';
import FloatingPanelControlledPosition from '@/components/examples/floating-panel/controlled-position.tsx';
import FloatingPanelControlledSize from '@/components/examples/floating-panel/controlled-size.tsx';
import FloatingPanelEscapeDismiss from '@/components/examples/floating-panel/escape-dismiss.tsx';
import FloatingPanelLazyMount from '@/components/examples/floating-panel/lazy-mount.tsx';
import FloatingPanelRootProvider from '@/components/examples/floating-panel/root-provider.tsx';
import floatingPanelSolidAdvancedCustomization from '../../../snippets/floating-panel/solid/advanced-customization.tsx?raw';
import floatingPanelSolidAnchorPosition from '../../../snippets/floating-panel/solid/anchor-position.tsx?raw';
import floatingPanelSolidBasic from '../../../snippets/floating-panel/solid/basic.tsx?raw';
import floatingPanelSolidBoundary from '../../../snippets/floating-panel/solid/boundary.tsx?raw';
import floatingPanelSolidContext from '../../../snippets/floating-panel/solid/context.tsx?raw';
import floatingPanelSolidControlledOpen from '../../../snippets/floating-panel/solid/controlled-open.tsx?raw';
import floatingPanelSolidControlledPosition from '../../../snippets/floating-panel/solid/controlled-position.tsx?raw';
import floatingPanelSolidControlledSize from '../../../snippets/floating-panel/solid/controlled-size.tsx?raw';
import floatingPanelSolidEscapeDismiss from '../../../snippets/floating-panel/solid/escape-dismiss.tsx?raw';
import floatingPanelSolidLazyMount from '../../../snippets/floating-panel/solid/lazy-mount.tsx?raw';
import floatingPanelSolidRootProvider from '../../../snippets/floating-panel/solid/root-provider.tsx?raw';

## Référence API

<PrimitiveReference href="https://ark-ui.com/docs/components/floating-panel" />

## Choisir le bon composant

Utilisez `FloatingPanel` pour un espace de travail flottant et non modal, par exemple un inspecteur,
un panneau de propriétés ou une fenêtre utilitaire. Utilisez `Popover` pour un contenu compact ancré
à un déclencheur, `Dialog` pour les tâches modales et `Drawer` pour un panneau fixé au bord de la
fenêtre avec des gestes ou des points d'ancrage.

## Exemple de base


Conservez les parties Ark UI explicites. `FloatingPanelPositioner` applique la position et la taille
calculées par Ark, tandis que `FloatingPanelContent` représente la surface visible de la fenêtre.
L'en-tête sert de poignée de déplacement et `FloatingPanelResizeTriggerGroup` ajoute les poignées de redimensionnement.

<ExampleFrame variant="component">
  <FloatingPanelBasic />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/floating-panel/basic.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={floatingPanelSolidBasic} />

    </Tab>
  </Tabs>
</div>
</ExampleCode>

## Installation avec shadcn

Consultez le [Démarrage rapide](/docs/quick-start) pour la configuration complète du paquet, du registre et des styles.

<Tabs groupId="framework">
  <Tab label="React" value="react">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-react/floating-panel' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-react-tailwind/floating-panel' },
  ]}
/>

  </Tab>
  <Tab label="Solid" value="solid">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-solid/floating-panel' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-solid-tailwind/floating-panel' },
  ]}
/>

  </Tab>
</Tabs>

## Anatomie

```text
FloatingPanel
├─ FloatingPanelTrigger
└─ Sous-arbre de l'overlay (placé automatiquement dans un portail)
   └─ FloatingPanelPositioner
      └─ FloatingPanelContent
         ├─ FloatingPanelDragTrigger
         │  └─ FloatingPanelHeader
         │     ├─ FloatingPanelTitle
         │     │  └─ FloatingPanelDragIndicator (facultatif)
         │     └─ FloatingPanelControl
         │        ├─ FloatingPanelStageTrigger
         │        └─ FloatingPanelCloseTrigger / FloatingPanelCloseIcon
         ├─ FloatingPanelBody
         ├─ FloatingPanelFooter
         └─ FloatingPanelResizeTrigger / FloatingPanelResizeTriggerGroup
```

| Partie                                      | Rôle                                                                    |
| ------------------------------------------- | ----------------------------------------------------------------------- |
| `FloatingPanel`                            | Gère l'ouverture, la position, la taille, le déplacement, le redimensionnement et le mode de la fenêtre. |
| `FloatingPanelTrigger`                     | Ouvre la fenêtre ou inverse son état.                                   |
| `FloatingPanelPositioner`                  | Applique les valeurs de position, de taille, de transformation et de z-index calculées par Ark. |
| `FloatingPanelContent`                     | Surface visible de la fenêtre flottante.                               |
| `FloatingPanelDragTrigger`                 | Définit la zone déplaçable, généralement l'en-tête.                     |
| `FloatingPanelHeader`                      | Partie d'en-tête Ark et conteneur de la poignée de déplacement.          |
| `FloatingPanelTitle`                       | Définit le nom accessible de la fenêtre.                                |
| `FloatingPanelControl`                     | Regroupe les commandes de mode et de fermeture.                         |
| `FloatingPanelStageTrigger`                | Passe entre les modes `default`, `minimized` et `maximized`.             |
| `FloatingPanelCloseTrigger` / `FloatingPanelCloseIcon` | Ferme la fenêtre.                                             |
| `FloatingPanelBody`                        | Zone principale du contenu, avec défilement.                            |
| `FloatingPanelFooter`                      | Zone facultative pour les actions ou le statut.                         |
| `FloatingPanelResizeTrigger`               | Poignée de redimensionnement pour un axe Ark.                           |
| `FloatingPanelResizeTriggerGroup`          | Affiche toutes les poignées ou seulement celles qui sont sélectionnées.  |
| `FloatingPanelRootProvider`                | Relie les parties à l'état créé avec `useFloatingPanel()`. |

`FloatingPanelDragIndicator`, `FloatingPanelCloseIcon`, `FloatingPanelFooter` et
`FloatingPanelResizeTriggerGroup` sont des utilitaires moduix. Ils complètent les parties
structurelles d'Ark UI sans les remplacer.

## Composition

Utilisez `FloatingPanel` pour l'état local. Gardez `FloatingPanelPositioner` et `FloatingPanelContent` explicites, entourez
l'en-tête avec `FloatingPanelDragTrigger` et ajoutez `FloatingPanelTitle` pour un nom accessible. `FloatingPanelPositioner` est placé dans
un portail par défaut. Définissez `portalled={false}` ou transmettez `portalRef` lorsque le panneau
doit se trouver dans un autre conteneur.

Si l'état doit être créé en dehors de l'arbre du panneau, créez-le avec
`useFloatingPanel()` et transmettez-le à `FloatingPanelRootProvider`. N'utilisez pas `FloatingPanel`
et `FloatingPanelRootProvider` pour la même instance d'état. `position` et `size` se contrôlent séparément. Utilisez
`FloatingPanelResizeTriggerGroup axes` ou des parties `FloatingPanelResizeTrigger` individuelles uniquement lorsque l'ensemble
de poignées par défaut ne convient pas. Avec `asChild`, transmettez un seul enfant sémantique ; Solid
utilise la forme avec fonction de rendu de cette propriété.

## Exemples

### Position initiale depuis l'ancre

Utilisez `getAnchorPosition` pour calculer la position initiale de la fenêtre à partir du rectangle du déclencheur.

<ExampleFrame variant="component">
  <FloatingPanelAnchorPosition />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs groupId="framework" values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]} className="docs-tabs">
    <Tab value="react">

```tsx pure file="../../../src/components/examples/floating-panel/anchor-position.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={floatingPanelSolidAnchorPosition} />

    </Tab>
  </Tabs>
</div>
</ExampleCode>

### Limiter la zone

Transmettez `getBoundaryEl` et définissez `allowOverflow={false}` pour maintenir la fenêtre dans un
conteneur précis pendant son déplacement.

<ExampleFrame variant="component">
  <FloatingPanelBoundary />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs groupId="framework" values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]} className="docs-tabs">
    <Tab value="react">

```tsx pure file="../../../src/components/examples/floating-panel/boundary.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={floatingPanelSolidBoundary} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/floating-panel/floating-panel-boundary.module.css"
```
</div>
</ExampleCode>

### Contexte

Utilisez `FloatingPanelContext` ou `useFloatingPanelContext` pour lire l'état et les méthodes de la
fenêtre dans l'arbre des composants. Dans Solid, la valeur du contexte est un accessor.

<ExampleFrame variant="component">
  <FloatingPanelContext />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs groupId="framework" values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]} className="docs-tabs">
    <Tab value="react">

```tsx pure file="../../../src/components/examples/floating-panel/context.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={floatingPanelSolidContext} />

    </Tab>
  </Tabs>
</div>
</ExampleCode>

### Ouverture contrôlée

Utilisez `open` avec `onOpenChange` lorsque l'état de l'application doit contrôler la visibilité de la fenêtre.

<ExampleFrame variant="component">
  <FloatingPanelControlledOpen />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs groupId="framework" values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]} className="docs-tabs">
    <Tab value="react">

```tsx pure file="../../../src/components/examples/floating-panel/controlled-open.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={floatingPanelSolidControlledOpen} />

    </Tab>
  </Tabs>
</div>
</ExampleCode>

### Position contrôlée

Contrôlez `position` lorsqu'une autre partie de l'application doit connaître les coordonnées actuelles de la fenêtre.

<ExampleFrame variant="component">
  <FloatingPanelControlledPosition />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs groupId="framework" values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]} className="docs-tabs">
    <Tab value="react">

```tsx pure file="../../../src/components/examples/floating-panel/controlled-position.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={floatingPanelSolidControlledPosition} />

    </Tab>
  </Tabs>
</div>
</ExampleCode>

### Taille contrôlée

Contrôlez `size` lorsque l'interface doit conserver ou afficher les dimensions actuelles de la fenêtre.

<ExampleFrame variant="component">
  <FloatingPanelControlledSize />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs groupId="framework" values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]} className="docs-tabs">
    <Tab value="react">

```tsx pure file="../../../src/components/examples/floating-panel/controlled-size.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={floatingPanelSolidControlledSize} />

    </Tab>
  </Tabs>
</div>
</ExampleCode>

### Fermeture par Escape

`closeOnEscape` est activé par défaut. Ajoutez `autoFocus` à `FloatingPanelContent` pour que la touche Escape
fonctionne dès l'ouverture de la fenêtre non modale.

<ExampleFrame variant="component">
  <FloatingPanelEscapeDismiss />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs groupId="framework" values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]} className="docs-tabs">
    <Tab value="react">

```tsx pure file="../../../src/components/examples/floating-panel/escape-dismiss.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={floatingPanelSolidEscapeDismiss} />

    </Tab>
  </Tabs>
</div>
</ExampleCode>

### Montage différé

Utilisez `lazyMount` et `unmountOnExit` lorsque le contenu doit être monté à la première ouverture,
puis retiré après l'animation de sortie.

<ExampleFrame variant="component">
  <FloatingPanelLazyMount />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs groupId="framework" values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]} className="docs-tabs">
    <Tab value="react">

```tsx pure file="../../../src/components/examples/floating-panel/lazy-mount.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={floatingPanelSolidLazyMount} />

    </Tab>
  </Tabs>
</div>
</ExampleCode>

### FloatingPanelRootProvider

Utilisez `useFloatingPanel` avec `FloatingPanelRootProvider` lorsque des commandes situées en dehors
de l'arbre du panneau doivent appeler `minimize`, `maximize` et `setOpen`. Dans Solid, le store se lit
via un accessor.

<ExampleFrame variant="component">
  <FloatingPanelRootProvider />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs groupId="framework" values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]} className="docs-tabs">
    <Tab value="react">

```tsx pure file="../../../src/components/examples/floating-panel/root-provider.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={floatingPanelSolidRootProvider} />

    </Tab>
  </Tabs>
</div>
</ExampleCode>

### Personnalisation avancée

Utilisez des parties `FloatingPanelResizeTrigger` distinctes lorsque chaque axe de redimensionnement doit avoir
son propre emplacement ou style.

<ExampleFrame variant="component">
  <FloatingPanelAdvancedCustomization />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs groupId="framework" values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]} className="docs-tabs">
    <Tab value="react">

```tsx pure file="../../../src/components/examples/floating-panel/advanced-customization.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={floatingPanelSolidAdvancedCustomization} />

    </Tab>
  </Tabs>
</div>
</ExampleCode>

## Styles

### Propriétés CSS

<div className="docs-reference">
  <Tabs
    values={[{ label: 'CSS Variables', value: 'CSS Variables' }]}
    className="docs-reference-tabs"
    contentItemClassName="docs-reference-panel"
  >
    <Tab value="CSS Variables">
      <div>
        <FloatingPanelDocs.FloatingPanelCssPropertiesPanel />
      </div>
    </Tab>
  </Tabs>
</div>

### Points d'accroche pour les styles

Les parties Ark UI conservent `data-scope="floating-panel"` et leurs valeurs `data-part`. moduix ajoute
`data-slot="floating-panel-*"` aux conteneurs rendus. Utilisez `data-state="open|closed"`, `data-dragging`,
`data-disabled`, `data-stage`, `data-topmost`, `data-behind`, `data-minimized`, `data-maximized`,
`data-staged` et `data-axis` sur les parties concernées pour appliquer des styles selon l'état.

`FloatingPanelPositioner` expose `--width`, `--height`, `--x`, `--y`, `--reference-width`, `--reference-height`,
`--available-width`, `--available-height`, `--z-index` et `--transform-origin`. Utilisez les variables
qu'Ark met à jour à l'exécution pour le dimensionnement et les transformations au lieu de dupliquer
les mesures dans le code de l'application.
