---
title: Image Cropper
component: image-cropper
description: Composant permettant de recadrer et de transformer des images, avec prise en charge du zoom, de la rotation et du rapport d’aspect.
---

import * as ImageCropperDocs from '@/components/examples/image-cropper/css-properties';
import imageCropperSolidAdvancedCustomization from '../../../snippets/image-cropper/solid/advanced-customization.tsx?raw';
import imageCropperSolidAspectRatio from '../../../snippets/image-cropper/solid/aspect-ratio.tsx?raw';
import imageCropperSolidBasic from '../../../snippets/image-cropper/solid/basic.tsx?raw';
import imageCropperSolidCircleCrop from '../../../snippets/image-cropper/solid/circle-crop.tsx?raw';
import imageCropperSolidControlledZoom from '../../../snippets/image-cropper/solid/controlled-zoom.tsx?raw';
import imageCropperSolidCropPreview from '../../../snippets/image-cropper/solid/crop-preview.tsx?raw';
import imageCropperSolidEvents from '../../../snippets/image-cropper/solid/events.tsx?raw';
import imageCropperSolidFixedCropArea from '../../../snippets/image-cropper/solid/fixed-crop-area.tsx?raw';
import imageCropperSolidInitialCrop from '../../../snippets/image-cropper/solid/initial-crop.tsx?raw';
import imageCropperSolidMinAndMaxSize from '../../../snippets/image-cropper/solid/min-and-max-size.tsx?raw';
import imageCropperSolidRootProvider from '../../../snippets/image-cropper/solid/root-provider.tsx?raw';
import imageCropperSolidRotationFlipAndReset from '../../../snippets/image-cropper/solid/rotation-flip-and-reset.tsx?raw';
import ImageCropperAdvancedCustomization from '@/components/examples/image-cropper/advanced-customization.tsx';
import ImageCropperAspectRatio from '@/components/examples/image-cropper/aspect-ratio.tsx';
import ImageCropperBasic from '@/components/examples/image-cropper/basic.tsx';
import ImageCropperCircleCrop from '@/components/examples/image-cropper/circle-crop.tsx';
import ImageCropperControlledZoom from '@/components/examples/image-cropper/controlled-zoom.tsx';
import ImageCropperCropPreview from '@/components/examples/image-cropper/crop-preview.tsx';
import ImageCropperEvents from '@/components/examples/image-cropper/events.tsx';
import ImageCropperFixedCropArea from '@/components/examples/image-cropper/fixed-crop-area.tsx';
import ImageCropperInitialCrop from '@/components/examples/image-cropper/initial-crop.tsx';
import ImageCropperMinAndMaxSize from '@/components/examples/image-cropper/min-and-max-size.tsx';
import ImageCropperRootProvider from '@/components/examples/image-cropper/root-provider.tsx';
import ImageCropperRotationFlipAndReset from '@/components/examples/image-cropper/rotation-flip-and-reset.tsx';

## Référence de l’API

<PrimitiveReference href="https://ark-ui.com/docs/components/image-cropper" />

## Exemple de base

Utilisez `ImageCropper` avec `Viewport`, `Image` et `CropArea`. Passez l’URL de l’image à
`ImageCropper.Image`. `CropArea` ajoute la zone de sélection, les lignes de la grille et les poignées
de redimensionnement.

<ExampleFrame variant="component">
  <ImageCropperBasic />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs groupId="framework" values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]} className="docs-tabs">
    <Tab value="react">

```tsx pure file="../../../src/components/examples/image-cropper/basic.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={imageCropperSolidBasic} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

Si vous appelez `getCroppedImage()` pour une image provenant d’une autre origine, le serveur de
l’image doit autoriser CORS et `ImageCropper.Image` doit recevoir `crossOrigin="anonymous"`. Les
exemples utilisent cette configuration pour l’image Unsplash.

## Installation avec shadcn

<Tabs groupId="framework">
  <Tab label="React" value="react">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-react/image-cropper' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-react-tailwind/image-cropper' },
  ]}
/>

  </Tab>
  <Tab label="Solid" value="solid">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-solid/image-cropper' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-solid-tailwind/image-cropper' },
  ]}
/>

  </Tab>
</Tabs>

## Anatomie

```tsx
<ImageCropper>
  <ImageCropper.Viewport>
    <ImageCropper.Image />
    <ImageCropper.CropArea />
  </ImageCropper.Viewport>
</ImageCropper>
```

| Partie                                | Rôle                                                                                           |
| ------------------------------------- | ---------------------------------------------------------------------------------------------- |
| `ImageCropper` / `ImageCropper.Root` | Composant racine qui gère l’état du recadrage et des transformations, les attributs ARIA et les variables CSS mises à jour pendant l’utilisation. |
| `ImageCropper.RootProvider`          | Racine liée à une instance `useImageCropper` créée en dehors de l’arbre de composants.          |
| `ImageCropper.Viewport`              | Zone d’affichage qui masque ce qui dépasse et permet de déplacer l’image.                      |
| `ImageCropper.Image`                 | Élément d’image auquel Ark applique les transformations.                                       |
| `ImageCropper.Selection`             | Zone de recadrage focalisable et cible des interactions au clavier. Avec `fixedCropArea`, elle est retirée de l’ordre de tabulation. |
| `ImageCropper.Grid`                  | Ligne de guidage horizontale ou verticale, non interactive. `axis` est obligatoire.            |
| `ImageCropper.Handle`                | Poignée de redimensionnement pour une position de `ImageCropper.handles`. `position` est obligatoire. |
| `ImageCropper.CropArea`              | Zone prête à l’emploi avec les deux grilles et toutes les poignées. Elle n’accepte ni `children` ni `asChild`. |

## Composition

Utilisez `ImageCropper.CropArea` pour la zone de recadrage standard. Elle crée les deux grilles et
toutes les poignées de redimensionnement, et n’accepte donc ni `children` ni `asChild`. Utilisez
`className`, `gridClassName` et `handleClassName` pour styliser la sélection et ses parties générées.

Utilisez `ImageCropper.Context` pour les contrôles placés dans la racine. Créez `useImageCropper()`
et transmettez-le à `ImageCropper.RootProvider` lorsqu’une barre d’outils, un aperçu ou la logique
d’un formulaire doit accéder à l’instance en dehors de l’arbre de composants. Pour une même instance
d’état, rendez `Root` ou `RootProvider`, jamais les deux.

Utilisez `zoom`, `rotation` ou `flip` en mode contrôlé avec le rappel correspondant lorsque l’état est
géré ailleurs dans l’application. Ark transmet la nouvelle valeur dans l’objet `details`.
`fixedCropArea` fixe la sélection et fait de la zone d’affichage la surface de déplacement de l’image.

## Exemples

### Rapport d’aspect

Définissez `aspectRatio` pour conserver un rapport largeur-hauteur précis. Modifiez-le depuis l’état
React lorsque l’utilisateur choisit un format de sortie.

<ExampleFrame variant="component">
  <ImageCropperAspectRatio />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs groupId="framework" values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]} className="docs-tabs">
    <Tab value="react">

```tsx pure file="../../../src/components/examples/image-cropper/aspect-ratio.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={imageCropperSolidAspectRatio} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/image-cropper/image-cropper-aspect-ratio.module.css"
```
</div>

</ExampleCode>

### Recadrage circulaire

Utilisez `cropShape="circle"` avec `aspectRatio={1}` pour les avatars et les images de profil.

<ExampleFrame variant="component">
  <ImageCropperCircleCrop />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs groupId="framework" values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]} className="docs-tabs">
    <Tab value="react">

```tsx pure file="../../../src/components/examples/image-cropper/circle-crop.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={imageCropperSolidCircleCrop} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Zoom contrôlé

Utilisez `zoom` en mode contrôlé avec `onZoomChange` lorsque des boutons ou une autre partie de
l’interface doivent modifier ou afficher le niveau de zoom. Limitez la plage avec `minZoom` et
`maxZoom`.

<ExampleFrame variant="component">
  <ImageCropperControlledZoom />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs groupId="framework" values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]} className="docs-tabs">
    <Tab value="react">

```tsx pure file="../../../src/components/examples/image-cropper/controlled-zoom.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={imageCropperSolidControlledZoom} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/image-cropper/image-cropper-controlled-zoom.module.css"
```
</div>

</ExampleCode>

### Aperçu du recadrage

Appelez `getCroppedImage()` sur une instance de `useImageCropper` pour créer le résultat recadré. Avec
`{ output: 'dataUrl' }`, la méthode renvoie une URL de données que vous pouvez utiliser comme source d’un
élément `img`. Utilisez `getCropData()` si vous avez seulement besoin des coordonnées du recadrage
et des données de transformation.

<ExampleFrame variant="component">
  <ImageCropperCropPreview />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs groupId="framework" values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]} className="docs-tabs">
    <Tab value="react">

```tsx pure file="../../../src/components/examples/image-cropper/crop-preview.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={imageCropperSolidCropPreview} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/image-cropper/image-cropper-crop-preview.module.css"
```
</div>

</ExampleCode>

### Événements

Abonnez-vous à `onCropChange` et `onZoomChange` pour afficher les données actuelles du recadrage ou
les synchroniser avec d’autres éléments de l’interface.

<ExampleFrame variant="component">
  <ImageCropperEvents />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs groupId="framework" values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]} className="docs-tabs">
    <Tab value="react">

```tsx pure file="../../../src/components/examples/image-cropper/events.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={imageCropperSolidEvents} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/image-cropper/image-cropper-events.module.css"
```
</div>

</ExampleCode>

### Zone de recadrage fixe

Activez `fixedCropArea` lorsque la zone doit conserver la même taille et la même position pendant que
l’utilisateur déplace l’image derrière elle.

<ExampleFrame variant="component">
  <ImageCropperFixedCropArea />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs groupId="framework" values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]} className="docs-tabs">
    <Tab value="react">

```tsx pure file="../../../src/components/examples/image-cropper/fixed-crop-area.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={imageCropperSolidFixedCropArea} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Recadrage initial

Définissez `initialCrop` pour ouvrir le composant avec un rectangle de recadrage prédéfini, exprimé
en coordonnées de la zone d’affichage et en pixels.

<ExampleFrame variant="component">
  <ImageCropperInitialCrop />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs groupId="framework" values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]} className="docs-tabs">
    <Tab value="react">

```tsx pure file="../../../src/components/examples/image-cropper/initial-crop.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={imageCropperSolidInitialCrop} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Taille minimale et maximale

Limitez la taille de la zone avec `minWidth`, `minHeight`, `maxWidth` et `maxHeight`.

<ExampleFrame variant="component">
  <ImageCropperMinAndMaxSize />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs groupId="framework" values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]} className="docs-tabs">
    <Tab value="react">

```tsx pure file="../../../src/components/examples/image-cropper/min-and-max-size.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={imageCropperSolidMinAndMaxSize} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Root Provider

Utilisez `RootProvider` avec une instance de `useImageCropper` lorsqu’une barre d’outils ou un autre
élément de l’interface doit piloter le recadrage en dehors de son arbre de composants.

<ExampleFrame variant="component">
  <ImageCropperRootProvider />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs groupId="framework" values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]} className="docs-tabs">
    <Tab value="react">

```tsx pure file="../../../src/components/examples/image-cropper/root-provider.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={imageCropperSolidRootProvider} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/image-cropper/image-cropper-root-provider.module.css"
```
</div>

</ExampleCode>

### Rotation, retournement et réinitialisation

Utilisez `ImageCropper.Context` pour relier une barre d’outils aux actions de zoom, de rotation, de
retournement et de réinitialisation. Dans cet exemple, la rotation et le retournement sont contrôlés
depuis l’état React.

<ExampleFrame variant="component">
  <ImageCropperRotationFlipAndReset />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs groupId="framework" values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]} className="docs-tabs">
    <Tab value="react">

```tsx pure file="../../../src/components/examples/image-cropper/rotation-flip-and-reset.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={imageCropperSolidRotationFlipAndReset} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/image-cropper/image-cropper-rotation-flip-and-reset.module.css"
```
</div>

</ExampleCode>

### Personnalisation avancée

Remplacez `CropArea` par les parties explicites `Selection`, `Grid` et `Handle` lorsque la zone
nécessite une structure différente ou des enfants personnalisés. `Grid` requiert `axis` et `Handle`
requiert `position`.

<ExampleFrame variant="component">
  <ImageCropperAdvancedCustomization />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs groupId="framework" values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]} className="docs-tabs">
    <Tab value="react">

```tsx pure file="../../../src/components/examples/image-cropper/advanced-customization.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={imageCropperSolidAdvancedCustomization} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

## Styles

Pour les styles structurels, utilisez `className` en React ou `class` en Solid sur les parties
exportées. Les variantes Tailwind utilisent les utilitaires natifs et les classes de l’application ;
les variantes CSS Modules se configurent avec les propriétés du composant ci-dessous. Les hooks
`data-slot` et les attributs d’état Ark sont communs aux deux approches.

### Propriétés CSS

<div className="docs-reference">
  <Tabs
    values={[{ label: 'Variables CSS', value: 'CSS Variables' }]}
    className="docs-reference-tabs"
    contentItemClassName="docs-reference-panel"
  >
    <Tab>
      <ImageCropperDocs.ImageCropperCssPropertiesPanel />
    </Tab>
  </Tabs>
</div>

### Sélecteurs de style

Utilisez les valeurs stables de `data-slot` `image-cropper-root`, `image-cropper-root-provider`,
`image-cropper-viewport`, `image-cropper-image`, `image-cropper-selection`, `image-cropper-grid` et
`image-cropper-handle` pour les styles locaux. Ark expose également `data-scope="image-cropper"`,
`data-part` et des attributs d’état tels que `data-fixed`, `data-shape`, `data-pinch`, `data-ready`,
`data-disabled`, `data-measured`, `data-dragging`, `data-panning`, `data-axis` et `data-position`
sur les parties concernées.

Ark écrit `--crop-width`, `--crop-height`, `--crop-x`, `--crop-y`, `--image-zoom`,
`--image-rotation`, `--image-offset-x` et `--image-offset-y` sur la racine. Ces variables en lecture
seule reflètent l’état courant et peuvent servir à synchroniser une interface personnalisée. Ne les
redéfinissez pas. Utilisez les propriétés `--moduix-image-cropper-*` documentées ci-dessus pour les
modifications visuelles.
