---
title: Marquee
component: marquee
description: Un composant basé sur Ark UI pour faire défiler en continu des logos, des annonces ou d’autres contenus répétés.
---

import * as MarqueeDocs from '@/components/examples/marquee';

## Référence de l’API

<PrimitiveReference href="https://ark-ui.com/docs/components/marquee" />

## Exemple de base

Utilisez la forme courte `Marquee` comme racine, puis composez explicitement `Viewport`, `Content` et `Item` à
l’intérieur. Si le contenu défilant transmet une information, ajoutez un libellé avec `aria-label` ou
`translations.root`. `Marquee` fournit le comportement et les styles d’animation nécessaires, mais ne définit ni le style
des éléments ni celui du cadre. Dans l’exemple, le style des éléments est défini séparément et `PreviewFrame` limite uniquement
la largeur de l’aperçu.

<style>{MarqueeDocs.marqueeExampleCss}</style>

<PreviewFrame maxWidth="lg">
  ```tsx preview file="../../en/docs/_snippets/marquee/basic.tsx"

```
</PreviewFrame>

## Installation avec shadcn

<ShadcnInstall packageName="marquee" />

## Anatomie

```text
Marquee / Marquee.Root
├─ Marquee.Edge (facultatif)
├─ Marquee.Viewport
│  └─ Marquee.Content
│     └─ Marquee.Item
└─ Marquee.Edge (facultatif)

Marquee.RootProvider
└─ même arbre de parties, relié à l’état créé par useMarquee() de moduix
```

| Partie                       | Rôle                                                                                              |
| ---------------------------- | ------------------------------------------------------------------------------------------------- |
| `Marquee` / `Marquee.Root`   | État du composant, libellés, identifiants, orientation, état de pause et variables CSS.            |
| `Marquee.RootProvider`       | Rend l’arbre du composant à partir de l’état renvoyé par `useMarquee()`.                           |
| `Marquee.Viewport`           | Zone d’affichage qui masque ce qui dépasse du contenu défilant.                                   |
| `Marquee.Content`            | Conteneur du contenu animé. Ark le duplique si nécessaire.                                         |
| `Marquee.Item`               | Élément individuel du contenu.                                                                    |
| `Marquee.Edge`               | Dégradé facultatif qui fait disparaître le contenu au bord. `side` en choisit le côté.           |
| `Marquee.Context`            | Accès avancé au contexte du composant `Marquee` qui l’englobe.                                   |

## Composition

### Composition recommandée

Utilisez la forme courte `Marquee` et gardez l’arbre des parties Ark explicite. La bibliothèque fournit les images clés
de l’animation et les attributs d’état ; le style des éléments et de la racine reste à la charge du CSS de votre application.

### Quand créer une composition personnalisée

Ajoutez `Marquee.Edge` lorsque le contenu doit se fondre dans l’arrière-plan de la page. Utilisez
`useMarquee()` de moduix avec `Marquee.RootProvider` lorsque des commandes externes doivent appeler `pause()`,
`resume()`, `togglePause()` ou `restart()`. Ne rendez pas `Marquee` et `Marquee.RootProvider` pour une même instance d’état.

Utilisez `Marquee.Context` ou `useMarqueeContext()` lorsqu’un contrôle placé dans l’arbre du composant doit lire
l’API courante. Utilisez `useMarquee()` avec `Marquee.RootProvider` lorsque l’état est créé séparément ou partagé
avec des contrôles situés à l’extérieur de l’arbre.

Utilisez `asChild` sur les parties Ark si vous devez remplacer l’élément rendu. L’élément de remplacement doit être
sémantiquement adapté et rester l’unique enfant. Utilisez `ids` lorsqu’une composition personnalisée nécessite des
identifiants stables pour la racine, la zone d’affichage ou le contenu.

### Propriétés par défaut

Le tableau présente les valeurs par défaut de la racine. `Marquee.Edge` exige une valeur pour `side`, et
`Marquee.RootProvider` exige un `value` renvoyé par `useMarquee()`.

| Propriété            | Valeur par défaut | Remarques                                                                                 |
| -------------------- | ----------------- | ----------------------------------------------------------------------------------------- |
| `side`               | `start`           | Valeurs possibles : `start`, `end`, `top` et `bottom`. Les deux dernières activent l’orientation verticale. |
| `speed`              | `50`              | Vitesse en pixels par seconde. Ark calcule la durée à partir de la taille du contenu.                    |
| `spacing`            | `1rem`            | Espace entre les copies du contenu et les éléments.                                                      |
| `delay`              | `0`               | Délai avant le début de l’animation, en secondes.                                                        |
| `loopCount`          | `0`               | Nombre de boucles. `0` signifie que le défilement est infini.                                             |
| `autoFill`           | `false`           | Répète le contenu jusqu’à remplir la zone d’affichage.                                                    |
| `pauseOnInteraction` | `false`           | Met en pause au survol ou lorsque le contenu reçoit le focus. Recommandé pour le contenu à lire.         |
| `reverse`            | `false`           | Inverse le sens de l’animation sans modifier `side`.                                                      |
| `defaultPaused`      | `false`           | État de pause initial du composant non contrôlé.                                                         |
| `paused`             | -                 | État de pause contrôlé. À associer à `onPauseChange`.                                                     |
| `onPauseChange`      | -                 | Reçoit les détails Ark sur l’état de pause.                                                              |
| `onLoopComplete`     | -                 | Appelé après chaque boucle.                                                                              |
| `onComplete`         | -                 | Appelé après la dernière boucle lorsque `loopCount` est supérieur à `0`.                                |
| `translations`       | -                 | Libellé localisé de la racine via `translations.root`. Utilisez-le ou fournissez `aria-label`.            |
| `ids`                | -                 | Identifiants stables de la racine, de la zone d’affichage et des instances de contenu.                   |
| `asChild`            | -                 | Disponible sur chaque partie Ark pour remplacer l’élément rendu.                                         |
| `Marquee.Edge side`  | -                 | Côté obligatoire : `start`, `end`, `top` ou `bottom`.                                                     |
| `RootProvider.value` | -                 | Valeur `UseMarqueeReturn` obligatoire renvoyée par `useMarquee()`.                                       |

### Comportement

Pour contrôler le défilement depuis l’état React, utilisez `paused` avec `onPauseChange(details)`. `defaultPaused`
ne définit que l’état initial d’un composant non contrôlé. Utilisez `loopCount`, `onLoopComplete` et `onComplete`
lorsque le défilement doit s’arrêter après un nombre défini de boucles. Les callbacks conservent les signatures
d’origine d’Ark.

Ark expose la racine comme une région nommée et masque les copies du contenu aux technologies d’assistance. Fournissez
un `aria-label` pertinent ou `translations.root`. Activez `pauseOnInteraction` si le contenu est interactif ou doit
rester lisible, et n’utilisez pas le composant pour afficher une information critique.

Le composant ne déplace pas automatiquement le focus et ne propose pas de navigation au clavier avec les flèches.
Lorsque `pauseOnInteraction` est activé, le focus met le composant en pause et sa perte relance le défilement.

Lorsque la préférence système de réduction des animations est active, moduix désactive l’animation du contenu sans
le masquer.

Le `ref` de chaque partie est transmis à l’élément DOM Ark correspondant.

## Exemples

### Remplissage automatique

Utilisez `autoFill` lorsque le contenu ne suffit pas à remplir la zone d’affichage. `spacing` définit l’espace entre
les copies du contenu.

<style>{MarqueeDocs.marqueeExampleCss}</style>

<PreviewFrame maxWidth="lg">
  ```tsx preview file="../../en/docs/_snippets/marquee/auto-fill.tsx"

```
</PreviewFrame>

### Fondu sur les bords

Ajoutez `Marquee.Edge` sur les côtés où le contenu doit se fondre dans l’arrière-plan de la page. Cette partie est
décorative et n’intercepte pas les événements de pointeur.

<style>{MarqueeDocs.marqueeExampleCss}</style>

<PreviewFrame maxWidth="lg">
  ```tsx preview file="../../en/docs/_snippets/marquee/edges.tsx"

```
</PreviewFrame>

### Boucles limitées

Définissez `loopCount` pour limiter le nombre de boucles. `onLoopComplete` est appelé après chaque boucle et
`onComplete` après la dernière.

<style>{MarqueeDocs.marqueeExampleCss}</style>

<PreviewFrame maxWidth="lg">
  ```tsx preview file="../../en/docs/_snippets/marquee/finite-loops.tsx"

```
</PreviewFrame>

### Pause lors des interactions

Utilisez `pauseOnInteraction` pour mettre le composant en pause lorsque l’utilisateur le survole ou place le focus
dans son contenu.

<style>{MarqueeDocs.marqueeExampleCss}</style>

<PreviewFrame maxWidth="lg">
  ```tsx preview file="../../en/docs/_snippets/marquee/pause-on-interaction.tsx"

```
</PreviewFrame>

### Root Provider

Utilisez `useMarquee()` de moduix avec `Marquee.RootProvider` lorsque des commandes externes doivent mettre le
défilement en pause, le reprendre ou le relancer.

<style>{MarqueeDocs.marqueeExampleCss}</style>

<PreviewFrame maxWidth="lg">
  ```tsx preview file="../../en/docs/_snippets/marquee/programmatic-control.tsx"

```
</PreviewFrame>

### Sens inverse

Utilisez `reverse` lorsque le contenu doit se déplacer dans le sens opposé à celui indiqué par `side`, sans modifier
l’orientation.

<style>{MarqueeDocs.marqueeExampleCss}</style>

<PreviewFrame maxWidth="lg">
  ```tsx preview file="../../en/docs/_snippets/marquee/reverse.tsx"

```
</PreviewFrame>

### De droite à gauche

Placez le composant dans un `LocaleProvider` configuré avec une locale RTL. Les côtés logiques `start` et `end`
suivent alors la direction du texte.

<style>{MarqueeDocs.marqueeExampleCss}</style>

<PreviewFrame maxWidth="lg">
  ```tsx preview file="../../en/docs/_snippets/marquee/rtl.tsx"

```
</PreviewFrame>

### Vitesse

Réglez le mouvement avec `speed`, exprimé en pixels par seconde. Une vitesse plus faible facilite la lecture.

<style>{MarqueeDocs.marqueeExampleCss}</style>

<PreviewFrame maxWidth="lg">
  ```tsx preview file="../../en/docs/_snippets/marquee/speed.tsx"

```
</PreviewFrame>

### Orientation verticale

Définissez `side="top"` ou `side="bottom"` pour faire défiler le contenu verticalement. Par défaut, la racine utilise
`--moduix-marquee-vertical-height`, ce qui donne à la zone d’affichage une hauteur stable.

<style>{MarqueeDocs.marqueeVerticalCss}</style>

<PreviewFrame maxWidth="sm">
  ```tsx preview file="../../en/docs/_snippets/marquee/vertical.tsx"

```
</PreviewFrame>

## Styles

Marquee inclut les images clés nécessaires à l’animation du contenu. Utilisez `className` pour les dimensions et le
style des éléments, les variables CSS pour les dimensions, la couleur, l’espacement et le fondu des bords, et les
attributs d’état Ark pour les styles liés au comportement.

`Marquee` est transparent par défaut et n’ajoute ni bordure, ni arrière-plan, ni marge intérieure, ni arrondi. Si vous
avez besoin d’un conteneur avec un fond ou une bordure, définissez-le dans le CSS de votre application.

### Propriétés CSS

<div className="docs-reference">
  <Tabs values={[{ label: 'Variables CSS', value: 'CSS Variables' }]} className="docs-reference-tabs" contentItemClassName="docs-reference-panel">
    <Tab><MarqueeDocs.MarqueeCssPropertiesPanel /></Tab>
  </Tabs>
</div>

### Points d’accroche CSS

- `data-slot="marquee-root"`, `marquee-root-provider`, `marquee-viewport`, `marquee-content`, `marquee-item` et `marquee-edge`.
- Attributs Ark : `data-scope="marquee"`, `data-part="root|viewport|content|item|edge"`, `data-state="paused|idle"`, `data-orientation`, `data-side`, `data-index`, `data-paused`, `data-reverse` et `data-clone`.
- Variables CSS Ark : `--marquee-duration`, `--marquee-spacing`, `--marquee-delay`, `--marquee-loop-count` et `--marquee-translate`.
