---
title: JSON Tree View
component: json-tree-view
description: Inspectez des valeurs JSON et JavaScript dans une arborescence accessible et dépliable avec JsonTreeView de moduix.
---

import JsonTreeViewBasicDemo from '@/components/examples/json-tree-view/basic.tsx';
import JsonTreeViewCompactPreviewsDemo from '@/components/examples/json-tree-view/compact-previews.tsx';
import jsonTreeViewSolidBasic from '../../../snippets/json-tree-view/solid/basic.tsx?raw';
import jsonTreeViewSolidCompactPreviews from '../../../snippets/json-tree-view/solid/compact-previews.tsx?raw';

## Référence de l’API

<PrimitiveReference href="https://ark-ui.com/docs/utilities/json-tree-view" />

## Choisir le bon composant

Utilisez `JsonTreeView` lorsque vous disposez déjà d’une valeur JavaScript à inspecter et que vous ne voulez pas
écrire de rendu récursif. Choisissez `TreeView` lorsque vous contrôlez le modèle des nœuds et devez gérer les
libellés, les icônes, les liens, le renommage ou le contenu personnalisé des branches.

## Exemple de base

Transmettez via `data` n’importe quelle valeur JavaScript de type JSON ou comparable, puis affichez
`<JsonTreeViewTree />` à l’intérieur du composant. `JsonTreeView` génère automatiquement les nœuds des objets, des
tableaux et des valeurs. moduix gère le style des lignes, l’indentation, les états du focus et l’icône d’ouverture
par défaut.

L’exemple ouvre d’emblée deux niveaux. Utilisez `defaultExpandedDepth` pour choisir la partie d’une réponse ou d’une
configuration à afficher au premier rendu.

<ExampleFrame variant="component">
  <JsonTreeViewBasicDemo />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/json-tree-view/basic.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={jsonTreeViewSolidBasic} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

## Installation avec shadcn

Consultez le [Démarrage rapide](/docs/quick-start) pour la configuration complète du paquet, du registre et des styles.

<Tabs groupId="framework">
  <Tab label="React" value="react">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-react/json-tree-view' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-react-tailwind/json-tree-view' },
  ]}
/>

  </Tab>
  <Tab label="Solid" value="solid">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-solid/json-tree-view' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-solid-tailwind/json-tree-view' },
  ]}
/>

  </Tab>
</Tabs>

## Anatomie

```text
JsonTreeView
└─ JsonTreeViewTree
   └─ branches et éléments JSON générés

JsonTreeViewRootProvider
└─ même arbre relié à un état useJsonTreeView
```

| Partie | Rôle |
| --- | --- |
| `JsonTreeView` | Gère les données, la collection générée et l’état de l’arbre Ark. |
| `JsonTreeViewRootProvider` | Relie les parties à une instance d’état `useJsonTreeView` créée en dehors de l’arbre. |
| `JsonTreeViewTree` | Affiche les nœuds générés pour les objets, tableaux et valeurs. |

## Composition

Gardez `JsonTreeViewTree` comme enfant de `JsonTreeView` ou de `JsonTreeView` : il affiche les nœuds JSON
générés. Lorsque des contrôles externes gèrent l’état de l’arbre, créez `useJsonTreeView` à partir du package
correspondant au framework et transmettez son état à `JsonTreeViewRootProvider`, puis affichez `JsonTreeViewTree` à l’intérieur.
Utilisez `expandedValue`, `selectedValue`, `checkedValue` ou `focusedValue` en mode contrôlé uniquement lorsque
l’état appartient à l’application. Ne rendez pas `Root` et `RootProvider` pour le même état.

## Exemples

### Aperçus compacts

Utilisez `maxPreviewItems` pour limiter les aperçus d’objets et de tableaux, et `collapseStringsAfterLength` pour
raccourcir les chaînes longues. L’utilisateur peut ouvrir un nœud pour consulter sa valeur complète.

<ExampleFrame variant="component">
  <JsonTreeViewCompactPreviewsDemo />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/json-tree-view/compact-previews.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={jsonTreeViewSolidCompactPreviews} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

## Styles

Utilisez `className` en React ou `class` en Solid sur `Root`, `RootProvider` ou `Tree` pour modifier localement la
mise en page. Les variantes Tailwind utilisent des classes utilitaires et les classes de l’application ; les
variantes CSS Modules utilisent leurs styles et les tokens de base.

Lorsque `arrow` est omis, `JsonTreeViewTree` utilise le `ChevronRightIcon` fourni par moduix. Passez votre propre
icône via `arrow` si la vue d’inspection utilise un autre indicateur d’ouverture.

Les nœuds générés conservent les attributs Ark `data-scope="json-tree-view"` et `data-part`. moduix ajoute les
repères stables `data-slot="json-tree-view-root"`, `json-tree-view-root-provider` et `json-tree-view-tree`.

`JsonTreeViewTree` transmet la prop Ark `indentGuide` lorsque vous activez les guides d’indentation. moduix ne
style pas les éléments `[data-part='branch-indent-guide']` générés ; ciblez cet attribut avec vos propres styles
lorsque les guides sont activés.

Le composant utilise directement les tokens de base, sans couche de variables
`--moduix-json-tree-view-*`. Ajoutez une classe ou ciblez les attributs des parties Ark lorsqu’une vue
d’inspection a besoin d’un style différent.
