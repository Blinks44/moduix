---
title: Simple Grid
component: simple-grid
description: Créez des grilles réactives à colonnes égales en React ou Solid avec une installation par paquet ou shadcn.
---

import SimpleGridBasic from '@/components/examples/simple-grid/basic.tsx';
import SimpleGridFixedColumns from '@/components/examples/simple-grid/fixed-columns.tsx';
import SimpleGridMediaGallery from '@/components/examples/simple-grid/media-gallery.tsx';
import simpleGridSolidBasic from '../../../snippets/simple-grid/solid/basic.tsx?raw';
import simpleGridSolidFixedColumns from '../../../snippets/simple-grid/solid/fixed-columns.tsx?raw';
import simpleGridSolidMediaGallery from '../../../snippets/simple-grid/solid/media-gallery.tsx?raw';

## Référence de l’API

`SimpleGrid` n’est pas une primitive d’Ark UI. moduix fournit un composant de mise en page compact,
construit avec la fabrique Ark. Il suit les recommandations d’Ark pour la
[composition](https://ark-ui.com/docs/guides/composition), les
[styles](https://ark-ui.com/docs/guides/styling) et les [références](https://ark-ui.com/docs/guides/ref).
Ses propriétés `columns` et `minChildWidth` reprennent le modèle de mise en page de
[SimpleGrid](https://chakra-ui.com/docs/components/simple-grid) de Chakra UI, sans reprendre les
propriétés de style adaptatif de Chakra.

## Exemple de base


Utilisez `minChildWidth` lorsque le nombre de colonnes égales doit suivre la largeur disponible. Le
composant s’appuie sur `auto-fit` et `minmax()` de CSS Grid ; les valeurs numériques sont interprétées
en pixels, tandis que les chaînes peuvent contenir n’importe quelle longueur CSS valide.

Le nombre de colonnes s’ajuste à la largeur de l’élément racine, et les enfants restent des éléments
directs de la grille. Si un enfant contient du contenu qui ne peut pas être coupé, ajoutez
`min-width: 0` ou une règle de débordement adaptée sur cet enfant.

<ExampleFrame variant="component">
  <SimpleGridBasic />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/simple-grid/basic.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={simpleGridSolidBasic} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/simple-grid/simple-grid-basic.module.css"
```
</div>

</ExampleCode>

## Installation avec shadcn

Consultez le [Démarrage rapide](/docs/quick-start) pour la configuration complète du paquet, du registre et des styles.

<Tabs groupId="framework">
  <Tab label="React" value="react">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-react/simple-grid' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-react-tailwind/simple-grid' },
  ]}
/>

  </Tab>
  <Tab label="Solid" value="solid">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-solid/simple-grid' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-solid-tailwind/simple-grid' },
  ]}
/>

  </Tab>
</Tabs>

## Anatomie

`SimpleGrid` rend un seul élément racine CSS Grid et n’ajoute aucune enveloppe autour de ses enfants.

```text
SimpleGrid
└─ root[data-scope="simple-grid"][data-part="root"][data-slot="simple-grid-root"]
   └─ children
```

| Partie       | Rôle                                                     |
| ------------ | -------------------------------------------------------- |
| `SimpleGrid` | Élément racine qui crée des colonnes CSS Grid de même largeur. |

## Composition

Utilisez la forme courte `<SimpleGrid>` pour la racine ; `SimpleGrid.Root` est un alias équivalent
conservé pour la compatibilité. Choisissez `minChildWidth` pour un nombre de colonnes intrinsèque ou
`columns` pour un nombre fixe. Si les deux propriétés sont définies, `minChildWidth` est prioritaire.

Utilisez `asChild` lorsque la racine de la grille doit être un élément sémantique comme `ul`, `section`
ou `nav`. Cette option attend exactement un élément enfant, qui devient la racine rendue. En React,
transmettez `asChild` comme booléen ; en Solid, utilisez sa forme fonctionnelle pour renvoyer cet
élément. Pour des zones de grille nommées, des pistes de tailles différentes ou des éléments qui
s’étendent sur plusieurs pistes, ne transmettez aucune propriété de mise en page et définissez le
modèle dans votre CSS.

## Exemples

### Nombre fixe de colonnes

Utilisez `columns` lorsque le nombre de colonnes égales doit rester fixe, par exemple pour une grille
compacte de métriques.

<ExampleFrame variant="component">
  <SimpleGridFixedColumns />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/simple-grid/fixed-columns.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={simpleGridSolidFixedColumns} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/simple-grid/simple-grid-fixed-columns.module.css"
```
</div>

</ExampleCode>

### Galerie de médias

Utilisez `asChild` lorsque la grille elle-même doit porter la sémantique d’une liste. Dans cet exemple,
la racine devient un `ul` et chaque image reste dans un élément `li` direct.

<ExampleFrame variant="component">
  <SimpleGridMediaGallery />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/simple-grid/media-gallery.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={simpleGridSolidMediaGallery} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/simple-grid/simple-grid-media-gallery.module.css"
```
</div>

</ExampleCode>

## Styles

`SimpleGrid` ne définit aucune variable CSS propre au composant. Ses propriétés de mise en page sont
écrites dans les styles inline de la racine rendue ; ne transmettez donc ni `columns` ni
`minChildWidth` si la feuille de style doit gérer `grid-template-columns`.

Utilisez `className` en React ou `class` en Solid pour les styles structurels. Les variantes Tailwind
utilisent les utilitaires natifs et les classes de l’application ; les attributs de données et le hook
`data-slot` sont communs aux deux approches de style.

### Points d’accroche CSS

- `className` ou `class` permet de styliser la racine rendue pour sa largeur, son positionnement, son
  arrière-plan et sa mise en page adaptative.
- `data-scope="simple-grid"` et `data-part="root"` suivent les conventions de style d’Ark.
- `data-slot="simple-grid-root"` est le sélecteur moduix stable à utiliser dans votre CSS local.
- `columns`, `minChildWidth`, `gap`, `rowGap` et `columnGap` écrivent des styles inline, prioritaires
  sur les déclarations des classes de l’application.
- `style` est fusionné après ces propriétés et constitue le dernier remplacement disponible pour
  l’instance.
- Le composant ne fournit aucun hook de style lié à un état ou à une animation.
