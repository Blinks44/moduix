---
title: Empty
component: empty
description: Composant pour les écrans sans résultats, sans données ou affichés lors de la première utilisation.
---

import * as EmptyDocs from '@/components/examples/empty/css-properties';
import EmptyAdvancedCustomization from '@/components/examples/empty/advanced-customization.tsx';
import EmptyBasic from '@/components/examples/empty/basic.tsx';
import EmptyWithoutActions from '@/components/examples/empty/without-actions.tsx';
import EmptyWithoutIcon from '@/components/examples/empty/without-icon.tsx';
import emptySolidAdvancedCustomization from '../../../snippets/empty/solid/advanced-customization.tsx?raw';
import emptySolidBasic from '../../../snippets/empty/solid/basic.tsx?raw';
import emptySolidWithoutActions from '../../../snippets/empty/solid/without-actions.tsx?raw';
import emptySolidWithoutIcon from '../../../snippets/empty/solid/without-icon.tsx?raw';

## Référence de l’API

Ark UI ne propose pas de primitive dédiée aux états vides. `moduix` construit `Empty` avec la
[fabrique Ark](https://ark-ui.com/react/docs/guides/composition#the-ark-factory) officielle et suit
les recommandations d’Ark pour la [composition](https://ark-ui.com/react/docs/guides/composition),
les [styles](https://ark-ui.com/react/docs/guides/styling) et les
[références](https://ark-ui.com/react/docs/guides/ref). Sa composition s’inspire également du composant
[Empty State de Chakra UI](https://chakra-ui.com/docs/components/empty-state). Contrairement à Chakra UI,
`moduix` expose `Empty.Icon` et `Empty.Actions` comme des parties placées directement sous la racine,
au même niveau que `Empty.Content`. Vous pouvez ainsi les placer dans l’ordre adapté à votre écran.

## Exemple de base

`Empty` est disponible pour React et Solid, dans les paquets CSS Modules comme dans les variantes
Tailwind. L’aperçu interactif de cette page utilise React avec CSS Modules ; les onglets de code
présentent la source native correspondant au même contrat public.

Commencez par `<Empty />` et ajoutez les parties du composant dont vous avez besoin. Ajoutez
`Empty.Icon` et `Empty.Actions` uniquement si l’état vide a besoin d’un repère visuel ou d’une action claire.

<ExampleFrame variant="component">
  <EmptyBasic />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/empty/basic.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={emptySolidBasic} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/empty/empty-basic.module.css"
```
</div>

</ExampleCode>

## Installation avec shadcn

Consultez le [Démarrage rapide](/docs/quick-start) pour la configuration complète du paquet, du registre et des styles.

<Tabs groupId="framework">
  <Tab label="React" value="react">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-react/empty' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-react-tailwind/empty' },
  ]}
/>

  </Tab>
  <Tab label="Solid" value="solid">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-solid/empty' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-solid-tailwind/empty' },
  ]}
/>

  </Tab>
</Tabs>

## Anatomie

```text
Empty / Empty.Root
├─ Empty.Icon (facultatif)
├─ Empty.Content
│  ├─ Empty.Title
│  └─ Empty.Description
└─ Empty.Actions (facultatif)
```

| Partie                 | Rôle                                                                                                   |
| ---------------------- | ------------------------------------------------------------------------------------------------------ |
| `Empty` / `Empty.Root` | Conteneur racine pour les espacements, la bordure, l’arrière-plan et l’alignement.                    |
| `Empty.Icon`           | Conteneur facultatif pour une icône ou un autre petit élément visuel.                                 |
| `Empty.Content`        | Regroupe le titre et la description avec des espacements plus compacts.                               |
| `Empty.Title`          | Titre principal de l’état vide.                                                                       |
| `Empty.Description`    | Contenu complémentaire sous le titre. Il peut contenir des paragraphes, des liens ou d’autres éléments de type bloc. |
| `Empty.Actions`        | Ligne facultative d’actions qui revient à la ligne si nécessaire.                                      |

## Composition

Composez `Empty` à partir de ses parties placées directement sous la racine. `Empty.Content`
regroupe le titre et la description, tandis que `Empty.Icon` et `Empty.Actions` sont des parties
facultatives au même niveau. Vous pouvez les omettre ou les réorganiser selon l’écran. `Empty` est
uniquement présentatif et ne gère ni l’état de l’application ni le comportement `aria-live`.

Utilisez `asChild` sur `Empty` ou `Empty.Title` lorsque la section qui l’accueille nécessite un
conteneur sémantique ou un autre niveau de titre. Passez un seul enfant à une partie `asChild` pour
qu’elle puisse lui transmettre ses propriétés et sa référence. Ajoutez la région, le rôle, la gestion
du focus ou le comportement `aria-live` nécessaires dans l’interface environnante.

## Exemples

### Sans actions

N’ajoutez pas `Empty.Actions` lorsque l’état est informatif et ne nécessite aucune action immédiate.

<ExampleFrame variant="component">
  <EmptyWithoutActions />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/empty/without-actions.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={emptySolidWithoutActions} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/empty/empty-without-actions.module.css"
```
</div>

</ExampleCode>

### Sans icône

L’icône est facultative : un état vide composé uniquement de texte peut donc rester compact.

<ExampleFrame variant="component">
  <EmptyWithoutIcon />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/empty/without-icon.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={emptySolidWithoutIcon} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/empty/empty-without-icon.module.css"
```
</div>

</ExampleCode>

### Personnalisation avancée

Utilisez la propriété `asChild` sur `Empty` lorsque l’état vide doit être rendu dans un conteneur
sémantique, tel que `section`. Utilisez-la sur `Empty.Title` lorsque le titre doit avoir un autre niveau.
Comme `Empty.Description` est rendu sous la forme d’un `div`, il peut contenir des paragraphes, des liens
et d’autres éléments de type bloc.

<ExampleFrame variant="component">
  <EmptyAdvancedCustomization />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/empty/advanced-customization.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={emptySolidAdvancedCustomization} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/empty/empty-advanced-customization.module.css"
```
</div>

</ExampleCode>

## Styles

Personnalisez l’apparence de `Empty` avec `className`, les sélecteurs Ark fondés sur
`data-scope="empty"` / `data-part`, les attributs `data-slot="empty-*"` stables et les variables CSS
publiques `--moduix-empty-*` présentées ci-dessous.

### Propriétés CSS

<div className="docs-reference">
  <Tabs
    values={[{ label: 'Variables CSS', value: 'CSS Variables' }]}
    className="docs-reference-tabs"
    contentItemClassName="docs-reference-panel"
  >
    <Tab>
      <EmptyDocs.EmptyCssPropertiesPanel />
    </Tab>
  </Tabs>
</div>

### Points d’accroche CSS

Ciblez la partie qui contrôle la propriété visuelle à modifier :

- `[data-scope="empty"][data-part="root"]` ou `[data-slot="empty-root"]` pour la mise en page du
  conteneur, la bordure, l’arrière-plan et les surcharges de l’ombre
- `[data-scope="empty"][data-part="icon"]` ou `[data-slot="empty-icon"]` pour les dimensions, les
  espacements internes et les couleurs du conteneur de l’icône
- `[data-scope="empty"][data-part="content"]` ou `[data-slot="empty-content"]` pour la largeur et
  l’espacement du bloc de texte
- `[data-scope="empty"][data-part="title"]` ou `[data-slot="empty-title"]`, ainsi que
  `[data-scope="empty"][data-part="description"]` ou `[data-slot="empty-description"]` pour la typographie
- `[data-scope="empty"][data-part="actions"]` ou `[data-slot="empty-actions"]` pour l’espacement
  et l’alignement de la ligne d’actions
