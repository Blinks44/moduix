---
title: Number Input
component: number-input
description: Champ de saisie numérique basé sur Ark UI, avec boutons d’incrémentation et de décrémentation, formatage, validation et glissement facultatif.
---

import * as NumberInputDocs from '@/components/examples/number-input/number-input';
import NumberInputBasic from '../../en/docs/_snippets/number-input/basic.tsx';
import NumberInputControlled from '../../en/docs/_snippets/number-input/controlled.tsx';
import NumberInputDisabledAndReadOnly from '../../en/docs/_snippets/number-input/disabled-and-read-only.tsx';
import NumberInputField from '../../en/docs/_snippets/number-input/field.tsx';
import NumberInputFormatting from '../../en/docs/_snippets/number-input/formatting.tsx';
import NumberInputMinAndMax from '../../en/docs/_snippets/number-input/min-and-max.tsx';
import NumberInputMouseWheel from '../../en/docs/_snippets/number-input/mouse-wheel.tsx';
import NumberInputFormSubmission from '../../en/docs/_snippets/number-input/form-submission.tsx';
import NumberInputPrecision from '../../en/docs/_snippets/number-input/precision.tsx';
import NumberInputRootProvider from '../../en/docs/_snippets/number-input/root-provider.tsx';
import NumberInputScrubbing from '../../en/docs/_snippets/number-input/scrubbing.tsx';
import NumberInputValueText from '../../en/docs/_snippets/number-input/value-text.tsx';
import NumberInputCustomIcons from '../../en/docs/_snippets/number-input/custom-icons.tsx';

## Référence de l’API

<PrimitiveReference href="https://ark-ui.com/docs/components/number-input" />

## Exemple de base

Utilisez `NumberInput` avec `NumberInput.Field` pour afficher un champ standard accompagné de boutons d’incrémentation et de décrémentation.

<ExampleFrame variant="component">
  <NumberInputBasic />
</ExampleFrame>

<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }]} className="docs-tabs">
    <Tab value="React">

```tsx file="../../en/docs/_snippets/number-input/basic.tsx"
```

    </Tab>
  </Tabs>
</div>

## Installation avec shadcn

<ShadcnInstall packageName="number-input" />

## Anatomie

```text
NumberInput.Root
├─ NumberInput.Label
├─ NumberInput.Scrubber (facultatif)
├─ NumberInput.Field (raccourci recommandé)
│  └─ affiche NumberInput.Control
│     ├─ NumberInput.DecrementTrigger
│     ├─ NumberInput.Input
│     └─ NumberInput.IncrementTrigger
└─ NumberInput.ValueText (facultatif)

NumberInput.RootProvider
└─ même arborescence reliée à l’état créé avec `useNumberInput()`
```

| Partie                             | Rôle                                                                         |
| ---------------------------------- | ---------------------------------------------------------------------------- |
| `NumberInput` / `NumberInput.Root` | Gère l’état du champ numérique, l’interprétation de la saisie, le formatage et le comportement au clavier. |
| `NumberInput.RootProvider`         | Relie les parties à l’état créé avec `useNumberInput()`.                    |
| `NumberInput.Context`              | Expose la valeur formatée, sa représentation numérique et les méthodes permettant de manipuler l’état. |
| `NumberInput.Label`                | Libellé accessible associé au champ géré.                                   |
| `NumberInput.Scrubber`             | Zone de glissement facultative qui modifie la valeur selon le déplacement du pointeur. |
| `NumberInput.Control`              | Regroupe le champ et les boutons d’incrémentation et de décrémentation.    |
| `NumberInput.Field`                | Partie pratique qui affiche le contrôle standard et le groupe de boutons. |
| `NumberInput.DecrementTrigger`     | Diminue la valeur actuelle. Affiche par défaut l’icône moins de moduix.   |
| `NumberInput.Input`                | Champ de texte numérique modifiable géré par Ark UI.                         |
| `NumberInput.IncrementTrigger`     | Augmente la valeur actuelle. Affiche par défaut l’icône plus de moduix.  |
| `NumberInput.ValueText`            | Texte en lecture seule qui affiche la valeur formatée.                      |

## Composition

### Composition recommandée

`NumberInput` analyse la saisie, vérifie les limites, incrémente et décrémente la valeur au clavier, répète la modification
tant qu’un bouton est maintenu, prend en charge le glissement et formate la valeur. Activez `allowMouseWheel` lorsque la
molette doit modifier la valeur dans votre scénario. Utilisez `NumberInput.Field` pour afficher les parties standard.

Donnez au champ un nom accessible avec `NumberInput.Label`, `aria-label` ou `aria-labelledby`. Ajoutez
`NumberInput.ValueText` lorsque la valeur formatée doit également apparaître à l’extérieur du champ de saisie.

Conservez `value` et `defaultValue` sous forme de chaînes de caractères. Ark signale les modifications avec
`onValueChange(details)`, où `details.value` est la valeur sous forme de chaîne et `details.valueAsNumber` sa représentation numérique.

### Quand utiliser une composition personnalisée

Utilisez `NumberInput.Control` avec ses parties individuelles lorsque `NumberInput.Field` ne convient pas à cause de
l’ordre des boutons, des icônes ou des styles du champ. Ajoutez `NumberInput.Scrubber` à côté du champ pour permettre une
modification par glissement.

### Propriétés par défaut

| Propriété                         | Valeur par défaut       | Rôle                                                                  |
| --------------------------------- | ----------------------- | --------------------------------------------------------------------- |
| `allowOverflow`                   | `true`                  | Autorise les valeurs saisies à dépasser `min` et `max`.               |
| `clampValueOnBlur`                | `true`                  | Ramène la valeur à `min` ou `max` lorsque le champ perd le focus.      |
| `focusInputOnChange`              | `true`                  | Conserve le focus dans le champ après une modification par bouton.     |
| `inputMode`                       | `'decimal'`             | Affiche un clavier adapté aux nombres décimaux sur les appareils mobiles compatibles. |
| `largeStep`                       | `10 * step`             | Quantité modifiée avec `Shift` et une touche fléchée.                   |
| `locale`                          | `'en-US'`               | Locale utilisée pour formater la valeur.                              |
| `max`                             | `Number.MAX_SAFE_INTEGER` | Valeur maximale.                                                      |
| `min`                             | `Number.MIN_SAFE_INTEGER` | Valeur minimale.                                                      |
| `spinOnPress`                     | `true`                  | Répète la modification tant qu’un bouton est maintenu.                |
| `step`                            | `1`                     | Quantité ajoutée ou retirée à chaque modification.                    |
| `smallStep`                       | `step / 10`             | Quantité modifiée avec `Alt` et une touche fléchée.                    |
| `NumberInput.Field`               | -                       | Affiche le contrôle standard, le champ et le groupe de boutons.        |
| `NumberInput.DecrementTrigger`    | -                       | Affiche l’icône moins de moduix lorsqu’aucun enfant n’est fourni.      |
| `NumberInput.IncrementTrigger`    | -                       | Affiche l’icône plus de moduix lorsqu’aucun enfant n’est fourni.       |

### Comportement

- Utilisez `formatOptions` avec `Intl.NumberFormat` pour le formatage localisé ou monétaire. Définissez `locale` lorsque
  la locale de formatage n’est pas `en-US`.
- Utilisez `translations.incrementLabel`, `translations.decrementLabel` et `translations.valueText` pour localiser les
  libellés accessibles et le texte de la valeur.
- `min`, `max`, `step`, `allowOverflow`, `clampValueOnBlur`, `spinOnPress`, `inputMode` et `allowMouseWheel` sont
  transmis tels quels à Ark.
- `largeStep` définit la quantité modifiée avec `Shift` et une touche fléchée, tandis que `smallStep` s’applique avec
  `Alt` et une touche fléchée. `Home` et `End` amènent la valeur à `min` et `max`.
- `onValueInvalid(details)` signale que la valeur sort de la plage configurée. Les détails indiquent la cause avec
  `rangeUnderflow` ou `rangeOverflow`.
- Définissez `clampValueOnBlur={false}` si une valeur saisie hors de `min` ou `max` doit rester inchangée lorsque le champ
  perd le focus.
- `onValueCommit(details)` est appelé lorsque la valeur est validée à la perte du focus ou après l’appui sur Entrée.
  Utilisez `onFocusChange` pour suivre les changements de focus et `ids` lorsque des libellés ou des descriptions externes
  ont besoin d’identifiants stables.
- Les parties Ark acceptent `asChild` pour être rendues dans l’élément de votre choix. `NumberInput.Field` rend
  volontairement une structure fixe en trois parties et transmet sa `ref` à `NumberInput.Control`.
- `useNumberInput`, `useNumberInputContext` et `NumberInput.Context` sont exportés par `@moduix/react`.
- `name` et `form` sont transmis au spinbutton visible pour la soumission par un formulaire natif. Ajoutez un `<input>`
  natif masqué via `NumberInput.Context` avec `context.valueAsNumber` uniquement lorsque le formulaire attend la
  représentation numérique plutôt que la chaîne affichée.

## Exemples

### État contrôlé

Conservez l’état contrôlé sous forme de chaîne et utilisez `details.value` dans `onValueChange`.

<ExampleFrame variant="component">
  <NumberInputControlled />
</ExampleFrame>

<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx file="../../en/docs/_snippets/number-input/controlled.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/number-input/number-input-controlled.module.css"
```

    </Tab>
  </Tabs>
</div>

### État désactivé et lecture seule

Utilisez `disabled` pour désactiver toute interaction. Utilisez `readOnly` lorsque la valeur doit rester sélectionnable,
mais ne doit pas pouvoir être modifiée.

<ExampleFrame variant="component">
  <NumberInputDisabledAndReadOnly />
</ExampleFrame>

<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx file="../../en/docs/_snippets/number-input/disabled-and-read-only.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/number-input/number-input-disabled-and-read-only.module.css"
```

    </Tab>
  </Tabs>
</div>

### Field

Utilisez `Field` pour associer l’état du champ de formulaire au texte d’aide, au message d’erreur et aux informations
d’accessibilité.

<ExampleFrame variant="component">
  <NumberInputField />
</ExampleFrame>

<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx file="../../en/docs/_snippets/number-input/field.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/number-input/number-input-field.module.css"
```

    </Tab>
  </Tabs>
</div>

### Formatage

Transmettez `formatOptions` pour formater la valeur visible avec `Intl.NumberFormat`.

<ExampleFrame variant="component">
  <NumberInputFormatting />
</ExampleFrame>

<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }]} className="docs-tabs">
    <Tab value="React">

```tsx file="../../en/docs/_snippets/number-input/formatting.tsx"
```

    </Tab>
  </Tabs>
</div>

### Minimum et maximum

Utilisez `min`, `max` et `step` pour définir la plage et le pas des changements effectués au clavier, avec les boutons ou
par glissement. La valeur est ramenée dans la plage à la perte du focus par défaut. Définissez
`clampValueOnBlur={false}` si une valeur saisie hors plage doit rester visible après la perte du focus.

<ExampleFrame variant="component">
  <NumberInputMinAndMax />
</ExampleFrame>

<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }]} className="docs-tabs">
    <Tab value="React">

```tsx file="../../en/docs/_snippets/number-input/min-and-max.tsx"
```

    </Tab>
  </Tabs>
</div>

### Molette de la souris

Activez `allowMouseWheel` lorsque la molette doit modifier la valeur dans votre scénario.

<ExampleFrame variant="component">
  <NumberInputMouseWheel />
</ExampleFrame>

<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }]} className="docs-tabs">
    <Tab value="React">

```tsx file="../../en/docs/_snippets/number-input/mouse-wheel.tsx"
```

    </Tab>
  </Tabs>
</div>

### Soumission native du formulaire

Définissez `name` pour inclure le spinbutton visible dans la soumission d’un formulaire natif. Ajoutez un `<input>` natif
masqué via `NumberInput.Context` uniquement lorsque le formulaire attend la représentation numérique plutôt que la chaîne
affichée.

<ExampleFrame variant="component">
  <NumberInputFormSubmission />
</ExampleFrame>

<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx file="../../en/docs/_snippets/number-input/form-submission.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/number-input/number-input-form-submission.module.css"
```

    </Tab>
  </Tabs>
</div>

### Précision

Utilisez `formatOptions` avec `Intl.NumberFormatOptions` lorsque la valeur affichée doit comporter un nombre fixe de
chiffres après la virgule.

<ExampleFrame variant="component">
  <NumberInputPrecision />
</ExampleFrame>

<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }]} className="docs-tabs">
    <Tab value="React">

```tsx file="../../en/docs/_snippets/number-input/precision.tsx"
```

    </Tab>
  </Tabs>
</div>

### Root Provider

Utilisez `useNumberInput()` avec `NumberInput.RootProvider` lorsque l’état doit être créé en dehors de l’arbre de rendu
qui affiche les parties du composant.

<ExampleFrame variant="component">
  <NumberInputRootProvider />
</ExampleFrame>

<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx file="../../en/docs/_snippets/number-input/root-provider.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/number-input/number-input-root-provider.module.css"
```

    </Tab>
  </Tabs>
</div>

### Modification par glissement

Faites glisser `NumberInput.Scrubber` vers la gauche ou la droite pour modifier la valeur.

<ExampleFrame variant="component">
  <NumberInputScrubbing />
</ExampleFrame>

<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx file="../../en/docs/_snippets/number-input/scrubbing.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/number-input/number-input-scrubbing.module.css"
```

    </Tab>
  </Tabs>
</div>

### Texte de la valeur

Utilisez `NumberInput.ValueText` lorsque la valeur formatée doit également apparaître à l’extérieur du champ modifiable.

<ExampleFrame variant="component">
  <NumberInputValueText />
</ExampleFrame>

<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }]} className="docs-tabs">
    <Tab value="React">

```tsx file="../../en/docs/_snippets/number-input/value-text.tsx"
```

    </Tab>
  </Tabs>
</div>

### Personnalisation avancée

Assemblez directement les parties de bas niveau au lieu d’utiliser `NumberInput.Field` lorsque les icônes des boutons ou
le style du champ doivent être personnalisés. Transmettez `translations` lorsque le contenu personnalisé des boutons
nécessite des libellés localisés pour les technologies d’assistance.

<ExampleFrame variant="component">
  <NumberInputCustomIcons />
</ExampleFrame>

<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx file="../../en/docs/_snippets/number-input/custom-icons.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/number-input/number-input-advanced-customization.module.css"
```

    </Tab>
  </Tabs>
</div>

## Styles

### Propriétés CSS

<div className="docs-reference">
  <Tabs
    values={[{ label: 'Variables CSS', value: 'CSS Variables' }]}
    className="docs-reference-tabs"
    contentItemClassName="docs-reference-panel"
  >
    <Tab>
      <NumberInputDocs.NumberInputCssPropertiesPanel />
    </Tab>
  </Tabs>
</div>

### Points d’accroche CSS

- Valeurs `data-slot` de moduix : `number-input-root`, `number-input-root-provider`, `number-input-label`,
  `number-input-scrubber`, `number-input-control`, `number-input-decrement-trigger`, `number-input-input`,
  `number-input-increment-trigger` et `number-input-value-text`.
- Attributs d’état Ark : `data-disabled`, `data-invalid`, `data-focus` et `data-scrubbing`.
- Attributs des parties Ark : `data-scope="number-input"` et valeurs de `data-part` correspondant à l’anatomie Ark.
