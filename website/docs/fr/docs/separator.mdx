---
title: Separator
component: separator
description: Sépare le contenu ou les groupes associés par une ligne horizontale ou verticale, avec une sémantique adaptée aux technologies d’assistance.
---

import * as SeparatorDocs from '@/components/examples/separator/css-properties';
import SeparatorBasic from '@/components/examples/separator/basic.tsx';
import SeparatorDecorative from '@/components/examples/separator/decorative.tsx';
import SeparatorSizes from '@/components/examples/separator/sizes.tsx';
import SeparatorVariants from '@/components/examples/separator/variants.tsx';
import SeparatorVertical from '@/components/examples/separator/vertical.tsx';
import SeparatorAsChild from '@/components/examples/separator/as-child.tsx';

## Référence de l’API

Ark UI ne fournit pas de primitive dédiée pour les séparateurs. moduix construit ce composant avec la [fabrique Ark](https://ark-ui.com/docs/guides/composition#the-ark-factory) officielle et suit les recommandations d’Ark en matière de [composition](https://ark-ui.com/docs/guides/composition), de [styles](https://ark-ui.com/docs/guides/styling) et de [ref](https://ark-ui.com/docs/guides/ref).

Ses propriétés `orientation`, `variant` et `size` reprennent le modèle familier du `Separator` de Chakra UI. Consultez la [documentation de Chakra UI relative à Separator](https://chakra-ui.com/docs/components/separator), qui a servi de référence pour cette API.

## Exemple de base

Utilisez `Separator` pour marquer visuellement la limite entre des sections liées.

<ExampleFrame variant="component">
  <SeparatorBasic />
</ExampleFrame>

<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx file="../../../src/components/examples/separator/basic.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/separator/separator-basic.module.css"
```

    </Tab>
  </Tabs>
</div>

## Installation avec shadcn

<ShadcnInstall packageName="separator" />

## Anatomie

`Separator` n’expose qu’une seule partie racine.

```text
Separator / Separator.Root
└─ root[data-scope="separator"][data-part="root"][data-slot="separator-root"]
```

| Partie                         | Rôle                                                                   |
| ------------------------------ | ---------------------------------------------------------------------- |
| `Separator` / `Separator.Root` | Élément séparateur racine. Définit l’orientation, les informations d’accessibilité et le style visuel. |

Utilisez `className` et les propriétés CSS `--moduix-separator-*` pour styliser la racine. Utilisez
`asChild` si un autre élément doit héberger le séparateur.

## Composition

### Composition recommandée

Pour la plupart des séparateurs, utilisez la forme courte `<Separator />`. Il s’agit du même composant racine
que `<Separator.Root />` ; vous pouvez donc passer de l’un à l’autre sans modifier le comportement.

### Quand créer une composition personnalisée

Utilisez `asChild` uniquement si vous devez faire porter le séparateur par un autre élément. `Separator` rend
déjà un séparateur sémantique. Son enfant doit être un élément unique capable de recevoir les attributs fusionnés
`role`, ARIA et `data-*`, ainsi que `className` et `ref`.

### Propriétés par défaut

| Propriété      | Valeur par défaut | Remarques                                         |
| -------------- | ----------------- | ------------------------------------------------ |
| `orientation`  | `horizontal`      | `horizontal`, `vertical`                        |
| `variant`      | `solid`           | `solid`, `dashed`, `dotted`                     |
| `size`         | `sm`              | `xs`, `sm`, `md`, `lg`                          |
| `asChild`      | `false`           | Composition avec la fabrique Ark et un enfant unique |
| `role`         | `separator`       | Transmettez `presentation` pour une ligne décorative |

### Comportement

- Lorsque son rôle est `separator`, `Separator` définit `aria-orientation` à partir de `orientation`. Pour un autre rôle, comme `presentation`, il omet `aria-orientation`, même si vous le transmettez vous-même.
- Utilisez `role="presentation"` lorsque la ligne est purement visuelle et n’apporte aucune information de structure aux technologies d’assistance.
- Les séparateurs horizontaux occupent toute la largeur disponible par défaut.
- Les séparateurs verticaux ont par défaut une hauteur de `1em`, ce qui les aligne naturellement sur le texte en ligne et les rangées d’actions compactes.
- `Separator` n’est pas interactif : il n’expose ni état, ni fonction de rappel, ni prise en charge de la désactivation, ni gestion du focus.
- Pour un simple espace, utilisez des primitives de mise en page ou des tokens d’espacement.

## Exemples

### Décoratif

Utilisez `role="presentation"` pour une ligne purement visuelle qui n’apporte aucune structure aux technologies
d’assistance.

<ExampleFrame variant="component">
  <SeparatorDecorative />
</ExampleFrame>

<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx file="../../../src/components/examples/separator/decorative.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/separator/separator-decorative.module.css"
```

    </Tab>
  </Tabs>
</div>

### Tailles

Utilisez `size` pour choisir l’épaisseur du séparateur, puis les propriétés CSS pour l’ajuster si nécessaire.

<ExampleFrame variant="component">
  <SeparatorSizes />
</ExampleFrame>

<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx file="../../../src/components/examples/separator/sizes.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/separator/separator-sizes.module.css"
```

    </Tab>
  </Tabs>
</div>

### Variantes

Utilisez `variant` pour choisir une ligne continue, en tirets ou en pointillés.

<ExampleFrame variant="component">
  <SeparatorVariants />
</ExampleFrame>

<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx file="../../../src/components/examples/separator/variants.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/separator/separator-variants.module.css"
```

    </Tab>
  </Tabs>
</div>

### Vertical

Utilisez `orientation="vertical"` pour les groupes disposés sur une ligne, comme des liens de navigation.

<ExampleFrame variant="component">
  <SeparatorVertical />
</ExampleFrame>

<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx file="../../../src/components/examples/separator/vertical.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/separator/separator-vertical.module.css"
```

    </Tab>
  </Tabs>
</div>

### Personnalisation avancée

Utilisez `asChild` lorsqu’un élément hôte précis doit recevoir le rôle de séparateur, les sélecteurs de style et la
`ref` transmise. Dans les autres cas, utilisez directement `<Separator />`.

<ExampleFrame variant="component">
  <SeparatorAsChild />
</ExampleFrame>

<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx file="../../../src/components/examples/separator/as-child.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/separator/separator-as-child.module.css"
```

    </Tab>
  </Tabs>
</div>

## Styles

`Separator` expose les sélecteurs de partie compatibles avec Ark et un ensemble ciblé de propriétés CSS
`--moduix-separator-*` pour la couleur, le style de bordure, l’épaisseur et la longueur selon l’orientation.

### Propriétés CSS

<div className="docs-reference">
  <Tabs
    values={[{ label: 'Variables CSS', value: 'Variables CSS' }]}
    className="docs-reference-tabs"
    contentItemClassName="docs-reference-panel"
  >
    <Tab>
      <SeparatorDocs.SeparatorCssPropertiesPanel />
    </Tab>
  </Tabs>
</div>

### Points d’accroche CSS

- `data-scope="separator"` et `data-part="root"` identifient la partie racine au format Ark.
- `data-slot="separator-root"` est le sélecteur moduix stable.
- `data-orientation="horizontal"` et `data-orientation="vertical"` permettent de cibler les styles propres à chaque axe.
- `data-size="xs|sm|md|lg"` et `data-variant="solid|dashed|dotted"` exposent les paramètres du style visuel.
- `className` et `style` s’appliquent directement à l’élément séparateur racine.
