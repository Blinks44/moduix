---
title: Separator
component: separator
description: Sépare le contenu ou les groupes associés par une ligne horizontale ou verticale, avec une sémantique adaptée aux technologies d’assistance.
---

import * as SeparatorDocs from '@/components/examples/separator/css-properties';
import SeparatorBasic from '@/components/examples/separator/basic.tsx';
import SeparatorDecorative from '@/components/examples/separator/decorative.tsx';
import SeparatorSizes from '@/components/examples/separator/sizes.tsx';
import SeparatorVariants from '@/components/examples/separator/variants.tsx';
import SeparatorVertical from '@/components/examples/separator/vertical.tsx';
import SeparatorAsChild from '@/components/examples/separator/as-child.tsx';
import separatorSolidBasic from '../../../snippets/separator/solid/basic.tsx?raw';
import separatorSolidDecorative from '../../../snippets/separator/solid/decorative.tsx?raw';
import separatorSolidSizes from '../../../snippets/separator/solid/sizes.tsx?raw';
import separatorSolidVariants from '../../../snippets/separator/solid/variants.tsx?raw';
import separatorSolidVertical from '../../../snippets/separator/solid/vertical.tsx?raw';
import separatorSolidAsChild from '../../../snippets/separator/solid/as-child.tsx?raw';

## Référence de l’API

Ark UI ne fournit pas de primitive dédiée pour les séparateurs. moduix construit ce composant avec la [fabrique Ark](https://ark-ui.com/docs/guides/composition#the-ark-factory) officielle et suit les recommandations d’Ark en matière de [composition](https://ark-ui.com/docs/guides/composition), de [styles](https://ark-ui.com/docs/guides/styling) et de [ref](https://ark-ui.com/docs/guides/ref).

Ses propriétés `orientation`, `variant` et `size` reprennent le modèle familier du `Separator` de Chakra UI. Consultez la [documentation de Chakra UI relative à Separator](https://chakra-ui.com/docs/components/separator), qui a servi de référence pour cette API.

## Exemple de base


Utilisez `Separator` pour marquer visuellement la limite entre des sections liées. Par défaut, le
composant utilise `orientation="horizontal"`, `variant="solid"` et `size="sm"`.

<ExampleFrame variant="component">
  <SeparatorBasic />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/separator/basic.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={separatorSolidBasic} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/separator/separator-basic.module.css"
```
</div>

</ExampleCode>

## Installation avec shadcn

Consultez le [Démarrage rapide](/docs/quick-start) pour la configuration complète du paquet, du registre et des styles.

<Tabs groupId="framework">
  <Tab label="React" value="react">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-react/separator' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-react-tailwind/separator' },
  ]}
/>

  </Tab>
  <Tab label="Solid" value="solid">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-solid/separator' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-solid-tailwind/separator' },
  ]}
/>

  </Tab>
</Tabs>

## Anatomie

`Separator` n’expose qu’une seule partie racine.

```text
Separator
└─ root[data-scope="separator"][data-part="root"][data-slot="separator-root"]
```

| Partie      | Rôle                                                                    |
| ----------- | ----------------------------------------------------------------------- |
| `Separator` | Élément séparateur racine. Définit l’orientation, l’accessibilité et le style. |

## Composition

`Separator` se limite à une partie racine : il ne gère donc aucun état et ne nécessite pas de
provider. Pour un séparateur courant, utilisez `<Separator />`. Lorsqu’un autre élément doit porter
le séparateur, utilisez `asChild` avec
un seul enfant capable d’accepter les propriétés fusionnées de rôle, ARIA, `data-*`, de classe et de
ref. En React, l’élément enfant est transmis directement ; en Solid, utilisez
`asChild={(props) => <hr {...props()} />}`.

## Exemples

### Décoratif

Utilisez `role="presentation"` pour une ligne purement visuelle qui n’apporte aucune structure aux
technologies d’assistance.

<ExampleFrame variant="component">
  <SeparatorDecorative />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/separator/decorative.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={separatorSolidDecorative} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/separator/separator-decorative.module.css"
```
</div>

</ExampleCode>

### Tailles

Utilisez `size` pour choisir l’épaisseur du séparateur, puis les propriétés CSS pour l’ajuster si
nécessaire.

<ExampleFrame variant="component">
  <SeparatorSizes />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/separator/sizes.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={separatorSolidSizes} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/separator/separator-sizes.module.css"
```
</div>

</ExampleCode>

### Variantes

Utilisez `variant` pour choisir une ligne continue, en tirets ou en pointillés.

<ExampleFrame variant="component">
  <SeparatorVariants />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/separator/variants.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={separatorSolidVariants} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/separator/separator-variants.module.css"
```
</div>

</ExampleCode>

### Vertical

Utilisez `orientation="vertical"` pour les groupes disposés sur une ligne, comme des liens de navigation.

<ExampleFrame variant="component">
  <SeparatorVertical />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/separator/vertical.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={separatorSolidVertical} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/separator/separator-vertical.module.css"
```
</div>

</ExampleCode>

### Personnalisation avancée

Utilisez `asChild` lorsqu’un élément hôte précis doit recevoir le rôle de séparateur, les points
d’accroche de style et la `ref` transmise. Dans les autres cas, utilisez directement
`<Separator />`.

<ExampleFrame variant="component">
  <SeparatorAsChild />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/separator/as-child.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={separatorSolidAsChild} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/separator/separator-as-child.module.css"
```
</div>

</ExampleCode>

## Styles

Utilisez `className` en React ou `class` en Solid sur la racine. Les variantes Tailwind utilisent
des classes utilitaires natives et les classes du consommateur ; les propriétés CSS ci-dessous
personnalisent les variantes CSS Modules. Les attributs de données et les sélecteurs `data-slot`
sont communs aux deux modes de style.

### Propriétés CSS

<div className="docs-reference">
  <Tabs
    values={[{ label: 'Variables CSS', value: 'Variables CSS' }]}
    className="docs-reference-tabs"
    contentItemClassName="docs-reference-panel"
  >
    <Tab>
      <SeparatorDocs.SeparatorCssPropertiesPanel />
    </Tab>
  </Tabs>
</div>

### Points d’accroche CSS

- `data-scope="separator"` et `data-part="root"` identifient la partie racine au format Ark.
- `data-slot="separator-root"` est le sélecteur moduix stable.
- `data-orientation="horizontal"` et `data-orientation="vertical"` permettent de cibler les styles propres à chaque axe.
- `data-size="xs|sm|md|lg"` et `data-variant="solid|dashed|dotted"` exposent les paramètres du style visuel.
- `className` ou `class`, ainsi que `style`, s’appliquent directement à l’élément séparateur racine.
