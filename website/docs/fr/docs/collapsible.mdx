---
title: Collapsible
component: collapsible
description: Un composant accessible pour déplier ou replier une seule zone de contenu avec React ou Solid.
---

import * as CollapsibleDocs from '@/components/examples/collapsible/css-properties';
import CollapsibleBasic from '@/components/examples/collapsible/basic.tsx';
import CollapsibleContext from '@/components/examples/collapsible/context.tsx';
import CollapsibleControlled from '@/components/examples/collapsible/controlled.tsx';
import CollapsibleDefaultOpen from '@/components/examples/collapsible/default-open.tsx';
import CollapsibleDisabled from '@/components/examples/collapsible/disabled.tsx';
import CollapsibleLazyMount from '@/components/examples/collapsible/lazy-mount.tsx';
import CollapsibleNested from '@/components/examples/collapsible/nested.tsx';
import CollapsiblePartialCollapse from '@/components/examples/collapsible/partial-collapse.tsx';
import CollapsiblePartialCollapseWidth from '@/components/examples/collapsible/partial-collapse-width.tsx';
import CollapsibleRootProvider from '@/components/examples/collapsible/root-provider.tsx';
import CollapsibleCustomComposition from '@/components/examples/collapsible/custom-composition.tsx';
import collapsibleSolidBasic from '../../../snippets/collapsible/solid/basic.tsx?raw';
import collapsibleSolidContext from '../../../snippets/collapsible/solid/context.tsx?raw';
import collapsibleSolidControlled from '../../../snippets/collapsible/solid/controlled.tsx?raw';
import collapsibleSolidDefaultOpen from '../../../snippets/collapsible/solid/default-open.tsx?raw';
import collapsibleSolidDisabled from '../../../snippets/collapsible/solid/disabled.tsx?raw';
import collapsibleSolidLazyMount from '../../../snippets/collapsible/solid/lazy-mount.tsx?raw';
import collapsibleSolidNested from '../../../snippets/collapsible/solid/nested.tsx?raw';
import collapsibleSolidPartialCollapse from '../../../snippets/collapsible/solid/partial-collapse.tsx?raw';
import collapsibleSolidPartialCollapseWidth from '../../../snippets/collapsible/solid/partial-collapse-width.tsx?raw';
import collapsibleSolidRootProvider from '../../../snippets/collapsible/solid/root-provider.tsx?raw';
import collapsibleSolidCustomComposition from '../../../snippets/collapsible/solid/custom-composition.tsx?raw';

## Référence de l’API

<PrimitiveReference href="https://ark-ui.com/docs/components/collapsible" />

## Choisir le bon composant

Utilisez `Collapsible` pour une seule section indépendante. Préférez `Accordion` lorsque plusieurs
sections liées doivent partager la navigation au clavier ou un état coordonné.

## Exemple de base


Utilisez `Collapsible` avec `Trigger`, `Content` et, si besoin, `Indicator` pour déplier ou replier une
seule zone de contenu. Placez le contenu dans `Collapsible.Body` lorsque vous avez besoin du conteneur
interne recommandé pour les espacements et la mise en page.

<ExampleFrame variant="component">
  <CollapsibleBasic />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/collapsible/basic.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={collapsibleSolidBasic} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/collapsible/collapsible-basic.module.css"
```

</div>

</ExampleCode>

## Installation avec shadcn

Consultez le [Démarrage rapide](/docs/quick-start) pour la configuration complète du paquet, du registre et des styles.

<Tabs groupId="framework">
  <Tab label="React" value="react">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-react/collapsible' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-react-tailwind/collapsible' },
  ]}
/>

  </Tab>
  <Tab label="Solid" value="solid">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-solid/collapsible' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-solid-tailwind/collapsible' },
  ]}
/>

  </Tab>
  </Tabs>

## Anatomie

```text
Collapsible / Collapsible.Root
├─ Collapsible.Trigger
│  ├─ libellé
│  └─ Collapsible.Indicator (facultatif)
└─ Collapsible.Content
   └─ Collapsible.Body
       └─ contenu

Collapsible.RootProvider
└─ même arbre de parties relié à un store useCollapsible()
```

| Partie                             | Rôle                                                                    |
| ---------------------------------- | ----------------------------------------------------------------------- |
| `Collapsible` / `Collapsible.Root` | Gère l’état d’une seule section repliable.                              |
| `Collapsible.RootProvider`         | Relie les parties à l’état créé avec `useCollapsible`.                  |
| `Collapsible.Trigger`              | Bouton accessible qui déplie ou replie le contenu.                       |
| `Collapsible.Indicator`            | Indicateur facultatif qui affiche par défaut un chevron moduix.         |
| `Collapsible.Content`              | Zone animée dont Ark mesure les dimensions.                             |
| `Collapsible.Body`                 | Conteneur interne pour les espacements et la mise en page du contenu.    |

## Composition

`Collapsible` conserve son état dans la racine. Utilisez `defaultOpen` pour une zone non contrôlée, ou
`open` avec `onOpenChange(details)` lorsqu’une autre partie de l’application gère l’état. Le callback
conserve l’objet `details` d’Ark : l’état contrôlé se lit donc avec `details.open`.

Utilisez `Collapsible.RootProvider` avec `useCollapsible()` du paquet correspondant au framework lorsque
le store doit être créé en dehors de l’arbre rendu ou partagé entre plusieurs limites de composition.
Transmettez ce store à `RootProvider` et ne rendez pas une seconde racine `Collapsible` pour le même état.

Placez les espacements et les styles de surface sur `Collapsible.Body`, et non sur
`Collapsible.Content`, afin qu’Ark puisse mesurer correctement la partie animée. `Collapsible.Indicator`
est facultatif et affiche par défaut le chevron moduix.

Utilisez `asChild` uniquement lorsqu’un élément sémantique appartenant à l’application doit recevoir le
comportement du déclencheur ; il doit rester un bouton accessible au clavier. Avec Solid, transmettez une
fonction de rendu, par exemple `asChild={(props) => <button {...props()} />}`.

## Exemples

### Contexte

Utilisez `useCollapsibleContext()` dans un composant descendant lorsqu’il doit lire l’état ou fermer
la zone par programmation.

<ExampleFrame variant="component">
  <CollapsibleContext />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/collapsible/context.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={collapsibleSolidContext} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/collapsible/collapsible-context.module.css"
```

</div>

</ExampleCode>

### État contrôlé

Utilisez `open` avec le callback Ark `onOpenChange(details)` lorsqu’une autre partie de l’interface
gère l’état.

<ExampleFrame variant="component">
  <CollapsibleControlled />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/collapsible/controlled.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={collapsibleSolidControlled} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/collapsible/collapsible-controlled.module.css"
```

</div>

</ExampleCode>

### Ouvert par défaut

Définissez `defaultOpen` pour qu’un composant non contrôlé soit ouvert dès son rendu initial.

<ExampleFrame variant="component">
  <CollapsibleDefaultOpen />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/collapsible/default-open.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={collapsibleSolidDefaultOpen} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/collapsible/collapsible-default-open.module.css"
```

</div>

</ExampleCode>

### État désactivé

Ajoutez `disabled` à la racine pour empêcher l’utilisateur d’ouvrir ou de fermer la zone.

<ExampleFrame variant="component">
  <CollapsibleDisabled />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/collapsible/disabled.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={collapsibleSolidDisabled} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/collapsible/collapsible-disabled.module.css"
```

</div>

</ExampleCode>

### Montage différé

Activez `lazyMount` pour ne monter le contenu qu’à sa première ouverture. Ajoutez `unmountOnExit` pour
le retirer du DOM après sa fermeture.

<ExampleFrame variant="component">
  <CollapsibleLazyMount />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/collapsible/lazy-mount.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={collapsibleSolidLazyMount} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/collapsible/collapsible-lazy-mount.module.css"
```

</div>

</ExampleCode>

### Imbriqué

Imbriquez des racines indépendantes lorsqu’une section ouverte contient un autre niveau de détails facultatif.

<ExampleFrame variant="component">
  <CollapsibleNested />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/collapsible/nested.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={collapsibleSolidNested} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/collapsible/collapsible-nested.module.css"
```

</div>

</ExampleCode>

### Repli partiel

Utilisez `collapsedHeight` ou `collapsedWidth` pour laisser une partie du contenu visible lorsque la zone
est fermée. Les éléments interactifs de cette partie deviennent inertes (`inert`) tant que la zone est
repliée. Placez donc les actions qui doivent rester disponibles en dehors de `Collapsible.Content`.

<ExampleFrame variant="component">
  <CollapsiblePartialCollapse />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/collapsible/partial-collapse.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={collapsibleSolidPartialCollapse} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/collapsible/collapsible-partial-collapse.module.css"
```

</div>

</ExampleCode>

### Largeur partielle

Utilisez `collapsedWidth` lorsqu’un aperçu horizontal est plus utile qu’un aperçu limité en hauteur.

<ExampleFrame variant="component">
  <CollapsiblePartialCollapseWidth />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/collapsible/partial-collapse-width.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={collapsibleSolidPartialCollapseWidth} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/collapsible/collapsible-partial-collapse-width.module.css"
```

</div>

</ExampleCode>

### Root Provider

Créez l’état avec `useCollapsible()` et transmettez-le à `Collapsible.RootProvider` lorsque du code
extérieur à l’arborescence rendue doit y accéder.

<ExampleFrame variant="component">
  <CollapsibleRootProvider />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/collapsible/root-provider.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={collapsibleSolidRootProvider} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/collapsible/collapsible-root-provider.module.css"
```

</div>

</ExampleCode>

### Personnalisation avancée

Utilisez `asChild` pour appliquer le comportement du déclencheur à un bouton défini par votre application.
Les styles par défaut de `Collapsible.Trigger` ne sont alors pas appliqués, mais le comportement Ark et
les attributs d’état sont conservés.

<ExampleFrame variant="component">
  <CollapsibleCustomComposition />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/collapsible/custom-composition.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={collapsibleSolidCustomComposition} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/collapsible/collapsible-custom-composition.module.css"
```

</div>

</ExampleCode>

## Styles

Utilisez `className` avec React ou `class` avec Solid sur les parties exportées pour les styles
structurels. Pour les styles qui dépendent de l’état, privilégiez les attributs de données Ark tels
que `data-state` et `data-disabled`, ainsi que les sélecteurs `data-slot` de moduix. Les variantes
Tailwind utilisent les utilitaires natifs et les classes de l’application ; les variables ci-dessous
personnalisent les variantes CSS Modules. Les attributs de données et les hooks `data-slot` sont
communs aux deux modes de style.

Placez les espacements sur `Collapsible.Body`, et non sur `Collapsible.Content`, afin qu’Ark puisse
mesurer et animer correctement le contenu.

### Propriétés CSS

<CssPropertiesSection properties={CollapsibleDocs.collapsibleOverrideCssProperties} />

### Points d’accroche CSS

- Les parties DOM exposent `data-slot="collapsible-root"`, `collapsible-root-provider`,
  `collapsible-trigger`, `collapsible-indicator`, `collapsible-content` et `collapsible-body`.
- Les parties Ark exposent `data-scope="collapsible"` et les valeurs `data-part` de la racine, du
  déclencheur, de l’indicateur et du contenu.
- Ark ajoute `data-state="open" | "closed"` à la racine, au déclencheur, à l’indicateur et au contenu.
- Ark ajoute `data-collapsible` à la partie `Content`.
- `data-disabled` marque les parties déclencheur, indicateur et contenu lorsqu’elles sont désactivées.
- `data-has-collapsed-size` marque le contenu configuré avec `collapsedHeight` ou `collapsedWidth`.
- L’animation du contenu utilise les variables Ark `--height`, `--width`, `--collapsed-height` et `--collapsed-width`.
