---
title: Color Picker
component: color-picker
description: Un sélecteur de couleurs basé sur Ark UI pour choisir une couleur, modifier ses canaux et sélectionner des couleurs prédéfinies.
---

import * as ColorPickerDocs from '@/components/examples/color-picker/css-properties';
import ColorPickerAdvancedCustomization from '@/components/examples/color-picker/advanced-customization.tsx';
import ColorPickerBasic from '@/components/examples/color-picker/basic.tsx';
import ColorPickerCompactTrigger from '@/components/examples/color-picker/compact-trigger.tsx';
import ColorPickerControlled from '@/components/examples/color-picker/controlled.tsx';
import ColorPickerDisabled from '@/components/examples/color-picker/disabled.tsx';
import ColorPickerFieldState from '@/components/examples/color-picker/field-state.tsx';
import ColorPickerFormUsage from '@/components/examples/color-picker/form-usage.tsx';
import ColorPickerFormatViews from '@/components/examples/color-picker/format-views.tsx';
import ColorPickerInline from '@/components/examples/color-picker/inline.tsx';
import ColorPickerInsideDialog from '@/components/examples/color-picker/inside-dialog.tsx';
import ColorPickerInputOnly from '@/components/examples/color-picker/input-only.tsx';
import ColorPickerOpenControlled from '@/components/examples/color-picker/open-controlled.tsx';
import ColorPickerRootProvider from '@/components/examples/color-picker/root-provider.tsx';
import ColorPickerSliderOnly from '@/components/examples/color-picker/slider-only.tsx';
import ColorPickerSwatchOnly from '@/components/examples/color-picker/swatch-only.tsx';
import ColorPickerSwatches from '@/components/examples/color-picker/swatches.tsx';
import ColorPickerValueSwatch from '@/components/examples/color-picker/value-swatch.tsx';

import colorPickerSolidAdvancedCustomization from '../../../snippets/color-picker/solid/advanced-customization.tsx?raw';
import colorPickerSolidBasic from '../../../snippets/color-picker/solid/basic.tsx?raw';
import colorPickerSolidCompactTrigger from '../../../snippets/color-picker/solid/compact-trigger.tsx?raw';
import colorPickerSolidControlled from '../../../snippets/color-picker/solid/controlled.tsx?raw';
import colorPickerSolidDisabled from '../../../snippets/color-picker/solid/disabled.tsx?raw';
import colorPickerSolidFieldState from '../../../snippets/color-picker/solid/field-state.tsx?raw';
import colorPickerSolidFormUsage from '../../../snippets/color-picker/solid/form-usage.tsx?raw';
import colorPickerSolidFormatViews from '../../../snippets/color-picker/solid/format-views.tsx?raw';
import colorPickerSolidInline from '../../../snippets/color-picker/solid/inline.tsx?raw';
import colorPickerSolidInsideDialog from '../../../snippets/color-picker/solid/inside-dialog.tsx?raw';
import colorPickerSolidInputOnly from '../../../snippets/color-picker/solid/input-only.tsx?raw';
import colorPickerSolidOpenControlled from '../../../snippets/color-picker/solid/open-controlled.tsx?raw';
import colorPickerSolidRootProvider from '../../../snippets/color-picker/solid/root-provider.tsx?raw';
import colorPickerSolidSliderOnly from '../../../snippets/color-picker/solid/slider-only.tsx?raw';
import colorPickerSolidSwatchOnly from '../../../snippets/color-picker/solid/swatch-only.tsx?raw';
import colorPickerSolidSwatches from '../../../snippets/color-picker/solid/swatches.tsx?raw';
import colorPickerSolidValueSwatch from '../../../snippets/color-picker/solid/value-swatch.tsx?raw';

## Référence de l’API

<PrimitiveReference href="https://ark-ui.com/docs/components/color-picker" />

## Choisir le bon composant

Choisissez `ColorPicker` lorsque l’utilisateur doit sélectionner une couleur ou l’ajuster avec précision. Si une valeur
connue doit simplement être saisie, sans modifier directement la couleur, utilisez `Input`.

## Exemple de base

Commencez par un champ compact composé du composant racine, d’un champ pour un canal, de la pastille par défaut du bouton
d’ouverture et du contenu explicite de la fenêtre contextuelle. Les parties internes peuvent rester sans enfants : le
composant leur applique alors sa composition intégrée. Ajoutez explicitement `ColorPicker.HiddenInput` et un `name` si
la couleur doit être envoyée avec le formulaire.

<ExampleFrame variant="component">
  <ColorPickerBasic />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/color-picker/basic.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={colorPickerSolidBasic} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/color-picker/color-picker-basic.module.css"
```
</div>

</ExampleCode>

## Installation avec shadcn

<Tabs groupId="framework">
  <Tab label="React" value="react">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-react/color-picker' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-react-tailwind/color-picker' },
  ]}
/>

  </Tab>
  <Tab label="Solid" value="solid">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-solid/color-picker' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-solid-tailwind/color-picker' },
  ]}
/>

  </Tab>
</Tabs>

## Anatomie

```text
ColorPicker / ColorPicker.Root
├─ ColorPicker.Label
├─ ColorPicker.Control
│  ├─ ColorPicker.ChannelInput[channel]
│  └─ ColorPicker.Trigger
│     ├─ ColorPicker.TransparencyGrid
│     └─ ColorPicker.ValueSwatch
├─ Sous-arbre de la fenêtre contextuelle (rendu par défaut via un portail)
│  └─ ColorPicker.Positioner
│     └─ ColorPicker.Content
│        ├─ ColorPicker.Area
│        │  ├─ ColorPicker.AreaBackground
│        │  └─ ColorPicker.AreaThumb
│        ├─ ColorPicker.EyeDropperTrigger
│        ├─ ColorPicker.ChannelSlider[channel]
│        │  ├─ ColorPicker.TransparencyGrid
│        │  ├─ ColorPicker.ChannelSliderTrack
│        │  └─ ColorPicker.ChannelSliderThumb
│        ├─ ColorPicker.SwatchGroup
│        │  └─ ColorPicker.SwatchTrigger[value]
│        │     └─ ColorPicker.Swatch[value]
│        │        └─ ColorPicker.SwatchIndicator
│        └─ ColorPicker.View[format]
│           └─ ColorPicker.ChannelInput[channel]
└─ ColorPicker.HiddenInput (explicite)
```

| Partie | Rôle |
| --- | --- |
| `ColorPicker` / `ColorPicker.Root` | Gère la couleur et son format, l’ouverture de la fenêtre contextuelle et la participation au formulaire. |
| `ColorPicker.HiddenInput` | Champ natif d’Ark ; composition explicite décrite ci-dessous. |
| `ColorPicker.Control` | Regroupe les champs visibles et le bouton d’ouverture. |
| `ColorPicker.Trigger` | Bouton qui ouvre la fenêtre contextuelle et affiche par défaut la pastille de la couleur actuelle. |
| `ColorPicker.Content` | Contenu de la fenêtre contextuelle basée sur Ark. |
| `ColorPicker.Area` | Zone bidimensionnelle de sélection de la couleur. |
| `ColorPicker.ChannelSlider` | Contrôle unidimensionnel de la teinte, du canal alpha ou d’un autre canal. |
| `ColorPicker.Sliders` | Paire intégrée de curseurs pour la teinte et le canal alpha. |
| `ColorPicker.ChannelInput` | Champ texte ou numérique pour un canal de couleur donné. |
| `ColorPicker.SwatchTrigger` | Bouton de sélection d’une couleur prédéfinie. |
| `ColorPicker.ValueSwatch` | Affiche une pastille de la couleur actuelle. |

`ColorPicker.RootProvider`, `parseColor`, `useColorPicker` et `useColorPickerContext` couvrent les scénarios avancés
habituels de gestion d’état. Importez directement les types auxiliaires d’Ark uniquement si nécessaire.

## Composition

Utilisez `defaultValue` pour un état local. Lorsque l’application doit contrôler la couleur sélectionnée ou l’ouverture, passez `value` avec `onValueChange(details)` ou `open` avec `onOpenChange(details)`. Pour créer l’état en dehors de l’arbre rendu, passez le résultat de `useColorPicker()` à `ColorPicker.RootProvider`; ne rendez pas `ColorPicker` ou `ColorPicker.Root` pour le même état.

`ColorPicker.Positioner` est rendu dans un portail par défaut. Définissez `portalled={false}` lorsque la palette doit rester dans un autre contexte d’empilement. Pour inclure la couleur dans un formulaire natif, définissez `name` sur la racine et rendez explicitement `ColorPicker.HiddenInput`. `Area`, `ChannelSlider` et `SwatchTrigger` fournissent leurs parties visuelles par défaut; ne passez des enfants que pour modifier leur structure.

## Exemples

### Zone et curseurs

Utilisez cette composition compacte lorsque les utilisateurs ont besoin de la zone de couleur et des curseurs de canaux,
mais pas de champs pour saisir les canaux au clavier.

<ExampleFrame variant="component">
  <ColorPickerSliderOnly />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/color-picker/slider-only.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={colorPickerSolidSliderOnly} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Bouton d’ouverture compact

Utilisez `data-fit-content` sur `ColorPicker.Trigger` lorsque la largeur du bouton doit suivre son contenu plutôt que
former une pastille carrée. Ce format convient par exemple pour remplacer un champ de couleur basé sur un bouton.

<ExampleFrame variant="component">
  <ColorPickerCompactTrigger />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/color-picker/compact-trigger.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={colorPickerSolidCompactTrigger} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/color-picker/color-picker-compact-trigger.module.css"
```
</div>

</ExampleCode>

### Valeur contrôlée

Utilisez une valeur contrôlée lorsque la couleur sélectionnée doit rester synchronisée avec l’état de l’application.

<ExampleFrame variant="component">
  <ColorPickerControlled />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/color-picker/controlled.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={colorPickerSolidControlled} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Composant désactivé

Définissez `disabled` sur le composant racine lorsque toutes les parties doivent devenir inactives tout en laissant la
couleur actuelle visible.

<ExampleFrame variant="component">
  <ColorPickerDisabled />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/color-picker/disabled.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={colorPickerSolidDisabled} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### État du champ

Utilisez les propriétés d’état du formulaire du composant racine, comme `required` et `invalid`, lorsque le sélecteur se
trouve dans un conteneur de champ et doit exposer cet état avec les attributs de données d’Ark.

<ExampleFrame variant="component">
  <ColorPickerFieldState />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/color-picker/field-state.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={colorPickerSolidFieldState} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/color-picker/color-picker-field-state.module.css"
```
</div>

</ExampleCode>

### Utilisation dans un formulaire

Définissez `name` sur `ColorPicker` et ajoutez explicitement `ColorPicker.HiddenInput`.

<ExampleFrame variant="component">
  <ColorPickerFormUsage />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/color-picker/form-usage.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={colorPickerSolidFormUsage} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Vues selon le format

Utilisez `format`, `onFormatChange`, `FormatSelect` et `View` lorsque les utilisateurs doivent passer d’un groupe de
champs de canaux à un autre sans modifier la structure du sélecteur. `FormatSelect` conserve le sélecteur de format natif
d’Ark, tandis que son affichage replié reprend le style du champ `Select` de moduix et présente uniformément les formats
RGBA, HSLA et HSBA. Le navigateur gère la liste des options.

<ExampleFrame variant="component">
  <ColorPickerFormatViews />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/color-picker/format-views.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={colorPickerSolidFormatViews} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/color-picker/color-picker-format-views.module.css"
```
</div>

</ExampleCode>

### Affichage en ligne

Utilisez `inline` dans les panneaux de réglages lorsque les outils de couleur doivent rester visibles sans bouton
d’ouverture de fenêtre contextuelle.

<ExampleFrame variant="component">
  <ColorPickerInline />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/color-picker/inline.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={colorPickerSolidInline} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/color-picker/color-picker-inline.module.css"
```
</div>

</ExampleCode>

### Saisie directe

Utilisez des champs de canaux avec une pastille passive lorsque la saisie au clavier est importante et qu’une fenêtre
contextuelle ajouterait une complexité inutile à l’interface.

<ExampleFrame variant="component">
  <ColorPickerInputOnly />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/color-picker/input-only.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={colorPickerSolidInputOnly} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/color-picker/color-picker-input-only.module.css"
```
</div>

</ExampleCode>

### Dans un Dialog

Lorsque le sélecteur se trouve dans un dialogue modal, désactivez le portail pour sa fenêtre contextuelle. Elle reste ainsi
dans le même contexte d’empilement que le dialogue et demeure interactive au-dessus de son contenu.

<ExampleFrame variant="component">
  <ColorPickerInsideDialog />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/color-picker/inside-dialog.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={colorPickerSolidInsideDialog} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/color-picker/color-picker-inside-dialog.module.css"
```
</div>

</ExampleCode>

### Ouverture contrôlée

Utilisez l’état contrôlé `open` lorsqu’un autre contrôle ou un autre flux détermine la visibilité de la fenêtre contextuelle.

<ExampleFrame variant="component">
  <ColorPickerOpenControlled />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/color-picker/open-controlled.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={colorPickerSolidOpenControlled} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/color-picker/color-picker-open-controlled.module.css"
```
</div>

</ExampleCode>

### RootProvider

Utilisez `useColorPicker` de moduix avec `RootProvider` lorsque l’état du sélecteur doit être créé en dehors de l’arbre
rendu et partagé avec des contrôles voisins.

<ExampleFrame variant="component">
  <ColorPickerRootProvider />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/color-picker/root-provider.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={colorPickerSolidRootProvider} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/color-picker/color-picker-root-provider.module.css"
```
</div>

</ExampleCode>

### Pastilles uniquement

Utilisez une composition limitée aux pastilles lorsque la palette prédéfinie suffit et que la zone, les curseurs et les
champs sont inutiles.

<ExampleFrame variant="component">
  <ColorPickerSwatchOnly />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/color-picker/swatch-only.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={colorPickerSolidSwatchOnly} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/color-picker/color-picker-swatch-only.module.css"
```
</div>

</ExampleCode>

### Palette prédéfinie

Ajoutez des pastilles dans la fenêtre contextuelle lorsque le sélecteur doit proposer une palette prédéfinie en plus de la
sélection libre. `closeOnSelect` conserve le comportement d’Ark et ferme la fenêtre après la sélection d’une pastille.

<ExampleFrame variant="component">
  <ColorPickerSwatches />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/color-picker/swatches.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={colorPickerSolidSwatches} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Valeur actuelle

Utilisez `ValueSwatch` et `ValueText` lorsque l’interface voisine doit afficher la couleur choisie sans ouvrir le
sélecteur complet.

<ExampleFrame variant="component">
  <ColorPickerValueSwatch />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/color-picker/value-swatch.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={colorPickerSolidValueSwatch} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/color-picker/color-picker-value-swatch.module.css"
```
</div>

</ExampleCode>

### Personnalisation avancée

Utilisez des parties et des enfants explicites lorsque la composition intégrée ne suffit pas et qu’une autre structure
visuelle est nécessaire.

<ExampleFrame variant="component">
  <ColorPickerAdvancedCustomization />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/color-picker/advanced-customization.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={colorPickerSolidAdvancedCustomization} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

## Styles

Utilisez `className` en React ou `class` en Solid sur les parties exportées. Les variantes Tailwind utilisent les utilitaires natifs et les classes de votre application; les variantes CSS Modules utilisent les variables CSS publiques `--moduix-color-picker-*` présentées ci-dessous. Les deux versions exposent les hooks stables `data-slot="color-picker-*"` et les attributs Ark `data-scope` / `data-part`.
### Propriétés CSS

<CssPropertiesSection properties={ColorPickerDocs.colorPickerOverrideCssProperties} />

### Sélecteurs de style

Utilisez la partie qui contrôle l’aspect visuel concerné :

- `ColorPicker` pour la largeur, les espacements, la couleur du texte et l’opacité de l’état désactivé ;
- `ColorPicker.Control`, `ChannelInput`, `Trigger`, `FormatSelect` et `FormatTrigger` pour les éléments visibles du champ ;
- `ColorPicker.Positioner` et `ColorPicker.Content` pour la couche, la largeur, les espacements, l’arrondi, l’ombre et
  les animations d’état de la fenêtre contextuelle ;
- `ColorPicker.Area`, `AreaBackground` et `AreaThumb` pour la zone de couleur bidimensionnelle ;
- `ColorPicker.ChannelSlider`, `ChannelSliderTrack`, `ChannelSliderThumb` et `Sliders` pour les curseurs de teinte,
  de canal alpha et des autres canaux ;
- `ColorPicker.SwatchGroup`, `SwatchTrigger`, `Swatch` et `SwatchIndicator` pour les grilles de couleurs prédéfinies ;
- `ColorPicker.ValueSwatch`, `ValueText`, `View` et `TransparencyGrid` pour l’affichage de la valeur et la mise en page
  adaptée au format choisi.

Les principaux attributs d’état sont `data-state`, `data-focus`, `data-invalid`, `data-disabled`, `data-readonly`,
`data-required`, `data-channel`, `data-orientation`, `data-value`, `data-placement` et `data-side`. Les variables Ark
disponibles à l’exécution comprennent `--color`, `--value`, `--available-width`, `--available-height` et
`--transform-origin`.

Lorsque `prefers-reduced-motion` est activé, les animations d’ouverture et de fermeture de la fenêtre contextuelle durent
1 ms. Ark peut ainsi terminer son cycle de fermeture sans mouvement visible.
