---
title: Select
component: select
description: Composition Select basée sur Ark UI pour choisir une ou plusieurs valeurs dans une liste contextuelle.
---

import * as SelectDocs from '@/components/examples/select/css-properties';
import SelectAdvancedCustomization from '@/components/examples/select/advanced-customization.tsx';
import SelectBasic from '@/components/examples/select/basic.tsx';
import SelectControlled from '@/components/examples/select/controlled.tsx';
import SelectCustomItemLayout from '@/components/examples/select/custom-item-layout.tsx';
import SelectDynamicItems from '@/components/examples/select/dynamic-items.tsx';
import SelectField from '@/components/examples/select/field.tsx';
import SelectFormUsage from '@/components/examples/select/form-usage.tsx';
import SelectGrouping from '@/components/examples/select/grouping.tsx';
import SelectLazyMount from '@/components/examples/select/lazy-mount.tsx';
import SelectMaxSelection from '@/components/examples/select/max-selection.tsx';
import SelectMultiple from '@/components/examples/select/multiple.tsx';
import SelectNativeFormControl from '@/components/examples/select/native-form-control.tsx';
import SelectOverflow from '@/components/examples/select/overflow.tsx';
import SelectRootProvider from '@/components/examples/select/root-provider.tsx';
import SelectSearchInPopup from '@/components/examples/select/search-in-popup.tsx';
import SelectSelectAll from '@/components/examples/select/select-all.tsx';
import SelectSelectOnHighlight from '@/components/examples/select/select-on-highlight.tsx';

## Référence de l’API

<PrimitiveReference href="https://ark-ui.com/docs/components/select" />

## Choisir le bon composant

Utilisez `Select` lorsque l’utilisateur choisit une valeur dans une collection fixe et ne saisit pas
de texte dans le déclencheur. Préférez `Combobox` lorsqu’une saisie permet de rechercher ou de
créer une valeur.

## Exemple de base

Passez une collection à `Select`, utilisez `Select.Field` pour le champ visible standard et gardez
la structure de la fenêtre contextuelle explicite. La propriété `clearLabel` ajoute un bouton
d’effacement accessible. Ajoutez explicitement `Select.HiddenSelect` lorsque la soumission, le
remplissage automatique, la validation et la réinitialisation natives sont nécessaires.

<ExampleFrame variant="component">
  <SelectBasic />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/select/basic.tsx"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

## Installation avec shadcn

<ShadcnInstall packageName="select" />

## Anatomie

```text
Select / Select.Root
├─ Select.Label
├─ Select.Control / Select.Field
│  ├─ Select.Trigger
│  │  ├─ Select.ValueText
│  ├─ Select.ClearTrigger
│  └─ Select.Indicator
├─ Sous-arbre de l’overlay (portail automatique)
│  └─ Select.Positioner
│     └─ Select.Content
│        ├─ Select.List
│        └─ Select.ItemGroup
│           ├─ Select.ItemGroupLabel
│           └─ Select.Item[item]
│              ├─ Select.ItemText
│              │  └─ Select.ItemTextContent
│              │     ├─ Select.ItemTextIcon
│              │     └─ Select.ItemTextLabel
│              └─ Select.ItemIndicator
└─ Select.HiddenSelect (explicite)
```

| Partie                    | Rôle                                                                                             |
| ------------------------- | ------------------------------------------------------------------------------------------------ |
| `Select` / `Select.Root`  | État racine, collection, sélection, cycle de vie de la fenêtre contextuelle et données du formulaire. |
| `Select.HiddenSelect` | Champ natif d’Ark ; composition explicite décrite ci-dessous. |
| `Select.RootProvider`     | Affiche l’arborescence Select à partir de l’état renvoyé par `Select.useSelect`.                 |
| `Select.Label`            | Libellé accessible associé au déclencheur et au contrôle de formulaire natif.                   |
| `Select.Control`          | Enveloppe du champ visible qui reflète les états de focus, d’erreur et d’ouverture.              |
| `Select.Field`            | Composant utilitaire moduix pour le champ, le déclencheur et les actions standard.               |
| `Select.Trigger`          | Bouton qui ouvre la fenêtre contextuelle et gère la saisie au clavier.                          |
| `Select.ValueText`        | Placeholder ou libellés des éléments sélectionnés.                                               |
| `Select.ClearTrigger`     | Bouton qui efface la valeur actuelle.                                                            |
| `Select.Indicator`        | Indicateur du déclencheur avec l’icône de flèche moduix par défaut.                              |
| `Select.Positioner`       | Calque flottant qui expose les variables CSS Ark pour le positionnement.                        |
| `Select.Content`          | Contenu de la fenêtre contextuelle et conteneur de la listbox.                                   |
| `Select.List`             | Enveloppe de liste facultative pour la virtualisation et les mises en page personnalisées.      |
| `Select.ItemGroup`        | Enveloppe d’un groupe d’éléments associés de la collection.                                     |
| `Select.ItemGroupLabel`   | Libellé d’un groupe de la collection.                                                            |
| `Select.Item`             | Ligne sélectionnable. Passez l’élément de collection avec `item`.                               |
| `Select.ItemText`         | Texte utilisé pour l’affichage et l’accessibilité.                                               |
| `Select.ItemTextContent`  | Composant de mise en page pour du texte d’élément enrichi.                                       |
| `Select.ItemTextIcon`     | Conteneur d’icône pour du texte d’élément enrichi.                                               |
| `Select.ItemTextLabel`    | Conteneur de libellé pour du texte d’élément enrichi.                                            |
| `Select.ItemIndicator`    | Indicateur de sélection avec l’icône de coche moduix par défaut.                                 |

## Composition

### Champ de formulaire natif

`Select.HiddenSelect` est le select natif d’Ark, exporté directement. Ajoutez-le explicitement
dans `Select` ou `Select.RootProvider` : la racine ne le crée pas automatiquement.
Définissez `name` sur la racine pour la soumission et `form` pour un formulaire externe.
Le champ prend aussi en charge la validation du navigateur et la réinitialisation.

La racine rend automatiquement `Positioner` via un portail. Utilisez `portalled={false}` pour un
rendu dans le flux normal et `portalRef` pour cibler un conteneur personnalisé. `Positioner` et
`Content` restent explicites et peuvent être stylisés séparément.

### Composition recommandée

`Select.Field` affiche `Control`, `Trigger`, `ValueText` et l’indicateur par défaut. Transmettez
`clearLabel` pour ajouter une action d’effacement avec libellé, ou `indicator={<Icon />}` pour
remplacer uniquement l’icône de flèche. Gardez `Positioner`, `Content` et les éléments de la liste
explicitement dans la composition.

`Select` est la forme abrégée de la racine. `Select.Root` est le membre équivalent de l’espace de
noms.

### Quand créer une composition personnalisée

Composez explicitement `Control`, `Trigger` et les parties visuelles lorsque chacune doit être
assemblée et stylisée indépendamment. `asChild` exige un conteneur sémantique unique capable de
recevoir les props Ark et d’afficher ses enfants. Utilisez `div` ou `section`, et
non un élément interactif tel que `button`. `Select.List` sert aux enveloppes de liste personnalisées
et à la virtualisation.

### Propriétés par défaut

Par défaut, le contenu de la fenêtre contextuelle est monté uniquement à la première ouverture, puis
démonté après l’animation de fermeture. Définissez `unmountOnExit={false}` pour le conserver après
la première ouverture. Ne définissez les deux propriétés du cycle de vie sur `false` que si un rendu
initial immédiat est nécessaire.

| Propriété                | Valeur par défaut | Remarques                                                                     |
| ------------------------ | ----------------- | ----------------------------------------------------------------------------- |
| `closeOnSelect`          | `true`            | Ferme la fenêtre contextuelle après la sélection d’un élément.                |
| `composite`              | `true`            | Active la navigation au clavier dans la listbox.                              |
| `defaultValue`           | `[]`              | Tableau de chaînes, y compris en sélection unique.                            |
| `lazyMount`              | `true`            | Diffère le montage du contenu jusqu’à l’ouverture.                            |
| `loopFocus`              | `false`           | Par défaut, la navigation avec les flèches ne revient pas au début.           |
| `multiple`               | `false`           | Autorise plusieurs valeurs lorsqu’elle est activée.                           |
| `unmountOnExit`          | `true`            | Supprime le contenu après l’animation de fermeture.                           |
| `ValueText.placeholder`  | -                 | Texte affiché lorsque le tableau de valeurs est vide.                         |

### Comportement

`value` et `defaultValue` sont des tableaux de chaînes de caractères, y compris en sélection unique.
Le callback `onValueChange(details)` expose les clés sélectionnées dans `details.value` et les
éléments de la collection dans `details.items`. Les callbacks `onOpenChange(details)`,
`onHighlightChange(details)` et `onSelect(details)` reçoivent les objets details d’Ark UI sans
modification.

Utilisez `Select.useSelect` avec `Select.RootProvider` lorsque l’état doit être lu en dehors de
l’arborescence Select. Pour lire l’état à l’intérieur de cette arborescence, utilisez
`Select.Context`, `Select.ItemContext`, `Select.useSelectContext` et `Select.useSelectItemContext`.
`Field.Root` et `Fieldset.Root` transmettent via le contexte Ark les états `disabled`, `invalid`,
`required` et `read-only`.

`Select.HiddenSelect` conserve la validation du navigateur, le remplissage automatique, la
réinitialisation du formulaire et l’amélioration progressive. Pour une collection virtualisée où
le rendu de toutes les options natives est indésirable, fournissez votre propre intégration de formulaire.

Lorsque la réduction des mouvements est activée, Select désactive les transitions du déclencheur et
les animations d’ouverture et de fermeture de la fenêtre contextuelle. La sélection, la restauration
du focus et le cycle de montage restent inchangés.

## Exemples

### État contrôlé

Contrôlez les valeurs sélectionnées depuis l’état React et mettez-les à jour avec `details.value`.

<ExampleFrame variant="component">
  <SelectControlled />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/select/controlled.tsx"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Disposition personnalisée des éléments

Utilisez `Select.ItemTextContent`, `Select.ItemTextIcon` et `Select.ItemTextLabel` dans `ItemText`
lorsqu’un élément doit afficher une icône stable et un libellé tronqué.

<ExampleFrame variant="component">
  <SelectCustomItemLayout />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/select/custom-item-layout.tsx"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Éléments dynamiques

Mettez à jour la collection lorsque les éléments disponibles changent. La composition Select reste
identique.

<ExampleFrame variant="component">
  <SelectDynamicItems />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/select/dynamic-items.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/select/select-dynamic-items.module.css"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Champ de formulaire

Enveloppez Select dans `Field.Root` lorsque le libellé, le texte d’aide ou les états `invalid`,
`required` et `disabled` s’appliquent au champ de formulaire.

<ExampleFrame variant="component">
  <SelectField />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/select/field.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/select/select-field.module.css"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Utilisation dans un formulaire

Le `Select.HiddenSelect` explicitement composé préserve la soumission native du formulaire. Lisez les
valeurs envoyées depuis `FormData`.

<ExampleFrame variant="component">
  <SelectFormUsage />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/select/form-usage.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/select/select-form-usage.module.css"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Regroupement

Définissez `groupBy` dans la collection et affichez `collection.group()` avec `ItemGroup` et
`ItemGroupLabel`.

<ExampleFrame variant="component">
  <SelectGrouping />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/select/grouping.tsx"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Montage différé

Utilisez `lazyMount` et `unmountOnExit` lorsque le contenu de la fenêtre contextuelle ne doit être
monté que pendant l’utilisation de Select.

<ExampleFrame variant="component">
  <SelectLazyMount />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/select/lazy-mount.tsx"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Nombre maximal d’éléments sélectionnés

Limitez le nombre d’éléments sélectionnés en rejetant les tableaux trop longs et en désactivant les
éléments restants.

<ExampleFrame variant="component">
  <SelectMaxSelection />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/select/max-selection.tsx"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Sélection multiple

Définissez `multiple` pour autoriser plusieurs valeurs sélectionnées. `ValueText` affiche les
libellés des éléments sélectionnés à partir de la collection.

<ExampleFrame variant="component">
  <SelectMultiple />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/select/multiple.tsx"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Contrôle de formulaire natif

Composez explicitement `Select.HiddenSelect` lorsque la sélection doit participer à un formulaire
natif. Cet exemple montre les valeurs envoyées dans `FormData`.

<ExampleFrame variant="component">
  <SelectNativeFormControl />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/select/native-form-control.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/select/select-native-form-control.module.css"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Débordement

Pour les longues listes, utilisez `positioning.fitViewport`. Le contenu limite déjà sa hauteur avec
la variable Ark `--available-height` et reste défilable.

<ExampleFrame variant="component">
  <SelectOverflow />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/select/overflow.tsx"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Root Provider

Utilisez `Select.useSelect` avec `Select.RootProvider` lorsque l’état doit être lu ou contrôlé en
dehors de l’arborescence Select rendue.

<ExampleFrame variant="component">
  <SelectRootProvider />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/select/root-provider.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/select/select-root-provider.module.css"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Recherche dans la fenêtre contextuelle

Placez un contrôle de filtrage à côté de la listbox dans la fenêtre contextuelle. Conservez l’état de
la requête et la collection filtrée dans votre composant, sans étendre l’API Select.

<ExampleFrame variant="component">
  <SelectSearchInPopup />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/select/search-in-popup.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/select/select-search-in-popup.module.css"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Tout sélectionner

Placez les actions groupées en dehors de la listbox. `Select.useSelect` permet d’utiliser l’action et
Select avec la même instance d’état Ark.

<ExampleFrame variant="component">
  <SelectSelectAll />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/select/select-all.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/select/select-select-all.module.css"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Sélection lors du déplacement dans la liste

Utilisez `onHighlightChange(details)` lorsque le déplacement entre les options doit mettre
immédiatement à jour la sélection.

<ExampleFrame variant="component">
  <SelectSelectOnHighlight />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/select/select-on-highlight.tsx"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Personnalisation avancée

N’utilisez pas `Select.Field` lorsque le déclencheur, le texte de la valeur ou l’indicateur doivent
être composés et stylisés indépendamment.

<ExampleFrame variant="component">
  <SelectAdvancedCustomization />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/select/advanced-customization.tsx"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

## Styles

Personnalisez Select avec `className`, les hooks stables `data-slot="select-*"`, les attributs Ark
et les variables CSS publiques `--moduix-select-*`.

### Propriétés CSS

<CssPropertiesSection properties={SelectDocs.selectOverrideCssProperties} />

### Points d’accroche CSS

`Select.Field` affiche les slots existants `select-control`, `select-trigger`, `select-value-text`,
`select-indicator` et le slot facultatif `select-clear-trigger`. Sa propriété `indicator` fournit
l’enfant du slot d’indicateur existant. Les composants de texte enrichi exposent les slots
`select-item-text-content`, `select-item-text-icon` et `select-item-text-label`.

- Les racines fournissent `data-slot="select-root"` et `data-slot="select-root-provider"`.
- L’anatomie Ark utilise `data-scope="select"` et `data-part`.
- Le style des états utilise `data-state="open" | "closed"`, `data-focus`, `data-invalid`,
  `data-disabled`, `data-readonly`, `data-required`, `data-placeholder-shown`,
  `data-highlighted` et, pour les éléments, `data-state="checked" | "unchecked"`.
- `Select.Positioner` fournit `--reference-width`, `--reference-height`, `--available-width`,
  `--available-height`, `--x`, `--y`, `--z-index` et `--transform-origin`.
