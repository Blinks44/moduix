---
title: Slider
component: slider
description: Un contrôle permettant de sélectionner une valeur unique ou une plage de valeurs dans un intervalle défini.
---

import * as SliderDocs from '@/components/examples/slider/css-properties';
import SliderAdvancedCustomization from '@/components/examples/slider/advanced-customization.tsx';
import SliderBasic from '@/components/examples/slider/basic.tsx';
import SliderCenterOrigin from '@/components/examples/slider/center-origin.tsx';
import SliderChangeEvents from '@/components/examples/slider/change-events.tsx';
import SliderContext from '@/components/examples/slider/context.tsx';
import SliderControlled from '@/components/examples/slider/controlled.tsx';
import SliderDisabled from '@/components/examples/slider/disabled.tsx';
import SliderDraggingIndicator from '@/components/examples/slider/dragging-indicator.tsx';
import SliderForm from '@/components/examples/slider/form.tsx';
import SliderInvalid from '@/components/examples/slider/invalid.tsx';
import SliderMarks from '@/components/examples/slider/marks.tsx';
import SliderMinAndMax from '@/components/examples/slider/min-and-max.tsx';
import SliderRange from '@/components/examples/slider/range.tsx';
import SliderReadOnly from '@/components/examples/slider/read-only.tsx';
import SliderRootProvider from '@/components/examples/slider/root-provider.tsx';
import SliderStep from '@/components/examples/slider/step.tsx';
import SliderThumbAlignment from '@/components/examples/slider/thumb-alignment.tsx';
import SliderThumbCollision from '@/components/examples/slider/thumb-collision.tsx';
import SliderThumbOverlap from '@/components/examples/slider/thumb-overlap.tsx';
import SliderVertical from '@/components/examples/slider/vertical.tsx';
import SliderVerticalWithMarks from '@/components/examples/slider/vertical-with-marks.tsx';

## Référence de l’API

<PrimitiveReference href="https://ark-ui.com/docs/components/slider" />

## Choisir le bon composant

Utilisez `Slider` lorsque l’utilisateur doit ajuster une valeur numérique dans un intervalle défini, au pointeur ou au clavier.

- Choisissez **`Slider`** pour le volume, la luminosité, les budgets, les seuils et les plages à plusieurs poignées.
- Choisissez **`Input`** ou **`NumberInput`** lorsque l’utilisateur doit saisir une valeur exacte plutôt que la choisir en faisant glisser le curseur.
- Choisissez **`ProgressLinear`** ou **`ProgressCircular`** lorsque la valeur sert uniquement à l’affichage.

## Exemple de base

`Slider.Thumbs` est le raccourci visuel recommandé. Pour participer à un formulaire natif, composez
chaque `Slider.Thumb` explicitement et placez `Slider.HiddenInput` à l’intérieur.

<ExampleFrame variant="component">
  <SliderBasic />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/slider/basic.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/slider/slider-basic.module.css"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

## Installation avec shadcn

<ShadcnInstall packageName="slider" />

## Anatomie

```text
Slider / Slider.Root
├─ Slider.Label
├─ Slider.ValueText
├─ Slider.Control
│  ├─ Slider.Track
│  │  └─ Slider.Range
│  └─ Slider.Thumbs
│     └─ Slider.Thumb[index]
│        ├─ Slider.DraggingIndicator
│        └─ Slider.HiddenInput (explicite)
└─ Slider.MarkerGroup
   └─ Slider.Marker[value]
```

| Partie                                  | Rôle                                                                    |
| --------------------------------------- | ----------------------------------------------------------------------- |
| `Slider` / `Slider.Root`               | Racine Ark stylisée pour un état contrôlé ou non contrôlé.              |
| `Slider.RootProvider`                  | Racine connectée à un état `useSlider` créé à l’extérieur.             |
| `Slider.Label`                         | Libellé visible du groupe.                                              |
| `Slider.ValueText`                     | Texte de la valeur actuelle généré par Ark.                             |
| `Slider.Control`                       | Zone d’interaction au pointeur.                                         |
| `Slider.Track`                         | Piste visuelle complète.                                                |
| `Slider.Range`                         | Partie remplie entre l’origine configurée et la valeur.                 |
| `Slider.Thumb`                         | Poignée du slider qui peut recevoir le focus.                           |
| `Slider.Thumbs`                        | Utilitaire moduix qui génère une poignée par valeur.                    |
| `Slider.MarkerGroup` / `Slider.Marker` | Groupe facultatif de repères et repère individuel.                      |
| `Slider.DraggingIndicator`             | Bulle de valeur facultative affichée pendant le glissement.             |

## Composition

### Composition recommandée

Utilisez l’arbre de parties Ark : racine, libellé, texte de valeur facultatif, zone de contrôle, piste, plage et
`Slider.Thumbs`. Le composant génère une poignée visuelle par valeur. Utilisez des poignées explicites pour les champs masqués.

`Slider` est la forme abrégée de la racine, équivalente à `Slider.Root`.

### Quand créer une composition personnalisée

Utilisez `asChild` lorsqu’une partie doit être rendue avec un élément personnalisé. Conservez un seul enfant sémantique
capable de recevoir les propriétés Ark. Utilisez un `Slider.Thumb` explicite lorsque certaines poignées nécessitent des
enfants personnalisés, des noms accessibles, un ordre particulier ou un `Slider.HiddenInput` imbriqué.
Pour accéder à l’état de façon avancée, utilisez `Slider.Context` ou `useSliderContext` pour lire l’état sous la racine.
Pour créer l’état en dehors de l’arbre affiché, importez `useSlider` depuis `@moduix/react/slider` et utilisez-le avec
`Slider.RootProvider`.

### Propriétés par défaut

| Propriété                 | Par défaut   | Remarques                                                               |
| ------------------------- | ------------ | ----------------------------------------------------------------------- |
| `defaultValue`            | `[min]`      | Ark stocke les valeurs sous la forme `number[]`.                        |
| `orientation`             | `horizontal` | Utilisez `vertical` pour un axe clavier vertical.                       |
| `min`                     | `0`          | Limite inférieure.                                                      |
| `max`                     | `100`        | Limite supérieure.                                                      |
| `step`                    | `1`          | Pas utilisé par le pointeur et le clavier.                              |
| `largeStep`               | `10 * step`  | Pas utilisé par Page Up, Page Down et les touches Shift + flèche.        |
| `minStepsBetweenThumbs`   | `0`          | Nombre minimal de pas entre les poignées d’une plage.                   |
| `origin`                  | `start`      | Origine du remplissage pour un slider à valeur unique.                  |
| `thumbAlignment`          | `contain`    | Utilisez `center` pour laisser la poignée dépasser de la piste.          |
| `thumbCollisionBehavior`  | `none`       | Utilisez `push` ou `swap` pour modifier les collisions.                 |
| `thumbSize`               | mesuré       | Remplace les dimensions de poignée mesurées par Ark.                    |

`value`, `defaultValue`, `onValueChange(details)` et `onValueChangeEnd(details)` utilisent les objets `details` d’Ark.
Lisez le tableau dans `details.value`. `onFocusChange(details)` indique la poignée qui reçoit le focus. Lorsque
`defaultValue` est omis, la valeur initiale est `[min]`, soit `[0]` par défaut. Utilisez `ids` pour fournir des identifiants
stables lorsque d’autres éléments doivent référencer les parties du slider.

### Comportement

`Slider` suit le modèle WAI-ARIA du slider d’Ark. Donnez à chaque poignée un nom accessible avec `Slider.Label`,
`aria-label` ou `aria-labelledby`. Les sliders horizontaux utilisent les flèches gauche et droite ; les sliders
verticaux utilisent les flèches haut et bas. `Page Up`, `Page Down` et `Shift + ArrowUp` ou `Shift + ArrowDown` utilisent
`largeStep` (par défaut, `10 * step`). `Home` et `End` placent la poignée active sur les valeurs minimale et maximale.

Placez un `Slider.HiddenInput` dans chaque `Slider.Thumb` explicite pour la soumission et la
réinitialisation. `id`, `asChild`, `disabled`, `invalid`, `readOnly`, `name`, `form`, `ids`,
`aria-label`, `aria-labelledby` et `getAriaValueText(details)` sont transmis à Ark.

## Exemples

### Centre comme origine

Définissez `origin="center"` lorsque la valeur représente un décalage positif ou négatif, par exemple un équilibre autour de zéro.

<ExampleFrame variant="component">
  <SliderCenterOrigin />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/slider/center-origin.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/slider/slider-center-origin.module.css"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Événements de modification

Utilisez `onValueChange` pour mettre à jour l’interface pendant l’interaction et `onValueChangeEnd` lorsque le glissement
ou l’interaction au clavier est terminé.

<ExampleFrame variant="component">
  <SliderChangeEvents />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/slider/change-events.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/slider/slider-change-events.module.css"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Contexte

Utilisez `useSliderContext` de moduix dans un composant enfant lorsque celui-ci doit lire l’état du slider, par exemple
`value`, `dragging` ou l’état du focus.

<ExampleFrame variant="component">
  <SliderContext />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/slider/context.tsx"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### État contrôlé

Contrôlez le slider depuis l’état React en conservant le tableau fourni par `details.value`.

<ExampleFrame variant="component">
  <SliderControlled />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/slider/controlled.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/slider/slider-controlled.module.css"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Désactivé

Utilisez `disabled` pour garder la valeur visible tout en désactivant les interactions.

<ExampleFrame variant="component">
  <SliderDisabled />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/slider/disabled.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/slider/slider-disabled.module.css"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Formulaire

Définissez `name` et placez `Slider.HiddenInput` dans chaque poignée explicite pour envoyer la valeur actuelle.

<ExampleFrame variant="component">
  <SliderForm />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/slider/form.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/slider/slider-form.module.css"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Indicateur de glissement

Placez `Slider.DraggingIndicator` dans une poignée pour afficher sa valeur active pendant le glissement.

<ExampleFrame variant="component">
  <SliderDraggingIndicator />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/slider/dragging-indicator.tsx"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Valeur non valide

Utilisez `invalid` lorsque la valeur sélectionnée ne passe pas la validation. La piste, la plage et la poignée exposent
l’état invalide tout en restant interactives.

<ExampleFrame variant="component">
  <SliderInvalid />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/slider/invalid.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/slider/slider-invalid.module.css"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Repères

Utilisez `Slider.MarkerGroup` et `Slider.Marker` pour afficher des repères. Les repères Ark exposent
`data-state="under-value | at-value | over-value"`, que vous pouvez utiliser pour les styles.

<ExampleFrame variant="component">
  <SliderMarks />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/slider/marks.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/slider/slider-marks.module.css"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Minimum et maximum

Utilisez `min` et `max` lorsque l’intervalle du slider ne commence pas à `0` ou ne se termine pas à `100`.

<ExampleFrame variant="component">
  <SliderMinAndMax />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/slider/min-and-max.tsx"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Plage

Transmettez un tableau contenant une valeur par poignée. `Slider.Thumbs` génère les poignées correspondantes et leurs
champs natifs.

<ExampleFrame variant="component">
  <SliderRange />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/slider/range.tsx"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Lecture seule

Utilisez `readOnly` lorsque la valeur doit rester visible et pouvoir recevoir le focus sans être modifiée. moduix utilise le
curseur système par défaut dans cet état afin que le slider ne paraisse pas interactif.

<ExampleFrame variant="component">
  <SliderReadOnly />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/slider/read-only.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/slider/slider-read-only.module.css"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Root Provider

Utilisez `useSlider` de moduix avec `Slider.RootProvider` lorsque l’état doit être créé en dehors de l’arbre qui affiche
les parties du slider. Placez ces parties dans `RootProvider` et n’utilisez pas une seconde racine pour le même état.

<ExampleFrame variant="component">
  <SliderRootProvider />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/slider/root-provider.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/slider/slider-root-provider.module.css"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Incrément

Utilisez `step` pour des incréments fractionnaires ou plus fins.

<ExampleFrame variant="component">
  <SliderStep />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/slider/step.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/slider/slider-step.module.css"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Alignement de la poignée

Utilisez `thumbAlignment="center"` lorsque la poignée doit dépasser des extrémités de la piste.

<ExampleFrame variant="component">
  <SliderThumbAlignment />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/slider/thumb-alignment.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/slider/slider-thumb-alignment.module.css"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Collision des poignées

Utilisez `thumbCollisionBehavior` pour définir ce qui se passe lorsque les poignées d’une plage se rencontrent. `none`
(par défaut) empêche une poignée de dépasser l’autre, `push` déplace l’autre poignée et `swap` échange leur position.

<ExampleFrame variant="component">
  <SliderThumbCollision />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/slider/thumb-collision.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/slider/slider-thumb-collision.module.css"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Chevauchement des poignées

Utilisez `minStepsBetweenThumbs` pour imposer un écart minimal entre les poignées d’une plage. L’écart est calculé ainsi :
`minStepsBetweenThumbs * step`.

<ExampleFrame variant="component">
  <SliderThumbOverlap />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/slider/thumb-overlap.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/slider/slider-thumb-overlap.module.css"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Orientation verticale

Définissez `orientation="vertical"` et donnez une hauteur explicite à la racine.

<ExampleFrame variant="component">
  <SliderVertical />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/slider/vertical.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/slider/slider-vertical.module.css"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Orientation verticale avec repères

En orientation verticale, les repères restent alignés sur la piste et exposent les mêmes attributs d’état.

<ExampleFrame variant="component">
  <SliderVerticalWithMarks />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/slider/vertical-with-marks.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/slider/slider-vertical-with-marks.module.css"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Personnalisation avancée

Utilisez des `Slider.Thumb` explicites plutôt que `Slider.Thumbs` lorsque chaque poignée nécessite des enfants
personnalisés, un nom accessible ou un ordre particulier.

<ExampleFrame variant="component">
  <SliderAdvancedCustomization />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/slider/advanced-customization.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/slider/slider-advanced-customization.module.css"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

## Styles

Personnalisez l’apparence de `Slider` avec `className`, les attributs Ark, les points d’accroche stables
`data-slot="slider-*"`, les variables CSS d’Ark et les variables CSS publiques de moduix ci-dessous.

### Propriétés CSS

<div className="docs-reference">
  <Tabs
    values={[{ label: 'Variables CSS', value: 'CSS Variables' }]}
    className="docs-reference-tabs"
    contentItemClassName="docs-reference-panel"
  >
    <Tab>
      <SliderDocs.SliderCssPropertiesPanel />
    </Tab>
  </Tabs>
</div>

### Points d’accroche CSS

Ark ajoute `data-scope="slider"` et `data-part` à toutes les parties affichées. moduix ajoute également les valeurs
`data-slot` suivantes : `slider-root`, `slider-root-provider`, `slider-label`, `slider-value-text`, `slider-control`,
`slider-track`, `slider-range`, `slider-thumb`, `slider-hidden-input`, `slider-marker-group`, `slider-marker` et
`slider-dragging-indicator`.

Les principaux attributs d’état d’Ark sont `data-orientation`, `data-disabled`, `data-invalid`, `data-focus`,
`data-dragging`, le `data-state` du repère et le `data-state="open | closed"` de l’indicateur de glissement. Les
variables CSS d’exécution d’Ark sont `--slider-thumb-width`, `--slider-thumb-height`, `--slider-thumb-transform`,
`--slider-range-start`, `--slider-range-end`, `--translate-x` et `--translate-y`.

moduix ajoute `data-readonly` à `Slider` lorsque la propriété `readOnly` est définie. Dans l’état invalide, la piste,
la plage sélectionnée, la poignée et les repères actifs utilisent par défaut la couleur destructive. Vous pouvez les
redéfinir séparément avec `--moduix-slider-track-border-color-invalid`, `--moduix-slider-range-bg-invalid`,
`--moduix-slider-thumb-border-color-invalid`, `--moduix-slider-thumb-focus-ring-color-invalid` et
`--moduix-slider-marker-dot-bg-active-invalid`.

L’anneau de focus par défaut de la poignée utilise `:focus-visible` pour le focus clavier et `data-dragging` d’Ark
pendant le glissement. Il disparaît lorsque le pointeur est relâché.
