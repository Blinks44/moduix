---
title: Toggle Group
component: toggle-group
description: Un groupe de boutons à bascule navigable au clavier pour une sélection unique ou multiple dans React et Solid.
---

import * as ToggleGroupDocs from '@/components/examples/toggle-group/css-properties';
import { CssPropertiesSection } from '@/components/mdx/Components';
import ToggleGroupAsChild from '@/components/examples/toggle-group/as-child.tsx';
import ToggleGroupBasic from '@/components/examples/toggle-group/basic.tsx';
import ToggleGroupContext from '@/components/examples/toggle-group/context.tsx';
import ToggleGroupControlled from '@/components/examples/toggle-group/controlled.tsx';
import ToggleGroupDisabled from '@/components/examples/toggle-group/disabled.tsx';
import ToggleGroupIcons from '@/components/examples/toggle-group/icons.tsx';
import ToggleGroupMultipleSelection from '@/components/examples/toggle-group/multiple-selection.tsx';
import ToggleGroupRootProvider from '@/components/examples/toggle-group/root-provider.tsx';
import ToggleGroupVariants from '@/components/examples/toggle-group/variants.tsx';
import ToggleGroupVertical from '@/components/examples/toggle-group/vertical.tsx';
import toggleGroupSolidAsChild from '../../../snippets/toggle-group/solid/as-child.tsx?raw';
import toggleGroupSolidBasic from '../../../snippets/toggle-group/solid/basic.tsx?raw';
import toggleGroupSolidContext from '../../../snippets/toggle-group/solid/context.tsx?raw';
import toggleGroupSolidControlled from '../../../snippets/toggle-group/solid/controlled.tsx?raw';
import toggleGroupSolidDisabled from '../../../snippets/toggle-group/solid/disabled.tsx?raw';
import toggleGroupSolidIcons from '../../../snippets/toggle-group/solid/icons.tsx?raw';
import toggleGroupSolidMultipleSelection from '../../../snippets/toggle-group/solid/multiple-selection.tsx?raw';
import toggleGroupSolidRootProvider from '../../../snippets/toggle-group/solid/root-provider.tsx?raw';
import toggleGroupSolidVariants from '../../../snippets/toggle-group/solid/variants.tsx?raw';
import toggleGroupSolidVertical from '../../../snippets/toggle-group/solid/vertical.tsx?raw';

## Référence de l’API

`ToggleGroup` s’appuie sur la primitive Toggle Group d’Ark UI et ajoute les props visuelles de moduix
pour les variantes et les tailles. Importez-le depuis le package du framework correspondant :
`@moduix/react/toggle-group`, `@moduix/solid/toggle-group`, `@moduix/react-tailwind/toggle-group`
ou `@moduix/solid-tailwind/toggle-group`.

<PrimitiveReference href="https://ark-ui.com/docs/components/toggle-group" />

## Choisir le bon composant

Utilisez `ToggleGroup` lorsque plusieurs boutons à bascule doivent former un seul contrôle navigable au
clavier. Le groupe peut autoriser une seule valeur sélectionnée ou plusieurs.

- Choisissez **`ToggleGroup`** pour les contrôles segmentés, les ensembles de filtres compacts et les barres d’outils de mise en forme.
- Choisissez **`Toggle`** pour une seule action autonome à deux états.
- Choisissez **`Tabs`** lorsque la sélection doit également afficher le panneau associé.
- Choisissez **`RadioGroup`** lorsque les choix sont des options de formulaire et doivent s’intégrer aux formulaires natifs.

## Exemple de base


Utilisez `ToggleGroup` comme racine et ajoutez un `ToggleGroup.Item` pour chaque choix. Donnez au groupe
un nom accessible avec `aria-label` ou `aria-labelledby`.

<ExampleFrame variant="component">
  <ToggleGroupBasic />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/toggle-group/basic.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={toggleGroupSolidBasic} />

    </Tab>
  </Tabs>
</div>
</ExampleCode>

## Installation avec shadcn

Consultez le [Démarrage rapide](/docs/quick-start) pour la configuration complète du paquet, du registre et des styles.

<Tabs groupId="framework">
  <Tab label="React" value="react">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-react/toggle-group' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-react-tailwind/toggle-group' },
  ]}
/>

  </Tab>
  <Tab label="Solid" value="solid">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-solid/toggle-group' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-solid-tailwind/toggle-group' },
  ]}
/>

  </Tab>
</Tabs>

## Anatomie

```text
ToggleGroup / ToggleGroup.Root
├─ ToggleGroup.Context (facultatif)
└─ ToggleGroup.Item[value]

ToggleGroup.RootProvider
└─ ToggleGroup.Item[value]
```

| Partie                              | Rôle                                                                                   |
| ----------------------------------- | -------------------------------------------------------------------------------------- |
| `ToggleGroup` / `ToggleGroup.Root`  | Partie racine qui gère les valeurs sélectionnées, le focus, l’orientation et l’état désactivé. |
| `ToggleGroup.Item`                  | Élément interactif avec un `value` obligatoire et l’état de sélection géré par Ark.    |
| `ToggleGroup.RootProvider`          | Relie l’arbre des éléments à l’état créé avec `useToggleGroup()`.                      |
| `ToggleGroup.Context`               | Expose l’état actuel du groupe via une render prop.                                    |

## Composition

Utilisez `defaultValue` pour un groupe non contrôlé, ou `value` avec `onValueChange(details)` lorsque
la sélection est gérée par une autre partie de l’application. Les deux valeurs sont des `string[]`, en
sélection unique comme en sélection multiple. Utilisez `ToggleGroup.RootProvider` avec
`useToggleGroup()` du package correspondant lorsque l’état doit être créé en dehors de l’arbre rendu.
React lit le store comme un objet, tandis que Solid lit son état réactif via l’accesseur retourné.

Utilisez `ToggleGroup.Context` ou `useToggleGroupContext()` lorsqu’un élément personnalisé a besoin de
l’état actuel du groupe. Avec `asChild`, React reçoit un enfant qui se comporte comme un bouton, tandis
que Solid reçoit une fonction de rendu qui retourne l’élément portant le comportement Ark. Ne rendez pas
`ToggleGroup.Root` et `ToggleGroup.RootProvider` pour la même instance d’état.

## Exemples

### asChild

Utilisez `asChild` sur un élément lorsqu’un bouton personnalisé doit porter l’élément rendu. Conservez
exactement un enfant qui se comporte comme un bouton afin qu’Ark puisse lui transmettre la sémantique du
groupe et le comportement clavier.

<ExampleFrame variant="component">
  <ToggleGroupAsChild />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/toggle-group/as-child.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={toggleGroupSolidAsChild} />

    </Tab>
  </Tabs>
</div>
</ExampleCode>

### Contexte

Utilisez `useToggleGroupContext()` dans un élément personnalisé lorsque son contenu a besoin de l’état
actuel du groupe. `ToggleGroup.Context` fournit le même état via une render prop.

<ExampleFrame variant="component">
  <ToggleGroupContext />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/toggle-group/context.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={toggleGroupSolidContext} />

    </Tab>
  </Tabs>
</div>
</ExampleCode>

### Valeur contrôlée

Contrôlez `value` lorsque les éléments sélectionnés doivent rester synchronisés avec l’état du reste de
l’application.

<ExampleFrame variant="component">
  <ToggleGroupControlled />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/toggle-group/controlled.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={toggleGroupSolidControlled} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/toggle-group/toggle-group-controlled.module.css"
```
</div>
</ExampleCode>

### Groupe et élément désactivés

Utilisez `disabled` sur le groupe ou sur un élément précis pour empêcher les interactions.

<ExampleFrame variant="component">
  <ToggleGroupDisabled />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/toggle-group/disabled.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={toggleGroupSolidDisabled} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/toggle-group/toggle-group-disabled.module.css"
```
</div>
</ExampleCode>

### Icônes

Passez les icônes comme des enfants ordinaires. Les éléments qui n’affichent qu’une icône doivent toujours
avoir un libellé accessible.

<ExampleFrame variant="component">
  <ToggleGroupIcons />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/toggle-group/icons.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={toggleGroupSolidIcons} />

    </Tab>
  </Tabs>
</div>
</ExampleCode>

### Sélection multiple

Utilisez `multiple` lorsque plusieurs choix peuvent rester actifs en même temps.

<ExampleFrame variant="component">
  <ToggleGroupMultipleSelection />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/toggle-group/multiple-selection.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={toggleGroupSolidMultipleSelection} />

    </Tab>
  </Tabs>
</div>
</ExampleCode>

### Variantes

Définissez `variant` sur le groupe lorsque tous les éléments doivent partager le même style visuel.

<ExampleFrame variant="component">
  <ToggleGroupVariants />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/toggle-group/variants.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={toggleGroupSolidVariants} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/toggle-group/toggle-group-variants.module.css"
```
</div>
</ExampleCode>

### Orientation verticale

Définissez `orientation="vertical"` lorsque les choix doivent être disposés en colonne.

<ExampleFrame variant="component">
  <ToggleGroupVertical />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/toggle-group/vertical.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={toggleGroupSolidVertical} />

    </Tab>
  </Tabs>
</div>
</ExampleCode>

### Personnalisation avancée

Utilisez `ToggleGroup.RootProvider` avec `useToggleGroup()` lorsque l’instance d’état doit être créée
en dehors de l’arbre rendu du groupe.

<ExampleFrame variant="component">
  <ToggleGroupRootProvider />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/toggle-group/root-provider.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={toggleGroupSolidRootProvider} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/toggle-group/toggle-group-advanced-customization.module.css"
```
</div>
</ExampleCode>

## Styles

Utilisez `className` en React ou `class` en Solid sur les parties exportées pour les styles structurels.
Les variantes Tailwind utilisent les utilitaires natifs et les classes du consommateur ; les variantes
CSS Modules exposent les tokens `--moduix-toggle-group-*`. Les éléments héritent de `variant` et de
`size` depuis la racine et peuvent remplacer l’une de ces props lorsqu’un élément doit avoir un style différent.

### Propriétés CSS

<CssPropertiesSection properties={ToggleGroupDocs.toggleGroupOverrideCssProperties} />

Le tableau couvre les propriétés du groupe. Chaque `ToggleGroup.Item` prend également en charge les
[propriétés CSS de Toggle](/docs/toggle#css-properties) pour ses couleurs et ses états d’interaction.
Définissez une propriété `--moduix-toggle-*` sur la racine pour styliser tous les éléments, ou sur un
élément pour ne remplacer qu’un seul contrôle.

### Points d’accroche CSS

- Sélecteurs `data-slot` stables : `toggle-group-root`, `toggle-group-root-provider` et `toggle-group-item`.
- Sélecteurs des parties Ark : `data-scope="toggle-group"` et `data-part="root" | "item"`.
- Sélecteurs d’état Ark : `data-state="on" | "off"`, `data-disabled`, `data-focus` et
  `data-orientation="horizontal" | "vertical"`.
- Sélecteurs moduix : `data-variant` et `data-size`.
