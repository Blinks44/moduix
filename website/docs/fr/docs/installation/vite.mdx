---
title: Vite
description: Configurez moduix dans une application Vite React ou Solid par paquet ou avec la CLI shadcn.
---

# Vite

Les exemples ci-dessous utilisent les paquets CSS Modules, qui proposent le catalogue complet. Pour
un composant disponible avec Tailwind, suivez la [configuration du paquet Tailwind](/docs/quick-start#tailwind-css)
et choisissez le paquet ou le registre React ou Solid correspondant.

Ce guide part d’une application existante, créée avec un modèle Vite pour React, React + TypeScript,
Solid ou Solid + TypeScript.

## Installer moduix comme paquet

<Tabs groupId="framework">
  <Tab label="React" value="react">

<PackageManagerTabs command="install @moduix/react @ark-ui/react" />

```tsx title="src/main.tsx"
import '@moduix/react/style.css';
```

```tsx title="src/App.tsx"
import { Button } from '@moduix/react/button';

export function App() {
  return <Button>Continue</Button>;
}
```

  </Tab>
  <Tab label="Solid" value="solid">

<PackageManagerTabs command="install @moduix/solid @ark-ui/solid" />

```tsx title="src/index.tsx"
import '@moduix/solid/style.css';
```

```tsx title="src/App.tsx"
import { Button } from '@moduix/solid/button';

export function App() {
  return <Button>Continue</Button>;
}
```

  </Tab>
</Tabs>

## Installer avec la CLI shadcn

Effectuez la [configuration initiale de la CLI shadcn](/docs/quick-start#installation-avec-la-cli-shadcn),
puis faites pointer `@/*` vers `src/*`. Les modèles TypeScript de Vite répartissent leur
configuration entre `tsconfig.json` et un fichier propre à l’application. Ajoutez l’alias aux deux :

```json title="tsconfig.json"
{
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "@/*": ["./src/*"]
    }
  }
}
```

Reprenez ces valeurs dans `compilerOptions` de `tsconfig.app.json`, puis ajoutez à Vite l’alias adapté
au framework. Comme l’exemple importe `node:url`, installez les types Node.js s’ils ne sont pas déjà
présents :

<PackageManagerTabs command="install -D @types/node" />

<Tabs groupId="framework">
  <Tab label="React" value="react">

```ts title="vite.config.ts"
import { fileURLToPath, URL } from 'node:url';
import react from '@vitejs/plugin-react';
import { defineConfig } from 'vite';

export default defineConfig({
  plugins: [react()],
  resolve: {
    alias: {
      '@': fileURLToPath(new URL('./src', import.meta.url)),
    },
  },
});
```

<PackageManagerTabs command="shadcn@latest add @moduix-react/button" dlx />

  </Tab>
  <Tab label="Solid" value="solid">

```ts title="vite.config.ts"
import { fileURLToPath, URL } from 'node:url';
import { defineConfig } from 'vite';
import solid from 'vite-plugin-solid';

export default defineConfig({
  plugins: [solid()],
  resolve: {
    alias: {
      '@': fileURLToPath(new URL('./src', import.meta.url)),
    },
  },
});
```

<PackageManagerTabs command="shadcn@latest add @moduix-solid/button" dlx />

  </Tab>
</Tabs>

Dans le point d’entrée du framework, importez la feuille de styles générée à la place de celle du
paquet ; n’importez pas les deux :

<Tabs groupId="framework">
  <Tab label="React" value="react">

```tsx title="src/main.tsx"
import '@/lib/moduix/styles/style.css';
```

  </Tab>
  <Tab label="Solid" value="solid">

```tsx title="src/index.tsx"
import '@/lib/moduix/styles/style.css';
```

  </Tab>
</Tabs>

## Vérifier l’installation

Démarrez Vite. Avec le paquet, le composant importé depuis le paquet moduix choisi doit être mis en
forme. Avec shadcn, la feuille de styles générée et l’import `@/components/ui/button` doivent être
résolus dans l’éditeur comme pendant la compilation.

Consultez le [guide officiel de Vite](https://vite.dev/guide/) pour créer un projet et choisir un modèle.
