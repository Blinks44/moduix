---
title: Format Number
description: Formatez les nombres selon une locale avec une devise, un pourcentage, une unité ou une notation compacte grâce à Format.Number d’Ark UI.
---

import FormatNumberBasic from '@/components/examples/format-number/basic.tsx';
import FormatNumberOptions from '@/components/examples/format-number/options.tsx';
import formatNumberSolidBasic from '../../../snippets/format-number/solid/basic.tsx?raw';
import formatNumberSolidLocale from '../../../snippets/format-number/solid/locale.tsx?raw';
import formatNumberSolidOptions from '../../../snippets/format-number/solid/options.tsx?raw';

`Format.Number` formate une valeur numérique selon les règles `Intl.NumberFormat` du navigateur. C’est un formateur en
lecture seule, pas un champ de saisie.

## Référence de l’API

<PrimitiveReference href="https://ark-ui.com/docs/utilities/format-number" />

## Exemple de base

Passez un nombre à `value`. Sans option de formatage, le résultat suit la locale active. `Format.Number` ne rend que
du texte : ajoutez vous-même le libellé et la structure qui l’entourent.

L’aperçu utilise l’implémentation React, car le site de documentation fonctionne avec React. L’onglet Solid contient
la version Solid native du même exemple.

<ExampleFrame variant="component">
  <FormatNumberBasic />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/format-number/basic.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={formatNumberSolidBasic} />

    </Tab>
  </Tabs>
</div>
</ExampleCode>

## Options de formatage

`Format.Number` accepte les options standard de `Intl.NumberFormatOptions`, notamment `style`, `currency`, `unit`,
`unitDisplay`, `notation` et les options qui contrôlent le nombre de décimales.

<ExampleFrame variant="component">
  <FormatNumberOptions />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/format-number/options.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={formatNumberSolidOptions} />

    </Tab>
  </Tabs>
</div>
</ExampleCode>

`style="percent"` multiplie la valeur par 100 : passez `0.0642` pour afficher `6.4%` avec
`maximumFractionDigits={1}`. Pour conserver un nombre fixe de décimales, définissez à la fois
`minimumFractionDigits` et `maximumFractionDigits`. Utilisez `notation="compact"` pour abréger les grandes valeurs
et `style="unit"` avec une valeur `unit` prise en charge pour les mesures.

## Locale

`LocaleProvider` fournit la locale à `Format.Number` et aux autres formateurs Ark dans son sous-arbre. Sans fournisseur,
la locale par défaut est `en-US`. Importez le fournisseur depuis le package Ark correspondant au framework.

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/format-number/locale.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={formatNumberSolidLocale} />

    </Tab>
  </Tabs>
</div>
</ExampleCode>
