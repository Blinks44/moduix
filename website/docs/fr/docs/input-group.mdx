---
title: Input Group
component: input-group
description: Créez des champs de saisie avec préfixes, suffixes et actions en ligne en React ou Solid, installés par paquet ou avec la CLI shadcn.
---

import * as InputGroupDocs from '@/components/examples/input-group/css-properties';
import InputGroupAsChild from '@/components/examples/input-group/as-child.tsx';
import InputGroupBasic from '@/components/examples/input-group/basic.tsx';
import InputGroupDisabled from '@/components/examples/input-group/disabled.tsx';
import InputGroupFieldState from '@/components/examples/input-group/field-state.tsx';
import InputGroupPrefixAndSuffix from '@/components/examples/input-group/prefix-and-suffix.tsx';
import InputGroupSizes from '@/components/examples/input-group/sizes.tsx';
import InputGroupWithAction from '@/components/examples/input-group/with-action.tsx';
import inputGroupSolidAsChild from '../../../snippets/input-group/solid/as-child.tsx?raw';
import inputGroupSolidBasic from '../../../snippets/input-group/solid/basic.tsx?raw';
import inputGroupSolidDisabled from '../../../snippets/input-group/solid/disabled.tsx?raw';
import inputGroupSolidFieldState from '../../../snippets/input-group/solid/field-state.tsx?raw';
import inputGroupSolidPrefixAndSuffix from '../../../snippets/input-group/solid/prefix-and-suffix.tsx?raw';
import inputGroupSolidSizes from '../../../snippets/input-group/solid/sizes.tsx?raw';
import inputGroupSolidWithAction from '../../../snippets/input-group/solid/with-action.tsx?raw';

## Référence de l’API

`InputGroup` est une composition propre à moduix, et non une primitive Ark UI. Il associe des
éléments de la fabrique Ark aux composants moduix `Input` et `Button`, fondés sur Ark. La composition
suit les recommandations d’Ark pour la [composition](https://ark-ui.com/docs/guides/composition), les
[styles](https://ark-ui.com/docs/guides/styling) et la [transmission des refs](https://ark-ui.com/docs/guides/ref).

## Choisir le bon composant

- Utilisez **`Input`** pour un champ standard sur une seule ligne.
- Utilisez **`InputGroup`** lorsque le champ doit intégrer un préfixe, un suffixe ou une action.
- Utilisez **`PasswordInput`** pour un champ de mot de passe avec un bouton pour afficher ou masquer le mot de passe.
- Utilisez **`NumberInput`** lorsque le champ doit gérer des valeurs numériques et un pas de modification.
- Utilisez **`Textarea`** pour du contenu sur plusieurs lignes.
- Utilisez **`Editable`** lorsqu’un texte doit d’abord être affiché comme une valeur, puis devenir modifiable à la demande.

## Exemple de base


Utilisez `InputGroup` comme racine et composez un seul `InputGroupInput` avec les parties
`InputGroupAddon`, `InputGroupText` ou `InputGroupButton` nécessaires.

<ExampleFrame variant="component">
  <InputGroupBasic />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/input-group/basic.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={inputGroupSolidBasic} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/input-group/input-group-basic.module.css"
```
</div>

</ExampleCode>

## Installation avec shadcn

Consultez le [Démarrage rapide](/docs/quick-start) pour la configuration complète du paquet, du registre et des styles.

<Tabs groupId="framework">
  <Tab label="React" value="react">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-react/input-group' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-react-tailwind/input-group' },
  ]}
/>

  </Tab>
  <Tab label="Solid" value="solid">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-solid/input-group' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-solid-tailwind/input-group' },
  ]}
/>

  </Tab>
</Tabs>

## Anatomie

```text
InputGroup
├─ InputGroupAddon (facultatif)
├─ InputGroupInput
├─ InputGroupText (facultatif)
└─ InputGroupButton (facultatif)
```

| Partie                            | Rôle                                                       |
| --------------------------------- | ---------------------------------------------------------- |
| `InputGroup`                    | Conteneur passif avec styles et contexte de taille partagés. |
| `InputGroupInput`               | Élément `<input>` natif qui possède la valeur et l’état du champ. |
| `InputGroupAddon`               | Préfixe ou suffixe avec un fond.                           |
| `InputGroupText`                | Texte intégré sans fond.                                  |
| `InputGroupButton`              | Bouton d’action associé au champ.                         |

## Composition

Conservez un seul `InputGroupInput` dans chaque groupe. Le champ imbriqué possède la valeur, le
comportement du formulaire, l’état de validation et la `ref` ; les props contrôlées ou non contrôlées
se placent donc sur ce champ, tandis que le libellé et les messages de validation vont dans `Field`.
La taille de la racine est héritée par `InputGroupInput` et `InputGroupButton` ; les boutons ont
par défaut `type="button"` et n’héritent pas de `disabled` depuis `Field`. En React, utilisez
`onChange` pour un champ contrôlé ; en Solid, utilisez `onInput`.

Utilisez `InputGroupAddon` et `InputGroupText` pour du contenu intégré non interactif, et `InputGroupButton` pour les actions. La
racine, `InputGroupAddon` et `InputGroupText` prennent en charge `asChild` avec un seul enfant sémantique qui accepte les
props fusionnées et la ref. Utilisez `Editable` lorsque la valeur doit passer du mode lecture au mode édition.

## Exemples

### État désactivé

Placez `disabled` sur le champ natif. Désactivez aussi les boutons associés s’ils doivent être
indisponibles.

<ExampleFrame variant="component">
  <InputGroupDisabled />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/input-group/disabled.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={inputGroupSolidDisabled} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/input-group/input-group-disabled.module.css"
```
</div>

</ExampleCode>

### État du champ

Définissez `invalid` sur `Field` lorsque la validation échoue. Ark transmet l’état et les attributs
ARIA nécessaires à `InputGroupInput`, puis le groupe reflète cet état.

<ExampleFrame variant="component">
  <InputGroupFieldState />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/input-group/field-state.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={inputGroupSolidFieldState} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/input-group/input-group-field-state.module.css"
```
</div>

</ExampleCode>

### Préfixe et suffixe

Utilisez `InputGroupAddon` pour un préfixe ou un suffixe avec fond et `InputGroupText` pour un texte intégré sans fond.

<ExampleFrame variant="component">
  <InputGroupPrefixAndSuffix />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/input-group/prefix-and-suffix.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={inputGroupSolidPrefixAndSuffix} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/input-group/input-group-prefix-and-suffix.module.css"
```
</div>

</ExampleCode>

### Tailles

Définissez `size` une seule fois sur la racine pour que le champ, l’addon, le texte et le bouton
restent alignés.

<ExampleFrame variant="component">
  <InputGroupSizes />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/input-group/sizes.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={inputGroupSolidSizes} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/input-group/input-group-sizes.module.css"
```
</div>

</ExampleCode>

### Avec une action

Utilisez `InputGroupButton` pour une action liée à la valeur du champ. Dans cet exemple, le champ
est contrôlé et `Send` ne devient disponible qu’après la saisie d’une valeur.

<ExampleFrame variant="component">
  <InputGroupWithAction />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/input-group/with-action.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={inputGroupSolidWithAction} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/input-group/input-group-with-action.module.css"
```
</div>

</ExampleCode>

### Personnalisation avancée

Utilisez `asChild` lorsque la racine doit rendre un conteneur personnalisé à la place du `div` par
défaut. Le groupe conserve ses styles et son contexte de taille, tandis que l’enfant doit rester un
conteneur de mise en page sémantique.

<ExampleFrame variant="component">
  <InputGroupAsChild />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/input-group/as-child.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={inputGroupSolidAsChild} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/input-group/input-group-as-child.module.css"
```
</div>

</ExampleCode>

## Styles

Utilisez `className` en React ou `class` en Solid sur les parties exportées pour appliquer les styles
structurels. Pour les styles liés à l’état, privilégiez les attributs de données Ark comme
`data-invalid`, `data-disabled` et `data-readonly`, ainsi que les sélecteurs `data-slot` de moduix.
Les variantes Tailwind se personnalisent avec des utilitaires et des classes supplémentaires ; les
variables ci-dessous concernent uniquement les variantes CSS Modules. Les attributs de données et les
sélecteurs `data-slot` sont communs aux deux approches.

### Propriétés CSS

<CssPropertiesSection properties={InputGroupDocs.inputGroupOverrideCssProperties} />

### Points d’accroche CSS

- `data-slot="input-group-root"` et `data-size="xs" | "sm" | "md" | "lg" | "xl"` sur la racine
- `data-scope="input-group"` et `data-part="root" | "addon" | "text"` sur les parties de la fabrique Ark
- `data-slot="input-root"` et les attributs d’état du champ Ark
- `data-slot="input-group-addon"`
- `data-slot="input-group-text"`
- `data-slot="input-group-button"` sur le bouton d’action du groupe
- les états imbriqués `data-invalid`, `aria-invalid="true"`, `data-disabled`/`:disabled` et `data-readonly`/`:read-only` sont reflétés sur la racine via `:has(...)`

Les hooks `data-slot`, `data-scope`, `data-part` et `data-size` gérés par le composant restent
stables même si vous fournissez vos propres propriétés. `className` en React et `class` en Solid
s’ajoutent toujours aux styles du composant.
