---
title: Progress Linear
component: progress-linear
description: Indicateur de progression linéaire pour suivre l’avancement d’une tâche, qu’il soit connu ou non.
---

import * as ProgressLinearDocs from '@/components/examples/progress-linear/css-properties';
import ProgressLinearBasic from '@/components/examples/progress-linear/basic.tsx';
import ProgressLinearControlled from '@/components/examples/progress-linear/controlled.tsx';
import ProgressLinearIndeterminate from '@/components/examples/progress-linear/indeterminate.tsx';
import ProgressLinearInitialValue from '@/components/examples/progress-linear/initial-value.tsx';
import ProgressLinearMinMax from '@/components/examples/progress-linear/min-max.tsx';
import ProgressLinearStateViews from '@/components/examples/progress-linear/state-views.tsx';
import ProgressLinearValueText from '@/components/examples/progress-linear/value-text.tsx';
import ProgressLinearVertical from '@/components/examples/progress-linear/vertical.tsx';
import ProgressLinearRootProvider from '@/components/examples/progress-linear/root-provider.tsx';
import progressLinearSolidBasic from '../../../snippets/progress-linear/solid/basic.tsx?raw';
import progressLinearSolidControlled from '../../../snippets/progress-linear/solid/controlled.tsx?raw';
import progressLinearSolidIndeterminate from '../../../snippets/progress-linear/solid/indeterminate.tsx?raw';
import progressLinearSolidInitialValue from '../../../snippets/progress-linear/solid/initial-value.tsx?raw';
import progressLinearSolidMinMax from '../../../snippets/progress-linear/solid/min-max.tsx?raw';
import progressLinearSolidStateViews from '../../../snippets/progress-linear/solid/state-views.tsx?raw';
import progressLinearSolidValueText from '../../../snippets/progress-linear/solid/value-text.tsx?raw';
import progressLinearSolidVertical from '../../../snippets/progress-linear/solid/vertical.tsx?raw';
import progressLinearSolidRootProvider from '../../../snippets/progress-linear/solid/root-provider.tsx?raw';

## Référence de l’API

<PrimitiveReference href="https://ark-ui.com/docs/components/progress-linear" />

## Choisir le bon composant

Utilisez `ProgressLinear` lorsque l’utilisateur doit suivre l’avancement sur une barre. Préférez
`ProgressCircular` pour un indicateur d’état compact dans une carte, une barre d’outils ou tout autre
espace limité.

## Exemple de base

`ProgressLinear` est disponible pour React et Solid dans les packages CSS Modules et Tailwind. L’aperçu
de cette page utilise le package React CSS Modules ; les onglets de framework montrent le code natif
pour le même contrat public.

Composez la barre de progression avec les parties explicites `Label`, `ValueText`, `Track` et
`Range`. Donnez à `Track` un nom accessible qui décrit la tâche.

<ExampleFrame variant="component">
  <ProgressLinearBasic />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/progress-linear/basic.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={progressLinearSolidBasic} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

## Installation avec shadcn

<Tabs groupId="framework">
  <Tab label="React" value="react">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-react/progress-linear' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-react-tailwind/progress-linear' },
  ]}
/>

  </Tab>
  <Tab label="Solid" value="solid">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-solid/progress-linear' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-solid-tailwind/progress-linear' },
  ]}
/>

  </Tab>
</Tabs>

Consultez le [guide de démarrage rapide](/docs/quick-start) pour configurer le registry et installer
les packages.

## Anatomie

```text
ProgressLinear / ProgressLinear.Root
├─ ProgressLinear.Label
├─ ProgressLinear.ValueText
├─ ProgressLinear.Track
│  └─ ProgressLinear.Range
└─ ProgressLinear.View

ProgressLinear.RootProvider
└─ le même arbre de parties relié à ProgressLinear.useProgress()
```

| Partie                                    | Rôle                                                                            |
| ----------------------------------------- | ------------------------------------------------------------------------------- |
| `ProgressLinear` / `ProgressLinear.Root`  | Gère l’état de progression et transmet à Ark le formatage, les identifiants et les autres propriétés. |
| `ProgressLinear.RootProvider`             | Relie les parties à l’état renvoyé par `ProgressLinear.useProgress()`.          |
| `ProgressLinear.Context`                  | Transmet l’état courant à une fonction de rendu.                                |
| `ProgressLinear.Label`                    | Libellé visible de la tâche. Il ne devient pas automatiquement le nom accessible de `Track`. |
| `ProgressLinear.ValueText`                | Affiche le pourcentage formaté et annonce les changements. Utilisez `Context` pour un texte personnalisé. |
| `ProgressLinear.Track`                    | Piste de progression. Ark lui ajoute le rôle et les attributs ARIA de valeur.  |
| `ProgressLinear.Range`                    | Partie remplie qui représente l’avancement actuel.                             |
| `ProgressLinear.View`                     | Affiche son contenu uniquement lorsque son `state` correspond à l’état courant. |

## Composition

Utilisez `defaultValue` pour une progression non contrôlée. Pour une progression contrôlée, utilisez
`value` avec `onValueChange(details)` lorsque votre application gère la valeur courante. Utilisez
`defaultValue={null}` ou `value={null}` lorsque l’avancement est inconnu. Si l’état est créé avec
`ProgressLinear.useProgress()`, rendez les parties dans `ProgressLinear.RootProvider` plutôt que dans
`ProgressLinear` ou `ProgressLinear.Root`. Utilisez `ProgressLinear.View` pour le contenu dépendant de
l’état et `ProgressLinear.Context` lorsque votre texte personnalisé doit accéder à l’état courant.

## Exemples

### État contrôlé

Conservez `value` dans l’état lorsque la progression doit rester synchronisée avec un autre contrôle
ou une tâche externe.

<ExampleFrame variant="component">
  <ProgressLinearControlled />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/progress-linear/controlled.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={progressLinearSolidControlled} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Progression indéterminée

Définissez `defaultValue={null}` ou `value={null}` lorsque la tâche est en cours mais que son
achèvement ne peut pas encore être mesuré.

<ExampleFrame variant="component">
  <ProgressLinearIndeterminate />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/progress-linear/indeterminate.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={progressLinearSolidIndeterminate} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Valeur initiale

Utilisez `defaultValue` pour définir la valeur initiale d’une progression non contrôlée.

<ExampleFrame variant="component">
  <ProgressLinearInitialValue />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/progress-linear/initial-value.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={progressLinearSolidInitialValue} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Minimum et maximum

Utilisez `min` et `max` lorsque la plage de progression diffère de la plage par défaut, de `0` à
`100`.

<ExampleFrame variant="component">
  <ProgressLinearMinMax />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/progress-linear/min-max.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={progressLinearSolidMinMax} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Vues d’état

Utilisez `ProgressLinear.View` lorsque le texte ou l’interface d’accompagnement doit changer avec
l’état de progression Ark.

<ExampleFrame variant="component">
  <ProgressLinearStateViews />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/progress-linear/state-views.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={progressLinearSolidStateViews} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Texte de la valeur

Utilisez `ProgressLinear.Context` avec `translations.value(details)` lorsque le texte visible de la
valeur doit être formulé pour une tâche précise.

<ExampleFrame variant="component">
  <ProgressLinearValueText />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/progress-linear/value-text.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={progressLinearSolidValueText} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Orientation verticale

Définissez `orientation="vertical"` lorsque la partie remplie doit progresser du bas vers le haut.
En mode vertical, la hauteur par défaut est de `12rem`. Si votre mise en page nécessite une autre
hauteur, redéfinissez `--moduix-progress-linear-height`.

<ExampleFrame variant="component">
  <ProgressLinearVertical />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/progress-linear/vertical.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={progressLinearSolidVertical} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Personnalisation avancée

Créez l’état de progression avec `ProgressLinear.useProgress()` dans un composant parent, puis
reliez-le aux parties rendues avec `ProgressLinear.RootProvider`.

<ExampleFrame variant="component">
  <ProgressLinearRootProvider />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/progress-linear/root-provider.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={progressLinearSolidRootProvider} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

## Styles

Utilisez `className` dans React ou `class` dans Solid sur les parties exportées pour modifier la
structure visuelle. Pour les styles dépendants de l’état, utilisez les attributs de données Ark et
les points d’accroche `data-slot` de moduix. Les variantes Tailwind utilisent des utilitaires natifs et
des classes du consommateur ; les variables ci-dessous personnalisent les variantes CSS Modules.

La racine a une largeur par défaut de `12rem`. En mode vertical, sa hauteur par défaut est également
de `12rem`. Utilisez les variables publiques `--moduix-progress-linear-*` pour adapter le composant
à votre mise en page.

### Propriétés CSS

<div className="docs-reference">
  <Tabs
    values={[{ label: 'Variables CSS', value: 'CSS Variables' }]}
    className="docs-reference-tabs"
    contentItemClassName="docs-reference-panel"
  >
    <Tab>
      <ProgressLinearDocs.ProgressLinearCssPropertiesPanel />
    </Tab>
  </Tabs>
</div>

### Points d’accroche CSS

- `data-slot="progress-linear-root"`, `progress-linear-root-provider`, `progress-linear-label`,
  `progress-linear-value-text`, `progress-linear-track`, `progress-linear-range` et
  `progress-linear-view`.
- Attributs Ark : `data-scope="progress"`, `data-part`, `data-state="indeterminate|loading|complete"`,
  `data-orientation="horizontal|vertical"`, `data-value` et `data-max`.
