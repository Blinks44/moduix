---
title: Clipboard
component: clipboard
description: Créez des presse-papiers accessibles avec React, Solid ou Vue, installés par paquet ou avec shadcn.
---

import AsChildClipboardDemo from '@/components/examples/clipboard/as-child.tsx';
import ClipboardBasic from '@/components/examples/clipboard/basic.tsx';
import ContextClipboardDemo from '@/components/examples/clipboard/context.tsx';
import ClipboardStatusDemo from '@/components/examples/clipboard/copy-status.tsx';
import CustomCopyTextClipboardDemo from '@/components/examples/clipboard/custom-copy-text.tsx';
import DisabledClipboardDemo from '@/components/examples/clipboard/disabled.tsx';
import RootProviderClipboardDemo from '@/components/examples/clipboard/root-provider.tsx';
import TimeoutClipboardDemo from '@/components/examples/clipboard/timeout.tsx';
import ValueTextClipboardDemo from '@/components/examples/clipboard/value-text.tsx';
import * as ClipboardDocs from '@/components/examples/clipboard/css-properties';
import clipboardSolidAsChild from '../../../snippets/clipboard/solid/as-child.tsx?raw';
import clipboardSolidBasic from '../../../snippets/clipboard/solid/basic.tsx?raw';
import clipboardSolidContext from '../../../snippets/clipboard/solid/context.tsx?raw';
import clipboardSolidCopyStatus from '../../../snippets/clipboard/solid/copy-status.tsx?raw';
import clipboardSolidCustomCopyText from '../../../snippets/clipboard/solid/custom-copy-text.tsx?raw';
import clipboardSolidDisabled from '../../../snippets/clipboard/solid/disabled.tsx?raw';
import clipboardSolidRootProvider from '../../../snippets/clipboard/solid/root-provider.tsx?raw';
import clipboardSolidTimeout from '../../../snippets/clipboard/solid/timeout.tsx?raw';
import clipboardSolidValueText from '../../../snippets/clipboard/solid/value-text.tsx?raw';
import clipboardVueAsChild from '../../../snippets/clipboard/vue/as-child.vue?raw';
import clipboardVueBasic from '../../../snippets/clipboard/vue/basic.vue?raw';
import clipboardVueContext from '../../../snippets/clipboard/vue/context.vue?raw';
import clipboardVueCopyStatus from '../../../snippets/clipboard/vue/copy-status.vue?raw';
import clipboardVueCustomCopyText from '../../../snippets/clipboard/vue/custom-copy-text.vue?raw';
import clipboardVueDisabled from '../../../snippets/clipboard/vue/disabled.vue?raw';
import clipboardVueRootProvider from '../../../snippets/clipboard/vue/root-provider.vue?raw';
import clipboardVueTimeout from '../../../snippets/clipboard/vue/timeout.vue?raw';
import clipboardVueValueText from '../../../snippets/clipboard/vue/value-text.vue?raw';

## Référence de l’API

<PrimitiveReference href="https://ark-ui.com/docs/components/clipboard" />

## Choisir le bon composant

Utilisez `Clipboard` lorsque les utilisateurs doivent copier une valeur textuelle. Selon les parties
utilisées, cette valeur peut être affichée dans un champ en lecture seule ou comme texte ordinaire.

## Exemple de base

Utilisez `Clipboard` comme racine. Placez une seule surface pour la valeur - `ClipboardInput` ou
`ClipboardValueText` - ainsi que `ClipboardTrigger` dans `ClipboardControl`. `ClipboardLabel` est
facultatif. Le bouton peut contenir `ClipboardIndicator`. En React et Solid, ses contenus `children`
et `copied` acceptent du texte comme des icônes. En Vue, utilisez le slot par défaut et le slot nommé
`copied` pour le même contenu.

React, Solid et Vue utilisent les mêmes noms de valeurs plats. Importez `Clipboard` comme racine et
importez directement chaque partie supplémentaire, par exemple `ClipboardInput` et `ClipboardTrigger`.
Ces mêmes noms fonctionnent avec les paquets npm et les sources du registre shadcn.

<ExampleFrame variant="component">
  <ClipboardBasic />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
      { label: 'Vue', value: 'vue' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/clipboard/basic.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={clipboardSolidBasic} />

    </Tab>
    <Tab value="vue">

<CodeBlockRuntime lang="vue" code={clipboardVueBasic} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/clipboard/component-basic.module.css"
```
</div>

</ExampleCode>

## Installation avec shadcn

Consultez le [Démarrage rapide](/docs/quick-start) pour la configuration complète du paquet, du registre et des styles.

<Tabs groupId="framework">
  <Tab label="React" value="react">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-react/clipboard' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-react-tailwind/clipboard' },
  ]}
/>

  </Tab>
  <Tab label="Solid" value="solid">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-solid/clipboard' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-solid-tailwind/clipboard' },
  ]}
/>

  </Tab>
  <Tab label="Vue" value="vue">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-vue/clipboard' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-vue-tailwind/clipboard' },
  ]}
/>

  </Tab>
</Tabs>

## Anatomie

```text
Clipboard | ClipboardRootProvider
├─ ClipboardLabel (facultatif)
├─ ClipboardControl
│  ├─ ClipboardInput ou ClipboardValueText
│  └─ ClipboardTrigger
│     └─ ClipboardIndicator (facultatif)
```

| Partie | Rôle |
| --- | --- |
| `Clipboard` | Gère la valeur et l’état temporaire qui suit la copie. |
| `ClipboardRootProvider` | Relie les parties à un store créé avec `useClipboard()`. |
| `ClipboardLabel` | Fournit un libellé accessible pour la valeur. |
| `ClipboardControl` | Regroupe l’affichage de la valeur et le bouton de copie. |
| `ClipboardInput` | Affiche la valeur dans un champ en lecture seule. Utilisez un état contrôlé ou `ClipboardContext.setValue()` pour la modifier. |
| `ClipboardTrigger` | Copie la valeur actuelle lorsqu’il est activé. Il s’affiche comme un bouton par défaut. |
| `ClipboardIndicator` | Affiche un contenu différent selon que la valeur a été copiée ou non. Par défaut : icônes de copie et de confirmation ; utilisez `copied="Copied"` et `children` en React/Solid, ou les slots Vue correspondants. |
| `ClipboardValueText` | Affiche la valeur sous forme de texte lorsqu’un champ n’est pas adapté. |

## Composition

`Clipboard` gère la valeur et l’état après copie. Placez une seule surface pour la valeur
(`ClipboardInput` ou `ClipboardValueText`) et `ClipboardTrigger` dans `ClipboardControl` ;
`ClipboardIndicator` est facultatif dans le bouton. Il suit le même état de copie, et moduix
fournit les icônes de copie et de confirmation lorsque son contenu est omis. Passez
`copied="Copied"` et `children` pour des libellés textuels en React et Solid ; en Vue, utilisez le
slot par défaut et le slot nommé `copied`.

Utilisez `defaultValue` pour une valeur non contrôlée. Lorsqu’une autre partie de l’application gère
la valeur ou doit réagir à la copie, utilisez `value` avec `onValueChange(details)` et
`onStatusChange(details)` ; `timeout` définit la durée pendant laquelle l’état copié reste actif.

Utilisez `useClipboard()` avec `ClipboardRootProvider` uniquement lorsque le store doit
être créé ou partagé en dehors de l’arbre rendu. Les descendants peuvent lire cet état avec
`ClipboardContext` ou `useClipboardContext()`. En Solid, `useClipboard()` renvoie une fonction d’accès :
transmettez-le avec `value={clipboard}` et lisez l’état avec `clipboard().copied` ou
`clipboard().value`. En Vue, `useClipboard()` renvoie directement le store réactif : transmettez-le
avec `:value="clipboard"` et lisez `clipboard.copied` ou `clipboard.value`. La racine n’a pas d’état `disabled` : transmettez la prop native `disabled` à
`ClipboardInput` et à `ClipboardTrigger`. Utilisez `asChild` sur ces parties uniquement lorsqu’un
autre élément sémantique doit porter le nœud DOM rendu.

## Exemples

### Contexte

Lisez l’état du presse-papiers depuis un contenu imbriqué avec `ClipboardContext`, ou utilisez
`useClipboardContext()` dans un composant enfant. Le contexte expose `copied`, `value`, `setValue` et
`copy`.

<ExampleFrame variant="component">
  <ContextClipboardDemo />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
      { label: 'Vue', value: 'vue' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/clipboard/context.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={clipboardSolidContext} />

    </Tab>
    <Tab value="vue">

<CodeBlockRuntime lang="vue" code={clipboardVueContext} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/clipboard/component-context.module.css"
```
</div>

</ExampleCode>

### État de la copie

Utilisez `onStatusChange(details)` lorsqu’une autre partie de l’interface doit réagir après une copie
réussie. L’exemple conserve un compteur fondé sur `details.copied`.

<ExampleFrame variant="component">
  <ClipboardStatusDemo />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
      { label: 'Vue', value: 'vue' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/clipboard/copy-status.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={clipboardSolidCopyStatus} />

    </Tab>
    <Tab value="vue">

<CodeBlockRuntime lang="vue" code={clipboardVueCopyStatus} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/clipboard/component-copy-status.module.css"
```
</div>

</ExampleCode>

### Texte de copie personnalisé

Fournissez votre propre contenu ou une valeur `copied` à `ClipboardIndicator` lorsque le bouton
doit utiliser des libellés adaptés à votre produit.

<ExampleFrame variant="component">
  <CustomCopyTextClipboardDemo />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
      { label: 'Vue', value: 'vue' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/clipboard/custom-copy-text.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={clipboardSolidCustomCopyText} />

    </Tab>
    <Tab value="vue">

<CodeBlockRuntime lang="vue" code={clipboardVueCustomCopyText} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/clipboard/component-custom-copy-text.module.css"
```
</div>

</ExampleCode>

### État désactivé

Transmettez `disabled` au champ et au bouton lorsque les utilisateurs peuvent voir la valeur, mais ne
doivent ni la modifier ni la copier.

<ExampleFrame variant="component">
  <DisabledClipboardDemo />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
      { label: 'Vue', value: 'vue' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/clipboard/disabled.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={clipboardSolidDisabled} />

    </Tab>
    <Tab value="vue">

<CodeBlockRuntime lang="vue" code={clipboardVueDisabled} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/clipboard/component-disabled.module.css"
```
</div>

</ExampleCode>

### Root Provider

Créez le store avec `useClipboard()` et transmettez-le à `ClipboardRootProvider` lorsque
l’état doit être partagé ou créé indépendamment de l’arbre de composants rendu.

<ExampleFrame variant="component">
  <RootProviderClipboardDemo />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
      { label: 'Vue', value: 'vue' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/clipboard/root-provider.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={clipboardSolidRootProvider} />

    </Tab>
    <Tab value="vue">

<CodeBlockRuntime lang="vue" code={clipboardVueRootProvider} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/clipboard/component-root-provider.module.css"
```
</div>

</ExampleCode>

### Délai

Définissez `timeout` lorsque l’état copié doit rester visible plus ou moins longtemps que les trois
secondes par défaut.

<ExampleFrame variant="component">
  <TimeoutClipboardDemo />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
      { label: 'Vue', value: 'vue' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/clipboard/timeout.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={clipboardSolidTimeout} />

    </Tab>
    <Tab value="vue">

<CodeBlockRuntime lang="vue" code={clipboardVueTimeout} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/clipboard/component-timeout.module.css"
```
</div>

</ExampleCode>

### Texte de la valeur

Utilisez `ClipboardValueText` lorsque la valeur doit apparaître comme du texte plutôt que dans un
champ de saisie.

<ExampleFrame variant="component">
  <ValueTextClipboardDemo />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
      { label: 'Vue', value: 'vue' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/clipboard/value-text.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={clipboardSolidValueText} />

    </Tab>
    <Tab value="vue">

<CodeBlockRuntime lang="vue" code={clipboardVueValueText} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/clipboard/component-value-text.module.css"
```
</div>

</ExampleCode>

### Personnalisation avancée

Utilisez `asChild` pour que vos propres éléments de champ et de bouton rendent les parties Clipboard,
tout en conservant l’état et le comportement du composant.

<ExampleFrame variant="component">
  <AsChildClipboardDemo />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
      { label: 'Vue', value: 'vue' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/clipboard/as-child.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={clipboardSolidAsChild} />

    </Tab>
    <Tab value="vue">

<CodeBlockRuntime lang="vue" code={clipboardVueAsChild} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/clipboard/component-advanced-customization.module.css"
```
</div>

</ExampleCode>

## Styles

Utilisez `className` en React ou `class` en Solid et Vue sur les parties exportées pour les styles
structurels. Les variantes Tailwind utilisent les utilitaires natifs et les classes du consommateur ;
les variantes CSS Modules utilisent les variables CSS ci-dessous. Pour les styles liés à la copie,
utilisez l’attribut Ark `data-copied` avec les hooks `data-slot` de moduix.

### Propriétés CSS

<CssPropertiesSection properties={ClipboardDocs.clipboardOverrideCssProperties} />

### Points d’accroche CSS

- `Clipboard` -> `data-slot="clipboard-root"`
- `ClipboardRootProvider` -> `data-slot="clipboard-root-provider"`
- `ClipboardLabel` -> `data-slot="clipboard-label"`
- `ClipboardControl` -> `data-slot="clipboard-control"`
- `ClipboardInput` -> `data-slot="clipboard-input"`
- `ClipboardTrigger` -> `data-slot="clipboard-trigger"`
- `ClipboardIndicator` -> `data-slot="clipboard-indicator"`
- `ClipboardValueText` -> `data-slot="clipboard-value-text"`
- Ark ajoute `data-copied` à `Clipboard`, `ClipboardLabel`, `ClipboardControl`, `ClipboardInput` et
  `ClipboardTrigger` tant que l’état copié est actif.
- Ark ajoute `data-readonly` à `ClipboardInput` lorsque le champ est en lecture seule.
- `ClipboardIndicator` ajoute `data-slot="clipboard-indicator-idle-icon"` et
  `data-slot="clipboard-indicator-copied-icon"` aux nœuds de ses icônes par défaut.
