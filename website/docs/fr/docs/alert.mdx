---
title: Alert
component: alert
description: Messages intégrés à la page pour les mises à jour de statut, la validation et les messages système.
---

import * as AlertDocs from '@/components/examples/alert/css-properties';
import AlertBasic from '@/components/examples/alert/basic.tsx';
import AlertCustomComposition from '@/components/examples/alert/custom-composition.tsx';
import AlertCustomHeading from '@/components/examples/alert/custom-heading.tsx';
import AlertError from '@/components/examples/alert/error.tsx';
import AlertStatuses from '@/components/examples/alert/statuses.tsx';
import AlertWithIcon from '@/components/examples/alert/with-icon.tsx';

import alertSolidBasic from '../../../snippets/alert/solid/basic.tsx?raw';
import alertSolidCustomComposition from '../../../snippets/alert/solid/custom-composition.tsx?raw';
import alertSolidCustomHeading from '../../../snippets/alert/solid/custom-heading.tsx?raw';
import alertSolidError from '../../../snippets/alert/solid/error.tsx?raw';
import alertSolidStatuses from '../../../snippets/alert/solid/statuses.tsx?raw';
import alertSolidWithIcon from '../../../snippets/alert/solid/with-icon.tsx?raw';

## Référence de l’API

Ark UI ne fournit pas de primitive `alert` dédiée. moduix construit `Alert` avec la fabrique officielle
d’Ark, en suivant les recommandations d’Ark en matière de [composition](https://ark-ui.com/docs/guides/composition), de
[styles](https://ark-ui.com/docs/guides/styling) et de [gestion des refs](https://ark-ui.com/docs/guides/ref).

Importez `Alert` depuis `@moduix/react/alert`. Vous pouvez utiliser `Alert` comme composant racine ou
passer par `Alert.Root` : les deux formes rendent la même partie racine. Les autres parties publiques sont
`Alert.Indicator`, `Alert.Content`, `Alert.Title`,
`Alert.Description` et `Alert.Actions`.

Chaque partie accepte les propriétés usuelles de l’élément qu’elle rend, notamment `className` et
`asChild`, et transmet la ref à l’élément rendu.

## Choisir le bon composant

Utilisez `Alert` lorsqu’un message doit faire partie de la mise en page et occuper une place dans le
flux normal du document.

- Utilisez **`Alert`** pour les mises à jour de statut, les résultats de validation et les messages système dans les formulaires, les panneaux ou le contenu de la page.
- Utilisez **`Toast`** pour les notifications temporaires qui s’affichent au-dessus de la page, n’occupent pas de place dans le flux du document et se ferment automatiquement.


Les mêmes exports sont disponibles depuis `@moduix/solid/alert` pour Solid.
## Exemple de base

Pour un message intégré courant, utilisez la forme courte `<Alert>` avec `Alert.Title` et
`Alert.Description`. Ajoutez `Alert.Indicator` lorsqu’une icône aide à comprendre plus rapidement le
type de message.


Ce composant est disponible pour React et Solid, avec des variantes CSS Modules et Tailwind. L’aperçu utilise React avec CSS Modules ; les onglets affichent le code natif correspondant à chaque framework.
<ExampleFrame variant="component">
  <AlertBasic />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/alert/basic.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={alertSolidBasic} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

## Installation avec shadcn

Consultez le [Démarrage rapide](/docs/quick-start) pour la configuration complète du paquet, du registre et des styles.

<Tabs groupId="framework">
  <Tab label="React" value="react">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-react/alert' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-react-tailwind/alert' },
  ]}
/>

  </Tab>
  <Tab label="Solid" value="solid">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-solid/alert' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-solid-tailwind/alert' },
  ]}
/>

  </Tab>
</Tabs>

## Anatomie

Placez le contenu du message dans `Alert.Content`. `Alert.Indicator` est facultatif et se place à
côté de ce contenu lorsqu’il aide à identifier le message.

```text
Alert / Alert.Root
├─ Alert.Indicator (optional)
└─ Alert.Content
   ├─ Alert.Title
   ├─ Alert.Description
   ├─ Alert.Actions (optional)
   └─ Extra custom blocks
```

| Partie                 | Rôle                                                                  | `data-part`   |
| ---------------------- | --------------------------------------------------------------------- | ------------- |
| `Alert` / `Alert.Root` | Élément racine. Applique les styles du statut et la sémantique automatique de région dynamique. | `root`        |
| `Alert.Indicator`      | Repère visuel facultatif. Les icônes SVG qu’il contient ont une taille cohérente. | `indicator`   |
| `Alert.Content`        | Conteneur en colonne obligatoire pour le contenu et les actions.   | `content`     |
| `Alert.Title`          | Titre de l’alerte. Rend un `p` par défaut.                              | `title`       |
| `Alert.Description`    | Texte complémentaire du message.                                       | `description` |
| `Alert.Actions`        | Ligne d’actions facultative avec retour à la ligne dans `Content`.     | `actions`     |

## Composition

`Alert` est un composant de présentation : le composant qui l’affiche gère sa visibilité et sa fermeture. Il ne prend pas en charge le focus ni le clavier des boutons et des liens qu’il contient. `Alert.Content` est obligatoire et regroupe le titre, la description, les contrôles et le reste du contenu. Placez les contrôles dans `Alert.Actions` lorsqu’ils doivent bénéficier de la mise en ligne avec retour à la ligne.

Si le titre doit utiliser un autre élément sémantique, utilisez `Alert.Title asChild` avec un seul enfant sémantique. En Solid, `asChild` utilise la forme avec fonction de rendu d’Ark UI, comme dans l’exemple « Titre personnalisé ».
## Exemples

### Titre personnalisé

Utilisez `asChild` sur `Alert.Title` lorsque le titre de l’alerte doit s’inscrire dans la hiérarchie
des titres de la page.

<ExampleFrame variant="component">
  <AlertCustomHeading />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/alert/custom-heading.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={alertSolidCustomHeading} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Erreur

Définissez `status="error"` lorsque le message doit être annoncé avec une priorité élevée et bénéficier
d’un accent visuel plus marqué.

<ExampleFrame variant="component">
  <AlertError />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/alert/error.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={alertSolidError} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Statuts

Utilisez les quatre statuts pour distinguer les messages d’information, de réussite, d’avertissement et
d’erreur. Ajoutez `Alert.Indicator` avec l’icône de votre choix lorsque ce repère visuel est utile.

<ExampleFrame variant="component">
  <AlertStatuses />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/alert/statuses.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={alertSolidStatuses} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/alert/alert-statuses.module.css"
```
</div>

</ExampleCode>

### Avec icône

Placez une icône moduix ou un SVG personnalisé dans `Alert.Indicator`.

<ExampleFrame variant="component">
  <AlertWithIcon />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/alert/with-icon.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={alertSolidWithIcon} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Personnalisation avancée

Utilisez `Alert.Content` et `Alert.Actions` lorsque le message doit proposer des actions pour corriger
un problème ou poursuivre le parcours. Gérez l’état de fermeture dans le composant qui affiche
l’alerte si vous devez pouvoir la masquer localement.

<ExampleFrame variant="component">
  <AlertCustomComposition />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/alert/custom-composition.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={alertSolidCustomComposition} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/alert/alert-custom-composition.module.css"
```
</div>

</ExampleCode>

## Styles

Utilisez `className` en React ou `class` en Solid sur la racine et ses différentes parties pour
appliquer des styles locaux. Les variantes Tailwind se personnalisent avec des utilitaires et des
classes supplémentaires. Dans les variantes CSS Modules, définissez les variables
`--moduix-alert-*` sur la racine lorsqu’une alerte nécessite un thème local.

Dans les variantes CSS Modules, les variables communes `--moduix-alert-bg`,
`--moduix-alert-border-color`, `--moduix-alert-color` et `--moduix-alert-indicator-color`
s’appliquent à tous les statuts. Utilisez les variables `--moduix-alert-<status>-*` correspondantes
lorsqu’un seul statut doit changer.

Les statuts intégrés utilisent également les tokens de palette partagée :

| Statut    | Token partagé                | Valeur par défaut            |
| --------- | ---------------------------- | ---------------------------- |
| `info`    | `--moduix-color-card`        | surface de carte du thème    |
| `success` | `--moduix-color-success`     | `oklch(0.627 0.194 149.214)` |
| `warning` | `--moduix-color-warning`     | `oklch(0.795 0.184 86.047)`  |
| `error`   | `--moduix-color-destructive` | couleur destructive du thème |

### Propriétés CSS

<CssPropertiesSection properties={AlertDocs.alertCssProperties} />

### Sélecteurs de style

- Sélecteurs `data-slot` stables : `alert-root`, `alert-indicator`, `alert-content`, `alert-title`, `alert-description` et `alert-actions`
- Sélecteurs de style Ark : `data-scope="alert"` avec `data-part="root"`, `indicator`, `content`, `title`, `description` ou `actions`
- `Alert.Root` écrit le statut sélectionné dans `data-status`
