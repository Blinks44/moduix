---
title: Progress Circular
component: progress-circular
description: Indicateur circulaire pour suivre une progression déterminée ou indéterminée.
---

import * as ProgressCircularDocs from '@/components/examples/progress-circular/css-properties';
import ProgressCircularBasic from '@/components/examples/progress-circular/basic.tsx';
import CirclePartsProgressCircular from '@/components/examples/progress-circular/circle-parts.tsx';
import ControlledProgressCircular from '@/components/examples/progress-circular/controlled.tsx';
import IndeterminateProgressCircular from '@/components/examples/progress-circular/indeterminate.tsx';
import MinMaxProgressCircular from '@/components/examples/progress-circular/min-max.tsx';
import RootProviderProgressCircular from '@/components/examples/progress-circular/root-provider.tsx';
import StateViewsProgressCircular from '@/components/examples/progress-circular/state-views.tsx';
import ValueTextProgressCircular from '@/components/examples/progress-circular/value-text.tsx';
import progressCircularSolidBasic from '../../../snippets/progress-circular/solid/basic.tsx?raw';
import progressCircularSolidCircleParts from '../../../snippets/progress-circular/solid/circle-parts.tsx?raw';
import progressCircularSolidControlled from '../../../snippets/progress-circular/solid/controlled.tsx?raw';
import progressCircularSolidIndeterminate from '../../../snippets/progress-circular/solid/indeterminate.tsx?raw';
import progressCircularSolidMinMax from '../../../snippets/progress-circular/solid/min-max.tsx?raw';
import progressCircularSolidRootProvider from '../../../snippets/progress-circular/solid/root-provider.tsx?raw';
import progressCircularSolidStateViews from '../../../snippets/progress-circular/solid/state-views.tsx?raw';
import progressCircularSolidValueText from '../../../snippets/progress-circular/solid/value-text.tsx?raw';

## Référence de l’API

<PrimitiveReference href="https://ark-ui.com/docs/components/progress-circular" />

## Choisir le bon composant

Utilisez `ProgressCircular` pour afficher une indication compacte de l’avancement dans un texte,
des cartes, des badges de barre d’outils et de petits tableaux de bord. Choisissez `ProgressLinear`
lorsque la progression se compare plus facilement sur une barre horizontale.

## Exemple de base


Utilisez `ProgressCircularRing` pour afficher le cercle SVG standard, sa piste et son arc de
progression. Ajoutez `ProgressCircularValueText` pour afficher la valeur actuelle. L’exemple place
ce texte au centre du cercle grâce à un conteneur externe. Donnez à `ProgressCircularRing` un `aria-label` qui
identifie la tâche et correspond au libellé visible.

<ExampleFrame variant="component">
  <ProgressCircularBasic />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/progress-circular/basic.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={progressCircularSolidBasic} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/progress-circular/component-basic.module.css"
```
</div>

</ExampleCode>

## Installation avec shadcn

Consultez le [Démarrage rapide](/docs/quick-start) pour la configuration complète du paquet, du registre et des styles.

<Tabs groupId="framework">
  <Tab label="React" value="react">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-react/progress-circular' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-react-tailwind/progress-circular' },
  ]}
/>

  </Tab>
  <Tab label="Solid" value="solid">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-solid/progress-circular' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-solid-tailwind/progress-circular' },
  ]}
/>

  </Tab>
</Tabs>

## Anatomie

```text
ProgressCircular
├─ ProgressCircularLabel
├─ ProgressCircularCircle
│  ├─ ProgressCircularCircleTrack
│  └─ ProgressCircularCircleRange
├─ ProgressCircularRing
│  └─ fixed ProgressCircularCircle, ProgressCircularCircleTrack, and ProgressCircularCircleRange subtree
├─ ProgressCircularValueText
└─ ProgressCircularView

ProgressCircularRootProvider
└─ same part tree connected to useProgress()
```

| Partie                                        | Rôle                                                                                |
| --------------------------------------------- | ----------------------------------------------------------------------------------- |
| `ProgressCircular`  | Gère l’état de progression, le formatage et les identifiants générés.               |
| `ProgressCircularRootProvider`               | Relie les parties à l’état créé par `useProgress()`.               |
| `ProgressCircularContext`                    | Expose l’état courant via une fonction de rendu.                                     |
| `ProgressCircularLabel`                      | Fournit le contexte visible de l’indicateur de progression.                         |
| `ProgressCircularRing`                       | Partie pratique qui rend le cercle, la piste et l’arc par défaut.                   |
| `ProgressCircularCircle`                     | Élément SVG auquel Ark ajoute le rôle et les attributs ARIA de valeur.              |
| `ProgressCircularCircleTrack`                | Cercle d’arrière-plan.                                                              |
| `ProgressCircularCircleRange`                | Arc au premier plan qui reflète la progression actuelle ou l’état indéterminé.      |
| `ProgressCircularValueText`                  | Affiche la valeur formatée et annonce les mises à jour.                             |
| `ProgressCircularView`                       | Rend du contenu pour les états `indeterminate`, `loading` ou `complete`.            |

## Composition

`ProgressCircular` est le raccourci de `ProgressCircular`. Utilisez `ProgressCircularRing`
lorsque le cercle standard suffit : il combine `ProgressCircularCircle`, `ProgressCircularCircleTrack` et `ProgressCircularCircleRange`, mais ne
rend pas et ne centre pas `ProgressCircularValueText`. Gardez la composition du libellé, du cercle et du texte de
valeur explicite lorsque la valeur doit apparaître au centre.

Utilisez `defaultValue` lorsque le composant possède l’état de progression. Utilisez `value` avec
`onValueChange(details)` lorsque l’application le possède. Définissez `defaultValue={null}` ou
`value={null}` pour une progression indéterminée. Lorsque l’état doit être créé en dehors de l’arbre
rendu, créez-le avec `useProgress()` et passez le store à
`ProgressCircularRootProvider` au lieu de rendre une autre racine pour le même store.

## Exemples

### État contrôlé

Conservez `value` dans l’état de l’application lorsque la progression doit rester synchronisée avec
un autre contrôle ou une tâche externe.

<ExampleFrame variant="component">
  <ControlledProgressCircular />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/progress-circular/controlled.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={progressCircularSolidControlled} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/progress-circular/component-controlled.module.css"
```
</div>

</ExampleCode>

### Progression indéterminée

Définissez `defaultValue={null}` ou `value={null}` lorsque la tâche est en cours, mais que sa
progression ne peut pas encore être mesurée.

<ExampleFrame variant="component">
  <IndeterminateProgressCircular />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/progress-circular/indeterminate.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={progressCircularSolidIndeterminate} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/progress-circular/component-indeterminate.module.css"
```
</div>

</ExampleCode>

### Minimum et maximum

Utilisez `min` et `max` pour représenter des valeurs dans une plage autre que la plage par défaut de
`0` à `100`.

<ExampleFrame variant="component">
  <MinMaxProgressCircular />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/progress-circular/min-max.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={progressCircularSolidMinMax} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/progress-circular/component-min-max.module.css"
```
</div>

</ExampleCode>

### Texte de la valeur

Utilisez `ProgressCircularContext` avec `translations.value(details)` lorsque le texte visible de la valeur doit
être formulé pour une tâche précise. Rendez `progress.valueAsString` comme enfant de `ProgressCircularValueText`
afin que le texte visible corresponde à la chaîne de valeur accessible.

<ExampleFrame variant="component">
  <ValueTextProgressCircular />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/progress-circular/value-text.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={progressCircularSolidValueText} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Personnalisation avancée

Utilisez les parties individuelles du cercle à la place de `ProgressCircularRing` lorsque
l’arborescence SVG ou la gestion de l’état doit être personnalisée. Seule la composition directe
permet de donner à `ProgressCircularCircleTrack` et `ProgressCircularCircleRange` leurs propres classes, comme dans cet exemple.

<ExampleFrame variant="component">
  <CirclePartsProgressCircular />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/progress-circular/circle-parts.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={progressCircularSolidCircleParts} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/progress-circular/component-circle-parts.module.css"
```
</div>

</ExampleCode>

#### ProgressCircularRootProvider

Créez le store avec `useProgress()` et passez-le à `ProgressCircularRootProvider` lorsqu’un autre
composant doit lire ou mettre à jour la progression en dehors de l’arbre rendu.

<ExampleFrame variant="component">
  <RootProviderProgressCircular />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/progress-circular/root-provider.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={progressCircularSolidRootProvider} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/progress-circular/component-root-provider.module.css"
```
</div>

</ExampleCode>

#### Vues d’état

Utilisez `ProgressCircularView` lorsque le texte ou l’interface d’accompagnement doit changer avec
l’état de progression Ark.

<ExampleFrame variant="component">
  <StateViewsProgressCircular />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/progress-circular/state-views.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={progressCircularSolidStateViews} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/progress-circular/component-state-views.module.css"
```
</div>

</ExampleCode>

## Styles

Utilisez `className` en React et `class` en Solid sur les parties exportées pour la structure et
l’apparence. Les variantes Tailwind utilisent des classes utilitaires natives et les classes du
consommateur. Les variantes CSS Modules exposent les variables CSS ci-dessous. Pour les styles liés
à l’état, utilisez les attributs de données Ark, les variables SVG Ark et les points d’accroche
`data-slot` de moduix.

Utilisez `--moduix-progress-circular-range-indeterminate-bg` pour donner à la progression
indéterminée une couleur sémantique distincte. Lorsque l’utilisateur préfère réduire les animations,
l’arc n’effectue ni transition ni rotation. Utilisez
`--moduix-progress-circular-range-indeterminate-reduced-motion-dasharray` pour conserver un repère
statique visible.

### Propriétés CSS

<div className="docs-reference">
  <Tabs
    values={[{ label: 'Variables CSS', value: 'CSS Variables' }]}
    className="docs-reference-tabs"
    contentItemClassName="docs-reference-panel"
  >
    <Tab>
      <ProgressCircularDocs.ProgressCircularCssPropertiesPanel />
    </Tab>
  </Tabs>
</div>

### Points d’accroche CSS

- `data-slot="progress-circular-root"`, `data-slot="progress-circular-root-provider"`,
  `data-slot="progress-circular-label"`, `data-slot="progress-circular-circle"`,
  `data-slot="progress-circular-circle-track"`, `data-slot="progress-circular-circle-range"`,
  `data-slot="progress-circular-value-text"` et `data-slot="progress-circular-view"`.
- `ProgressCircularRing` utilise les mêmes `data-slot`, `className`, ref et variables de taille et
  d’épaisseur `--moduix-progress-circular-*` que `ProgressCircularCircle`.
- Attributs Ark : `data-scope="progress"`, `data-part`, `data-state="indeterminate|loading|complete"`,
  `data-orientation`, `data-value` et `data-max`.
- Variables SVG Ark du cercle : `--size`, `--thickness`, `--radius`, `--circumference`, `--percent`
  et `--offset`.
