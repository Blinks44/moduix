---
title: Timer
component: timer
description: Un composant pour suivre le temps écoulé ou restant, avec des contrôles explicites en React ou Solid.
---

import * as TimerDocs from '@/components/examples/timer/css-properties';
import TimerBasic from '@/components/examples/timer/basic.tsx';
import TimerCountdown from '@/components/examples/timer/countdown.tsx';
import TimerEvents from '@/components/examples/timer/events.tsx';
import TimerInterval from '@/components/examples/timer/interval.tsx';
import TimerPomodoro from '@/components/examples/timer/pomodoro.tsx';
import TimerRootProvider from '@/components/examples/timer/root-provider.tsx';
import timerSolidBasic from '../../../snippets/timer/solid/basic.tsx?raw';
import timerSolidCountdown from '../../../snippets/timer/solid/countdown.tsx?raw';
import timerSolidEvents from '../../../snippets/timer/solid/events.tsx?raw';
import timerSolidInterval from '../../../snippets/timer/solid/interval.tsx?raw';
import timerSolidPomodoro from '../../../snippets/timer/solid/pomodoro.tsx?raw';
import timerSolidRootProvider from '../../../snippets/timer/solid/root-provider.tsx?raw';

## Référence de l’API

<PrimitiveReference href="https://ark-ui.com/docs/components/timer" />

## Exemple de base


`TimerSegments` est la façon la plus simple d’afficher le temps au format courant, avec des deux-points comme
séparateurs. Par défaut, il affiche les heures, les minutes et les secondes. Passez `types` pour choisir un autre
ensemble d’unités. Utilisez les éléments de composition détaillés si vous avez besoin de libellés d’unité, de
séparateurs personnalisés ou d’une mise en forme différente pour chaque valeur.

`TimerSegments` accepte toutes les propriétés de `TimerArea`, sauf `children` et `asChild`, et transmet la ref de
la zone. Comme il crée lui-même les éléments et les séparateurs, utilisez `TimerArea` si vous devez remplacer
l’élément hôte ou fournir vos propres enfants.

<ExampleFrame variant="component">
  <TimerBasic />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/timer/basic.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={timerSolidBasic} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

## Installation avec shadcn

Consultez le [Démarrage rapide](/docs/quick-start) pour la configuration complète du paquet, du registre et des styles.

<Tabs groupId="framework">
  <Tab label="React" value="react">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-react/timer' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-react-tailwind/timer' },
  ]}
/>

  </Tab>
  <Tab label="Solid" value="solid">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-solid/timer' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-solid-tailwind/timer' },
  ]}
/>

  </Tab>
</Tabs>

## Anatomie

```text
Timer
├─ TimerArea
│  ├─ TimerItem
│  └─ TimerSeparator
└─ TimerControl
   └─ TimerActionTrigger

TimerRootProvider
└─ même arbre de composition relié à l’état créé par `useTimer()`

TimerSegments
└─ TimerArea avec les éléments TimerItem et TimerSeparator
```

| Élément | Rôle |
| ----------------------- | ------------------------------------------------------------------------ |
| `Timer` | Gère l’état du minuteur Ark, ses identifiants, l’intervalle et les fonctions de rappel. |
| `TimerRootProvider` | Relie les éléments à l’état créé par `useTimer()`. |
| `TimerSegments` | Construit les unités de temps standard et leurs séparateurs. |
| `TimerArea` | Regroupe les valeurs de temps visibles. |
| `TimerItem` | Affiche une unité de temps formatée, comme `hours`, `minutes` ou `seconds`. |
| `TimerSeparator` | Affiche un séparateur entre les unités de temps. |
| `TimerControl` | Regroupe les déclencheurs d’action. |
| `TimerActionTrigger` | Déclenche l’action `start`, `pause`, `resume`, `reset` ou `restart`. |

## Composition

`Timer` gère l’état du minuteur. `TimerSegments` crée lui-même les éléments `TimerArea`,
`TimerItem` et `TimerSeparator`. Utilisez donc la composition complète avec `TimerArea` pour ajouter des libellés
d’unité, des séparateurs personnalisés ou une mise en page propre à chaque valeur.

Utilisez `TimerRootProvider` avec `useTimer()` lorsque l’état doit être créé en dehors de l’arbre de composition
rendu. `TimerContext` et `useTimerContext()` conviennent lorsque d’autres éléments de l’interface doivent accéder à
l’état ou aux méthodes du minuteur. En Solid, l’état et le contexte se lisent via des fonctions réactives : utilisez
`timer()` et `api()`.

## Exemples

### Compte à rebours

Définissez `countdown` et `startMs` si l’affichage doit décompter jusqu’à zéro.

<ExampleFrame variant="component">
  <TimerCountdown />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/timer/countdown.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={timerSolidCountdown} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/timer/timer-countdown.module.css"
```
</div>

</ExampleCode>

### Événements

Utilisez `onTick` et `onComplete` pour les effets de bord liés au cycle de vie du minuteur.

<ExampleFrame variant="component">
  <TimerEvents />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/timer/events.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={timerSolidEvents} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/timer/timer-events.module.css"
```
</div>

</ExampleCode>

### Intervalle

Utilisez une valeur plus petite pour `interval` avec un élément `milliseconds` si l’interface doit être actualisée
plusieurs fois par seconde.

<ExampleFrame variant="component">
  <TimerInterval />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/timer/interval.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={timerSolidInterval} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/timer/timer-interval.module.css"
```
</div>

</ExampleCode>

### Pomodoro

Utilisez `onComplete` pour passer du travail à la pause et inversement lorsque le minuteur doit enchaîner les cycles.

<ExampleFrame variant="component">
  <TimerPomodoro />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/timer/pomodoro.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={timerSolidPomodoro} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/timer/timer-pomodoro.module.css"
```
</div>

</ExampleCode>

### Personnalisation avancée

Utilisez `useTimer()` lorsque l’état et les méthodes du minuteur doivent aussi être disponibles en dehors de l’arbre
de composition rendu. C’est également la bonne approche pour ajouter des libellés d’unité ou composer des éléments
personnalisés.

<ExampleFrame variant="component">
  <TimerRootProvider />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/timer/root-provider.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={timerSolidRootProvider} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/timer/timer-root-provider.module.css"
```
</div>

</ExampleCode>

## Styles

Pour modifier la structure, utilisez `className` sur les éléments exportés et les variables CSS pour les valeurs
visuelles par défaut. Les attributs Ark restent disponibles pour cibler chaque élément.

### Propriétés CSS

<div className="docs-reference">
  <Tabs
    values={[{ label: 'Variables CSS', value: 'CSS Variables' }]}
    className="docs-reference-tabs"
    contentItemClassName="docs-reference-panel"
  >
    <Tab>
      <TimerDocs.TimerCssPropertiesPanel />
    </Tab>
  </Tabs>
</div>

### Sélecteurs et attributs de style

- Sélecteurs `data-slot="timer-root"`, `timer-root-provider`, `timer-area`, `timer-item`,
  `timer-separator`, `timer-control` et `timer-action-trigger`.
- Attributs de structure Ark : `data-scope="timer"` et
  `data-part="root|area|item|separator|control|action-trigger"`.
- `TimerItem` expose `data-type` et la variable CSS `--value` mise à jour par Ark.
- Ark ajoute l’attribut `hidden` aux déclencheurs d’action indisponibles dans l’état courant du minuteur.
