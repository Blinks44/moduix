---
title: Mot de passe oublié
description: Un formulaire prêt à copier pour demander la réinitialisation du mot de passe avec les composants moduix.
---

import { ForgotPassword } from '@/components/recipes/forgot-password/forgot-password';
import forgotPasswordSolid from '../../../snippets/recipes/solid/forgot-password.tsx?raw';
import forgotPasswordRouteReact from '../../../snippets/recipes/react/forgot-password-route.tsx?raw';
import forgotPasswordRouteSolid from '../../../snippets/recipes/solid/forgot-password-route.tsx?raw';

# Mot de passe oublié

Utilisez cette recette comme point de départ pour un écran de récupération du mot de passe. Elle associe
`Card`, `Field`, `Input` et `Button`. Le navigateur vérifie que l’adresse e-mail est renseignée et
correctement formatée avant d’appeler `onSubmit`. La recette n’appelle aucun service de réinitialisation
et n’affiche pas les états de réussite ou d’erreur : ajoutez cette logique dans votre application.

L’aperçu interactif utilise le paquet React. Les onglets de code présentent des implémentations natives pour React et Solid avec les mêmes styles.

<ExampleFrame variant="recipe" clientOnly>
  <PreviewFrame maxWidth="sm">
  <ForgotPassword onSubmit={(event) => event.preventDefault()} />
  </PreviewFrame>
</ExampleFrame>

Dans l’aperçu, le gestionnaire `onSubmit` se contente d’appeler `event.preventDefault()` : aucun e-mail
réel n’est envoyé. Copiez la recette et son fichier CSS dans votre application, puis reliez-la à votre API
de réinitialisation.

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/recipes/forgot-password/forgot-password.tsx"

```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={forgotPasswordSolid} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/recipes/forgot-password/forgot-password-form.module.css"

```
</div>

</ExampleCode>

## Installation

<ShadcnInstall packageName={['button', 'card', 'field', 'input']} itemKind="recipe" frameworks copiedSource />

## Utiliser la recette dans une route

Copiez la recette et son fichier CSS depuis les onglets ci-dessus, puis importez `ForgotPassword` depuis
le fichier copié. Affichez `ForgotPassword` sur la route où l’utilisateur demande la réinitialisation du mot de passe.
Le navigateur applique les contraintes natives avant d’appeler `onSubmit`. Récupérez l’adresse dans
`FormData` et envoyez-la à votre API de réinitialisation :

<Tabs
  groupId="framework"
  values={[
    { label: 'React', value: 'react' },
    { label: 'Solid', value: 'solid' },
  ]}
>
  <Tab value="react">

<CodeBlockRuntime lang="tsx" code={forgotPasswordRouteReact} />

  </Tab>
  <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={forgotPasswordRouteSolid} />

  </Tab>
</Tabs>

Les données du formulaire contiennent un seul champ : `email`. Remplacez `/api/forgot-password` et le chemin
d’importation par les valeurs de votre application.

## Avant la mise en production

Le code copié reste volontairement minimal. Ajoutez le comportement et l’interface d’erreur nécessaires
à votre application :

- Pendant la requête, désactivez le bouton d’envoi pour éviter les doublons. Utilisez `loading` ou `disabled`
  sur `Button`.
- Après une requête réussie, affichez la même confirmation neutre pour toutes les adresses, par exemple
  `Si un compte correspond à cette adresse, nous enverrons un lien de réinitialisation.` Ne révélez pas si
  le compte existe.
- Si le serveur limite les requêtes, indiquez à l’utilisateur quand réessayer. Faites appliquer la limite
  réelle par le serveur.
- En cas d’échec, conservez l’adresse, affichez une erreur générale avec `role="alert"` et permettez une
  nouvelle tentative.
- Dans le code copié, remplacez le lien `/sign-in` par la route de votre application.
