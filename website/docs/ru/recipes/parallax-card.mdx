---
title: Карточка с параллаксом
description: Готовый к копированию рецепт moduix с декоративным фоновым изображением и лёгким 3D-наклоном.
---

import { ParallaxCardPreview } from '@/components/recipes/parallax-card-preview';
import parallaxCardSolid from '../../../snippets/recipes/solid/parallax-card.tsx?raw';

# Карточка с параллаксом

Этот рецепт сочетает `CardBackground`, небольшой обработчик указателя и CSS-трансформации. Получается
карточка с фоновым изображением на всю площадь и лёгким 3D-наклоном. Это готовая композиция приложения,
а не компонент `ParallaxCard`, экспортируемый moduix.

`CardBackground` размещает изображение за содержимым карточки. Градиент поверх изображения и контраст
текста задаются в CSS-примере, поэтому их можно настроить под выбранное изображение. Наклон включается
только на устройствах с точным указателем, поддерживающим наведение. Если такой указатель недоступен или
пользователь включил уменьшение анимации, карточка остаётся статичной.

Живое превью использует React-пакет. Во вкладках исходников приведены нативные реализации для React и Solid с общими стилями.

<ExampleFrame variant="recipe" clientOnly>
  <PreviewFrame maxWidth="sm">
  <ParallaxCardPreview />
  </PreviewFrame>
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/recipes/parallax-card.tsx"

```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={parallaxCardSolid} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/recipes/parallax-card.module.css"

```
</div>

</ExampleCode>

## Установка

<ShadcnInstall packageName="card" itemKind="recipe" frameworks copiedSource />

## Используйте рецепт в приложении

Скопируйте компонент и CSS выше в своё приложение, затем:

- замените URL изображения и видимый текст своим содержимым;
- оставьте для изображения внутри `CardBackground` пустой `alt=""`, поскольку оно декоративное. Если
  изображение передаёт важную информацию, используйте `CardMedia` и добавьте альтернативный текст;
- воспринимайте наклон как визуальное дополнение. Карточка должна оставаться читаемой и удобной без
  наведения и анимации;
- добавьте настоящую ссылку или кнопку, если карточка должна выполнять действие. Если вся карточка является
  одной ссылкой без вложенных элементов управления, используйте `Card asChild`. Если в карточке есть
  отдельные действия, поместите `CardLink` в `CardTitle`.
