---
title: Панель управления с боковой навигацией
description: Готовый каркас панели управления с изменяемой по ширине и сворачиваемой боковой навигацией, вложенными пунктами и меню рабочего пространства.
---

import { SidebarDashboardPreview } from '@/components/recipes/sidebar-dashboard-preview';

# Панель управления с боковой навигацией

Используйте этот рецепт как основу для десктопной оболочки приложения. Он объединяет `Sidebar`,
`Collapsible` и `Menu`: ширину навигации можно менять, панель можно сворачивать, группу Projects
можно раскрывать, а в нижней части панели находится меню рабочего пространства. В основной области
показан небольшой обзор с карточками показателей, чтобы было понятно, куда добавить содержимое
приложения.

Ширину панели можно менять перетаскиванием разделителя или с клавиатуры. `Sidebar.Trigger`
сворачивает и разворачивает панель, а `Sidebar.Tooltip` показывает подписи пунктов, когда панель
сведена к узкой полосе с иконками.

Это готовая композиция для копирования, а не отдельный компонент `SidebarDashboard`, экспортируемый
из `@moduix/react`. Скопируйте компонент и CSS ниже, затем замените демонстрационные ссылки,
подписи и данные панели.

<ExampleFrame clientOnly>
  <SidebarDashboardPreview />
</ExampleFrame>

<div className="docs-reference">
  <Tabs values={[{ label: 'Компонент', value: 'Component' }, { label: 'Стили', value: 'Styles' }]} className="docs-tabs">
    <Tab value="Component">

```tsx file="../../../src/components/recipes/sidebar-dashboard.tsx"

```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/recipes/sidebar-dashboard.module.css"

```

    </Tab>
  </Tabs>
</div>

## Установка

<ShadcnInstall packageName={['button', 'card', 'collapsible', 'menu', 'sidebar']} itemKind="recipe" copiedSource dependencies={['lucide-react']} />

## Настройте под приложение

Замените якоря `#overview`, `#website` и другие демонстрационные ссылки маршрутами приложения. Если
роутер предоставляет компонент ссылки, используйте его с `Sidebar.NavigationButton` или
`Sidebar.NavigationSubButton` и пропом `asChild`, чтобы роутер управлял навигацией. Помечайте
активный маршрут через `aria-current="page"`.

Используйте `Sidebar.Tooltip` для кнопок навигации с иконками, чтобы в свёрнутом состоянии у них
оставались подписи. Для вложенных групп используйте `Collapsible`, а для действий рабочего
пространства - `Menu`. Свяжите кнопку `New project` с настоящим сценарием создания проекта, а
статические показатели и сообщение в основной области замените данными приложения.

## Адаптивное поведение

Рецепт рассчитан на десктопный макет. `Sidebar` не предоставляет мобильную выдвижную панель и не
выбирает за вас адаптивную модель навигации. На небольших экранах добавьте `Drawer` или другой
оверлей, используя подходящую приложению структуру навигации. CSS примера только переводит
карточки показателей в одну колонку при ширине меньше `40rem`.

## Сохранение ширины панели

В примере используется стандартная ширина панели, которая не сохраняется между посещениями. Если
приложению нужно запоминать ширину на десктопе, управляйте `Sidebar` через `size`, обновляйте это
значение в `onResize(details)` во время изменения ширины, а окончательное значение сохраняйте из
`onResizeEnd(details)`. В SSR-приложении читайте хранилище браузера после гидратации и оставляйте
рабочее значение по умолчанию для первого рендера.
