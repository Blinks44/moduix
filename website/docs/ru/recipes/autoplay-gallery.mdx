---
title: Галерея с автопрокруткой
description: Рецепт галереи с автопрокруткой на Carousel, текстом поверх слайдов, стрелками навигации и индикаторами прогресса.
---

import { AutoplayGalleryPreview } from '@/components/recipes/autoplay-gallery-preview';

# Галерея с автопрокруткой

Используйте этот рецепт для основного контента, который должен переключаться автоматически, но при этом оставаться
под управлением пользователя. В нём `Carousel` показывает соседние слайды частично, выводит текст поверх изображения,
добавляет стрелки навигации и заполняющийся индикатор для каждого интервала автопрокрутки.

<ExampleFrame clientOnly>
  <AutoplayGalleryPreview />
</ExampleFrame>

<div className="docs-reference">
  <Tabs values={[{ label: 'Компонент', value: 'Компонент' }, { label: 'Стили', value: 'Стили' }]} className="docs-tabs">
    <Tab value="Компонент">

```tsx file="../../../src/components/recipes/autoplay-gallery.tsx"
```

    </Tab>
    <Tab value="Стили">

```css file="../../../src/components/recipes/autoplay-gallery.module.css"
```

    </Tab>
  </Tabs>
</div>

## Установка

<ShadcnInstall packageName="carousel" itemKind="recipe" copiedSource />

Эта команда добавляет `Carousel` в проект. Скопируйте компонент рецепта и его таблицу стилей из вкладок выше, затем
замените демонстрационные данные своим контентом.

## Использование на странице

Скопированный рецепт можно сразу адаптировать:

- Замените данные `slides` своими изображениями, категориями, заголовками, описаниями и альтернативным текстом.
  Сохраняйте стабильные значения `id` и поддерживайте `slideCount` равным `slides.length`.
- Измените `aria-label`, чтобы он описывал назначение вашей карусели, а не демонстрационного примера.
- Синхронизируйте `autoplay.delay` и длительность анимации прогресса. В рецепте используются `3500` миллисекунд и
  CSS-анимация `3.5s`, поэтому индикатор заполняется как раз к появлению следующего слайда.
- Перед публикацией замените ссылки Unsplash. Используйте источник изображений, которым управляет ваше приложение и
  который соответствует его требованиям к загрузке, кэшированию и содержимому.

Настройте `slidesPerPage`, `spacing` и `padding`, чтобы изменить видимую часть соседнего слайда и отступы галереи
внутри контейнера.

## Как это работает

Рецепт использует два блока `Carousel.Context`, подключённых к одному корневому компоненту. Первый получает API
карусели, отмечает активный слайд и выводит кнопки перехода назад и вперёд. Второй создаёт индикаторы прогресса из
`api.pageSnapPoints` и отмечает текущий индикатор атрибутом `data-current`.

При нажатии на стрелку или индикатор вызывается `requestAnimationFrame(api.play)`. Ручная навигация приостанавливает
автопрокрутку, поэтому запуск в следующем кадре даёт карусели сначала завершить переход, а затем начать новый
интервал. Взаимодействие свайпом или колёсиком мыши тоже ставит автопрокрутку на паузу. Если после такого действия
воспроизведение должно возобновляться автоматически, добавьте это в свою логику взаимодействия или выведите
`Carousel.AutoplayTrigger`.

Ark приостанавливает автопрокрутку, пока документ скрыт. Рецепт отслеживает `visibilitychange` и вызывает публичный
метод `api.play()`, когда документ снова становится видимым.

## Доступность и анимация

Задайте корневому компоненту доступное имя через `aria-label` или `aria-labelledby`. Для содержательных изображений
пишите понятный `alt`, а для декоративных используйте `alt=""`, если вся информация уже есть в тексте слайда.
Оставляйте на экране кнопки перехода и индикаторы, чтобы пользователю не приходилось ждать таймер.

При включённом `prefers-reduced-motion: reduce` рецепт отключает переходы слайдов и анимацию индикатора. Таймер
автопрокрутки при этом продолжает переключать карусель. Если пользователю нужно останавливать автоматические
переходы, добавьте `Carousel.AutoplayTrigger` или уберите свойство `autoplay`. Полное описание API и управления
автопрокруткой смотрите в [документации Carousel](/docs/carousel).
