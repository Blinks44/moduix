---
title: Раскрывающаяся группа аватаров
description: Компактная группа перекрывающихся аватаров, которая раздвигается при наведении или фокусе с клавиатуры и показывает подсказку для каждого участника.
---

import { ExpandableAvatarGroup } from '@/components/recipes/expandable-avatar-group';

# Раскрывающаяся группа аватаров

Используйте этот рецепт для небольшой команды, когда место ограничено, но пользователям всё равно
нужно легко различать участников. Наведите указатель на аватар или переведите на него фокус, чтобы
раздвинуть группу и увидеть имя и роль участника.

Этот рецепт можно скопировать в приложение. Это не компонент `ExpandableAvatarGroup`, экспортируемый
из `@moduix/react`, а композиция на основе `Avatar` и `Tooltip`. Установите эти компоненты, скопируйте
код и стили ниже, а затем замените демонстрационных участников данными из приложения.

<ExampleFrame clientOnly>
  <ExpandableAvatarGroup />
</ExampleFrame>

<div className="docs-reference">
  <Tabs values={[{ label: 'Компонент', value: 'Component' }, { label: 'Стили', value: 'Styles' }]} className="docs-tabs">
    <Tab value="Component">

```tsx file="../../../src/components/recipes/expandable-avatar-group.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/recipes/expandable-avatar-group.module.css"
```

    </Tab>

  </Tabs>
</div>

## Установка

<ShadcnInstall packageName={['avatar', 'tooltip']} itemKind="recipe" copiedSource />

## Настройка рецепта

Замените демонстрационный массив `members` данными из приложения и сохраняйте стабильный порядок
участников. Для каждого участника указывайте URL изображения из приложения.
`Avatar.Fallback` показывает инициалы, пока изображение загружается или недоступно.

Каждый участник в рецепте представлен нативной кнопкой. `Tooltip.Trigger asChild` позволяет подсказке
использовать эту кнопку без дополнительной обёртки, а `Tooltip.Body` показывает имя и роль участника.
В `aria-label` кнопки указаны оба значения, потому что у изображения задано `alt=""`, а подсказка
дополняет доступное имя, но не заменяет его. Текст подсказки должен быть коротким и необязательным
для понимания интерфейса.

Если нажатие на участника должно открывать страницу или выполнять действие, подключите это поведение
к кнопке или замените её подходящим семантическим элементом. Не убирайте доступное имя.

Используйте `role="group"` с понятной подписью для группы. Для большой команды показывайте только
нескольких первых участников и добавляйте кнопку `+N`, которая открывает полный список с поиском.

Вся анимация выполняется средствами CSS. Активная кнопка слегка приподнимается, а остальные кнопки
раздвигаются, освобождая для неё место. Правило `prefers-reduced-motion: reduce` в примере отключает
плавные переходы, но само перемещение выполняется сразу. Если политика доступности в приложении требует
полностью убрать это перемещение, задайте `transform: none` для состояний наведения и фокуса в этом
медиазапросе.
