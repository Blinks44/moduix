---
title: Clipboard
component: clipboard
description: Доступный Clipboard для React, Solid и Vue с установкой через пакет или shadcn CLI.
---

import AsChildClipboardDemo from '@/components/examples/clipboard/as-child.tsx';
import ClipboardBasic from '@/components/examples/clipboard/basic.tsx';
import ContextClipboardDemo from '@/components/examples/clipboard/context.tsx';
import ClipboardStatusDemo from '@/components/examples/clipboard/copy-status.tsx';
import CustomCopyTextClipboardDemo from '@/components/examples/clipboard/custom-copy-text.tsx';
import DisabledClipboardDemo from '@/components/examples/clipboard/disabled.tsx';
import RootProviderClipboardDemo from '@/components/examples/clipboard/root-provider.tsx';
import TimeoutClipboardDemo from '@/components/examples/clipboard/timeout.tsx';
import ValueTextClipboardDemo from '@/components/examples/clipboard/value-text.tsx';
import * as ClipboardDocs from '@/components/examples/clipboard/css-properties';
import clipboardSolidAsChild from '../../../snippets/clipboard/solid/as-child.tsx?raw';
import clipboardSolidBasic from '../../../snippets/clipboard/solid/basic.tsx?raw';
import clipboardSolidContext from '../../../snippets/clipboard/solid/context.tsx?raw';
import clipboardSolidCopyStatus from '../../../snippets/clipboard/solid/copy-status.tsx?raw';
import clipboardSolidCustomCopyText from '../../../snippets/clipboard/solid/custom-copy-text.tsx?raw';
import clipboardSolidDisabled from '../../../snippets/clipboard/solid/disabled.tsx?raw';
import clipboardSolidRootProvider from '../../../snippets/clipboard/solid/root-provider.tsx?raw';
import clipboardSolidTimeout from '../../../snippets/clipboard/solid/timeout.tsx?raw';
import clipboardSolidValueText from '../../../snippets/clipboard/solid/value-text.tsx?raw';
import clipboardVueAsChild from '../../../snippets/clipboard/vue/as-child.vue?raw';
import clipboardVueBasic from '../../../snippets/clipboard/vue/basic.vue?raw';
import clipboardVueContext from '../../../snippets/clipboard/vue/context.vue?raw';
import clipboardVueCopyStatus from '../../../snippets/clipboard/vue/copy-status.vue?raw';
import clipboardVueCustomCopyText from '../../../snippets/clipboard/vue/custom-copy-text.vue?raw';
import clipboardVueDisabled from '../../../snippets/clipboard/vue/disabled.vue?raw';
import clipboardVueRootProvider from '../../../snippets/clipboard/vue/root-provider.vue?raw';
import clipboardVueTimeout from '../../../snippets/clipboard/vue/timeout.vue?raw';
import clipboardVueValueText from '../../../snippets/clipboard/vue/value-text.vue?raw';

## Справочник API

<PrimitiveReference href="https://ark-ui.com/docs/components/clipboard" />

## Выбор подходящего компонента

Используйте `Clipboard`, когда пользователю нужно скопировать текст. В зависимости от выбранных
частей значение можно показать в поле только для чтения или вывести как обычный текст.

## Базовый пример

Используйте `Clipboard` как корневой компонент. Внутри `ClipboardControl` разместите одну
поверхность значения - `ClipboardInput` или `ClipboardValueText` - и `ClipboardTrigger`.
`ClipboardLabel` необязателен. Внутри триггера можно разместить `ClipboardIndicator`. В React и
Solid его `children` и `copied` принимают текст и иконки, а в Vue для этого используются обычный
слот и именованный слот `copied`.

React, Solid и Vue используют одинаковые плоские имена. Импортируйте `Clipboard` как корневой
компонент, а каждую дополнительную часть - напрямую, например `ClipboardInput` и
`ClipboardTrigger`. Эти имена одинаково работают в npm-пакетах и исходниках shadcn-реестра.

<ExampleFrame variant="component">
  <ClipboardBasic />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
      { label: 'Vue', value: 'vue' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/clipboard/basic.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={clipboardSolidBasic} />

    </Tab>
    <Tab value="vue">

<CodeBlockRuntime lang="vue" code={clipboardVueBasic} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/clipboard/component-basic.module.css"
```
</div>

</ExampleCode>

## Установка через shadcn

Полную настройку пакета, реестра и стилей см. в разделе [Быстрый старт](/docs/quick-start).

<Tabs groupId="framework">
  <Tab label="React" value="react">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-react/clipboard' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-react-tailwind/clipboard' },
  ]}
/>

  </Tab>
  <Tab label="Solid" value="solid">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-solid/clipboard' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-solid-tailwind/clipboard' },
  ]}
/>

  </Tab>
  <Tab label="Vue" value="vue">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-vue/clipboard' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-vue-tailwind/clipboard' },
  ]}
/>

  </Tab>
</Tabs>

## Анатомия

```text
Clipboard | ClipboardRootProvider
├─ ClipboardLabel (необязательно)
├─ ClipboardControl
│  ├─ ClipboardInput или ClipboardValueText
│  └─ ClipboardTrigger
│     └─ ClipboardIndicator (необязательно)
```

| Часть | Назначение |
| --- | --- |
| `Clipboard` | Хранит значение и управляет временным состоянием после копирования. |
| `ClipboardRootProvider` | Связывает части с состоянием, созданным через `useClipboard()`. |
| `ClipboardLabel` | Задаёт доступную подпись для значения. |
| `ClipboardControl` | Объединяет отображение значения и кнопку копирования. |
| `ClipboardInput` | Показывает значение в поле только для чтения. Для изменения используйте контролируемое состояние или `ClipboardContext.setValue()`. |
| `ClipboardTrigger` | Копирует текущее значение при активации. По умолчанию отображается как кнопка. |
| `ClipboardIndicator` | Показывает разное содержимое до и после копирования. По умолчанию это иконки копирования и подтверждения; в React и Solid передайте `copied="Copied"` и `children`, а в Vue используйте соответствующие слоты. |
| `ClipboardValueText` | Выводит значение как текст, когда поле ввода не подходит. |

## Композиция

`Clipboard` владеет значением и состоянием после копирования. Внутри `ClipboardControl` размещайте
одну поверхность значения (`ClipboardInput` или `ClipboardValueText`) и `ClipboardTrigger`;
`ClipboardIndicator` необязателен внутри триггера. Он следует состоянию копирования, а moduix
подставляет стандартные иконки копирования и подтверждения, если содержимое не задано. Для
текстовых подписей в React и Solid передайте `copied="Copied"` и `children`, а в Vue используйте
обычный и именованный слот `copied`.

Используйте `defaultValue` для неконтролируемого значения. Если значение принадлежит другой части
приложения или интерфейсу нужно реагировать на копирование, используйте `value` вместе с
`onValueChange(details)` и `onStatusChange(details)`; `timeout` задаёт длительность состояния после
копирования.

Используйте `useClipboard()` вместе с `ClipboardRootProvider`, только если состояние
нужно создать или разделить вне отрисовываемого дерева. Вложенные компоненты могут читать его через
`ClipboardContext` или `useClipboardContext()`. В Solid `useClipboard()` возвращает функцию доступа:
передайте её как `value={clipboard}` и читайте состояние как `clipboard().copied` или
`clipboard().value`. В Vue `useClipboard()` сразу возвращает реактивное состояние: передайте его
как `:value="clipboard"` и читайте `clipboard.copied` или `clipboard.value`. У корневого компонента нет состояния `disabled`: передайте нативное свойство
`disabled` и `ClipboardInput`, и `ClipboardTrigger`. Используйте `asChild` для этих частей только
если другой семантический элемент должен отрисовать узел DOM.

## Примеры

### Контекст

Читайте состояние буфера обмена из вложенного контента через `ClipboardContext` или используйте
`useClipboardContext()` во вложенном компоненте. Контекст предоставляет `copied`, `value`, `setValue`
и `copy`.

<ExampleFrame variant="component">
  <ContextClipboardDemo />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
      { label: 'Vue', value: 'vue' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/clipboard/context.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={clipboardSolidContext} />

    </Tab>
    <Tab value="vue">

<CodeBlockRuntime lang="vue" code={clipboardVueContext} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/clipboard/component-context.module.css"
```
</div>

</ExampleCode>

### Статус копирования

Используйте `onStatusChange(details)`, если другая часть интерфейса должна реагировать на успешное
копирование. В примере счётчик увеличивается на основе `details.copied`.

<ExampleFrame variant="component">
  <ClipboardStatusDemo />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
      { label: 'Vue', value: 'vue' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/clipboard/copy-status.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={clipboardSolidCopyStatus} />

    </Tab>
    <Tab value="vue">

<CodeBlockRuntime lang="vue" code={clipboardVueCopyStatus} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/clipboard/component-copy-status.module.css"
```
</div>

</ExampleCode>

### Свой текст копирования

Передайте собственное содержимое или значение `copied` в `ClipboardIndicator`, если кнопке нужны
подписи, соответствующие вашему продукту.

<ExampleFrame variant="component">
  <CustomCopyTextClipboardDemo />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
      { label: 'Vue', value: 'vue' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/clipboard/custom-copy-text.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={clipboardSolidCustomCopyText} />

    </Tab>
    <Tab value="vue">

<CodeBlockRuntime lang="vue" code={clipboardVueCustomCopyText} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/clipboard/component-custom-copy-text.module.css"
```
</div>

</ExampleCode>

### Отключённое состояние

Передайте `disabled` и полю, и кнопке, если пользователь может видеть значение, но не должен его
изменять или копировать.

<ExampleFrame variant="component">
  <DisabledClipboardDemo />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
      { label: 'Vue', value: 'vue' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/clipboard/disabled.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={clipboardSolidDisabled} />

    </Tab>
    <Tab value="vue">

<CodeBlockRuntime lang="vue" code={clipboardVueDisabled} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/clipboard/component-disabled.module.css"
```
</div>

</ExampleCode>

### Root Provider

Создайте состояние с помощью `useClipboard()` и передайте его в
`ClipboardRootProvider`, если состояние нужно разделить между компонентами или создать отдельно от
дерева компонентов.

<ExampleFrame variant="component">
  <RootProviderClipboardDemo />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
      { label: 'Vue', value: 'vue' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/clipboard/root-provider.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={clipboardSolidRootProvider} />

    </Tab>
    <Tab value="vue">

<CodeBlockRuntime lang="vue" code={clipboardVueRootProvider} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/clipboard/component-root-provider.module.css"
```
</div>

</ExampleCode>

### Задержка

Укажите `timeout`, если состояние после копирования должно отображаться дольше или меньше стандартных
трёх секунд.

<ExampleFrame variant="component">
  <TimeoutClipboardDemo />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
      { label: 'Vue', value: 'vue' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/clipboard/timeout.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={clipboardSolidTimeout} />

    </Tab>
    <Tab value="vue">

<CodeBlockRuntime lang="vue" code={clipboardVueTimeout} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/clipboard/component-timeout.module.css"
```
</div>

</ExampleCode>

### Текст значения

Используйте `ClipboardValueText`, если значение должно отображаться как текст, а не внутри поля
ввода.

<ExampleFrame variant="component">
  <ValueTextClipboardDemo />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
      { label: 'Vue', value: 'vue' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/clipboard/value-text.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={clipboardSolidValueText} />

    </Tab>
    <Tab value="vue">

<CodeBlockRuntime lang="vue" code={clipboardVueValueText} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/clipboard/component-value-text.module.css"
```
</div>

</ExampleCode>

### Расширенная настройка

Используйте `asChild`, чтобы передать свои элементы поля и кнопки для отрисовки частей Clipboard,
сохранив состояние и поведение компонента.

<ExampleFrame variant="component">
  <AsChildClipboardDemo />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
      { label: 'Vue', value: 'vue' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/clipboard/as-child.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={clipboardSolidAsChild} />

    </Tab>
    <Tab value="vue">

<CodeBlockRuntime lang="vue" code={clipboardVueAsChild} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/clipboard/component-advanced-customization.module.css"
```
</div>

</ExampleCode>

## Стилизация

Используйте `className` в React или `class` в Solid и Vue на экспортируемых частях для структурных стилей.
Варианты Tailwind используют стандартные утилиты и классы потребителя, а варианты CSS Modules -
приведённые ниже CSS-переменные. Для стилей состояния после копирования используйте атрибут Ark
`data-copied` вместе с хуками moduix `data-slot`.

### CSS-свойства

<CssPropertiesSection properties={ClipboardDocs.clipboardOverrideCssProperties} />

### Хуки стилизации

- `Clipboard` -> `data-slot="clipboard-root"`
- `ClipboardRootProvider` -> `data-slot="clipboard-root-provider"`
- `ClipboardLabel` -> `data-slot="clipboard-label"`
- `ClipboardControl` -> `data-slot="clipboard-control"`
- `ClipboardInput` -> `data-slot="clipboard-input"`
- `ClipboardTrigger` -> `data-slot="clipboard-trigger"`
- `ClipboardIndicator` -> `data-slot="clipboard-indicator"`
- `ClipboardValueText` -> `data-slot="clipboard-value-text"`
- Пока активно состояние копирования, Ark добавляет `data-copied` к `Clipboard`, `ClipboardLabel`,
  `ClipboardControl`, `ClipboardInput` и `ClipboardTrigger`.
- Ark добавляет `data-readonly` к `ClipboardInput`, когда поле доступно только для чтения.
- `ClipboardIndicator` добавляет `data-slot="clipboard-indicator-idle-icon"` и
  `data-slot="clipboard-indicator-copied-icon"` к стандартным узлам иконок.
