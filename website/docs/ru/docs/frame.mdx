---
title: Frame
description: Рендерите содержимое React или Solid в изолированном iframe с помощью Frame из Ark UI.
---

import FrameBasic from '@/components/examples/frame/basic.tsx';
import FrameSizing from '@/components/examples/frame/sizing.tsx';
import frameSolidBasic from '../../../snippets/frame/solid/basic.tsx?raw';
import frameSolidSizing from '../../../snippets/frame/solid/sizing.tsx?raw';
import frameSolidLifecycle from '../../../snippets/frame/solid/lifecycle.tsx?raw';
import frameSolidStaticDocument from '../../../snippets/frame/solid/static-document.tsx?raw';

`Frame` рендерит содержимое React или Solid в отдельном документе iframe.

## Справочник API

<PrimitiveReference href="https://ark-ui.com/docs/utilities/frame" />

## Базовый пример

`Frame` рендерит свои дочерние элементы внутри документа iframe. Всегда задавайте содержательный `title`: он становится
доступным именем iframe. В примере через `head` также передаётся элемент `<style>`, потому что стили родительского
документа не проникают внутрь iframe.

<ExampleFrame variant="component">
  <FrameBasic />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/frame/basic.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={frameSolidBasic} />

    </Tab>
  </Tabs>
</div>
</ExampleCode>

## Стили содержимого и размер

Проп `head` принимает JSX-содержимое и добавляет его в `<head>` iframe. Используйте его для стилей, шрифтов и
метаданных. Таблицы стилей, необходимые вложенному интерфейсу, также нужно передать через этот проп.

`Frame` отслеживает размер смонтированного содержимого и записывает его в пикселях в CSS-переменные `--width` и
`--height` на элементе iframe. Используйте `block-size: var(--height, 10rem)`, если iframe должен расти вместе с
содержимым. Переменную `--width` можно использовать так же для inline-размера.

<ExampleFrame variant="component">
  <FrameSizing />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/frame/sizing.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={frameSolidSizing} />

    </Tab>
  </Tabs>
</div>
</ExampleCode>

## Колбэки жизненного цикла

Используйте `onMount` и `onUnmount` для ресурсов, которыми управляет встроенный интерфейс. Оба колбэка вызываются без
аргументов. С помощью `ref` приложение может получить доступ к документу iframe, если это нужно для настройки.

Добавляйте только скрипты, которыми управляет ваше приложение. Для стилей и метаданных используйте `head`.

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/frame/lifecycle.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={frameSolidLifecycle} />

    </Tab>
  </Tabs>
</div>
</ExampleCode>

## Статические HTML-документы

Используйте `srcDoc` в React или `srcdoc` в Solid, когда iframe должен начинаться с готового HTML-документа, а не с
элементов, отрендеренных фреймворком. Это нативные варианты записи одного и того же атрибута iframe в каждом фреймворке.

Для недоверенного HTML задайте нативный атрибут `sandbox` и оставьте только необходимые содержимому разрешения. Не
добавляйте одновременно `allow-scripts` и `allow-same-origin` для недоверенного содержимого.

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/frame/static-document.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={frameSolidStaticDocument} />

    </Tab>
  </Tabs>
</div>
</ExampleCode>
