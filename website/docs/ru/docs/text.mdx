---
title: Text
component: text
description: Компонент для оформления обычного и встроенного текста, смысловых акцентов и усечённых превью.
---

import * as TextDocs from '@/components/examples/text/css-properties';
import TextAlignment from '@/components/examples/text/alignment.tsx';
import TextBasic from '@/components/examples/text/basic.tsx';
import TextCustomComposition from '@/components/examples/text/custom-composition.tsx';
import TextSemantics from '@/components/examples/text/semantics.tsx';
import TextSize from '@/components/examples/text/size.tsx';
import TextTone from '@/components/examples/text/tone.tsx';
import TextTruncation from '@/components/examples/text/truncation.tsx';
import TextWeight from '@/components/examples/text/weight.tsx';
import textSolidAlignment from '../../../snippets/text/solid/alignment.tsx?raw';
import textSolidBasic from '../../../snippets/text/solid/basic.tsx?raw';
import textSolidCustomComposition from '../../../snippets/text/solid/custom-composition.tsx?raw';
import textSolidSemantics from '../../../snippets/text/solid/semantics.tsx?raw';
import textSolidSize from '../../../snippets/text/solid/size.tsx?raw';
import textSolidTone from '../../../snippets/text/solid/tone.tsx?raw';
import textSolidTruncation from '../../../snippets/text/solid/truncation.tsx?raw';
import textSolidWeight from '../../../snippets/text/solid/weight.tsx?raw';

## Справочник API

В Ark UI нет отдельного компонента `Text`. В moduix это типографический компонент, построенный на официальной
[фабрике Ark](https://ark-ui.com/docs/guides/composition#the-ark-factory). Он следует рекомендациям Ark по
[композиции](https://ark-ui.com/docs/guides/composition), [стилизации](https://ark-ui.com/docs/guides/styling) и
[работе с `ref`](https://ark-ui.com/docs/guides/ref).

## Выбор подходящего компонента

Используйте `Text` для обычного и вспомогательного текста, встроенных акцентов и других текстовых элементов интерфейса,
которые должны соответствовать типографической шкале moduix.

- Выбирайте **`Heading`** для структуры документа и иерархии заголовков.
- Выбирайте **`Text`**, когда содержимое должно сохранять семантику абзаца или встроенного текста и использовать стили основного текста.
- Используйте локальный CSS, если нужно управлять разметкой форматированного текста, отступами или раскрытием усечённого текста.

## Базовый пример


Для абзаца используйте корневой элемент `p`, выбранный по умолчанию. Передавайте `as`, когда нужен другой
семантический HTML-элемент, но оформление `Text` должно сохраниться.

<ExampleFrame variant="component">
  <TextBasic />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/text/basic.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={textSolidBasic} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/text/text-basic.module.css"
```
</div>

</ExampleCode>

## Установка через shadcn

Полную настройку пакета, реестра и стилей см. в разделе [Быстрый старт](/docs/quick-start).

<Tabs groupId="framework">
  <Tab label="React" value="react">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-react/text' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-react-tailwind/text' },
  ]}
/>

  </Tab>
  <Tab label="Solid" value="solid">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-solid/text' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-solid-tailwind/text' },
  ]}
/>

  </Tab>
</Tabs>

## Анатомия

`Text` выводит один корневой элемент с помощью фабрики Ark через плоский корневой экспорт.

```text
Text
└─ текст или встроенное содержимое
```

| Часть                 | Назначение                                                       |
| --------------------- | ---------------------------------------------------------------- |
| `Text`              | Типографический корневой элемент с семантическим HTML и стабильными data-атрибутами. |

## Композиция

`Text` состоит только из корневого элемента и не владеет состоянием или дочерними частями. Используйте `<Text>`
для корня, `as` для поддерживаемого семантического элемента, а `asChild` только
когда один существующий элемент должен получить свойства и `ref` текста. Усечение ограничивает только отображение,
поэтому состояние раскрытия и управляющий элемент остаются в окружающем интерфейсе.

## Примеры

### Выравнивание

Используйте `align`, когда выравнивание относится к тексту, а не к окружающему контейнеру.

<ExampleFrame variant="component">
  <TextAlignment />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/text/alignment.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={textSolidAlignment} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/text/text-alignment.module.css"
```
</div>

</ExampleCode>

### Семантика

Используйте `as`, чтобы выбрать семантический HTML-элемент и сохранить типографические стили `Text`.

<ExampleFrame variant="component">
  <TextSemantics />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/text/semantics.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={textSolidSemantics} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/text/text-semantics.module.css"
```
</div>

</ExampleCode>

### Размер

Используйте `size`, чтобы изменить визуальную иерархию, не переходя на другой типографический компонент.

<ExampleFrame variant="component">
  <TextSize />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/text/size.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={textSolidSize} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/text/text-size.module.css"
```
</div>

</ExampleCode>

### Тон

Используйте `tone`, чтобы показать роль текста без локальных классов цвета.

<ExampleFrame variant="component">
  <TextTone />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/text/tone.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={textSolidTone} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/text/text-tone.module.css"
```
</div>

</ExampleCode>

### Усечение

Используйте `truncate` для однострочного усечения с многоточием, а `lineClamp` для компактных многострочных превью.
Если заданы оба свойства, действует `lineClamp` - оно переопределяет однострочное усечение. Логику раскрытия
полного текста добавляйте отдельно, в окружающем интерфейсе.

<ExampleFrame variant="component">
  <TextTruncation />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/text/truncation.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={textSolidTruncation} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/text/text-truncation.module.css"
```
</div>

</ExampleCode>

### Насыщенность

Используйте `weight`, когда акцент должен остаться в пределах того же семантического элемента.

<ExampleFrame variant="component">
  <TextWeight />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/text/weight.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={textSolidWeight} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/text/text-weight.module.css"
```
</div>

</ExampleCode>

### Расширенная настройка

Используйте `asChild`, когда корнем должен стать существующий пользовательский элемент, а не одно из поддерживаемых
встроенных значений `as`. Дочерний элемент должен быть единственным и принимать передаваемые свойства и `ref`.

<ExampleFrame variant="component">
  <TextCustomComposition />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/text/custom-composition.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={textSolidCustomComposition} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

## Стилизация

Оформляйте `Text` с помощью `className`, data-атрибутов корневого элемента и публичного CSS API. Варианты
с CSS Modules предоставляют перечисленные ниже переменные `--moduix-text-*`, а варианты Tailwind используют
классы-утилиты и переопределения классов на стороне приложения.

### CSS-свойства

<div className="not-prose docs-reference">
  <Tabs
    values={[{ label: 'CSS-переменные', value: 'CSS Variables' }]}
    className="docs-reference-tabs"
    contentItemClassName="docs-reference-panel"
  >
    <Tab>
      <TextDocs.TextCssPropertiesPanel />
    </Tab>
  </Tabs>
</div>

### Хуки стилизации

- `data-scope="text"` и `data-part="root"` идентифицируют корневой элемент Ark.
- `data-slot="text-root"` - стабильный селектор moduix на корневом элементе компонента.
- `data-size` отражает вычисленный визуальный размер: `xs`, `sm`, `md`, `lg` или `xl`.
- `data-weight` отражает вычисленную насыщенность: `regular`, `medium`, `semibold` или `bold`.
- `data-tone` отражает активный вариант тона: `default`, `muted`, `subtle`, `primary` или `destructive`.
- `data-align` появляется, только когда `align` явно задан как `start`, `center`, `end`, `left`, `right` или `justify`.
- `data-truncate` появляется при активном однострочном усечении с многоточием.
- `data-line-clamp` появляется, когда `lineClamp` - положительное целое число. В этом случае `lineClamp` задаёт
  резервное количество строк. Укажите `--moduix-text-line-clamp`, чтобы переопределить его через публичный CSS API.
