---
title: Typeset
component: typeset
description: Настраивайте ритм чтения для семантического HTML, отрендеренного Markdown и потокового контента.
---

import * as TypesetDocs from '@/components/examples/typeset/css-properties';
import TypesetBasic from '@/components/examples/typeset/basic.tsx';
import TypesetExcludedSubtree from '@/components/examples/typeset/excluded-subtree.tsx';
import TypesetMarkdownExtensions from '@/components/examples/typeset/markdown-extensions.tsx';
import TypesetReadingRhythm from '@/components/examples/typeset/reading-rhythm.tsx';
import TypesetScrollableTable from '@/components/examples/typeset/scrollable-table.tsx';
import TypesetStreamingContent from '@/components/examples/typeset/streaming-content.tsx';

## Справочник API

`Typeset` задаёт единый ритм чтения семантическому HTML внутри своего контейнера. Он не разбирает и
не рендерит Markdown, не фильтрует небезопасный контент и не задаёт ширину текста. Всё это остаётся
за приложением.

`Typeset` и `Typeset.Scroll` построены с помощью
[фабрики Ark](https://ark-ui.com/docs/guides/composition#the-ark-factory), поэтому поддерживают
нативные свойства `div`, ref и композицию через `asChild`.

## Базовый пример

Оберните в `<Typeset>` HTML, который уже подготовил ваш рендерер. Используйте `asChild`, только
когда внешний контейнер должен быть семантическим элементом, например `article` или `section`.
Оставьте списки обычными `<ul>` и `<ol>`: Typeset сам задаст их маркеры и интервалы без utility-классов.

<ExampleFrame variant="component">
  <TypesetBasic />
</ExampleFrame>

<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }]} className="docs-tabs">
    <Tab value="React">

```tsx file="../../../src/components/examples/typeset/basic.tsx"
```

    </Tab>
  </Tabs>
</div>

## Установка через shadcn

```bash
npx shadcn@latest add @moduix-react/typeset
```

## Анатомия

```text
Typeset
└─ Typeset.Scroll (необязательно)
```

| Часть | Назначение |
| --- | --- |
| `Typeset` / `Typeset.Root` | Корневой контейнер, который стилизует семантические дочерние элементы. |
| `Typeset.Scroll` | Фокусируемая область с горизонтальной прокруткой для широкого отрендеренного блока. |

## Композиция

### Рекомендуемая композиция

Разбирайте Markdown, фильтруйте небезопасный контент, рендерьте его и задавайте ширину текста в
приложении. `Typeset` только оформляет дочерние элементы внутри корня и намеренно не имеет свойства
для ширины. Оберните широкую таблицу или другой блок в `Typeset.Scroll`, если его лучше прокручивать
по горизонтали.

### Когда нужна собственная композиция

Если части содержимого нужен свой визуальный стиль, используйте семантическую разметку напрямую, а
затем добавьте `.not-typeset` или `data-not-typeset`, чтобы исключить этот элемент и всё его
содержимое. Для каждой пользовательской области `Typeset.Scroll` укажите `aria-label` или
`aria-labelledby`. Если у области есть доступное имя, moduix добавит `role="region"`, если только вы
не задали другую роль. При необходимости можно передать и свой `tabIndex`.

### Свойства по умолчанию

| Часть | По умолчанию | Примечания |
| --- | --- | --- |
| `Typeset` | `div` | Используйте `asChild` ровно с одним семантическим дочерним элементом, чтобы отрендерить другой контейнер. |
| `Typeset.Scroll` | `div`, `tabIndex={0}` | Фокус по умолчанию позволяет прокручивать с клавиатуры широкую статическую таблицу. Укажите доступное имя для пользовательской области прокрутки, чтобы добавить ей `role="region"`. |

### Особенности поведения

Корневой `Typeset` не управляет состоянием, взаимодействием с клавиатуры, элементами формы и не
создаёт скрытых полей. У `Typeset.Scroll` по умолчанию есть фокус, поэтому браузер может прокручивать
широкий статический блок. Стили добавляют интервал перед отрендеренными блоками и не используют
`:last-child`, `:has()` и `:empty`, поэтому новые элементы потока не меняют разметку уже показанного
содержимого.

## Примеры

### Исключение элементов интерфейса

Исключите вложенный фрагмент, если у него свой визуальный стиль, например встроенный элемент
управления или карточка. И `.not-typeset`, и `data-not-typeset` исключают отмеченный элемент со всем
содержимым, включая вложенный корень `Typeset`.

<ExampleFrame variant="component">
  <TypesetExcludedSubtree />
</ExampleFrame>

<div className="docs-reference">
  <Tabs
    values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]}
    className="docs-tabs"
  >
    <Tab value="React">

```tsx file="../../../src/components/examples/typeset/excluded-subtree.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/typeset/typeset-excluded-subtree.module.css"
```

    </Tab>
  </Tabs>
</div>

### Расширения Markdown

Списки задач, раскрывающиеся блоки `details`, списки определений и сноски GFM используют те же токены
и интервалы между блоками, что и основные HTML-элементы.

<ExampleFrame variant="component">
  <TypesetMarkdownExtensions />
</ExampleFrame>

<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }]} className="docs-tabs">
    <Tab value="React">

```tsx file="../../../src/components/examples/typeset/markdown-extensions.tsx"
```

    </Tab>
  </Tabs>
</div>

### Ритм чтения

Настройте CSS-переменные, когда для раздела нужен более свободный или плотный ритм чтения без новых
свойств компонента.

<ExampleFrame variant="component">
  <TypesetReadingRhythm />
</ExampleFrame>

<div className="docs-reference">
  <Tabs
    values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]}
    className="docs-tabs"
  >
    <Tab value="React">

```tsx file="../../../src/components/examples/typeset/reading-rhythm.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/typeset/typeset-reading-rhythm.module.css"
```

    </Tab>
  </Tabs>
</div>

### Прокручиваемая таблица

Оберните таблицу, созданную вашим рендерером, в `Typeset.Scroll`, если она должна сохранить ширину и
прокручиваться в узком контейнере. Дайте области прокрутки доступное имя, которое описывает таблицу
внутри неё.

<ExampleFrame variant="component">
  <TypesetScrollableTable />
</ExampleFrame>

<div className="docs-reference">
  <Tabs
    values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]}
    className="docs-tabs"
  >
    <Tab value="React">

```tsx file="../../../src/components/examples/typeset/scrollable-table.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/typeset/typeset-scrollable-table.module.css"
```

    </Tab>
  </Tabs>
</div>

### Потоковое содержимое

Для потока сообщений используйте более плотный ритм. Каждый новый блок добавляет только свой
интервал, поэтому уже показанное содержимое остаётся на месте по мере роста потока.

<ExampleFrame variant="component">
  <TypesetStreamingContent />
</ExampleFrame>

<div className="docs-reference">
  <Tabs
    values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]}
    className="docs-tabs"
  >
    <Tab value="React">

```tsx file="../../../src/components/examples/typeset/streaming-content.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/typeset/typeset-streaming-content.module.css"
```

    </Tab>
  </Tabs>
</div>

## Стилизация

### CSS-свойства

<div className="docs-reference">
  <Tabs
    values={[{ label: 'CSS-переменные', value: 'CSS Variables' }]}
    className="docs-reference-tabs"
    contentItemClassName="docs-reference-panel"
  >
    <Tab value="CSS Variables">
      <TypesetDocs.TypesetCssPropertiesPanel />
    </Tab>
  </Tabs>
</div>

Ритм чтения задают `--moduix-typeset-size`, `--moduix-typeset-leading` и
`--moduix-typeset-flow`. Переменные шрифта необязательны, а цвета, границы и радиус берутся из
активной темы moduix. Правила элементов находятся в слое `moduix.components` и используют
низкоспецифичные селекторы `:where()`, поэтому обычный CSS приложения может переопределять отдельные
элементы без `!important`.

### Хуки стилизации

- `data-slot="typeset"` выбирает корневой контейнер.
- `data-slot="typeset-scroll"` выбирает необязательную обёртку переполнения и её состояние при фокусе.
- `[data-scope="typeset"][data-part="root"]` и `[data-part="scroll"]` дают стабильные хуки для
  частей компонента.
