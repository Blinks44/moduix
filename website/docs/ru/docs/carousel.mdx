---
title: Carousel
component: carousel
description: Карусель с постраничной навигацией на базе Ark UI. Поддерживает автопрокрутку, внешнее управление состоянием и слайды разной ширины.
---

import * as CarouselDocs from '@/components/examples/carousel/css-properties';
import AdvancedCustomizationCarousel from '@/components/examples/carousel/advanced-customization.tsx';
import AutoplayCarousel from '@/components/examples/carousel/autoplay.tsx';
import TravelCarousel from '@/components/examples/carousel/basic.tsx';
import CardCarousel from '@/components/examples/carousel/cards.tsx';
import ControlledCarousel from '@/components/examples/carousel/controlled.tsx';
import DynamicSlidesCarousel from '@/components/examples/carousel/dynamic.tsx';
import LoopCarousel from '@/components/examples/carousel/loop.tsx';
import MouseDragCarousel from '@/components/examples/carousel/mouse-drag.tsx';
import PauseOnHoverCarousel from '@/components/examples/carousel/pause-on-hover.tsx';
import RootProviderCarousel from '@/components/examples/carousel/root-provider.tsx';
import ScrollToCarousel from '@/components/examples/carousel/scroll-to-slide.tsx';
import SlidesPerPageCarousel from '@/components/examples/carousel/slides-per-page.tsx';
import SpacingCarousel from '@/components/examples/carousel/spacing.tsx';
import ThumbnailIndicatorCarousel from '@/components/examples/carousel/thumbnail-indicators.tsx';
import VariableSizeCarousel from '@/components/examples/carousel/variable-sizes.tsx';
import VerticalCarousel from '@/components/examples/carousel/vertical.tsx';
import carouselSolidAdvancedCustomization from '../../../snippets/carousel/solid/advanced-customization.tsx?raw';
import carouselSolidAutoplay from '../../../snippets/carousel/solid/autoplay.tsx?raw';
import carouselSolidBasic from '../../../snippets/carousel/solid/basic.tsx?raw';
import carouselSolidCards from '../../../snippets/carousel/solid/cards.tsx?raw';
import carouselSolidControlled from '../../../snippets/carousel/solid/controlled.tsx?raw';
import carouselSolidDynamic from '../../../snippets/carousel/solid/dynamic.tsx?raw';
import carouselSolidLoop from '../../../snippets/carousel/solid/loop.tsx?raw';
import carouselSolidMouseDrag from '../../../snippets/carousel/solid/mouse-drag.tsx?raw';
import carouselSolidPauseOnHover from '../../../snippets/carousel/solid/pause-on-hover.tsx?raw';
import carouselSolidRootProvider from '../../../snippets/carousel/solid/root-provider.tsx?raw';
import carouselSolidScrollToSlide from '../../../snippets/carousel/solid/scroll-to-slide.tsx?raw';
import carouselSolidSlidesPerPage from '../../../snippets/carousel/solid/slides-per-page.tsx?raw';
import carouselSolidSpacing from '../../../snippets/carousel/solid/spacing.tsx?raw';
import carouselSolidThumbnailIndicators from '../../../snippets/carousel/solid/thumbnail-indicators.tsx?raw';
import carouselSolidVariableSizes from '../../../snippets/carousel/solid/variable-sizes.tsx?raw';
import carouselSolidVertical from '../../../snippets/carousel/solid/vertical.tsx?raw';

## Справочник API

<PrimitiveReference href="https://ark-ui.com/docs/components/carousel" />

## Базовый пример

Начните с `Carousel` в качестве корневого компонента. Добавьте `Carousel.ItemGroup` для ленты слайдов,
поместите кнопки перехода в `Carousel.Control`, а стандартный пейджер выведите с помощью
`Carousel.Indicators` и `Carousel.ProgressText`.

Carousel доступен для React и Solid в вариантах CSS Modules и Tailwind. Предпросмотр на этой странице
использует пакет React CSS Modules, а вкладки показывают нативный исходный код для того же публичного
контракта.

<ExampleFrame variant="component">
  <TravelCarousel />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/carousel/basic.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={carouselSolidBasic} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/carousel/carousel-basic.module.css"
```
</div>

</ExampleCode>

## Установка через shadcn

<Tabs groupId="framework">
  <Tab label="React" value="react">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-react/carousel' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-react-tailwind/carousel' },
  ]}
/>

  </Tab>
  <Tab label="Solid" value="solid">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-solid/carousel' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-solid-tailwind/carousel' },
  ]}
/>

  </Tab>
</Tabs>

Полная настройка пакета, реестра и таблиц стилей описана в разделе [Quick Start](/docs/quick-start).

## Анатомия

```text
Carousel / Carousel.Root
├─ Carousel.ItemGroup
│  └─ Carousel.Item[index]
├─ Carousel.Control
│  ├─ Carousel.PrevTrigger
│  └─ Carousel.NextTrigger
├─ Carousel.IndicatorGroup
│  └─ Carousel.Indicator[index]
└─ Carousel.ProgressText

Carousel.RootProvider
└─ та же структура частей, подключённая к хранилищу useCarousel()
```

| Элемент                      | Назначение                                                        |
| ---------------------------- | ----------------------------------------------------------------- |
| `Carousel` / `Carousel.Root` | Управляет состоянием, навигацией и коллекцией слайдов.            |
| `Carousel.RootProvider`      | Подключает части к состоянию, созданному через `useCarousel()`.   |
| `Carousel.Control`           | Контейнер для кнопок навигации и других элементов управления.    |
| `Carousel.ItemGroup`         | Лента слайдов, содержащая части `Carousel.Item`.                 |
| `Carousel.Item`              | Отдельный слайд со стабильным `index`.                            |
| `Carousel.PrevTrigger`       | Кнопка перехода на предыдущую страницу.                          |
| `Carousel.NextTrigger`       | Кнопка перехода на следующую страницу.                           |
| `Carousel.Indicators`        | Компонент moduix для стандартных точек страниц.                  |
| `Carousel.IndicatorGroup`    | Контейнер для пользовательских индикаторов страниц.              |
| `Carousel.Indicator`         | Кнопка перехода на страницу с указанным индексом.                |
| `Carousel.ProgressText`      | Текст с номером текущей страницы и общим числом страниц.          |

## Композиция

`Carousel` хранит состояние внутри корневого компонента. Используйте `defaultPage` для
неконтролируемой карусели, а `page` вместе с `onPageChange(details)`, если текущей страницей управляет
маршрутизация, фильтры или другая часть приложения. Свойство `slideCount` обязательно и должно
соответствовать отрисованным элементам при изменении коллекции.

Используйте `Carousel.RootProvider` вместе с `useCarousel()`, если хранилище нужно создать вне дерева
компонентов или разделить между границами композиции. Передайте это хранилище в `RootProvider` и не
отрисовывайте второй корневой `Carousel` для того же состояния.

`Carousel.Indicators` читает `pageSnapPoints` из контекста и создаёт стандартные индикаторы. Для
пользовательского содержимого индикаторов разместите `Carousel.IndicatorGroup` и `Carousel.Indicator`
внутри `Carousel.Context`; используйте `pageSnapPoints` из API контекста, если число индикаторов должно
соответствовать рассчитанным страницам.

## Примеры

### Автопрокрутка

Используйте автопрокрутку для галерей, в которых слайды переключаются сами, но оставляйте видимые
элементы управления. В этом примере автопрокрутка останавливается при наведении указателя на карусель
или при переходе фокуса внутрь неё. Пользователь может запустить её снова через
`Carousel.AutoplayTrigger`.

<ExampleFrame variant="component">
  <AutoplayCarousel />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs groupId="framework" values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]} className="docs-tabs">
    <Tab value="react">

```tsx pure file="../../../src/components/examples/carousel/autoplay.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={carouselSolidAutoplay} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/carousel/carousel-autoplay.module.css"
```
</div>
</ExampleCode>

Полный пример галереи с боковой навигацией, текстом на слайдах и индикаторами прогресса, которые
заполняются по мере переключения, смотрите в [рецепте галереи с автопрокруткой](/recipes/autoplay-gallery).

### Карточки

Внутри элементов карусели можно размещать любой контент. Если карточка должна быть уже области слайда,
задайте ей собственные стили и выровняйте её по центру.

<ExampleFrame variant="component">
  <CardCarousel />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs groupId="framework" values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]} className="docs-tabs">
    <Tab value="react">

```tsx pure file="../../../src/components/examples/carousel/cards.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={carouselSolidCards} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/carousel/carousel-cards.module.css"
```
</div>
</ExampleCode>

### Управляемое состояние

Используйте управляемое состояние, если текущая страница должна синхронизироваться с внешней
навигацией, фильтрами или аналитикой.

<ExampleFrame variant="component">
  <ControlledCarousel />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs groupId="framework" values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]} className="docs-tabs">
    <Tab value="react">

```tsx pure file="../../../src/components/examples/carousel/controlled.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={carouselSolidControlled} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/carousel/carousel-controlled.module.css"
```
</div>
</ExampleCode>

### Динамические слайды

Если число слайдов меняется после первого рендера, обновляйте `slideCount` одновременно с
отрисовываемыми элементами.

<ExampleFrame variant="component">
  <DynamicSlidesCarousel />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs groupId="framework" values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]} className="docs-tabs">
    <Tab value="react">

```tsx pure file="../../../src/components/examples/carousel/dynamic.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={carouselSolidDynamic} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/carousel/carousel-dynamic.module.css"
```
</div>
</ExampleCode>

### Зацикливание

Установите `loop`, чтобы после последнего слайда карусель возвращалась к первому.

<ExampleFrame variant="component">
  <LoopCarousel />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs groupId="framework" values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]} className="docs-tabs">
    <Tab value="react">

```tsx pure file="../../../src/components/examples/carousel/loop.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={carouselSolidLoop} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/carousel/carousel-loop.module.css"
```
</div>
</ExampleCode>

### Перетаскивание мышью

Включите перетаскивание мышью, чтобы на компьютере пользователи могли листать слайды тем же жестом,
что и на сенсорном экране.

<ExampleFrame variant="component">
  <MouseDragCarousel />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs groupId="framework" values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]} className="docs-tabs">
    <Tab value="react">

```tsx pure file="../../../src/components/examples/carousel/mouse-drag.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={carouselSolidMouseDrag} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/carousel/carousel-mouse-drag.module.css"
```
</div>
</ExampleCode>

### Пауза при наведении

Используйте этот вариант, если автопрокрутка должна останавливаться, пока указатель находится над
каруселью. В примере `Carousel.Context` вызывает `api.pause()` и `api.play()`, а фокус внутри карусели
также приостанавливает прокрутку.

<ExampleFrame variant="component">
  <PauseOnHoverCarousel />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs groupId="framework" values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]} className="docs-tabs">
    <Tab value="react">

```tsx pure file="../../../src/components/examples/carousel/pause-on-hover.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={carouselSolidPauseOnHover} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/carousel/carousel-pause-on-hover.module.css"
```
</div>
</ExampleCode>

### Провайдер корня

Используйте `Carousel.RootProvider`, когда состояние карусели нужно создать вне отрисовываемого дерева
и разделить между границами композиции.

<ExampleFrame variant="component">
  <RootProviderCarousel />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs groupId="framework" values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]} className="docs-tabs">
    <Tab value="react">

```tsx pure file="../../../src/components/examples/carousel/root-provider.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={carouselSolidRootProvider} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/carousel/carousel-root-provider.module.css"
```
</div>
</ExampleCode>

### Переход к слайду

Используйте API карусели из контекста, когда другой элемент управления должен сразу перейти к нужному
слайду.

<ExampleFrame variant="component">
  <ScrollToCarousel />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs groupId="framework" values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]} className="docs-tabs">
    <Tab value="react">

```tsx pure file="../../../src/components/examples/carousel/scroll-to-slide.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={carouselSolidScrollToSlide} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/carousel/carousel-scroll-to-slide.module.css"
```
</div>
</ExampleCode>

### Слайдов на странице

Увеличьте `slidesPerPage`, чтобы одновременно показывать несколько элементов вместо одной панели на
всю ширину.

<ExampleFrame variant="component">
  <SlidesPerPageCarousel />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs groupId="framework" values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]} className="docs-tabs">
    <Tab value="react">

```tsx pure file="../../../src/components/examples/carousel/slides-per-page.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={carouselSolidSlidesPerPage} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/carousel/carousel-slides-per-page.module.css"
```
</div>
</ExampleCode>

### Интервал

Увеличьте `spacing`, чтобы добавить расстояние между соседними слайдами в редакционных макетах,
товарных лентах или фирменных интерфейсах.

<ExampleFrame variant="component">
  <SpacingCarousel />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs groupId="framework" values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]} className="docs-tabs">
    <Tab value="react">

```tsx pure file="../../../src/components/examples/carousel/spacing.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={carouselSolidSpacing} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/carousel/carousel-spacing.module.css"
```
</div>
</ExampleCode>

### Индикаторы-миниатюры

Выведите собственное содержимое индикаторов, если пейджер должен показывать миниатюры слайдов вместо
обычных точек.

<ExampleFrame variant="component">
  <ThumbnailIndicatorCarousel />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs groupId="framework" values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]} className="docs-tabs">
    <Tab value="react">

```tsx pure file="../../../src/components/examples/carousel/thumbnail-indicators.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={carouselSolidThumbnailIndicators} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/carousel/carousel-thumbnail-indicators.module.css"
```
</div>
</ExampleCode>

### Разные размеры

Включите `autoSize`, чтобы размеры слайдов определялись их содержимым, а не фиксированной сеткой
страницы.

<ExampleFrame variant="component">
  <VariableSizeCarousel />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs groupId="framework" values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]} className="docs-tabs">
    <Tab value="react">

```tsx pure file="../../../src/components/examples/carousel/variable-sizes.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={carouselSolidVariableSizes} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/carousel/carousel-variable-sizes.module.css"
```
</div>
</ExampleCode>

### Вертикальная ориентация

Переключите карусель в вертикальную ориентацию, если сама карусель и её элементы управления должны
работать вдоль вертикальной оси.

<ExampleFrame variant="component">
  <VerticalCarousel />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs groupId="framework" values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]} className="docs-tabs">
    <Tab value="react">

```tsx pure file="../../../src/components/examples/carousel/vertical.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={carouselSolidVertical} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/carousel/carousel-vertical.module.css"
```
</div>
</ExampleCode>

### Расширенная настройка

Не используйте `Carousel.Indicators`, если нужно самостоятельно отрисовать `pageSnapPoints` или
разместить элементы управления пейджером рядом с другими элементами, работающими через контекст.

<ExampleFrame variant="component">
  <AdvancedCustomizationCarousel />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs groupId="framework" values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]} className="docs-tabs">
    <Tab value="react">

```tsx pure file="../../../src/components/examples/carousel/advanced-customization.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={carouselSolidAdvancedCustomization} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/carousel/carousel-advanced-customization.module.css"
```
</div>
</ExampleCode>

## Стилизация

Используйте `className` в React или `class` в Solid на экспортируемых частях для структурных стилей.
Для стилей состояний предпочитайте атрибуты Ark UI, например `data-orientation`, `data-current` и
`data-dragging`, вместе с селекторами moduix `data-slot`. Варианты Tailwind используют нативные
utility-классы и переопределения через классы потребителя; приведённый ниже справочник CSS-свойств
относится к пакетам CSS Modules.

### CSS-свойства

<CssPropertiesSection properties={CarouselDocs.carouselOverrideCssProperties} />

### Хуки стилизации

- `data-slot="carousel-root"`, `carousel-root-provider`, `carousel-control`, `carousel-item-group`,
  `carousel-item`, `carousel-prev-trigger`, `carousel-next-trigger`, `carousel-indicator-group`,
  `carousel-indicator`, `carousel-autoplay-trigger`, `carousel-autoplay-indicator` и
  `carousel-progress-text` — стабильные селекторы экспортируемых частей.
- Ark UI также добавляет к отрисованным узлам `data-scope="carousel"` и имена `data-part`.
- `data-orientation="horizontal" | "vertical"` доступен на корневом и структурных элементах.
- `Carousel.ItemGroup` получает `data-dragging` во время перетаскивания.
- `Carousel.Indicator` получает `data-current`, а `Carousel.Item` — `data-index` и `data-inview`.
- `Carousel.Indicator` получает `data-readonly`, а `Carousel.AutoplayTrigger` — `data-pressed`.
