---
title: Tabs
component: tabs
description: Переключение между связанными панелями с доступной клавиатурной навигацией, управляемым состоянием, ленивым монтированием, ссылками и вертикальной ориентацией.
---

import * as TabsDocs from '@/components/examples/tabs/css-properties';
import TabsBasic from '@/components/examples/tabs/basic.tsx';
import TabsControlledValue from '@/components/examples/tabs/controlled-value.tsx';
import TabsCustomIcons from '@/components/examples/tabs/custom-icons.tsx';
import TabsDisabledTab from '@/components/examples/tabs/disabled-tab.tsx';
import TabsLazyMount from '@/components/examples/tabs/lazy-mount.tsx';
import TabsLine from '@/components/examples/tabs/line.tsx';
import TabsLinks from '@/components/examples/tabs/links.tsx';
import TabsManualActivation from '@/components/examples/tabs/manual-activation.tsx';
import TabsRootProvider from '@/components/examples/tabs/root-provider.tsx';
import TabsVertical from '@/components/examples/tabs/vertical.tsx';
import tabsPreviewStyles from '@/components/examples/tabs/tabs-preview.module.css';
import tabsSolidBasic from '../../../snippets/tabs/solid/basic.tsx?raw';
import tabsSolidControlledValue from '../../../snippets/tabs/solid/controlled-value.tsx?raw';
import tabsSolidCustomIcons from '../../../snippets/tabs/solid/custom-icons.tsx?raw';
import tabsSolidDisabledTab from '../../../snippets/tabs/solid/disabled-tab.tsx?raw';
import tabsSolidLazyMount from '../../../snippets/tabs/solid/lazy-mount.tsx?raw';
import tabsSolidLine from '../../../snippets/tabs/solid/line.tsx?raw';
import tabsSolidLinks from '../../../snippets/tabs/solid/links.tsx?raw';
import tabsSolidManualActivation from '../../../snippets/tabs/solid/manual-activation.tsx?raw';
import tabsSolidRootProvider from '../../../snippets/tabs/solid/root-provider.tsx?raw';
import tabsSolidVertical from '../../../snippets/tabs/solid/vertical.tsx?raw';

## Справочник API

<PrimitiveReference href="https://ark-ui.com/docs/components/tabs" />

## Какой компонент выбрать

Используйте `Tabs`, чтобы переключаться между связанными представлениями в одной области страницы.
При выборе вкладки открывается соответствующая панель, а остальные панели скрываются.

- Выбирайте **`Tabs`**, когда каждая вкладка должна открывать одну связанную с ней панель.
- Выбирайте **`Accordion`**, когда разделы должны оставаться расположенными друг под другом и открываться независимо.
- Выбирайте **`ToggleGroup`**, когда элемент управления меняет состояние или режим, но не владеет содержимым панели.

## Базовый пример


Начните с `Tabs` и задайте одинаковое значение `value` для каждой пары `TabsTrigger` и
`TabsContent`. Добавьте `TabsIndicator`, если выбранную вкладку нужно отметить визуально.

<ExampleFrame variant="component">
  <div className={tabsPreviewStyles.root}>
    <TabsBasic />
  </div>
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/tabs/basic.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={tabsSolidBasic} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

## Установка через shadcn

Полную настройку пакета, реестра и стилей см. в разделе [Быстрый старт](/docs/quick-start).

<Tabs groupId="framework">
  <Tab label="React" value="react">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-react/tabs' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-react-tailwind/tabs' },
  ]}
/>

  </Tab>
  <Tab label="Solid" value="solid">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-solid/tabs' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-solid-tailwind/tabs' },
  ]}
/>

  </Tab>
</Tabs>

## Анатомия

```text
Tabs
├─ TabsList
│  ├─ TabsTrigger[value]
│  ├─ TabsTrigger[value]
│  └─ TabsIndicator
└─ TabsContent[value]

TabsRootProvider
└─ те же части, подключённые к состоянию из useTabs()

TabsContext
└─ render prop
```

| Часть                 | Назначение                                                         |
| --------------------- | ------------------------------------------------------------------ |
| `Tabs`                | Корневая часть, управляющая выбранным значением, фокусом, ориентацией и режимом. |
| `TabsRootProvider`   | Подключает части к состоянию, созданному с помощью `useTabs()` в moduix. |
| `TabsContext`        | Передаёт текущее состояние вкладок через render prop.             |
| `TabsList`           | Группирует триггеры и управляет перемещением фокуса между ними.   |
| `TabsTrigger`        | Интерактивная кнопка вкладки или хост `asChild` с обязательным `value`. |
| `TabsIndicator`      | Необязательный индикатор выбранной вкладки.                       |
| `TabsContent`        | Панель содержимого, связанная с триггером с таким же `value`.     |

## Композиция

Используйте `defaultValue` для неконтролируемого компонента. Если выбором управляет другая часть
приложения, передайте `value` и `onValueChange(details)`, а выбранное значение прочитайте из
`details.value`.

Используйте `TabsRootProvider` вместе с `useTabs()`, если состояние нужно создать вне дерева
компонента или разделить с соседним интерфейсом. Передайте хранилище в `TabsRootProvider` и не
создавайте для него ещё один корень `Tabs`. Для ссылки или компонента роутера используйте
`asChild` на `TabsTrigger`, а `TabsIndicator` добавляйте явно: `TabsList` не создаёт его
автоматически.

## Примеры

### Управляемое состояние

Управляйте выбранной вкладкой через состояние приложения, если активной панели нужно синхронизироваться
с другими элементами интерфейса.

<ExampleFrame variant="component">
  <div className={tabsPreviewStyles.root}>
    <TabsControlledValue />
  </div>
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/tabs/controlled-value.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={tabsSolidControlledValue} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Собственные значки

Размещайте значки и подписи непосредственно внутри `TabsTrigger`.

<ExampleFrame variant="component">
  <div className={tabsPreviewStyles.root}>
    <TabsCustomIcons />
  </div>
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/tabs/custom-icons.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={tabsSolidCustomIcons} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Отключённая вкладка

Отключите вкладку, если она должна оставаться видимой, но пока недоступна.

<ExampleFrame variant="component">
  <div className={tabsPreviewStyles.root}>
    <TabsDisabledTab />
  </div>
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/tabs/disabled-tab.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={tabsSolidDisabledTab} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Ленивое монтирование

Используйте `lazyMount`, если неактивные панели не должны монтироваться до выбора. Добавьте
`unmountOnExit`, чтобы после ухода с вкладки удалять их из DOM.

<ExampleFrame variant="component">
  <div className={tabsPreviewStyles.root}>
    <TabsLazyMount />
  </div>
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/tabs/lazy-mount.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={tabsSolidLazyMount} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Вариант с линией

Используйте `variant="line"`, если индикатор должен располагаться на активном краю, а не заполнять
выбранную вкладку. Этот вариант доступен для горизонтальных вкладок.

<ExampleFrame variant="component">
  <div className={tabsPreviewStyles.root}>
    <TabsLine />
  </div>
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/tabs/line.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={tabsSolidLine} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Ссылки

Используйте `asChild`, если триггеры вкладок должны отображаться как ссылки, которыми управляет
ваш роутер.

<ExampleFrame variant="component">
  <div className={tabsPreviewStyles.root}>
    <TabsLinks />
  </div>
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/tabs/links.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={tabsSolidLinks} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Ручная активация

Установите `activationMode="manual"`, если стрелки должны перемещать фокус, не меняя выбранную
вкладку. Активируйте её кликом, клавишей Enter или пробелом.

<ExampleFrame variant="component">
  <div className={tabsPreviewStyles.root}>
    <TabsManualActivation />
  </div>
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/tabs/manual-activation.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={tabsSolidManualActivation} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Вертикальная ориентация

Используйте `orientation="vertical"`, если список вкладок должен располагаться рядом с активной
панелью. В вертикальном варианте используется заполненный индикатор, поэтому выбранный элемент
легко заметить.

<ExampleFrame variant="component">
  <div className={tabsPreviewStyles.root}>
    <TabsVertical />
  </div>
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/tabs/vertical.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={tabsSolidVertical} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Расширенная настройка

Создайте состояние вкладок через `useTabs()`, если оно должно существовать до отображения дерева
частей или если соседнему интерфейсу нужно читать выбранное значение.

<ExampleFrame variant="component">
  <div className={tabsPreviewStyles.root}>
    <TabsRootProvider />
  </div>
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/tabs/root-provider.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={tabsSolidRootProvider} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/tabs/tabs-root-provider.module.css"
```
</div>

</ExampleCode>

## Стилизация

Используйте `className` в React или `class` в Solid на экспортируемых частях для структурных
стилей. Для стилей состояний предпочитайте атрибуты Ark, такие как `data-selected`,
`data-disabled` и `data-orientation`, вместе с селекторами moduix `data-slot`. Варианты на Tailwind
используют нативные утилиты и классы потребителя, а переменные ниже настраивают варианты с CSS Modules.

### CSS-свойства

<div className="docs-reference">
  <Tabs
    values={[{ label: 'CSS Variables', value: 'CSS Variables' }]}
    className="docs-reference-tabs"
    contentItemClassName="docs-reference-panel"
  >
    <Tab>
      <TabsDocs.TabsCssPropertiesPanel />
    </Tab>
  </Tabs>
</div>

### Хуки стилизации

Выбирайте часть, которая отвечает за нужный визуальный аспект:

- `Tabs` для ширины, общего расстояния, ориентации и переопределений на уровне варианта
- `TabsList` для фона, границ, оформления списка и расстояния вокруг индикатора
- `TabsTrigger` для раскладки подписи, расстояния до значка, выбранного и отключённого состояний, а также фокуса
- `TabsIndicator` для цвета, размера и переходов индикатора
- `TabsContent` для поверхности панели, внутренних отступов и стилей фокуса

Основные публичные хуки: `data-slot="tabs-root"`, `data-slot="tabs-root-provider"`,
`data-slot="tabs-list"`, `data-slot="tabs-trigger"`, `data-slot="tabs-indicator"` и
`data-slot="tabs-content"`.

Полезные хуки Ark: `data-scope="tabs"`, `data-part`, `data-orientation`, `data-selected`,
`data-disabled`, `data-focus`, а также переменные индикатора `--left`, `--top`, `--width`,
`--height` и `--transition-property`.
