---
title: Alert
component: alert
description: Встроенные сообщения о статусе, результатах проверки и системной обратной связи.
---

import * as AlertDocs from '@/components/examples/alert/css-properties';
import AlertBasic from '@/components/examples/alert/basic.tsx';
import AlertCustomComposition from '@/components/examples/alert/custom-composition.tsx';
import AlertCustomHeading from '@/components/examples/alert/custom-heading.tsx';
import AlertError from '@/components/examples/alert/error.tsx';
import AlertStatuses from '@/components/examples/alert/statuses.tsx';
import AlertWithIcon from '@/components/examples/alert/with-icon.tsx';
import alertSolidBasic from '../../../snippets/alert/solid/basic.tsx?raw';
import alertSolidCustomComposition from '../../../snippets/alert/solid/custom-composition.tsx?raw';
import alertSolidCustomHeading from '../../../snippets/alert/solid/custom-heading.tsx?raw';
import alertSolidError from '../../../snippets/alert/solid/error.tsx?raw';
import alertSolidStatuses from '../../../snippets/alert/solid/statuses.tsx?raw';
import alertSolidWithIcon from '../../../snippets/alert/solid/with-icon.tsx?raw';

## Справочник API

В Ark UI нет отдельного примитива `alert`. moduix собирает `Alert` с помощью официальной
[фабрики Ark](https://ark-ui.com/docs/guides/composition#the-ark-factory), следуя рекомендациям Ark по
[композиции](https://ark-ui.com/docs/guides/composition), [стилизации](https://ark-ui.com/docs/guides/styling) и
[работе с ref](https://ark-ui.com/docs/guides/ref).

Импортируйте `Alert` из `@moduix/react/alert` или `@moduix/solid/alert`. Вызов `Alert` и обращение к
`Alert.Root` дают одну и ту же корневую часть. Остальные публичные части: `Alert.Indicator`, `Alert.Content`,
`Alert.Title`, `Alert.Description` и `Alert.Actions`.

Каждая часть принимает стандартные свойства своего HTML-элемента, включая `className`/`class` и
`asChild`, а переданный ref указывает на отрисованный элемент. По умолчанию `status` равен `info`,
корневой элемент получает `role="status"`, а для `status="error"` — `role="alert"`. `Alert.Indicator`
скрыт от вспомогательных технологий, поэтому основной смысл сообщения должен быть в заголовке или описании.

## Выбор подходящего компонента

Используйте `Alert`, если сообщение должно входить в разметку страницы и занимать место в обычном
потоке документа.

- Используйте **`Alert`** для встроенных обновлений статуса, результатов проверки и системной обратной связи в формах, панелях или содержимом страницы.
- Используйте **`Toast`** для временных уведомлений, которые отображаются поверх страницы, не занимают место в потоке документа и закрываются автоматически.

## Базовый пример

Alert доступен для React и Solid как в пакетах с CSS Modules, так и в вариантах на Tailwind. Интерактивный
предварительный просмотр на этой странице использует React и CSS Modules, а вкладки с кодом показывают
нативную реализацию того же публичного контракта.

Для обычного встроенного сообщения используйте короткую запись `<Alert>` с `Alert.Title` и
`Alert.Description`. Добавьте `Alert.Indicator`, если значок помогает быстрее понять тип сообщения.

<ExampleFrame variant="component">
  <AlertBasic />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/alert/basic.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={alertSolidBasic} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

## Установка через shadcn

Выберите реестр, соответствующий фреймворку и способу стилизации. В [быстром старте](/docs/quick-start)
описаны настройка shadcn и установка общей основы.

<Tabs groupId="framework">
  <Tab label="React" value="react">

**CSS Modules**

<PackageManagerTabs command="shadcn@latest add @moduix-react/alert" dlx />

**Tailwind CSS**

<PackageManagerTabs command="shadcn@latest add @moduix-react-tailwind/alert" dlx />

  </Tab>
  <Tab label="Solid" value="solid">

**CSS Modules**

<PackageManagerTabs command="shadcn@latest add @moduix-solid/alert" dlx />

**Tailwind CSS**

<PackageManagerTabs command="shadcn@latest add @moduix-solid-tailwind/alert" dlx />

  </Tab>
</Tabs>

## Анатомия

Для компактного сообщения поместите `Alert.Title` и `Alert.Description` прямо в корневой элемент.
Если рядом с индикатором нужно сгруппировать текст и элементы управления, поместите их в `Alert.Content`.

```text
Alert / Alert.Root
├─ Alert.Indicator (optional)
├─ Alert.Title
├─ Alert.Description
└─ Alert.Content (optional)
   ├─ Alert.Title
   ├─ Alert.Description
   ├─ Alert.Actions (optional)
   └─ Extra custom blocks
```

| Часть                  | Назначение                                                               | `data-part`   |
| ---------------------- | ------------------------------------------------------------------------ | ------------- |
| `Alert` / `Alert.Root` | Корневой элемент. Применяет стили статуса и автоматически задаёт семантику области обновлений. | `root`        |
| `Alert.Indicator`      | Необязательный визуальный ориентир. Вложенные SVG-иконки получают единый размер. | `indicator`   |
| `Alert.Content`        | Необязательная колонка для группировки текста и действий.                | `content`     |
| `Alert.Title`          | Заголовок сообщения. По умолчанию рендерится как `p`.                     | `title`       |
| `Alert.Description`    | Дополнительный текст сообщения.                                           | `description` |
| `Alert.Actions`        | Необязательная строка действий с переносом внутри `Content`.              | `actions`     |

## Композиция

`Alert` отвечает только за отображение: компонент, который его рендерит, владеет состоянием видимости
и закрытия. `Alert` не управляет фокусом и поведением клавиатуры для кнопок и ссылок, которые вы в него
помещаете. Используйте `Alert.Content`, только если заголовок, описание и элементы управления должны
находиться в одной колонке рядом с `Alert.Indicator`; для стандартной строки действий с переносом
поместите их в `Alert.Actions`.

Если заголовку нужен другой семантический элемент, используйте `Alert.Title asChild` и передайте ровно
один семантически подходящий дочерний элемент. В Solid для `asChild` используется функция рендера — она
показана в примере «Другой уровень заголовка».

## Примеры

### Другой уровень заголовка

Используйте `asChild` в `Alert.Title`, если заголовок сообщения должен занять определённый уровень
в иерархии заголовков страницы.

<ExampleFrame variant="component">
  <AlertCustomHeading />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/alert/custom-heading.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={alertSolidCustomHeading} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Ошибка

Задайте `status="error"`, если сообщение нужно объявить сразу и визуально выделить сильнее.

<ExampleFrame variant="component">
  <AlertError />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/alert/error.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={alertSolidError} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Статусы

Используйте четыре статуса, чтобы различать информационные сообщения, сообщения об успехе,
предупреждения и ошибки. Добавьте `Alert.Indicator` с подходящей иконкой, если визуальный ориентир
помогает быстрее понять сообщение.

<ExampleFrame variant="component">
  <AlertStatuses />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/alert/statuses.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={alertSolidStatuses} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/alert/alert-statuses.module.css"
```
</div>

</ExampleCode>

### С иконкой

Поместите иконку moduix или собственную SVG-иконку внутрь `Alert.Indicator`.

<ExampleFrame variant="component">
  <AlertWithIcon />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/alert/with-icon.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={alertSolidWithIcon} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Расширенная настройка

Используйте `Alert.Content` и `Alert.Actions`, когда сообщению нужны кнопки или ссылки для исправления
проблемы и дальнейших действий. Если локальное сообщение нужно скрывать, храните состояние его видимости
в компоненте, который выводит `Alert`.

<ExampleFrame variant="component">
  <AlertCustomComposition />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/alert/custom-composition.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={alertSolidCustomComposition} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/alert/alert-custom-composition.module.css"
```
</div>

</ExampleCode>

## Стилизация

Используйте `className` или `class` на корневой части и её частях для локальных стилей. Задавайте
переменные `--moduix-alert-*` на корневом элементе, если уведомлению нужна локальная тема.

Общие переменные `--moduix-alert-bg`, `--moduix-alert-border-color`, `--moduix-alert-color` и
`--moduix-alert-indicator-color` действуют для всех статусов. Используйте соответствующие
переменные `--moduix-alert-<status>-*`, если нужно изменить только один статус.

Встроенные статусы также используют общие токены палитры:

| Статус    | Общий токен                  | По умолчанию                 |
| --------- | ---------------------------- | ---------------------------- |
| `info`    | `--moduix-color-card`        | карточная поверхность темы   |
| `success` | `--moduix-color-success`     | `oklch(0.627 0.194 149.214)` |
| `warning` | `--moduix-color-warning`     | `oklch(0.795 0.184 86.047)`  |
| `error`   | `--moduix-color-destructive` | деструктивный цвет темы      |

### Свойства CSS

<CssPropertiesSection properties={AlertDocs.alertCssProperties} />

### Селекторы для стилизации

- Стабильные селекторы `data-slot`: `alert-root`, `alert-indicator`, `alert-content`, `alert-title`, `alert-description` и `alert-actions`
- Селекторы в стиле Ark: `data-scope="alert"` с `data-part="root"`, `indicator`, `content`, `title`, `description` или `actions`
- `Alert.Root` записывает выбранный статус в `data-status`
