---
title: Toggle Group
component: toggle-group
description: Группа кнопок-переключателей с навигацией с клавиатуры для одиночного и множественного выбора в React и Solid.
---

import * as ToggleGroupDocs from '@/components/examples/toggle-group/css-properties';
import { CssPropertiesSection } from '@/components/mdx/Components';
import ToggleGroupAsChild from '@/components/examples/toggle-group/as-child.tsx';
import ToggleGroupBasic from '@/components/examples/toggle-group/basic.tsx';
import ToggleGroupContext from '@/components/examples/toggle-group/context.tsx';
import ToggleGroupControlled from '@/components/examples/toggle-group/controlled.tsx';
import ToggleGroupDisabled from '@/components/examples/toggle-group/disabled.tsx';
import ToggleGroupIcons from '@/components/examples/toggle-group/icons.tsx';
import ToggleGroupMultipleSelection from '@/components/examples/toggle-group/multiple-selection.tsx';
import ToggleGroupRootProvider from '@/components/examples/toggle-group/root-provider.tsx';
import ToggleGroupVariants from '@/components/examples/toggle-group/variants.tsx';
import ToggleGroupVertical from '@/components/examples/toggle-group/vertical.tsx';
import toggleGroupSolidAsChild from '../../../snippets/toggle-group/solid/as-child.tsx?raw';
import toggleGroupSolidBasic from '../../../snippets/toggle-group/solid/basic.tsx?raw';
import toggleGroupSolidContext from '../../../snippets/toggle-group/solid/context.tsx?raw';
import toggleGroupSolidControlled from '../../../snippets/toggle-group/solid/controlled.tsx?raw';
import toggleGroupSolidDisabled from '../../../snippets/toggle-group/solid/disabled.tsx?raw';
import toggleGroupSolidIcons from '../../../snippets/toggle-group/solid/icons.tsx?raw';
import toggleGroupSolidMultipleSelection from '../../../snippets/toggle-group/solid/multiple-selection.tsx?raw';
import toggleGroupSolidRootProvider from '../../../snippets/toggle-group/solid/root-provider.tsx?raw';
import toggleGroupSolidVariants from '../../../snippets/toggle-group/solid/variants.tsx?raw';
import toggleGroupSolidVertical from '../../../snippets/toggle-group/solid/vertical.tsx?raw';

## Справочник API

`ToggleGroup` оборачивает примитив Toggle Group из Ark UI и добавляет визуальные свойства moduix для
вариантов и размеров. Импортируйте его из пакета нужного фреймворка: `@moduix/react/toggle-group`,
`@moduix/solid/toggle-group`, `@moduix/react-tailwind/toggle-group` или
`@moduix/solid-tailwind/toggle-group`.

<PrimitiveReference href="https://ark-ui.com/docs/components/toggle-group" />

## Выбор подходящего компонента

Используйте `ToggleGroup`, когда связанные кнопки-переключатели должны работать как один элемент
управления с навигацией с клавиатуры. Группа может поддерживать один или несколько выбранных вариантов.

- Выберите **`ToggleGroup`** для сегментированных переключателей, компактных наборов фильтров и панелей форматирования.
- Выберите **`Toggle`** для одного отдельного действия с двумя состояниями.
- Выберите **`Tabs`**, если выбор должен также показывать связанную панель содержимого.
- Выберите **`RadioGroup`**, если варианты являются полями формы и должны участвовать в её нативной отправке.

## Базовый пример

`ToggleGroup` доступен для React и Solid в пакетах с CSS Modules и в вариантах на Tailwind.
Интерактивный предпросмотр ниже использует пакет React с CSS Modules, а вкладки с кодом показывают
нативный исходный код для того же публичного API.

Используйте `ToggleGroup` как корневой компонент и добавьте по одному `ToggleGroup.Item` на каждый
вариант. Задайте группе доступное имя через `aria-label` или `aria-labelledby`.

<ExampleFrame variant="component">
  <ToggleGroupBasic />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/toggle-group/basic.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={toggleGroupSolidBasic} />

    </Tab>
  </Tabs>
</div>
</ExampleCode>

## Установка через shadcn

Полную настройку пакета, реестра и стилей см. в разделе [Быстрый старт](/docs/quick-start).

<Tabs groupId="framework">
  <Tab label="React" value="react">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-react/toggle-group' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-react-tailwind/toggle-group' },
  ]}
/>

  </Tab>
  <Tab label="Solid" value="solid">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-solid/toggle-group' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-solid-tailwind/toggle-group' },
  ]}
/>

  </Tab>
</Tabs>

## Анатомия

```text
ToggleGroup / ToggleGroup.Root
├─ ToggleGroup.Context (необязательно)
└─ ToggleGroup.Item[value]

ToggleGroup.RootProvider
└─ ToggleGroup.Item[value]
```

| Часть                               | Назначение                                                                            |
| ----------------------------------- | ------------------------------------------------------------------------------------- |
| `ToggleGroup` / `ToggleGroup.Root`  | Корневая часть, управляющая выбранными значениями, фокусом, ориентацией и отключённым состоянием. |
| `ToggleGroup.Item`                  | Интерактивный элемент с обязательным `value` и состоянием выбора, которым управляет Ark. |
| `ToggleGroup.RootProvider`          | Подключает дерево элементов к состоянию, созданному с помощью `useToggleGroup()`.     |
| `ToggleGroup.Context`               | Передаёт текущее состояние группы через render prop.                                  |

## Композиция

Используйте `defaultValue` для неконтролируемой группы, а `value` вместе с `onValueChange(details)`,
если состояние выбора хранится в другой части приложения. Оба свойства имеют тип `string[]` в режимах
одиночного и множественного выбора. Используйте `ToggleGroup.RootProvider` с `useToggleGroup()` из
пакета нужного фреймворка, если состояние нужно создать вне дерева. В React хранилище читается как
объект, а в Solid его реактивное состояние доступно через возвращаемую функцию.

Используйте `ToggleGroup.Context` или `useToggleGroupContext()`, если пользовательскому элементу нужно
текущее состояние группы. При использовании `asChild` React получает одного потомка, который ведёт себя
как кнопка, а Solid получает render function, возвращающую элемент с поведением Ark. Не используйте
`ToggleGroup.Root` и `ToggleGroup.RootProvider` одновременно для одного экземпляра состояния.

## Примеры

### asChild

Используйте `asChild` на элементе, если отрисовываемым элементом должна управлять пользовательская
кнопка. Передавайте ровно одного потомка, который ведёт себя как кнопка, чтобы Ark мог объединить с ним
семантику группы и клавиатурное поведение.

<ExampleFrame variant="component">
  <ToggleGroupAsChild />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/toggle-group/as-child.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={toggleGroupSolidAsChild} />

    </Tab>
  </Tabs>
</div>
</ExampleCode>

### Контекст

Используйте `useToggleGroupContext()` в пользовательском элементе, если его содержимому нужно текущее
состояние группы. `ToggleGroup.Context` предоставляет то же состояние через render prop.

<ExampleFrame variant="component">
  <ToggleGroupContext />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/toggle-group/context.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={toggleGroupSolidContext} />

    </Tab>
  </Tabs>
</div>
</ExampleCode>

### Управляемое состояние

Управляйте `value`, если выбранные элементы должны оставаться синхронизированными с состоянием
остального приложения.

<ExampleFrame variant="component">
  <ToggleGroupControlled />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/toggle-group/controlled.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={toggleGroupSolidControlled} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/toggle-group/toggle-group-controlled.module.css"
```
</div>
</ExampleCode>

### Отключённая группа и элемент

Используйте `disabled` на группе или отдельном элементе, чтобы запретить взаимодействие.

<ExampleFrame variant="component">
  <ToggleGroupDisabled />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/toggle-group/disabled.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={toggleGroupSolidDisabled} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/toggle-group/toggle-group-disabled.module.css"
```
</div>
</ExampleCode>

### Иконки

Передавайте иконки как обычные дочерние элементы. Элементам только с иконками всегда нужно доступное имя.

<ExampleFrame variant="component">
  <ToggleGroupIcons />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/toggle-group/icons.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={toggleGroupSolidIcons} />

    </Tab>
  </Tabs>
</div>
</ExampleCode>

### Множественный выбор

Используйте `multiple`, если несколько вариантов могут оставаться выбранными одновременно.

<ExampleFrame variant="component">
  <ToggleGroupMultipleSelection />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/toggle-group/multiple-selection.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={toggleGroupSolidMultipleSelection} />

    </Tab>
  </Tabs>
</div>
</ExampleCode>

### Варианты

Установите `variant` на группе, если все элементы должны использовать одинаковое оформление.

<ExampleFrame variant="component">
  <ToggleGroupVariants />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/toggle-group/variants.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={toggleGroupSolidVariants} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/toggle-group/toggle-group-variants.module.css"
```
</div>
</ExampleCode>

### Вертикальная ориентация

Установите `orientation="vertical"`, если варианты должны располагаться столбцом.

<ExampleFrame variant="component">
  <ToggleGroupVertical />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/toggle-group/vertical.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={toggleGroupSolidVertical} />

    </Tab>
  </Tabs>
</div>
</ExampleCode>

### Расширенная настройка

Используйте `ToggleGroup.RootProvider` с `useToggleGroup()`, если экземпляр состояния нужно создать
вне дерева отрисованной группы.

<ExampleFrame variant="component">
  <ToggleGroupRootProvider />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/toggle-group/root-provider.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={toggleGroupSolidRootProvider} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/toggle-group/toggle-group-advanced-customization.module.css"
```
</div>
</ExampleCode>

## Стилизация

Используйте `className` в React или `class` в Solid на экспортируемых частях для структурных стилей.
Варианты Tailwind используют нативные утилиты и классы потребителя, а варианты CSS Modules
предоставляют токены `--moduix-toggle-group-*`. Элементы наследуют `variant` и `size` от корневой
части и могут переопределить любое из этих свойств для отдельного оформления.

### Свойства CSS

<CssPropertiesSection properties={ToggleGroupDocs.toggleGroupOverrideCssProperties} />

В таблице перечислены свойства уровня группы. Каждый `ToggleGroup.Item` также поддерживает [CSS-свойства
Toggle](/docs/toggle#css-properties) для цветов и состояний взаимодействия. Задайте свойство
`--moduix-toggle-*` на корневом компоненте, чтобы оформить все элементы, или на элементе, чтобы изменить
оформление только одного элемента.

### Хуки стилизации

- Стабильные селекторы `data-slot`: `toggle-group-root`, `toggle-group-root-provider` и `toggle-group-item`.
- Селекторы частей Ark: `data-scope="toggle-group"` и `data-part="root" | "item"`.
- Селекторы состояния Ark: `data-state="on" | "off"`, `data-disabled`, `data-focus` и
  `data-orientation="horizontal" | "vertical"`.
- Селекторы moduix: `data-variant` и `data-size`.
