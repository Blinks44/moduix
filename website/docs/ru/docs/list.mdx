---
title: List
component: list
description: Семантический список с настраиваемыми отступами, типографикой, цветом и нативными маркерами.
---

import * as ListDocs from '@/components/examples/list/css-properties';
import ListBasic from '@/components/examples/list/basic.tsx';
import ListCustomComposition from '@/components/examples/list/custom-composition.tsx';
import ListMarkerless from '@/components/examples/list/markerless.tsx';
import ListNativeItems from '@/components/examples/list/native-items.tsx';
import ListOrdered from '@/components/examples/list/ordered.tsx';
import ListRootAsChild from '@/components/examples/list/root-as-child.tsx';
import ListTone from '@/components/examples/list/tone.tsx';
import listSolidBasic from '../../../snippets/list/solid/basic.tsx?raw';
import listSolidCustomComposition from '../../../snippets/list/solid/custom-composition.tsx?raw';
import listSolidMarkerless from '../../../snippets/list/solid/markerless.tsx?raw';
import listSolidNativeItems from '../../../snippets/list/solid/native-items.tsx?raw';
import listSolidOrdered from '../../../snippets/list/solid/ordered.tsx?raw';
import listSolidRootAsChild from '../../../snippets/list/solid/root-as-child.tsx?raw';
import listSolidTone from '../../../snippets/list/solid/tone.tsx?raw';

## Справочник API

В Ark UI нет отдельного примитива для списка. moduix использует официальный
[Ark factory](https://ark-ui.com/docs/guides/composition#the-ark-factory), чтобы собирать нативные элементы `ul`,
`ol` и `li`. Подробнее о базовой модели можно узнать в руководствах Ark по
[композиции](https://ark-ui.com/docs/guides/composition), [стилизации](https://ark-ui.com/docs/guides/styling) и
[передаче ref](https://ark-ui.com/docs/guides/ref).

## Какой компонент выбрать

Используйте `List` для статического сгруппированного контента: например, шагов, характеристик или метаданных.
Компонент сохраняет нативную семантику `ul` и `ol` и добавляет управление отступами, типографикой, цветом и
маркерами.

Если пользователю нужно перемещаться по списку с клавиатуры и выбирать один или несколько вариантов, используйте
[Listbox](/docs/listbox). `Listbox` управляет фокусом, коллекцией и выбором элементов.

## Базовый пример

`List` по умолчанию отрисовывает семантический `<ul>`. Используйте `ListItem`, если нужны стабильная часть
элемента и её хуки стилизации, или передавайте обычные `<li>`, когда дополнительная обёртка не нужна. Корневой
`List` управляет отступами, типографикой, цветом и маркерами. Если `marker` не задан, используются семантические
значения по умолчанию; при необходимости передайте `disc`, `decimal` или `none` явно.

<ExampleFrame variant="component">
  <ListBasic />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/list/basic.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={listSolidBasic} />

    </Tab>
  </Tabs>
</div>
</ExampleCode>

## Установка через shadcn

Полную настройку пакета, реестра и стилей см. в разделе [Быстрый старт](/docs/quick-start).

<Tabs groupId="framework">
  <Tab label="React" value="react">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-react/list' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-react-tailwind/list' },
  ]}
/>

  </Tab>
  <Tab label="Solid" value="solid">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-solid/list' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-solid-tailwind/list' },
  ]}
/>

  </Tab>
</Tabs>

## Анатомия

```text
List
└─ ListItem | li
```

| Часть                | Назначение                                                                                     |
| -------------------- | ----------------------------------------------------------------------------------------------- |
| `List`     | Семантический корень списка, который отрисовывает `ul` или `ol` и управляет маркерами, отступами, размером и цветом. |
| `ListItem` | Необязательная часть, которая отрисовывает `li`, передаёт нативные атрибуты и ref и поддерживает `asChild`.          |

## Композиция

Используйте `List` как корень. Его непосредственными дочерними элементами должны быть `ListItem`
или нативные `li`, поскольку отступы корня и стилизация маркеров рассчитаны на непосредственные элементы списка.
Используйте `ListItem`, если нужны его стабильный слот, `className`/`class` или `asChild`; в остальных случаях
достаточно обычного `li`. Используйте `asChild`, только когда компонент приложения отрисовывает один семантический
`ul`, `ol` или `li` и передаёт полученные props и ref. В Solid для этого используется вариант `asChild` с
render-функцией. У `List` нет машины состояний и callback-обработчиков, поэтому выбор с клавиатуры относится к
`Listbox`.

## Примеры

### Пользовательский корень

Используйте `List asChild`, когда компонент приложения должен сам отрисовать семантический элемент списка. В React
он должен передать полученные props и ref одному `<ul>` или `<ol>`, а в Solid нужно использовать вариант с
render-функцией.

<ExampleFrame variant="component">
  <ListRootAsChild />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/list/root-as-child.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={listSolidRootAsChild} />

    </Tab>
  </Tabs>
</div>
</ExampleCode>

### Без маркеров

Используйте `marker="none"`, если нужна семантика списка без видимых маркеров. В этом режиме `List` по умолчанию
добавляет `role="list"`, если вы не передали другое значение `role`.

<ExampleFrame variant="component">
  <ListMarkerless />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/list/markerless.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={listSolidMarkerless} />

    </Tab>
  </Tabs>
</div>
</ExampleCode>

### Нативные элементы

Используйте нативные `li`, если вам не нужна экспортируемая часть `ListItem`.

<ExampleFrame variant="component">
  <ListNativeItems />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/list/native-items.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={listSolidNativeItems} />

    </Tab>
  </Tabs>
</div>
</ExampleCode>

### Упорядоченный список

Используйте `as="ol"`, чтобы отрисовать упорядоченный список и сохранить стилизацию корневого элемента moduix.
Нативные свойства `start`, `reversed` и `type` передаются корневому элементу упорядоченного списка.

<ExampleFrame variant="component">
  <ListOrdered />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/list/ordered.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={listSolidOrdered} />

    </Tab>
  </Tabs>
</div>
</ExampleCode>

### Тон

Используйте `tone`, чтобы задать семантический цвет текста и маркеров списка.

<ExampleFrame variant="component">
  <ListTone />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/list/tone.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={listSolidTone} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/list/list-tone.module.css"
```
</div>
</ExampleCode>

### Расширенная настройка

Используйте `ListItem asChild`, когда пользовательский компонент должен сам отрисовать `<li>`. Он обязан передать
полученные props и ref и сохранить семантический элемент `<li>`. Во всех остальных случаях достаточно использовать
`ListItem className` или `class` напрямую.

<ExampleFrame variant="component">
  <ListCustomComposition />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/list/custom-composition.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={listSolidCustomComposition} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/list/list-custom-composition.module.css"
```
</div>
</ExampleCode>

## Стилизация

Стилизуйте `List` с помощью свойства класса (`className` в React и `class` в Solid), экспортируемых атрибутов
`data-*` в стиле Ark, нативного `li::marker` и публичных CSS-переменных `--moduix-list-*`, перечисленных ниже.
Ниже приведён полный список переменных публичного CSS API.

### CSS-свойства

<div className="docs-reference">
  <Tabs
    values={[{ label: 'CSS-переменные', value: 'CSS Variables' }]}
    className="docs-reference-tabs"
    contentItemClassName="docs-reference-panel"
  >
    <Tab>
      <ListDocs.ListCssPropertiesPanel />
    </Tab>
  </Tabs>
</div>

### Хуки стилизации

`List` предоставляет следующие публичные хуки стилизации:

- корневой элемент: `data-scope="list"`, `data-part="root"`, `data-slot="list-root"`
- элемент: `data-scope="list"`, `data-part="item"`, `data-slot="list-item"`
- `data-gap="xs" | "sm" | "md" | "lg" | "xl" | "2xl"` для стилей, связанных с отступами
- `data-marker="auto" | "disc" | "decimal" | "none"` для стилей, связанных с маркерами
- `data-size="xs" | "sm" | "md" | "lg" | "xl"` для стилей размера типографики
- `data-tone="default" | "muted" | "subtle" | "primary" | "destructive"` для семантических цветов
- нативный селектор `li::marker` для переопределения цвета или толщины маркера
