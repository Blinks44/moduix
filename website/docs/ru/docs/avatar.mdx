---
title: Avatar
component: avatar
description: 'Компактное представление пользователя или объекта: Ark UI управляет загрузкой изображения и показом резервного содержимого.'
---

import * as AvatarDocs from '@/components/examples/avatar/css-properties';
import AvatarBasic from '@/components/examples/avatar/basic.tsx';
import AvatarCustomComposition from '@/components/examples/avatar/custom-composition.tsx';
import AvatarCustomFallback from '@/components/examples/avatar/custom-fallback.tsx';
import AvatarCustomImage from '@/components/examples/avatar/custom-image.tsx';
import AvatarEvents from '@/components/examples/avatar/events.tsx';
import AvatarRootProvider from '@/components/examples/avatar/root-provider.tsx';
import AvatarSizesAndFallbackOnly from '@/components/examples/avatar/sizes-and-fallback-only.tsx';
import avatarSolidBasic from '../../../snippets/avatar/solid/basic.tsx?raw';
import avatarSolidCustomComposition from '../../../snippets/avatar/solid/custom-composition.tsx?raw';
import avatarSolidCustomFallback from '../../../snippets/avatar/solid/custom-fallback.tsx?raw';
import avatarSolidCustomImage from '../../../snippets/avatar/solid/custom-image.tsx?raw';
import avatarSolidEvents from '../../../snippets/avatar/solid/events.tsx?raw';
import avatarSolidRootProvider from '../../../snippets/avatar/solid/root-provider.tsx?raw';
import avatarSolidSizesAndFallbackOnly from '../../../snippets/avatar/solid/sizes-and-fallback-only.tsx?raw';
import avatarVueBasic from '../../../snippets/avatar/vue/basic.vue?raw';
import avatarVueCustomComposition from '../../../snippets/avatar/vue/custom-composition.vue?raw';
import avatarVueCustomFallback from '../../../snippets/avatar/vue/custom-fallback.vue?raw';
import avatarVueCustomImage from '../../../snippets/avatar/vue/custom-image.vue?raw';
import avatarVueEvents from '../../../snippets/avatar/vue/events.vue?raw';
import avatarVueRootProvider from '../../../snippets/avatar/vue/root-provider.vue?raw';
import avatarVueSizesAndFallbackOnly from '../../../snippets/avatar/vue/sizes-and-fallback-only.vue?raw';

## Справочник API

<PrimitiveReference href="https://ark-ui.com/docs/components/avatar" />

## Базовый пример


Используйте вместе `Avatar`, `AvatarFallback` и `AvatarImage`. Не убирайте `AvatarFallback` из
дерева, чтобы Ark UI мог переключаться между ним и изображением при изменении состояния загрузки.

В React, Solid и Vue действуют одинаковые плоские имена значений. Импортируйте `Avatar` как корень и
каждую дополнительную часть отдельно, например `AvatarImage` и `AvatarFallback`. Эти же имена
работают и с npm-пакетами, и с исходниками из shadcn-реестра.

<ExampleFrame variant="component">
  <AvatarBasic />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
      { label: 'Vue', value: 'vue' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/avatar/basic.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={avatarSolidBasic} />

    </Tab>
    <Tab value="vue">

<CodeBlockRuntime lang="vue" code={avatarVueBasic} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

## Установка через shadcn

Полную настройку пакета, реестра и стилей см. в разделе [Быстрый старт](/docs/quick-start).

<Tabs groupId="framework">
  <Tab label="React" value="react">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-react/avatar' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-react-tailwind/avatar' },
  ]}
/>

  </Tab>
  <Tab label="Solid" value="solid">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-solid/avatar' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-solid-tailwind/avatar' },
  ]}
/>

  </Tab>
  <Tab label="Vue" value="vue">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-vue/avatar' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-vue-tailwind/avatar' },
  ]}
/>

  </Tab>
</Tabs>

## Анатомия

```text
Avatar
├─ AvatarFallback
└─ AvatarImage

AvatarRootProvider
└─ те же части, подключённые к хранилищу useAvatar()
```

| Часть                 | Назначение                                                             |
| --------------------- | ---------------------------------------------------------------------- |
| `Avatar`              | Создаёт корневой элемент и хранит состояние аватара Ark UI.            |
| `AvatarRootProvider`  | Подключает состояние Ark UI, созданное за пределами компонента.        |
| `AvatarFallback`      | Показывает переданное потребителем содержимое до загрузки изображения.  |
| `AvatarImage`         | Показывает изображение после успешной загрузки.                        |

## Композиция

Размещайте `AvatarFallback` и `AvatarImage` внутри одного корневого компонента, чтобы Ark мог
переключать их видимость. Если состояние должно принадлежать другой части приложения, создайте его
с помощью `useAvatar()` из пакета нужного фреймворка и напрямую передайте в `AvatarRootProvider`;
не добавляйте для того же хранилища ещё один корневой компонент. В React хранилище передаётся как
объект, а Solid предоставляет реактивную функцию доступа. В Vue напрямую передавайте результат
`useAvatar()` в `AvatarRootProvider`. В React и Solid для пользовательского изображения используйте
`useAvatarContext()` и сохраняйте состояния `hidden` и `visible`, возвращаемые `getImageProps()`.
В Vue `AvatarContext` предоставляет эти данные через слот. Применяйте `asChild` только с одним
семантически подходящим дочерним элементом, если другой элемент должен владеть корневым DOM-узлом.

## Примеры

### Пользовательское резервное содержимое

Резервным содержимым может быть иконка или другое компактное обозначение пользователя. Задайте
доступное имя, если по этому содержимому нужно понять что-то, чего нет в окружающем тексте.

<ExampleFrame variant="component">
  <AvatarCustomFallback />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
      { label: 'Vue', value: 'vue' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/avatar/custom-fallback.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={avatarSolidCustomFallback} />

    </Tab>
    <Tab value="vue">

<CodeBlockRuntime lang="vue" code={avatarVueCustomFallback} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/avatar/avatar-custom-fallback.module.css"
```
</div>

</ExampleCode>

### Пользовательское изображение

Используйте `useAvatarContext()` из пакета moduix для React или Solid, если компонент изображения
или пользовательский рендерер должен подключиться к жизненному циклу загрузки Ark. В Vue состояние
изображения доступно через слот `AvatarContext`.

<ExampleFrame variant="component">
  <AvatarCustomImage />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
      { label: 'Vue', value: 'vue' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/avatar/custom-image.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={avatarSolidCustomImage} />

    </Tab>
    <Tab value="vue">

<CodeBlockRuntime lang="vue" code={avatarVueCustomImage} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/avatar/avatar-custom-image.module.css"
```
</div>

</ExampleCode>

### События

Используйте `onStatusChange(details)`, если интерфейсу вокруг аватара нужно реагировать на жизненный
цикл изображения.

<ExampleFrame variant="component">
  <AvatarEvents />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
      { label: 'Vue', value: 'vue' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/avatar/events.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={avatarSolidEvents} />

    </Tab>
    <Tab value="vue">

<CodeBlockRuntime lang="vue" code={avatarVueEvents} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/avatar/avatar-events.module.css"
```
</div>

</ExampleCode>

### Root Provider

Создайте состояние с помощью `useAvatar` из соответствующего пакета, если внешним элементам управления
нужно работать с аватаром. Передайте его в `AvatarRootProvider` вместо `Avatar`.

<ExampleFrame variant="component">
  <AvatarRootProvider />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
      { label: 'Vue', value: 'vue' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/avatar/root-provider.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={avatarSolidRootProvider} />

    </Tab>
    <Tab value="vue">

<CodeBlockRuntime lang="vue" code={avatarVueRootProvider} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/avatar/avatar-root-provider.module.css"
```
</div>

</ExampleCode>

### Размеры и аватар без изображения

Свойство `size` в moduix задаёт один из распространённых размеров дизайн-токенов для любого корневого
компонента. Аватар только с резервным содержимым подходит, если источника изображения нет.

<ExampleFrame variant="component">
  <AvatarSizesAndFallbackOnly />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
      { label: 'Vue', value: 'vue' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/avatar/sizes-and-fallback-only.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={avatarSolidSizesAndFallbackOnly} />

    </Tab>
    <Tab value="vue">

<CodeBlockRuntime lang="vue" code={avatarVueSizesAndFallbackOnly} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/avatar/avatar-sizes-and-fallback-only.module.css"
```
</div>

</ExampleCode>

### Расширенная настройка

Используйте `asChild`, если сам аватар должен быть интерактивным элементом, например ссылкой. Это
заменяет стандартный корневой DOM-узел, сохраняя состояние Ark, поведение резервного содержимого и
явную композицию частей.

<ExampleFrame variant="component">
  <AvatarCustomComposition />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
      { label: 'Vue', value: 'vue' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/avatar/custom-composition.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={avatarSolidCustomComposition} />

    </Tab>
    <Tab value="vue">

<CodeBlockRuntime lang="vue" code={avatarVueCustomComposition} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/avatar/avatar-custom-composition.module.css"
```
</div>

</ExampleCode>

## Стилизация

Для структурных стилей используйте `className` в React или `class` в Solid и Vue на экспортируемых частях.
Варианты Tailwind используют обычные утилитарные классы и классы потребителя; приведённый ниже список
CSS-переменных относится к вариантам с CSS Modules. Если стиль зависит от состояния изображения,
используйте атрибуты данных Ark вместе с селекторами `data-slot` moduix.

### CSS-свойства

<CssPropertiesSection properties={AvatarDocs.avatarCssProperties} />

### Хуки стилизации

- `data-slot="avatar-root"`, `avatar-root-provider`, `avatar-image` и `avatar-fallback`.
- Атрибуты анатомии Ark: `data-scope="avatar"` и
  `data-part="root" | "image" | "fallback"`.
- Состояние изображения Ark: `data-state="visible" | "hidden"` на `AvatarImage` и
  `AvatarFallback`.
- Состояние размера moduix: `data-size="xs" | "sm" | "md" | "lg" | "xl"` на
  `Avatar` и `AvatarRootProvider`, если передан `size`.
