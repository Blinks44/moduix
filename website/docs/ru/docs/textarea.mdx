---
title: Textarea
component: textarea
description: Нативное многострочное поле ввода с поддержкой Field и стилями moduix.
---

import * as TextareaDocs from '@/components/examples/textarea/css-properties';
import TextareaAutoResize from '@/components/examples/textarea/auto-resize.tsx';
import TextareaBasic from '@/components/examples/textarea/basic.tsx';
import TextareaControlled from '@/components/examples/textarea/controlled.tsx';
import TextareaDisabledAndReadonly from '@/components/examples/textarea/disabled-and-readonly.tsx';
import TextareaFieldValidation from '@/components/examples/textarea/field-validation.tsx';
import TextareaNativeAttributes from '@/components/examples/textarea/native-attributes.tsx';
import textareaSolidAutoResize from '../../../snippets/textarea/solid/auto-resize.tsx?raw';
import textareaSolidBasic from '../../../snippets/textarea/solid/basic.tsx?raw';
import textareaSolidControlled from '../../../snippets/textarea/solid/controlled.tsx?raw';
import textareaSolidDisabledAndReadonly from '../../../snippets/textarea/solid/disabled-and-readonly.tsx?raw';
import textareaSolidFieldValidation from '../../../snippets/textarea/solid/field-validation.tsx?raw';
import textareaSolidNativeAttributes from '../../../snippets/textarea/solid/native-attributes.tsx?raw';

## Справочник API

В Ark UI нет отдельного примитива `Textarea`. В moduix этот компонент построен на
[`Field.Textarea` из Ark UI](https://ark-ui.com/docs/components/field): он сохраняет нативный API
`textarea`, поддерживает `autoresize` и `asChild`, передаёт ref на нативный элемент и наследует
состояние `Field`.

## Выбор подходящего компонента

Используйте `Textarea` для обычного текста, который может занимать несколько строк и должен вести
себя как нативное поле `textarea` в браузере.

- Выбирайте **`Input`** для коротких однострочных значений, например имён, URL и компактных фильтров.
- Выбирайте **`Textarea`** для заметок, комментариев, описаний и другого длинного текста со свободным вводом.
- Выбирайте **`Editable`**, если текст сначала отображается, а редактирование включается отдельно.
- Выбирайте **`Field` + `Textarea`**, если вы хотите декларативно задать метку, пояснение,
  обязательность или валидацию.

## Базовое использование

Поместите `Textarea` внутрь `Field`, если полю нужны доступная метка или поясняющий текст. Это
рекомендуемый вариант для комментариев, заметок, описаний задач и похожих полей.

Textarea доступен для React и Solid в вариантах с CSS Modules и Tailwind. Предпросмотр использует
пакет React CSS Modules, а вкладки фреймворков показывают нативный код с тем же публичным контрактом.

<ExampleFrame variant="component">
  <TextareaBasic />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/textarea/basic.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={textareaSolidBasic} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/textarea/textarea-basic.module.css"
```
</div>

</ExampleCode>

## Установка через shadcn

<Tabs groupId="framework">
  <Tab label="React" value="react">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-react/textarea' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-react-tailwind/textarea' },
  ]}
/>

  </Tab>
  <Tab label="Solid" value="solid">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-solid/textarea' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-solid-tailwind/textarea' },
  ]}
/>

  </Tab>
</Tabs>

## Анатомия

В публичной структуре есть только одна часть:

```text
Field (optional wrapper)
└─ Textarea
```

| Часть      | Назначение                                               |
| ---------- | -------------------------------------------------------- |
| `Textarea` | Нативный `textarea` со стилями moduix и состоянием `Field`. |

`Textarea` не экспортирует дополнительные оболочки, составные подкомпоненты, провайдеры и
контекстные хуки, скрытые поля, подпись или счётчик символов.

## Композиция

Для неуправляемого поля используйте `defaultValue`. Если состояние хранится в приложении,
используйте `value` и нативный `onChange`, передавая значение через примитив состояния выбранного
фреймворка.

Оборачивайте `Textarea` в `Field`, `Field.Label`, `Field.HelperText` и `Field.ErrorText`, когда
нужны метка, пояснение или валидация. Если `Textarea` находится вне `Field`, задайте ему доступное
имя через нативный `<label>` или `aria-label`.

Включайте `autoresize`, только если поле должно расти вместе с содержимым. В этом режиме Ark
отключает ручное изменение размера, поэтому `--moduix-textarea-resize` действует только при
`autoresize={false}`. При использовании `asChild` передавайте один семантический элемент textarea,
который принимает объединённые свойства и ref.

## Примеры

### Автоматический размер

Включите `autoresize`, чтобы поле увеличивалось по высоте во время ввода. Пока этот режим включён,
Ark отключает ручное изменение размера.

<ExampleFrame variant="component">
  <TextareaAutoResize />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/textarea/auto-resize.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={textareaSolidAutoResize} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/textarea/textarea-auto-resize.module.css"
```
</div>

</ExampleCode>

### Управляемое значение

Передавайте `value` и нативный `onChange`, если содержимым должно управлять состояние приложения.

<ExampleFrame variant="component">
  <TextareaControlled />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/textarea/controlled.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={textareaSolidControlled} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/textarea/textarea-controlled.module.css"
```
</div>

</ExampleCode>

### Отключённое поле и режим только для чтения

Используйте `disabled`, если с полем нельзя взаимодействовать. Используйте `readOnly`, если текст
можно читать и выделять, но нельзя изменять.

<ExampleFrame variant="component">
  <TextareaDisabledAndReadonly />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/textarea/disabled-and-readonly.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={textareaSolidDisabledAndReadonly} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/textarea/textarea-disabled-and-readonly.module.css"
```
</div>

</ExampleCode>

### Валидация поля

Задайте `invalid` у `Field`. Ark добавит к `Textarea` атрибуты `aria-invalid` и `data-invalid` и
свяжет его с текстом ошибки.

<ExampleFrame variant="component">
  <TextareaFieldValidation />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/textarea/field-validation.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={textareaSolidFieldValidation} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/textarea/textarea-field-validation.module.css"
```
</div>

</ExampleCode>

### Нативные атрибуты и отправка формы

Используйте нативные свойства `textarea`, например `rows`, `maxLength` и `spellCheck`, чтобы настроить
поведение браузера. В этом примере форма отправляется, а текущее значение читается через `FormData`.

<ExampleFrame variant="component">
  <TextareaNativeAttributes />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/textarea/native-attributes.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={textareaSolidNativeAttributes} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/textarea/textarea-native-attributes.module.css"
```
</div>

</ExampleCode>

## Стилизация

В React настраивайте поле через `className`, а в Solid через `class`, вместе с
`data-slot="textarea-root"`. Варианты Tailwind используют нативные utility-классы и классы
потребителя. Публичные переменные `--moduix-textarea-*` ниже относятся к вариантам с CSS Modules.

### CSS-свойства

<div className="docs-reference">
<Tabs
    values={[{ label: 'CSS-переменные', value: 'CSS Variables' }]}
    className="docs-reference-tabs"
    contentItemClassName="docs-reference-panel"
> <Tab> <TextareaDocs.TextareaCssPropertiesPanel /> </Tab> </Tabs>
</div>

### Хуки стилизации

Оформляйте внешний вид через `Textarea`, а расположение поля и дополнительного текста через окружающий `Field`.

- `Textarea` отвечает за границу, фон, placeholder, изменение размера и индикатор фокуса.
- `Field` задаёт ширину, интервалы и расположение метки, описания и сообщения об ошибке.
- `data-slot="textarea-root"` служит стабильным селектором для локальных стилей.
- `data-autoresize` присутствует, когда включён `autoresize`.
- Для стилизации состояний используйте `data-invalid`, `[aria-invalid='true']`, `[disabled]`,
  `data-disabled`, `:read-only` и `data-readonly`.
