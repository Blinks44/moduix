---
title: Swap
component: swap
description: Анимируйте переход между двумя визуальными состояниями с готовыми пресетами moduix на базе Ark UI.
---

import SwapAnimationPresets from '@/components/examples/swap/animation-presets.tsx';
import SwapBasic from '@/components/examples/swap/basic.tsx';
import * as SwapDocs from '@/components/examples/swap/css-properties';
import SwapCustomAnimation from '@/components/examples/swap/custom-animation.tsx';
import SwapRootProvider from '@/components/examples/swap/root-provider.tsx';
import swapSolidAnimationPresets from '../../../snippets/swap/solid/animation-presets.tsx?raw';
import swapSolidBasic from '../../../snippets/swap/solid/basic.tsx?raw';
import swapSolidCustomAnimation from '../../../snippets/swap/solid/custom-animation.tsx?raw';
import swapSolidRootProvider from '../../../snippets/swap/solid/root-provider.tsx?raw';

## Справочник API

<PrimitiveReference href="https://ark-ui.com/docs/utilities/swap" />

## Базовый пример


Используйте `Swap`, когда элемент интерфейса должен переключаться между двумя визуальными состояниями,
например когда иконка меняется после действия. Передайте текущее логическое значение в `swap`: при
`false` отображается индикатор `off`, а при `true` индикатор `on`.

<ExampleFrame variant="component">
  <SwapBasic />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/swap/basic.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={swapSolidBasic} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

## Установка через shadcn

Полную настройку пакета, реестра и стилей см. в разделе [Быстрый старт](/docs/quick-start).

<Tabs groupId="framework">
  <Tab label="React" value="react">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-react/swap' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-react-tailwind/swap' },
  ]}
/>

  </Tab>
  <Tab label="Solid" value="solid">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-solid/swap' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-solid-tailwind/swap' },
  ]}
/>

  </Tab>
</Tabs>

## Анатомия

```text
Swap
├─ SwapIndicator type="off"
└─ SwapIndicator type="on"

SwapRootProvider
└─ то же дерево индикаторов, подключённое к состоянию из useSwap()
```

| Часть                | Назначение                                                        |
| -------------------- | ----------------------------------------------------------------- |
| `Swap` | Передаёт состояние `swap` и размещает оба визуальных состояния.  |
| `SwapRootProvider`  | Подключает части к состоянию, созданному через `useSwap()`. |
| `SwapIndicator`     | Отрисовывает состояние `on` или `off`, выбранное свойством `type`. |

## Композиция

`Swap` отвечает только за визуальное представление. Состояние, взаимодействие, фокус и доступное имя
оставьте у окружающего семантического элемента. Для индикаторов, содержащих только иконки, добавьте
`aria-hidden="true"`. Размещайте оба индикатора непосредственными дочерними элементами, чтобы они
использовали одну ячейку сетки, а встроенные рецепты оставались в пределах этого экземпляра `Swap`.

По умолчанию `Swap` использует анимацию `scale`. Передайте в `animation` `fade`, `scale`, `rotate` или
`flip`, либо задайте пользовательское имя и опишите CSS через `data-animation` корня и атрибуты
`data-state` индикаторов от Ark. Если состояние нужно создать вне дерева, передайте результат
`useSwap()` в `SwapRootProvider` и не создавайте для этого состояния ещё один корень. При
использовании `asChild` передавайте ровно один семантический дочерний элемент.

## Примеры

### Предустановки анимации

Выберите встроенный рецепт и активируйте кнопку, чтобы увидеть переход. Текущее значение и кнопки
выбора рецепта отображаются под демонстрацией.

<ExampleFrame variant="component">
  <SwapAnimationPresets />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/swap/animation-presets.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={swapSolidAnimationPresets} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/swap/swap-animation-presets.module.css"
```
</div>

</ExampleCode>

### Пользовательская анимация

Передайте пользовательское имя `animation` и обращайтесь в CSS к `data-animation` корня и атрибуту
`data-state` индикаторов от Ark. Рецепт `bounce` ниже написан на обычном CSS. Пользовательский рецепт
должен описывать анимации появления и выхода и предусматривать вариант для пользователей, включивших
уменьшение анимации.

<ExampleFrame variant="component">
  <SwapCustomAnimation />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/swap/custom-animation.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={swapSolidCustomAnimation} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/swap/swap-custom-animation.module.css"
```
</div>

</ExampleCode>

### Root Provider

Создайте состояние отдельно с помощью `useSwap()`, затем передайте его в `SwapRootProvider`
через свойство `value`. В этом примере используется `asChild`, поэтому семантическая кнопка становится
отрисованным корнем `Swap`.

<ExampleFrame variant="component">
  <SwapRootProvider />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/swap/root-provider.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={swapSolidRootProvider} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/swap/swap-root-provider.module.css"
```
</div>

</ExampleCode>

## Стилизация

Встроенные рецепты используют перечисленные ниже CSS-переменные в вариантах с CSS Modules. Варианты
на Tailwind используют нативные utility-классы и классы, заданные потребителем. Пользовательское имя
анимации отключает встроенные рецепты, но в собственном CSS по-прежнему можно использовать
`--moduix-swap-transition`.

Ark закрепляет раскладку inline-стилями: корень получает `display: inline-grid`, а каждый индикатор -
`grid-area: 1 / 1` и `display: inline-flex`. Переопределения `display` и `grid-area` через классы не
работают ни в одном из вариантов, поэтому меняйте эти свойства через свойство `style` (например,
`style={{ display: 'grid' }}`).

В вариантах Tailwind рецепт flip применяет к корню `data-[animation=flip]:[perspective:24rem]`, который
имеет более высокую специфичность, чем обычный utility-класс `perspective` приложения, поэтому, чтобы
его переопределить, повторите тот же задокументированный вариант (например,
`data-[animation=flip]:[perspective:32rem]`), а трек с CSS Modules вместо этого предоставляет
`--moduix-swap-flip-perspective`.

### CSS-свойства

<CssPropertiesSection properties={SwapDocs.swapOverrideCssProperties} />

### Хуки стилизации

- `Swap` использует `data-slot="swap-root"`.
- `SwapRootProvider` использует `data-slot="swap-root-provider"`.
- `SwapIndicator` использует `data-slot="swap-indicator"`.
- `animation` доступно как `data-animation` на `Swap` и `SwapRootProvider`.
- Встроенные рецепты нацелены только на непосредственных потомков `SwapIndicator` своего корня.
- Ark добавляет `data-scope="swap"` и `data-part="root" | "indicator"`.
- Ark добавляет индикаторам `data-state="open" | "closed"`. moduix использует эти атрибуты во
  встроенных и пользовательских рецептах анимации.
