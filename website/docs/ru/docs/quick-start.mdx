---
title: Быстрый старт
description: Установите moduix как пакет или добавьте исходный код компонентов через реестр shadcn.
---

import { FrameworkGrid } from '@/components/docs/framework-grid';
import { Card, Cards } from '@/components/mdx/Components';

# Быстрый старт

moduix можно использовать в приложении двумя равнозначными способами:

- Установить `@moduix/react` и получать обновления через менеджер пакетов.
- Добавить компоненты через shadcn CLI, чтобы хранить их исходный код, стили и утилиты в своём репозитории.

Оба способа используют одни и те же реализации компонентов, контракты Ark UI и систему токенов. Выбирайте исходя
из того, где должен находиться исходный код и как вы планируете его обновлять.

## Выберите способ установки

<Cards>
  <Card
    title="Установить как пакет"
    description="Оставьте moduix в node_modules и получайте обновления через менеджер пакетов."
    href="#установить-как-пакет"
  />
  <Card
    title="Установить через shadcn CLI"
    description="Добавьте исходный код компонентов и стили в приложение, чтобы менять их напрямую."
    href="#установить-через-shadcn-cli"
  />
</Cards>

Для начала нужны React 18 или 19, `@ark-ui/react` и фреймворк или сборщик, умеющий импортировать CSS из npm-пакетов.
Если фреймворк этого требует, размещайте интерактивные компоненты в клиентском компоненте.

За доступность отвечают все участники: Ark UI реализует поведение примитивов, moduix задаёт контракты обёрток и
настройки по умолчанию, а приложение предоставляет подписи элементов, контент, сообщения валидации и сценарий
взаимодействия.

## Выберите фреймворк

Независимо от способа установки moduix работает одинаково в любом React-стеке. Выберите свой фреймворк, чтобы узнать,
куда подключать стили, как настроить алиас исходного кода и где разместить клиентские компоненты.

<FrameworkGrid />

## Установить как пакет

Выберите этот способ, если хотите оставить moduix в `node_modules` и обновлять его через обычный менеджер пакетов.

### 1. Установите пакет

Компоненты, CSS и дизайн-токены поставляются одним пакетом. React 18 или 19 и Ark UI указаны как peer-зависимости:

<PackageManagerTabs command="install @moduix/react @ark-ui/react" />

`react`, `react-dom` и `@ark-ui/react` остаются зависимостями приложения, поэтому moduix не добавляет отдельные копии
React и примитивов.

### 2. Добавьте стили

Один раз импортируйте обязательный базовый файл стилей в точке входа приложения:

```tsx
import '@moduix/react/style.css';
```

Этот файл задаёт общие дизайн-токены и базовый слой, от которого зависят стили компонентов. Стили конкретных
компонентов подключаются вместе с самими компонентами.

Все дизайн-токены moduix используют пространство имён `--moduix-*`. В проекте можно сохранить собственные имена
токенов и выборочно сопоставить их в файле стилей, который загружается после `style.css`; см. [Токены](/docs/tokens).

Если нужен дополнительный сброс стилей, импортируйте `reset.css` перед основным файлом:

```tsx
import '@moduix/react/reset.css';
import '@moduix/react/style.css';
```

Так `style.css` отвечает только за глобальную основу, а стили неиспользуемых компонентов не попадут в бандл.

Если нужна готовая визуальная основа, импортируйте один из необязательных пресетов после `style.css`; см. [Темы](/docs/themes).

### 3. Используйте компонент

Импортируйте компоненты из соответствующих подпутей `@moduix/react/*`:

```tsx
import { Button } from '@moduix/react/button';
import { Dialog } from '@moduix/react/dialog';

export function Example() {
  return (
    <Dialog>
      <Dialog.Trigger asChild>
        <Button>Open dialog</Button>
      </Dialog.Trigger>
      <Dialog.Backdrop />
      <Dialog.Positioner>
        <Dialog.Content>
          <Dialog.Header>
            <Dialog.Title>Project settings</Dialog.Title>
            <Dialog.Description>Review the settings for this project.</Dialog.Description>
          </Dialog.Header>
          <Dialog.Footer>
            <Dialog.CloseTrigger asChild>
              <Button variant="outline">Close</Button>
            </Dialog.CloseTrigger>
          </Dialog.Footer>
        </Dialog.Content>
      </Dialog.Positioner>
    </Dialog>
  );
}
```

Установка завершена, если кнопка оформлена стилями moduix, диалог открывается с клавиатуры и мышью, а после
закрытия фокус возвращается на кнопку.

## Установить через shadcn CLI

Выберите этот способ, если хотите хранить исходный код компонентов и стили в своём репозитории. shadcn CLI читает
реестр moduix, подтягивает зависимости и размещает файлы по настроенным алиасам.

### 1. Настройте shadcn

Создайте `components.json` в корне проекта с конфигурацией реестра moduix:

```json title="components.json"
{
  "$schema": "https://ui.shadcn.com/schema.json",
  "style": "new-york",
  "rsc": false,
  "tsx": true,
  "tailwind": {
    "config": "",
    "css": "",
    "baseColor": "",
    "cssVariables": true,
    "prefix": ""
  },
  "aliases": {
    "components": "@/components",
    "ui": "@/components/ui",
    "lib": "@/lib",
    "hooks": "@/hooks",
    "utils": "@/lib/utils"
  },
  "registries": {
    "@moduix-react": "https://moduix.dev/r/react/{name}.json"
  }
}
```

Объект `tailwind` требуется схеме конфигурации shadcn, но moduix не использует Tailwind и не требует его установки.
Пустые пути и `baseColor` не позволяют CLI искать файлы Tailwind или загружать палитру shadcn.

Оставьте `rsc: false`, если проект не использует React Server Components. В приложении с RSC установите `rsc: true`.
При `rsc: true` CLI добавляет `use client` к клиентским компонентам из реестра. Для собственных обёрток, в которых
есть состояние, эффекты или обработчики событий, добавляйте эту директиву сами.

Проверьте, что проект разрешает алиас `@/*` к каталогу с исходным кодом. Типичная структура с `src` использует:

```json
{
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "@/*": ["./src/*"]
    }
  }
}
```

Благодаря этому `shadcn` CLI использует алиасы и URL реестра, чтобы поместить сгенерированные файлы в каталог исходного
кода, а не в буквальную папку `@`. В [настройке фреймворка](#выберите-фреймворк) указано, нужен ли этот алиас также
в конфигурации сборщика и какую корневую папку исходного кода выбрать.

### 2. Изучите содержимое реестра

Перед тем как добавлять файлы, проверьте конфигурацию и описание нужного компонента:

```bash
npx shadcn@latest info --json
npx shadcn@latest search @moduix-react --query "dialog"
npx shadcn@latest view @moduix-react/dialog
npx shadcn@latest add @moduix-react/dialog --dry-run
```

Ищите решение для нужной задачи, а не знакомое название библиотеки. В реестре есть компоненты, темы и их объявленные
зависимости. Тот же способ поиска через shadcn MCP описан в [руководстве по AI](/docs/ai).

### 3. Добавьте компонент

Добавляйте компоненты из реестра moduix:

<PackageManagerTabs command="shadcn@latest add @moduix-react/button" dlx />

Чтобы добавить больше компонентов, выполните ту же команду с именем другого компонента, например
`@moduix-react/accordion`.

Реестр добавляет только общие служебные файлы, необходимые компонентам. Глобальный шрифт он не добавляет, поэтому
типографика продолжает наследоваться от приложения.

### 4. Импортируйте сгенерированные стили

Импортируйте базовый файл стилей из файлов, которые `shadcn` добавил в проект:

```tsx
import '@/lib/moduix/styles/style.css';
```

Если при таком способе установки нужен дополнительный сброс стилей, добавьте отдельный пакет из реестра:

```bash
npx shadcn@latest add @moduix-react/reset
```

Затем импортируйте его перед основным файлом стилей:

```tsx
import '@/lib/moduix/styles/reset.css';
import '@/lib/moduix/styles/style.css';
```

В примерах ниже используются `@/components` и `@/lib`. Если проект использует другие алиасы, обновите
`components.json` и импортируйте сгенерированные файлы по соответствующим путям.

### 5. Используйте созданные компоненты

Импортируйте компоненты из созданных файлов в своём проекте:

```tsx
import { Button } from '@/components/moduix/button';
import { Accordion } from '@/components/moduix/accordion';
```

Большинство компонентов собрано из именованных частей. Такая структура сохраняет доступное поведение и позволяет
управлять разметкой, компоновкой и стилями в приложении.

Подход к хранению исходного кода вдохновлён [shadcn/ui](https://ui.shadcn.com/), а структура компонентов и их
поведение соответствуют [Ark UI](https://ark-ui.com/).

## Настройте слои порталов

Эта настройка нужна при обоих способах установки. `Dialog`, `Popover`, `Menu`, `Select` и другие компоненты
выводят содержимое оверлеев через порталы. Изолируйте корневой элемент приложения, чтобы подложки и всплывающие
элементы всегда были поверх страницы и не конфликтовали с локальными значениями `z-index`.

```tsx title="layout.tsx"
<body>
  <div className="root">{children}</div>
</body>
```

```css title="styles.css"
.root {
  isolation: isolate;
}
```

Если приложение поддерживает Safari на iOS 26, проверьте прокручиваемое модальное окно при видимой панели браузера.
Если контент виден под подложкой, задайте `body` значение `position: relative`, чтобы абсолютно позиционированная
подложка могла закрыть всю видимую область.

```css title="styles.css"
body {
  position: relative;
}
```

## Стилизация

moduix совместим с подходом к стилям, который уже используется в проекте:

- Обычный CSS с глобальными селекторами, каскадными слоями и CSS-переменными.
- CSS Modules с `className` и классами слотов компонентов.
- Утилитарные классы Tailwind CSS для компоновки и точечных изменений.
- CSS-in-JS-библиотеки, которые добавляют классы или передают стили через собственные обёртки.

В стандартной плотности основные элементы управления используют `--moduix-size-md: 36px`, а строки меню и других
всплывающих списков используют `--moduix-size-sm: 32px`. Компоненты используют общую шкалу, поэтому изменение этих токенов
одновременно обновит связанные поля ввода, триггеры, кнопки и элементы меню.

На страницах компонентов перечислены их публичные CSS-переменные и точки настройки стилей. Для изменений во всей
системе используйте общие семантические токены, а переменные компонентов оставьте для отдельных исключений. Полная
иерархия описана в [Токенах](/docs/tokens), а рецепты анимации в [Анимациях](/docs/animations).

## Устранение неполадок

| Проблема | Что проверить |
| --- | --- |
| Компоненты отображаются без стилей | Один раз импортируйте правильный `style.css` после дополнительного сброса стилей, если он используется. |
| Сборщик не находит импорт созданного компонента | Проверьте алиас `@/*` в TypeScript и сборщике, затем `components.json`. |
| Всплывающий элемент отображается под содержимым страницы | Добавьте `isolation: isolate` корневому элементу приложения и избегайте несвязанных глобальных значений `z-index`. |
| При серверном рендеринге появляются ошибки о клиентских API | В нужных фреймворках размещайте интерактивные компоненты в клиентском компоненте. |

## Читайте далее

- [Переход с shadcn](/docs/migrate-from-shadcn): имена компонентов, импорты, стилизация и различия в способах
  хранения исходного кода.
- [Паттерны композиции](/docs/composition-patterns): правила композиции с приоритетом обёрток и низкоуровневые
  возможности тонкой настройки.
- [Токены](/docs/tokens): общие дизайн-токены, глобальные переопределения и резервные значения компонентов.
- [Тёмная тема](/docs/dark-mode) и [режим справа налево (RTL)](/docs/rtl): настройка темы и направления для всего
  приложения.
- [Обновление и миграция](/docs/updating): процессы обновления пакета и исходного кода в проекте.
- [Анимации](/docs/animations): значения анимации по умолчанию и рецепты локальных переходов.
