---
title: Tooltip
component: tooltip
description: Короткая дополнительная подсказка, которая появляется при наведении или когда триггер получает фокус.
---

import * as TooltipDocs from '@/components/examples/tooltip/css-properties';
import TooltipAdvancedCustomization from '@/components/examples/tooltip/advanced-customization.tsx';
import TooltipBasic from '@/components/examples/tooltip/basic.tsx';
import TooltipContext from '@/components/examples/tooltip/context.tsx';
import TooltipControlled from '@/components/examples/tooltip/controlled.tsx';
import TooltipDelay from '@/components/examples/tooltip/delay.tsx';
import TooltipDisabledTrigger from '@/components/examples/tooltip/disabled-trigger.tsx';
import TooltipMultipleTriggers from '@/components/examples/tooltip/multiple-triggers.tsx';
import TooltipPositioning from '@/components/examples/tooltip/positioning.tsx';
import TooltipRootProvider from '@/components/examples/tooltip/root-provider.tsx';
import TooltipWithArrow from '@/components/examples/tooltip/with-arrow.tsx';
import TooltipWithinFixedContainers from '@/components/examples/tooltip/within-fixed-containers.tsx';
import tooltipSolidAdvancedCustomization from '../../../snippets/tooltip/solid/advanced-customization.tsx?raw';
import tooltipSolidBasic from '../../../snippets/tooltip/solid/basic.tsx?raw';
import tooltipSolidContext from '../../../snippets/tooltip/solid/context.tsx?raw';
import tooltipSolidControlled from '../../../snippets/tooltip/solid/controlled.tsx?raw';
import tooltipSolidDelay from '../../../snippets/tooltip/solid/delay.tsx?raw';
import tooltipSolidDisabledTrigger from '../../../snippets/tooltip/solid/disabled-trigger.tsx?raw';
import tooltipSolidMultipleTriggers from '../../../snippets/tooltip/solid/multiple-triggers.tsx?raw';
import tooltipSolidPositioning from '../../../snippets/tooltip/solid/positioning.tsx?raw';
import tooltipSolidRootProvider from '../../../snippets/tooltip/solid/root-provider.tsx?raw';
import tooltipSolidWithArrow from '../../../snippets/tooltip/solid/with-arrow.tsx?raw';
import tooltipSolidWithinFixedContainers from '../../../snippets/tooltip/solid/within-fixed-containers.tsx?raw';

## Справочник API

<PrimitiveReference href="https://ark-ui.com/docs/components/tooltip" />

## Выбор подходящего компонента

Используйте `Tooltip`, чтобы показывать короткую дополнительную информацию при наведении или
когда триггер получает фокус.

- Выбирайте **`Tooltip`** для коротких подписей и некликабельных контекстных подсказок.
- Выбирайте **`Popover`**, если во всплывающем элементе нужны подробное содержимое, действия или элементы формы.
- Выбирайте **`HoverCard`**, если нужно показать дополнительный предпросмотр, связанный с триггером.

## Базовый пример


Используйте `TooltipTrigger` для элемента, который открывает подсказку, а `TooltipBody` для её
содержимого. `TooltipBody` объединяет `TooltipPositioner` и `TooltipContent`. У триггера должно
быть собственное доступное имя. Содержимое подсказки дополняет интерфейс и не заменяет видимый
текст или `aria-label`.

<ExampleFrame variant="component">
  <TooltipBasic />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/tooltip/basic.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={tooltipSolidBasic} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

## Установка через shadcn

Полную настройку пакета, реестра и стилей см. в разделе [Быстрый старт](/docs/quick-start).

<Tabs groupId="framework">
  <Tab label="React" value="react">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-react/tooltip' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-react-tailwind/tooltip' },
  ]}
/>

  </Tab>
  <Tab label="Solid" value="solid">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-solid/tooltip' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-solid-tailwind/tooltip' },
  ]}
/>

  </Tab>
</Tabs>

## Анатомия

```text
Tooltip
├─ TooltipTrigger
└─ Поддерево всплывающего содержимого (автоматически выводится через портал)
   └─ TooltipPositioner
      └─ TooltipContent
         └─ содержимое
```

Добавьте `TooltipArrow` первым потомком содержимого, если всплывающий элемент должен указывать на
триггер. `TooltipArrow` по умолчанию выводит `TooltipArrowTip`.

```text
Tooltip
├─ TooltipTrigger
└─ Поддерево всплывающего содержимого (автоматически выводится через портал)
   └─ TooltipPositioner
      └─ TooltipContent
         ├─ TooltipArrow
         │  └─ TooltipArrowTip
         └─ содержимое
```

| Часть | Назначение |
| --- | --- |
| `Tooltip` | Управляет состоянием подсказки, открытием и закрытием, задержками, позиционированием и жизненным циклом. |
| `TooltipRootProvider` | Отображает подсказку из объекта состояния `useTooltip()`, созданного за пределами дерева. |
| `TooltipContext` | Передаёт текущее состояние пользовательским потомкам через render prop. |
| `TooltipBody` | Удобная обёртка, которая одновременно выводит `TooltipPositioner` и `TooltipContent`. |
| `TooltipTrigger` | Интерактивный элемент, открывающий подсказку при наведении или фокусе. |
| `TooltipDisabledTrigger` | Доступная для фокуса обёртка для одного отключённого нативного элемента управления, которому нужно пояснение. |
| `TooltipPositioner` | Позиционирует содержимое и предоставляет переменные позиционирования Ark. |
| `TooltipContent` | Видимая поверхность подсказки. |
| `TooltipArrow` | Необязательная позиционируемая обёртка стрелки, по умолчанию выводящая `TooltipArrowTip`. |
| `TooltipArrowTip` | Видимый наконечник стрелки. |

## Композиция

Для стандартной композиции `Trigger` и содержимого используйте `TooltipBody`: он объединяет
`TooltipPositioner` и `TooltipContent`. Используйте эти части явно, если им нужны независимые
стили или структура. Корневой компонент владеет состоянием: `defaultOpen` подходит для
неконтролируемой подсказки, а `open` вместе с `onOpenChange(details)` используется, когда состояние
хранится в другой части приложения. Используйте `TooltipRootProvider` с `useTooltip()`, если
хранилище создаётся за пределами дерева, а `TooltipContext` или `useTooltipContext()`, если
состояние нужно потомку. `asChild` принимает один доступный для фокуса элемент, а в Solid
используется функция рендеринга. `TooltipDisabledTrigger` предназначен для одного отключённого
нативного элемента. Портал включён по умолчанию, поэтому задавайте `portalled={false}` или
передавайте `portalRef` только при необходимости ограничить подсказку конкретной DOM-областью.

## Примеры

### Контекст

Используйте `useTooltipContext()` внутри потомка, которому нужно текущее состояние подсказки.

<ExampleFrame variant="component">
  <TooltipContext />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/tooltip/context.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={tooltipSolidContext} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Управляемое состояние

Управляйте открытием подсказки с помощью `open` и `onOpenChange(details)`.

<ExampleFrame variant="component">
  <TooltipControlled />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/tooltip/controlled.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={tooltipSolidControlled} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Задержка

Укажите `openDelay` и `closeDelay` у корневого компонента, чтобы настроить задержки при наведении и
фокусе.

<ExampleFrame variant="component">
  <TooltipDelay />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/tooltip/delay.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={tooltipSolidDelay} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Отключённый триггер

Используйте `TooltipDisabledTrigger`, когда отключённому нативному элементу управления всё ещё
нужно краткое пояснение. Обёртка может получать фокус, не меняя состояние отключённого потомка.

<ExampleFrame variant="component">
  <TooltipDisabledTrigger />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/tooltip/disabled-trigger.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={tooltipSolidDisabledTrigger} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Несколько триггеров

Задайте каждому триггеру `value` и используйте `onTriggerValueChange(details)`, чтобы связать одну
подсказку с несколькими триггерами.

<ExampleFrame variant="component">
  <TooltipMultipleTriggers />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/tooltip/multiple-triggers.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={tooltipSolidMultipleTriggers} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/tooltip/component-multiple-triggers.module.css"
```
</div>

</ExampleCode>

### Позиционирование

Передавайте в `Tooltip` параметры позиционирования Ark, включая размещение и значения отступов.

<ExampleFrame variant="component">
  <TooltipPositioning />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/tooltip/positioning.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={tooltipSolidPositioning} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/tooltip/component-positioning.module.css"
```
</div>

</ExampleCode>

### Root Provider

Используйте `useTooltip()` вместе с `TooltipRootProvider`, если состояние подсказки создаётся за
пределами дерева, в котором она отображается.

<ExampleFrame variant="component">
  <TooltipRootProvider />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/tooltip/root-provider.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={tooltipSolidRootProvider} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Со стрелкой

Добавьте `TooltipArrow` первым потомком содержимого, если подсказка должна визуально указывать на
триггер. При использовании `TooltipBody` разместите его непосредственно внутри `TooltipBody`,
обёртка сама добавит стандартный `TooltipArrowTip`.

<ExampleFrame variant="component">
  <TooltipWithArrow />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/tooltip/with-arrow.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={tooltipSolidWithArrow} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### В фиксированных контейнерах

Используйте `positioning.strategy: "fixed"`, если триггер находится в фиксированной или липкой
области интерфейса. В этом примере задано `closeOnScroll={false}`, чтобы можно было сфокусировать
триггер, прокрутить панель и проверить выравнивание подсказки.

<ExampleFrame variant="component">
  <TooltipWithinFixedContainers />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/tooltip/within-fixed-containers.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={tooltipSolidWithinFixedContainers} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/tooltip/component-within-fixed-containers.module.css"
```
</div>

</ExampleCode>

### Расширенная настройка

Используйте явные части `TooltipPositioner` и `TooltipContent`, когда им нужны независимые
композиция или стилизация.

<ExampleFrame variant="component">
  <TooltipAdvancedCustomization />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/tooltip/advanced-customization.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={tooltipSolidAdvancedCustomization} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

## Стилизация

Используйте `className` в React или `class` в Solid у экспортируемых частей для структурных
стилей. Для состояний предпочитайте атрибуты Ark, такие как `data-state`, `data-placement` и
`data-side`, вместе с селекторами moduix `data-slot`. Варианты Tailwind используют нативные
утилиты и классы потребителя, а переменные CSS ниже настраивают вариант с CSS Modules.

### CSS-свойства

<div className="not-prose">
  <Tabs
    values={[{ label: 'Переменные CSS', value: 'CSS Variables' }]}
    className="docs-reference-tabs"
    contentItemClassName="docs-reference-panel"
  >
    <Tab>
      <TooltipDocs.TooltipCssPropertiesPanel />
    </Tab>
  </Tabs>
</div>

### Точки для стилизации

- `TooltipBody` не создаёт DOM-узел. При явной композиции стилизуйте отдельно
  `TooltipPositioner` и `TooltipContent`.
- `TooltipTrigger` отвечает за оформление триггера, состояния при наведении и открытии, а также за
  стили фокуса.
- `TooltipDisabledTrigger` отвечает за обёртку отключённого элемента управления и его прямого
  отключённого потомка.
- `TooltipPositioner` отвечает за ограничения плавающего позиционирования.
- `TooltipContent` отвечает за оформление всплывающей поверхности.
- `TooltipArrow` и `TooltipArrowTip` отвечают за размер, фон и обводку стрелки.

Основные публичные точки привязки: `data-slot="tooltip-trigger"`,
`data-slot="tooltip-positioner"`, `data-slot="tooltip-content"`,
`data-slot="tooltip-disabled-trigger"`, `data-slot="tooltip-arrow"` и
`data-slot="tooltip-arrow-tip"`.

Ark также добавляет к соответствующим частям `data-scope="tooltip"`, `data-part`,
`data-state="open" | "closed"`, `data-value`, `data-current`, `data-expanded`, `data-instant`,
`data-placement` и `data-side`. Positioner предоставляет CSS-переменные времени выполнения, в том
числе `--available-width`, `--available-height`, `--reference-width`, `--reference-height`, `--x`,
`--y`, `--z-index` и `--transform-origin`.
