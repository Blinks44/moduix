---
title: Highlight
description: Выделяйте слова и фразы внутри существующего текста с помощью нативных элементов mark и стилей moduix.
---

import * as HighlightDocs from '@/components/examples/highlight/css-properties';
import HighlightBasic from '@/components/examples/highlight/basic.tsx';
import HighlightDynamicQuery from '@/components/examples/highlight/dynamic-query.tsx';
import HighlightExactMatch from '@/components/examples/highlight/exact-match.tsx';
import HighlightIgnoreCase from '@/components/examples/highlight/ignore-case.tsx';
import HighlightMatchAll from '@/components/examples/highlight/match-all.tsx';
import HighlightMultipleQueries from '@/components/examples/highlight/multiple-queries.tsx';
import highlightSolidBasic from '../../../snippets/highlight/solid/basic.tsx?raw';
import highlightSolidDynamicQuery from '../../../snippets/highlight/solid/dynamic-query.tsx?raw';
import highlightSolidExactMatch from '../../../snippets/highlight/solid/exact-match.tsx?raw';
import highlightSolidIgnoreCase from '../../../snippets/highlight/solid/ignore-case.tsx?raw';
import highlightSolidMatchAll from '../../../snippets/highlight/solid/match-all.tsx?raw';
import highlightSolidMultipleQueries from '../../../snippets/highlight/solid/multiple-queries.tsx?raw';

## Справочник API

<PrimitiveReference href="https://ark-ui.com/docs/utilities/highlight" />

## Выбор подходящего компонента

Используйте `Highlight`, чтобы выделить поисковые термины внутри текста, у которого уже есть
семантический контейнер. Компонент выводит совпавшие фрагменты, но не добавляет собственную обёртку.

- Выбирайте **`Highlight`**, когда нужно выделить один или несколько терминов в готовом предложении, подписи или текстовом блоке.
- Выбирайте **`Text`**, когда всей строке нужно только типографическое оформление, без поиска совпадений.

## Базовый пример


Передайте полный исходный текст в `text`, а искомый термин в `query`. Разместите `Highlight` внутри
текстового компонента или HTML-элемента, который отвечает за внешнюю разметку и семантику.

<ExampleFrame variant="component">
  <HighlightBasic />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/highlight/basic.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={highlightSolidBasic} />

    </Tab>
  </Tabs>
</div>
</ExampleCode>

## Установка через shadcn

Полную настройку пакета, реестра и стилей см. в разделе [Быстрый старт](/docs/quick-start).

<Tabs groupId="framework">
  <Tab label="React" value="react">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-react/highlight' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-react-tailwind/highlight' },
  ]}
/>

  </Tab>
  <Tab label="Solid" value="solid">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-solid/highlight' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-solid-tailwind/highlight' },
  ]}
/>

  </Tab>
</Tabs>

## Анатомия

У `Highlight` одна публичная корневая часть. Компонент возвращает фрагмент и не добавляет отдельную
обёртку. Каждый совпавший фрагмент становится нативным элементом `<mark>`, а текст без совпадений
остаётся обычным текстом в окружающей разметке.

```text
Highlight
└─ совпавшие фрагменты в отдельных элементах <mark>
```

| Часть                          | Назначение                                                        |
| ------------------------------ | ----------------------------------------------------------------- |
| `Highlight` | Применяет стили moduix к каждому совпадению в переданном тексте. |

## Композиция

`Highlight` не хранит состояние и не создаёт обёртку для внешней разметки. Размещайте его внутри
готового текстового контейнера, например `Text`, заголовка, элемента списка или ячейки таблицы.
Управляйте `query` из родительского компонента, если поисковый запрос меняется. Для массива строк
`matchAll` должен оставаться равным `true`; если каждому совпадению нужен другой элемент или
интерактивное поведение, создайте такую разметку самостоятельно.

## Примеры

### Динамический запрос

Управляйте `query` состоянием React или Solid, чтобы выделять текущий термин во время поиска или
фильтрации видимого текста.

<ExampleFrame variant="component">
  <HighlightDynamicQuery />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/highlight/dynamic-query.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={highlightSolidDynamicQuery} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/highlight/highlight-dynamic-query.module.css"
```
</div>
</ExampleCode>

### Точное совпадение

Включите `exactMatch`, если частичные совпадения дают слишком много лишних результатов. В этом
примере `box` совпадёт с отдельным словом, но не с `checkbox` и `combobox`. Поиск использует
границы слов JavaScript: для латинского текста они работают надёжно, но не распознают кириллицу и
некоторые другие системы письма как целые слова.

<ExampleFrame variant="component">
  <HighlightExactMatch />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/highlight/exact-match.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={highlightSolidExactMatch} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/highlight/highlight-exact-match.module.css"
```
</div>
</ExampleCode>

### Игнорирование регистра

Включите `ignoreCase`, если регистр букв в исходном тексте не должен влиять на результат поиска.

<ExampleFrame variant="component">
  <HighlightIgnoreCase />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/highlight/ignore-case.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={highlightSolidIgnoreCase} />

    </Tab>
  </Tabs>
</div>
</ExampleCode>

### Все совпадения

Включите `matchAll`, если нужно выделить каждое вхождение, а не только первое.

<ExampleFrame variant="component">
  <HighlightMatchAll />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/highlight/match-all.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={highlightSolidMatchAll} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/highlight/highlight-match-all.module.css"
```
</div>
</ExampleCode>

### Несколько запросов

Передайте массив строк, если нужно выделить несколько терминов в одном тексте. Для массива
`matchAll` по умолчанию равен `true`; значение `matchAll={false}` приводит к ошибке.

<ExampleFrame variant="component">
  <HighlightMultipleQueries />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/highlight/multiple-queries.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={highlightSolidMultipleQueries} />

    </Tab>
  </Tabs>
</div>
</ExampleCode>

## Стилизация

Совпавшие фрагменты получают стандартные стили moduix для `<mark>`. Для локальной правки в React
используйте `className`, а в Solid `class`. Варианты CSS Modules также предоставляют переменные
`--moduix-highlight-*`; для Tailwind используйте utility-классы или классы потребителя.

### CSS-свойства

<CssPropertiesSection properties={HighlightDocs.highlightCssProperties} />

### Хуки стилизации

- Каждый совпавший `<mark>` получает `data-scope="highlight"` и `data-part="root"`.
- `data-slot="highlight-root"` является стабильным селектором moduix для каждого совпавшего `mark`.
- Такие свойства, как `className`/`class`, `style`, `title`, `id` и `data-*`, применяются к каждому совпавшему `<mark>`.
- Компонент не создаёт обёртку, не добавляет атрибут состояния и не задаёт CSS-переменных Ark.
