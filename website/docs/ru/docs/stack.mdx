---
title: Stack
component: stack
description: Небольшой flex-компонент для вертикальной и горизонтальной раскладки в React и Solid.
---

import * as StackDocs from '@/components/examples/stack/css-properties';
import StackBasic from '@/components/examples/stack/basic.tsx';
import StackFill from '@/components/examples/stack/growing-child-stack.tsx';
import StackLoadingLayout from '@/components/examples/stack/loading-layout.tsx';
import StackResponsiveDirection from '@/components/examples/stack/responsive-direction.tsx';
import StackRowLayout from '@/components/examples/stack/row-layout.tsx';
import StackSemanticElement from '@/components/examples/stack/semantic-element.tsx';
import StackSeparator from '@/components/examples/stack/separator.tsx';
import StackWrappedItems from '@/components/examples/stack/wrapped-items.tsx';
import stackSolidBasic from '../../../snippets/stack/solid/basic.tsx?raw';
import stackSolidFill from '../../../snippets/stack/solid/growing-child-stack.tsx?raw';
import stackSolidLoadingLayout from '../../../snippets/stack/solid/loading-layout.tsx?raw';
import stackSolidResponsiveDirection from '../../../snippets/stack/solid/responsive-direction.tsx?raw';
import stackSolidRowLayout from '../../../snippets/stack/solid/row-layout.tsx?raw';
import stackSolidSemanticElement from '../../../snippets/stack/solid/semantic-element.tsx?raw';
import stackSolidSeparator from '../../../snippets/stack/solid/separator.tsx?raw';
import stackSolidWrappedItems from '../../../snippets/stack/solid/wrapped-items.tsx?raw';

## Справочник API

В Ark UI нет отдельного примитива `Stack`. В moduix этот компонент реализован на основе официальной
[фабрики Ark](https://ark-ui.com/docs/guides/composition#the-ark-factory) и следует рекомендациям Ark по
[композиции](https://ark-ui.com/docs/guides/composition), [стилизации](https://ark-ui.com/docs/guides/styling) и
[работе с ref](https://ark-ui.com/docs/guides/ref). Названия свойств раскладки взяты из документации
[Stack](https://chakra-ui.com/docs/components/stack) в Chakra UI.

## Базовый пример

Stack доступен для React и Solid в пакетах с CSS Modules и в вариантах на Tailwind. Интерактивное
превью на этой странице использует React и CSS Modules, а вкладки показывают нативный исходный код
для того же публичного контракта.

Используйте `Stack`, когда достаточно одного flex-контейнера и нужно настроить направление, промежуток и
выравнивание.

<ExampleFrame variant="component">
  <StackBasic />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/stack/basic.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={stackSolidBasic} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

## Установка через shadcn

Полную настройку пакета, реестра и стилей см. в разделе [Быстрый старт](/docs/quick-start).

<Tabs groupId="framework">
  <Tab label="React" value="react">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-react/stack' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-react-tailwind/stack' },
  ]}
/>

  </Tab>
  <Tab label="Solid" value="solid">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-solid/stack' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-solid-tailwind/stack' },
  ]}
/>

  </Tab>
</Tabs>

## Анатомия

`Stack` отрисовывает один корневой flex-контейнер по модели фабрики Ark. Компонент управляет направлением,
промежутком, выравниванием, переносом элементов и простым адаптивным переключением направления. Остальные правила
раскладки задавайте в локальном CSS.

```text
Stack / Stack.Root
└─ root[data-scope="stack"][data-part="root"][data-slot="stack-root"]
   └─ children
```

| Часть                  | Назначение                                                               |
| ---------------------- | ------------------------------------------------------------------------ |
| `Stack` / `Stack.Root` | Корневая flex-обёртка для направления, промежутка, выравнивания и расстояния между дочерними элементами. |

## Композиция

`Stack` не владеет управляемым состоянием и предоставляет один корневой элемент. Для обычного вывода `div` используйте
короткую форму `<Stack>`, а `Stack.Root` является её псевдонимом. Свойство `fill` влияет на `Stack` как на flex-элемент,
а не на его дочерние элементы. Добавляйте `Separator` напрямую, если нужен разделитель, и используйте `asChild`, когда
корневой элемент должен иметь семантический тег.

## Примеры

### Вложенный Stack, занимающий свободное место

Используйте `fill`, когда вложенный `Stack` должен занять оставшееся место в flex-строке.

<ExampleFrame variant="component">
  <StackFill />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/stack/growing-child-stack.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={stackSolidFill} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Раскладка состояния загрузки

Объедините `Stack` и `Skeleton`, если для состояния загрузки нужны только flex-разметка и отступы.

<ExampleFrame variant="component">
  <StackLoadingLayout />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/stack/loading-layout.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={stackSolidLoadingLayout} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Адаптивное направление

Используйте адаптивный `direction`, чтобы переключать раскладку с колонки на мобильных экранах на строку на больших.
Значение `desktop` начинает действовать с `640px`; если указать только одну часть адаптивного значения, Stack
использует её в обоих диапазонах.

<ExampleFrame variant="component">
  <StackResponsiveDirection />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/stack/responsive-direction.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={stackSolidResponsiveDirection} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Горизонтальная раскладка

Используйте `direction="row"` и свойства выравнивания, чтобы расположить содержимое в одну строку.

<ExampleFrame variant="component">
  <StackRowLayout />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/stack/row-layout.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={stackSolidRowLayout} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Семантический элемент

Используйте `asChild` от Ark, когда корневая обёртка раскладки должна также описывать структуру документа.

<ExampleFrame variant="component">
  <StackSemanticElement />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/stack/semantic-element.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={stackSolidSemanticElement} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Разделитель

Добавляйте `Separator` напрямую, когда горизонтальному или вертикальному `Stack` нужен повторяющийся визуальный
разделитель между дочерними элементами. `gap` применяется к каждому дочернему элементу, включая разделители.

<ExampleFrame variant="component">
  <StackSeparator />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/stack/separator.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={stackSolidSeparator} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Перенос элементов

Используйте `wrap="wrap"`, когда элементы должны переходить на следующую строку.

<ExampleFrame variant="component">
  <StackWrappedItems />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/stack/wrapped-items.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={stackSolidWrappedItems} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

## Стилизация

Используйте `className` в React или `class` в Solid на корневом элементе для структурных стилей. Варианты Tailwind
используют нативные утилитарные классы и переопределения потребителя, а CSS-переменные ниже настраивают варианты
CSS Modules. Атрибуты данных и хук `data-slot` общие для обоих вариантов стилизации.

### CSS-свойства

`Stack` предоставляет публичные CSS-переменные в корневом элементе. По умолчанию для них используются `column` и
`initial`; свойства `direction` и `fill` записывают встроенные значения только при передаче, а `style` может
переопределить их для конкретного экземпляра.

<div className="docs-reference">
  <Tabs
    values={[{ label: 'CSS-переменные', value: 'CSS-переменные' }]}
    className="docs-reference-tabs"
    contentItemClassName="docs-reference-panel"
  >
    <Tab>
      <StackDocs.StackCssPropertiesPanel />
    </Tab>
  </Tabs>
</div>

### Хуки стилизации

- `className` или `class` задаёт стили единственному корневому элементу: ширину, внутренние отступы, границы, фон и другие свойства оформления.
- `data-scope="stack"` и `data-part="root"` соответствуют соглашениям Ark для стилизации.
- `data-slot="stack-root"` - стабильный селектор moduix для корневого элемента в локальном CSS.
- `style` применяется последним и может переопределить вычисляемые `gap`, `alignItems`, `justifyContent`, `flexWrap`
  и любые переменные `--moduix-stack-*`, записанные свойствами.
- В публичном контракте нет атрибутов состояния, CSS-переменных для анимации или объекта `slotProps`.
