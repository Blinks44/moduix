---
title: Bleed
component: bleed
description: Компонент для выхода отдельных блоков за границы ограниченного контейнера.
---

import * as BleedDocs from '@/components/examples/bleed/css-properties';
import BleedBasic from '@/components/examples/bleed/basic.tsx';
import BleedBlockBleed from '@/components/examples/bleed/block-bleed.tsx';
import BleedCustomStyling from '@/components/examples/bleed/custom-styling.tsx';
import BleedInlineAmounts from '@/components/examples/bleed/inline-amounts.tsx';
import BleedSemanticElement from '@/components/examples/bleed/semantic-element.tsx';
import bleedSolidBasic from '../../../snippets/bleed/solid/basic.tsx?raw';
import bleedSolidBlockBleed from '../../../snippets/bleed/solid/block-bleed.tsx?raw';
import bleedSolidCustomStyling from '../../../snippets/bleed/solid/custom-styling.tsx?raw';
import bleedSolidInlineAmounts from '../../../snippets/bleed/solid/inline-amounts.tsx?raw';
import bleedSolidSemanticElement from '../../../snippets/bleed/solid/semantic-element.tsx?raw';

## Справочник API

В Ark UI нет отдельного примитива `bleed`. В moduix компонент `Bleed` построен на официальной
[фабрике Ark](https://ark-ui.com/docs/guides/composition#the-ark-factory) и следует рекомендациям Ark по
[композиции](https://ark-ui.com/docs/guides/composition), [стилизации](https://ark-ui.com/docs/guides/styling) и
[работе с ref](https://ark-ui.com/docs/guides/ref).

Используйте [Container](/docs/container), чтобы сохранить удобную для чтения ширину основного содержимого.
Размещайте внутри него `Bleed`, если за границы контейнера должен выйти только медиаблок, фон, разделитель или баннер.

Импортируйте `Bleed` из пакета, соответствующего фреймворку и способу стилизации: для CSS Modules это
`@moduix/react/bleed` или `@moduix/solid/bleed`, а для Tailwind - по соответствующему пути
`@moduix/react-tailwind/bleed` или `@moduix/solid-tailwind/bleed`.

## Базовый пример

Bleed доступен для React и Solid в пакетах с CSS Modules и в вариантах на Tailwind. Интерактивный пример
на этой странице использует пакет React с CSS Modules, а вкладки с фреймворками показывают нативный исходный код
того же публичного API.

Значение `inline="full"` по умолчанию позволяет медиаблоку, баннеру или декоративному элементу секции растянуться
до краёв окна браузера и при этом остаться в обычном потоке документа.

<ExampleFrame variant="component">
  <BleedBasic />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/bleed/basic.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={bleedSolidBasic} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/bleed/bleed-basic.module.css"
```
</div>

</ExampleCode>

## Установка через shadcn

Полную настройку пакета, реестра и стилей см. в разделе [Быстрый старт](/docs/quick-start).

<Tabs groupId="framework">
  <Tab label="React" value="react">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-react/bleed' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-react-tailwind/bleed' },
  ]}
/>

  </Tab>
  <Tab label="Solid" value="solid">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-solid/bleed' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-solid-tailwind/bleed' },
  ]}
/>

  </Tab>
</Tabs>

## Анатомия

`Bleed` отрисовывает один корневой элемент в соответствии с соглашениями Ark. Он применяет отрицательные внешние
отступы по строчной и/или блочной оси и не добавляет внутреннюю обёртку.

```text
Bleed / Bleed.Root
└─ root[data-scope="bleed"][data-part="root"][data-slot="bleed-root"]
   └─ children
```

| Часть                  | Назначение                                                                  |
| ---------------------- | --------------------------------------------------------------------------- |
| `Bleed` / `Bleed.Root` | Корневой компонент для выхода содержимого по строчной и блочной оси.       |

## Композиция

`Bleed` отвечает только за визуальное оформление и не владеет состоянием. У него одна корневая часть, поэтому
дополнительные части не нужно согласовывать; `Bleed.Root` - псевдоним `Bleed`. По умолчанию `inline` равен
`full`, `block` - `none`, а `asChild` выключен. Если через `asChild` корневым элементом должен стать другой
семантический элемент, передайте в React ровно одного дочернего элемента, который принимает DOM-свойства и ref,
или используйте функцию рендера в Solid. В React ref указывает на отрисованный корневой или дочерний элемент;
функция рендера Solid не передаёт ref от компонента к пользовательскому элементу.

Сочетайте `inline` и `block`, если поверхность должна выйти за отступы по обеим логическим осям. `inline="full"`
рассчитывается относительно окна браузера, поэтому во вложенной оболочке могут понадобиться локальные переопределения
`--moduix-bleed-inline-full` и `--moduix-bleed-inline-full-size`.

## Примеры

### Выход за пределы по блочной оси

Добавьте `block`, если поверхности нужно выйти за внутренние отступы контейнера и по блочной оси.

<ExampleFrame variant="component">
  <BleedBlockBleed />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/bleed/block-bleed.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={bleedSolidBlockBleed} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/bleed/bleed-block-bleed.module.css"
```
</div>

</ExampleCode>

### Настройка для вложенного контейнера

Локально переопределите `--moduix-bleed-inline-full` и `--moduix-bleed-inline-full-size`, если вложенная оболочка
должна растянуть `Bleed` до границ своей области, а не до краёв окна браузера.

<ExampleFrame variant="component">
  <BleedCustomStyling />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/bleed/custom-styling.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={bleedSolidCustomStyling} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/bleed/bleed-custom-styling.module.css"
```
</div>

</ExampleCode>

### Значения по строчной оси

Задайте `inline`, чтобы управлять выходом содержимого за пределы контейнера по строчной оси. В примере сравниваются
фиксированные значения `sm` и `lg` с выходом `full` до краёв окна браузера.

<ExampleFrame variant="component">
  <BleedInlineAmounts />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/bleed/inline-amounts.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={bleedSolidInlineAmounts} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/bleed/bleed-inline-amounts.module.css"
```
</div>

</ExampleCode>

### Семантический элемент

Используйте `asChild`, если содержимому нужен более подходящий семантический элемент. В этом примере элемент
`figure` становится корневым элементом `Bleed`.

<ExampleFrame variant="component">
  <BleedSemanticElement />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/bleed/semantic-element.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={bleedSolidSemanticElement} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/bleed/bleed-semantic-element.module.css"
```
</div>

</ExampleCode>

## Стилизация

`Bleed` предоставляет CSS-переменные и стабильные data-атрибуты на корневом элементе. Для структурных стилей
используйте `className` в React или `class` в Solid. Варианты Tailwind используют обычные utility-классы и классы
приложения, а переменные ниже настраивают варианты с CSS Modules. Data-атрибуты и `data-slot` общие для обоих
способов стилизации. Предки с `overflow: hidden` или `overflow: clip` могут обрезать выход за границы.

### CSS-свойства

<CssPropertiesSection properties={BleedDocs.bleedCssProperties} />

### Хуки стилизации

- `data-scope="bleed"` и `data-part="root"` служат хуками стилизации, согласованными с Ark, на экспортируемом корневом элементе.
- `data-slot="bleed-root"` - стабильный селектор moduix на экспортируемом корневом элементе.
- `data-inline` показывает текущий режим выхода по строчной оси: `none`, `xs`, `sm`, `md`, `lg`, `xl` или `full`.
- `data-block` показывает текущий режим выхода по блочной оси: `none`, `xs`, `sm`, `md`, `lg` или `xl`.
- Публичный CSS API `--moduix-bleed-*` включает значения фиксированной шкалы и расчёт ширины до краёв окна
  браузера, который используется при `inline="full"`.
