---
title: Button
component: button
description: Стилизованный элемент управления для действий на основе модели композиции Ark factory.
---

import * as ButtonDocs from '@/components/examples/button';

## Справочник API

В Ark UI нет отдельного примитива `button`. moduix создаёт этот компонент с помощью официального
[Ark factory](https://ark-ui.com/docs/guides/composition#the-ark-factory) и следует рекомендациям Ark по
[композиции](https://ark-ui.com/docs/guides/composition), [стилизации](https://ark-ui.com/docs/guides/styling) и
[работе с ref](https://ark-ui.com/docs/guides/ref).

## Базовый пример

Для повседневных действий используйте короткую запись `<Button />`. `Button.Root` - эквивалентная запись через пространство имён.

```tsx preview file="../../en/docs/_snippets/button/basic.tsx"

```

## Установка через shadcn

<ShadcnInstall packageName="button" />

## Анатомия

`Button` состоит из одной части.

```text
Button.Root
└─ root[data-scope="button"][data-part="root"][data-slot="button-root"]
   └─ children
```

| Часть                    | Роль                                                   |
| ------------------------ | ------------------------------------------------------ |
| `Button` / `Button.Root` | Интерактивный корневой элемент со стилями moduix.      |
| `Button`                 | Вызываемый алиас `Button.Root`.                        |

## Композиция

### Рекомендуемая композиция

Для действий используйте нативный корневой элемент. По умолчанию он получает `type="button"`, чтобы форма случайно не отправилась. Для отправки формы явно задайте `type="submit"`.

### Свойства по умолчанию

| Свойство  | По умолчанию | Значения                                                                             |
| --------- | ------------ | ------------------------------------------------------------------------------------ |
| `loading` | `false`      | `boolean`                                                                            |
| `variant` | `default`    | `default`, `outline`, `secondary`, `destructive`, `destructive-outline`, `ghost`, `link` |
| `size`    | `md`         | `xs`, `sm`, `md`, `lg`, `xl`, `icon-sm`, `icon-md`, `icon-lg`                       |
| `type`    | `button`     | Только нативные корневые элементы                                                    |

### Когда нужен собственный элемент

Используйте `asChild`, если один семантически подходящий дочерний элемент должен отвечать за DOM-узел. При такой композиции Ark требует ровно одного дочернего элемента. Значение `type="button"` по умолчанию не добавляется к элементу, переданному через `asChild`.

Если `Dialog.Trigger`, `Menu.Trigger` или другая часть Ark использует Button как дочерний элемент, значения `data-scope` и `data-part` родительской части имеют приоритет на общем DOM-узле. Button сохраняет классы, заданные рецептом, и стабильный `data-slot`, а Ark продолжает объединять обработчики триггера и ref.

Для навигации используйте настоящую ссылку. На элементе, переданном через `asChild`, свойства `disabled`, `loading` и `aria-disabled="true"` устанавливают `aria-disabled`, включают стили отключённого состояния и блокируют активацию по клику. Нативный атрибут `disabled` на такой элемент не добавляется.

### Особенности поведения

- Нативные атрибуты, `className` и `style` передаются без изменений. Клики по отключённым корневым элементам блокируются.
- Переданный ref указывает на отрисованный корневой элемент.
- Свойства `disabled`, `aria-disabled="true"` и `loading` устанавливают `data-disabled`.
- `loading` - это короткая запись для настройки состояния: она устанавливает `data-loading`, `aria-busy` и стили отключённого состояния, а спиннер и метка остаются полностью компонуемыми.
- Прямые дочерние элементы `svg` наследуют `currentColor` и используют `--moduix-button-icon-size`.
- Кнопки только со значком должны иметь доступное имя.
- Свойства Chakra `colorPalette`, адаптивные свойства рецепта, `ButtonGroup` и управляемые свойства загрузки намеренно не входят в контракт moduix для компонента с одним корневым элементом.

## Примеры

### Ссылка с `asChild`

Используйте `asChild` для навигации, чтобы элемент ссылки сохранил семантику ссылки и получил стили кнопки.

```tsx preview file="../../en/docs/_snippets/button/as-child-link.tsx"

```

### Отключённая кнопка

Используйте `disabled` для нативных кнопок и элементов, переданных через `asChild`.

```tsx preview file="../../en/docs/_snippets/button/disabled.tsx"

```

### Иконки

Иконки могут стоять до или после текста. Их порядок в JSX и значение `gap` у корневого элемента Button определяют расположение элементов.

```tsx preview file="../../en/docs/_snippets/button/icons.tsx"

```

### Состояние загрузки

Используйте `loading`, чтобы настроить состояние загрузки, а метку, спиннер и асинхронный жизненный цикл явно задайте в коде приложения. Для элементов, переданных через `asChild`, этот проп также блокирует активацию по клику.

```tsx preview file="../../en/docs/_snippets/button/pending-state.tsx"

```

### Ref

Переданный ref указывает на отрисованный корневой элемент. С его помощью можно программно перевести элемент в фокус или измерить его.

```tsx preview file="../../en/docs/_snippets/button/ref.tsx"

```

### Размеры

Размеры текстовых кнопок и кнопок только со значком задаются через один и тот же API корневого элемента.

```tsx preview file="../../en/docs/_snippets/button/sizes.tsx"

```

### Варианты

Используйте варианты, чтобы показать приоритет и назначение действия.

```tsx preview file="../../en/docs/_snippets/button/variants.tsx"

```

## Стилизация

### CSS-свойства

<CssPropertiesSection properties={ButtonDocs.buttonCssProperties} />

### Селекторы для стилизации

- `data-slot="button-root"` - стабильный селектор moduix.
- У самостоятельного корневого элемента по умолчанию используются `data-scope="button"` и `data-part="root"`. Родитель Ark, который компонует Button, может заменить их значениями своей анатомии.
- `data-variant` и `data-size` показывают активные вариант и размер.
- `data-disabled` присутствует для нативных `disabled`, `aria-disabled="true"` и `loading`.
- `data-loading` присутствует, когда `loading={true}`.
- `[data-disabled]` управляет стилями отключённого состояния для нативных `disabled`, `aria-disabled="true"` и `loading`.
- В активных вариантах кнопки используется лёгкое смещение при нажатии. Для `link` меняется только прозрачность, а если пользователь предпочитает уменьшить движение, смещение отключается.
