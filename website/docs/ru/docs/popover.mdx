---
title: Popover
component: popover
description: Доступное всплывающее окно для компактного интерактивного содержимого, привязанного к триггеру или другому элементу.
---

import * as PopoverDocs from '@/components/examples/popover/css-properties';
import PopoverAdvancedCustomization from '@/components/examples/popover/advanced-customization.tsx';
import PopoverAnchor from '@/components/examples/popover/anchor.tsx';
import PopoverBasic from '@/components/examples/popover/basic.tsx';
import PopoverCloseBehavior from '@/components/examples/popover/close-behavior.tsx';
import PopoverContext from '@/components/examples/popover/context.tsx';
import PopoverControlled from '@/components/examples/popover/controlled.tsx';
import PopoverLazyMount from '@/components/examples/popover/lazy-mount.tsx';
import PopoverModal from '@/components/examples/popover/modal.tsx';
import PopoverMultipleTriggers from '@/components/examples/popover/multiple-triggers.tsx';
import PopoverNested from '@/components/examples/popover/nested.tsx';
import PopoverPlacement from '@/components/examples/popover/placement.tsx';
import PopoverRootProvider from '@/components/examples/popover/root-provider.tsx';
import PopoverSameWidth from '@/components/examples/popover/same-width.tsx';
import PopoverWithArrow from '@/components/examples/popover/with-arrow.tsx';
import popoverSolidAdvancedCustomization from '../../../snippets/popover/solid/advanced-customization.tsx?raw';
import popoverSolidAnchor from '../../../snippets/popover/solid/anchor.tsx?raw';
import popoverSolidBasic from '../../../snippets/popover/solid/basic.tsx?raw';
import popoverSolidCloseBehavior from '../../../snippets/popover/solid/close-behavior.tsx?raw';
import popoverSolidContext from '../../../snippets/popover/solid/context.tsx?raw';
import popoverSolidControlled from '../../../snippets/popover/solid/controlled.tsx?raw';
import popoverSolidLazyMount from '../../../snippets/popover/solid/lazy-mount.tsx?raw';
import popoverSolidModal from '../../../snippets/popover/solid/modal.tsx?raw';
import popoverSolidMultipleTriggers from '../../../snippets/popover/solid/multiple-triggers.tsx?raw';
import popoverSolidNested from '../../../snippets/popover/solid/nested.tsx?raw';
import popoverSolidPlacement from '../../../snippets/popover/solid/placement.tsx?raw';
import popoverSolidRootProvider from '../../../snippets/popover/solid/root-provider.tsx?raw';
import popoverSolidSameWidth from '../../../snippets/popover/solid/same-width.tsx?raw';
import popoverSolidWithArrow from '../../../snippets/popover/solid/with-arrow.tsx?raw';

## Справочник API

<PrimitiveReference href="https://ark-ui.com/docs/components/popover" />

## Какой компонент выбрать

Используйте `Popover` для компактного интерактивного содержимого, которое открывается по триггеру.
Для коротких неинтерактивных подсказок подходит `Tooltip`, а для крупных сценариев, требующих
полного внимания пользователя, используйте `Dialog`.

## Базовый пример


Используйте части, показанные в примере, явно. Настройте позиционирование на корневом компоненте,
а `asChild` используйте, если триггером должна быть ваша кнопка.

Используйте `Popover` как корневой компонент, а остальные части импортируйте напрямую, например
`PopoverTrigger` и `PopoverContent`. Эти плоские имена одинаковы в npm-пакетах и исходниках
shadcn-реестра.

<ExampleFrame variant="component">
  <PopoverBasic />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
<Tabs groupId="framework" values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]} className="docs-tabs">
<Tab value="react">

```tsx pure file="../../../src/components/examples/popover/basic.tsx"
```

</Tab>
<Tab value="solid">
<CodeBlockRuntime lang="tsx" code={popoverSolidBasic} />
</Tab>
</Tabs>
</div>

</ExampleCode>

## Установка через shadcn

Полную настройку пакета, реестра и стилей см. в разделе [Быстрый старт](/docs/quick-start).

<Tabs groupId="framework">
  <Tab label="React" value="react">
<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-react/popover' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-react-tailwind/popover' },
  ]}
/>
  </Tab>
  <Tab label="Solid" value="solid">
<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-solid/popover' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-solid-tailwind/popover' },
  ]}
/>
  </Tab>
</Tabs>

## Анатомия

```text
Popover
├─ PopoverAnchor (необязательно)
├─ PopoverTrigger
│  └─ PopoverIndicator (необязательно)
└─ Поддерево оверлея (автоматически переносится в портал)
   └─ PopoverPositioner
      └─ PopoverContent
         ├─ PopoverArrow
         │  └─ PopoverArrowTip
         ├─ PopoverHeader
         │  ├─ PopoverTitle
         │  └─ PopoverDescription
         ├─ PopoverBody
         ├─ PopoverFooter
         └─ PopoverCloseTrigger / PopoverCloseIcon

PopoverRootProvider
└─ то же дерево частей, связанное с состоянием moduix из usePopover()

PopoverContext
└─ render prop с текущим состоянием всплывающего окна
```

| Часть                         | Назначение                                                          |
| ----------------------------- | ------------------------------------------------------------------- |
| `Popover`    | Управляет состоянием, открытием и закрытием, фокусом, закрытием при внешнем взаимодействии и позиционированием. |
| `PopoverRootProvider`        | Подключает части к состоянию, созданному через `usePopover()`.      |
| `PopoverContext`             | Передаёт потомкам текущее состояние через render prop.              |
| `PopoverAnchor`              | Необязательный элемент, относительно которого выполняется позиционирование. |
| `PopoverTrigger`             | Кнопка, открывающая и закрывающая окно; для триггера можно задать `value`. |
| `PopoverIndicator`           | Необязательный индикатор состояния триггера.                        |
| `PopoverPositioner`          | Плавающий слой с CSS-переменными Ark для позиционирования.          |
| `PopoverContent`             | Видимая интерактивная область всплывающего окна.                    |
| `PopoverArrow` / `PopoverArrowTip`  | Необязательная позиционируемая стрелка и её видимый наконечник.     |
| `PopoverTitle`               | Доступный заголовок, связанный с содержимым.                        |
| `PopoverDescription`         | Доступное описание, связанное с содержимым.                         |
| `PopoverCloseTrigger`        | Низкоуровневый триггер, закрывающий окно.                            |
| `PopoverCloseIcon`            | Вспомогательная часть moduix, объединяющая триггер закрытия Ark и кнопку с иконкой. |
| `PopoverHeader` / `PopoverBody`      | Вспомогательные обёртки moduix со стабильными слотами.              |
| `PopoverFooter`               | Вспомогательная обёртка moduix для строки действий.                 |

В коде приложения используйте `Popover` как корневой компонент. Остальные части импортируйте
напрямую из пакета, чтобы композиция оставалась явной и понятной.

## Композиция

Оставляйте `PopoverPositioner` и `PopoverContent` явными, чтобы структуру оверлея было удобно
стилизовать. `PopoverPositioner` по умолчанию размещается в портале. Установите `portalled={false}` для
размещения в текущем DOM-дереве или передайте `portalRef` для собственного контейнера портала. При
использовании `PopoverRootProvider` настройте `portalled` в `usePopover()` и передайте store
провайдеру. Один и тот же store нельзя одновременно подключать через `Popover` и
`PopoverRootProvider`.

Используйте `PopoverAnchor`, когда позиционирование должен определять другой элемент. Применяйте `asChild`
с одним семантически подходящим дочерним элементом, если ваша разметка должна отрисовать триггер,
якорь или триггер закрытия. Для вложенного Popover установите `portalled={false}` на корневой части,
чтобы он оставался внутри родительского оверлея. Если несколько триггеров используют одно окно,
задайте каждому `value` и обновляйте содержимое через `onTriggerValueChange(details)`.

## Примеры

### Якорный элемент

Используйте `PopoverAnchor`, когда другой элемент должен задавать точку отсчёта для позиционирования.

<ExampleFrame variant="component">
  <PopoverAnchor />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
<Tabs groupId="framework" values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]} className="docs-tabs">
<Tab value="react">

```tsx pure file="../../../src/components/examples/popover/anchor.tsx"
```

</Tab>
<Tab value="solid"><CodeBlockRuntime lang="tsx" code={popoverSolidAnchor} /></Tab>
</Tabs>
**Стили**
```css file="../../../src/components/examples/popover/popover-anchor.module.css"
```
</div>

</ExampleCode>

### Поведение закрытия

Отключайте закрытие при взаимодействии за пределами окна или по Escape, только если в сценарии
есть явное действие для закрытия.

<ExampleFrame variant="component"><PopoverCloseBehavior /></ExampleFrame>

<ExampleCode>
<div className="docs-reference">
<Tabs groupId="framework" values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]} className="docs-tabs">
<Tab value="react">

```tsx pure file="../../../src/components/examples/popover/close-behavior.tsx"
```

</Tab>
<Tab value="solid"><CodeBlockRuntime lang="tsx" code={popoverSolidCloseBehavior} /></Tab>
</Tabs>
</div>

</ExampleCode>

### Контекст

Используйте `usePopoverContext()` внутри потомка, которому нужно текущее состояние окна.
`PopoverContext` подойдёт, когда для композиции удобнее render prop.

<ExampleFrame variant="component"><PopoverContext /></ExampleFrame>

<ExampleCode>
<div className="docs-reference">
<Tabs groupId="framework" values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]} className="docs-tabs">
<Tab value="react">

```tsx pure file="../../../src/components/examples/popover/context.tsx"
```

</Tab>
<Tab value="solid"><CodeBlockRuntime lang="tsx" code={popoverSolidContext} /></Tab>
</Tabs>
</div>

</ExampleCode>

### Управляемое состояние

Храните `open` в состоянии фреймворка и читайте `details.open` из `onOpenChange`, чтобы
синхронизировать окно с остальным интерфейсом.

<ExampleFrame variant="component"><PopoverControlled /></ExampleFrame>

<ExampleCode>
<div className="docs-reference">
<Tabs groupId="framework" values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]} className="docs-tabs">
<Tab value="react">

```tsx pure file="../../../src/components/examples/popover/controlled.tsx"
```

</Tab>
<Tab value="solid"><CodeBlockRuntime lang="tsx" code={popoverSolidControlled} /></Tab>
</Tabs>
**Стили**
```css file="../../../src/components/examples/popover/popover-controlled.module.css"
```
</div>

</ExampleCode>

### Отложенное монтирование

Используйте `lazyMount` и `unmountOnExit`, когда содержимое должно находиться в DOM только во
время работы с окном.

<ExampleFrame variant="component"><PopoverLazyMount /></ExampleFrame>

<ExampleCode>
<div className="docs-reference">
<Tabs groupId="framework" values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]} className="docs-tabs">
<Tab value="react">

```tsx pure file="../../../src/components/examples/popover/lazy-mount.tsx"
```

</Tab>
<Tab value="solid"><CodeBlockRuntime lang="tsx" code={popoverSolidLazyMount} /></Tab>
</Tabs>
</div>

</ExampleCode>

### Модальный режим

Включайте модальный режим, когда пользователь должен оставаться внутри окна. Он удерживает фокус
и скрывает внешнее содержимое от вспомогательных технологий. Используйте `initialFocusEl`, чтобы
выбрать первый элемент для фокуса.

<ExampleFrame variant="component"><PopoverModal /></ExampleFrame>

<ExampleCode>
<div className="docs-reference">
<Tabs groupId="framework" values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]} className="docs-tabs">
<Tab value="react">

```tsx pure file="../../../src/components/examples/popover/modal.tsx"
```

</Tab>
<Tab value="solid"><CodeBlockRuntime lang="tsx" code={popoverSolidModal} /></Tab>
</Tabs>
**Стили**
```css file="../../../src/components/examples/popover/popover-modal.module.css"
```
</div>

</ExampleCode>

### Несколько триггеров

Разделяйте одну область содержимого между несколькими триггерами: задайте каждому `value` и
реагируйте на активное значение.

<ExampleFrame variant="component"><PopoverMultipleTriggers /></ExampleFrame>

<ExampleCode>
<div className="docs-reference">
<Tabs groupId="framework" values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]} className="docs-tabs">
<Tab value="react">

```tsx pure file="../../../src/components/examples/popover/multiple-triggers.tsx"
```

</Tab>
<Tab value="solid"><CodeBlockRuntime lang="tsx" code={popoverSolidMultipleTriggers} /></Tab>
</Tabs>
**Стили**
```css file="../../../src/components/examples/popover/popover-multiple-triggers.module.css"
```
</div>

</ExampleCode>

### Вложенный Popover

Вложенные Popover имеют независимое состояние. Отрисовывайте вложенный Popover с
`portalled={false}`, чтобы он оставался внутри родительского оверлея и участвовал в его слоях.

<ExampleFrame variant="component"><PopoverNested /></ExampleFrame>

<ExampleCode>
<div className="docs-reference">
<Tabs groupId="framework" values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]} className="docs-tabs">
<Tab value="react">

```tsx pure file="../../../src/components/examples/popover/nested.tsx"
```

</Tab>
<Tab value="solid"><CodeBlockRuntime lang="tsx" code={popoverSolidNested} /></Tab>
</Tabs>
**Стили**
```css file="../../../src/components/examples/popover/popover-nested.module.css"
```
</div>

</ExampleCode>

### Позиционирование

Передавайте параметры позиционирования Ark через свойство `positioning` корневого компонента.

<ExampleFrame variant="component"><PopoverPlacement /></ExampleFrame>

<ExampleCode>
<div className="docs-reference">
<Tabs groupId="framework" values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]} className="docs-tabs">
<Tab value="react">

```tsx pure file="../../../src/components/examples/popover/placement.tsx"
```

</Tab>
<Tab value="solid"><CodeBlockRuntime lang="tsx" code={popoverSolidPlacement} /></Tab>
</Tabs>
</div>

</ExampleCode>

### Root Provider

Используйте `usePopover()` вместе с `PopoverRootProvider`, когда внешние по отношению к дереву
Popover элементы должны читать или изменять его состояние.

<ExampleFrame variant="component"><PopoverRootProvider /></ExampleFrame>

<ExampleCode>
<div className="docs-reference">
<Tabs groupId="framework" values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]} className="docs-tabs">
<Tab value="react">

```tsx pure file="../../../src/components/examples/popover/root-provider.tsx"
```

</Tab>
<Tab value="solid"><CodeBlockRuntime lang="tsx" code={popoverSolidRootProvider} /></Tab>
</Tabs>
**Стили**
```css file="../../../src/components/examples/popover/popover-root-provider.module.css"
```
</div>

</ExampleCode>

### Одинаковая ширина

Установите `positioning.sameWidth`, если содержимое должно совпадать по ширине с триггером.

<ExampleFrame variant="component"><PopoverSameWidth /></ExampleFrame>

<ExampleCode>
<div className="docs-reference">
<Tabs groupId="framework" values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]} className="docs-tabs">
<Tab value="react">

```tsx pure file="../../../src/components/examples/popover/same-width.tsx"
```

</Tab>
<Tab value="solid"><CodeBlockRuntime lang="tsx" code={popoverSolidSameWidth} /></Tab>
</Tabs>
**Стили**
```css file="../../../src/components/examples/popover/popover-same-width.module.css"
```
</div>

</ExampleCode>

### Со стрелкой

Добавьте `PopoverArrow`, если окно должно визуально указывать на элемент, относительно которого
оно позиционируется. Обёртка по умолчанию выводит стилизованный moduix `PopoverArrowTip`.

<ExampleFrame variant="component"><PopoverWithArrow /></ExampleFrame>

<ExampleCode>
<div className="docs-reference">
<Tabs groupId="framework" values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]} className="docs-tabs">
<Tab value="react">

```tsx pure file="../../../src/components/examples/popover/with-arrow.tsx"
```

</Tab>
<Tab value="solid"><CodeBlockRuntime lang="tsx" code={popoverSolidWithArrow} /></Tab>
</Tabs>
</div>

</ExampleCode>

### Расширенная настройка

Составляйте части Ark напрямую, когда окну нужен компактный пользовательский заголовок или другая
разметка, для которой не подходят вспомогательные части moduix. Стрелка необязательна, поэтому её
можно не добавлять, если панели не нужен визуальный указатель.

<ExampleFrame variant="component"><PopoverAdvancedCustomization /></ExampleFrame>

<ExampleCode>
<div className="docs-reference">
<Tabs groupId="framework" values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]} className="docs-tabs">
<Tab value="react">

```tsx pure file="../../../src/components/examples/popover/advanced-customization.tsx"
```

</Tab>
<Tab value="solid"><CodeBlockRuntime lang="tsx" code={popoverSolidAdvancedCustomization} /></Tab>
</Tabs>
**Стили**
```css file="../../../src/components/examples/popover/popover-advanced-customization.module.css"
```
</div>

</ExampleCode>

## Стилизация

Для структурных стилей используйте `className` в React и `class` в Solid на экспортируемых частях.
Варианты Tailwind используют нативные utility-классы и классы потребителя. CSS-переменные ниже
настраивают варианты CSS Modules, а атрибуты Ark и `data-slot` moduix общие для обоих вариантов.

### CSS Properties

Используйте публичные переменные ниже, чтобы настроить Popover без замены его структуры.

<div className="docs-reference">
  <Tabs values={[{ label: 'CSS Variables', value: 'CSS Variables' }]} className="docs-reference-tabs" contentItemClassName="docs-reference-panel">
    <Tab value="CSS Variables">
      <div><PopoverDocs.PopoverCssPropertiesPanel /></div>
    </Tab>
  </Tabs>
</div>

### Точки стилизации

Каждая отрисованная часть принимает `className` или `class`. Части Ark сохраняют `data-scope="popover"`
и значения `data-part`, а moduix добавляет к обёрткам стабильные `data-slot="popover-*"`. `Popover`
и `PopoverRootProvider` предоставляют состояние, но не создают DOM-элементы. Для стилей, зависящих
от состояния, используйте `data-state="open|closed"`, `data-placement`, `data-side`, `data-value`,
`data-nested`, `data-has-nested`, `data-expanded` и `data-current` для нескольких триггеров с `value`.

`PopoverPositioner` предоставляет `--reference-width`, `--reference-height`, `--available-width`,
`--available-height`, `--x`, `--y`, `--z-index` и `--transform-origin`. `PopoverContent` предоставляет
`--layer-index` и `--nested-layer-count`, а `PopoverArrow` предоставляет переменные измерения стрелки Ark.
Содержимое переносит длинные строки без пробелов и отключает анимацию, если пользователь предпочитает
уменьшить движение.
