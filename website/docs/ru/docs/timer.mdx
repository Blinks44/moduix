---
title: Timer
component: timer
description: Компонент для отсчёта прошедшего или оставшегося времени с явными элементами управления в React и Solid.
---

import * as TimerDocs from '@/components/examples/timer/css-properties';
import TimerBasic from '@/components/examples/timer/basic.tsx';
import TimerCountdown from '@/components/examples/timer/countdown.tsx';
import TimerEvents from '@/components/examples/timer/events.tsx';
import TimerInterval from '@/components/examples/timer/interval.tsx';
import TimerPomodoro from '@/components/examples/timer/pomodoro.tsx';
import TimerRootProvider from '@/components/examples/timer/root-provider.tsx';
import timerSolidBasic from '../../../snippets/timer/solid/basic.tsx?raw';
import timerSolidCountdown from '../../../snippets/timer/solid/countdown.tsx?raw';
import timerSolidEvents from '../../../snippets/timer/solid/events.tsx?raw';
import timerSolidInterval from '../../../snippets/timer/solid/interval.tsx?raw';
import timerSolidPomodoro from '../../../snippets/timer/solid/pomodoro.tsx?raw';
import timerSolidRootProvider from '../../../snippets/timer/solid/root-provider.tsx?raw';

## Справочник API

<PrimitiveReference href="https://ark-ui.com/docs/components/timer" />

## Базовый пример

Timer доступен для React и Solid в пакетах CSS Modules и Tailwind. Превью на этой странице использует пакет React
CSS Modules, а вкладки фреймворков показывают нативный исходный код с тем же публичным контрактом.

`Timer.Segments` проще всего использовать для классического отображения времени с разделителями `:`. По умолчанию
компонент показывает часы, минуты и секунды. Передайте `types`, чтобы выбрать другой набор единиц. Если нужны
подписи единиц, собственные разделители или отдельная стилизация каждой части, используйте полную композицию с
`Timer.Area`, `Timer.Item` и `Timer.Separator`.

`Timer.Segments` поддерживает все свойства `Timer.Area`, кроме `children` и `asChild`, и пробрасывает ref области.
Компонент сам создаёт элементы и разделители, поэтому для замены корневого элемента или собственных дочерних
элементов используйте `Timer.Area`.

<ExampleFrame variant="component">
  <TimerBasic />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/timer/basic.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={timerSolidBasic} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

## Установка через shadcn

<Tabs groupId="framework">
  <Tab label="React" value="react">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-react/timer' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-react-tailwind/timer' },
  ]}
/>

  </Tab>
  <Tab label="Solid" value="solid">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-solid/timer' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-solid-tailwind/timer' },
  ]}
/>

  </Tab>
</Tabs>

## Анатомия

```text
Timer.Root
├─ Timer.Area
│  ├─ Timer.Item
│  └─ Timer.Separator
└─ Timer.Control
   └─ Timer.ActionTrigger

Timer.RootProvider
└─ то же дерево, подключённое к состоянию из `useTimer()`

Timer.Segments
└─ Timer.Area со стандартными элементами Timer.Item и Timer.Separator
```

| Элемент                 | Назначение                                                               |
| ----------------------- | ------------------------------------------------------------------------ |
| `Timer` / `Timer.Root`  | Управляет состоянием таймера Ark, его ID, интервалом и обратными вызовами. |
| `Timer.RootProvider`    | Подключает элементы к состоянию, созданному с помощью `useTimer()`.      |
| `Timer.Segments`        | Собирает стандартные единицы времени и разделители.                      |
| `Timer.Area`            | Группирует отображаемые значения времени.                                |
| `Timer.Item`            | Отображает одну отформатированную единицу времени, например `hours`, `minutes` или `seconds`. |
| `Timer.Separator`       | Отображает разделитель между единицами времени.                          |
| `Timer.Control`         | Группирует триггеры действий.                                             |
| `Timer.ActionTrigger`   | Запускает действие `start`, `pause`, `resume`, `reset` или `restart`.    |

## Композиция

`Timer` эквивалентен `Timer.Root` и управляет состоянием таймера. `Timer.Segments` сам создаёт части `Timer.Area`,
`Timer.Item` и `Timer.Separator`, поэтому для подписей единиц, собственных разделителей или отдельной разметки
используйте полную композицию с `Timer.Area`.

Используйте `Timer.RootProvider` с `useTimer()`, когда состояние нужно создать отдельно от отображаемого дерева.
`Timer.Context` и `useTimerContext()` подходят, если другим элементам интерфейса нужны состояние или методы таймера.
В Solid состояние и контекст читаются через реактивные функции, поэтому обращайтесь к ним как к `timer()` и
`api()`.

## Примеры

### Обратный отсчёт

Задайте `countdown` и `startMs`, если отображение должно идти обратным отсчётом до нуля.

<ExampleFrame variant="component">
  <TimerCountdown />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/timer/countdown.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={timerSolidCountdown} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/timer/timer-countdown.module.css"
```
</div>

</ExampleCode>

### События

Используйте `onTick` и `onComplete` для побочных эффектов, связанных с жизненным циклом таймера.

<ExampleFrame variant="component">
  <TimerEvents />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/timer/events.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={timerSolidEvents} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/timer/timer-events.module.css"
```
</div>

</ExampleCode>

### Интервал

Уменьшите `interval` и добавьте элемент `milliseconds`, если UI должен обновляться несколько раз в секунду.

<ExampleFrame variant="component">
  <TimerInterval />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/timer/interval.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={timerSolidInterval} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/timer/timer-interval.module.css"
```
</div>

</ExampleCode>

### Помодоро

Используйте `onComplete`, чтобы переключать режимы работы и отдыха и запускать следующий цикл таймера.

<ExampleFrame variant="component">
  <TimerPomodoro />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/timer/pomodoro.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={timerSolidPomodoro} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/timer/timer-pomodoro.module.css"
```
</div>

</ExampleCode>

### Расширенная настройка

Используйте `useTimer()`, если состояние и методы таймера должны быть доступны за пределами отображаемого дерева
композиции. Это также подход для подписей единиц и другой пользовательской композиции элементов.

<ExampleFrame variant="component">
  <TimerRootProvider />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/timer/root-provider.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={timerSolidRootProvider} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/timer/timer-root-provider.module.css"
```
</div>

</ExampleCode>

## Стилизация

Для изменения структуры используйте `className` на экспортируемых элементах, а CSS-переменные, чтобы настроить
визуальные значения по умолчанию. Атрибуты Ark также можно использовать для стилизации отдельных элементов.

### CSS-свойства

<div className="docs-reference">
  <Tabs
    values={[{ label: 'CSS Variables', value: 'CSS Variables' }]}
    className="docs-reference-tabs"
    contentItemClassName="docs-reference-panel"
  >
    <Tab>
      <TimerDocs.TimerCssPropertiesPanel />
    </Tab>
  </Tabs>
</div>

### Селекторы и атрибуты стиля

- Селекторы `data-slot="timer-root"`, `timer-root-provider`, `timer-area`, `timer-item`,
  `timer-separator`, `timer-control` и `timer-action-trigger`.
- Атрибуты анатомии Ark: `data-scope="timer"` и
  `data-part="root|area|item|separator|control|action-trigger"`.
- `Timer.Item` предоставляет `data-type` и переменную CSS `--value`, которую обновляет Ark.
- Ark добавляет атрибут `hidden` к триггерам действий, недоступным в текущем состоянии таймера.
