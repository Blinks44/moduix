---
title: Skeleton
component: skeleton
description: Заполнитель для текста, карточек, медиа и разделов страницы во время загрузки в React и Solid.
---

import * as SkeletonDocs from '@/components/examples/skeleton/css-properties';
import SkeletonBasic from '@/components/examples/skeleton/basic.tsx';
import SkeletonCard from '@/components/examples/skeleton/card.tsx';
import SkeletonCustomHost from '@/components/examples/skeleton/custom-host.tsx';
import SkeletonLoadedContent from '@/components/examples/skeleton/loaded-content.tsx';
import SkeletonMediaObject from '@/components/examples/skeleton/media-object.tsx';
import SkeletonResponsiveLayout from '@/components/examples/skeleton/responsive-layout.tsx';
import SkeletonVariants from '@/components/examples/skeleton/variants.tsx';
import skeletonSolidBasic from '../../../snippets/skeleton/solid/basic.tsx?raw';
import skeletonSolidCard from '../../../snippets/skeleton/solid/card.tsx?raw';
import skeletonSolidCustomHost from '../../../snippets/skeleton/solid/custom-host.tsx?raw';
import skeletonSolidLoadedContent from '../../../snippets/skeleton/solid/loaded-content.tsx?raw';
import skeletonSolidMediaObject from '../../../snippets/skeleton/solid/media-object.tsx?raw';
import skeletonSolidResponsiveLayout from '../../../snippets/skeleton/solid/responsive-layout.tsx?raw';
import skeletonSolidVariants from '../../../snippets/skeleton/solid/variants.tsx?raw';

## Справочник API

В Ark UI нет отдельного примитива `Skeleton`. moduix создаёт `Skeleton` на основе официальной
[фабрики Ark](https://ark-ui.com/docs/guides/composition#the-ark-factory) и следует рекомендациям Ark по
[композиции](https://ark-ui.com/docs/guides/composition), [стилизации](https://ark-ui.com/docs/guides/styling) и
[работе с ref](https://ark-ui.com/docs/guides/ref).

## Базовый пример

`Skeleton` доступен для React и Solid в пакетах с CSS Modules и Tailwind. Предпросмотр на этой странице
использует пакет React с CSS Modules, а вкладки фреймворков показывают нативный исходный код с тем же публичным
контрактом.

Используйте `Skeleton` для строк текста, карточек, аватаров и медиа-заполнителей. Окружающая разметка отвечает
за отступы и адаптивную компоновку. Инструкции по установке пакетов, настройке реестра и выбору способа
стилизации находятся в разделе [Быстрый старт](/docs/quick-start).

<ExampleFrame variant="component">
  <SkeletonBasic />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/skeleton/basic.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={skeletonSolidBasic} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/skeleton/skeleton-basic.module.css"
```
</div>

</ExampleCode>

## Установка через shadcn

Полную настройку пакета, реестра и стилей см. в разделе [Быстрый старт](/docs/quick-start).

<Tabs groupId="framework">
  <Tab label="React" value="react">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-react/skeleton' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-react-tailwind/skeleton' },
  ]}
/>

  </Tab>
  <Tab label="Solid" value="solid">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-solid/skeleton' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-solid-tailwind/skeleton' },
  ]}
/>

  </Tab>
</Tabs>

## Анатомия

`Skeleton` состоит из одной визуальной части. Компоновку задаёт окружающая разметка.

```text
Skeleton.Root
└─ children (необязательно, отображаются при loading={false})
```

| Часть                        | Назначение                                                                                  |
| ---------------------------- | ------------------------------------------------------------------------------------------- |
| `Skeleton` / `Skeleton.Root` | Блок-заполнитель. По умолчанию рендерит элемент фабрики Ark; с `asChild` корневым можно сделать другой семантический элемент. |

## Композиция

`Skeleton` не управляет состоянием загрузки. Область данных или страницы, которая владеет запросом, передаёт
`loading` и должна получать `aria-busy`; сам заполнитель служит только для оформления. Установите
`loading={false}`, чтобы показать дочерние элементы в том же корневом элементе.

`Skeleton` состоит из одной визуальной части, поэтому карточки, медиаобъекты и адаптивные раскладки следует
собирать в собственной обёртке. В пакетах с CSS Modules можно использовать `Stack`, а в пакетах Tailwind -
нативные классы-утилиты flex и grid. Используйте `asChild`, только если нужен другой семантический корневой
элемент; в Solid в `asChild` передаётся функция рендера.

## Примеры

### Карточка

Используйте крупный блок и более короткие строки текста, чтобы сохранить компоновку карточки во время загрузки.

<ExampleFrame variant="component">
  <SkeletonCard />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/skeleton/card.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={skeletonSolidCard} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/skeleton/skeleton-card.module.css"
```
</div>

</ExampleCode>

### Загруженное содержимое

Используйте `loading`, чтобы сохранить один и тот же корневой элемент при замене заполнителя реальным содержимым.

<ExampleFrame variant="component">
  <SkeletonLoadedContent />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/skeleton/loaded-content.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={skeletonSolidLoadedContent} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/skeleton/skeleton-loaded-content.module.css"
```
</div>

</ExampleCode>

### Медиаобъект

Используйте `boxSize` с полным скруглением для заполнителя аватара, а строки текста разместите рядом.

<ExampleFrame variant="component">
  <SkeletonMediaObject />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/skeleton/media-object.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={skeletonSolidMediaObject} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/skeleton/skeleton-media-object.module.css"
```
</div>

</ExampleCode>

### Адаптивная раскладка

Оставьте логику адаптивной компоновки в обёртке, а каждый `Skeleton` используйте как отдельный визуальный блок.

<ExampleFrame variant="component">
  <SkeletonResponsiveLayout />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/skeleton/responsive-layout.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={skeletonSolidResponsiveLayout} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/skeleton/skeleton-responsive-layout.module.css"
```
</div>

</ExampleCode>

### Варианты

Используйте `variant`, чтобы выбрать пульсирующее или статичное отображение заполнителя.

<ExampleFrame variant="component">
  <SkeletonVariants />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/skeleton/variants.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={skeletonSolidVariants} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/skeleton/skeleton-variants.module.css"
```
</div>

</ExampleCode>

### Расширенная настройка

Используйте `asChild`, когда корневым элементом должен стать семантический `section`, а не стандартный `div`.

<ExampleFrame variant="component">
  <SkeletonCustomHost />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/skeleton/custom-host.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={skeletonSolidCustomHost} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/skeleton/skeleton-custom-host.module.css"
```
</div>

</ExampleCode>

## Стилизация

Используйте `className` в React или `class` в Solid для структурных стилей и классов-утилит приложения.
Варианты Tailwind используют нативные utility-классы и классы приложения, а варианты с CSS Modules предоставляют
переменные ниже. Атрибуты состояния и хуки `data-slot` общие для обоих способов стилизации.

### CSS-переменные

<div className="docs-reference">
  <Tabs
    values={[{ label: 'CSS-переменные', value: 'CSS-переменные' }]}
    className="docs-reference-tabs"
    contentItemClassName="docs-reference-panel"
  >
    <Tab>
      <SkeletonDocs.SkeletonCssPropertiesPanel />
    </Tab>
  </Tabs>
</div>

### Селекторы и атрибуты для стилизации

- `[data-scope="skeleton"][data-part="root"]` выбирает экспортируемый корневой элемент.
- `data-slot="skeleton-root"` - стабильный селектор moduix для корневого элемента.
- `data-state="loading"` и `data-state="loaded"` соответствуют свойству `loading`.
- `data-variant="pulse"` и `data-variant="none"` соответствуют выбранному варианту.
- `data-loading` присутствует только тогда, когда активны стили заполнителя.
