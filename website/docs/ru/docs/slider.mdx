---
title: Slider
component: slider
description: Элемент управления для выбора одного значения или диапазона в заданных границах.
---

import * as SliderDocs from '@/components/examples/slider/css-properties';
import SliderAdvancedCustomization from '@/components/examples/slider/advanced-customization.tsx';
import SliderBasic from '@/components/examples/slider/basic.tsx';
import SliderCenterOrigin from '@/components/examples/slider/center-origin.tsx';
import SliderChangeEvents from '@/components/examples/slider/change-events.tsx';
import SliderContext from '@/components/examples/slider/context.tsx';
import SliderControlled from '@/components/examples/slider/controlled.tsx';
import SliderDisabled from '@/components/examples/slider/disabled.tsx';
import SliderDraggingIndicator from '@/components/examples/slider/dragging-indicator.tsx';
import SliderForm from '@/components/examples/slider/form.tsx';
import SliderInvalid from '@/components/examples/slider/invalid.tsx';
import SliderMarks from '@/components/examples/slider/marks.tsx';
import SliderMinAndMax from '@/components/examples/slider/min-and-max.tsx';
import SliderRange from '@/components/examples/slider/range.tsx';
import SliderReadOnly from '@/components/examples/slider/read-only.tsx';
import SliderRootProvider from '@/components/examples/slider/root-provider.tsx';
import SliderStep from '@/components/examples/slider/step.tsx';
import SliderThumbAlignment from '@/components/examples/slider/thumb-alignment.tsx';
import SliderThumbCollision from '@/components/examples/slider/thumb-collision.tsx';
import SliderThumbOverlap from '@/components/examples/slider/thumb-overlap.tsx';
import SliderVertical from '@/components/examples/slider/vertical.tsx';
import SliderVerticalWithMarks from '@/components/examples/slider/vertical-with-marks.tsx';

## Справочник API

<PrimitiveReference href="https://ark-ui.com/docs/components/slider" />

## Выбор подходящего компонента

Используйте `Slider`, когда пользователю нужно выбрать числовое значение в заданных границах с помощью указателя
или клавиатуры.

- Выберите **`Slider`** для громкости, яркости, бюджета, пороговых значений и диапазонов с несколькими ползунками.
- Выберите **`Input`** или **`NumberInput`**, если значение нужно точно ввести с клавиатуры, а не выбирать перетаскиванием.
- Выберите **`ProgressLinear`** или **`ProgressCircular`**, если значение нужно только показать.

## Базовый пример

`Slider.Thumbs` - рекомендуемый способ автоматически создать стилизованный ползунок для каждого значения. Каждый
ползунок, включая явно добавленный `Slider.Thumb`, автоматически добавляет нативное поле ввода для формы.

<ExampleFrame variant="component">
  <SliderBasic />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/slider/basic.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/slider/slider-basic.module.css"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

## Установка через shadcn

<ShadcnInstall packageName="slider" />

## Анатомия

```text
Slider / Slider.Root
├─ Slider.Label
├─ Slider.ValueText
├─ Slider.Control
│  ├─ Slider.Track
│  │  └─ Slider.Range
│  └─ Slider.Thumbs
│     └─ Slider.Thumb[index]
│        ├─ Slider.DraggingIndicator
│        └─ нативное поле ввода (автоматически)
└─ Slider.MarkerGroup
   └─ Slider.Marker[value]
```

| Часть                                  | Назначение                                                       |
| -------------------------------------- | ---------------------------------------------------------------- |
| `Slider` / `Slider.Root`               | Стилизованный корневой элемент Ark для управляемого и неуправляемого состояния. |
| `Slider.RootProvider`                  | Корневой элемент, подключённый к состоянию `useSlider`, созданному отдельно.    |
| `Slider.Label`                         | Видимая подпись группы.                                                        |
| `Slider.ValueText`                     | Текст текущего значения, который формирует Ark.                               |
| `Slider.Control`                       | Область взаимодействия с указателем.                                          |
| `Slider.Track`                         | Полная визуальная дорожка.                                                     |
| `Slider.Range`                         | Заполненная часть от точки отсчёта до текущего значения.                       |
| `Slider.Thumb`                         | Ползунок, который можно сфокусировать.                                        |
| `Slider.Thumbs`                        | Вспомогательный компонент moduix, создающий ползунок для каждого значения.      |
| `Slider.MarkerGroup` / `Slider.Marker` | Необязательная группа отметок и отдельная отметка.                             |
| `Slider.DraggingIndicator`             | Необязательное всплывающее значение во время перетаскивания.                   |

## Композиция

### Рекомендуемая композиция

Используйте дерево частей Ark: корневой элемент, подпись, при необходимости текст значения, область управления, дорожку,
заполненный диапазон и `Slider.Thumbs`. Компонент создаёт по одному ползунку на каждое значение, а каждый ползунок
автоматически добавляет нативное поле ввода для формы.

`Slider` - короткая запись для корня, эквивалентная `Slider.Root`.

### Когда нужна собственная композиция

Используйте `asChild`, если часть должна рендериться в вашем элементе. Внутри должен оставаться один семантический
дочерний элемент, который принимает свойства Ark. Если отдельным ползункам нужны свои дочерние элементы, доступные имена
или определённый порядок, используйте `Slider.Thumb` явно: нативное поле ввода при этом сохраняется. Для расширенного доступа
к состоянию используйте `Slider.Context` или `useSliderContext`, чтобы читать состояние под корневым элементом. Если состояние
нужно создать вне дерева слайдера, импортируйте `useSlider` из `@moduix/react/slider` и используйте его с
`Slider.RootProvider`.

### Свойства по умолчанию

| Свойство                 | По умолчанию  | Примечания                                                                 |
| ------------------------ | ------------- | -------------------------------------------------------------------------- |
| `defaultValue`           | `[min]`       | Ark хранит значения в виде `number[]`.                                    |
| `orientation`            | `horizontal`  | `vertical` переключает вертикальную ориентацию.                           |
| `min`                    | `0`           | Нижняя граница.                                                            |
| `max`                    | `100`         | Верхняя граница.                                                           |
| `step`                   | `1`           | Шаг для указателя и клавиатуры.                                            |
| `largeStep`              | `10 * step`   | Шаг для Page Up, Page Down и Shift + стрелки.                              |
| `minStepsBetweenThumbs`  | `0`           | Минимальное число шагов между ползунками диапазона.                       |
| `origin`                 | `start`       | Точка, от которой заполняется диапазон с одним значением.                  |
| `thumbAlignment`         | `contain`     | Используйте `center`, чтобы ползунок выходил за края дорожки.              |
| `thumbCollisionBehavior` | `none`        | Используйте `push` или `swap`, чтобы изменить поведение при встрече.       |
| `thumbSize`              | измеряется    | Переопределяет размеры ползунка, измеренные Ark.                           |

`value`, `defaultValue`, `onValueChange(details)` и `onValueChangeEnd(details)` используют объекты details Ark.
Читайте массив из `details.value`. `onFocusChange(details)` сообщает, какой ползунок получил фокус. Если `defaultValue`
не задан, начальное значение равно `[min]`, то есть по умолчанию `[0]`. Используйте `ids`, если другим элементам нужны
стабильные идентификаторы частей слайдера.

### Особенности поведения

`Slider` реализует паттерн слайдера WAI-ARIA из Ark. У каждого ползунка должно быть доступное имя, заданное через
`Slider.Label`, `aria-label` или `aria-labelledby`. В горизонтальном слайдере работают стрелки влево и вправо, в
вертикальном - вверх и вниз. `Page Up`, `Page Down` и `Shift + ArrowUp` или `Shift + ArrowDown` используют `largeStep`
(по умолчанию `10 * step`). `Home` и `End` устанавливают минимальное и максимальное значения активного ползунка.

Каждый `Slider.Thumb` автоматически добавляет скрытое нативное поле ввода. Оно участвует в отправке формы и синхронизируется
при её сбросе. `id`, `asChild`, `disabled`, `invalid`, `readOnly`, `name`, `form`, `ids`, `aria-label`, `aria-labelledby`
и `getAriaValueText(details)` без изменений передаются в Ark.

## Примеры

### Центр как точка отсчёта

Задайте `origin="center"`, если значение представляет положительное или отрицательное смещение, например баланс относительно нуля.

<ExampleFrame variant="component">
  <SliderCenterOrigin />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/slider/center-origin.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/slider/slider-center-origin.module.css"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### События изменения

Используйте `onValueChange`, чтобы обновлять интерфейс во время взаимодействия, а `onValueChangeEnd` - когда перетаскивание
или взаимодействие с клавиатурой завершено.

<ExampleFrame variant="component">
  <SliderChangeEvents />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/slider/change-events.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/slider/slider-change-events.module.css"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Контекст

Используйте `useSliderContext` из moduix внутри дочернего компонента, если ему нужно читать состояние слайдера, например
`value`, `dragging` или состояние фокуса.

<ExampleFrame variant="component">
  <SliderContext />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/slider/context.tsx"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Управляемое состояние

Управляйте слайдером из состояния React, сохраняя массив из `details.value`.

<ExampleFrame variant="component">
  <SliderControlled />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/slider/controlled.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/slider/slider-controlled.module.css"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Отключённый

Используйте `disabled`, чтобы оставить значение видимым, но отключить взаимодействие.

<ExampleFrame variant="component">
  <SliderDisabled />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/slider/disabled.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/slider/slider-disabled.module.css"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Форма

Задайте `name`, чтобы отправлять текущее значение через нативную форму. moduix автоматически добавляет и синхронизирует
скрытое поле ввода.

<ExampleFrame variant="component">
  <SliderForm />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/slider/form.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/slider/slider-form.module.css"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Индикатор перетаскивания

Поместите `Slider.DraggingIndicator` внутрь ползунка, чтобы показывать его текущее значение при перетаскивании.

<ExampleFrame variant="component">
  <SliderDraggingIndicator />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/slider/dragging-indicator.tsx"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Некорректное значение

Используйте `invalid`, если выбранное значение не проходит проверку. Дорожка, диапазон и ползунок получают состояние
ошибки, но остаются интерактивными.

<ExampleFrame variant="component">
  <SliderInvalid />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/slider/invalid.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/slider/slider-invalid.module.css"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Деления

Используйте `Slider.MarkerGroup` и `Slider.Marker`, чтобы добавить отметки на дорожку. Ark добавляет к отметкам
`data-state="under-value | at-value | over-value"`, который можно использовать в стилях.

<ExampleFrame variant="component">
  <SliderMarks />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/slider/marks.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/slider/slider-marks.module.css"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Минимум и максимум

Задайте `min` и `max`, если диапазон слайдера не начинается с `0` или не заканчивается на `100`.

<ExampleFrame variant="component">
  <SliderMinAndMax />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/slider/min-and-max.tsx"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Диапазон

Передайте массив, содержащий по одному значению на каждый ползунок. `Slider.Thumbs` создаст соответствующие ползунки
и скрытые поля ввода для формы.

<ExampleFrame variant="component">
  <SliderRange />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/slider/range.tsx"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Только для чтения

Используйте `readOnly`, если значение должно оставаться видимым, а ползунок - доступным для фокуса, но изменять его нельзя.
В этом состоянии moduix использует курсор по умолчанию, поэтому слайдер не выглядит интерактивным.

<ExampleFrame variant="component">
  <SliderReadOnly />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/slider/read-only.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/slider/slider-read-only.module.css"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Root Provider

Используйте `useSlider` из moduix с `Slider.RootProvider`, если состояние нужно создать вне дерева, в котором отображаются
части слайдера. Рендерьте эти части внутри `RootProvider` и не добавляйте второй корень для того же состояния.

<ExampleFrame variant="component">
  <SliderRootProvider />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/slider/root-provider.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/slider/slider-root-provider.module.css"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Шаг

Используйте `step` для дробного или более точного шага.

<ExampleFrame variant="component">
  <SliderStep />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/slider/step.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/slider/slider-step.module.css"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Выравнивание ползунка

Используйте `thumbAlignment="center"`, если ползунок должен выходить за края дорожки.

<ExampleFrame variant="component">
  <SliderThumbAlignment />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/slider/thumb-alignment.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/slider/slider-thumb-alignment.module.css"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Столкновение ползунков

Используйте `thumbCollisionBehavior`, чтобы задать поведение ползунков диапазона при встрече. Значение `none` (по умолчанию)
не позволяет одному ползунку пройти дальше другого, `push` двигает второй ползунок, а `swap` меняет их местами.

<ExampleFrame variant="component">
  <SliderThumbCollision />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/slider/thumb-collision.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/slider/slider-thumb-collision.module.css"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Перекрытие ползунков

Используйте `minStepsBetweenThumbs`, чтобы задать минимальный зазор между ползунками диапазона. Фактический зазор равен
`minStepsBetweenThumbs * step`.

<ExampleFrame variant="component">
  <SliderThumbOverlap />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/slider/thumb-overlap.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/slider/slider-thumb-overlap.module.css"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Вертикальный

Задайте `orientation="vertical"` и явно задайте корневому элементу высоту.

<ExampleFrame variant="component">
  <SliderVertical />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/slider/vertical.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/slider/slider-vertical.module.css"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Вертикальный с делениями

В вертикальной ориентации отметки выравниваются по дорожке и используют те же атрибуты состояния.

<ExampleFrame variant="component">
  <SliderVerticalWithMarks />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/slider/vertical-with-marks.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/slider/slider-vertical-with-marks.module.css"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Расширенная настройка

Используйте отдельные `Slider.Thumb` вместо `Slider.Thumbs`, если каждому ползунку нужны свои дочерние элементы, доступное
имя или определённый порядок.

<ExampleFrame variant="component">
  <SliderAdvancedCustomization />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/slider/advanced-customization.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/slider/slider-advanced-customization.module.css"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

## Стилизация

Настраивайте внешний вид `Slider` через `className`, атрибуты Ark, стабильные точки настройки `data-slot="slider-*"`,
CSS-переменные Ark и публичные CSS-переменные moduix ниже.

### CSS-свойства

<div className="docs-reference">
  <Tabs
    values={[{ label: 'CSS-переменные', value: 'CSS Variables' }]}
    className="docs-reference-tabs"
    contentItemClassName="docs-reference-panel"
  >
    <Tab>
      <SliderDocs.SliderCssPropertiesPanel />
    </Tab>
  </Tabs>
</div>

### Хуки стилизации

Ark добавляет `data-scope="slider"` и `data-part` ко всем отрендеренным частям. moduix также добавляет
значения `data-slot`: `slider-root`, `slider-root-provider`, `slider-label`, `slider-value-text`,
`slider-control`, `slider-track`, `slider-range`, `slider-thumb`, `slider-hidden-input`,
`slider-marker-group`, `slider-marker` и `slider-dragging-indicator`.

К важным атрибутам состояния Ark относятся `data-orientation`, `data-disabled`, `data-invalid`,
`data-focus`, `data-dragging`, `data-state` у отметок и `data-state="open | closed"` индикатора
перетаскивания. CSS-переменные Ark, которые используются во время работы: `--slider-thumb-width`, `--slider-thumb-height`,
`--slider-thumb-transform`, `--slider-range-start`, `--slider-range-end`, `--translate-x` и `--translate-y`.

moduix добавляет `data-readonly` к `Slider`, когда задано свойство `readOnly`. В состоянии ошибки по умолчанию
дорожка, выбранный диапазон, ползунок и активные отметки окрашиваются деструктивным цветом.
Их можно переопределить по отдельности через `--moduix-slider-track-border-color-invalid`,
`--moduix-slider-range-bg-invalid`, `--moduix-slider-thumb-border-color-invalid`,
`--moduix-slider-thumb-focus-ring-color-invalid` и `--moduix-slider-marker-dot-bg-active-invalid`.

Стандартное кольцо фокуса ползунка использует `:focus-visible` при навигации с клавиатуры и `data-dragging` Ark
во время перетаскивания. После отпускания указателя оно исчезает.
