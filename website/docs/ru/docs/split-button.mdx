---
title: Split Button
component: split-button
description: Группа с основной кнопкой и меню связанных действий на базе частей moduix Button и Menu.
---

import SplitButtonBasic from '@/components/examples/split-button/basic.tsx';
import SplitButtonControlledMenuState from '@/components/examples/split-button/controlled-menu-state.tsx';
import SplitButtonDisabledAction from '@/components/examples/split-button/disabled-action.tsx';
import SplitButtonLinkAction from '@/components/examples/split-button/link-action.tsx';
import SplitButtonMenuComposition from '@/components/examples/split-button/menu-composition.tsx';
import SplitButtonPositioning from '@/components/examples/split-button/positioning.tsx';
import SplitButtonSizes from '@/components/examples/split-button/sizes.tsx';
import SplitButtonVariants from '@/components/examples/split-button/variants.tsx';
import SplitButtonWithIcons from '@/components/examples/split-button/with-icons.tsx';
import * as SplitButtonDocs from '@/components/examples/split-button/css-properties';
import splitButtonSolidBasic from '../../../snippets/split-button/solid/basic.tsx?raw';
import splitButtonSolidControlledMenuState from '../../../snippets/split-button/solid/controlled-menu-state.tsx?raw';
import splitButtonSolidDisabledAction from '../../../snippets/split-button/solid/disabled-action.tsx?raw';
import splitButtonSolidLinkAction from '../../../snippets/split-button/solid/link-action.tsx?raw';
import splitButtonSolidMenuComposition from '../../../snippets/split-button/solid/menu-composition.tsx?raw';
import splitButtonSolidPositioning from '../../../snippets/split-button/solid/positioning.tsx?raw';
import splitButtonSolidSizes from '../../../snippets/split-button/solid/sizes.tsx?raw';
import splitButtonSolidVariants from '../../../snippets/split-button/solid/variants.tsx?raw';
import splitButtonSolidWithIcons from '../../../snippets/split-button/solid/with-icons.tsx?raw';

## Справочник API

В Ark UI нет отдельного примитива split button. moduix собирает этот компонент с помощью
[фабрики Ark](https://ark-ui.com/docs/guides/composition#the-ark-factory) и сохраняет поведение
[Ark Menu](https://ark-ui.com/docs/components/menu).

## Как выбрать компонент

Используйте `SplitButton`, когда одно действие должно быть доступно сразу, а связанные альтернативы можно
поместить в меню. Для одного действия подойдёт `Button`. Если все действия равнозначны, используйте `Menu`.

## Базовый пример


Используйте `<SplitButton>` как корневой компонент и оставляйте структуру меню явной:
`SplitButtonAction`, `SplitButtonTrigger`, `SplitButtonPositioner` и `SplitButtonContent`.
Основное действие использует тот же контракт `onClick`, что и `Button`.

<ExampleFrame variant="component">
  <SplitButtonBasic />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/split-button/basic.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={splitButtonSolidBasic} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/split-button/split-button-basic.module.css"
```
</div>

</ExampleCode>

## Установка через shadcn

Полную настройку пакета, реестра и стилей см. в разделе [Быстрый старт](/docs/quick-start).

<Tabs groupId="framework">
  <Tab label="React" value="react">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-react/split-button' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-react-tailwind/split-button' },
  ]}
/>

  </Tab>
  <Tab label="Solid" value="solid">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-solid/split-button' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-solid-tailwind/split-button' },
  ]}
/>

  </Tab>
</Tabs>

## Анатомия

```text
SplitButton
├─ SplitButtonAction
├─ SplitButtonTrigger
└─ Ветка всплывающего меню (по умолчанию в портале)
   └─ SplitButtonPositioner
      └─ SplitButtonContent
         ├─ MenuItem
         ├─ MenuSeparator
         └─ другие общие части Menu
```

| Часть                             | Назначение                                                               |
| -------------------------------- | ------------------------------------------------------------------------ |
| `SplitButton`                    | Корень, управляющий состоянием меню и семантической группой двух кнопок. |
| `SplitButtonAction`             | Основное действие `Button`.                                             |
| `SplitButtonTrigger`            | Триггер Ark Menu в виде кнопки moduix; по умолчанию показывает шеврон.  |
| `SplitButtonPositioner`         | Размещает всплывающее меню Ark.                                          |
| `SplitButtonContent`            | Поверхность содержимого Ark Menu для общих частей `MenuItem` и `MenuItemGroup`.              |

## Композиция

`SplitButton` объединяет основное действие `SplitButtonAction` и `SplitButtonTrigger`. Оставляйте дерево всплывающего меню явным:
`SplitButtonPositioner` → `SplitButtonContent`, а общие части `MenuItem` и `MenuItemGroup` размещайте внутри `SplitButtonContent`. Позиционер автоматически
рендерится в портале; используйте `portalled={false}` или `portalRef`, только если меню должно находиться
в другом контейнере. Корневые `size` и `variant` задают значения по умолчанию для обеих кнопок. Корень
рендерит семантический `role="group"`; задайте `aria-label` или `aria-labelledby`, если окружающий
контекст ещё не идентифицирует группу.

По умолчанию состоянием меню управляет сам компонент. Передайте `open` и `onOpenChange(details)`, если состояние
должно принадлежать другой части приложения. Для расширенного доступа к состоянию используйте общий API `Menu`,
а не добавляйте отдельную обёртку состояния для split button. Используйте `SplitButtonAction asChild` для ссылки; собственное
содержимое триггера передавайте через `children`, чтобы триггер меню Ark и кнопка moduix оставались одним элементом.

## Примеры

### Управляемое состояние меню

Используйте тот же контракт управляемого состояния, что и у Ark Menu, если состоянием открытия нужно управлять
из внешнего компонента.

<ExampleFrame variant="component">
  <SplitButtonControlledMenuState />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/split-button/controlled-menu-state.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={splitButtonSolidControlledMenuState} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Отключённое основное действие

Отключите основное действие, но оставьте меню доступным, если в нём есть полезная альтернатива.

<ExampleFrame variant="component">
  <SplitButtonDisabledAction />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/split-button/disabled-action.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={splitButtonSolidDisabledAction} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Действие-ссылка

Используйте `asChild` на `SplitButtonAction`, если основное действие должно быть ссылкой.

<ExampleFrame variant="component">
  <SplitButtonLinkAction />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/split-button/link-action.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={splitButtonSolidLinkAction} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Позиционирование

Настройте положение всплывающего меню с помощью корневого свойства Ark `positioning`, сохранив явную структуру
его частей.

<ExampleFrame variant="component">
  <SplitButtonPositioning />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/split-button/positioning.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={splitButtonSolidPositioning} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Размеры

Используйте корневой `size`, когда обе кнопки должны масштабироваться одновременно.

<ExampleFrame variant="component">
  <SplitButtonSizes />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/split-button/sizes.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={splitButtonSolidSizes} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/split-button/split-button-sizes.module.css"
```
</div>

</ExampleCode>

### Варианты

Используйте корневой `variant`, когда обе кнопки должны иметь одинаковое оформление.

<ExampleFrame variant="component">
  <SplitButtonVariants />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/split-button/variants.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={splitButtonSolidVariants} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/split-button/split-button-variants.module.css"
```
</div>

</ExampleCode>

### С иконками

Добавьте иконку к основному действию, если она помогает понять его назначение, но не заменяет текстовую подпись.

<ExampleFrame variant="component">
  <SplitButtonWithIcons />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/split-button/with-icons.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={splitButtonSolidWithIcons} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Расширенная настройка

Используйте `MenuItemGroup`, `MenuItemGroupLabel`, `MenuItem` и `MenuSeparator` внутри `SplitButtonContent`, если дополнительным действиям нужны группы,
разделители или сгруппированные пункты.

<ExampleFrame variant="component">
  <SplitButtonMenuComposition />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/split-button/menu-composition.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={splitButtonSolidMenuComposition} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

## Стилизация

Для структурных стилей передавайте экспортируемым частям `className` в React или `class` в Solid. Варианты на
Tailwind настраиваются обычными утилитами и пользовательскими классами; перечисленные ниже переменные относятся
к вариантам с CSS Modules. Атрибуты данных и хуки `data-slot` общие для обоих способов стилизации.

Объединённый вид создают общие стили `Button` и `Menu` вместе со слотами SplitButton. Разделитель и внутренние
отступы триггера настраиваются переменными `--moduix-split-button-separator-*` и
`--moduix-split-button-trigger-padding-x-*`.

### CSS-свойства

<CssPropertiesSection properties={SplitButtonDocs.splitButtonCssProperties} />

### Селекторы для стилизации

- `data-scope="split-button"` и `data-part="root"` задают семантическую группу.
- `data-slot="split-button-root"` выбирает обёртку группы.
- `data-slot="split-button-action"` выбирает основное действие.
- `data-slot="split-button-trigger"` выбирает триггер меню.
- `data-slot="split-button-positioner"` выбирает позиционер Ark Menu.
- `data-slot="split-button-content"` выбирает поверхность содержимого Ark Menu.
- `SplitButtonTrigger` получает атрибуты состояния Ark Menu, включая `aria-expanded`, `aria-controls` и `data-state`.
- `SplitButtonContent` получает атрибуты Ark Menu, включая `data-state`, `data-side`, `data-placement`,
  `data-nested` и `data-has-nested`.
