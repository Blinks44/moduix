---
title: Date Picker
component: date-picker
description: Компонент выбора даты на базе Ark UI с календарём во всплывающем окне или на странице, выбором диапазона и нескольких дат.
---

import * as DatePickerDocs from '@/components/examples/date-picker';

## Справочник API

<PrimitiveReference href="https://ark-ui.com/docs/components/date-picker" />

## Выбор подходящего компонента

- Выберите **`Input`** для обычного текстового ввода даты, если календарь не нужен.
- Выберите **`DateInput`**, если пользователям нужно редактировать сегменты даты с клавиатуры.
- Выберите **`DatePicker`**, если пользователям нужно редактируемое поле даты и всплывающий или встроенный календарь.

## Базовый пример

`DatePicker` использует состояние и поведение календаря Ark UI, а для распространённых сценариев предоставляет
готовые части. Используйте `DatePicker.Field` для стандартного поля с вводом и действиями, а
`DatePicker.DayTable` для таблицы дней.

<style>{DatePickerDocs.datePickerExampleCss}</style>

<PreviewFrame contentWidth="fit-content" maxWidth="sm">
  ```tsx preview file="../../en/docs/_snippets/date-picker/basic.tsx"

```
</PreviewFrame>

## Установка через shadcn

<ShadcnInstall packageName="date-picker" />

## Анатомия

```text
DatePicker
├─ DatePicker.Label
├─ DatePicker.Control
│  ├─ DatePicker.Input
│  ├─ DatePicker.ClearTrigger
│  └─ DatePicker.Trigger
├─ DatePicker.Field / DatePicker.RangeField (готовые элементы управления)
├─ Поддерево всплывающего календаря (корневой компонент автоматически помещает его в портал)
│  └─ DatePicker.Positioner
│     └─ DatePicker.Content
│        ├─ DatePicker.View[view="day" | "month" | "year"]
│        │  ├─ DatePicker.ViewControl
│        │  │  ├─ DatePicker.PrevTrigger
│        │  │  ├─ DatePicker.ViewTrigger / DatePicker.RangeText
│        │  │  └─ DatePicker.NextTrigger
│        │  ├─ DatePicker.DayTable (готовая таблица дней)
│        │  └─ DatePicker.Table
│        │     ├─ DatePicker.TableHead / TableBody / TableRow
│        │     ├─ DatePicker.TableHeader
│        │     ├─ DatePicker.TableCell
│        │     ├─ DatePicker.TableCellTrigger
│        │     ├─ DatePicker.WeekNumberHeaderCell
│        │     └─ DatePicker.WeekNumberCell
│        ├─ DatePicker.MonthSelect / DatePicker.YearSelect
│        └─ DatePicker.PresetTrigger
├─ DatePicker.ValueText (необязательный текст с выбранными значениями)
└─ DatePicker.Context
```

| Часть                              | Назначение                                                               |
| --------------------------------- | ------------------------------------------------------------------------ |
| `DatePicker` / `DatePicker.Root`  | Управляет значением, представлением, состоянием открытия, локалью, валидацией и контекстом формы. |
| `DatePicker.RootProvider`         | Рендерит части, используя состояние из `useDatePicker()`.               |
| `DatePicker.Label`                | Доступная подпись поля.                                                  |
| `DatePicker.Control`              | Обёртка поля, которая получает атрибуты состояния Ark.                  |
| `DatePicker.Field`                | Готовое поле с вводом, очисткой и открытием календаря.                   |
| `DatePicker.RangeField`           | Готовое поле диапазона с индексированными полями начала и конца.         |
| `DatePicker.Input`                | Редактируемое поле даты, участвующее в отправке нативной формы.          |
| `DatePicker.Trigger`              | Открывает календарь и по умолчанию использует иконку календаря.         |
| `DatePicker.ClearTrigger`         | Очищает текущее значение и по умолчанию использует иконку закрытия.      |
| `DatePicker.Positioner`           | Позиционирует всплывающее окно и предоставляет CSS-переменные Ark.       |
| `DatePicker.Content`              | Контейнер календаря во всплывающем или встроенном режиме.               |
| `DatePicker.View`                 | Панель дней, месяцев или лет.                                             |
| `DatePicker.ViewControl`          | Заголовок с элементами навигации по календарю.                           |
| `DatePicker.PrevTrigger`          | Переключает на предыдущий месяц, год или десятилетие.                    |
| `DatePicker.NextTrigger`          | Переключает на следующий месяц, год или десятилетие.                    |
| `DatePicker.ViewTrigger`          | Переключает представление между днями, месяцами и годами.                |
| `DatePicker.RangeText`             | Показывает видимый диапазон дат в заголовке календаря.                   |
| `DatePicker.ValueText`             | Показывает выбранные значения, необязательно с плейсхолдером или render prop. |
| `DatePicker.DayTable`              | Стандартная таблица дней, построенная на основе контекста Ark.           |
| `DatePicker.Table`                 | Корень таблицы для собственной сетки календаря.                          |
| `DatePicker.TableHead`             | Контейнер строки заголовка столбцов календаря.                           |
| `DatePicker.TableBody`             | Контейнер строк календаря.                                                |
| `DatePicker.TableRow`              | Строка с заголовками или ячейками календаря.                              |
| `DatePicker.TableHeader`           | Ячейка заголовка столбца календаря.                                       |
| `DatePicker.TableCell`             | Ячейка календаря для дня, месяца или года.                                |
| `DatePicker.TableCellTrigger`      | Элемент выбора внутри ячейки календаря.                                   |
| `DatePicker.WeekNumberHeaderCell`  | Ячейка заголовка столбца с номерами недель.                              |
| `DatePicker.WeekNumberCell`        | Ячейка с номером недели.                                                  |
| `DatePicker.MonthSelect`           | Нативный список выбора месяца, связанный с датой в фокусе.               |
| `DatePicker.YearSelect`            | Нативный список выбора года, связанный с датой в фокусе.                 |
| `DatePicker.PresetTrigger`         | Кнопка предустановленного диапазона.                                     |
| `DatePicker.Context`               | Доступ к текущему состоянию календаря без добавления разметки.           |

## Композиция

### Рекомендуемая композиция

Используйте `DatePicker.Field` для стандартного поля одной даты, `DatePicker.RangeField` для поля диапазона, а
`DatePicker.DayTable` для таблицы дней. Корневой компонент автоматически помещает `Positioner` в портал.
Используйте `portalled={false}`, чтобы оставить всплывающее окно в текущем DOM-поддереве, или `portalRef`, чтобы
указать другой контейнер. Задайте `inline`, если календарь должен оставаться в обычном потоке документа. Части
всплывающего календаря остаются явными, поэтому их можно стилизовать отдельно.

### Когда нужна своя композиция

Используйте `DatePicker.Control`, `DatePicker.Input`, `DatePicker.Trigger`, `DatePicker.ClearTrigger` и низкоуровневые
части `Table*` напрямую, когда нужна другая структура поля или сетки календаря. Используйте `DatePicker.Context`,
если собственной разметке нужно состояние календаря: `weekDays`, `weeks`, `getMonthsGrid()`, `getYearsGrid()`,
`getOffset()` или вспомогательные действия. Используйте `asChild` для другой семантической оболочки части, а `ids`,
если внешняя подпись или вспомогательный текст должны использовать стабильные ID компонента.

### Свойства по умолчанию

По умолчанию содержимое всплывающего календаря монтируется при первом открытии и удаляется после анимации закрытия.
Задайте `lazyMount={false}`, чтобы рендерить его сразу, или `unmountOnExit={false}`, чтобы оставить его в DOM после
первого открытия.

| Свойство                                 | По умолчанию       | Примечания                                        |
| ---------------------------------------- | ------------------ | ------------------------------------------------- |
| `DatePicker.Field.placeholder`           | шаблон даты Ark для локали | Заменяет автоматически созданный плейсхолдер поля.              |
| `DatePicker.Field.clearLabel`            | перевод Ark UI             | Заменяет подпись кнопки очистки.                                |
| `DatePicker.Field.triggerLabel`          | перевод Ark UI             | Заменяет подпись кнопки открытия.                               |
| `DatePicker.RangeField.startPlaceholder` | шаблон даты Ark для локали | Заменяет плейсхолдер поля для начальной даты диапазона.          |
| `DatePicker.RangeField.endPlaceholder`   | шаблон даты Ark для локали | Заменяет плейсхолдер поля для конечной даты диапазона.          |
| `DatePicker.DayTable.showHeader`         | `true`                     | Показывает кнопки предыдущего месяца, выбора представления и следующего месяца. |
| `DatePicker.DayTable.showWeekNumbers`    | `false`                    | Показывает заголовок и ячейки с номерами недель.                |
| `DatePicker.openOnClick`                 | `false`                    | Открывает календарь при нажатии на поле.                        |
| `DatePicker.closeOnSelect`               | `true`                     | Закрывает календарь после выбора; не действует в режиме нескольких дат. |

### Особенности поведения

`value`, `defaultValue` и `onValueChange(details)` используют массив `DateValue[]` из Ark. Из объекта `details` можно
получить `details.value`, `details.valueAsString` и `details.view`: выбранное значение, его отформатированное
представление и текущую панель. Импортируйте `DatePicker.Context`, `useDatePicker` и `useDatePickerContext` из
`@moduix/react`; при необходимости импортируйте `parseDate` и типы дат из `@ark-ui/react/date-picker`.
Стандартная ширина корневого компонента и всплывающего окна составляет `18.75rem` (300 px). Если нужна более широкая
композиция, переопределите `--moduix-date-picker-width` или `--moduix-date-picker-content-width`. Поскольку содержимое
окна рендерится в портале, задавайте переменные его размера на `DatePicker.Content` или другом элементе внутри дерева
всплывающего календаря, а не только на `DatePicker`.

`open`, `defaultOpen` и `onOpenChange(details)` управляют состоянием всплывающего окна. `view`, `defaultView`,
`minView`, `maxView` и `onViewChange(details)` управляют активной панелью календаря. Задайте `openOnClick`, чтобы
открывать календарь при нажатии на поле. `closeOnSelect` закрывает календарь после полного выбора и не действует в
режиме нескольких дат. Если композиция рендерит только одну панель, укажите это представление одновременно в `minView`
и `maxView`.

Внутри `Field` или `Fieldset` moduix компонент `DatePicker` наследует значения `disabled` и `invalid` из контекста.
`Field` также передаёт `readOnly` и `required`. Свойства, заданные непосредственно на `DatePicker`, имеют приоритет.
`DatePicker.Field` всегда использует индекс поля `0`. `DatePicker.RangeField` резервирует индексы `0` и `1` для полей
начальной и конечной даты. Для другой структуры полей используйте `Control` и `Input` напрямую.

Используйте `selectionMode="single"`, `"range"` или `"multiple"`. `maxSelectedDates` применяется только к
множественному выбору. `numOfMonths`, `fixedWeeks` и `showWeekNumbers` изменяют таблицу календаря.

Для недоступных дат используйте `min`, `max` и `isDateUnavailable(date, locale)`, чтобы эти ограничения применялись
и к полю, и к ячейкам календаря. Настройте локаль и форматирование с помощью `locale`, `timeZone`, `startOfWeek`,
`format`, `parse`, `translations` и `createCalendar`.

## Примеры

### Управляемое состояние

Управляйте массивом выбранных дат, обновляя его из `details.value`.

<style>{DatePickerDocs.datePickerExampleCss}</style>

<PreviewFrame contentWidth="fit-content" maxWidth="sm">
  ```tsx preview file="../../en/docs/_snippets/date-picker/controlled.tsx"

```

</PreviewFrame>

### Представление по умолчанию и фиксированное число недель

Используйте `defaultView`, чтобы открывать календарь на нужной панели. Добавьте `fixedWeeks`, чтобы панель дней
сохраняла высоту в шесть строк при переходе между месяцами.

<style>{DatePickerDocs.datePickerExampleCss}</style>

<PreviewFrame contentWidth="fit-content" maxWidth="sm">
  ```tsx preview file="../../en/docs/_snippets/date-picker/default-view-and-fixed-weeks.tsx"

```

</PreviewFrame>

### Состояние поля

`DatePicker` получает состояние поля из Ark. Задавайте `invalid`, `disabled`, `readOnly` и `required` на корневом
компоненте, если не оборачиваете его в `Field`.

<style>{DatePickerDocs.datePickerExampleCss}</style>

<PreviewFrame contentWidth="fit-content" maxWidth="sm">
  ```tsx preview file="../../en/docs/_snippets/date-picker/field-state.tsx"

```

</PreviewFrame>

### Форма

Передайте `name`, чтобы включить выбранную дату в отправку нативной формы. Если не задать плейсхолдер и подписи
действий, `DatePicker.Field` возьмёт их локализованные варианты из Ark UI.

<style>{DatePickerDocs.datePickerExampleCss}</style>

<PreviewFrame contentWidth="fit-content" maxWidth="sm">
  ```tsx preview file="../../en/docs/_snippets/date-picker/form.tsx"

```
</PreviewFrame>

### Встроенный календарь и номера недель

Задайте `inline`, чтобы календарь оставался в потоке документа. В этом режиме размещайте `Content` непосредственно
внутри корневого компонента, без `Positioner`.

<style>{DatePickerDocs.datePickerExampleCss}</style>

<PreviewFrame contentWidth="fit-content" maxWidth="sm">
  ```tsx preview file="../../en/docs/_snippets/date-picker/inline-and-week-numbers.tsx"

```

</PreviewFrame>

### Локаль и формат

`locale`, `startOfWeek`, `format` и `parse` передаются в Ark UI без изменений.

<style>{DatePickerDocs.datePickerExampleCss}</style>

<PreviewFrame contentWidth="fit-content" maxWidth="sm">
  ```tsx preview file="../../en/docs/_snippets/date-picker/locale-and-format.tsx"

```

</PreviewFrame>

### Минимальная дата, максимальная дата и недоступные даты

Используйте `min`, `max` и `isDateUnavailable` для правил валидации, которые должны применяться и к полю, и к
ячейкам календаря.

<style>{DatePickerDocs.datePickerExampleCss}</style>

<PreviewFrame contentWidth="fit-content" maxWidth="sm">
  ```tsx preview file="../../en/docs/_snippets/date-picker/min-max-and-unavailable.tsx"

```

</PreviewFrame>

### Выбор месяца и года

Ark предоставляет нативные части `DatePicker.MonthSelect` и `DatePicker.YearSelect`. В этом примере они заменены
на `Select` из moduix и `DatePicker.Context`. Заголовок выглядит так же, как в остальных компонентах библиотеки, и
при этом обновляет дату, на которой сфокусирован календарь Ark.

<style>{DatePickerDocs.datePickerExampleCss}</style>

<PreviewFrame contentWidth="fit-content" maxWidth="sm">
  ```tsx preview file="../../en/docs/_snippets/date-picker/month-year-select.tsx"

```

</PreviewFrame>

### Выбор месяца

Если пользователи выбирают только месяц, задайте `defaultView="month"` и `minView="month"`. Используйте `format` и
`parse`, если поле должно принимать строку в формате «месяц/год».

<style>{DatePickerDocs.datePickerExampleCss}</style>

<PreviewFrame contentWidth="fit-content" maxWidth="sm">
  ```tsx preview file="../../en/docs/_snippets/date-picker/month-picker.tsx"

```

</PreviewFrame>

### Несколько дат

Используйте `selectionMode="multiple"` и при необходимости ограничьте количество выбранных дат через
`maxSelectedDates`. Ark не помещает несколько значений в одно текстовое поле. Отображайте выбранные даты отдельными
чипами через `DatePicker.Context` и вызывайте `datePicker.setValue()`, когда пользователь удаляет чип.

<style>{DatePickerDocs.datePickerExampleCss}</style>

<PreviewFrame contentWidth="fit-content" maxWidth="sm">
  ```tsx preview file="../../en/docs/_snippets/date-picker/multiple.tsx"

```

</PreviewFrame>

### Несколько месяцев

Задайте `numOfMonths`, чтобы одновременно показывать несколько месяцев и пользователям было проще выбрать даты.

<style>{DatePickerDocs.datePickerExampleCss}</style>

<PreviewFrame contentWidth="fit-content" maxWidth="lg">
  ```tsx preview file="../../en/docs/_snippets/date-picker/multiple-months.tsx"

```

</PreviewFrame>

### Предустановки

`DatePicker.PresetTrigger` принимает встроенные имена предустановленных диапазонов Zag, например `last7Days`, или
пользовательский массив `DateValue[]`. Используйте режим диапазона с двумя индексированными полями, чтобы выбранная
предустановка показывала обе даты.

<style>{DatePickerDocs.datePickerExampleCss}</style>

<PreviewFrame contentWidth="fit-content" maxWidth="sm">
  ```tsx preview file="../../en/docs/_snippets/date-picker/presets.tsx"

```

</PreviewFrame>

### Диапазон

Используйте `selectionMode="range"` с двумя значениями. Ark управляет подсветкой диапазона при наведении, отметками
первого и последнего дня и форматированием поля. Для диапазона нужны два индексированных поля: для начальной и
конечной даты.

<style>{DatePickerDocs.datePickerExampleCss}</style>

<PreviewFrame contentWidth="fit-content" maxWidth="sm">
  ```tsx preview file="../../en/docs/_snippets/date-picker/range.tsx"

```

</PreviewFrame>

### Root Provider

Используйте `useDatePicker()` из `@moduix/react` вместе с `DatePicker.RootProvider`, когда состояние календаря нужно
разделить с элементами управления за пределами дерева компонента.

<style>{DatePickerDocs.datePickerExampleCss}</style>

<PreviewFrame contentWidth="fit-content" maxWidth="sm">
  ```tsx preview file="../../en/docs/_snippets/date-picker/root-provider.tsx"

```

</PreviewFrame>

### Выбор сегодняшней даты

Используйте `DatePicker.Context`, если соседняя кнопка должна вызывать методы Ark, например `selectToday()`.

<style>{DatePickerDocs.datePickerExampleCss}</style>

<PreviewFrame contentWidth="fit-content" maxWidth="sm">
  ```tsx preview file="../../en/docs/_snippets/date-picker/select-today.tsx"

```

</PreviewFrame>

### Дата и время

Чтобы сохранить время в выбранном значении, используйте объекты `CalendarDateTime`. Скомпонуйте календарь с
`DateInput`, настроенным на ввод минут, чтобы оба элемента редактировали одно значение.

<style>{DatePickerDocs.datePickerExampleCss}</style>

<PreviewFrame contentWidth="fit-content" maxWidth="lg">
  ```tsx preview file="../../en/docs/_snippets/date-picker/with-time.tsx"

```

</PreviewFrame>

### Выбор года

Если пользователи выбирают только год, задайте `defaultView="year"`, `minView="year"` и `maxView="year"`. В заголовке
используется `RangeText` вместо `ViewTrigger`, поскольку год уже является самым старшим доступным представлением.
В примере изначально выбран 2026 год, а поле показывает только год.

<style>{DatePickerDocs.datePickerExampleCss}</style>

<PreviewFrame contentWidth="fit-content" maxWidth="sm">
  ```tsx preview file="../../en/docs/_snippets/date-picker/year-picker.tsx"

```

</PreviewFrame>

### Расширенная настройка

Используйте полную композицию Ark, если сетку календаря нужно отрисовать по-своему. В этом случае
`DatePicker.Field` и `DatePicker.DayTable` не используются, но состояние Ark и стилизованные части остаются доступны.

<style>{DatePickerDocs.datePickerExampleCss}</style>

<PreviewFrame contentWidth="fit-content" maxWidth="sm">
  ```tsx preview file="../../en/docs/_snippets/date-picker/advanced-customization.tsx"

```

</PreviewFrame>

## Стилизация

### CSS-свойства

<div className="docs-reference">
  <Tabs
    values={[{ label: 'CSS-переменные', value: 'CSS Variables' }]}
    className="docs-reference-tabs"
    contentItemClassName="docs-reference-panel"
  >
    <Tab>
      <DatePickerDocs.DatePickerCssPropertiesPanel />
    </Tab>
  </Tabs>
</div>

### Хуки стилизации

Используйте эти атрибуты `data-slot`, чтобы обращаться к отдельным частям компонента:

- `data-slot="date-picker-root"` на корневом компоненте.
- `data-slot="date-picker-root-provider"` на корневом компоненте провайдера.
- `data-slot="date-picker-label"` на подписи поля.
- `data-slot="date-picker-control"` на обёртке поля, `DatePicker.Field` и `DatePicker.RangeField`.
- `data-slot="date-picker-input"` на редактируемом поле.
- `data-slot="date-picker-trigger"` и `data-slot="date-picker-clear-trigger"` на действиях поля.
- `data-slot="date-picker-value-text"` на тексте с выбранными значениями.
- `data-slot="date-picker-positioner"` и `data-slot="date-picker-content"` на всплывающем слое.
- `data-slot="date-picker-range-text"` на тексте с видимым диапазоном дат.
- `data-slot="date-picker-view"`, `date-picker-view-control`, `date-picker-view-trigger`,
  `date-picker-prev-trigger` и `date-picker-next-trigger` на навигации календаря.
- `data-slot="date-picker-table-*"` на частях таблицы и `data-slot="date-picker-table-cell-trigger"` на ячейках.
- `data-slot="date-picker-week-number-header-cell"` и `data-slot="date-picker-week-number-cell"` на ячейках
  с номерами недель.
- `data-slot="date-picker-month-select"` и `data-slot="date-picker-year-select"` на нативных списках выбора.
- `data-slot="date-picker-preset-trigger"` на кнопках предустановленных диапазонов.

Атрибуты состояния Ark включают `data-state`, `data-disabled`, `data-readonly`, `data-invalid`,
`data-focus`, `data-selected`, `data-today`, `data-unavailable`, `data-outside-range`,
`data-in-range`, `data-range-start`, `data-range-end`, `data-in-hover-range`,
`data-hover-range-start`, `data-hover-range-end` и `data-view`.
