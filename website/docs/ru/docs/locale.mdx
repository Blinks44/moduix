---
title: Локаль
description: Передавайте локаль и направление текста форматтерам Ark и локализованному содержимому в React и Solid.
---

import LocaleBasic from '@/components/examples/locale/basic.tsx';
import LocaleContext from '@/components/examples/locale/context.tsx';
import localeSolidBasic from '../../../snippets/locale/solid/basic.tsx?raw';
import localeSolidContext from '../../../snippets/locale/solid/context.tsx?raw';
import localeSolidDocument from '../../../snippets/locale/solid/document.tsx?raw';

`LocaleProvider` передаёт локаль и направление текста всему поддереву. Форматтеры Ark и локализованное содержимое
читают этот контекст.

## Справочник API

<PrimitiveReference href="https://ark-ui.com/docs/utilities/locale" />

Импортируйте утилиту из пакета Ark UI для используемого фреймворка:

<Tabs
  groupId="framework"
  values={[
    { label: 'React', value: 'react' },
    { label: 'Solid', value: 'solid' },
  ]}
  className="docs-tabs"
>
  <Tab value="react">

```tsx
import { LocaleProvider, useLocaleContext } from '@ark-ui/react/locale';
```

  </Tab>
  <Tab value="solid">

```tsx
import { LocaleProvider, useLocaleContext } from '@ark-ui/solid/locale';
```

  </Tab>
</Tabs>

`Format.Number`, `Format.Time` и другие утилиты `Format` по-прежнему импортируются из `@ark-ui/react` или
`@ark-ui/solid`.

## Задайте локаль

Оберните часть интерфейса, в которой используется одна локаль. Форматтеры и компоненты, учитывающие локаль, ниже
провайдера будут читать общий контекст.

<ExampleFrame variant="component">
  <PreviewFrame maxWidth="sm">
    <LocaleBasic />
  </PreviewFrame>
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/locale/basic.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={localeSolidBasic} />

    </Tab>
  </Tabs>
</div>
</ExampleCode>

`LocaleProvider` принимает строку локали в формате BCP 47, например `fr-FR`. Без провайдера Ark использует `en-US` и
`ltr`.

## Прочитайте контекст локали

`useLocaleContext` возвращает `locale` и `dir` из ближайшего провайдера. Используйте их, если собственная разметка
должна следовать тому же языку и направлению, что и компоненты, учитывающие локаль. В React значения читаются
напрямую, а в Solid доступны через accessor-функцию.

<ExampleFrame variant="component">
  <PreviewFrame maxWidth="sm">
    <LocaleContext />
  </PreviewFrame>
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/locale/context.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={localeSolidContext} />

    </Tab>
  </Tabs>
</div>
</ExampleCode>

## Задайте язык и направление документа

`LocaleProvider` вычисляет направление `dir` для Ark по локали, но не меняет нативные атрибуты документа `lang` и
`dir`. Если всё приложение использует одно направление, задайте оба атрибута на корневом элементе документа:

<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/locale/document.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={localeSolidDocument} />

    </Tab>
  </Tabs>
</div>

Для RTL-раздела внутри приложения с направлением слева направо задайте `lang` и соответствующий `dir` на корневом
элементе этого раздела, как в примере с контекстом. Если пользователи могут переключать LTR и RTL без перезагрузки
страницы, обновляйте атрибуты документа одновременно с `locale`. Подробнее о порталах, логических CSS-свойствах и
направленных иконках читайте в разделе [Справа налево (RTL)](/docs/rtl).
