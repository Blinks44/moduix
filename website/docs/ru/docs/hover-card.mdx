---
title: Hover Card
component: hover-card
description: Компонент Hover Card на базе Ark UI для дополнительных предпросмотров, открывающихся при наведении или получении фокуса.
---

import * as HoverCardDocs from '@/components/examples/hover-card/css-properties';
import BasicHoverCard from '@/components/examples/hover-card/basic.tsx';
import ContextHoverCard from '@/components/examples/hover-card/context.tsx';
import ControlledHoverCard from '@/components/examples/hover-card/controlled.tsx';
import DelayHoverCard from '@/components/examples/hover-card/delay.tsx';
import DialogHoverCard from '@/components/examples/hover-card/dialog.tsx';
import DisabledHoverCard from '@/components/examples/hover-card/disabled.tsx';
import MultipleTriggersHoverCard from '@/components/examples/hover-card/multiple-triggers.tsx';
import PositioningHoverCard from '@/components/examples/hover-card/positioning.tsx';
import RootProviderHoverCard from '@/components/examples/hover-card/root-provider.tsx';
import HoverCardWithArrow from '@/components/examples/hover-card/with-arrow.tsx';
import hoverCardSolidBasic from '../../../snippets/hover-card/solid/basic.tsx?raw';
import hoverCardSolidContext from '../../../snippets/hover-card/solid/context.tsx?raw';
import hoverCardSolidControlled from '../../../snippets/hover-card/solid/controlled.tsx?raw';
import hoverCardSolidDelay from '../../../snippets/hover-card/solid/delay.tsx?raw';
import hoverCardSolidDialog from '../../../snippets/hover-card/solid/dialog.tsx?raw';
import hoverCardSolidDisabled from '../../../snippets/hover-card/solid/disabled.tsx?raw';
import hoverCardSolidMultipleTriggers from '../../../snippets/hover-card/solid/multiple-triggers.tsx?raw';
import hoverCardSolidPositioning from '../../../snippets/hover-card/solid/positioning.tsx?raw';
import hoverCardSolidRootProvider from '../../../snippets/hover-card/solid/root-provider.tsx?raw';
import hoverCardSolidWithArrow from '../../../snippets/hover-card/solid/with-arrow.tsx?raw';

## Справочник API

<PrimitiveReference href="https://ark-ui.com/docs/components/hover-card" />

## Выбор подходящего компонента

Используйте `Tooltip` для коротких неинтерактивных подсказок. Выбирайте `Popover`, если во
всплывающем окне нужны более подробные сведения или действия. Используйте `HoverCard`, когда
триггер должен показывать дополнительный предпросмотр, например профиля или статьи.

## Базовый пример


Соберите карточку из явных частей Ark. `HoverCardTrigger` открывает карточку, а
`HoverCardPositioner` и `HoverCardContent` задают её всплывающее содержимое. В базовой
композиции стрелка не используется. Поместите содержимое в `HoverCardBody`, если оно может
превысить доступную высоту: оно будет прокручиваться, а `HoverCardContent` останется
необрезанным для дополнительной стрелки.

<ExampleFrame variant="component">
  <BasicHoverCard />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/hover-card/basic.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={hoverCardSolidBasic} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/hover-card/hover-card-basic.module.css"
```
</div>

</ExampleCode>

## Установка через shadcn

Полную настройку пакета, реестра и стилей см. в разделе [Быстрый старт](/docs/quick-start).

<Tabs groupId="framework">
  <Tab label="React" value="react">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-react/hover-card' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-react-tailwind/hover-card' },
  ]}
/>

  </Tab>
  <Tab label="Solid" value="solid">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-solid/hover-card' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-solid-tailwind/hover-card' },
  ]}
/>

  </Tab>
</Tabs>

## Анатомия

```text
HoverCard
├─ HoverCardTrigger
└─ Всплывающий слой (автоматически выводится через портал)
   └─ HoverCardPositioner
      └─ HoverCardContent
         ├─ HoverCardArrow
         │  └─ HoverCardArrowTip
         └─ HoverCardBody
            └─ содержимое

HoverCardRootProvider
└─ то же дерево частей, связанное с состоянием moduix useHoverCard()
```

| Часть                          | Назначение                                                                    |
| ------------------------------ | ----------------------------------------------------------------------------- |
| `HoverCard`                 | Управляет состоянием, жизненным циклом, задержками, позиционированием, идентификаторами и взаимодействием за пределами карточки. |
| `HoverCardRootProvider`       | Подключает части к состоянию, созданному через `useHoverCard()`.              |
| `HoverCardContext`            | Передаёт потомкам текущее состояние через render prop.                       |
| `HoverCardTrigger`            | Интерактивный элемент, открывающий карточку при наведении или фокусе. Используйте `asChild`, чтобы передать роль триггера ссылке или другому семантическому элементу. |
| `HoverCardPositioner`         | Позиционирует всплывающий слой и предоставляет CSS-переменные Ark.           |
| `HoverCardContent`            | Видимая область карточки, которая закрывается при взаимодействии за её пределами. |
| `HoverCardArrow`              | Необязательная обёртка позиционируемой стрелки, по умолчанию выводящая `HoverCardArrowTip`. |
| `HoverCardArrowTip`           | Наконечник стрелки, оформляемый с помощью переменных Ark.                     |
| `HoverCardBody`               | Необязательная прокручиваемая область содержимого для ограниченных предпросмотров. |

`HoverCard` - единственное публичное имя корневого компонента. Используйте его вместе с частями
с префиксом семейства в коде приложения.

## Композиция

`HoverCard` управляет состоянием и по умолчанию выводит `HoverCardPositioner` через портал.
Передайте `portalled={false}` внутри другого слоя или укажите `portalRef` для собственного
контейнера. Используйте `HoverCardRootProvider` с `useHoverCard()`, если состояние должно жить
за пределами дерева карточки: React читает его как объект, а Solid через accessor.

Оставляйте `HoverCardArrow` внутри `HoverCardContent`, а длинный предпросмотр помещайте в
`HoverCardBody`: `Content` сохраняет видимость стрелки, а `Body` становится областью прокрутки.
Используйте `asChild` с одним фокусируемым потомком, если триггером должна быть ссылка или другой
семантический элемент. Если несколько триггеров используют одно содержимое, задайте каждому
`value` и обновляйте внешнее содержимое через `onTriggerValueChange(details)`.

## Примеры

### Контекст

Используйте `HoverCardContext`, чтобы читать состояние внутри дерева карточки, не меняя его
видимую структуру.

<ExampleFrame variant="component">
  <ContextHoverCard />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/hover-card/context.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={hoverCardSolidContext} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/hover-card/hover-card-context.module.css"
```
</div>

</ExampleCode>

### Управляемое состояние

Управляйте открытием карточки с помощью `open` и `onOpenChange(details)`.

<ExampleFrame variant="component">
  <ControlledHoverCard />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/hover-card/controlled.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={hoverCardSolidControlled} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/hover-card/hover-card-controlled.module.css"
```
</div>

</ExampleCode>

### Задержка

Настройте время открытия и закрытия с помощью `openDelay` и `closeDelay` у корневого компонента.

<ExampleFrame variant="component">
  <DelayHoverCard />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/hover-card/delay.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={hoverCardSolidDelay} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/hover-card/hover-card-delay.module.css"
```
</div>

</ExampleCode>

### Диалог

Размещайте Hover Card непосредственно внутри диалога, если карточка должна оставаться в его
слое оверлея. При открытии диалог может установить начальный фокус на триггере Hover Card, и
карточка откроется после `openDelay`. Укажите в `initialFocusEl` другой элемент, если предпросмотр
должен оставаться закрытым до наведения или получения фокуса.

<ExampleFrame variant="component">
  <DialogHoverCard />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/hover-card/dialog.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={hoverCardSolidDialog} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/hover-card/hover-card-dialog.module.css"
```
</div>

</ExampleCode>

### Отключённое состояние

Установите `disabled`, чтобы запретить открытие карточки, но оставить видимое содержимое
триггера.

<ExampleFrame variant="component">
  <DisabledHoverCard />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/hover-card/disabled.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={hoverCardSolidDisabled} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/hover-card/hover-card-disabled.module.css"
```
</div>

</ExampleCode>

### Несколько триггеров

Назначьте каждому триггеру `value` и используйте `onTriggerValueChange(details)`, чтобы несколько
триггеров работали с одним всплывающим содержимым.

<ExampleFrame variant="component">
  <MultipleTriggersHoverCard />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/hover-card/multiple-triggers.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={hoverCardSolidMultipleTriggers} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/hover-card/hover-card-multiple-triggers.module.css"
```
</div>

</ExampleCode>

### Позиционирование

Передавайте параметры позиционирования Ark через свойство `positioning`.

<ExampleFrame variant="component">
  <PositioningHoverCard />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/hover-card/positioning.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={hoverCardSolidPositioning} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/hover-card/hover-card-positioning.module.css"
```
</div>

</ExampleCode>

### Root Provider

Используйте `useHoverCard()` с `HoverCardRootProvider`, когда элементам управления или другим
частям интерфейса за пределами дерева карточки нужно читать её состояние или изменять его.

<ExampleFrame variant="component">
  <RootProviderHoverCard />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/hover-card/root-provider.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={hoverCardSolidRootProvider} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/hover-card/hover-card-root-provider.module.css"
```
</div>

</ExampleCode>

### Со стрелкой

Добавьте `HoverCardArrow` внутрь `HoverCardContent`, если нужно визуально связать всплывающее
окно с триггером. Обёртка по умолчанию выводит `HoverCardArrowTip`; помещайте предпросмотр в
`HoverCardBody`, чтобы стрелка оставалась частью поверхности с границей, а содержимое могло
прокручиваться.

<ExampleFrame variant="component">
  <HoverCardWithArrow />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/hover-card/with-arrow.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={hoverCardSolidWithArrow} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/hover-card/hover-card-with-arrow.module.css"
```
</div>

</ExampleCode>

## Стилизация

Для структурных стилей используйте `className` в React или `class` в Solid на экспортируемых
частях. Варианты Tailwind используют нативные utility-классы и классы потребителя, а переменные
ниже настраивают варианты CSS Modules. Атрибуты состояния и хуки `data-slot` общие для обоих
вариантов стилизации.

### CSS-свойства

<div className="not-prose">
  <Tabs
    values={[{ label: 'Переменные CSS', value: 'CSS Variables' }]}
    className="docs-reference-tabs"
    contentItemClassName="docs-reference-panel"
  >
    <Tab value="CSS Variables">
      <HoverCardDocs.HoverCardCssPropertiesPanel />
    </Tab>
  </Tabs>
</div>

### Точки для стилизации

Moduix добавляет атрибуты `data-slot="hover-card-*"` к триггеру, позиционеру, содержимому, стрелке,
наконечнику стрелки и области содержимого. `HoverCard` и `HoverCardRootProvider` только
предоставляют состояние и не создают DOM-элементы, поэтому у них нет собственных точек стилизации.
Ark добавляет к своим частям `data-scope="hover-card"`
и `data-part`. Для стилизации с учётом состояния используйте `data-state="open|closed"`,
`data-placement`, `data-side`, `data-value`, `data-current`, `data-nested` и `data-has-nested`.

`HoverCardPositioner` предоставляет `--reference-width`, `--reference-height`, `--available-width`,
`--available-height`, `--x`, `--y`, `--z-index` и `--transform-origin`. `HoverCardContent`
предоставляет `--layer-index` и `--nested-layer-count`. `HoverCardArrow` предоставляет
`--arrow-size`, `--arrow-size-half`, `--arrow-background` и `--arrow-offset`.
