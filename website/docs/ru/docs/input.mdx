---
title: Input
component: input
description: Стилизованный однострочный `<input>` на основе Field.Input из Ark UI с нативными событиями и стилями moduix.
---

import * as InputDocs from '@/components/examples/input/css-properties';
import InputAsChild from '@/components/examples/input/as-child.tsx';
import InputBasic from '@/components/examples/input/basic.tsx';
import InputControlled from '@/components/examples/input/controlled.tsx';
import InputFieldState from '@/components/examples/input/field-state.tsx';
import InputFile from '@/components/examples/input/file-input.tsx';
import InputNativeAttributes from '@/components/examples/input/native-attributes.tsx';
import InputSizes from '@/components/examples/input/sizes.tsx';
import InputStandalone from '@/components/examples/input/standalone-input.tsx';
import InputStates from '@/components/examples/input/disabled-and-readonly.tsx';
import inputSolidAsChild from '../../../snippets/input/solid/as-child.tsx?raw';
import inputSolidBasic from '../../../snippets/input/solid/basic.tsx?raw';
import inputSolidControlled from '../../../snippets/input/solid/controlled.tsx?raw';
import inputSolidFieldState from '../../../snippets/input/solid/field-state.tsx?raw';
import inputSolidFile from '../../../snippets/input/solid/file-input.tsx?raw';
import inputSolidNativeAttributes from '../../../snippets/input/solid/native-attributes.tsx?raw';
import inputSolidSizes from '../../../snippets/input/solid/sizes.tsx?raw';
import inputSolidStandalone from '../../../snippets/input/solid/standalone-input.tsx?raw';
import inputSolidStates from '../../../snippets/input/solid/disabled-and-readonly.tsx?raw';

## Справочник API

Компонент `Input` основан на стилизованном [`Field.Input` из Ark UI](https://ark-ui.com/docs/components/field). Он следует рекомендациям Ark по [композиции](https://ark-ui.com/docs/guides/composition), [стилизации](https://ark-ui.com/docs/guides/styling) и [передаче ref](https://ark-ui.com/docs/guides/ref). Компонент отрисовывает нативный однострочный `<input>` и сохраняет стандартное поведение браузера в формах и нативный API событий каждого фреймворка.

Используйте `Field`, если полю нужны метка, поясняющий текст или сообщение об ошибке, автоматически
созданные идентификаторы или общее состояние.

## Выбор подходящего компонента

- Используйте **`Input`** для обычного однострочного поля ввода.
- Используйте **`InputGroup`**, если к полю нужно добавить префикс, суффикс, дополнительный текст или действие в одной строке.
- Используйте **`PasswordInput`** для поля пароля с переключателем видимости.
- Используйте **`NumberInput`**, если нужен числовой ввод с изменением значения по шагам и соответствующими кнопками.
- Используйте **`Textarea`** для многострочного текста.
- Используйте **`Editable`**, если значение сначала должно отображаться как текст, а затем переходить в режим редактирования.

## Базовый пример

`Input` доступен для React и Solid в пакетах с CSS Modules и в вариантах на Tailwind. Предпросмотр
на этой странице использует React и CSS Modules, а вкладки с кодом показывают нативный исходный
код для одного и того же публичного API.

Поместите `Input` в `Field`, если полю нужны метка, пояснение или сообщение об ошибке. `Field` свяжет эти элементы с полем, автоматически создаст нужные идентификаторы и передаст его состояние.

<ExampleFrame variant="component">
  <InputBasic />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/input/basic.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={inputSolidBasic} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/input/input-basic.module.css"
```
</div>

</ExampleCode>

## Установка через shadcn

Полную настройку пакета, реестра и стилей см. в разделе [Быстрый старт](/docs/quick-start).

<Tabs groupId="framework">
  <Tab label="React" value="react">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-react/input' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-react-tailwind/input' },
  ]}
/>

  </Tab>
  <Tab label="Solid" value="solid">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-solid/input' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-solid-tailwind/input' },
  ]}
/>

  </Tab>
</Tabs>

## Анатомия

```text
Field (необязательная оболочка)
└─ Input / Input.Root
```

| Часть                   | Назначение                                                                  |
| ----------------------- | --------------------------------------------------------------------------- |
| `Input` / `Input.Root` | Стилизованная версия `Field.Input` из Ark UI, которая отрисовывает нативный однострочный `<input>`. |

`Input.Root` и `Input` ссылаются на один и тот же компонент и отрисовывают одинаковый элемент
управления.

## Композиция

`Field` владеет меткой, поясняющим текстом или сообщением об ошибке, автоматически созданными
идентификаторами и общим состоянием `disabled`, `invalid`, `required` или `readOnly`. `Input` остаётся
нативным элементом: в React управляемом поле используйте `value` и `onChange(event)`, а в
неуправляемом - `defaultValue`. Компонент не управляет валидацией, фокусом или навигацией с
клавиатуры.

Для автономного поля задайте доступное имя самостоятельно. Используйте `asChild` только с одним
фокусируемым элементом, совместимым с полем ввода: Ark объединит в этом DOM-узле свойства `Input` и
`Field`, класс и `ref`.

## Примеры

### Управляемое значение

Используйте этот вариант, когда внешняя логика должна управлять текущим значением. В React читайте
новое значение из нативного события `onChange(event)`, а в Solid используйте `onInput`. Передавайте
текущее значение обратно через `value`.

<ExampleFrame variant="component">
  <InputControlled />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/input/controlled.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={inputSolidControlled} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/input/input-controlled.module.css"
```
</div>

</ExampleCode>

### Отключённое поле и режим только для чтения

Используйте `disabled`, если с полем нельзя взаимодействовать. Используйте `readOnly`, если
значение можно просматривать и выделять, но нельзя изменять. Для одного элемента задавайте эти
свойства непосредственно на `Input`. Если метка, поясняющий текст и поле должны иметь одно
состояние, задавайте свойства на `Field`.

<ExampleFrame variant="component">
  <InputStates />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/input/disabled-and-readonly.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={inputSolidStates} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/input/input-disabled-and-readonly.module.css"
```
</div>

</ExampleCode>

### Валидация поля

Задавайте `invalid` на `Field`, когда значение не проходит проверку. Ark добавит к `Input`
`aria-invalid` и `data-invalid` и свяжет его с текстом ошибки.

<ExampleFrame variant="component">
  <InputFieldState />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/input/field-state.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={inputSolidFieldState} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/input/input-field-state.module.css"
```
</div>

</ExampleCode>

### Выбор файла

Для простой формы `Input` достаточно, чтобы открыть нативный диалог выбора файла. Используйте
`FileUpload`, если приложение должно хранить выбранные файлы в состоянии, показывать область
перетаскивания или управлять процессом загрузки.

<ExampleFrame variant="component">
  <InputFile />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/input/file-input.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={inputSolidFile} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/input/input-file-input.module.css"
```
</div>

</ExampleCode>

### Нативные атрибуты

Используйте нативные атрибуты для управления поведением браузера: валидацией, режимом ввода,
автозаполнением и отправкой формы. Задавайте `htmlSize` для ширины в символах, а `size` для
изменения визуального размера в React и Solid.

<ExampleFrame variant="component">
  <InputNativeAttributes />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/input/native-attributes.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={inputSolidNativeAttributes} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/input/input-native-attributes.module.css"
```
</div>

</ExampleCode>

### Размеры

Свойство moduix `size` управляет визуальным размером. Оно не связано с `htmlSize`, которое задаёт
нативную ширину в символах.

<ExampleFrame variant="component">
  <InputSizes />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/input/sizes.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={inputSolidSizes} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/input/input-sizes.module.css"
```
</div>

</ExampleCode>

### Автономное поле

`Field` необязателен. Если `Input` используется отдельно, задайте доступное имя с помощью HTML
`<label>`, `aria-label` или `aria-labelledby`. Нативные свойства по-прежнему работают, но без
контекста `Field` идентификаторы не создаются автоматически, а поясняющий текст и сообщение об
ошибке не связываются с полем.

<ExampleFrame variant="component">
  <InputStandalone />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/input/standalone-input.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={inputSolidStandalone} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/input/input-standalone-input.module.css"
```
</div>

</ExampleCode>

### Расширенная настройка

Используйте `asChild` только тогда, когда вместо стандартного элемента `Input` должен быть отрисован другой DOM-элемент.
Передавайте один семантически корректный элемент, который может получить фокус, объединённые
свойства и `ref`.

<ExampleFrame variant="component">
  <InputAsChild />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/input/as-child.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={inputSolidAsChild} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/input/input-as-child.module.css"
```
</div>

</ExampleCode>

## Стилизация

Используйте `className` в React и `class` в Solid, чтобы стилизовать `Input`. Варианты с CSS Modules
поддерживают переменные ниже, а варианты на Tailwind используют нативные utility-классы и классы
потребителя. Для стилей состояний используйте общие data-атрибуты и нативные селекторы состояния.

### CSS-свойства

<div className="docs-reference">
  <Tabs
    values={[{ label: 'CSS-переменные', value: 'CSS Variables' }]}
    className="docs-reference-tabs"
    contentItemClassName="docs-reference-panel"
  >
    <Tab>
      <InputDocs.InputCssPropertiesPanel />
    </Tab>
  </Tabs>
</div>

### Хуки стилизации

- `data-slot="input-root"`
- `data-size="xs" | "sm" | "md" | "lg" | "xl"`
- `data-scope="field"` и `data-part="input"`
- `data-invalid`, `data-required` и `data-readonly` Ark внутри `Field`
- нативные `:disabled`, `:read-only`, `:focus-visible` и `[aria-invalid="true"]`
