---
title: Format Byte
description: Форматируйте размеры в React и Solid с помощью Ark UI, выбирая десятичную или двоичную систему единиц и локализованный вывод.
---

import FormatByteBasic from '@/components/examples/format-byte/basic.tsx';
import FormatByteUnitSystem from '@/components/examples/format-byte/unit-system.tsx';
import formatByteSolidBasic from '../../../snippets/format-byte/solid/basic.tsx?raw';
import formatByteSolidUnitSystem from '../../../snippets/format-byte/solid/unit-system.tsx?raw';
import formatByteSolidLocale from '../../../snippets/format-byte/solid/locale.tsx?raw';

`Format.Byte` форматирует число байтов в понятный текст. Импортируйте его из пакета Ark для нужного фреймворка.

## Справочник API

<PrimitiveReference href="https://ark-ui.com/docs/utilities/format-byte" />

Импортируйте `Format` из соответствующего пакета Ark UI:

<Tabs
  groupId="framework"
  values={[
    { label: 'React', value: 'react' },
    { label: 'Solid', value: 'solid' },
  ]}
  className="docs-tabs"
>
  <Tab value="react">

```tsx
import { Format } from '@ark-ui/react';
```

  </Tab>
  <Tab value="solid">

```tsx
import { Format } from '@ark-ui/solid';
```

  </Tab>
</Tabs>

## Базовый пример

Передайте в `value` число байтов. `Format.Byte` выводит только отформатированный текст, поэтому разместите его рядом
с подписью или другим элементом, который объясняет значение. В примерах для одного значения используется абзац, а
для нескольких значений с общим контекстом - список определений.

Предпросмотр использует React-исходник, потому что документация работает на React. Во вкладке Solid показан нативный
исходник Solid с тем же результатом и API.

<ExampleFrame variant="component">
  <PreviewFrame maxWidth="sm">
    <FormatByteBasic />
  </PreviewFrame>
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/format-byte/basic.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={formatByteSolidBasic} />

    </Tab>
  </Tabs>
</div>
</ExampleCode>

## Десятичная и двоичная системы

Используйте `unitSystem="decimal"` для кратных 1000 и `unitSystem="binary"` для кратных 1024. Выбирайте систему
осознанно, если продукт различает заявленную ёмкость диска и технические значения хранения.

<ExampleFrame variant="component">
  <PreviewFrame maxWidth="sm">
    <FormatByteUnitSystem />
  </PreviewFrame>
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/format-byte/unit-system.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={formatByteSolidUnitSystem} />

    </Tab>
  </Tabs>
</div>
</ExampleCode>

## Локаль

`Format.Byte` использует локаль ближайшего `LocaleProvider`. У него нет свойства `locale`. Оберните локализованную часть
интерфейса провайдером из соответствующего пакета Ark UI:

<Tabs
  groupId="framework"
  values={[
    { label: 'React', value: 'react' },
    { label: 'Solid', value: 'solid' },
  ]}
  className="docs-tabs"
>
  <Tab value="react">

```tsx pure file="../../../src/components/examples/format-byte/locale.tsx"
```

  </Tab>
  <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={formatByteSolidLocale} />

  </Tab>
</Tabs>

## Параметры

| Свойство | Значения | По умолчанию | Назначение |
| --- | --- | --- | --- |
| `value` | `number` | обязательный | Число байтов для форматирования. |
| `unit` | `'byte'` или `'bit'` | `'byte'` | Единица, которую представляет `value`. |
| `unitDisplay` | `'short'`, `'narrow'` или `'long'` | `'short'` | Способ записи названия единицы. |
| `unitSystem` | `'decimal'` или `'binary'` | `'decimal'` | Основа пересчёта значения. |

В текущем публичном типе `FormatByteProps` нет параметров `precision`, `locale` и `sizes`. Для смены локали используйте
`LocaleProvider`, а для настройки форматтера - только перечисленные выше свойства.
