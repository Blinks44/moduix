---
title: SolidStart
description: Подключение moduix к SolidStart через пакеты или shadcn, включая глобальные стили, алиасы, безопасный SSR и гидратацию.
---

# SolidStart

SolidStart рендерит Solid-приложение на сервере и гидратирует его в браузере. В этом руководстве
предполагается, что проект уже создан с помощью `create-solid`.

## Выберите вариант настройки

- **Среда:** Solid.
- **Стилизация:** CSS Modules или Tailwind CSS v4.
- **Поставка:** пакет или исходный код, добавленный через shadcn CLI.

Сначала выполните общую [настройку пакета](/docs/quick-start#установить-как-пакет) или
[настройку shadcn](/docs/quick-start#установить-через-shadcn-cli) для выбранного варианта стилей. Ниже
показаны импорты пакета на CSS Modules; корень SolidStart, алиас, SSR и гидратация не меняются с
`@moduix/solid-tailwind` или `@moduix-solid-tailwind`.

Все показанные ниже команды установки пакетов и импорты таблиц стилей относятся к CSS Modules.
Используйте показанный ниже reset moduix или корректный собственный reset приложения. С Tailwind сохраняйте
глобальную таблицу стилей, настроенную в Быстром старте.

## Установка из пакета

Установите moduix и зависимости для Solid:

<PackageManagerTabs command="install @moduix/solid @ark-ui/solid" />

Один раз импортируйте основные стили из корня приложения:

```tsx title="src/app.tsx"
import '@moduix/solid/reset.css';
import '@moduix/solid/style.css';
```

Импортируйте компоненты из соответствующих подпутей пакета:

```tsx title="src/routes/index.tsx"
import { Button } from '@moduix/solid/button';

export default function Home() {
  return <Button>Continue</Button>;
}
```

## Установка через shadcn CLI

В SolidStart алиас `~` указывает на `src`, а в примерах реестра moduix используется `@`. Выполните
[первоначальную настройку shadcn CLI](/docs/quick-start#установить-через-shadcn-cli), затем задайте
алиас `@/*` для `src/*` в `tsconfig.json`:

```json title="tsconfig.json"
{
  "compilerOptions": {
    "paths": {
      "@/*": ["./src/*"]
    }
  }
}
```

Добавьте тот же алиас в существующую конфигурацию Vite:

```ts title="vite.config.ts"
import { fileURLToPath, URL } from 'node:url';
import { solidStart } from '@solidjs/start/config';
import { defineConfig } from 'vite';

export default defineConfig({
  plugins: [solidStart()],
  resolve: {
    alias: {
      '@': fileURLToPath(new URL('./src', import.meta.url)),
    },
  },
});
```

Затем добавьте компонент для Solid:

<ShadcnInstallOptions options={[{ label: 'CSS Modules', command: 'shadcn@latest add @moduix-solid/button' }, { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-solid-tailwind/button' }]} />

Импортируйте сгенерированные основные стили из `src/app.tsx` вместо стилей пакета:

```tsx title="src/app.tsx"
import '@/lib/moduix/styles/style.css';
```

## Проверка

Запустите сервер разработки и откройте главную страницу. Серверный HTML должен сразу получить стили
moduix, а гидратация должна пройти без расхождений и сохранить интерактивность компонентов.

Создание проекта и актуальные требования к среде описаны в официальном
[руководстве SolidStart](https://docs.solidjs.com/solid-start/v2/getting-started).
