---
title: React Router
description: "Подключите moduix к React Router в режиме фреймворка: общий CSS, разметка без проблем с SSR и алиасы."
---

# React Router

Эта инструкция относится к React Router в режиме фреймворка. В режиме данных используйте те же импорты пакета в
уже существующей точке входа Vite.

## Установите пакет

<PackageManagerTabs command="install @moduix/react @ark-ui/react" />

Один раз импортируйте базовую таблицу стилей из корневого маршрута:

```tsx title="app/root.tsx"
import '@moduix/react/style.css';
```

Используйте компоненты в модуле маршрута:

```tsx title="app/routes/home.tsx"
import { Button } from '@moduix/react/button';

export default function Home() {
  return <Button>Continue</Button>;
}
```

## Установка через shadcn CLI

Завершите [настройку shadcn CLI](/docs/quick-start#установить-через-shadcn-cli). Если сгенерированный код
должен находиться в стандартном каталоге `app`, сопоставьте `@/*` с `app/*` вместо `src/*`:

```json title="tsconfig.json"
{
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "@/*": ["./app/*"]
    }
  }
}
```

В режиме фреймворка React Router использует Vite. Поэтому добавьте такой же алиас в `vite.config.ts`, как показано
в [настройке Vite](/docs/installation/vite#установка-через-shadcn-cli), только замените `./src` на `./app`. Затем
импортируйте `@/lib/moduix/styles/style.css` из `app/root.tsx`.

## Проверка

Настройка завершена, когда начальный серверный рендеринг совпадает с гидратированной страницей, а интерактивные
компоненты работают после гидратации. Не меняйте разметку во время рендеринга в зависимости от глобальных объектов
браузера.

Полная настройка фреймворка приведена в [руководстве по установке React Router](https://reactrouter.com/start/framework/installation).
