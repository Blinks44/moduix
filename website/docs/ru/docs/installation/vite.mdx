---
title: Vite
description: Подключение moduix к Vite на React или Solid через пакеты или shadcn, включая глобальные стили, пути TypeScript и алиасы Vite.
---

# Vite

В этом руководстве предполагается существующее приложение, созданное из шаблона Vite для React,
React + TypeScript, Solid или Solid + TypeScript.

## Выберите вариант настройки

- **Среда:** React или Solid с соответствующим плагином Vite.
- **Стилизация:** CSS Modules или Tailwind CSS v4.
- **Поставка:** пакет или исходный код, добавленный через shadcn CLI.

Сначала выполните общую [настройку пакета](/docs/quick-start#установить-как-пакет) или
[настройку shadcn](/docs/quick-start#установить-через-shadcn-cli) для выбранного варианта стилей. Ниже
показаны импорты пакетов на CSS Modules; точки входа Vite и настройка алиасов не меняются при выборе
соответствующего Tailwind-пакета или реестра.

Все показанные ниже команды установки пакетов и импорты таблиц стилей относятся к CSS Modules.
Используйте показанный ниже reset moduix или корректный собственный reset приложения. С Tailwind сохраняйте
глобальную таблицу стилей, настроенную в Быстром старте.

## Установка из пакета

<Tabs groupId="framework">
  <Tab label="React" value="react">

<PackageManagerTabs command="install @moduix/react @ark-ui/react" />

```tsx title="src/main.tsx"
import '@moduix/react/reset.css';
import '@moduix/react/style.css';
```

```tsx title="src/App.tsx"
import { Button } from '@moduix/react/button';

export function App() {
  return <Button>Continue</Button>;
}
```

  </Tab>
  <Tab label="Solid" value="solid">

<PackageManagerTabs command="install @moduix/solid @ark-ui/solid" />

```tsx title="src/index.tsx"
import '@moduix/solid/reset.css';
import '@moduix/solid/style.css';
```

```tsx title="src/App.tsx"
import { Button } from '@moduix/solid/button';

export function App() {
  return <Button>Continue</Button>;
}
```

  </Tab>
</Tabs>

## Установка через shadcn CLI

Выполните [первоначальную настройку shadcn CLI](/docs/quick-start#установить-через-shadcn-cli), затем
задайте алиас `@/*` для `src/*`. В шаблонах Vite для TypeScript настройки разделены между
`tsconfig.json` и конфигурацией приложения. Добавьте алиас в оба файла:

```json title="tsconfig.json"
{
  "compilerOptions": {
    "paths": {
      "@/*": ["./src/*"]
    }
  }
}
```

Добавьте те же значения `compilerOptions` в `tsconfig.app.json`, а затем настройте алиас в Vite для
выбранного фреймворка. В примере используется `node:url`, поэтому установите типы Node.js, если их
ещё нет в проекте:

<PackageManagerTabs command="install -D @types/node" />

<Tabs groupId="framework">
  <Tab label="React" value="react">

```ts title="vite.config.ts"
import { fileURLToPath, URL } from 'node:url';
import react from '@vitejs/plugin-react';
import { defineConfig } from 'vite';

export default defineConfig({
  plugins: [react()],
  resolve: {
    alias: {
      '@': fileURLToPath(new URL('./src', import.meta.url)),
    },
  },
});
```

<ShadcnInstallOptions options={[{ label: 'CSS Modules', command: 'shadcn@latest add @moduix-react/button' }, { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-react-tailwind/button' }]} />

  </Tab>
  <Tab label="Solid" value="solid">

```ts title="vite.config.ts"
import { fileURLToPath, URL } from 'node:url';
import { defineConfig } from 'vite';
import solid from 'vite-plugin-solid';

export default defineConfig({
  plugins: [solid()],
  resolve: {
    alias: {
      '@': fileURLToPath(new URL('./src', import.meta.url)),
    },
  },
});
```

<ShadcnInstallOptions options={[{ label: 'CSS Modules', command: 'shadcn@latest add @moduix-solid/button' }, { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-solid-tailwind/button' }]} />

  </Tab>
</Tabs>

Импортируйте сгенерированные основные стили из точки входа вместо стилей пакета.
Подключать оба файла не нужно:

<Tabs groupId="framework">
  <Tab label="React" value="react">

```tsx title="src/main.tsx"
import '@/lib/moduix/styles/style.css';
```

  </Tab>
  <Tab label="Solid" value="solid">

```tsx title="src/index.tsx"
import '@/lib/moduix/styles/style.css';
```

  </Tab>
</Tabs>

## Проверка

Запустите Vite. При установке из пакета компонент выбранного фреймворка должен отображаться со
стилями. При установке через shadcn редактор и сборщик должны разрешать сгенерированный файл стилей
и импорт `@/components/ui/button`.

Создание проекта и доступные шаблоны описаны в официальном [руководстве Vite](https://vite.dev/guide/).
