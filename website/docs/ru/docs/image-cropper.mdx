---
title: Image Cropper
component: image-cropper
description: Компонент для кадрирования и преобразования изображений с поддержкой масштабирования, поворота и соотношения сторон.
---

import * as ImageCropperDocs from '@/components/examples/image-cropper/css-properties';
import imageCropperSolidAdvancedCustomization from '../../../snippets/image-cropper/solid/advanced-customization.tsx?raw';
import imageCropperSolidAspectRatio from '../../../snippets/image-cropper/solid/aspect-ratio.tsx?raw';
import imageCropperSolidBasic from '../../../snippets/image-cropper/solid/basic.tsx?raw';
import imageCropperSolidCircleCrop from '../../../snippets/image-cropper/solid/circle-crop.tsx?raw';
import imageCropperSolidControlledZoom from '../../../snippets/image-cropper/solid/controlled-zoom.tsx?raw';
import imageCropperSolidCropPreview from '../../../snippets/image-cropper/solid/crop-preview.tsx?raw';
import imageCropperSolidEvents from '../../../snippets/image-cropper/solid/events.tsx?raw';
import imageCropperSolidFixedCropArea from '../../../snippets/image-cropper/solid/fixed-crop-area.tsx?raw';
import imageCropperSolidInitialCrop from '../../../snippets/image-cropper/solid/initial-crop.tsx?raw';
import imageCropperSolidMinAndMaxSize from '../../../snippets/image-cropper/solid/min-and-max-size.tsx?raw';
import imageCropperSolidRootProvider from '../../../snippets/image-cropper/solid/root-provider.tsx?raw';
import imageCropperSolidRotationFlipAndReset from '../../../snippets/image-cropper/solid/rotation-flip-and-reset.tsx?raw';
import ImageCropperAdvancedCustomization from '@/components/examples/image-cropper/advanced-customization.tsx';
import ImageCropperAspectRatio from '@/components/examples/image-cropper/aspect-ratio.tsx';
import ImageCropperBasic from '@/components/examples/image-cropper/basic.tsx';
import ImageCropperCircleCrop from '@/components/examples/image-cropper/circle-crop.tsx';
import ImageCropperControlledZoom from '@/components/examples/image-cropper/controlled-zoom.tsx';
import ImageCropperCropPreview from '@/components/examples/image-cropper/crop-preview.tsx';
import ImageCropperEvents from '@/components/examples/image-cropper/events.tsx';
import ImageCropperFixedCropArea from '@/components/examples/image-cropper/fixed-crop-area.tsx';
import ImageCropperInitialCrop from '@/components/examples/image-cropper/initial-crop.tsx';
import ImageCropperMinAndMaxSize from '@/components/examples/image-cropper/min-and-max-size.tsx';
import ImageCropperRootProvider from '@/components/examples/image-cropper/root-provider.tsx';
import ImageCropperRotationFlipAndReset from '@/components/examples/image-cropper/rotation-flip-and-reset.tsx';

## Справочник API

<PrimitiveReference href="https://ark-ui.com/docs/components/image-cropper" />

## Базовый пример

Используйте `ImageCropper` вместе с `Viewport`, `Image` и `CropArea`. Передайте URL изображения в
`ImageCropper.Image`. Компонент `CropArea` сам добавит область кадрирования, линии сетки и маркеры
изменения размера.

<ExampleFrame variant="component">
  <ImageCropperBasic />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs groupId="framework" values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]} className="docs-tabs">
    <Tab value="react">

```tsx pure file="../../../src/components/examples/image-cropper/basic.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={imageCropperSolidBasic} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

Если вы вызываете `getCroppedImage()` для изображения с другого домена, сервер изображения должен
разрешать CORS, а в `ImageCropper.Image` нужно передать `crossOrigin="anonymous"`. В примерах это
уже настроено для изображения с Unsplash.

## Установка через shadcn

<Tabs groupId="framework">
  <Tab label="React" value="react">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-react/image-cropper' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-react-tailwind/image-cropper' },
  ]}
/>

  </Tab>
  <Tab label="Solid" value="solid">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-solid/image-cropper' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-solid-tailwind/image-cropper' },
  ]}
/>

  </Tab>
</Tabs>

## Анатомия

```tsx
<ImageCropper>
  <ImageCropper.Viewport>
    <ImageCropper.Image />
    <ImageCropper.CropArea />
  </ImageCropper.Viewport>
</ImageCropper>
```

| Часть                                | Назначение                                                                                  |
| ------------------------------------ | ------------------------------------------------------------------------------------------- |
| `ImageCropper` / `ImageCropper.Root` | Корневой компонент, который управляет состоянием кадрирования и преобразований, ARIA-атрибутами и CSS-переменными, обновляемыми во время работы. |
| `ImageCropper.RootProvider`          | Корневой компонент, связанный с экземпляром `useImageCropper`, созданным вне дерева компонентов. |
| `ImageCropper.Viewport`              | Область просмотра, в которой можно панорамировать изображение; всё за её границами скрывается. |
| `ImageCropper.Image`                 | Элемент изображения, к которому Ark применяет преобразования.                              |
| `ImageCropper.Selection`             | Фокусируемая область кадрирования и цель для управления с клавиатуры. При `fixedCropArea` она исключается из порядка перехода по Tab. |
| `ImageCropper.Grid`                  | Неинтерактивная горизонтальная или вертикальная линия сетки. Требуется `axis`.              |
| `ImageCropper.Handle`                | Маркер изменения размера для позиции из `ImageCropper.handles`. Требуется `position`.       |
| `ImageCropper.CropArea`              | Готовая область кадрирования с обеими сетками и всеми маркерами изменения размера. Не принимает `children` и `asChild`. |

## Композиция

Используйте `ImageCropper.CropArea` для стандартной области кадрирования. Она сама создаёт две сетки
и все маркеры изменения размера, поэтому не принимает `children` и `asChild`. Для стилизации
области и созданных частей используйте `className`, `gridClassName` и `handleClassName`.

Для элементов управления внутри корневого компонента используйте `ImageCropper.Context`. Создайте
`useImageCropper()` и передайте его в `ImageCropper.RootProvider`, если панели инструментов,
предварительному просмотру или логике формы нужен экземпляр кадрирования за пределами дерева
компонентов. Для одного экземпляра состояния выбирайте либо `Root`, либо `RootProvider`.

Используйте управляемые `zoom`, `rotation` или `flip` с соответствующим обратным вызовом, если
состоянием владеет другая часть приложения. Ark передаёт новое значение в объекте `details`.
`fixedCropArea` фиксирует область, а область просмотра становится поверхностью для панорамирования.

## Примеры

### Соотношение сторон

Задайте `aspectRatio`, чтобы сохранять фиксированное соотношение ширины и высоты области. Если
пользователь выбирает формат результата, меняйте это свойство из состояния React.

<ExampleFrame variant="component">
  <ImageCropperAspectRatio />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs groupId="framework" values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]} className="docs-tabs">
    <Tab value="react">

```tsx pure file="../../../src/components/examples/image-cropper/aspect-ratio.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={imageCropperSolidAspectRatio} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/image-cropper/image-cropper-aspect-ratio.module.css"
```
</div>

</ExampleCode>

### Круглое кадрирование

Используйте `cropShape="circle"` вместе с `aspectRatio={1}` для аватаров и изображений профиля.

<ExampleFrame variant="component">
  <ImageCropperCircleCrop />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs groupId="framework" values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]} className="docs-tabs">
    <Tab value="react">

```tsx pure file="../../../src/components/examples/image-cropper/circle-crop.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={imageCropperSolidCircleCrop} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Управляемый масштаб

Используйте управляемые `zoom` и `onZoomChange`, если кнопки или другая часть интерфейса должны
изменять или показывать текущий масштаб. Ограничьте диапазон с помощью `minZoom` и `maxZoom`.

<ExampleFrame variant="component">
  <ImageCropperControlledZoom />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs groupId="framework" values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]} className="docs-tabs">
    <Tab value="react">

```tsx pure file="../../../src/components/examples/image-cropper/controlled-zoom.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={imageCropperSolidControlledZoom} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/image-cropper/image-cropper-controlled-zoom.module.css"
```
</div>

</ExampleCode>

### Предварительный просмотр кадра

Вызовите `getCroppedImage()` у экземпляра `useImageCropper`, чтобы получить готовый результат.
С параметром `{ output: 'dataUrl' }` метод возвращает data URL, который можно передать в `src`
элемента `img`. Используйте `getCropData()`, если нужны только координаты кадрирования и данные
о преобразованиях.

<ExampleFrame variant="component">
  <ImageCropperCropPreview />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs groupId="framework" values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]} className="docs-tabs">
    <Tab value="react">

```tsx pure file="../../../src/components/examples/image-cropper/crop-preview.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={imageCropperSolidCropPreview} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/image-cropper/image-cropper-crop-preview.module.css"
```
</div>

</ExampleCode>

### События

Подпишитесь на `onCropChange` и `onZoomChange`, чтобы показывать текущие данные кадрирования или
синхронизировать их с другими элементами интерфейса.

<ExampleFrame variant="component">
  <ImageCropperEvents />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs groupId="framework" values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]} className="docs-tabs">
    <Tab value="react">

```tsx pure file="../../../src/components/examples/image-cropper/events.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={imageCropperSolidEvents} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/image-cropper/image-cropper-events.module.css"
```
</div>

</ExampleCode>

### Фиксированная область кадрирования

Включите `fixedCropArea`, если область кадрирования должна сохранять размер и положение, пока
пользователь панорамирует изображение под ней.

<ExampleFrame variant="component">
  <ImageCropperFixedCropArea />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs groupId="framework" values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]} className="docs-tabs">
    <Tab value="react">

```tsx pure file="../../../src/components/examples/image-cropper/fixed-crop-area.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={imageCropperSolidFixedCropArea} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Начальная область кадрирования

Передайте `initialCrop`, чтобы открыть компонент с заранее заданным прямоугольником кадрирования в
координатах области просмотра и пикселях.

<ExampleFrame variant="component">
  <ImageCropperInitialCrop />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs groupId="framework" values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]} className="docs-tabs">
    <Tab value="react">

```tsx pure file="../../../src/components/examples/image-cropper/initial-crop.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={imageCropperSolidInitialCrop} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Минимальный и максимальный размер

Ограничьте размер области кадрирования с помощью `minWidth`, `minHeight`, `maxWidth` и `maxHeight`.

<ExampleFrame variant="component">
  <ImageCropperMinAndMaxSize />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs groupId="framework" values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]} className="docs-tabs">
    <Tab value="react">

```tsx pure file="../../../src/components/examples/image-cropper/min-and-max-size.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={imageCropperSolidMinAndMaxSize} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Root Provider

Используйте `RootProvider` с экземпляром `useImageCropper`, если панель инструментов или другой
сценарий должны управлять кадрированием за пределами дерева его компонентов.

<ExampleFrame variant="component">
  <ImageCropperRootProvider />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs groupId="framework" values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]} className="docs-tabs">
    <Tab value="react">

```tsx pure file="../../../src/components/examples/image-cropper/root-provider.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={imageCropperSolidRootProvider} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/image-cropper/image-cropper-root-provider.module.css"
```
</div>

</ExampleCode>

### Поворот, отражение и сброс

Используйте `ImageCropper.Context`, чтобы подключить к панели инструментов действия масштабирования,
поворота, отражения и сброса. В этом примере состояние поворота и отражения контролируется из React.

<ExampleFrame variant="component">
  <ImageCropperRotationFlipAndReset />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs groupId="framework" values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]} className="docs-tabs">
    <Tab value="react">

```tsx pure file="../../../src/components/examples/image-cropper/rotation-flip-and-reset.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={imageCropperSolidRotationFlipAndReset} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/image-cropper/image-cropper-rotation-flip-and-reset.module.css"
```
</div>

</ExampleCode>

### Расширенная настройка

Замените `CropArea` на отдельные части `Selection`, `Grid` и `Handle`, если области кадрирования
нужна другая структура или собственные дочерние элементы. Для `Grid` требуется `axis`, а для
`Handle` требуется `position`.

<ExampleFrame variant="component">
  <ImageCropperAdvancedCustomization />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs groupId="framework" values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]} className="docs-tabs">
    <Tab value="react">

```tsx pure file="../../../src/components/examples/image-cropper/advanced-customization.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={imageCropperSolidAdvancedCustomization} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

## Стилизация

Для структурных стилей используйте `className` в React и `class` в Solid на экспортируемых частях.
Варианты Tailwind используют нативные классы-утилиты и пользовательские классы, а варианты CSS Modules
настраиваются свойствами компонента ниже. Значения `data-slot` и атрибуты состояния Ark общие для
обоих способов стилизации.

### CSS-свойства

<div className="docs-reference">
  <Tabs
    values={[{ label: 'CSS-переменные', value: 'CSS Variables' }]}
    className="docs-reference-tabs"
    contentItemClassName="docs-reference-panel"
  >
    <Tab>
      <ImageCropperDocs.ImageCropperCssPropertiesPanel />
    </Tab>
  </Tabs>
</div>

### Селекторы для стилизации

Для локальной стилизации используйте стабильные значения `data-slot`: `image-cropper-root`,
`image-cropper-root-provider`, `image-cropper-viewport`, `image-cropper-image`,
`image-cropper-selection`, `image-cropper-grid` и `image-cropper-handle`. Ark также предоставляет
`data-scope="image-cropper"`, `data-part` и атрибуты состояния на соответствующих частях, включая
`data-fixed`, `data-shape`, `data-pinch`, `data-ready`, `data-disabled`, `data-measured`,
`data-dragging`, `data-panning`, `data-axis` и `data-position`.

Ark записывает в корневой элемент `--crop-width`, `--crop-height`, `--crop-x`, `--crop-y`,
`--image-zoom`, `--image-rotation`, `--image-offset-x` и `--image-offset-y`. Это значения только
для чтения, отражающие текущее состояние и подходящие для синхронизации собственного
интерфейса. Не переопределяйте их. Для визуальной настройки используйте описанные выше свойства
`--moduix-image-cropper-*`.
