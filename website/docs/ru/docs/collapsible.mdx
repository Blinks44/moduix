---
title: Collapsible
component: collapsible
description: Компонент для раскрытия и сворачивания одной области содержимого.
---

import * as CollapsibleDocs from '@/components/examples/collapsible/collapsible';
import CollapsibleBasic from '../../en/docs/_snippets/collapsible/basic.tsx';
import CollapsibleContext from '../../en/docs/_snippets/collapsible/context.tsx';
import CollapsibleControlled from '../../en/docs/_snippets/collapsible/controlled.tsx';
import CollapsibleDefaultOpen from '../../en/docs/_snippets/collapsible/default-open.tsx';
import CollapsibleDisabled from '../../en/docs/_snippets/collapsible/disabled.tsx';
import CollapsibleLazyMount from '../../en/docs/_snippets/collapsible/lazy-mount.tsx';
import CollapsibleNested from '../../en/docs/_snippets/collapsible/nested.tsx';
import CollapsiblePartialCollapse from '../../en/docs/_snippets/collapsible/partial-collapse.tsx';
import CollapsiblePartialCollapseWidth from '../../en/docs/_snippets/collapsible/partial-collapse-width.tsx';
import CollapsibleRootProvider from '../../en/docs/_snippets/collapsible/root-provider.tsx';
import CollapsibleCustomComposition from '../../en/docs/_snippets/collapsible/custom-composition.tsx';

## Справочник API

<PrimitiveReference href="https://ark-ui.com/docs/components/collapsible" />

## Какой компонент выбрать

Используйте `Collapsible` для одного независимого раскрывающегося блока. Если несколько связанных
секций должны использовать общую навигацию с клавиатуры или общее состояние, выбирайте `Accordion`.

## Базовый пример

Используйте `Collapsible` с частями `Trigger`, необязательным `Indicator` и `Content`, чтобы
раскрывать и сворачивать одну область содержимого. Размещайте содержимое внутри `Collapsible.Body`,
если нужна рекомендуемая внутренняя обёртка для отступов и размещения.

<ExampleFrame variant="component">
  <CollapsibleBasic />
</ExampleFrame>

<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx file="../../en/docs/_snippets/collapsible/basic.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/collapsible/collapsible-basic.module.css"
```

    </Tab>
  </Tabs>
</div>

## Установка через shadcn

<ShadcnInstall packageName="collapsible" />

## Анатомия

```text
Collapsible.Root
├─ Collapsible.Trigger
│  ├─ метка
│  └─ Collapsible.Indicator (необязательно)
└─ Collapsible.Content
   └─ Collapsible.Body
       └─ содержимое
```

| Часть                              | Назначение                                                               |
| ---------------------------------- | ------------------------------------------------------------------------ |
| `Collapsible` / `Collapsible.Root` | Управляет состоянием одного раскрывающегося блока.                       |
| `Collapsible.RootProvider`         | Подключает части к состоянию, созданному через `useCollapsible`.          |
| `Collapsible.Trigger`              | Доступная кнопка для раскрытия и сворачивания содержимого.                |
| `Collapsible.Indicator`            | Необязательный индикатор состояния с шевроном moduix по умолчанию.        |
| `Collapsible.Content`              | Анимируемая область, размер которой измеряет Ark.                        |
| `Collapsible.Body`                 | Внутренняя обёртка для отступов и размещения содержимого.                 |

## Композиция

### Рекомендуемая композиция

Начните с `Collapsible`, `Collapsible.Trigger`, `Collapsible.Content` и `Collapsible.Body`.
Добавляйте `Collapsible.Indicator`, если нужен визуальный признак состояния. Корневой компонент
принимает свойства `open`, `defaultOpen`, `onOpenChange(details)`, `disabled`, `collapsedHeight`,
`collapsedWidth`, `hideMode`, `lazyMount`, `unmountOnExit`, `ids` и `onExitComplete`.

Размещайте отступы и стили поверхности на `Collapsible.Body`, а не на `Collapsible.Content`. Ark
измеряет содержимое и предоставляет переменные `--height`, `--width`, `--collapsed-height` и
`--collapsed-width`.

При переходе с shadcn замените `CollapsibleTrigger` на `Collapsible.Trigger`, а `CollapsibleContent`
на `Collapsible.Content`. В moduix сохраняется объект `details` из Ark, поэтому в управляемом режиме
следующее состояние берите из `details.open`, а не из самого аргумента.

### Когда нужна своя композиция

Используйте `asChild`, если итоговый DOM-узел должен принадлежать другому семантическому элементу.
Часть добавляет к этому элементу поведение Ark и атрибуты состояния, а внешний вид остаётся под вашим
контролем. Пользовательский триггер должен оставаться доступным с клавиатуры и вести себя как кнопка.

Используйте `useCollapsible()` из `@moduix/react/collapsible` вместе с `Collapsible.RootProvider`, когда
состояние нужно создать за пределами дерева отрисованных компонентов. В moduix также доступны
`Collapsible.Context` и `useCollapsibleContext()`.

`Collapsible.Indicator` необязателен. Если не передать дочерние элементы, он выводит шеврон moduix,
направленный вниз. При необходимости передайте собственное содержимое.

### Свойства по умолчанию

| Свойство          | По умолчанию | Примечания                                                                    |
| ----------------- | ------------ | ----------------------------------------------------------------------------- |
| `collapsedHeight` | не задано    | Высота, которая сохраняется, когда содержимое свёрнуто.                       |
| `collapsedWidth`  | не задано    | Ширина, которая сохраняется, когда содержимое свёрнуто.                       |
| `defaultOpen`     | `false`      | При первом рендере область свёрнута.                                          |
| `disabled`        | `false`      | Триггер не может открыть или закрыть содержимое.                              |
| `hideMode`        | `display-none` | Смонтированное закрытое содержимое скрывается через `hidden`. `activity` требует React 19. |
| `lazyMount`       | `false`      | Содержимое монтируется при первом открытии.                                   |
| `open`            | не задано    | Управляемое состояние. Используйте вместе с `onOpenChange`.                  |
| `unmountOnExit`   | `false`      | После закрытия содержимое удаляется из DOM.                                   |

### Особенности поведения

- `onOpenChange` получает объект `details` от Ark. Следующее состояние находится в `details.open`.
- `open` описывает целевое состояние. `visible` остаётся равным `true`, пока закрывающееся содержимое
  смонтировано для анимации выхода.
- Триггер рендерится как нативная кнопка. Клавиши `Enter` и `Space` открывают и закрывают область.
- Не помещайте интерактивные элементы внутрь `Collapsible.Trigger`, чтобы триггер оставался кнопкой.
- При `hideMode="activity"` смонтированное закрытое содержимое использует режим Activity React 19,
  а его эффекты приостанавливаются. По умолчанию `hideMode="display-none"` используется атрибут
  `hidden`.
- Анимации содержимого и индикатора учитывают `prefers-reduced-motion`.

## Примеры

### Контекст

Используйте `useCollapsibleContext()` внутри дочернего компонента, которому нужно получить состояние
или программно закрыть область.

<ExampleFrame variant="component">
  <CollapsibleContext />
</ExampleFrame>

<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx file="../../en/docs/_snippets/collapsible/context.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/collapsible/collapsible-context.module.css"
```

    </Tab>
  </Tabs>
</div>

### Управляемое состояние

Используйте `open` с колбэком Ark `onOpenChange(details)`, когда состояние хранится в другой части
интерфейса.

<ExampleFrame variant="component">
  <CollapsibleControlled />
</ExampleFrame>

<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx file="../../en/docs/_snippets/collapsible/controlled.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/collapsible/collapsible-controlled.module.css"
```

    </Tab>
  </Tabs>
</div>

### Открыт по умолчанию

Задайте `defaultOpen`, чтобы неуправляемый компонент изначально был открыт.

<ExampleFrame variant="component">
  <CollapsibleDefaultOpen />
</ExampleFrame>

<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx file="../../en/docs/_snippets/collapsible/default-open.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/collapsible/collapsible-default-open.module.css"
```

    </Tab>
  </Tabs>
</div>

### Отключённое состояние

Задайте `disabled` корневому компоненту, чтобы запретить открытие и закрытие области.

<ExampleFrame variant="component">
  <CollapsibleDisabled />
</ExampleFrame>

<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx file="../../en/docs/_snippets/collapsible/disabled.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/collapsible/collapsible-disabled.module.css"
```

    </Tab>
  </Tabs>
</div>

### Отложенное монтирование

Установите `lazyMount`, чтобы монтировать содержимое только после первого открытия. Добавьте
`unmountOnExit`, чтобы удалять его из DOM после закрытия.

<ExampleFrame variant="component">
  <CollapsibleLazyMount />
</ExampleFrame>

<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx file="../../en/docs/_snippets/collapsible/lazy-mount.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/collapsible/collapsible-lazy-mount.module.css"
```

    </Tab>
  </Tabs>
</div>

### Вложенные компоненты

Если внутри раскрытой секции нужен ещё один необязательный блок, вкладывайте независимые корневые компоненты.

<ExampleFrame variant="component">
  <CollapsibleNested />
</ExampleFrame>

<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx file="../../en/docs/_snippets/collapsible/nested.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/collapsible/collapsible-nested.module.css"
```

    </Tab>
  </Tabs>
</div>

### Частичное сворачивание

Укажите `collapsedHeight` или `collapsedWidth`, чтобы после закрытия часть содержимого оставалась видимой.
Интерактивные элементы в этой области становятся недоступными для взаимодействия и навигации с
клавиатуры, пока компонент свёрнут. Поэтому действия, которые должны оставаться доступными, размещайте
за пределами `Collapsible.Content`.

<ExampleFrame variant="component">
  <CollapsiblePartialCollapse />
</ExampleFrame>

<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx file="../../en/docs/_snippets/collapsible/partial-collapse.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/collapsible/collapsible-partial-collapse.module.css"
```

    </Tab>
  </Tabs>
</div>

### Частичная ширина

Используйте `collapsedWidth`, когда удобнее оставлять видимой часть содержимого по ширине, а не по высоте.

<ExampleFrame variant="component">
  <CollapsiblePartialCollapseWidth />
</ExampleFrame>

<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx file="../../en/docs/_snippets/collapsible/partial-collapse-width.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/collapsible/collapsible-partial-collapse-width.module.css"
```

    </Tab>
  </Tabs>
</div>

### Root Provider

Создайте состояние через `useCollapsible()` и передайте его в `Collapsible.RootProvider`, если код за
пределами дерева отрисованных компонентов должен обращаться к этому состоянию.

<ExampleFrame variant="component">
  <CollapsibleRootProvider />
</ExampleFrame>

<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx file="../../en/docs/_snippets/collapsible/root-provider.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/collapsible/collapsible-root-provider.module.css"
```

    </Tab>
  </Tabs>
</div>

### Расширенная настройка

Используйте `asChild`, чтобы передать поведение триггера кнопке, которую стилизует ваше приложение.
Стилизация `Collapsible.Trigger` по умолчанию не применяется, но поведение Ark и атрибуты состояния сохраняются.

<ExampleFrame variant="component">
  <CollapsibleCustomComposition />
</ExampleFrame>

<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx file="../../en/docs/_snippets/collapsible/custom-composition.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/collapsible/collapsible-custom-composition.module.css"
```

    </Tab>
  </Tabs>
</div>

## Стилизация

Используйте `className` у экспортируемых частей для структурных стилей. Для стилей, зависящих от
состояния, предпочитайте атрибуты данных Ark, например `data-state` и `data-disabled`, вместе с
селекторами `data-slot` библиотеки moduix.

### CSS-свойства

<CssPropertiesSection properties={CollapsibleDocs.collapsibleOverrideCssProperties} />

### Хуки стилизации

- Части DOM предоставляют `data-slot="collapsible-root"`, `collapsible-root-provider`,
  `collapsible-trigger`, `collapsible-indicator`, `collapsible-content` и `collapsible-body`.
- Части Ark предоставляют `data-scope="collapsible"` и значения `data-part` для корня, триггера,
  индикатора и содержимого.
- Ark добавляет `data-state="open" | "closed"` к корневому компоненту, триггеру, индикатору и содержимому.
- Ark добавляет `data-collapsible` к части `Content`.
- `data-disabled` помечает триггер, индикатор и содержимое, если они отключены.
- `data-has-collapsed-size` помечает содержимое, настроенное через `collapsedHeight` или `collapsedWidth`.
- Для анимации содержимого используются переменные Ark `--height`, `--width`, `--collapsed-height` и `--collapsed-width`.
