---
title: Collapsible
component: collapsible
description: Доступный компонент для раскрытия и сворачивания одной области содержимого в React и Solid.
---

import * as CollapsibleDocs from '@/components/examples/collapsible/css-properties';
import CollapsibleBasic from '@/components/examples/collapsible/basic.tsx';
import CollapsibleContext from '@/components/examples/collapsible/context.tsx';
import CollapsibleControlled from '@/components/examples/collapsible/controlled.tsx';
import CollapsibleDefaultOpen from '@/components/examples/collapsible/default-open.tsx';
import CollapsibleDisabled from '@/components/examples/collapsible/disabled.tsx';
import CollapsibleLazyMount from '@/components/examples/collapsible/lazy-mount.tsx';
import CollapsibleNested from '@/components/examples/collapsible/nested.tsx';
import CollapsiblePartialCollapse from '@/components/examples/collapsible/partial-collapse.tsx';
import CollapsiblePartialCollapseWidth from '@/components/examples/collapsible/partial-collapse-width.tsx';
import CollapsibleRootProvider from '@/components/examples/collapsible/root-provider.tsx';
import CollapsibleCustomComposition from '@/components/examples/collapsible/custom-composition.tsx';
import collapsibleSolidBasic from '../../../snippets/collapsible/solid/basic.tsx?raw';
import collapsibleSolidContext from '../../../snippets/collapsible/solid/context.tsx?raw';
import collapsibleSolidControlled from '../../../snippets/collapsible/solid/controlled.tsx?raw';
import collapsibleSolidDefaultOpen from '../../../snippets/collapsible/solid/default-open.tsx?raw';
import collapsibleSolidDisabled from '../../../snippets/collapsible/solid/disabled.tsx?raw';
import collapsibleSolidLazyMount from '../../../snippets/collapsible/solid/lazy-mount.tsx?raw';
import collapsibleSolidNested from '../../../snippets/collapsible/solid/nested.tsx?raw';
import collapsibleSolidPartialCollapse from '../../../snippets/collapsible/solid/partial-collapse.tsx?raw';
import collapsibleSolidPartialCollapseWidth from '../../../snippets/collapsible/solid/partial-collapse-width.tsx?raw';
import collapsibleSolidRootProvider from '../../../snippets/collapsible/solid/root-provider.tsx?raw';
import collapsibleSolidCustomComposition from '../../../snippets/collapsible/solid/custom-composition.tsx?raw';

## Справочник API

<PrimitiveReference href="https://ark-ui.com/docs/components/collapsible" />

## Какой компонент выбрать

Используйте `Collapsible` для одного независимого раскрывающегося блока. Если несколько связанных
секций должны использовать общую навигацию с клавиатуры или общее состояние, выбирайте `Accordion`.

## Базовый пример


Используйте `Collapsible` с частями `Trigger`, необязательным `Indicator` и `Content`, чтобы
раскрывать и сворачивать одну область содержимого. Размещайте содержимое внутри `Collapsible.Body`,
если нужна рекомендуемая внутренняя обёртка для отступов и размещения.

<ExampleFrame variant="component">
  <CollapsibleBasic />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/collapsible/basic.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={collapsibleSolidBasic} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/collapsible/collapsible-basic.module.css"
```

</div>

</ExampleCode>

## Установка через shadcn

Полную настройку пакета, реестра и стилей см. в разделе [Быстрый старт](/docs/quick-start).

<Tabs groupId="framework">
  <Tab label="React" value="react">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-react/collapsible' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-react-tailwind/collapsible' },
  ]}
/>

  </Tab>
  <Tab label="Solid" value="solid">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-solid/collapsible' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-solid-tailwind/collapsible' },
  ]}
/>

  </Tab>
</Tabs>

## Анатомия

```text
Collapsible / Collapsible.Root
├─ Collapsible.Trigger
│  ├─ метка
│  └─ Collapsible.Indicator (необязательно)
└─ Collapsible.Content
   └─ Collapsible.Body
       └─ содержимое

Collapsible.RootProvider
└─ то же дерево частей, подключённое к состоянию из useCollapsible()
```

| Часть                              | Назначение                                                               |
| ---------------------------------- | ------------------------------------------------------------------------ |
| `Collapsible` / `Collapsible.Root` | Управляет состоянием одного раскрывающегося блока.                       |
| `Collapsible.RootProvider`         | Подключает части к состоянию, созданному через `useCollapsible`.          |
| `Collapsible.Trigger`              | Доступная кнопка для раскрытия и сворачивания содержимого.                |
| `Collapsible.Indicator`            | Необязательный индикатор состояния с шевроном moduix по умолчанию.        |
| `Collapsible.Content`              | Анимируемая область, размер которой измеряет Ark.                        |
| `Collapsible.Body`                 | Внутренняя обёртка для отступов и размещения содержимого.                 |

## Композиция

`Collapsible` хранит состояние в корневом компоненте. Используйте `defaultOpen` для неконтролируемого
раскрытия, а `open` с `onOpenChange(details)`, если состоянием управляет другая часть приложения.
Колбэк сохраняет объект `details` из Ark, поэтому управляемое состояние нужно брать из `details.open`.

Используйте `Collapsible.RootProvider` вместе с `useCollapsible()` из пакета нужного фреймворка, если
хранилище нужно создать вне дерева или разделить между границами композиции. Передайте это хранилище
в `RootProvider` и не рендерите второй корневой `Collapsible` для того же состояния.

Размещайте отступы и стили поверхности на `Collapsible.Body`, а не на `Collapsible.Content`, чтобы Ark
корректно измерял анимируемую область. `Collapsible.Indicator` необязателен и по умолчанию показывает
шеврон moduix.

Используйте `asChild`, только если поведение триггера должен получить семантический элемент приложения.
Он должен оставаться доступной с клавиатуры кнопкой. В Solid передайте функцию рендера, например
`asChild={(props) => <button {...props()} />}`.

## Примеры

### Контекст

Используйте `useCollapsibleContext()` внутри дочернего компонента, которому нужно получить состояние
или программно закрыть область.

<ExampleFrame variant="component">
  <CollapsibleContext />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/collapsible/context.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={collapsibleSolidContext} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/collapsible/collapsible-context.module.css"
```

</div>

</ExampleCode>

### Управляемое состояние

Используйте `open` с колбэком Ark `onOpenChange(details)`, когда состояние хранится в другой части
интерфейса.

<ExampleFrame variant="component">
  <CollapsibleControlled />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/collapsible/controlled.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={collapsibleSolidControlled} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/collapsible/collapsible-controlled.module.css"
```

</div>

</ExampleCode>

### Открыт по умолчанию

Задайте `defaultOpen`, чтобы неуправляемый компонент изначально был открыт.

<ExampleFrame variant="component">
  <CollapsibleDefaultOpen />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/collapsible/default-open.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={collapsibleSolidDefaultOpen} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/collapsible/collapsible-default-open.module.css"
```

</div>

</ExampleCode>

### Отключённое состояние

Задайте `disabled` корневому компоненту, чтобы запретить открытие и закрытие области.

<ExampleFrame variant="component">
  <CollapsibleDisabled />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/collapsible/disabled.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={collapsibleSolidDisabled} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/collapsible/collapsible-disabled.module.css"
```

</div>

</ExampleCode>

### Отложенное монтирование

Установите `lazyMount`, чтобы монтировать содержимое только после первого открытия. Добавьте
`unmountOnExit`, чтобы удалять его из DOM после закрытия.

<ExampleFrame variant="component">
  <CollapsibleLazyMount />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/collapsible/lazy-mount.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={collapsibleSolidLazyMount} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/collapsible/collapsible-lazy-mount.module.css"
```

</div>

</ExampleCode>

### Вложенные компоненты

Если внутри раскрытой секции нужен ещё один необязательный блок, вкладывайте независимые корневые компоненты.

<ExampleFrame variant="component">
  <CollapsibleNested />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/collapsible/nested.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={collapsibleSolidNested} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/collapsible/collapsible-nested.module.css"
```

</div>

</ExampleCode>

### Частичное сворачивание

Укажите `collapsedHeight` или `collapsedWidth`, чтобы после закрытия часть содержимого оставалась видимой.
Интерактивные элементы в этой области становятся недоступными для взаимодействия и навигации с
клавиатуры, пока компонент свёрнут. Поэтому действия, которые должны оставаться доступными, размещайте
за пределами `Collapsible.Content`.

<ExampleFrame variant="component">
  <CollapsiblePartialCollapse />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/collapsible/partial-collapse.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={collapsibleSolidPartialCollapse} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/collapsible/collapsible-partial-collapse.module.css"
```

</div>

</ExampleCode>

### Частичная ширина

Используйте `collapsedWidth`, когда удобнее оставлять видимой часть содержимого по ширине, а не по высоте.

<ExampleFrame variant="component">
  <CollapsiblePartialCollapseWidth />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/collapsible/partial-collapse-width.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={collapsibleSolidPartialCollapseWidth} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/collapsible/collapsible-partial-collapse-width.module.css"
```

</div>

</ExampleCode>

### Root Provider

Создайте состояние через `useCollapsible()` и передайте его в `Collapsible.RootProvider`, если код за
пределами дерева отрисованных компонентов должен обращаться к этому состоянию.

<ExampleFrame variant="component">
  <CollapsibleRootProvider />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/collapsible/root-provider.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={collapsibleSolidRootProvider} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/collapsible/collapsible-root-provider.module.css"
```

</div>

</ExampleCode>

### Расширенная настройка

Используйте `asChild`, чтобы передать поведение триггера кнопке, которую стилизует ваше приложение.
Стилизация `Collapsible.Trigger` по умолчанию не применяется, но поведение Ark и атрибуты состояния сохраняются.

<ExampleFrame variant="component">
  <CollapsibleCustomComposition />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/collapsible/custom-composition.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={collapsibleSolidCustomComposition} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/collapsible/collapsible-custom-composition.module.css"
```

</div>

</ExampleCode>

## Стилизация

Используйте `className` в React или `class` в Solid у экспортируемых частей для структурных стилей.
Для стилей, зависящих от состояния, предпочитайте атрибуты данных Ark, например `data-state` и
`data-disabled`, вместе с селекторами `data-slot` библиотеки moduix. Варианты на Tailwind используют
нативные утилиты и классы приложения, а переменные ниже настраивают варианты с CSS Modules. Атрибуты
данных и хуки `data-slot` общие для обоих способов стилизации.

Размещайте отступы на `Collapsible.Body`, а не на `Collapsible.Content`, чтобы Ark мог корректно
измерять и анимировать содержимое.

### CSS-свойства

<CssPropertiesSection properties={CollapsibleDocs.collapsibleOverrideCssProperties} />

### Хуки стилизации

- Части DOM предоставляют `data-slot="collapsible-root"`, `collapsible-root-provider`,
  `collapsible-trigger`, `collapsible-indicator`, `collapsible-content` и `collapsible-body`.
- Части Ark предоставляют `data-scope="collapsible"` и значения `data-part` для корня, триггера,
  индикатора и содержимого.
- Ark добавляет `data-state="open" | "closed"` к корневому компоненту, триггеру, индикатору и содержимому.
- Ark добавляет `data-collapsible` к части `Content`.
- `data-disabled` помечает триггер, индикатор и содержимое, если они отключены.
- `data-has-collapsed-size` помечает содержимое, настроенное через `collapsedHeight` или `collapsedWidth`.
- Для анимации содержимого используются переменные Ark `--height`, `--width`, `--collapsed-height` и `--collapsed-width`.
