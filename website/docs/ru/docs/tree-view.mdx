---
title: Tree View
component: tree-view
description: Доступное дерево на базе Ark UI для папок, файлов и любых вложенных данных.
---

import * as TreeViewDocs from '@/components/examples/tree-view/css-properties';
import AsyncTreeView from '@/components/examples/tree-view/async-loading.tsx';
import CheckboxTreeView from '@/components/examples/tree-view/checkboxes.tsx';
import ControlledSelectionTreeView from '@/components/examples/tree-view/controlled-selection.tsx';
import FileIconsTreeView from '@/components/examples/tree-view/file-icons.tsx';
import LinkedTreeView from '@/components/examples/tree-view/links.tsx';
import RootProviderTreeView from '@/components/examples/tree-view/root-provider.tsx';
import TreeViewBasic from '@/components/examples/tree-view/basic.tsx';
import treeViewSolidAsyncLoading from '../../../snippets/tree-view/solid/async-loading.tsx?raw';
import treeViewSolidBasic from '../../../snippets/tree-view/solid/basic.tsx?raw';
import treeViewSolidCheckboxes from '../../../snippets/tree-view/solid/checkboxes.tsx?raw';
import treeViewSolidControlledSelection from '../../../snippets/tree-view/solid/controlled-selection.tsx?raw';
import treeViewSolidFileIcons from '../../../snippets/tree-view/solid/file-icons.tsx?raw';
import treeViewSolidLinks from '../../../snippets/tree-view/solid/links.tsx?raw';
import treeViewSolidRootProvider from '../../../snippets/tree-view/solid/root-provider.tsx?raw';

## Справочник API

<PrimitiveReference href="https://ark-ui.com/docs/components/tree-view" />

## Базовый пример


Создайте коллекцию данных с помощью `createTreeCollection`, затем передайте её в `TreeView`.
`TreeView.Node` передаёт в рекурсивный рендерер текущие `node`, `indexPath` и `state`, поэтому можно
вывести ветку или конечный узел, сохранив явную структуру частей.

<ExampleFrame variant="component">
  <TreeViewBasic />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/tree-view/basic.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={treeViewSolidBasic} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

## Установка через shadcn

Полную настройку пакета, реестра и стилей см. в разделе [Быстрый старт](/docs/quick-start).

<Tabs groupId="framework">
  <Tab label="React" value="react">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-react/tree-view' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-react-tailwind/tree-view' },
  ]}
/>

  </Tab>
  <Tab label="Solid" value="solid">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-solid/tree-view' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-solid-tailwind/tree-view' },
  ]}
/>

  </Tab>
</Tabs>

## Анатомия

```text
TreeView / TreeView.Root
├─ TreeView.Label
└─ TreeView.Tree
   └─ TreeView.Node или TreeView.NodeProvider
      ├─ TreeView.Branch
      │  ├─ TreeView.BranchControl
      │  │  ├─ TreeView.BranchIndicator
      │  │  └─ TreeView.BranchText
      │  ├─ TreeView.BranchTrigger (опционально, отдельный триггер)
      │  └─ TreeView.BranchContent
      │     ├─ TreeView.BranchIndentGuide
      │     └─ вложенные узлы
      └─ TreeView.Item
         └─ TreeView.ItemText
```

| Часть                            | Назначение                                                            |
| -------------------------------- | --------------------------------------------------------------------- |
| `TreeView` / `TreeView.Root`     | Хранит коллекцию и управляет раскрытием, выбором, фокусом, флажками и переименованием. |
| `TreeView.RootProvider`          | Связывает части дерева с экземпляром, созданным через `useTreeView`. |
| `TreeView.Label`                 | Задаёт доступное имя дерева.                                         |
| `TreeView.Tree`                  | Создаёт контейнер дерева с ARIA-разметкой.                           |
| `TreeView.Node`                  | Упрощённый провайдер с `{ node, indexPath, state }`.                 |
| `TreeView.NodeProvider`          | Низкоуровневая граница контекста Ark для нестандартной композиции.   |
| `TreeView.Branch*`               | Раскрываемый узел, его элемент управления, индикатор, текст и содержимое. |
| `TreeView.Item*`                 | Конечный узел и необязательный индикатор выбранного состояния.       |
| `TreeView.NodeCheckbox*`         | Флажок и индикатор отмеченного или частично отмеченного состояния.   |
| `TreeView.NodeRenameInput`       | Поле ввода, которое появляется при переименовании узла.              |

## Композиция

Для рекурсивного рендера используйте `TreeView.Node`. Он передаёт `node`, `indexPath` и текущее
состояние узла, а вы выбираете части `Branch` или `Item`. Если вашему рекурсивному компоненту нужен
прямой доступ к контексту узла, используйте `TreeView.NodeProvider`, а не дополнительную обёртку над
`Node`. Когда состояние дерева должно быть доступно внешним элементам управления, создайте его через
`useTreeView` и передайте в `TreeView.RootProvider`; не создавайте второй root для того же хранилища.
Если у ветки есть отдельный `BranchTrigger`, задайте `role="none"` для `BranchControl`.

## Примеры

### Ссылки

Используйте `asChild` у `TreeView.Item`, когда конечный узел должен быть одним семантическим
элементом, например ссылкой. Этот элемент должен принимать props Ark, чтобы сохранить фокус и
клавиатурное управление.

<ExampleFrame variant="component">
  <LinkedTreeView />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/tree-view/links.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={treeViewSolidLinks} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Асинхронная загрузка

Если у ветки известно значение `childrenCount`, загрузите дочерние узлы через `loadChildren`. В
`onLoadChildrenComplete` замените коллекцию в состоянии приложения значением `details.collection`.

<ExampleFrame variant="component">
  <AsyncTreeView />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/tree-view/async-loading.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={treeViewSolidAsyncLoading} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Дерево с флажками

Разместите `NodeCheckbox` и в элементах управления ветками, и в конечных узлах. Индикатор по
умолчанию уже отображает отмеченное и частично отмеченное состояния.

<ExampleFrame variant="component">
  <CheckboxTreeView />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/tree-view/checkboxes.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={treeViewSolidCheckboxes} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Значки файлов

Помещайте декоративные значки файлов и папок внутрь `BranchText` и `ItemText`. Оставьте
`BranchIndicator` отдельным элементом, чтобы он отвечал только за раскрытие и сворачивание.

<ExampleFrame variant="component">
  <FileIconsTreeView />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/tree-view/file-icons.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={treeViewSolidFileIcons} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Множественный выбор

Задайте `selectionMode="multiple"`. Используйте Ctrl или ⌘, чтобы добавлять и убирать отдельные
элементы, и Shift, чтобы расширять диапазон выбора. В примере выбранные значения хранятся в состоянии
приложения.

<ExampleFrame variant="component">
  <ControlledSelectionTreeView />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/tree-view/controlled-selection.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={treeViewSolidControlledSelection} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/tree-view/tree-view-controlled-selection.module.css"
```
</div>

</ExampleCode>

### Root provider

Создайте состояние дерева через `useTreeView`, если кнопкам и другим элементам вне дерева нужны
методы вроде `expand()` или `collapse()`. Передайте состояние в `RootProvider`, а не создавайте второй
`TreeView.Root`.

<ExampleFrame variant="component">
  <RootProviderTreeView />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/tree-view/root-provider.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={treeViewSolidRootProvider} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/tree-view/tree-view-root-provider.module.css"
```
</div>

</ExampleCode>

## Стилизация

Используйте `className` в React или `class` в Solid на любой экспортируемой части для структурной
стилизации. Части ветки имеют `data-state="open|closed"`, части флажка имеют
`data-state="checked|unchecked|indeterminate"`, а части узлов имеют `data-focus`, `data-selected`,
`data-disabled`, `data-loading`, `data-renaming`, `data-depth`, `data-path` и `data-value`. Обёртка
использует переменные Ark `--depth` для отступов и `--height` для высоты при сворачивании, а также
отключает анимацию веток при предпочтении уменьшенного движения.

Варианты Tailwind используют нативные utility-классы и классы приложения. Варианты CSS Modules
предоставляют документированные переменные `--moduix-tree-view-*` ниже.

<div className="docs-reference">
  <Tabs
    values={[{ label: 'CSS Variables', value: 'CSS Variables' }]}
    className="docs-reference-tabs"
    contentItemClassName="docs-reference-panel"
  >
    <Tab>
      <TreeViewDocs.TreeViewCssPropertiesPanel />
    </Tab>
  </Tabs>
</div>

### Точки для стилизации

- `data-slot="tree-view-root"`, `tree-view-root-provider`, `tree-view-label`, `tree-view-tree`,
  `tree-view-branch`, `tree-view-branch-control`, `tree-view-branch-trigger`,
  `tree-view-branch-indicator`, `tree-view-branch-text`, `tree-view-branch-content`,
  `tree-view-branch-indent-guide`, `tree-view-item`, `tree-view-item-text`,
  `tree-view-item-indicator`, `tree-view-node-checkbox`, `tree-view-node-checkbox-indicator` и
  `tree-view-node-rename-input`.
- Для визуальной настройки вариантов CSS Modules используйте документированные переменные
  `--moduix-tree-view-*`.
