---
title: Badge
component: badge
description: "Компактная метка для краткой информации: статуса, категории, версии или количества."
---

import * as BadgeDocs from '@/components/examples/badge/css-properties';
import BadgeBasic from '@/components/examples/badge/basic.tsx';
import BadgeCustomStyling from '@/components/examples/badge/custom-styling.tsx';
import BadgeLink from '@/components/examples/badge/link.tsx';
import BadgeLongLabels from '@/components/examples/badge/long-labels.tsx';
import BadgeVariants from '@/components/examples/badge/variants.tsx';
import BadgeWithDot from '@/components/examples/badge/with-dot.tsx';
import BadgeWithIcon from '@/components/examples/badge/with-icon.tsx';
import badgeSolidBasic from '../../../snippets/badge/solid/basic.tsx?raw';
import badgeSolidCustomStyling from '../../../snippets/badge/solid/custom-styling.tsx?raw';
import badgeSolidLink from '../../../snippets/badge/solid/link.tsx?raw';
import badgeSolidLongLabels from '../../../snippets/badge/solid/long-labels.tsx?raw';
import badgeSolidVariants from '../../../snippets/badge/solid/variants.tsx?raw';
import badgeSolidWithDot from '../../../snippets/badge/solid/with-dot.tsx?raw';
import badgeSolidWithIcon from '../../../snippets/badge/solid/with-icon.tsx?raw';

## Справочник API

В Ark UI нет отдельного примитива `Badge`. В moduix компонент построен с помощью официальной
[фабрики Ark](https://ark-ui.com/docs/guides/composition#the-ark-factory) и следует рекомендациям Ark по
[композиции](https://ark-ui.com/docs/guides/composition), [стилизации](https://ark-ui.com/docs/guides/styling) и работе с
[ref](https://ark-ui.com/docs/guides/ref).

## Какой компонент выбрать

Используйте `Badge`, чтобы показать краткую информацию о другом объекте. Это компонент только для отображения,
поэтому он не заменяет элемент управления, с помощью которого пользователь выбирает или изменяет значение.

- Выбирайте **`Badge`** для коротких метаданных: статуса, категории, версии или количества.
- Выбирайте **`Tag`** для выбранных значений, фильтров, назначений или меток, которые относятся к текущему состоянию интерфейса.

## Базовый пример

Badge доступен для React и Solid как в пакетах с CSS Modules, так и в вариантах на Tailwind. Интерактивный
пример на этой странице использует React и CSS Modules, а вкладки с кодом показывают нативную реализацию
того же публичного API для каждого фреймворка.

Начните с обычного `<Badge>`. По умолчанию компонент отображается как `span` и не добавляет интерактивного
поведения. Укажите `variant`, чтобы выбрать `default`, `secondary`, `destructive`, `outline`, `ghost` или `link`,
и добавляйте `Badge.Dot`, только если статусу нужен визуальный индикатор.

<ExampleFrame variant="component">
  <BadgeBasic />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/badge/basic.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={badgeSolidBasic} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

## Установка через shadcn

Полную настройку пакета, реестра и стилей см. в разделе [Быстрый старт](/docs/quick-start).

<Tabs groupId="framework">
  <Tab label="React" value="react">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-react/badge' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-react-tailwind/badge' },
  ]}
/>

  </Tab>
  <Tab label="Solid" value="solid">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-solid/badge' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-solid-tailwind/badge' },
  ]}
/>

  </Tab>
</Tabs>

## Анатомия

Запись `Badge` - это сокращённая форма `Badge.Root`. У компонента небольшой публичный API, состоящий только
из видимых частей.

```text
Badge / Badge.Root
├─ Badge.Label (необязательно; используйте явно для усечения)
├─ Badge.Dot (необязательно)
└─ svg-иконка (необязательно)
```

| Часть                  | Назначение                                                                              |
| ---------------------- | --------------------------------------------------------------------------------------- |
| `Badge` / `Badge.Root` | Корневая часть. Отвечает за вариант, границу и отступы.                                |
| `Badge.Label`          | Явная часть с текстом. Позволяет обрезать составную подпись.                             |
| `Badge.Dot`            | Декоративная часть. Рисует круглый индикатор, который наследует цвет текста.             |

## Композиция

`Badge` отображает данные и не владеет состоянием. Обычный дочерний текст остаётся без изменений;
используйте `Badge.Label` явно, если подпись нужно усечь в ограниченном пространстве или объединить с
`Badge.Dot` либо иконкой. Для семантической ссылки или кнопки в React передайте в `asChild` ровно один
дочерний элемент. В Solid передайте в `asChild` функцию рендера - созданный элемент сохранит нативную
семантику. `Badge.Root` - вариант `Badge` с явным пространством имён.

## Примеры

### Пользовательская стилизация

Если готовый вариант почти подходит, но требует небольшой правки, настройте бейдж с помощью класса или
встроенных CSS-переменных.

<ExampleFrame variant="component">
  <BadgeCustomStyling />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/badge/custom-styling.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={badgeSolidCustomStyling} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/badge/badge-custom-styling.module.css"
```
</div>

</ExampleCode>

### Ссылка

Используйте `variant="link"` вместе с `asChild`, когда бейдж должен быть ссылкой в строке метаданных. Дочерний
элемент останется настоящей ссылкой и сохранит свою нативную семантику.

<ExampleFrame variant="component">
  <BadgeLink />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/badge/link.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={badgeSolidLink} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Длинные подписи

Оберните длинное значение в `Badge.Label`, чтобы усечь его внутри ограниченного по ширине бейджа. Добавьте
`title`, чтобы полное значение можно было увидеть при наведении курсора. На сенсорных устройствах выбирайте
подпись, которая помещается в доступное пространство.

<ExampleFrame variant="component">
  <BadgeLongLabels />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/badge/long-labels.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={badgeSolidLongLabels} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/badge/badge-long-labels.module.css"
```
</div>

</ExampleCode>

### Варианты

Используйте готовые варианты оформления для распространённых случаев.

<ExampleFrame variant="component">
  <BadgeVariants />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/badge/variants.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={badgeSolidVariants} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/badge/badge-variants.module.css"
```
</div>

</ExampleCode>

### С точкой

Добавляйте `Badge.Dot`, когда рядом с подписью нужен визуально связанный индикатор статуса. Смысл статуса должен
оставаться в видимом тексте, потому что точка декоративна.

<ExampleFrame variant="component">
  <BadgeWithDot />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/badge/with-dot.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={badgeSolidWithDot} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/badge/badge-with-dot.module.css"
```
</div>

</ExampleCode>

### С иконкой

Размещайте дочернюю SVG-иконку рядом с подписью, когда метаданным нужна дополнительная визуальная подсказка.

<ExampleFrame variant="component">
  <BadgeWithIcon />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/badge/with-icon.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={badgeSolidWithIcon} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/badge/badge-with-icon.module.css"
```
</div>

</ExampleCode>

## Стилизация

Начните с готового варианта оформления. Для разовой настройки размера или внешнего вида задайте публичные
переменные `--moduix-badge-*` на бейдже через класс или встроенный стиль. Используйте переменные
`--moduix-badge-<variant>-*`, чтобы настроить вариант во всём приложении. Общие переменные
`--moduix-badge-bg`, `--moduix-badge-border-color` и `--moduix-badge-color` переопределяют значения варианта
для отдельного бейджа. Таблица CSS-переменных относится к пакетам с CSS Modules; в пакетах Tailwind используются
обычные утилитарные классы и классы приложения.

Для структурных стилей передавайте экспортируемым частям `className` в React или `class` в Solid. Стабильные
хуки `data-slot` и атрибуты данных Ark общие для обоих подходов.

### CSS-свойства

<CssPropertiesSection properties={BadgeDocs.badgeCssProperties} />

### Хуки стилизации

- `data-slot="badge-root"`, `data-slot="badge-label"` и `data-slot="badge-dot"` - стабильные селекторы экспортируемых частей.
- `Badge.Root` также предоставляет `data-scope="badge"` и `data-part="root"`. `Badge.Label` и `Badge.Dot` предоставляют `data-part="label"` и `data-part="dot"`.
- `Badge.Root` записывает выбранный вариант в `data-variant`.
- Фокусируемые элементы, созданные через `Badge asChild` или `Badge.Root asChild`, получают стандартное кольцо фокуса. Корневые ссылки также получают стили наведения и курсор-указатель; оформление взаимодействия кнопок остаётся ответственностью приложения.
- Дочерние `svg`-иконки наследуют `currentColor`, не получают события указателя и используют `--moduix-badge-icon-size`.
- У Badge нет отдельного примитива или машины поведения Ark, поэтому он не предоставляет управляемые Ark-состояния и runtime CSS-переменные.
