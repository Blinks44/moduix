---
title: Chart
component: chart
description: Создавайте доступные адаптивные графики с TanStack Charts и оформляйте их с помощью figure, легенды, подсказки и анимации moduix.
---

import * as ChartDocs from '@/components/examples/chart/css-properties';
import ChartAdvancedCustomization from '@/components/examples/chart/advanced-customization.tsx';
import ChartAnimatedArea from '@/components/examples/chart/animated-area.tsx';
import ChartBasic from '@/components/examples/chart/basic.tsx';
import ChartDonut from '@/components/examples/chart/donut.tsx';
import ChartHeatmap from '@/components/examples/chart/heatmap.tsx';
import ChartHorizontalBars from '@/components/examples/chart/horizontal-bars.tsx';
import ChartColors from '@/components/examples/chart/line-colors.tsx';
import ChartScatter from '@/components/examples/chart/scatter.tsx';
import ChartStackedBars from '@/components/examples/chart/stacked-bars.tsx';
import chartSolidAdvancedCustomization from '../../../snippets/chart/solid/advanced-customization.tsx?raw';
import chartSolidAnimatedArea from '../../../snippets/chart/solid/animated-area.tsx?raw';
import chartSolidBasic from '../../../snippets/chart/solid/basic.tsx?raw';
import chartSolidDonut from '../../../snippets/chart/solid/donut.tsx?raw';
import chartSolidHeatmap from '../../../snippets/chart/solid/heatmap.tsx?raw';
import chartSolidHorizontalBars from '../../../snippets/chart/solid/horizontal-bars.tsx?raw';
import chartSolidColors from '../../../snippets/chart/solid/line-colors.tsx?raw';
import chartSolidScatter from '../../../snippets/chart/solid/scatter.tsx?raw';
import chartSolidStackedBars from '../../../snippets/chart/solid/stacked-bars.tsx?raw';

## Справочник API

В Ark UI нет отдельного примитива для графиков. moduix добавляет семантические `figure`, заголовок,
описание, легенду и компактное тело подсказки вокруг [API графиков TanStack
Charts](https://tanstack.com/charts/latest/docs/framework/react/reference/chart). Графические
элементы, шкалы, взаимодействия и колбэки вы по-прежнему задаёте средствами TanStack Charts, а
moduix предоставляет доступную оболочку композиции.

`@tanstack/charts` - необязательная peer-зависимость moduix. Пакеты для React и Solid используют
одинаковый API определения графика; пакет Solid работает через адаптер рендерера TanStack.

## Базовый пример


Создайте определение графика в TanStack Charts и передайте его в `ChartPlot`. Добавьте вокруг
графика видимые заголовок, описание и легенду, а для пользователей вспомогательных технологий
задайте короткое доступное имя в `ariaLabel`.

<ExampleFrame variant="component">
  <ChartBasic />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/chart/basic.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={chartSolidBasic} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

## Установка через shadcn

Полную настройку пакета, реестра и стилей см. в разделе [Быстрый старт](/docs/quick-start).

<Tabs groupId="framework">
  <Tab label="React" value="react">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-react/chart' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-react-tailwind/chart' },
  ]}
/>

  </Tab>
  <Tab label="Solid" value="solid">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-solid/chart' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-solid-tailwind/chart' },
  ]}
/>

  </Tab>
</Tabs>

## Анатомия

```text
Chart
├─ ChartHeader (необязательный figcaption)
│  ├─ ChartTitle
│  └─ ChartDescription
├─ ChartPlot
│  └─ Поверхность графика TanStack (по умолчанию SVG)
└─ ChartLegend (необязательно)
   └─ ChartLegendItem
```

| Часть | Назначение |
| --- | --- |
| `Chart` | Составной корневой элемент `figure`. Принимает нативные свойства, `asChild` и ref на `HTMLElement`. |
| `ChartPlot` | Хост TanStack Charts. Требует `definition` и `ariaLabel`, по умолчанию использует пресет анимации moduix и принимает `motion={false}`. |
| `ChartHeader` | Разметка `figcaption` для заголовка и описания. |
| `ChartTitle` | Заголовок графика, по умолчанию рендерится как `h3`. |
| `ChartDescription` | Видимое пояснение к графику. |
| `ChartLegend` | Семантический список видимых серий. |
| `ChartLegendItem` | Элемент легенды с необязательным цветом индикатора `color`. |

## Композиция

`Chart` - это презентационная оболочка. TanStack `definition` владеет графическими элементами,
шкалами, взаимодействиями и колбэками, поэтому храните его на уровне модуля или мемоизируйте
полностью, если он зависит от props или состояния. Собирайте `ChartHeader`, `ChartPlot` и
`ChartLegend` внутри корневого элемента; `ChartPlot` продолжает передавать TanStack состояние
рендеринга, фокуса и подсказки.

Используйте `renderTooltipBody`, только если нужно обернуть или расширить нативную подсказку:
отрендерите переданный `defaultBody`, а интерактивные элементы добавляйте только при `pinned=true`.
`Chart` и `ChartHeader` поддерживают `asChild`, если семантическая оболочка или её разметка
должны принадлежать приложению.

## Примеры

### Анимированная область

Область, линия, точки, группировка фокуса и анимация подсказки остаются нативным поведением
TanStack.

<ExampleFrame variant="component">
  <ChartAnimatedArea />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs groupId="framework" values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]} className="docs-tabs">
    <Tab value="react">

```tsx pure file="../../../src/components/examples/chart/animated-area.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={chartSolidAnimatedArea} />

    </Tab>
  </Tabs>
</div>
</ExampleCode>

### Цвета графика

Используйте токены moduix для графиков в цветовой шкале TanStack. CSS-свойства работают как значения
заливки SVG и автоматически учитывают активную тему.

<ExampleFrame variant="component">
  <ChartColors />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs groupId="framework" values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]} className="docs-tabs">
    <Tab value="react">

```tsx pure file="../../../src/components/examples/chart/line-colors.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={chartSolidColors} />

    </Tab>
  </Tabs>
</div>
</ExampleCode>

### Кольцевая диаграмма

Сочетайте необязательные примитивы TanStack `pie`, `polar` и `radialArc`, чтобы показывать части
целого. Та же композиция `ChartPlot`, анимации, фокуса, подсказки и легенды работает и за пределами
декартовых графиков.

<ExampleFrame variant="component">
  <ChartDonut />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs groupId="framework" values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]} className="docs-tabs">
    <Tab value="react">

```tsx pure file="../../../src/components/examples/chart/donut.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={chartSolidDonut} />

    </Tab>
  </Tabs>
</div>
</ExampleCode>

### Тепловая карта

Используйте `cell` с полосовыми шкалами по обеим осям, чтобы построить компактную категориальную
матрицу. Цветовой канал превращает уровень каждой ячейки в значение цвета с учётом темы.

<ExampleFrame variant="component">
  <ChartHeatmap />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs groupId="framework" values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]} className="docs-tabs">
    <Tab value="react">

```tsx pure file="../../../src/components/examples/chart/heatmap.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={chartSolidHeatmap} />

    </Tab>
  </Tabs>
</div>
</ExampleCode>

### Горизонтальные столбцы

Используйте `barX`, когда подписи категорий длинные или рейтинг должен читаться сверху вниз.
Числовое значение передаётся по оси x, а категория - по оси y.

<ExampleFrame variant="component">
  <ChartHorizontalBars />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs groupId="framework" values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]} className="docs-tabs">
    <Tab value="react">

```tsx pure file="../../../src/components/examples/chart/horizontal-bars.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={chartSolidHorizontalBars} />

    </Tab>
  </Tabs>
</div>
</ExampleCode>

### Точечная диаграмма

Используйте `dot`, чтобы сравнить два количественных значения. Цвет может передавать дополнительное
категориальное измерение, не меняя связь по осям x и y.

<ExampleFrame variant="component">
  <ChartScatter />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs groupId="framework" values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]} className="docs-tabs">
    <Tab value="react">

```tsx pure file="../../../src/components/examples/chart/scatter.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={chartSolidScatter} />

    </Tab>
  </Tabs>
</div>
</ExampleCode>

### Столбцы с накоплением

Используйте цветовой канал TanStack и раскладку `stack()` для столбцов с несколькими сериями.
`focus: 'group-x'` группирует подсказку и клавиатурный фокус по кварталу.

<ExampleFrame variant="component">
  <ChartStackedBars />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs groupId="framework" values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]} className="docs-tabs">
    <Tab value="react">

```tsx pure file="../../../src/components/examples/chart/stacked-bars.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={chartSolidStackedBars} />

    </Tab>
  </Tabs>
</div>
</ExampleCode>

### Расширенная настройка

Части фабрики Ark принимают `asChild`, поэтому могут применять свои стили к семантическим
элементам вашей разметки. В этом примере панели Chart и Button объединяются в один экран: стандартные
`figure` и `figcaption` заменяются на размеченные `section` и `header`, а легенда переносится в
заголовок.

`ChartPlot` остаётся хостом TanStack, а не слотом. Используйте `class` или `className` и `style`
для внешнего элемента, а `renderTooltipBody` - для расширения подсказки. Рендерите `defaultBody`
внутри своей оболочки, пока TanStack продолжает управлять позиционированием и порталом. Добавляйте
интерактивные элементы только в закреплённую подсказку.

<ExampleFrame variant="component">
  <ChartAdvancedCustomization />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs groupId="framework" values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]} className="docs-tabs">
    <Tab value="react">

```tsx pure file="../../../src/components/examples/chart/advanced-customization.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={chartSolidAdvancedCustomization} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/chart/chart-advanced-customization.module.css"
```
</div>
</ExampleCode>

## Стилизация

Стилизуйте `figure` через `Chart`, а хост TanStack - через `ChartPlot`. Публичные CSS-свойства
управляют поверхностью, контуром фокуса, оболочкой подсказки и её стандартной раскладкой. Геометрию
графика задавайте в определении TanStack через параметры графических элементов, шкалы и цветовые
каналы.

Варианты на Tailwind используют обычные утилиты и пользовательские классы, а перечисленные ниже
CSS-свойства настраивают варианты с CSS Modules. Атрибуты данных и селекторы `data-slot` общие для
обоих подходов.

Добавьте локальный класс (`className` в React или `class` в Solid), если графику нужны локальные
переопределения:

```css
.sales-chart {
  --moduix-chart-tooltip-bg: var(--moduix-color-card);
  --moduix-chart-tooltip-font-size: var(--moduix-text-sm);
  --moduix-chart-tooltip-line-height: var(--moduix-line-height-text-sm);
}
```

### CSS-свойства

<CssPropertiesSection properties={ChartDocs.chartCssProperties} />

### Хуки стилизации

- `Chart`, `ChartHeader`, `ChartTitle`, `ChartDescription`, `ChartLegend` и
  `ChartLegendItem` предоставляют `data-scope="chart"`, `data-part` и `data-slot`.
- `className` в React и `class` в Solid у `ChartPlot` применяются к внешнему элементу `.ts-chart-host`.
- Стандартная подсказка использует слоты данных `chart-tooltip-body`, `chart-tooltip-title`,
  `chart-tooltip-rows`, `chart-tooltip-row`, `chart-tooltip-swatch`, `chart-tooltip-label`,
  `chart-tooltip-value` и `chart-tooltip-text`.
- Используйте `--moduix-color-chart-1` - `--moduix-color-chart-5` в цветовых шкалах TanStack.
- Оболочка подсказки настраивается через документированные переменные `--ts-chart-tooltip-*`,
  связанные со свойствами moduix. Для собственных CSS-селекторов используйте `tooltip.className`,
  а для собственного содержимого - `renderTooltipBody` и нативный `defaultBody`.
