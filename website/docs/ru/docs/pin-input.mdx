---
title: Pin Input
component: pin-input
description: Компонент для ввода коротких PIN-кодов и кодов подтверждения по отдельным ячейкам, с автоматическим переводом фокуса и поддержкой OTP.
---

import * as PinInputDocs from '@/components/examples/pin-input/css-properties';
import PinInputBasic from '@/components/examples/pin-input/basic.tsx';
import PinInputBlurOnComplete from '@/components/examples/pin-input/blur-on-complete.tsx';
import PinInputChangeEvents from '@/components/examples/pin-input/change-events.tsx';
import PinInputField from '@/components/examples/pin-input/field.tsx';
import PinInputGroupedLayout from '@/components/examples/pin-input/grouped-layout.tsx';
import PinInputMasking from '@/components/examples/pin-input/masking.tsx';
import PinInputOtpMode from '@/components/examples/pin-input/otp-mode.tsx';
import PinInputPlaceholder from '@/components/examples/pin-input/placeholder.tsx';
import PinInputRootProvider from '@/components/examples/pin-input/root-provider.tsx';
import pinInputSolidBasic from '../../../snippets/pin-input/solid/basic.tsx?raw';
import pinInputSolidBlurOnComplete from '../../../snippets/pin-input/solid/blur-on-complete.tsx?raw';
import pinInputSolidChangeEvents from '../../../snippets/pin-input/solid/change-events.tsx?raw';
import pinInputSolidField from '../../../snippets/pin-input/solid/field.tsx?raw';
import pinInputSolidGroupedLayout from '../../../snippets/pin-input/solid/grouped-layout.tsx?raw';
import pinInputSolidMasking from '../../../snippets/pin-input/solid/masking.tsx?raw';
import pinInputSolidOtpMode from '../../../snippets/pin-input/solid/otp-mode.tsx?raw';
import pinInputSolidPlaceholder from '../../../snippets/pin-input/solid/placeholder.tsx?raw';
import pinInputSolidRootProvider from '../../../snippets/pin-input/solid/root-provider.tsx?raw';

## Справочник API

<PrimitiveReference href="https://ark-ui.com/docs/components/pin-input" />

## Выбор подходящего компонента

Используйте `PinInput`, если короткий код фиксированной длины нужно вводить по одному символу в отдельных ячейках.

- Выбирайте **`PinInput`** для одноразовых кодов, PIN-кодов, резервных кодов и кодов приглашений.
- Выбирайте **`PinInputInput`**, когда достаточно одного обычного текстового поля.
- Выбирайте **`PasswordInput`**, когда значение представляет собой одну секретную строку с переключателем видимости.

## Базовый пример

Используйте `PinInput` как корневой компонент и явно задайте `count`. Добавьте `PinInputLabel` для доступного имени,
поместите `PinInputInputs` внутрь `PinInputControl` и явно добавьте `PinInputHiddenInput`, если значение должно участвовать в работе
нативной формы.

<ExampleFrame variant="component">
  <PinInputBasic />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/pin-input/basic.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={pinInputSolidBasic} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

## Установка через shadcn

Полную настройку пакета, реестра и стилей см. в разделе [Быстрый старт](/docs/quick-start).

<Tabs groupId="framework">
  <Tab label="React" value="react">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-react/pin-input' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-react-tailwind/pin-input' },
  ]}
/>

  </Tab>
  <Tab label="Solid" value="solid">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-solid/pin-input' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-solid-tailwind/pin-input' },
  ]}
/>

  </Tab>
</Tabs>

## Анатомия

```text
PinInput
├─ PinInputLabel
├─ PinInputControl
│  ├─ PinInputInput[index=0]
│  ├─ PinInputInput[index=1]
│  └─ PinInputInput[index=2]
└─ PinInputHiddenInput (явно)

PinInputRootProvider
└─ то же дерево частей, связанное с состоянием usePinInput()
```

| Часть | Назначение |
| --- | --- |
| `PinInput` | Управляет количеством ячеек, состоянием значения, проверкой и параметрами формы. |
| `PinInputHiddenInput` | Нативное поле Ark, которое нужно добавлять явно. |
| `PinInputRootProvider` | Подключает части к состоянию, созданному через `usePinInput()`. |
| `PinInputContext` | Передаёт текущее состояние корневого компонента потомкам через render prop. |
| `PinInputLabel` | Задаёт подпись для группы PIN-кода или кода подтверждения. |
| `PinInputControl` | Контейнер видимых ячеек с индексами. |
| `PinInputInput` | Одна видимая ячейка для символа. Передайте индекс, начиная с нуля. |
| `PinInputInputs` | Создаёт по одному `PinInputInput` с индексом для каждой ячейки контекста Ark. |
| `PinInputSeparator` | Декоративный вспомогательный компонент moduix для группировки, а не часть примитива Ark. |
| `usePinInput` | Создаёт состояние для `PinInputRootProvider`. |
| `usePinInputContext` | Читает текущее состояние Pin Input внутри дерева компонента. |

## Композиция

Явно добавьте `PinInputHiddenInput` внутрь `PinInput` или `PinInputRootProvider`, если значение должно
отправляться с нативной формой и сбрасываться вместе с ней. Задайте корню `name`, а `form` используйте, если
форма находится вне дерева компонента.

`PinInputInputs` создаёт по одной индексированной ячейке для каждого элемента контекста Ark. Используйте явные
части `PinInputInput` с непрерывными индексами от нуля и `PinInputSeparator`, если код нужно разделить на группы. Значения
представлены массивом строк. Используйте `PinInputRootProvider` с `usePinInput`, когда состоянием или императивными
методами нужно управлять за пределами дерева компонентов. В React передаётся объект состояния, а в Solid
реактивный accessor.

Ark управляет переводом фокуса, распределением вставленного текста, редактированием с клавиатуры и
автозаполнением одноразовых кодов; добавьте `PinInputLabel`, чтобы у группы было доступное имя.
`PinInputSeparator` по умолчанию декоративный и получает `aria-hidden` и `role="presentation"`, поэтому
содержательный текст размещайте вне него.

## Примеры

### Снять фокус после завершения ввода

Укажите `blurOnComplete`, если после заполнения всех ячеек фокус должен покинуть последнее поле.

<ExampleFrame variant="component">
  <PinInputBlurOnComplete />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/pin-input/blur-on-complete.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={pinInputSolidBlurOnComplete} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### События изменения

Храните массив значений в состоянии и читайте `details.value` или `details.valueAsString` из колбэков Ark.

<ExampleFrame variant="component">
  <PinInputChangeEvents />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/pin-input/change-events.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={pinInputSolidChangeEvents} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/pin-input/pin-input-change-events.module.css"
```
</div>

</ExampleCode>

### Field

Поместите `PinInput` внутрь `Field`, если подпись, вспомогательный текст, сообщение об ошибке и состояние поля
должны быть связаны с видимыми ячейками механизмами доступности Ark для форм.

<ExampleFrame variant="component">
  <PinInputField />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/pin-input/field.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={pinInputSolidField} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/pin-input/pin-input-field.module.css"
```
</div>

</ExampleCode>

### Сгруппированная раскладка

Используйте явные части `PinInputInput` с `PinInputSeparator`, если разделитель должен разделить код на группы. Индексы должны
идти непрерывно во всей композиции.

<ExampleFrame variant="component">
  <PinInputGroupedLayout />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/pin-input/grouped-layout.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={pinInputSolidGroupedLayout} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Маскирование

Включите `mask`, если введённый код нельзя показывать открыто.

<ExampleFrame variant="component">
  <PinInputMasking />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/pin-input/masking.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={pinInputSolidMasking} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Режим OTP

Включите `otp`, чтобы добавить видимым полям `autocomplete="one-time-code"` и включить подсказки одноразовых
кодов в поддерживаемых мобильных браузерах.

<ExampleFrame variant="component">
  <PinInputOtpMode />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/pin-input/otp-mode.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={pinInputSolidOtpMode} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Символ-заполнитель

Передайте `placeholder`, чтобы показывать символ в каждой пустой ячейке. По умолчанию moduix оставляет заполнитель
пустым.

<ExampleFrame variant="component">
  <PinInputPlaceholder />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/pin-input/placeholder.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={pinInputSolidPlaceholder} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Root Provider

Используйте `usePinInput` с `PinInputRootProvider`, когда состоянием и императивными методами нужно управлять за пределами
дерева компонентов. В этом примере `PinInputInputs` заменён явными частями `PinInputInput` с индексами.

<ExampleFrame variant="component">
  <PinInputRootProvider />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/pin-input/root-provider.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={pinInputSolidRootProvider} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/pin-input/pin-input-root-provider.module.css"
```
</div>

</ExampleCode>

## Стилизация

### CSS-свойства

<div className="docs-reference">
  <Tabs
    values={[{ label: 'CSS-переменные', value: 'CSS Variables' }]}
    className="docs-reference-tabs"
    contentItemClassName="docs-reference-panel"
  >
    <Tab>
      <PinInputDocs.PinInputCssPropertiesPanel />
    </Tab>
  </Tabs>
</div>

Пакеты с CSS Modules предоставляют описанные выше публичные переменные. В Tailwind-вариантах используются
нативные utility-классы и пользовательские переопределения через `className` или `class`.

### Хуки стилизации

- Корневой элемент и `PinInputRootProvider` имеют `data-slot="pin-input-root"` и `data-slot="pin-input-root-provider"`.
- `PinInputLabel`, `PinInputControl` и `PinInputInput` имеют `data-slot="pin-input-label"`, `data-slot="pin-input-control"` и
  `data-slot="pin-input-input"`.
- Части Ark используют `data-scope="pin-input"` и `data-part="root" | "label" | "control" | "input"`.
- Для стилей состояния корня используются `data-disabled`, `data-invalid`, `data-complete` и `data-readonly`.
- Для стилей состояния ячейки используются `data-disabled`, `data-invalid`, `data-complete`, `data-filled` и
  `data-index`.
- `PinInputSeparator` имеет `data-slot="pin-input-separator"` и по умолчанию является декоративным.
