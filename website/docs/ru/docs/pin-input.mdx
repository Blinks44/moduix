---
title: Pin Input
component: pin-input
description: Компонент для ввода коротких PIN-кодов и кодов подтверждения по отдельным ячейкам, с автоматическим переводом фокуса и поддержкой OTP.
---

import * as PinInputDocs from '@/components/examples/pin-input/css-properties';
import PinInputBasic from '@/components/examples/pin-input/basic.tsx';
import PinInputBlurOnComplete from '@/components/examples/pin-input/blur-on-complete.tsx';
import PinInputChangeEvents from '@/components/examples/pin-input/change-events.tsx';
import PinInputField from '@/components/examples/pin-input/field.tsx';
import PinInputGroupedLayout from '@/components/examples/pin-input/grouped-layout.tsx';
import PinInputMasking from '@/components/examples/pin-input/masking.tsx';
import PinInputOtpMode from '@/components/examples/pin-input/otp-mode.tsx';
import PinInputPlaceholder from '@/components/examples/pin-input/placeholder.tsx';
import PinInputRootProvider from '@/components/examples/pin-input/root-provider.tsx';

## Справочник API

<PrimitiveReference href="https://ark-ui.com/docs/components/pin-input" />

## Выбор подходящего компонента

Используйте `PinInput`, если короткий код фиксированной длины нужно вводить по одному символу в отдельных ячейках.

- Выбирайте **`PinInput`** для одноразовых кодов, PIN-кодов, резервных кодов и кодов приглашений.
- Выбирайте **`Input`**, когда достаточно одного обычного текстового поля.
- Выбирайте **`PasswordInput`**, когда значение представляет собой одну секретную строку с переключателем видимости.

## Базовый пример

Для стандартной разметки используйте `<PinInput>`. Это сокращённая запись `<PinInput.Root>`. Явно задайте
`count`, чтобы SSR-разметка и ARIA-атрибуты были предсказуемыми.

Явно добавьте `PinInput.HiddenInput`. Передайте `name`, чтобы значение попало в отправляемую форму.
Если компонент находится вне дерева этой формы, укажите также `form`.

<ExampleFrame variant="component">
  <PinInputBasic />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/pin-input/basic.tsx"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

## Установка через shadcn

<ShadcnInstall packageName="pin-input" />

## Анатомия

```text
PinInput / PinInput.Root
├─ PinInput.Label
├─ PinInput.Control
│  ├─ PinInput.Input[index=0]
│  ├─ PinInput.Input[index=1]
│  └─ PinInput.Input[index=2]
└─ PinInput.HiddenInput (явно)
```

| Часть                                  | Назначение                                                                                         |
| -------------------------------------- | --------------------------------------------------------------------------------------------------- |
| `PinInput` / `PinInput.Root`           | Корневой компонент и машина состояний: количество ячеек, массив значений, проверка и параметры формы. |
| `PinInput.RootProvider`                | Создаёт дерево компонента на основе состояния, возвращённого `usePinInput`.                         |
| `PinInput.Context`                     | Передаёт потомкам состояние корневого элемента через render prop.                                  |
| `PinInput.Label`                       | Подпись группы для ввода PIN-кода или кода подтверждения.                                          |
| `PinInput.Control`                     | Контейнер видимых полей с индексами.                                                               |
| `PinInput.Input`                       | Одна видимая ячейка для символа. Передайте индекс `index`, начиная с нуля.                         |
| `PinInput.Inputs`                      | Создаёт по одному `Input` с индексом для каждой ячейки контекста Ark. Его `className` передаётся каждой ячейке. |
| `PinInput.Separator`                   | Декоративный вспомогательный компонент moduix для группировки. Это не часть примитива Ark.         |
| `usePinInput`                          | Создаёт состояние для `PinInput.RootProvider`.                                                     |
| `usePinInputContext`                    | Читает текущее состояние Pin Input внутри дерева компонента.                                      |

## Композиция

### Рекомендуемая композиция

Используйте компактную форму `<PinInput>`, добавьте видимую `Label` и поместите `Inputs` внутрь `Control`.
Передайте `name`, если код должен отправляться вместе с формой.

### Когда нужна собственная композиция

Явно добавьте части `Input` с индексами и `Separator`, если код нужно разделить на группы или отдельным
ячейкам требуются разные свойства. Используйте `PinInput.RootProvider` и `usePinInput`, когда состояние нужно
создать за пределами дерева компонентов. Индексы должны начинаться с нуля и идти непрерывно во всей композиции.

### Свойства по умолчанию

| Свойство         | По умолчанию    | Примечания                                                                                     |
| ---------------- | --------------- | ----------------------------------------------------------------------------------------------- |
| `count`          | -               | Количество ячеек. Указывайте явно для предсказуемой SSR-разметки и ARIA-атрибутов.              |
| `type`           | `numeric`       | `numeric`, `alphabetic` или `alphanumeric`.                                                      |
| `placeholder`    | `''` в moduix   | В Ark по умолчанию отображается `○`, а moduix использует пустую строку, пока свойство не задано. |
| `otp`            | `false`         | Добавляет `autocomplete="one-time-code"` видимым полям.                                         |
| `mask`           | `false`         | Скрывает введённые символы, как поле с `type="password"`.                                      |
| `blurOnComplete` | `false`         | Убирает фокус с последнего поля после заполнения всех ячеек.                                    |
| `selectOnFocus`  | -               | Выделяет текущее значение при получении фокуса.                                                 |
| `autoFocus`      | -               | Переводит фокус в первую ячейку при монтировании компонента.                                    |
| `autoSubmit`     | -               | Отправляет связанную форму после заполнения всех ячеек.                                         |
| `pattern`        | -               | Пользовательское регулярное выражение для допустимых символов.                                  |
| `sanitizeValue`  | -               | Очищает вставленное значение до проверки, например удаляет пробелы или дефисы.                 |
| `defaultValue`   | `[]`            | Начальное значение для неконтролируемого режима, массив строк.                                  |
| `value`          | -               | Управляемое значение, массив строк.                                                             |

### Особенности поведения

`value` и `defaultValue` принимают массивы строк. Колбэки `onValueChange(details)` и `onValueComplete(details)`
передают `details.value` и `details.valueAsString`. Колбэк `onValueInvalid(details)` передаёт отклонённое значение
в `details.value` и его индекс `details.index`.

Используйте `type` для встроенных наборов допустимых символов, а `pattern` для собственного регулярного выражения.
`sanitizeValue` получает вставленный текст и возвращает значение, которое нужно проверить.

`Field.Root` и `Fieldset.Root` могут передавать через контекст Ark состояния `disabled`, `invalid`, `required` и
`readOnly`. Передавайте в `PinInput` корневые свойства, например `name`, `form`, `ids` и `translations`. Корневой
элемент и его части поддерживают `asChild`.

Передайте `className` в `PinInput.Inputs`, если всем созданным ячейкам нужен один класс. Используйте явные части
`Input`, если для отдельных ячеек нужны разные свойства или стили.

Включайте `autoSubmit` только тогда, когда завершение ввода должно отправлять связанную форму. Компонент использует
клавиатурное поведение Ark: клавиши со стрелками перемещают фокус между ячейками, Backspace очищает ячейку и при
необходимости возвращает фокус назад, Delete очищает текущую ячейку, а вставка заполняет оставшиеся ячейки.

## Примеры

### Снять фокус после завершения ввода

Укажите `blurOnComplete`, если после заполнения всех ячеек фокус должен покинуть последнее поле.

<ExampleFrame variant="component">
  <PinInputBlurOnComplete />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/pin-input/blur-on-complete.tsx"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### События изменения

Храните массив значений в состоянии React и читайте `details.value` или `details.valueAsString` из колбэков Ark.

<ExampleFrame variant="component">
  <PinInputChangeEvents />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/pin-input/change-events.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/pin-input/pin-input-change-events.module.css"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Field

Поместите `PinInput` внутрь `Field`, если подпись, вспомогательный текст, сообщение об ошибке и состояние поля
должны быть связаны с видимыми ячейками благодаря механизмам доступности Ark для форм.

<ExampleFrame variant="component">
  <PinInputField />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/pin-input/field.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/pin-input/pin-input-field.module.css"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Сгруппированная раскладка

Используйте явные части `Input` с индексами, если разделитель должен разделить код на группы. Сохраняйте
непрерывную последовательность индексов, даже если поля отображаются в разных группах.

<ExampleFrame variant="component">
  <PinInputGroupedLayout />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/pin-input/grouped-layout.tsx"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Маскирование

Включите `mask`, если введённый код нельзя показывать открыто.

<ExampleFrame variant="component">
  <PinInputMasking />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/pin-input/masking.tsx"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Режим OTP

Включите `otp`, чтобы добавить видимым полям `autocomplete="one-time-code"` и включить подсказки одноразовых
кодов в поддерживаемых мобильных браузерах.

<ExampleFrame variant="component">
  <PinInputOtpMode />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/pin-input/otp-mode.tsx"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Символ-заполнитель

Передайте `placeholder`, чтобы показывать символ в каждой пустой ячейке. По умолчанию moduix оставляет заполнитель
пустым.

<ExampleFrame variant="component">
  <PinInputPlaceholder />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/pin-input/placeholder.tsx"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Расширенная настройка

Используйте `usePinInput` с `RootProvider`, когда состоянием и императивными методами нужно управлять за пределами
дерева компонентов. В этом примере `Inputs` заменён явными частями `Input` с индексами.

<ExampleFrame variant="component">
  <PinInputRootProvider />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/pin-input/root-provider.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/pin-input/pin-input-root-provider.module.css"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

## Доступность

Ark управляет переводом фокуса, распределением вставленного текста, редактированием с клавиатуры, данными формы
и автозаполнением одноразовых кодов. Добавьте `Label`, чтобы у группы было доступное имя.

`PinInput.Separator` по умолчанию является декоративным и получает `aria-hidden` и `role="presentation"`. Не
размещайте внутри него содержательный текст.

Для отправки значения в нативную форму передайте корню `name`, добавьте `PinInput.HiddenInput` и
используйте `form`, если форма находится в другом месте документа.

## Стилизация

### CSS-свойства

<div className="docs-reference">
  <Tabs
    values={[{ label: 'CSS-переменные', value: 'CSS Variables' }]}
    className="docs-reference-tabs"
    contentItemClassName="docs-reference-panel"
  >
    <Tab>
      <PinInputDocs.PinInputCssPropertiesPanel />
    </Tab>
  </Tabs>
</div>

### Хуки стилизации

- Корневой элемент и `RootProvider` имеют `data-slot="pin-input-root"` и `data-slot="pin-input-root-provider"`.
- `Label`, `Control` и `Input` имеют `data-slot="pin-input-label"`,
  `data-slot="pin-input-control"` и `data-slot="pin-input-input"`.
- Части Ark используют `data-scope="pin-input"` и `data-part="root" | "label" | "control" | "input"`.
- Для стилизации состояния корневого элемента используются `data-disabled`, `data-invalid`, `data-complete` и
  `data-readonly`.
- Для стилизации состояния ячейки используются `data-disabled`, `data-invalid`, `data-complete`, `data-filled` и
  `data-index`.
- `PinInput.Separator` имеет `data-slot="pin-input-separator"` и по умолчанию является декоративным.
