---
title: Pagination
component: pagination
description: Доступная навигация по страницам для списков, таблиц, результатов поиска и других данных с пагинацией.
---

import * as PaginationDocs from '@/components/examples/pagination/css-properties';
import PaginationAdvancedCustomization from '@/components/examples/pagination/advanced-customization.tsx';
import PaginationBasic from '@/components/examples/pagination/basic.tsx';
import PaginationContext from '@/components/examples/pagination/context.tsx';
import PaginationControlled from '@/components/examples/pagination/controlled.tsx';
import PaginationCustomized from '@/components/examples/pagination/customized.tsx';
import PaginationDataSlicing from '@/components/examples/pagination/data-slicing.tsx';
import PaginationLink from '@/components/examples/pagination/link.tsx';
import PaginationPageRange from '@/components/examples/pagination/page-range.tsx';
import PaginationPageSizeControl from '@/components/examples/pagination/page-size-control.tsx';
import PaginationRootProvider from '@/components/examples/pagination/root-provider.tsx';
import PaginationWithEdges from '@/components/examples/pagination/with-edges.tsx';

## Справочник API

<PrimitiveReference href="https://ark-ui.com/docs/components/pagination" />

## Базовый пример

Используйте `Pagination` как корневой компонент. `Pagination.Items` выводит кнопки страниц и многоточия
для текущего диапазона.

<ExampleFrame variant="component">
  <PaginationBasic />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/pagination/basic.tsx"
```
    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/pagination/pagination-basic.module.css"
```
    </Tab>
  </Tabs>
</div>

</ExampleCode>

## Установка через shadcn

<ShadcnInstall packageName="pagination" />

## Анатомия

```text
Pagination
├─ Pagination.FirstTrigger (необязательно)
├─ Pagination.PrevTrigger
├─ Pagination.Items (рекомендуется)
├─ Pagination.Context (расширенный доступ к состоянию через render prop)
│  ├─ Pagination.Item
│  └─ Pagination.Ellipsis
├─ Pagination.NextTrigger
└─ Pagination.LastTrigger (необязательно)

Pagination.RootProvider
└─ та же структура из триггеров, элементов, многоточий и контекста, подключённая к usePagination()
```

| Часть                             | Назначение                                                            |
| --------------------------------- | --------------------------------------------------------------------- |
| `Pagination`                      | Корневой компонент навигации, который управляет текущей страницей и числом элементов на странице. |
| `Pagination.RootProvider`         | Подключает части к состоянию, созданному через `Pagination.usePagination()`.                     |
| `Pagination.Items`                | Выводит рассчитанные элементы страниц и многоточия.                                              |
| `Pagination.Context`              | Открывает доступ к состоянию корня и методам навигации через render prop Ark UI.                  |
| `Pagination.usePagination`        | Создаёт состояние пагинации за пределами дерева, где отображается компонент.                      |
| `Pagination.usePaginationContext` | Даёт потомку доступ к состоянию пагинации.                                                        |
| `Pagination.Item`                 | Интерактивный элемент страницы. Принимает объект страницы в качестве свойств.                    |
| `Pagination.Ellipsis`             | Неинтерактивный маркер скрытой части диапазона страниц. Требует `index`.                          |
| `Pagination.PrevTrigger`          | Переходит на предыдущую страницу и отключается, когда выбрана первая страница.                    |
| `Pagination.NextTrigger`          | Переходит на следующую страницу и отключается, когда выбрана последняя страница.                  |
| `Pagination.FirstTrigger`         | Переходит на первую страницу и отключается, если она уже выбрана.                                 |
| `Pagination.LastTrigger`          | Переходит на последнюю страницу и отключается, если она уже выбрана.                              |

## Композиция

### Рекомендуемая композиция

`Pagination` и `Pagination.Root` управляют навигацией с клавиатуры, текущей страницей, числом
элементов на странице, состоянием кнопок навигации, расчётом диапазона страниц и подписями для средств
доступности. Укажите в `count` общее число элементов данных, а не число страниц. Вместе с `pageSize`
или `defaultPageSize` это позволит Ark вычислить `totalPages`.

Для стандартного списка страниц используйте `Pagination.Items`. Компонент выводит текущий массив
`pages`, рассчитанный Ark, через `Pagination.Item` и `Pagination.Ellipsis`, сохраняя выбранное
состояние, подписи и обработчики событий.

### Когда нужна собственная композиция

Используйте `Pagination.Context`, если нужно изменить отображение элементов страниц или элементов
управления рядом с ними. Если состояние нужно создать за пределами дерева пагинации, создайте его
с помощью `Pagination.usePagination()` и передайте в `Pagination.RootProvider`. Потомок может получить
то же состояние через `Pagination.usePaginationContext()`. Если вместо кнопок нужны ссылки, задайте
`type="link"` и передайте `getPageUrl(details)`.

### Свойства по умолчанию

| Свойство          | По умолчанию | Примечание                                                        |
| ----------------- | ------------ | ----------------------------------------------------------------- |
| `aria-label`      | `pagination` | Подпись области навигации из Ark; задайте `translations.rootLabel`, чтобы локализовать её. |
| `defaultPage`     | `1`          | Начальная активная страница в неуправляемом режиме.                 |
| `defaultPageSize` | `10`         | Начальное число элементов на странице в неуправляемом режиме.       |
| `siblingCount`    | `1`          | Число страниц рядом с активной страницей.                            |
| `boundaryCount`   | `1`          | Число страниц в начале и конце диапазона.                            |
| `type`            | `button`     | Тип элемента навигации. Для ссылок задайте `link` и передайте `getPageUrl(details)`. |

### Особенности поведения

Когда компонент должен сам управлять состоянием, используйте `defaultPage` и `defaultPageSize`.
Чтобы управлять состоянием из приложения, передайте `page`, `pageSize`, `onPageChange(details)` и
`onPageSizeChange(details)`. Колбэки получают объекты `details` от Ark, поэтому читайте `details.page`
и `details.pageSize` напрямую. Компонент сохраняет навигацию с клавиатуры, семантику выбранного элемента,
недоступность кнопок навигации, `ids`, `asChild`, локализацию и генерацию URL для ссылок. При
использовании `asChild` передавайте один семантический дочерний элемент, например ссылку.

## Примеры

### Управляемое состояние

Передайте текущую страницу через `page` и обновляйте её в `onPageChange(details)`.

<ExampleFrame variant="component">
  <PaginationControlled />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/pagination/controlled.tsx"
```
    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/pagination/pagination-controlled.module.css"
```
    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Контекст

Используйте `Pagination.Context`, чтобы разместить собственные элементы управления рядом со списком
страниц и вызывать методы навигации, например `goToFirstPage()` или `goToLastPage()`.

<ExampleFrame variant="component">
  <PaginationContext />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/pagination/context.tsx"
```
    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Постраничный вывод данных на клиенте

Используйте `pagination.slice(data)` из контекста, чтобы на текущей странице показывать только нужную
часть уже загруженного массива.

<ExampleFrame variant="component">
  <PaginationDataSlicing />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/pagination/data-slicing.tsx"
```
    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/pagination/pagination-data-slicing.module.css"
```
    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Навигация по ссылкам

Используйте `type="link"`, `getPageUrl(details)` и якоря с `asChild`, если переход между страницами
должен менять URL и выполнять настоящую навигацию.

<ExampleFrame variant="component">
  <PaginationLink />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/pagination/link.tsx"
```
    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/pagination/pagination-link.module.css"
```
    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Диапазон элементов

Используйте `pagination.pageRange`, чтобы показать, какие элементы видны на текущей странице.

<ExampleFrame variant="component">
  <PaginationPageRange />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/pagination/page-range.tsx"
```
    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/pagination/pagination-page-range.module.css"
```
    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Число элементов на странице

Вызывайте `pagination.setPageSize(nextSize)` из контекста, когда отдельный элемент управления меняет
число элементов на странице.

<ExampleFrame variant="component">
  <PaginationPageSizeControl />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/pagination/page-size-control.tsx"
```
    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/pagination/pagination-page-size-control.module.css"
```
    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Root Provider

Создайте состояние через `Pagination.usePagination()` и передайте его в `Pagination.RootProvider`,
если это состояние нужно использовать за пределами дерева с пагинацией.

<ExampleFrame variant="component">
  <PaginationRootProvider />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/pagination/root-provider.tsx"
```
    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/pagination/pagination-root-provider.module.css"
```
    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Локализация

Передайте `translations`, чтобы настроить подписи для средств доступности. Параметры `siblingCount`,
`boundaryCount` и параметры, задающие число элементов на странице, позволяют изменить видимый диапазон страниц.

<ExampleFrame variant="component">
  <PaginationCustomized />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/pagination/customized.tsx"
```
    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/pagination/pagination-customized.module.css"
```
    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Кнопки первой и последней страницы

Добавьте `Pagination.FirstTrigger` и `Pagination.LastTrigger`, если пользователям нужны быстрые
переходы к первой и последней странице.

<ExampleFrame variant="component">
  <PaginationWithEdges />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/pagination/with-edges.tsx"
```
    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/pagination/pagination-with-edges.module.css"
```
    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Расширенная настройка

Используйте `Pagination.Context` вместо `Pagination.Items`, если кнопкам страниц или многоточиям нужно
нестандартное содержимое. Передавайте каждый объект страницы в `Pagination.Item` без изменений, чтобы
сохранить состояние и доступность Ark.

<ExampleFrame variant="component">
  <PaginationAdvancedCustomization />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/pagination/advanced-customization.tsx"
```
    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/pagination/pagination-advanced-customization.module.css"
```
    </Tab>
  </Tabs>
</div>

</ExampleCode>

## Стилизация

moduix сохраняет атрибуты состояния Ark и добавляет стабильные хуки `data-slot` для локальной стилизации.
В номерах страниц используются табличные цифры и компактные горизонтальные отступы. Поэтому одно- и
двузначные номера сохраняют квадратную форму, а более длинные значения расширяются без обрезки.

### CSS-свойства

<div className="docs-reference">
  <Tabs
    values={[{ label: 'CSS-переменные', value: 'CSS Variables' }]}
    className="docs-reference-tabs"
    contentItemClassName="docs-reference-panel"
  >
    <Tab>
      <PaginationDocs.PaginationCssPropertiesPanel />
    </Tab>
  </Tabs>
</div>

### Хуки стилизации

- `data-scope="pagination"` и `data-part="item"` на `Pagination.Item`
- `data-scope="pagination"` и `data-part="prev-trigger"` на `Pagination.PrevTrigger`
- `data-scope="pagination"` и `data-part="next-trigger"` на `Pagination.NextTrigger`
- `data-scope="pagination"` и `data-part="first-trigger"` или `data-part="last-trigger"` на крайних триггерах
- `data-selected` на активном элементе страницы
- `data-disabled` на недоступных триггерах
- `data-slot="pagination-root"`, `pagination-root-provider`, `pagination-item`,
  `pagination-ellipsis`, `pagination-prev-trigger`, `pagination-next-trigger`,
  `pagination-first-trigger` и `pagination-last-trigger` для переопределения стилей moduix
