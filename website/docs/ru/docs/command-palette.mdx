---
title: Command Palette
component: command-palette
description: Палитра команд в диалоге с поиском и сочетаниями клавиш для быстрого доступа.
---

import * as CommandPaletteDocs from '@/components/examples/command-palette/css-properties';
import CommandPaletteBasic from '@/components/examples/command-palette/basic.tsx';
import AdvancedCommandPalette from '@/components/examples/command-palette/advanced-customization.tsx';
import ControlledCommandPalette from '@/components/examples/command-palette/controlled-open-state.tsx';
import CommandPaletteShortcutDemo from '@/components/examples/command-palette/enable-the-global-shortcut.tsx';
import CommandPaletteActionsDemo from '@/components/examples/command-palette/item-actions.tsx';
import commandPaletteSolidBasic from '../../../snippets/command-palette/solid/basic.tsx?raw';
import commandPaletteSolidAdvanced from '../../../snippets/command-palette/solid/advanced-customization.tsx?raw';
import commandPaletteSolidControlled from '../../../snippets/command-palette/solid/controlled-open-state.tsx?raw';
import commandPaletteSolidShortcut from '../../../snippets/command-palette/solid/enable-the-global-shortcut.tsx?raw';
import commandPaletteSolidActions from '../../../snippets/command-palette/solid/item-actions.tsx?raw';

## Справочник API

В Ark UI нет отдельного примитива `command-palette`. `CommandPalette` объединяет Ark
[`Dialog`](https://ark-ui.com/docs/components/dialog) и
[`Combobox`](https://ark-ui.com/docs/components/combobox), а также добавляет стандартный каркас палитры и
строку поиска. Компонент следует рекомендациям Ark по [композиции](https://ark-ui.com/docs/guides/composition),
[стилизации](https://ark-ui.com/docs/guides/styling) и [работе с ref](https://ark-ui.com/docs/guides/ref).

## Базовый пример

Просмотр на этой странице
использует пакет React CSS Modules,

Используйте `CommandPalettePanel` для стандартной структуры диалога. Он сам отрисовывает фон, контейнер
позиционирования, содержимое и основную область. Поместите внутрь `CommandPaletteCombobox`, передайте ему
коллекцию и выведите отфильтрованные команды в `CommandPaletteList`. `CommandPaletteSearch` добавляет поле
ввода и кнопку очистки, которая появляется после ввода запроса.

<ExampleFrame variant="component">
  <CommandPaletteBasic />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/command-palette/basic.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={commandPaletteSolidBasic} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/command-palette/command-palette-basic.module.css"
```
</div>

</ExampleCode>

## Установка через shadcn

Полную настройку пакета, реестра и стилей см. в разделе [Быстрый старт](/docs/quick-start).

<Tabs groupId="framework">
  <Tab label="React" value="react">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-react/command-palette' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-react-tailwind/command-palette' },
  ]}
/>

  </Tab>
  <Tab label="Solid" value="solid">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-solid/command-palette' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-solid-tailwind/command-palette' },
  ]}
/>

  </Tab>
</Tabs>

## Анатомия

```text
CommandPalette
├─ CommandPaletteTrigger
└─ Содержимое оверлея, автоматически помещаемое в портал
   ├─ CommandPaletteBackdrop
   └─ CommandPalettePositioner
      └─ CommandPaletteContent
         ├─ CommandPaletteHeader
         │  ├─ CommandPaletteTitle
         │  └─ CommandPaletteDescription
         └─ CommandPaletteBody
            └─ CommandPaletteCombobox
               ├─ CommandPaletteControl
               │  ├─ CommandPaletteInput
               │  └─ CommandPaletteClearTrigger
               ├─ CommandPaletteList
               │  ├─ CommandPaletteEmpty
               │  └─ CommandPaletteItemGroup
               │     ├─ CommandPaletteItemGroupLabel
               │     └─ CommandPaletteItem
               │        ├─ CommandPaletteItemIcon
               │        ├─ CommandPaletteItemText
               │        │  ├─ CommandPaletteItemLabel
               │        │  └─ CommandPaletteItemDescription
               │        ├─ CommandPaletteItemMeta
               │        └─ CommandPaletteItemIndicator
               └─ CommandPaletteFooter
                  └─ CommandPaletteKbd

CommandPalettePanel
└─ сокращённая композиция для CommandPaletteBackdrop, CommandPalettePositioner, CommandPaletteContent и CommandPaletteBody

CommandPaletteRootProvider
└─ то же дерево, подключённое к состоянию из useDialog()
```

| Часть | Назначение |
| --- | --- |
| `CommandPalette` | Управляет состоянием Ark Dialog и необязательным обработчиком глобального сочетания клавиш. |
| `CommandPaletteRootProvider` | Подключает части палитры к состоянию диалога, созданному вне дерева компонентов. |
| `CommandPaletteTrigger` | Открывает палитру. Используйте `asChild`, чтобы передать собственный элемент триггера. |
| `CommandPaletteBackdrop` | Фон Ark Dialog с состоянием анимации в `data-state`. |
| `CommandPalettePositioner` | Размещает диалог в области просмотра и не даёт его содержимому выйти за её пределы. |
| `CommandPaletteContent` | Содержимое диалога и визуальная поверхность палитры. |
| `CommandPalettePanel` | Готовый каркас со стандартными `CommandPaletteBackdrop`, `CommandPalettePositioner`, `CommandPaletteContent` и `CommandPaletteBody`. |
| `CommandPaletteHeader` / `CommandPaletteTitle` / `CommandPaletteDescription` | Необязательные элементы компоновки, заголовка и описания Ark Dialog. |
| `CommandPaletteBody` | Контейнер области Combobox внутри диалога. |
| `CommandPaletteCombobox` | Корневой компонент Ark Combobox для поиска и выбора команд. |
| `CommandPaletteControl` | Объединяет поле поиска и кнопку очистки. |
| `CommandPaletteInput` | Поле ввода Ark Combobox. |
| `CommandPaletteSearch` | Готовая строка поиска с `CommandPaletteControl`, `CommandPaletteInput` и `CommandPaletteClearTrigger`. Также добавляет полю ввода доступное имя по умолчанию. Если части нужно настраивать по отдельности, используйте явную композицию. |
| `CommandPaletteClearTrigger` | Очищает поисковый запрос и скрыта, пока поле пустое. |
| `CommandPaletteList` | Список результатов Ark Combobox, обёрнутый во встроенный `ScrollArea` из moduix. |
| `CommandPaletteEmpty` | Сообщение, которое показывается, если после фильтрации результатов нет. |
| `CommandPaletteItemGroup` / `CommandPaletteItemGroupLabel` | Группы результатов команд и их заголовки. |
| `CommandPaletteItem` | Строка команды с состояниями подсветки, выбора и отключения. |
| `CommandPaletteItemText` | Контейнер для названия и описания команды. |
| `CommandPaletteItemIndicator` | Индикатор выбранного состояния с иконкой галочки по умолчанию. |
| `CommandPaletteItemIcon`, `CommandPaletteItemLabel`, `CommandPaletteItemDescription`, `CommandPaletteItemMeta` | Визуальные части для иконки, текста, описания и метаданных справа. |
| `CommandPaletteSeparator` | Визуальный разделитель между группами команд. |
| `CommandPaletteFooter` / `CommandPaletteKbd` | Нижняя область и обёртка `Kbd` из moduix для обозначения сочетаний клавиш. |

## Композиция

Корневой компонент владеет состоянием Dialog и автоматически помещает `CommandPaletteBackdrop` и `CommandPalettePositioner` в портал.
Чтобы отрисовать их в текущем дереве, задайте `portalled={false}`, а для собственного контейнера передайте
`portalRef`. Для обычной структуры используйте `CommandPalettePanel` и `CommandPaletteSearch`, а при нестандартной разметке диалога или
поиска рендерите структурные части напрямую.

Храните фильтрацию коллекции рядом с владельцем `CommandPaletteCombobox`: настройте `useListCollection` с помощью
`itemToString`, `itemToValue` и `groupBy`, а в `onInputValueChange` вызывайте `filter(details.inputValue)`.
Если коллекция хранится вне палитры, сбрасывайте фильтр при закрытии. Задайте `closeOnSelect={false}`, чтобы
палитра оставалась открытой после действия, или используйте `CommandPaletteRootProvider` с `useDialog` из соответствующего
framework-пакета, если состояние диалога создаётся вне дерева компонентов.

## Примеры

### Управляемое состояние открытия

Используйте управляемое свойство `open`, если остальному интерфейсу тоже нужно управлять видимостью палитры.
В этом примере внешний фильтр коллекции сбрасывается при закрытии палитры.

<ExampleFrame variant="component">
  <ControlledCommandPalette />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/command-palette/controlled-open-state.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={commandPaletteSolidControlled} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/command-palette/command-palette-controlled-open-state.module.css"
```
</div>

</ExampleCode>

### Включение глобального сочетания клавиш

Задайте `shortcut="alt+k"`, чтобы зарегистрировать глобальное переключение палитры. Это единственный просмотр
на странице, в котором регистрируется `Alt+K`.

<ExampleFrame variant="component">
  <CommandPaletteShortcutDemo />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/command-palette/enable-the-global-shortcut.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={commandPaletteSolidShortcut} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/command-palette/command-palette-enable-the-global-shortcut.module.css"
```
</div>

</ExampleCode>

### Действия элемента

Обрабатывайте `onSelect(details)`, чтобы запускать команду. При значении `closeOnSelect` по умолчанию палитра
закрывается после выполнения выбранного действия.

<ExampleFrame variant="component">
  <CommandPaletteActionsDemo />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/command-palette/item-actions.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={commandPaletteSolidActions} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/command-palette/command-palette-item-actions.module.css"
```
</div>

</ExampleCode>

### Расширенная настройка

Используйте явные части диалога и поиска, если палитре нужны собственный заголовок или нестандартная строка
поиска. В примере сохраняется поведение Ark Dialog и Combobox, а готовые помощники `CommandPalettePanel` и `CommandPaletteSearch` заменяются
явной разметкой.

<ExampleFrame variant="component">
  <AdvancedCommandPalette />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/command-palette/advanced-customization.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={commandPaletteSolidAdvanced} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/command-palette/command-palette-advanced-customization.module.css"
```
</div>

</ExampleCode>

## Стилизация

Используйте CSS-переменные на частях триггера, фона, содержимого, списка и элементов, если палитра должна
соответствовать плотному интерфейсу или фирменному оформлению. Для изменения разметки или слоёв используйте
структурные части. `CommandPaletteList` остаётся прокручиваемым, но не показывает видимую полосу прокрутки.

### CSS-свойства

<CssPropertiesSection properties={CommandPaletteDocs.commandPaletteOverrideCssProperties} />

### Хуки стилизации

- Экспортируемые части предоставляют стабильные хуки `data-slot`, включая `command-palette-trigger`, `command-palette-backdrop`, `command-palette-positioner`, `command-palette-content`, `command-palette-title`, `command-palette-description`, `command-palette-header`, `command-palette-body`, `command-palette-combobox`, `command-palette-control`, `command-palette-input`, `command-palette-clear-trigger`, `command-palette-list`, `command-palette-scroll-area`, `command-palette-scroll-viewport`, `command-palette-scroll-content`, `command-palette-empty`, `command-palette-item-group`, `command-palette-item-group-label`, `command-palette-item`, `command-palette-item-text`, `command-palette-item-indicator`, `command-palette-item-icon`, `command-palette-item-label`, `command-palette-item-description`, `command-palette-item-meta`, `command-palette-separator`, `command-palette-footer` и `command-palette-kbd`.
- Хуки состояния Ark Dialog включают `data-scope="dialog"`, `data-part` и `data-state="open" | "closed"` у таких частей диалога, как триггер, фон и содержимое.
- Хуки состояния Ark Combobox включают `data-scope="combobox"`, `data-part`, `data-state`, `data-highlighted`, `data-disabled`, `data-invalid` и `data-empty` там, где они применимы.
- Анимация привязана к `data-state` Ark. Устаревшие атрибуты `data-starting-style`, `data-ending-style` и `data-open` не входят в контракт command-palette.
