---
title: Field
component: field
description: Контекст поля, который связывает элемент управления формы с подписью, пояснением, сообщением об ошибке и общим состоянием.
---

import * as FieldDocs from '@/components/examples/field/css-properties';
import FieldBasic from '@/components/examples/field/basic.tsx';
import FieldCheckbox from '@/components/examples/field/checkbox.tsx';
import FieldControlledInvalid from '@/components/examples/field/controlled-invalid.tsx';
import FieldDisabled from '@/components/examples/field/disabled.tsx';
import FieldInvalid from '@/components/examples/field/invalid.tsx';
import FieldItemTarget from '@/components/examples/field/item-target.tsx';
import FieldNativeSelect from '@/components/examples/field/native-select.tsx';
import FieldRadioGroup from '@/components/examples/field/radiogroup.tsx';
import FieldReadOnly from '@/components/examples/field/read-only.tsx';
import FieldRootProvider from '@/components/examples/field/root-provider.tsx';
import FieldSelect from '@/components/examples/field/select.tsx';
import FieldTextarea from '@/components/examples/field/textarea.tsx';

## Справочник API

<PrimitiveReference href="https://ark-ui.com/docs/components/field" />

## Выбор подходящего компонента

Используйте `Field`, когда одному элементу управления формы нужны видимая метка, пояснение, сообщение об ошибке
или общее состояние `disabled`, `invalid`, `required` либо `readOnly`.

- Выбирайте **`Field`** для одного элемента управления с меткой или поясняющим текстом.
- Выбирайте **`Fieldset`**, когда нескольким связанным элементам управления нужен общий заголовок группы.
- Используйте элемент управления отдельно, если связи ARIA на уровне поля и общее состояние не нужны.

## Базовый пример

Используйте `Field` как оболочку для одного элемента управления формы. Разместите `Field.Label` перед ним,
а затем при необходимости добавьте поясняющий текст или сообщение об ошибке.

<ExampleFrame variant="component">
  <FieldBasic />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/field/basic.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/field/field-basic.module.css"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

## Установка через shadcn

<ShadcnInstall packageName="field" />

## Анатомия

Публичное пространство имён `Field` сохраняет названия частей из Ark UI. Используйте части `Field.*` для
явной композиции Ark. В обычных формах лучше выбирать соответствующие компоненты moduix, например `Input`,
`Textarea` или `NativeSelect`.

```text
Field
├─ Field.Label
├─ Field.Input | Field.Textarea | Field.Select | custom control
├─ Field.HelperText
├─ Field.ErrorText
└─ Field.RequiredIndicator
```

| Часть                     | Назначение                                                                                           |
| ------------------------- | ----------------------------------------------------------------------------------------------------- |
| `Field` / `Field.Root`    | Корневой контекст. По умолчанию рендерит `<div role="group">`.                                        |
| `Field.RootProvider`      | Корень, использующий объект состояния `useField()`, созданный вне дерева компонентов.                |
| `Field.Context`           | Даёт доступ к текущему состоянию поля, не добавляя элемент в DOM.                                     |
| `Field.Label`             | Метка, связанная с текущим элементом управления.                                                      |
| `Field.Input`             | Низкоуровневый нативный input. Для обычного стилизованного поля используйте `Input`.                  |
| `Field.Textarea`          | Низкоуровневая нативная текстовая область. Для обычной текстовой области moduix используйте `Textarea`. |
| `Field.Select`            | Низкоуровневый нативный select. Для обычного стилизованного select используйте `NativeSelect`.        |
| `Field.HelperText`        | Поясняющий текст, связанный с элементом через `aria-describedby`.                                     |
| `Field.ErrorText`         | Текст ошибки, который рендерится, когда корень находится в состоянии `invalid`.                       |
| `Field.RequiredIndicator` | Визуальный маркер обязательного поля. С помощью `fallback` можно заменить маркер по умолчанию.        |
| `Field.Item`              | Вложенный контекст для группового элемента управления с `value`; ref указывает на локальную обёртку строки. |

## Композиция

### Рекомендуемая композиция

Оставьте в корне одну видимую метку, а под ней разместите основной элемент управления moduix, например `Input`,
`Textarea` или `NativeSelect`. Добавьте поясняющий текст или сообщение об ошибке, если пользователю нужны дополнительные
сведения или результат валидации.

Компоненты со своей составной корневой частью, например `Checkbox`, `RadioGroup` или `Select`, тоже можно размещать
внутри `Field`. У таких компонентов собственные свойства состояния, поэтому передавайте `required`, `invalid`,
`disabled` или `readOnly` в корень дочернего компонента, если именно он управляет этим состоянием.

### Когда нужна собственная композиция

Используйте `Field.Item value` вместе с пропом `target` у корня, когда в поле несколько нативных элементов управления,
а корневая метка должна быть связана только с одним из них. `Field.Input`, `Field.Textarea` и `Field.Select` доступны
для низкоуровневой композиции в стиле Ark, но в прикладном коде обычно лучше использовать отдельные компоненты moduix.

Если пользовательский элемент управления не работает с контекстом Ark Field, используйте `Field.Context` или
`useFieldContext` из `@moduix/react`, чтобы получить состояние поля и настроить его идентификаторы и ARIA-атрибуты.

### Свойства по умолчанию

| Свойство   | По умолчанию | Примечания                                                                                          |
| ---------- | ------------ | --------------------------------------------------------------------------------------------------- |
| `disabled` | `false`      | Помечает поле как отключённое и передаёт состояние поддерживаемым частям поля.                      |
| `invalid`  | `false`      | Управляет `Field.ErrorText`; передавайте свойство и составному дочернему компоненту, если он сам управляет валидацией. |
| `readOnly` | `false`      | Помечает поле доступным только для чтения и передаёт состояние поддерживаемым частям.               |
| `required` | `false`      | Помечает поле как обязательное. `Field.RequiredIndicator` по-прежнему нужно добавить явно.            |
| `target`   | -            | Значение `value` у `Field.Item`, на которое должна указывать корневая метка.                            |

### Особенности поведения

- `Field.ErrorText` не появляется в DOM, пока корень не находится в состоянии `invalid`.
- Поле не проверяет значения. Приложение или библиотека форм сами решают, когда нужно установить `invalid` в `true`.
- Длинные подписи, поясняющие тексты и сообщения об ошибках переносятся внутри поля и не выходят за пределы контейнера.
- Передайте в `Field.RootProvider` значение, возвращённое `useField()` из `@moduix/react`. Не используйте `Field.Root`
  и `Field.RootProvider` для одного и того же экземпляра поля.
- Используйте `Field.Context` или `useFieldContext` из `@moduix/react`, когда пользовательскому элементу управления нужно
  текущее состояние поля. Свойства Ark, например `ids`, `target` и `asChild`, доступны у соответствующих частей.
- `Field.Item` передаёт ref обёртке строки moduix, которую рендерит вокруг дочерних элементов.
- Составные компоненты, такие как `Checkbox` и `RadioGroup`, сами управляют своими нативными элементами формы. Сам `Field`
  не рендерит скрытый input.

## Примеры

### Checkbox

Используйте `Field` вокруг флажка, если ему нужны поясняющий текст или сообщение об ошибке на уровне поля. Передавайте
в `Checkbox` его собственные свойства состояния и формы.

<ExampleFrame variant="component">
  <FieldCheckbox />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/field/checkbox.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/field/field-checkbox.module.css"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Управляемое состояние `invalid`

Вычисляйте `invalid` из локального состояния, если валидация должна обновляться во время ввода. Текст ошибки появляется
только пока поле находится в невалидном состоянии.

<ExampleFrame variant="component">
  <FieldControlledInvalid />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/field/controlled-invalid.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/field/field-controlled-invalid.module.css"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Отключённое состояние

Установите `disabled` у корня, чтобы передать это состояние поддерживаемым частям поля Ark.

<ExampleFrame variant="component">
  <FieldDisabled />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/field/disabled.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/field/field-disabled.module.css"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Недопустимое состояние

Установите `invalid` у корня, когда валидацией управляет состояние приложения или библиотека форм. Если составной
дочерний компонент сам управляет невалидным состоянием, передайте это свойство и в его корень.

<ExampleFrame variant="component">
  <FieldInvalid />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/field/invalid.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/field/field-invalid.module.css"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Нативный Select

Используйте `NativeSelect` для нативного браузерного select. `Field` свяжет его с меткой, состоянием и описанием поля.

<ExampleFrame variant="component">
  <FieldNativeSelect />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/field/native-select.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/field/field-native-select.module.css"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### RadioGroup

Используйте `Field` с `RadioGroup`, когда одна метка, поясняющий текст или сообщение об ошибке должны описывать весь
набор вариантов, а не отдельные радиокнопки. Собственные свойства состояния передавайте в `RadioGroup`.

<ExampleFrame variant="component">
  <FieldRadioGroup />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/field/radiogroup.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/field/field-radiogroup.module.css"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Только для чтения

Установите `readOnly` у корня, если значение элемента управления можно скопировать, но нельзя изменить.

<ExampleFrame variant="component">
  <FieldReadOnly />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/field/read-only.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/field/field-read-only.module.css"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Select

Используйте составной `Select`, когда нативного select недостаточно и нужен настраиваемый список вариантов или интерфейс
выбора. Если пользователю нужно вводить текст для поиска или создания значения, используйте `Combobox`. Поскольку `Select`
сам управляет своим состоянием, при необходимости передавайте ему `required`, `invalid`, `disabled` или `readOnly`.

<ExampleFrame variant="component">
  <FieldSelect />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/field/select.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/field/field-select.module.css"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Текстовая область

Используйте `Textarea` для многострочного ввода, сохраняя связи поля с меткой, описанием и состоянием.

<ExampleFrame variant="component">
  <FieldTextarea />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/field/textarea.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/field/field-textarea.module.css"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Расширенная настройка

Используйте `Field.Item` с пропом `target` у корня, когда одна метка должна быть связана с элементом управления внутри группы.

<ExampleFrame variant="component">
  <FieldItemTarget />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/field/item-target.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/field/field-item-target.module.css"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

Используйте `useField` вместе с `Field.RootProvider`, когда состояние поля нужно создать вне дерева отрисовки.

<ExampleFrame variant="component">
  <FieldRootProvider />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/field/root-provider.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/field/field-root-provider.module.css"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

## Стилизация

Настраивайте `Field` через `className`, атрибуты Ark `data-scope="field"` и `data-part="..."`, хуки moduix
`data-slot="field-*"` и публичные CSS-переменные `--moduix-field-*`, приведённые ниже.

### CSS-свойства

<div className="docs-reference">
  <Tabs
    values={[{ label: 'CSS-переменные', value: 'CSS Variables' }]}
    className="docs-reference-tabs"
    contentItemClassName="docs-reference-panel"
  >
    <Tab>
      <FieldDocs.FieldCssPropertiesPanel />
    </Tab>
  </Tabs>
</div>

### Хуки стилизации

Ark добавляет `data-disabled`, `data-invalid`, `data-readonly` и `data-required` к соответствующим частям.
`Field.ErrorText` существует только пока текущий контекст поля находится в невалидном состоянии. Идентификатор
поясняющего текста входит в `aria-describedby` элемента управления, а идентификатор активного сообщения об ошибке
передаётся в `aria-errormessage`. Каждая часть DOM также сохраняет стабильный хук `data-slot="field-*"`.
