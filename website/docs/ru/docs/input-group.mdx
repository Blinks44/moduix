---
title: Input Group
component: input-group
description: Создавайте доступные группы полей ввода в React и Solid с установкой из пакета или через shadcn CLI.
---

import * as InputGroupDocs from '@/components/examples/input-group/css-properties';
import InputGroupAsChild from '@/components/examples/input-group/as-child.tsx';
import InputGroupBasic from '@/components/examples/input-group/basic.tsx';
import InputGroupDisabled from '@/components/examples/input-group/disabled.tsx';
import InputGroupFieldState from '@/components/examples/input-group/field-state.tsx';
import InputGroupPrefixAndSuffix from '@/components/examples/input-group/prefix-and-suffix.tsx';
import InputGroupSizes from '@/components/examples/input-group/sizes.tsx';
import InputGroupWithAction from '@/components/examples/input-group/with-action.tsx';
import inputGroupSolidAsChild from '../../../snippets/input-group/solid/as-child.tsx?raw';
import inputGroupSolidBasic from '../../../snippets/input-group/solid/basic.tsx?raw';
import inputGroupSolidDisabled from '../../../snippets/input-group/solid/disabled.tsx?raw';
import inputGroupSolidFieldState from '../../../snippets/input-group/solid/field-state.tsx?raw';
import inputGroupSolidPrefixAndSuffix from '../../../snippets/input-group/solid/prefix-and-suffix.tsx?raw';
import inputGroupSolidSizes from '../../../snippets/input-group/solid/sizes.tsx?raw';
import inputGroupSolidWithAction from '../../../snippets/input-group/solid/with-action.tsx?raw';

## Справочник API

`InputGroup` является композицией moduix, а не отдельным примитивом Ark UI. Он объединяет элементы
фабрики Ark с компонентами moduix `Input` и `Button`, построенными на Ark. Композиция
соответствует рекомендациям Ark по [композиции](https://ark-ui.com/docs/guides/composition),
[стилизации](https://ark-ui.com/docs/guides/styling) и [работе с ref](https://ark-ui.com/docs/guides/ref).

## Какой компонент выбрать

- Используйте **`Input`** для обычного однострочного поля.
- Используйте **`InputGroup`**, если в поле нужно разместить префикс, суффикс или действие.
- Используйте **`PasswordInput`** для поля пароля с переключателем видимости.
- Используйте **`NumberInput`** для числового поля с шагом изменения.
- Используйте **`Textarea`** для многострочного текста.
- Используйте **`Editable`**, если текст сначала должен отображаться как значение, а затем переходить в режим редактирования.

## Базовый пример

Интерактивное

Используйте `InputGroup` как корневой компонент и добавляйте к единственному
`InputGroup.Input` только необходимые части `Addon`, `Text` или `Button`.

<ExampleFrame variant="component">
  <InputGroupBasic />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/input-group/basic.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={inputGroupSolidBasic} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/input-group/input-group-basic.module.css"
```
</div>

</ExampleCode>

## Установка через shadcn

Полную настройку пакета, реестра и стилей см. в разделе [Быстрый старт](/docs/quick-start).

<Tabs groupId="framework">
  <Tab label="React" value="react">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-react/input-group' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-react-tailwind/input-group' },
  ]}
/>

  </Tab>
  <Tab label="Solid" value="solid">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-solid/input-group' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-solid-tailwind/input-group' },
  ]}
/>

  </Tab>
</Tabs>

## Анатомия

```text
InputGroup / InputGroup.Root
├─ InputGroup.Addon (необязательно)
├─ InputGroup.Input
├─ InputGroup.Text (необязательно)
└─ InputGroup.Button (необязательно)
```

| Часть                            | Назначение                                                  |
| -------------------------------- | ----------------------------------------------------------- |
| `InputGroup` / `InputGroup.Root` | Пассивный контейнер с общей стилизацией и контекстом размера. |
| `InputGroup.Input`               | Нативный `<input>`, который содержит значение формы и состояние поля. |
| `InputGroup.Addon`               | Префикс или суффикс с заливкой.                             |
| `InputGroup.Text`                | Встроенный текст без заливки.                               |
| `InputGroup.Button`              | Кнопка действия, связанного с полем.                       |

## Композиция

В каждой группе оставляйте ровно один `InputGroup.Input`. Вложенное поле отвечает за значение,
поведение формы, состояние валидации и `ref`, поэтому управляемые и неуправляемые свойства
задавайте на нём, а метку и сообщения валидации размещайте в `Field`. Размер корневого элемента
наследуют `InputGroup.Input` и `InputGroup.Button`; для кнопок по умолчанию используется
`type="button"`, но состояние `disabled` от `Field` они не наследуют. В React для управляемого
поля используйте `onChange`, а в Solid - `onInput`.

Используйте `Addon` и `Text` для неинтерактивного содержимого, а `Button` - для действий. Корень,
`Addon` и `Text` поддерживают `asChild` с одним семантическим дочерним элементом, принимающим
объединённые свойства и `ref`. Используйте `Editable`, если значение должно переключаться между
режимами просмотра и редактирования.

## Примеры

### Отключённое состояние

Передайте `disabled` нативному полю. Связанные кнопки отключайте отдельно, если они тоже должны
быть недоступны.

<ExampleFrame variant="component">
  <InputGroupDisabled />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/input-group/disabled.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={inputGroupSolidDisabled} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/input-group/input-group-disabled.module.css"
```
</div>

</ExampleCode>

### Состояние поля

Задайте `invalid` у `Field`, если проверка не пройдена. Ark передаст это состояние и нужные
ARIA-атрибуты в `InputGroup.Input`, а группа отразит состояние поля.

<ExampleFrame variant="component">
  <InputGroupFieldState />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/input-group/field-state.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={inputGroupSolidFieldState} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/input-group/input-group-field-state.module.css"
```
</div>

</ExampleCode>

### Префикс и суффикс

Используйте `Addon` для префикса или суффикса с заливкой, а `Text` - для встроенного текста без заливки.

<ExampleFrame variant="component">
  <InputGroupPrefixAndSuffix />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/input-group/prefix-and-suffix.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={inputGroupSolidPrefixAndSuffix} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/input-group/input-group-prefix-and-suffix.module.css"
```
</div>

</ExampleCode>

### Размеры

Задайте `size` один раз у корневого элемента, чтобы поле, дополнение, текст и кнопка оставались выровнены.

<ExampleFrame variant="component">
  <InputGroupSizes />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/input-group/sizes.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={inputGroupSolidSizes} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/input-group/input-group-sizes.module.css"
```
</div>

</ExampleCode>

### С действием

Используйте `InputGroup.Button` для действия, связанного со значением поля. В этом примере поле
управляемое, а кнопка `Send` становится доступной только после ввода значения.

<ExampleFrame variant="component">
  <InputGroupWithAction />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/input-group/with-action.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={inputGroupSolidWithAction} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/input-group/input-group-with-action.module.css"
```
</div>

</ExampleCode>

### Расширенная настройка

Используйте `asChild`, если вместо стандартного `div` нужен собственный контейнер. Группа сохранит
оформление и контекст размера, а дочерний элемент должен остаться семантическим контейнером для разметки.

<ExampleFrame variant="component">
  <InputGroupAsChild />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/input-group/as-child.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={inputGroupSolidAsChild} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/input-group/input-group-as-child.module.css"
```
</div>

</ExampleCode>

## Стилизация

Для структурных стилей передавайте экспортируемым частям `className` в React или `class` в Solid.
Состояния лучше оформлять через атрибуты данных Ark, например `data-invalid`, `data-disabled` и
`data-readonly`, вместе с селекторами `data-slot` moduix. Варианты на Tailwind настраиваются
обычными утилитами и пользовательскими классами; перечисленные ниже переменные относятся к
вариантам с CSS Modules. Атрибуты данных и селекторы `data-slot` общие для обоих подходов.

### CSS-свойства

<CssPropertiesSection properties={InputGroupDocs.inputGroupOverrideCssProperties} />

### Хуки стилизации

- `data-slot="input-group-root"` и `data-size="xs" | "sm" | "md" | "lg" | "xl"` на корне
- `data-scope="input-group"` и `data-part="root" | "addon" | "text"` на частях фабрики Ark
- `data-slot="input-root"` вместе с атрибутами состояния поля Ark
- `data-slot="input-group-addon"`
- `data-slot="input-group-text"`
- `data-slot="input-group-button"` у кнопки действия в группе
- вложенные состояния `data-invalid`, `aria-invalid="true"`, `data-disabled`/`:disabled` и `data-readonly`/`:read-only` отражаются на корне через `:has(...)`

Слоты и атрибуты `data-slot`, `data-scope`, `data-part` и `data-size`, которыми управляет
компонент, остаются неизменными, даже если вы передаёте собственные свойства. `className` в React
и `class` в Solid объединяются со стилями компонента.
