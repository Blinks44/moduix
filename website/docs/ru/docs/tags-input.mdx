---
title: Tags Input
component: tags-input
description: Редактируемое поле для списка строковых тегов с проверкой, отправкой формы и необязательными подсказками.
---

import * as TagsInputDocs from '@/components/examples/tags-input/css-properties';
import TagsInputAdvancedCustomization from '@/components/examples/tags-input/advanced-customization.tsx';
import TagsInputAllowDuplicates from '@/components/examples/tags-input/allow-duplicates.tsx';
import TagsInputBasic from '@/components/examples/tags-input/basic.tsx';
import TagsInputControlled from '@/components/examples/tags-input/controlled.tsx';
import TagsInputControlledInputValue from '@/components/examples/tags-input/controlled-input-value.tsx';
import TagsInputDelimiterAndPaste from '@/components/examples/tags-input/delimiter-and-paste.tsx';
import TagsInputDisableEditing from '@/components/examples/tags-input/disable-editing.tsx';
import TagsInputDisabledAndReadOnly from '@/components/examples/tags-input/disabled-and-read-only.tsx';
import TagsInputField from '@/components/examples/tags-input/field.tsx';
import TagsInputForm from '@/components/examples/tags-input/form.tsx';
import TagsInputMaxWithOverflow from '@/components/examples/tags-input/max-with-overflow.tsx';
import TagsInputRootProvider from '@/components/examples/tags-input/root-provider.tsx';
import TagsInputSanitizeAndBlur from '@/components/examples/tags-input/sanitize-and-blur.tsx';
import TagsInputValidationAndMax from '@/components/examples/tags-input/validation-and-max.tsx';
import TagsInputWithCombobox from '@/components/examples/tags-input/with-combobox.tsx';
import tagsInputSolidAdvancedCustomization from '../../../snippets/tags-input/solid/advanced-customization.tsx?raw';
import tagsInputSolidAllowDuplicates from '../../../snippets/tags-input/solid/allow-duplicates.tsx?raw';
import tagsInputSolidBasic from '../../../snippets/tags-input/solid/basic.tsx?raw';
import tagsInputSolidControlled from '../../../snippets/tags-input/solid/controlled.tsx?raw';
import tagsInputSolidControlledInputValue from '../../../snippets/tags-input/solid/controlled-input-value.tsx?raw';
import tagsInputSolidDelimiterAndPaste from '../../../snippets/tags-input/solid/delimiter-and-paste.tsx?raw';
import tagsInputSolidDisableEditing from '../../../snippets/tags-input/solid/disable-editing.tsx?raw';
import tagsInputSolidDisabledAndReadOnly from '../../../snippets/tags-input/solid/disabled-and-read-only.tsx?raw';
import tagsInputSolidField from '../../../snippets/tags-input/solid/field.tsx?raw';
import tagsInputSolidForm from '../../../snippets/tags-input/solid/form.tsx?raw';
import tagsInputSolidMaxWithOverflow from '../../../snippets/tags-input/solid/max-with-overflow.tsx?raw';
import tagsInputSolidRootProvider from '../../../snippets/tags-input/solid/root-provider.tsx?raw';
import tagsInputSolidSanitizeAndBlur from '../../../snippets/tags-input/solid/sanitize-and-blur.tsx?raw';
import tagsInputSolidValidationAndMax from '../../../snippets/tags-input/solid/validation-and-max.tsx?raw';
import tagsInputSolidWithCombobox from '../../../snippets/combobox/solid/with-tags-input.tsx?raw';

## Справочник API

<PrimitiveReference href="https://ark-ui.com/docs/components/tags-input" />

## Как выбрать компонент

Используйте `TagsInput`, когда пользователю нужно вводить и редактировать список строковых значений
в одном элементе управления формы.

- Выбирайте **`TagsInput`** для меток, навыков, фильтров, получателей, тем и других списков строк, которые пользователь вводит сам.
- Выбирайте **`Tag`**, когда интерфейс только показывает существующий токен и может предложить отдельное действие удаления.
- Выбирайте **`Combobox`**, когда пользователь выбирает значение из известного списка, а основное взаимодействие связано с фильтрацией.

## Базовый пример


Начните с `TagsInput`, `TagsInputLabel`, `TagsInputControl`, `TagsInputItems`,
`TagsInputInput` и `TagsInputClearTrigger`. Добавьте `TagsInputHiddenInput`, если теги нужно
отправлять вместе с формой.

<ExampleFrame variant="component">
  <TagsInputBasic />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/tags-input/basic.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={tagsInputSolidBasic} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/tags-input/tags-input-basic.module.css"
```
</div>

</ExampleCode>

## Установка через shadcn

Полную настройку пакета, реестра и стилей см. в разделе [Быстрый старт](/docs/quick-start).

<Tabs groupId="framework">
  <Tab label="React" value="react">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-react/tags-input' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-react-tailwind/tags-input' },
  ]}
/>

  </Tab>
  <Tab label="Solid" value="solid">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-solid/tags-input' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-solid-tailwind/tags-input' },
  ]}
/>

  </Tab>
</Tabs>

## Анатомия

```text
TagsInput
├─ TagsInputLabel
├─ TagsInputControl
│  ├─ TagsInputItems
│  └─ TagsInputItem[index, value]
│     ├─ TagsInputItemPreview
│     │  ├─ TagsInputItemText
│     │  └─ TagsInputItemDeleteTrigger
│     └─ TagsInputItemInput
│  ├─ TagsInputInput
│  └─ TagsInputClearTrigger
└─ TagsInputHiddenInput (explicit)

TagsInputRootProvider
└─ same part tree connected to a useTagsInput() store
```

| Часть | Назначение |
| --- | --- |
| `TagsInput` | Корень, который управляет состоянием, значениями тегов, вводом, проверкой и формой. |
| `TagsInputRootProvider` | Подключает части к состоянию, которое вернул `useTagsInput()`. |
| `TagsInputLabel` | Доступная подпись поля тегов. |
| `TagsInputControl` | Видимая область с тегами, полем ввода и кнопкой очистки. |
| `TagsInputItems` | Стандартно отображает редактируемые теги из текущего состояния. |
| `TagsInputItem` | Один тег. Передайте `index` и `value` для собственных элементов. |
| `TagsInputItemContext` | Доступ через render prop к состоянию текущего элемента внутри дерева компонентов. |
| `TagsInputItemPreview` | Отображает тег вне режима редактирования. |
| `TagsInputItemText` | Видимый текст тега. |
| `TagsInputItemDeleteTrigger` | Удаляет один тег и по умолчанию показывает компактный значок закрытия. |
| `TagsInputItemInput` | Поле для редактирования тега на месте. |
| `TagsInputInput` | Поле ввода новых тегов. |
| `TagsInputClearTrigger` | Удаляет все теги и по умолчанию использует оформление общей кнопки закрытия. |
| `TagsInputHiddenInput` | Явно добавляемое нативное поле для отправки формы. |
| `TagsInputContext` | Render prop с доступом к состоянию корня внутри дерева компонента. |

## Композиция

Используйте короткую форму `<TagsInput>` для стандартной композиции. Оставьте `TagsInputItems`,
`TagsInputInput` и `TagsInputClearTrigger` внутри `TagsInputControl`. Добавьте
`TagsInputHiddenInput` явно и передайте корню `name`, если теги нужно отправлять с формой. Если
связанная форма находится вне дерева компонента, задайте `form`.

Для собственного содержимого или действий тегов используйте `TagsInputContext` и свяжите его
значения с `TagsInputItem`, передав `index` и `value`. Оставьте `TagsInputItemInput` внутри каждого
элемента, если нужно сохранить редактирование на месте. Используйте `TagsInputRootProvider` с
`useTagsInput()` только тогда, когда состояние или методы вроде `addValue`, `clearValue` и `focus`
нужны за пределами дерева компонента. В Solid store является accessor: читайте `tagsInput().value`
и вызывайте `tagsInput().addValue()`. При объединении с `Combobox` используйте общие идентификаторы
поля и элемента управления и объединяйте поля через `asChild`.

## Примеры

### Разрешить дубликаты

Включите `allowDuplicates`, если одна и та же строка может появляться несколько раз.

<ExampleFrame variant="component">
  <TagsInputAllowDuplicates />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs groupId="framework" values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]} className="docs-tabs">
    <Tab value="react">

```tsx pure file="../../../src/components/examples/tags-input/allow-duplicates.tsx"
```

    </Tab>
    <Tab value="solid"><CodeBlockRuntime lang="tsx" code={tagsInputSolidAllowDuplicates} /></Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/tags-input/tags-input-allow-duplicates.module.css"
```
</div>
</ExampleCode>

### Управляемое состояние

Храните массив тегов в состоянии компонента, если от него зависит другая часть экрана или его нужно
сбрасывать программно.

<ExampleFrame variant="component">
  <TagsInputControlled />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs groupId="framework" values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]} className="docs-tabs">
    <Tab value="react">

```tsx pure file="../../../src/components/examples/tags-input/controlled.tsx"
```

    </Tab>
    <Tab value="solid"><CodeBlockRuntime lang="tsx" code={tagsInputSolidControlled} /></Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/tags-input/tags-input-controlled.module.css"
```
</div>
</ExampleCode>

### Управляемое значение поля ввода

Управляйте `inputValue` отдельно, если другой элемент интерфейса должен заранее заполнить или
очистить поле ввода.

<ExampleFrame variant="component">
  <TagsInputControlledInputValue />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs groupId="framework" values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]} className="docs-tabs">
    <Tab value="react">

```tsx pure file="../../../src/components/examples/tags-input/controlled-input-value.tsx"
```

    </Tab>
    <Tab value="solid"><CodeBlockRuntime lang="tsx" code={tagsInputSolidControlledInputValue} /></Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/tags-input/tags-input-controlled-input-value.module.css"
```
</div>
</ExampleCode>

### Разделитель и вставка

Используйте `delimiter` вместе с `addOnPaste`, если вставленный текст должен превратиться в несколько
тегов. В этом примере разделителями служат запятая, точка с запятой и пробел.

<ExampleFrame variant="component">
  <TagsInputDelimiterAndPaste />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs groupId="framework" values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]} className="docs-tabs">
    <Tab value="react">

```tsx pure file="../../../src/components/examples/tags-input/delimiter-and-paste.tsx"
```

    </Tab>
    <Tab value="solid"><CodeBlockRuntime lang="tsx" code={tagsInputSolidDelimiterAndPaste} /></Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/tags-input/tags-input-delimiter-and-paste.module.css"
```
</div>
</ExampleCode>

### Запретить редактирование

Задайте `editable={false}`, если существующие теги можно удалять, но нельзя редактировать на месте.

<ExampleFrame variant="component">
  <TagsInputDisableEditing />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs groupId="framework" values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]} className="docs-tabs">
    <Tab value="react">

```tsx pure file="../../../src/components/examples/tags-input/disable-editing.tsx"
```

    </Tab>
    <Tab value="solid"><CodeBlockRuntime lang="tsx" code={tagsInputSolidDisableEditing} /></Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/tags-input/tags-input-disable-editing.module.css"
```
</div>
</ExampleCode>

### Отключено и только для чтения

Используйте `disabled` для неинтерактивного поля. `readOnly` оставляет теги видимыми, но запрещает
добавлять, удалять и редактировать их.

<ExampleFrame variant="component">
  <TagsInputDisabledAndReadOnly />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs groupId="framework" values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]} className="docs-tabs">
    <Tab value="react">

```tsx pure file="../../../src/components/examples/tags-input/disabled-and-read-only.tsx"
```

    </Tab>
    <Tab value="solid"><CodeBlockRuntime lang="tsx" code={tagsInputSolidDisabledAndReadOnly} /></Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/tags-input/tags-input-disabled-and-read-only.module.css"
```
</div>
</ExampleCode>

### Field

Оберните `TagsInput` в `Field`, если подпись, поясняющий текст, невалидное состояние и сообщение об
ошибке должны использовать общую схему доступности форм Ark.

<ExampleFrame variant="component">
  <TagsInputField />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs groupId="framework" values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]} className="docs-tabs">
    <Tab value="react">

```tsx pure file="../../../src/components/examples/tags-input/field.tsx"
```

    </Tab>
    <Tab value="solid"><CodeBlockRuntime lang="tsx" code={tagsInputSolidField} /></Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/tags-input/tags-input-field.module.css"
```
</div>
</ExampleCode>

### Форма

Передайте корню `name` и добавьте `TagsInputHiddenInput`, чтобы список тегов попал в `FormData`.

<ExampleFrame variant="component">
  <TagsInputForm />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs groupId="framework" values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]} className="docs-tabs">
    <Tab value="react">

```tsx pure file="../../../src/components/examples/tags-input/form.tsx"
```

    </Tab>
    <Tab value="solid"><CodeBlockRuntime lang="tsx" code={tagsInputSolidForm} /></Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/tags-input/tags-input-form.module.css"
```
</div>
</ExampleCode>

### Максимум с превышением

Используйте `allowOverflow` вместе с `max`, если пользователь может временно превысить лимит, а поле
должно сообщить о невалидном состоянии.

<ExampleFrame variant="component">
  <TagsInputMaxWithOverflow />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs groupId="framework" values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]} className="docs-tabs">
    <Tab value="react">

```tsx pure file="../../../src/components/examples/tags-input/max-with-overflow.tsx"
```

    </Tab>
    <Tab value="solid"><CodeBlockRuntime lang="tsx" code={tagsInputSolidMaxWithOverflow} /></Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/tags-input/tags-input-max-with-overflow.module.css"
```
</div>
</ExampleCode>

### TagsInputRootProvider

Используйте `useTagsInput` с `TagsInputRootProvider`, когда состояние и императивные методы нужны за пределами
дерева отрисованного поля.

<ExampleFrame variant="component">
  <TagsInputRootProvider />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs groupId="framework" values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]} className="docs-tabs">
    <Tab value="react">

```tsx pure file="../../../src/components/examples/tags-input/root-provider.tsx"
```

    </Tab>
    <Tab value="solid"><CodeBlockRuntime lang="tsx" code={tagsInputSolidRootProvider} /></Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/tags-input/tags-input-root-provider.module.css"
```
</div>
</ExampleCode>

### Нормализация и потеря фокуса

Используйте `sanitizeValue`, чтобы нормализовать вводимые теги, а `blurBehavior="add"`, чтобы добавить
текущее значение при потере фокуса.

<ExampleFrame variant="component">
  <TagsInputSanitizeAndBlur />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs groupId="framework" values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]} className="docs-tabs">
    <Tab value="react">

```tsx pure file="../../../src/components/examples/tags-input/sanitize-and-blur.tsx"
```

    </Tab>
    <Tab value="solid"><CodeBlockRuntime lang="tsx" code={tagsInputSolidSanitizeAndBlur} /></Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/tags-input/tags-input-sanitize-and-blur.module.css"
```
</div>
</ExampleCode>

### Проверка и максимум

Используйте `validate`, `max` и `onValueInvalid`, чтобы отклонять недопустимые значения и понимать
причину отказа. Добавьте `maxLength`, если поле не должно принимать слишком длинные значения до
запуска проверки.

<ExampleFrame variant="component">
  <TagsInputValidationAndMax />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs groupId="framework" values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]} className="docs-tabs">
    <Tab value="react">

```tsx pure file="../../../src/components/examples/tags-input/validation-and-max.tsx"
```

    </Tab>
    <Tab value="solid"><CodeBlockRuntime lang="tsx" code={tagsInputSolidValidationAndMax} /></Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/tags-input/tags-input-validation-and-max.module.css"
```
</div>
</ExampleCode>

### Вместе с Combobox

Объедините `TagsInput` с `Combobox`, если поле должно предлагать варианты из известного списка и
одновременно разрешать пользовательские теги. Используйте общие идентификаторы поля и элемента
управления и объединяйте поля через `asChild`.

<ExampleFrame variant="component">
  <TagsInputWithCombobox />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs groupId="framework" values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]} className="docs-tabs">
    <Tab value="react">

```tsx pure file="../../../src/components/examples/tags-input/with-combobox.tsx"
```

    </Tab>
    <Tab value="solid"><CodeBlockRuntime lang="tsx" code={tagsInputSolidWithCombobox} /></Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/tags-input/tags-input-with-combobox.module.css"
```
</div>
</ExampleCode>

### Расширенная настройка

Не используйте `TagsInputItems`, если каждому тегу нужны собственное содержимое или действия. Свяжите значения
корня с полной структурой частей и оставьте `TagsInputItemInput` внутри каждого тега, если редактирование на
месте всё ещё нужно.

<ExampleFrame variant="component">
  <TagsInputAdvancedCustomization />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs groupId="framework" values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]} className="docs-tabs">
    <Tab value="react">

```tsx pure file="../../../src/components/examples/tags-input/advanced-customization.tsx"
```

    </Tab>
    <Tab value="solid"><CodeBlockRuntime lang="tsx" code={tagsInputSolidAdvancedCustomization} /></Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/tags-input/tags-input-advanced-customization.module.css"
```
</div>
</ExampleCode>

## Стилизация

### CSS-свойства

<div className="docs-reference">
  <Tabs
    values={[{ label: 'CSS-переменные', value: 'CSS Variables' }]}
    className="docs-reference-tabs"
    contentItemClassName="docs-reference-panel"
  >
    <Tab>
      <TagsInputDocs.TagsInputCssPropertiesPanel />
    </Tab>
  </Tabs>
</div>

### Хуки стилизации

- `TagsInput` и `TagsInputRootProvider` предоставляют `data-slot="tags-input-root"` и `data-slot="tags-input-root-provider"`.
- Части Ark сохраняют `data-scope="tags-input"` и значения `data-part` из анатомии.
- Состояния можно стилизовать через `data-disabled`, `data-readonly`, `data-invalid`, `data-focus`, `data-empty`, `data-required`, `data-value` и `data-highlighted`, если Ark добавляет их к части.
- Кнопки удаления и очистки имеют стабильные `data-slot`. Для отдельного тега используется компактный значок закрытия, а очистка всех тегов использует оформление общей кнопки закрытия.
- CSS Modules используют документированные переменные `--moduix-tags-input-*`. Варианты на Tailwind используют нативные утилиты и классы, которые задаёт потребитель.
- В режиме только для чтения moduix скрывает поле ввода, кнопки удаления тегов и кнопку очистки, оставляя видимыми только неизменяемые значения тегов.

## Доступность

Ark связывает подпись с полем, управляет выделением и редактированием тегов, удалением, вставкой и
скрытым полем формы. Явно добавленное скрытое поле сериализует список тегов для нативной отправки
формы. Передайте `name`, а при необходимости и `form`, чтобы связать его с формой.

### Поддержка клавиатуры

- `Enter` в поле ввода добавляет его текущее значение. `Enter` на выделенном теге переводит его в режим редактирования.
- Двойной щелчок по тегу также включает редактирование, если `editable` имеет значение `true`.
- Когда поле ввода пусто или курсор стоит в его начале, `ArrowLeft` и `ArrowRight` перемещают визуальный фокус между тегами.
- `Backspace` и `Delete` удаляют выделенный тег. `Backspace` также удаляет последний тег, если поле ввода пусто.
- При включённом `addOnPaste` сочетание `Control+V` добавляет вставленные значения после разделения их по `delimiter`.
