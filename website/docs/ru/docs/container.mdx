---
title: Container
component: container
description: Адаптивный примитив разметки для читабельной ширины содержимого и гибких горизонтальных отступов.
---

import * as ContainerDocs from '@/components/examples/container/css-properties';
import ContainerBasic from '@/components/examples/container/basic.tsx';
import ContainerFullWidth from '@/components/examples/container/full-width.tsx';
import ContainerGutters from '@/components/examples/container/gutters.tsx';
import ContainerSemanticElement from '@/components/examples/container/semantic-element.tsx';
import ContainerWithBleed from '@/components/examples/container/with-bleed.tsx';
import containerSolidBasic from '../../../snippets/container/solid/basic.tsx?raw';
import containerSolidFullWidth from '../../../snippets/container/solid/full-width.tsx?raw';
import containerSolidGutters from '../../../snippets/container/solid/gutters.tsx?raw';
import containerSolidSemanticElement from '../../../snippets/container/solid/semantic-element.tsx?raw';
import containerSolidWithBleed from '../../../snippets/container/solid/with-bleed.tsx?raw';

## Справочник API

В Ark UI нет отдельного примитива `container`. В moduix `Container` создан с помощью официальной
[фабрики Ark](https://ark-ui.com/docs/guides/composition#the-ark-factory), поэтому для него действуют рекомендации Ark по
[композиции](https://ark-ui.com/docs/guides/composition), [стилизации](https://ark-ui.com/docs/guides/styling) и
[передаче ref](https://ark-ui.com/docs/guides/ref).

## Базовый пример


Импортируйте `Container` из подходящего пакета: `@moduix/react/container` или `@moduix/solid/container` для CSS Modules,
а для Tailwind CSS используйте `@moduix/react-tailwind/container` или `@moduix/solid-tailwind/container`.

Используйте `Container` для центрированной колонки с читабельной шириной. Компонент добавляет адаптивные
горизонтальные отступы, а вертикальные интервалы остаются задачей внешней разметки.

<ExampleFrame variant="component">
  <ContainerBasic />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/container/basic.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={containerSolidBasic} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/container/container-basic.module.css"
```
</div>

</ExampleCode>

## Установка через shadcn

Полную настройку пакета, реестра и стилей см. в разделе [Быстрый старт](/docs/quick-start).

<Tabs groupId="framework">
  <Tab label="React" value="react">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-react/container' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-react-tailwind/container' },
  ]}
/>

  </Tab>
  <Tab label="Solid" value="solid">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-solid/container' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-solid-tailwind/container' },
  ]}
/>

  </Tab>
</Tabs>

## Анатомия

`Container` выводит один корневой элемент и не добавляет дополнительных обёрток.

```text
Container
└─ дочерние элементы
```

| Часть       | Назначение                                               |
| ----------- | -------------------------------------------------------- |
| `Container` | Корневой элемент разметки. Центрирует содержимое и задаёт его ширину и горизонтальные отступы. |

## Композиция

`Container` не управляет состоянием и не содержит дочерних частей, которые нужно согласовывать. Используйте его как
корень и размещайте содержимое страницы или секции непосредственно внутри.

Используйте `asChild`, когда семантический элемент должен стать отрисованным корнем. Передавайте ровно один дочерний
элемент: в React он вкладывается внутрь `Container`, а в Solid используется форма с render-функцией
`asChild={(props) => <main {...props()} />}`. Размещайте `Bleed` внутри `Container`, если только один дочерний блок
должен выйти за пределы ограниченной текстовой колонки.

## Примеры

### На всю ширину

Используйте `size="full"`, чтобы широкая секция заняла ширину родителя и сохранила адаптивный отступ по краям.

<ExampleFrame variant="component">
  <ContainerFullWidth />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/container/full-width.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={containerSolidFullWidth} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/container/container-full-width.module.css"
```
</div>

</ExampleCode>

### Отступы по краям

Используйте `gutter`, чтобы выбрать размер адаптивных горизонтальных отступов.

<ExampleFrame variant="component">
  <ContainerGutters />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/container/gutters.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={containerSolidGutters} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/container/container-gutters.module.css"
```
</div>

</ExampleCode>

### Семантический элемент

Используйте `asChild`, когда оболочка разметки также должна описывать структуру документа.

<ExampleFrame variant="component">
  <ContainerSemanticElement />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/container/semantic-element.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={containerSolidSemanticElement} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/container/container-semantic-element.module.css"
```
</div>

</ExampleCode>

### С Bleed

Сохраните ограниченную ширину текста и позвольте только более широкому дочернему блоку выйти за пределы колонки.

<ExampleFrame variant="component">
  <ContainerWithBleed />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/container/with-bleed.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={containerSolidWithBleed} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/container/container-with-bleed.module.css"
```
</div>

</ExampleCode>

## Стилизация

Для пакетов CSS Modules переопределяйте CSS-переменные на корневом элементе, если оболочке страницы нужны другие
значения ширины или отступов. Пакеты Tailwind вместо этих переменных используют utility-классы и классы потребителя.
Для локальных стилей блока, например фона, границ или внутренних отступов, используйте `className` в React и `class` в
Solid.

### CSS-свойства

<CssPropertiesSection properties={ContainerDocs.containerOverrideCssProperties} />

Эти переменные относятся к пакетам CSS Modules. В пакетах Tailwind те же варианты `size` и `gutter` задаются через
utility-классы.

### Хуки стилизации

- `Container` добавляет `data-slot="container-root"` на корневой элемент.
- `data-scope="container"` и `data-part="root"` соответствуют соглашениям Ark по стилизации.
- `data-size` выбирает предустановку ширины: `xs`, `sm`, `md`, `lg`, `xl`, `full`.
- `data-gutter` выбирает предустановку отступов: `none`, `sm`, `md`, `lg`.
- Переменные `--moduix-container-*` управляют горизонтальными отступами и расчётом `max-width` в CSS Modules.
  Вертикальные интервалы и расположение дочерних элементов задаются внешними стилями.
