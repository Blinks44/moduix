---
title: Close Button
component: close-button
description: Кнопка только со значком для закрытия уведомлений, карточек, оверлеев и других элементов интерфейса.
---

import * as CloseButtonDocs from '@/components/examples/close-button/css-properties';
import CloseButtonAsChild from '@/components/examples/close-button/as-child.tsx';
import CloseButtonBasic from '@/components/examples/close-button/basic.tsx';
import CloseButtonCustomChildren from '@/components/examples/close-button/custom-children.tsx';
import CloseButtonDisabled from '@/components/examples/close-button/disabled-state.tsx';
import closeButtonSolidAsChild from '../../../snippets/close-button/solid/as-child.tsx?raw';
import closeButtonSolidBasic from '../../../snippets/close-button/solid/basic.tsx?raw';
import closeButtonSolidCustomChildren from '../../../snippets/close-button/solid/custom-children.tsx?raw';
import closeButtonSolidDisabled from '../../../snippets/close-button/solid/disabled-state.tsx?raw';

## Справочник API

В Ark UI нет отдельного примитива для кнопки закрытия. `moduix` создаёт `CloseButton` с помощью официальной
[фабрики Ark](https://ark-ui.com/docs/guides/composition#the-ark-factory) и следует рекомендациям Ark по
[композиции](https://ark-ui.com/docs/guides/composition), [стилизации](https://ark-ui.com/docs/guides/styling) и
работе с [ref](https://ark-ui.com/docs/guides/ref).

## Базовый пример


Используйте `CloseButton`, чтобы добавить кнопку закрытия к уведомлению, карточке, оверлею или другой области
интерфейса. Свяжите обработчик `onClick` с состоянием, которое скрывает или удаляет эту область. Компонент
рендерит семантическую кнопку, задаёт доступное имя по умолчанию и показывает иконку `CloseIcon`, но не управляет
состоянием закрываемого элемента.

<ExampleFrame variant="component">
  <CloseButtonBasic />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/close-button/basic.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={closeButtonSolidBasic} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/close-button/close-button-basic.module.css"
```
</div>

</ExampleCode>

## Установка через shadcn

Полную настройку пакета, реестра и стилей см. в разделе [Быстрый старт](/docs/quick-start).

<Tabs groupId="framework">
  <Tab label="React" value="react">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-react/close-button' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-react-tailwind/close-button' },
  ]}
/>

  </Tab>
  <Tab label="Solid" value="solid">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-solid/close-button' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-solid-tailwind/close-button' },
  ]}
/>

  </Tab>
</Tabs>

## Анатомия

У `CloseButton` одна корневая часть, построенная по модели Ark.

```text
CloseButton
└─ root[data-scope="close-button"][data-part="root"][data-slot="close-button-root"]
   └─ CloseIcon (стандартная) | пользовательские дочерние элементы
```

| Часть         | Назначение                                                              |
| ------------- | ------------------------------------------------------------------------ |
| `CloseButton` | Интерактивный элемент закрытия на основе семантики кнопки с поддержкой доступности. |

## Композиция

`CloseButton` не хранит состояние закрываемой области: обработчик `onClick` должен изменить состояние, которое
скрывает или удаляет её. Если у Dialog, Drawer или другого оверлея уже есть триггер закрытия, используйте его,
чтобы родительский компонент сохранил управление закрытием и возвратом фокуса.

Используйте `asChild`, если другой семантический элемент `<button>` должен отвечать за итоговый DOM-узел. Разместите
иконку в дочернем элементе и при необходимости явно укажите на нём `type="button"`, особенно внутри формы. В Solid
`asChild` принимает функцию рендера: передайте её результат в дочерний элемент через `props()`.

## Примеры

### Пользовательское содержимое

Передайте содержимое через `children`, чтобы заменить стандартную иконку и сохранить контракт корневой кнопки.
Если имя кнопки задаётся отдельной подписью, сделайте пользовательскую иконку декоративной с помощью
`aria-hidden="true"`.

<ExampleFrame variant="component">
  <CloseButtonCustomChildren />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/close-button/custom-children.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={closeButtonSolidCustomChildren} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/close-button/close-button-custom-children.module.css"
```
</div>

</ExampleCode>

### Отключённое состояние

Используйте нативное `disabled`, если кнопка не должна получать фокус. Применяйте `aria-disabled="true"`, если
кнопка должна оставаться доступной для фокуса, но не должна активироваться.

<ExampleFrame variant="component">
  <CloseButtonDisabled />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/close-button/disabled-state.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={closeButtonSolidDisabled} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/close-button/close-button-disabled-state.module.css"
```
</div>

</ExampleCode>

### Использование `asChild`

Используйте `asChild`, если существующая семантическая кнопка должна остаться итоговым DOM-элементом и получить стили
и `data-*`-атрибуты `CloseButton`. Поместите иконку в дочерний элемент и явно укажите его тип.

<ExampleFrame variant="component">
  <CloseButtonAsChild />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/close-button/as-child.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={closeButtonSolidAsChild} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/close-button/close-button-as-child.module.css"
```
</div>

</ExampleCode>

## Стилизация

Используйте `className` в React или `class` в Solid на корневом элементе для структурных стилей. Варианты Tailwind
используют обычные utility-классы и классы приложения; переменные ниже относятся к вариантам с CSS Modules. Атрибуты
состояния и хуки `data-slot` общие для обоих вариантов стилизации.

### CSS-свойства

<CssPropertiesSection properties={CloseButtonDocs.closeButtonOverrideCssProperties} />

### Хуки стилизации

- `data-scope="close-button"` определяет семейство компонента.
- `data-part="root"` определяет единственную экспортируемую часть.
- `data-slot="close-button-root"` служит хуком слота moduix.
- `data-disabled` указывает на нормализованное отключённое состояние для `disabled` и `aria-disabled="true"`.
- `className`/`class` объединяется со стандартными стилями компонента на корневом элементе.
- В вариантах с CSS Modules размер непосредственно вложенных SVG задаётся через `--moduix-close-button-icon-size`.
