---
title: Kbd
component: kbd
description: Компактный компонент для обозначения сочетаний клавиш, команд и отдельных клавиш в интерфейсе.
---

import * as KbdDocs from '@/components/examples/kbd/css-properties';
import KbdBasic from '@/components/examples/kbd/basic.tsx';
import KbdCompactDensity from '@/components/examples/kbd/compact-density.tsx';
import KbdCustomElement from '@/components/examples/kbd/custom-element.tsx';
import KbdShortcutList from '@/components/examples/kbd/shortcut-list.tsx';
import KbdSingleKey from '@/components/examples/kbd/single-key.tsx';
import kbdSolidBasic from '../../../snippets/kbd/solid/basic.tsx?raw';
import kbdSolidCompactDensity from '../../../snippets/kbd/solid/compact-density.tsx?raw';
import kbdSolidCustomElement from '../../../snippets/kbd/solid/custom-element.tsx?raw';
import kbdSolidShortcutList from '../../../snippets/kbd/solid/shortcut-list.tsx?raw';
import kbdSolidSingleKey from '../../../snippets/kbd/solid/single-key.tsx?raw';

## Справочник API

В Ark UI нет отдельного примитива для отображения клавиш. `Kbd` в moduix построен на официальной [фабрике Ark](https://ark-ui.com/docs/guides/composition#the-ark-factory), поэтому использует привычные для Ark подходы к композиции,
стилизации и работе с `ref`. Подробнее о них можно узнать в руководствах Ark: [композиция](https://ark-ui.com/docs/guides/composition), [стилизация](https://ark-ui.com/docs/guides/styling) и [работа с ref](https://ark-ui.com/docs/guides/ref).

## Базовый пример

Kbd доступен для React и Solid в вариантах CSS Modules и Tailwind. Предпросмотр на этой странице использует пакет React CSS Modules, а вкладки фреймворков показывают нативный исходный код для того же публичного контракта.

Используйте `Kbd`, чтобы показать одну клавишу. Для сочетания из нескольких клавиш поместите клавиши и текстовые разделители в `Kbd.Group`.

<ExampleFrame variant="component">
  <KbdBasic />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/kbd/basic.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={kbdSolidBasic} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/kbd/kbd-basic.module.css"
```
</div>

</ExampleCode>

## Установка через shadcn

Полную настройку пакета, реестра и стилей см. в разделе [Быстрый старт](/docs/quick-start).

<Tabs groupId="framework">
  <Tab label="React" value="react">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-react/kbd' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-react-tailwind/kbd' },
  ]}
/>

  </Tab>
  <Tab label="Solid" value="solid">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-solid/kbd' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-solid-tailwind/kbd' },
  ]}
/>

  </Tab>
</Tabs>

## Анатомия

Структура сочетания проста. Знак `+` в этой схеме является обычным текстом, а не отдельной частью компонента.

```text
Kbd.Group
├─ Kbd / Kbd.Root
├─ +
└─ Kbd / Kbd.Root
```

| Часть               | Назначение                                                                  |
| ------------------- | --------------------------------------------------------------------------- |
| `Kbd` / `Kbd.Root`  | Корневой элемент клавиши. По умолчанию выводит элемент `<kbd>`.             |
| `Kbd.Group`         | Строчный контейнер для связанных клавиш. По умолчанию получает роль `group`. |

## Композиция

Используйте `Kbd` как короткую запись `Kbd.Root` для отдельной клавиши, а `Kbd.Group` для объединения связанных клавиш. Оставляйте разделители, например `+`, обычным текстом внутри группы и добавляйте `aria-label`, если визуальные символы или сокращения могут быть непонятны без контекста.

Kbd не хранит состояние и не обрабатывает нажатия клавиш. Используйте `asChild`, только когда другой компонент должен вывести семантический элемент `<kbd>`; передавайте ровно один дочерний элемент.

## Примеры

### Компактный размер

Используйте локальные CSS-переменные, если в отдельной части интерфейса нужны более компактные клавиши. Для этого не нужно добавлять новые свойства компонента.

<ExampleFrame variant="component">
  <KbdCompactDensity />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/kbd/compact-density.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={kbdSolidCompactDensity} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/kbd/kbd-compact-density.module.css"
```
</div>

</ExampleCode>

### Пользовательский элемент

Используйте `asChild`, если другой компонент уже выводит семантический элемент `<kbd>` и к нему нужно применить стили Kbd.

<ExampleFrame variant="component">
  <KbdCustomElement />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/kbd/custom-element.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={kbdSolidCustomElement} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Список сочетаний

Объедините `Kbd.Group` с поясняющим текстом, чтобы показать список доступных сочетаний клавиш.

<ExampleFrame variant="component">
  <KbdShortcutList />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/kbd/shortcut-list.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={kbdSolidShortcutList} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/kbd/kbd-shortcut-list.module.css"
```
</div>

</ExampleCode>

### Одна клавиша

Используйте `Kbd` напрямую, если нужно показать одну клавишу, например `Esc`.

<ExampleFrame variant="component">
  <KbdSingleKey />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/kbd/single-key.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={kbdSolidSingleKey} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

## Стилизация

Настраивайте `Kbd` и `Kbd.Group` с помощью `className` в React или `class` в Solid, атрибутов анатомии Ark, стабильных хуков `data-slot` и публичных CSS-переменных `--moduix-kbd-*`, перечисленных ниже. Варианты Tailwind используют нативные utility-классы, а варианты CSS Modules используют переменные компонента и CSS приложения.

### CSS-свойства

<CssPropertiesSection properties={KbdDocs.kbdOverrideCssProperties} />

### Хуки стилизации

- `data-slot="kbd-root"` выбирает отдельный элемент клавиши
- `data-slot="kbd-group"` выбирает строчный контейнер для сгруппированных сочетаний
- `[data-scope="kbd"][data-part="root"]` и `[data-part="group"]` предоставляют селекторы анатомии в стиле Ark
- Разделители в `Kbd.Group` наследуют цвет текста группы. Чтобы задать этот цвет, используйте `--moduix-kbd-group-separator-color`
