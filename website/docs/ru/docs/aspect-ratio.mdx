---
title: Aspect Ratio
component: aspect-ratio
description: Помещает медиаконтент и встроенное содержимое в адаптивный контейнер с фиксированным соотношением сторон.
---

import AspectRatioAdvancedCustomization from '@/components/examples/aspect-ratio/advanced-customization.tsx';
import AspectRatioBasic from '@/components/examples/aspect-ratio/basic.tsx';
import AspectRatioCardGrid from '@/components/examples/aspect-ratio/card-grid.tsx';
import AspectRatioEmbeddedContent from '@/components/examples/aspect-ratio/embedded-content.tsx';
import AspectRatioMigration from '@/components/examples/aspect-ratio/migrating-from-shadcn.tsx';
import aspectRatioSolidAdvancedCustomization from '../../../snippets/aspect-ratio/solid/advanced-customization.tsx?raw';
import aspectRatioSolidBasic from '../../../snippets/aspect-ratio/solid/basic.tsx?raw';
import aspectRatioSolidCardGrid from '../../../snippets/aspect-ratio/solid/card-grid.tsx?raw';
import aspectRatioSolidEmbeddedContent from '../../../snippets/aspect-ratio/solid/embedded-content.tsx?raw';
import aspectRatioSolidMigration from '../../../snippets/aspect-ratio/solid/migrating-from-shadcn.tsx?raw';

## Справочник API

В Ark UI нет отдельного примитива `aspect-ratio`. В moduix этот компонент построен на официальной
[фабрике Ark](https://ark-ui.com/docs/guides/composition#the-ark-factory) и следует рекомендациям Ark по
[композиции](https://ark-ui.com/docs/guides/composition), [стилизации](https://ark-ui.com/docs/guides/styling) и
[работе с ref](https://ark-ui.com/docs/guides/ref).

Импортируйте `AspectRatio` из пакета, соответствующего фреймворку и способу стилизации: для CSS Modules это
`@moduix/react/aspect-ratio` или `@moduix/solid/aspect-ratio`, а для Tailwind - по соответствующему пути
`@moduix/react-tailwind/aspect-ratio` или `@moduix/solid-tailwind/aspect-ratio`.

## Базовый пример


Используйте `AspectRatio` как корневой элемент адаптивного контейнера. По умолчанию он занимает всю доступную
ширину родительского контейнера. Если размер нужно ограничить или изменить, задайте корневому элементу `width`
или `max-width`, а в `ratio` передайте конечное положительное число.

`AspectRatio` управляет только соотношением сторон. Размеры и оформление дочерних элементов задавайте явно:
например, укажите изображению полную ширину и высоту вместе с `object-fit`. Обрезку и радиус добавляйте корневому
элементу только там, где они нужны композиции.

<ExampleFrame variant="component">
  <AspectRatioBasic />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/aspect-ratio/basic.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={aspectRatioSolidBasic} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/aspect-ratio/aspect-ratio-basic.module.css"
```
</div>

</ExampleCode>

## Установка через shadcn

Полную настройку пакета, реестра и стилей см. в разделе [Быстрый старт](/docs/quick-start).

<Tabs groupId="framework">
  <Tab label="React" value="react">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-react/aspect-ratio' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-react-tailwind/aspect-ratio' },
  ]}
/>

  </Tab>
  <Tab label="Solid" value="solid">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-solid/aspect-ratio' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-solid-tailwind/aspect-ratio' },
  ]}
/>

  </Tab>
</Tabs>

## Анатомия

```text
AspectRatio
└─ media | iframe | canvas | svg | custom content
```

| Часть         | Назначение                                           |
| ------------- | ---------------------------------------------------- |
| `AspectRatio` | Адаптивный контейнер с заданным соотношением сторон. |

## Композиция

`AspectRatio` отвечает только за раскладку и не владеет состоянием. У
компонента одна корневая часть, и дополнительные части не нужно комбинировать. Используйте `asChild` только если
отрисованным корневым элементом должен владеть другой семантический элемент: передайте ровно одного дочернего
элемента в React или используйте показанную в примере «Расширенная настройка» функцию рендера Solid. Размеры,
вписывание, границы, радиус и обрезка дочернего содержимого остаются ответственностью приложения.

## Примеры

### Карточки

Одинаковое соотношение сторон у медиаконтента помогает выровнять карточки в списке, даже если исходные изображения
имеют разные размеры.

<ExampleFrame variant="component">
  <AspectRatioCardGrid />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/aspect-ratio/card-grid.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={aspectRatioSolidCardGrid} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/aspect-ratio/aspect-ratio-card-grid.module.css"
```
</div>

</ExampleCode>

### Встроенное содержимое

Оберните видео или другой встроенный элемент в `AspectRatio`, чтобы размер контейнера не менялся во время загрузки
внешнего содержимого.

<ExampleFrame variant="component">
  <AspectRatioEmbeddedContent />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/aspect-ratio/embedded-content.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={aspectRatioSolidEmbeddedContent} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/aspect-ratio/aspect-ratio-embedded-content.module.css"
```
</div>

</ExampleCode>

### Миграция с shadcn

При переходе с shadcn задайте `AspectRatio` ширину, обрезку и радиус, а размер вложенного медиаконтента укажите явно.
Такой же корневой компонент подходит для `Image` из Next.js с `fill`.

<ExampleFrame variant="component">
  <AspectRatioMigration />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/aspect-ratio/migrating-from-shadcn.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={aspectRatioSolidMigration} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/aspect-ratio/aspect-ratio-migrating-from-shadcn.module.css"
```
</div>

</ExampleCode>

### Расширенная настройка

Используйте `asChild`, если корневым элементом должен быть семантический тег, например `figure`. Фабрика Ark
передаёт свойства компонента его единственному дочернему элементу.

<ExampleFrame variant="component">
  <AspectRatioAdvancedCustomization />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/aspect-ratio/advanced-customization.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={aspectRatioSolidAdvancedCustomization} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/aspect-ratio/aspect-ratio-advanced-customization.module.css"
```
</div>

</ExampleCode>

## Стилизация

### Атрибуты для стилизации

- `data-scope="aspect-ratio"` и `data-part="root"` служат согласованными с Ark селекторами для корневого элемента.
- `data-slot="aspect-ratio-root"` - стабильный селектор moduix для корневого элемента.
- Задавайте ширину, максимальную ширину и стили раскладки на корневом элементе. Если при определённой ширине экрана
  нужно другое соотношение сторон, переопределите `aspect-ratio` в своём CSS.
- Компонент не оформляет дочернее содержимое. Задавайте размеры медиаконтента и `object-fit` непосредственно
  дочернему элементу.
- Добавляйте `overflow: hidden` и радиус корневому элементу только там, где содержимое действительно нужно обрезать.
