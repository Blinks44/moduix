---
title: Spinner
component: spinner
description: Компактный индикатор загрузки, который сообщает ассистивным технологиям о состоянии загрузки и поддерживает несколько размеров и собственное содержимое.
---

import SpinnerAsChild from '@/components/examples/spinner/as-child.tsx';
import SpinnerBasic from '@/components/examples/spinner/basic.tsx';
import SpinnerButton from '@/components/examples/spinner/button.tsx';
import SpinnerCustomIndicator from '@/components/examples/spinner/custom-indicator.tsx';
import SpinnerInlineLoadingText from '@/components/examples/spinner/inline-loading-text.tsx';
import SpinnerSizes from '@/components/examples/spinner/sizes.tsx';
import * as SpinnerDocs from '@/components/examples/spinner/css-properties';
import spinnerSolidAsChild from '../../../snippets/spinner/solid/as-child.tsx?raw';
import spinnerSolidBasic from '../../../snippets/spinner/solid/basic.tsx?raw';
import spinnerSolidButton from '../../../snippets/spinner/solid/button.tsx?raw';
import spinnerSolidCustomIndicator from '../../../snippets/spinner/solid/custom-indicator.tsx?raw';
import spinnerSolidInlineLoadingText from '../../../snippets/spinner/solid/inline-loading-text.tsx?raw';
import spinnerSolidSizes from '../../../snippets/spinner/solid/sizes.tsx?raw';

## Справочник API

В Ark UI нет отдельного компонента Spinner. moduix строит `Spinner` с помощью фабрики `ark.span` из Ark UI
и предоставляет небольшой API вокруг одного корневого элемента. Компонент следует рекомендациям Ark по
[композиции](https://ark-ui.com/docs/guides/composition), [стилизации](https://ark-ui.com/docs/guides/styling) и
[передаче ref](https://ark-ui.com/docs/guides/ref).

## Базовый пример

Используйте `Spinner` для кратковременных состояний загрузки в кнопках, строках статуса и небольших блоках,
где контент ещё не появился. По умолчанию компонент сообщает ассистивным технологиям о загрузке: у корневого
элемента есть `role="status"` и запасное имя `aria-label="Loading"`. Это буквальная английская строка `Loading`,
поэтому в интерфейсе на другом языке задайте локализованный `aria-label` или используйте `aria-labelledby`.

При `prefers-reduced-motion: reduce` спиннер перестаёт вращаться: токен анимации spin из foundation
преобразуется в `none`, а индикатор Tailwind добавляет `motion-reduce:animate-none`. В пакетах с CSS
Modules задайте `--moduix-spinner-animation`, чтобы вернуть анимацию пользователям со включенным
режимом reduced-motion.


В большинстве случаев используйте `Spinner` как корневой элемент. `Spinner.Root` - псевдоним того же
корня, если эту часть композиции нужно обозначить явно.

<ExampleFrame variant="component">
  <SpinnerBasic />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/spinner/basic.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={spinnerSolidBasic} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

## Установка через shadcn

Полную настройку пакета, реестра и стилей см. в разделе [Быстрый старт](/docs/quick-start).

<Tabs groupId="framework">
  <Tab label="React" value="react">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-react/spinner' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-react-tailwind/spinner' },
  ]}
/>

  </Tab>
  <Tab label="Solid" value="solid">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-solid/spinner' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-solid-tailwind/spinner' },
  ]}
/>

  </Tab>
</Tabs>

## Анатомия

У `Spinner` есть только один публичный корневой элемент. В обычном режиме moduix сам добавляет внутреннюю
обёртку индикатора и стандартное кольцо. Это DOM-узлы и точки привязки для CSS, а не отдельные React-компоненты.
Собственное содержимое через `children` заменяет кольцо, а `asChild` заменяет корневой элемент и оставляет
разметку индикатора на дочернем элементе.

```text
Spinner / Spinner.Root
└─ root[data-scope="spinner"][data-part="root"][data-slot="spinner-root"][data-size]
   └─ indicator[data-scope="spinner"][data-part="indicator"][data-slot="spinner-indicator"]
      └─ ring[data-scope="spinner"][data-part="ring"][data-slot="spinner-ring"] (только по умолчанию)
```

| Часть               | Назначение                                                                                  |
| ------------------- | ------------------------------------------------------------------------------------------- |
| `Spinner` / `Spinner.Root` | Публичный корневой элемент. Принимает `size`, `decorative`, `asChild`, `children` и нативные свойства `span`. |
| indicator                   | Внутренняя вращающаяся обёртка, которая отвечает за размер и анимацию индикатора.               |
| ring                        | Внутреннее стандартное кольцо. Не создаётся, если передано собственное `children`.             |

## Композиция

`Spinner` только отображает состояние загрузки, которым владеет приложение: компонент не запускает,
не останавливает и не изменяет представленную операцию. Отрисовывайте его на основе состояния, где
уже хранится информация об этой операции.

Передайте собственное `children`, чтобы заменить стандартное кольцо и сохранить обычный корень,
размер и вращающуюся обёртку индикатора. Используйте `asChild`, только если другой компонент должен
владеть корневым элементом: в этом режиме встроенные индикатор и кольцо не создаются, поэтому ровно
один неинтерактивный дочерний элемент должен содержать разметку визуального индикатора.

## Примеры

### Пользовательский индикатор

Передайте собственное содержимое через `children`, если стандартное кольцо нужно заменить, но размер и семантика
статуса должны остаться прежними.

<ExampleFrame variant="component">
  <SpinnerCustomIndicator />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/spinner/custom-indicator.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={spinnerSolidCustomIndicator} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### В кнопке

Оставьте `Spinner` декоративным, если текст кнопки уже объясняет действие. В примере `aria-busy` передаётся кнопке,
а подпись под ней нужна только для демонстрации текущего состояния.

<ExampleFrame variant="component">
  <SpinnerButton />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/spinner/button.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={spinnerSolidButton} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/spinner/spinner-button.module.css"
```

</div>

</ExampleCode>

### Загрузка с текстом

Используйте `decorative`, когда видимый текст рядом со `Spinner` уже объясняет, что происходит.

<ExampleFrame variant="component">
  <SpinnerInlineLoadingText />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/spinner/inline-loading-text.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={spinnerSolidInlineLoadingText} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/spinner/spinner-inline-loading-text.module.css"
```

</div>

</ExampleCode>

### Размеры

Используйте небольшие размеры внутри компактных элементов управления, а большие - для самостоятельных состояний загрузки.

<ExampleFrame variant="component">
  <SpinnerSizes />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/spinner/sizes.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={spinnerSolidSizes} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/spinner/spinner-sizes.module.css"
```

</div>

</ExampleCode>

### Собственный корневой элемент

Используйте `asChild`, когда другой компонент должен владеть корневым элементом. Дочерний элемент получает атрибуты
и имя класса корня Spinner и должен сам отрисовать визуальный индикатор.

<ExampleFrame variant="component">
  <SpinnerAsChild />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/spinner/as-child.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={spinnerSolidAsChild} />

    </Tab>
  </Tabs>

**Стили**

```css file="../../../src/components/examples/spinner/spinner-as-child.module.css"
```

</div>

</ExampleCode>

## Стилизация

Стилизуйте `Spinner` с помощью `className` в React, `class` в Solid, селекторов Ark `data-scope` / `data-part`,
стабильных хуков `data-slot`, `data-size` и публичных переменных `--moduix-spinner-*`, перечисленных в CSS API.
Эти переменные доступны в пакетах CSS Modules; пакеты Tailwind используют классы-утилиты и пользовательские классы,
а не дублируют слой переменных CSS Modules.

### CSS-свойства

<div className="docs-reference">
  <Tabs
    values={[{ label: 'CSS-переменные', value: 'CSS Variables' }]}
    className="docs-reference-tabs"
    contentItemClassName="docs-reference-panel"
  >
    <Tab>
      <SpinnerDocs.SpinnerCssPropertiesPanel />
    </Tab>
  </Tabs>
</div>

### Хуки стилизации

- `[data-scope="spinner"][data-part="root"]` выбирает корневой элемент в соответствии с анатомией Ark.
- `[data-scope="spinner"][data-part="indicator"]` выбирает вращающуюся внутреннюю обёртку.
- `[data-scope="spinner"][data-part="ring"]` выбирает только стандартное кольцо.
- `data-slot="spinner-root"` - стабильный селектор moduix для отрисованного корня.
- `data-slot="spinner-indicator"` - стабильный селектор moduix для вращающейся внутренней обёртки.
- `data-slot="spinner-ring"` выбирает только стандартное кольцо.
- `Spinner` записывает в корень `data-size="inherit" | "xs" | "sm" | "md" | "lg" | "xl"`.
- `className` или `class` применяется к отрисованному корню. При `asChild` это единственный дочерний элемент.
  При необходимости выбирайте индикатор или кольцо дочерними селекторами.
