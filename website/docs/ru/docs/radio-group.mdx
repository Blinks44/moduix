---
title: Radio Group
component: radio-group
description: Группа радиокнопок для выбора одного варианта из нескольких.
---

import * as RadioGroupDocs from '@/components/examples/radio-group';

Используйте `RadioGroup`, когда пользователь должен выбрать ровно один вариант из видимого списка,
например фреймворк, тариф или тип аккаунта. Для независимых вариантов используйте `Checkbox`, а для
настройки, которая применяется сразу, `Switch`.

## Справочник API

`RadioGroup` следует примитиву radio-group из Ark UI и добавляет стили moduix, стабильные атрибуты
`data-slot` и вспомогательный компонент `Option`. Компонент импортируется из
`@moduix/react/radio-group`.

<PrimitiveReference href="https://ark-ui.com/docs/components/radio-group" />

## Базовый пример

Для большинства групп используйте `RadioGroup.Option`. Он выводит элемент с подписью, элементом управления
и видимым текстом. Каждый элемент автоматически получает скрытое нативное поле ввода. Если группа
находится внутри нативной формы, передайте `name` в `RadioGroup`, как показано в примере «Форма».

<style>{RadioGroupDocs.radioGroupExampleCss}</style>

<PreviewFrame contentWidth="fit-content" maxWidth="sm">
  ```tsx preview file="../../en/docs/_snippets/radio-group/basic.tsx"

  ```

</PreviewFrame>

## Установка через shadcn

<ShadcnInstall packageName="radio-group" />

## Анатомия

```text
RadioGroup.Root
├─ RadioGroup.Label
├─ RadioGroup.Indicator
└─ RadioGroup.Item[value]
   ├─ RadioGroup.ItemControl
   ├─ RadioGroup.ItemText
   └─ <input> (automatic)

RadioGroup.RootProvider
└─ RadioGroup.* + useRadioGroup()

RadioGroup.Option
└─ RadioGroup.Item + ItemControl + ItemText + <input> (automatic)
```

| Часть                         | Назначение                                                         |
| ---------------------------- | ------------------------------------------------------------------ |
| `RadioGroup` / `.Root`       | Хранит значение, управляет клавиатурной навигацией, ориентацией и формами. |
| `RadioGroup.RootProvider`    | Подключает части к состоянию, созданному через `useRadioGroup()`.  |
| `RadioGroup.Label`           | Доступная подпись группы.                                         |
| `RadioGroup.Item`            | Один вариант выбора, по умолчанию отображается как `label`.      |
| `RadioGroup.Option`          | Упрощённый элемент с элементом управления, текстом и скрытым полем ввода. |
| `RadioGroup.ItemControl`     | Визуальный элемент управления радиокнопки с поддержкой `size` из moduix. |
| `RadioGroup.ItemText`        | Видимый текст варианта.                                           |
| `RadioGroup.Indicator`       | Необязательный индикатор активного элемента с переменными размеров Ark. |

## Композиция

### Рекомендуемая композиция

`RadioGroup` является короткой формой `RadioGroup.Root`. Для обычного варианта с подписью
используйте `RadioGroup.Option`. Он принимает свойства элемента Ark, свойство `size` из moduix и
`className`. Класс применяется к базовому `Item`, поэтому к вложенным частям по-прежнему можно
настраивать через атрибуты `data-slot`.

### Когда нужна собственная композиция

Используйте `RadioGroup.RootProvider` вместе с `useRadioGroup()` из `@moduix/react`, когда состояние
нужно создать до отрисовки группы, разделить с элементами вне её дерева или читать в другой части
интерфейса. Передайте возвращённый объект состояния в `RootProvider` и не используйте `Root` и
`RootProvider` для одного экземпляра состояния одновременно.

Используйте явное дерево `Item`, когда вложенным частям нужны собственное размещение или классы, либо
когда `RadioGroup.Item asChild` должен использовать другой элемент как DOM-узел варианта. Прямым
дочерним элементом `Item` с `asChild` по-прежнему должен быть семантический `label`.

Для чтения состояния внутри дерева используйте `RadioGroup.Context`, `RadioGroup.ItemContext`,
`useRadioGroupContext()` или `useRadioGroupItemContext()` из `@moduix/react`.

### Свойства по умолчанию

| Свойство                           | По умолчанию | Примечания                                                        |
| ---------------------------------- | ------------ | ----------------------------------------------------------------- |
| `RadioGroup` / `RadioGroup.Root`   | -            | Применяются значения Ark, дополнительных настроек поведения moduix нет. |
| `RadioGroup.Option size`           | `md`         | Диаметр элемента управления и размер точки индикатора.            |
| `RadioGroup.ItemControl size`      | `md`         | Диаметр элемента управления moduix и размер точки индикатора.     |

### Особенности поведения

`RadioGroup.Root` принимает свойства Ark, в том числе `value`, `defaultValue`,
`onValueChange(details)`, `name`, `form`, `orientation`, `disabled`, `invalid`, `readOnly`,
`required`, `ids` и `asChild`. В обработчике изменения читайте выбранное значение из
`details.value`.

В каждом `RadioGroup.Item` автоматически создаётся нативное поле формы. Оно участвует в отправке и
проверке формы, а также в её сбросе, поэтому добавлять часть input вручную не нужно. Передайте
`name` в корневой компонент, если выбранное значение должно попасть в `FormData`.

Ark управляет контролируемым и неконтролируемым состоянием, WAI-ARIA-поведением, фокусом и
клавиатурной навигацией. Клавиша <kbd>Tab</kbd> переводит фокус на выбранный элемент или на первый,
если выбор ещё не сделан. <kbd>Space</kbd> выбирает элемент в фокусе, а клавиши со стрелками
перемещают фокус и выбирают следующий или предыдущий вариант.

`Field.Root` предоставляет видимую подпись, вспомогательный текст и сообщение об ошибке, но не передаёт
состояние в `RadioGroup`. Передавайте `disabled`, `invalid`, `readOnly` и `required` самой группе.
`Fieldset.Root` передаёт вложенным группам своё состояние Ark `disabled` и `invalid`.

Каждая стилизуемая часть передаёт `ref` соответствующей DOM-части Ark. Автоматическое нативное поле
ввода является внутренней деталью, на него нельзя получить отдельный публичный `ref`.

## Примеры

### asChild

Используйте `RadioGroup.Item asChild`, когда карточка или пользовательская разметка подписи должны
владеть DOM-узлом варианта. Прямым дочерним элементом оставляйте семантический `label`.

<style>{RadioGroupDocs.radioGroupAsChildCss}</style>

<PreviewFrame contentWidth="fit-content" maxWidth="sm">
  ```tsx preview file="../../en/docs/_snippets/radio-group/aschild.tsx"

  ```

</PreviewFrame>

### Управляемое состояние

Используйте `value` вместе с `onValueChange(details)`, когда выбранное значение хранится в состоянии
React или должно синхронно меняться с другими частями интерфейса.

<style>{RadioGroupDocs.radioGroupExampleCss}</style>

<PreviewFrame contentWidth="fit-content" maxWidth="sm">
  ```tsx preview file="../../en/docs/_snippets/radio-group/controlled.tsx"

  ```

</PreviewFrame>

### Отключённое состояние

Установите `disabled` на корневом компоненте, чтобы сделать недоступными все варианты и при этом
оставить выбранное значение видимым.

<style>{RadioGroupDocs.radioGroupExampleCss}</style>

<PreviewFrame contentWidth="fit-content" maxWidth="sm">
  ```tsx preview file="../../en/docs/_snippets/radio-group/disabled.tsx"

  ```

</PreviewFrame>

### Отключённый элемент

Установите `disabled` на отдельном `Item` или `Option`, если недоступен только один вариант.

<style>{RadioGroupDocs.radioGroupExampleCss}</style>

<PreviewFrame contentWidth="fit-content" maxWidth="sm">
  ```tsx preview file="../../en/docs/_snippets/radio-group/item-disabled.tsx"
  ```

</PreviewFrame>

### Форма

Передайте `name` в корневой компонент, если выбранное значение нужно отправить вместе с формой.
Нативное поле, которое автоматически создаётся для каждого элемента, участвует в отправке и сбросе
формы, поэтому добавлять часть input вручную не нужно.

<style>{RadioGroupDocs.radioGroupExampleCss}</style>

<PreviewFrame contentWidth="fit-content" maxWidth="sm">
  ```tsx preview file="../../en/docs/_snippets/radio-group/form.tsx"
  ```

</PreviewFrame>

### Индикатор

Отрисуйте `RadioGroup.Indicator`, если активному элементу нужно дополнительное измеряемое
выделение. Размещайте индикатор и радиокнопки в одном контексте позиционирования, а постороннее
содержимое выносите за его пределы, чтобы индикатор точно следовал геометрии элементов.

<style>{RadioGroupDocs.radioGroupIndicatorCss}</style>

<PreviewFrame contentWidth="fit-content" maxWidth="sm">
  ```tsx preview file="../../en/docs/_snippets/radio-group/indicator.tsx"

  ```

</PreviewFrame>

### Начальное значение

Установите `defaultValue`, если исходный выбранный элемент известен, а дальнейшими изменениями
должна управлять сама группа.

<style>{RadioGroupDocs.radioGroupExampleCss}</style>

<PreviewFrame contentWidth="fit-content" maxWidth="sm">
  ```tsx preview file="../../en/docs/_snippets/radio-group/initial-value.tsx"

  ```

</PreviewFrame>

### Ориентация

Установите `orientation="horizontal"`, если поведение клавиш со стрелками должно соответствовать
горизонтальной группе. Если визуальная раскладка тоже должна быть горизонтальной, поместите элементы
в строку с помощью встроенной обёртки.

<style>{RadioGroupDocs.radioGroupExampleCss}</style>

<PreviewFrame contentWidth="fit-content" maxWidth="sm">
  ```tsx preview file="../../en/docs/_snippets/radio-group/orientation.tsx"

  ```

</PreviewFrame>

### Root Provider

Используйте `RootProvider`, когда хранилище `useRadioGroup()` нужно разделить с элементами вне
дерева группы. В примере показано, как изменить выбранное значение из внешнего элемента управления.

<style>{RadioGroupDocs.radioGroupExampleCss}</style>

<PreviewFrame contentWidth="fit-content" maxWidth="sm">
  ```tsx preview file="../../en/docs/_snippets/radio-group/root-provider.tsx"

  ```

</PreviewFrame>

### Размеры

Используйте свойство `size` из moduix у `ItemControl` или `Option`, если элементу управления радиокнопки нужен
другой диаметр. Поддерживаются значения `xs`, `sm`, `md`, `lg` и `xl`.

<style>{RadioGroupDocs.radioGroupExampleCss}</style>

<PreviewFrame contentWidth="fit-content" maxWidth="sm">
  ```tsx preview file="../../en/docs/_snippets/radio-group/sizes.tsx"

  ```

</PreviewFrame>

### С Field

Используйте `Field.Root`, если группе нужны общие подпись, вспомогательный текст или сообщение об
ошибке. Передайте `invalid` и в `RadioGroup`, потому что `Field.Root` не устанавливает состояние
валидации группы.

<style>{RadioGroupDocs.radioGroupExampleCss}</style>

<PreviewFrame contentWidth="fit-content" maxWidth="sm">
  ```tsx preview file="../../en/docs/_snippets/radio-group/with-field.tsx"

  ```

</PreviewFrame>

### С Fieldset

Оберните группу в `Fieldset`, если несколько вариантов используют общую легенду и относятся к одному
разделу формы. `Fieldset` также может передать вложенной группе состояния `disabled` и `invalid`.

<style>{`
  ${RadioGroupDocs.radioGroupExampleCss}

  .centered-fieldset-example {
    width: fit-content;
    max-width: 100%;
    margin-inline: auto;
  }
`}</style>

<PreviewFrame contentWidth="fit-content" maxWidth="sm">
  ```tsx preview file="../../en/docs/_snippets/radio-group/with-fieldset.tsx"

  ```

</PreviewFrame>

### Расширенная настройка

Не используйте `Option`, если вложенным частям нужны собственные свойства, размещение или стили.
Соберите полное дерево из `Item`, `ItemControl` и `ItemText`.

<style>{RadioGroupDocs.radioGroupExampleCss}</style>

<PreviewFrame contentWidth="fit-content" maxWidth="sm">
  ```tsx preview file="../../en/docs/_snippets/radio-group/advanced-customization.tsx"

  ```

</PreviewFrame>

## Стилизация

Стилизуйте `RadioGroup` через `className`, стабильные атрибуты `data-slot="radio-group-*"`, атрибуты
Ark `data-scope`, `data-part` и `data-state`, а также публичные CSS-переменные `--moduix-radio-*`,
перечисленные ниже.

### CSS-свойства

<div className="docs-reference">
  <Tabs
    values={[{ label: 'CSS-переменные', value: 'CSS Variables' }]}
    className="docs-reference-tabs"
    contentItemClassName="docs-reference-panel"
  >
    <Tab>
      <RadioGroupDocs.RadioGroupCssPropertiesPanel />
    </Tab>
  </Tabs>
</div>

### Точки стилизации

- Атрибуты анатомии Ark: `data-scope="radio-group"` и `data-part` у root, label, item, item-control,
  item-text, item-hidden-input и indicator.
- Атрибуты состояний Ark: `data-state="checked|unchecked"`, `data-disabled`, `data-readonly`,
  `data-invalid`, `data-required`, `data-focus`, `data-focus-visible`, `data-hover`, `data-active`
  и `data-orientation`.
- Переменные индикатора Ark: `--transition-property`, `--left`, `--top`, `--width` и `--height`.
- Атрибуты moduix: `data-slot="radio-group-root"`, `radio-group-root-provider`, `radio-group-label`,
  `radio-group-item`, `radio-group-item-control`, `radio-group-item-text`, `radio-group-item-hidden-input` и
  `radio-group-indicator`.
