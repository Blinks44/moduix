---
title: Pagination
component: pagination
description: Accessible page navigation for lists, tables, search results, and other paginated content.
---

import * as PaginationDocs from '@/components/examples/pagination/css-properties';
import PaginationAdvancedCustomization from '@/components/examples/pagination/advanced-customization.tsx';
import PaginationBasic from '@/components/examples/pagination/basic.tsx';
import PaginationContext from '@/components/examples/pagination/context.tsx';
import PaginationControlled from '@/components/examples/pagination/controlled.tsx';
import PaginationCustomized from '@/components/examples/pagination/customized.tsx';
import PaginationDataSlicing from '@/components/examples/pagination/data-slicing.tsx';
import PaginationLink from '@/components/examples/pagination/link.tsx';
import PaginationPageRange from '@/components/examples/pagination/page-range.tsx';
import PaginationPageSizeControl from '@/components/examples/pagination/page-size-control.tsx';
import PaginationRootProvider from '@/components/examples/pagination/root-provider.tsx';
import PaginationWithEdges from '@/components/examples/pagination/with-edges.tsx';

## API Reference

<PrimitiveReference href="https://ark-ui.com/docs/components/pagination" />

## Basic

Use `Pagination` as the root component. `Pagination.Items` renders the page buttons and ellipses for
the current page range.

<ExampleFrame variant="component">
  <PaginationBasic />
</ExampleFrame>

<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx file="../../../src/components/examples/pagination/basic.tsx"
```
    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/pagination/pagination-basic.module.css"
```
    </Tab>
  </Tabs>
</div>

## Install with shadcn

<ShadcnInstall packageName="pagination" />

## Anatomy

```text
Pagination
├─ Pagination.FirstTrigger (optional)
├─ Pagination.PrevTrigger
├─ Pagination.Items (recommended)
├─ Pagination.Context (advanced render-prop access)
│  ├─ Pagination.Item
│  └─ Pagination.Ellipsis
├─ Pagination.NextTrigger
└─ Pagination.LastTrigger (optional)

Pagination.RootProvider
└─ same trigger, item, ellipsis, and context tree connected to usePagination()
```

| Part                              | Role                                                               |
| --------------------------------- | ------------------------------------------------------------------ |
| `Pagination`                      | Navigation root that manages the current page and page size.       |
| `Pagination.RootProvider`         | Connects parts to state created by `Pagination.usePagination()`.   |
| `Pagination.Items`                | Renders the computed page items and ellipses.                      |
| `Pagination.Context`              | Exposes root state and navigation helpers through an Ark render prop. |
| `Pagination.usePagination`        | Creates pagination state outside the rendered tree.                |
| `Pagination.usePaginationContext` | Reads pagination state from a descendant.                          |
| `Pagination.Item`                 | Interactive page control. Pass a page object as its props.        |
| `Pagination.Ellipsis`             | Non-interactive marker for a collapsed page range. Requires `index`. |
| `Pagination.PrevTrigger`          | Moves to the previous page and disables itself on the first page.  |
| `Pagination.NextTrigger`          | Moves to the next page and disables itself on the last page.      |
| `Pagination.FirstTrigger`         | Moves to the first page and disables itself when already there.    |
| `Pagination.LastTrigger`          | Moves to the last page and disables itself when already there.     |

## Composition

### Recommended composition

`Pagination` and `Pagination.Root` manage keyboard behavior, the current page, page size, disabled
triggers, page-range calculation, and accessible labels. Set `count` to the total number of data
items, not the number of pages. Pair it with `pageSize` or `defaultPageSize` so Ark can calculate
`totalPages`.

Use `Pagination.Items` for the standard page list. It renders Ark's current `pages` array with
`Pagination.Item` and `Pagination.Ellipsis`, keeping the correct selected state, labels, and event
handlers.

### When to go custom

Use `Pagination.Context` when you need custom page items or controls next to the page list. If state
must be created outside the rendered pagination tree, create it with `Pagination.usePagination()` and
pass it to `Pagination.RootProvider`. A descendant can read the same state with
`Pagination.usePaginationContext()`. Set `type="link"` and provide `getPageUrl(details)` when page
changes should use navigational links.

### Default props

| Prop              | Default      | Notes                                                       |
| ----------------- | ------------ | ----------------------------------------------------------- |
| `aria-label`      | `pagination` | Ark navigation landmark label; set `translations.rootLabel` to localize it. |
| `defaultPage`     | `1`          | Ark initial active page when uncontrolled.                  |
| `defaultPageSize` | `10`         | Ark initial items per page when uncontrolled.               |
| `siblingCount`    | `1`          | Ark pages shown beside the active page.                     |
| `boundaryCount`   | `1`          | Ark pages shown at the beginning and end of the page range. |
| `type`            | `button`     | Use `link` with `getPageUrl(details)` for anchors.          |

### Behavioral notes

Use `defaultPage` and `defaultPageSize` when the component should manage its own state. To control
the state from your application, pass `page`, `pageSize`, `onPageChange(details)`, and
`onPageSizeChange(details)`. The callbacks receive Ark detail objects, so read `details.page` and
`details.pageSize` directly. The wrapper keeps Ark's keyboard behavior, selected-item semantics,
disabled state, `ids`, `asChild`, translations, and link URL generation. When using `asChild`, pass
one semantic child, such as an anchor.

## Examples

### Controlled

Pass the active page with `page` and update it in `onPageChange(details)`.

<ExampleFrame variant="component">
  <PaginationControlled />
</ExampleFrame>

<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx file="../../../src/components/examples/pagination/controlled.tsx"
```
    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/pagination/pagination-controlled.module.css"
```
    </Tab>
  </Tabs>
</div>

### Context

Use `Pagination.Context` to place custom controls next to the page list and call helpers such as
`goToFirstPage()` and `goToLastPage()`.

<ExampleFrame variant="component">
  <PaginationContext />
</ExampleFrame>

<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }]} className="docs-tabs">
    <Tab value="React">

```tsx file="../../../src/components/examples/pagination/context.tsx"
```
    </Tab>
  </Tabs>
</div>

### Client-Side Data

Use `pagination.slice(data)` from context to render only the part of an already-loaded array that
belongs to the current page.

<ExampleFrame variant="component">
  <PaginationDataSlicing />
</ExampleFrame>

<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx file="../../../src/components/examples/pagination/data-slicing.tsx"
```
    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/pagination/pagination-data-slicing.module.css"
```
    </Tab>
  </Tabs>
</div>

### Link Navigation

Use `type="link"` with `getPageUrl(details)` and `asChild` anchors when changing pages should
navigate to a real URL.

<ExampleFrame variant="component">
  <PaginationLink />
</ExampleFrame>

<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx file="../../../src/components/examples/pagination/link.tsx"
```
    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/pagination/pagination-link.module.css"
```
    </Tab>
  </Tabs>
</div>

### Page Range

Use `pagination.pageRange` to show which items are visible on the current page.

<ExampleFrame variant="component">
  <PaginationPageRange />
</ExampleFrame>

<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx file="../../../src/components/examples/pagination/page-range.tsx"
```
    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/pagination/pagination-page-range.module.css"
```
    </Tab>
  </Tabs>
</div>

### Items Per Page

Call `pagination.setPageSize(nextSize)` from context when a separate control changes the number of
items per page.

<ExampleFrame variant="component">
  <PaginationPageSizeControl />
</ExampleFrame>

<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx file="../../../src/components/examples/pagination/page-size-control.tsx"
```
    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/pagination/pagination-page-size-control.module.css"
```
    </Tab>
  </Tabs>
</div>

### Root Provider

Create state with `Pagination.usePagination()` and pass it to `Pagination.RootProvider` when the
state owner lives outside the rendered pagination tree.

<ExampleFrame variant="component">
  <PaginationRootProvider />
</ExampleFrame>

<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx file="../../../src/components/examples/pagination/root-provider.tsx"
```
    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/pagination/pagination-root-provider.module.css"
```
    </Tab>
  </Tabs>
</div>

### Translations

Use `translations` to customize accessible labels. Adjust the visible page window with
`siblingCount`, `boundaryCount`, and the page-size props.

<ExampleFrame variant="component">
  <PaginationCustomized />
</ExampleFrame>

<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx file="../../../src/components/examples/pagination/customized.tsx"
```
    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/pagination/pagination-customized.module.css"
```
    </Tab>
  </Tabs>
</div>

### First and Last Page Controls

Add `Pagination.FirstTrigger` and `Pagination.LastTrigger` when users need shortcuts to the first
and last page.

<ExampleFrame variant="component">
  <PaginationWithEdges />
</ExampleFrame>

<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx file="../../../src/components/examples/pagination/with-edges.tsx"
```
    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/pagination/pagination-with-edges.module.css"
```
    </Tab>
  </Tabs>
</div>

### Advanced Customization

Use `Pagination.Context` instead of `Pagination.Items` when page buttons or ellipses need custom
content. Pass each page object to `Pagination.Item` unchanged so Ark can keep its state and
accessibility behavior.

<ExampleFrame variant="component">
  <PaginationAdvancedCustomization />
</ExampleFrame>

<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx file="../../../src/components/examples/pagination/advanced-customization.tsx"
```
    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/pagination/pagination-advanced-customization.module.css"
```
    </Tab>
  </Tabs>
</div>

## Styling

moduix keeps Ark's state attributes and adds stable `data-slot` hooks for local styling. Page items
use tabular figures and compact inline padding. One- and two-digit labels keep the default square
footprint, while longer labels can expand without being clipped.

### CSS Properties

<div className="docs-reference">
  <Tabs
    values={[{ label: 'CSS Variables', value: 'CSS Variables' }]}
    className="docs-reference-tabs"
    contentItemClassName="docs-reference-panel"
  >
    <Tab>
      <PaginationDocs.PaginationCssPropertiesPanel />
    </Tab>
  </Tabs>
</div>

### Styling hooks

- `data-scope="pagination"` and `data-part="item"` on `Pagination.Item`
- `data-scope="pagination"` and `data-part="prev-trigger"` on `Pagination.PrevTrigger`
- `data-scope="pagination"` and `data-part="next-trigger"` on `Pagination.NextTrigger`
- `data-scope="pagination"` with `data-part="first-trigger"` or `data-part="last-trigger"` on edge triggers
- `data-selected` on the active page item
- `data-disabled` on unavailable triggers
- `data-slot="pagination-root"`, `pagination-root-provider`, `pagination-item`,
  `pagination-ellipsis`, `pagination-prev-trigger`, `pagination-next-trigger`,
  `pagination-first-trigger`, and `pagination-last-trigger` for moduix overrides
