---
title: Toggle Group
component: toggle-group
description: A keyboard-navigable group of toggle buttons for single or multiple selection in React and Solid.
---

import * as ToggleGroupDocs from '@/components/examples/toggle-group/css-properties';
import { CssPropertiesSection } from '@/components/mdx/Components';
import ToggleGroupAsChild from '@/components/examples/toggle-group/as-child.tsx';
import ToggleGroupBasic from '@/components/examples/toggle-group/basic.tsx';
import ToggleGroupContext from '@/components/examples/toggle-group/context.tsx';
import ToggleGroupControlled from '@/components/examples/toggle-group/controlled.tsx';
import ToggleGroupDisabled from '@/components/examples/toggle-group/disabled.tsx';
import ToggleGroupIcons from '@/components/examples/toggle-group/icons.tsx';
import ToggleGroupMultipleSelection from '@/components/examples/toggle-group/multiple-selection.tsx';
import ToggleGroupRootProvider from '@/components/examples/toggle-group/root-provider.tsx';
import ToggleGroupVariants from '@/components/examples/toggle-group/variants.tsx';
import ToggleGroupVertical from '@/components/examples/toggle-group/vertical.tsx';
import toggleGroupSolidAsChild from '../../../snippets/toggle-group/solid/as-child.tsx?raw';
import toggleGroupSolidBasic from '../../../snippets/toggle-group/solid/basic.tsx?raw';
import toggleGroupSolidContext from '../../../snippets/toggle-group/solid/context.tsx?raw';
import toggleGroupSolidControlled from '../../../snippets/toggle-group/solid/controlled.tsx?raw';
import toggleGroupSolidDisabled from '../../../snippets/toggle-group/solid/disabled.tsx?raw';
import toggleGroupSolidIcons from '../../../snippets/toggle-group/solid/icons.tsx?raw';
import toggleGroupSolidMultipleSelection from '../../../snippets/toggle-group/solid/multiple-selection.tsx?raw';
import toggleGroupSolidRootProvider from '../../../snippets/toggle-group/solid/root-provider.tsx?raw';
import toggleGroupSolidVariants from '../../../snippets/toggle-group/solid/variants.tsx?raw';
import toggleGroupSolidVertical from '../../../snippets/toggle-group/solid/vertical.tsx?raw';

## API Reference

`ToggleGroup` wraps Ark UI's Toggle Group primitive and adds moduix visual props for variants and
sizes. Import it from the matching framework package: `@moduix/react/toggle-group`,
`@moduix/solid/toggle-group`, `@moduix/react-tailwind/toggle-group`, or
`@moduix/solid-tailwind/toggle-group`.

<PrimitiveReference href="https://ark-ui.com/docs/components/toggle-group" />

## Choosing the right component

Use `ToggleGroup` when related toggle buttons should work as one keyboard-navigable control. A group
can allow one selected value or several selected values.

- Choose **`ToggleGroup`** for segmented controls, compact filter sets, and formatting toolbars.
- Choose **`Toggle`** for one standalone two-state action.
- Choose **`Tabs`** when selecting an item should also show its associated panel.
- Choose **`RadioGroup`** when the choices are form options and need native form integration.

## Basic

`ToggleGroup` ships for React and Solid in both the CSS Modules and Tailwind packages. The rendered
preview below uses the React CSS Modules package; the framework tabs show native source for the same
public contract.

Use `ToggleGroup` as the root and add one `ToggleGroup.Item` for each choice. Give the group an
accessible name with `aria-label` or `aria-labelledby`.

<ExampleFrame variant="component">
  <ToggleGroupBasic />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/toggle-group/basic.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={toggleGroupSolidBasic} />

    </Tab>
  </Tabs>
</div>
</ExampleCode>

## Install with shadcn

See [Quick Start](/docs/quick-start) for full package setup, registry configuration, and styling guidance.

<Tabs groupId="framework">
  <Tab label="React" value="react">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-react/toggle-group' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-react-tailwind/toggle-group' },
  ]}
/>

  </Tab>
  <Tab label="Solid" value="solid">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-solid/toggle-group' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-solid-tailwind/toggle-group' },
  ]}
/>

  </Tab>
</Tabs>

## Anatomy

```text
ToggleGroup / ToggleGroup.Root
├─ ToggleGroup.Context (optional)
└─ ToggleGroup.Item[value]

ToggleGroup.RootProvider
└─ ToggleGroup.Item[value]
```

| Part                               | Role                                                                                   |
| ---------------------------------- | -------------------------------------------------------------------------------------- |
| `ToggleGroup` / `ToggleGroup.Root` | Root part that owns selected values, focus movement, orientation, and disabled state.   |
| `ToggleGroup.Item`                 | Interactive item with a required `value` and Ark selection state.                      |
| `ToggleGroup.RootProvider`         | Connects the item tree to state created with `useToggleGroup()`.                       |
| `ToggleGroup.Context`              | Exposes the current group state through a render prop.                                 |

## Composition

Use `defaultValue` for an uncontrolled group, or pass `value` with `onValueChange(details)` when
another part of the application owns the selection. Both values are `string[]` in single and
multiple-selection modes. Use `ToggleGroup.RootProvider` with `useToggleGroup()` from the matching
framework package when state must be created outside the rendered tree. React reads the store as an
object, while Solid reads its reactive state through the returned accessor.

Use `ToggleGroup.Context` or `useToggleGroupContext()` when a custom item needs the current group
state. With `asChild`, React receives one button-like child, while Solid receives a render function
that returns the element hosting the Ark behavior. Do not render `ToggleGroup.Root` and
`ToggleGroup.RootProvider` for the same state instance.

## Examples

### asChild

Use `asChild` on an item when a custom button must host the rendered element. Keep exactly one
button-like child so Ark can merge the group's semantics and keyboard behavior into it.

<ExampleFrame variant="component">
  <ToggleGroupAsChild />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/toggle-group/as-child.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={toggleGroupSolidAsChild} />

    </Tab>
  </Tabs>
</div>
</ExampleCode>

### Context

Use `useToggleGroupContext()` in a custom item when its contents need the current group state.
`ToggleGroup.Context` provides the same state through a render prop.

<ExampleFrame variant="component">
  <ToggleGroupContext />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/toggle-group/context.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={toggleGroupSolidContext} />

    </Tab>
  </Tabs>
</div>
</ExampleCode>

### Controlled

Control `value` when the selected items must stay in sync with surrounding application state.

<ExampleFrame variant="component">
  <ToggleGroupControlled />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/toggle-group/controlled.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={toggleGroupSolidControlled} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/toggle-group/toggle-group-controlled.module.css"
```
</div>
</ExampleCode>

### Disabled

Set `disabled` on the group or on a specific item to prevent interaction.

<ExampleFrame variant="component">
  <ToggleGroupDisabled />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/toggle-group/disabled.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={toggleGroupSolidDisabled} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/toggle-group/toggle-group-disabled.module.css"
```
</div>
</ExampleCode>

### Icons

Pass icons as regular children. Icon-only items should always include an accessible label.

<ExampleFrame variant="component">
  <ToggleGroupIcons />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/toggle-group/icons.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={toggleGroupSolidIcons} />

    </Tab>
  </Tabs>
</div>
</ExampleCode>

### Multiple Selection

Set `multiple` when several options can stay pressed at the same time.

<ExampleFrame variant="component">
  <ToggleGroupMultipleSelection />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/toggle-group/multiple-selection.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={toggleGroupSolidMultipleSelection} />

    </Tab>
  </Tabs>
</div>
</ExampleCode>

### Variants

Set `variant` on the group when every item should share the same visual treatment.

<ExampleFrame variant="component">
  <ToggleGroupVariants />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/toggle-group/variants.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={toggleGroupSolidVariants} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/toggle-group/toggle-group-variants.module.css"
```
</div>
</ExampleCode>

### Vertical

Set `orientation="vertical"` when the choices should stack in a column.

<ExampleFrame variant="component">
  <ToggleGroupVertical />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/toggle-group/vertical.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={toggleGroupSolidVertical} />

    </Tab>
  </Tabs>
</div>
</ExampleCode>

### Advanced Customization

Use `ToggleGroup.RootProvider` with `useToggleGroup()` when the state instance must be created
outside the rendered group tree.

<ExampleFrame variant="component">
  <ToggleGroupRootProvider />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/toggle-group/root-provider.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={toggleGroupSolidRootProvider} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/toggle-group/toggle-group-advanced-customization.module.css"
```
</div>
</ExampleCode>

## Styling

Use `className` in React or `class` in Solid on exported parts for structural styles. Tailwind
variants use native utilities and consumer classes; CSS Modules variants expose the
`--moduix-toggle-group-*` tokens. Items inherit `variant` and `size` from the root, and can override
either prop when one item needs different treatment.

### CSS Properties

<CssPropertiesSection properties={ToggleGroupDocs.toggleGroupOverrideCssProperties} />

The table covers group-level properties. Each `ToggleGroup.Item` also supports the [Toggle CSS
properties](/docs/toggle#css-properties) for its colors and interaction states. Set a
`--moduix-toggle-*` property on the root to theme every item, or on an item to override one control.

### Styling hooks

- Stable `data-slot` selectors: `toggle-group-root`, `toggle-group-root-provider`, and `toggle-group-item`.
- Ark part selectors: `data-scope="toggle-group"` and `data-part="root" | "item"`.
- Ark state selectors: `data-state="on" | "off"`, `data-disabled`, `data-focus`, and
  `data-orientation="horizontal" | "vertical"`.
- moduix selectors: `data-variant` and `data-size`.
