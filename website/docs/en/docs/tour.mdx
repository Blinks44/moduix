---
title: Tour
component: tour
description: Build guided product walkthroughs with Ark UI tour state, accessible overlay parts, and ready-made actions.
---

import { TourCssPropertiesPanel } from '@/components/examples/tour/css-properties';
import TourAdvancedCustomization from '@/components/examples/tour/advanced-customization.tsx';
import TourBasic from '@/components/examples/tour/basic.tsx';
import TourEvents from '@/components/examples/tour/events.tsx';
import TourWaitForInput from '@/components/examples/tour/wait-for-input.tsx';
import TourWithArrow from '@/components/examples/tour/with-arrow.tsx';

## API Reference

<PrimitiveReference href="https://ark-ui.com/docs/components/tour" />

## Basic

Create tour state with `useTour`, define its steps, and pass the returned object to `Tour`. This
example starts with a dialog, moves to a tooltip anchored to a page control, and finishes with a
floating step. Keep the parts in JSX so each one can be adjusted when needed. `Tour.Body` keeps
long copy scrollable inside the panel.

<ExampleFrame variant="component">
  <TourBasic />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/tour/basic.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/tour/tour-basic.module.css"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

## Install with shadcn

<ShadcnInstall packageName="tour" />

## Anatomy

```text
Tour
└─ Overlay subtree (portalled by default)
   ├─ Tour.Backdrop
   ├─ Tour.Spotlight
   └─ Tour.Positioner
      └─ Tour.Content
         ├─ Tour.Arrow
         ├─ Tour.CloseIcon / Tour.CloseTrigger
         ├─ Tour.Body
         │  ├─ Tour.Title
         │  ├─ Tour.Description
         │  └─ Tour.ProgressText
         └─ Tour.Control
            ├─ Tour.ActionList
            └─ Tour.Actions
               └─ Tour.ActionTrigger[action]
```

| Part | Role |
| --- | --- |
| `Tour` | Root bound to a `UseTourReturn` object. `Tour.Root` is an alias. |
| `Tour.Backdrop` / `Tour.Spotlight` | Optional page overlay and highlight for the current target. |
| `Tour.Positioner` / `Tour.Content` | Position and render Ark's `alertdialog` surface. |
| `Tour.Arrow` | Renders moduix's `ArrowTip` by default for tooltip steps. |
| `Tour.CloseIcon` | Accessible icon-only dismiss control. Use `CloseTrigger` for custom markup. |
| `Tour.Body` | Scrollable moduix wrapper for the title, description, and progress text. |
| `Tour.Control` / `Tour.ActionList` | Recommended action row and the actions from the current step. |
| `Tour.Actions` / `Tour.ActionTrigger` | Custom action UI while preserving each Ark action object. |

## Composition

### Recommended composition

`Tour` automatically portals `Backdrop`, `Spotlight`, and `Positioner`. Set `portalled={false}`
only when the overlay must remain inline, or pass `portalRef` to render it in a specific container.
Keep the structural parts explicit. Add `Tour.ActionList` when the actions declared in `steps` suit
your interface; it renders the current step's actions for you.

### When to go custom

Use `Tour.Actions` and `Tour.ActionTrigger` when an action needs custom markup. They keep Ark's
navigation, dismissal, and disabled behavior while letting you render your own controls. Pass
`onStepChange`, `onStepsChange`, or `onStatusChange` to `useTour` when surrounding UI or analytics
need to react to the tour. Inside the tree, use the moduix exports `useTourContext` or
`Tour.Context` to access the active step and navigation methods.

### Default props

The portalled overlay mounts when the tour first opens and unmounts after its exit animation by
default. Set `unmountOnExit={false}` to keep it mounted after the first open. Set both lifecycle
props to `false` only when the overlay must render eagerly.

| Prop | Default | Notes |
| --- | --- | --- |
| `closeOnEscape` | `true` | Escape dismisses the tour. |
| `closeOnInteractOutside` | `true` | Interaction outside the tour dismisses it unless disabled. |
| `keyboardNavigation` | `true` | Left and Right navigate available steps. |
| `preventInteraction` | `false` | Set to `true` to block page interaction while the tour is open. |
| `spotlightOffset` | `{ x: 10, y: 10 }` | Expands the highlighted area around the target. |
| `spotlightRadius` | `4` | Rounds the corners of the spotlight. |
| `lazyMount` | `true` | Defers rendering the overlay until the first open. |
| `unmountOnExit` | `true` | Unmounts the overlay after the closing animation. |

### Behavioral notes

`Tour.Title` gives the alert dialog its accessible name, and `Tour.Description` supplies its
description. Ark manages focus on open and close, Escape, outside interaction, and Left/Right
navigation. Leave `keyboardNavigation` enabled unless a step intentionally uses those keys. In
right-to-left layouts, Ark reverses Left and Right navigation.

## Examples

### Events

Use the detail objects from Ark callbacks to update surrounding UI or record analytics. Start the
tour, choose Next, then Done. The `PreviewMeta` in this example shows both the step and final-status
callbacks.

<ExampleFrame variant="component">
  <TourEvents />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/tour/events.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/tour/tour-events.module.css"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Wait for input

This example calls `tour.next()` from the input handler after the value reaches two characters.
For steps that depend on an asynchronous condition, `waitForElement`, `waitForElementValue`,
`waitForEvent`, and `waitForPromise` are also exported from `@moduix/react/tour`.

<ExampleFrame variant="component">
  <TourWaitForInput />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/tour/wait-for-input.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/tour/tour-wait-for-input.module.css"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### With Arrow

Add `Tour.Arrow` to a tooltip step when it needs a visual connection to its target, and enable the
step's `arrow` option so Ark can position it.

<ExampleFrame variant="component">
  <TourWithArrow />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/tour/with-arrow.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/tour/tour-with-arrow.module.css"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Advanced customization

Replace `Tour.ActionList` when each action needs its own markup. The example uses custom `Button`
children for Continue, Back, and Skip tour. Each `Tour.ActionTrigger` still receives the original
Ark action object, so navigation, dismissal, and disabled state continue to work.

<ExampleFrame variant="component">
  <TourAdvancedCustomization />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/tour/advanced-customization.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/tour/tour-advanced-customization.module.css"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

## Styling

### CSS Properties

<div className="docs-reference">
  <Tabs
    values={[{ label: 'CSS Variables', value: 'CSS Variables' }]}
    className="docs-reference-tabs"
    contentItemClassName="docs-reference-panel"
  >
    <Tab value="CSS Variables">
      <div className="docs-reference-panel docs-reference-panel--compact">
        <TourCssPropertiesPanel />
      </div>
    </Tab>
  </Tabs>
</div>

### Styling hooks

Use moduix `data-slot` hooks such as `tour-content`, `tour-body`, and `tour-action-trigger` for
local styling. Ark also provides `data-scope="tour"`, `data-part`, `data-state`, `data-type`,
`data-placement`, `data-side`, `data-step`, and `data-disabled`, plus runtime variables including
`--tour-layer`, `--tour-z-index`, `--available-width`, `--available-height`, `--transform-origin`,
and the arrow variables. Pass `className` to `Tour.ActionList` to style every generated trigger.
The stylesheet shortens overlay animations and removes action-button transitions for people who
prefer reduced motion.
