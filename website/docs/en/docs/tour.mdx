---
title: Tour
component: tour
description: Build guided product walkthroughs with Ark UI tour state, accessible overlay parts, and ready-made actions.
---

import { TourCssPropertiesPanel } from '@/components/examples/tour/css-properties';
import TourAdvancedCustomization from '@/components/examples/tour/advanced-customization.tsx';
import TourBasic from '@/components/examples/tour/basic.tsx';
import TourEvents from '@/components/examples/tour/events.tsx';
import TourWaitForInput from '@/components/examples/tour/wait-for-input.tsx';
import TourWithArrow from '@/components/examples/tour/with-arrow.tsx';
import tourSolidAdvancedCustomization from '../../../snippets/tour/solid/advanced-customization.tsx?raw';
import tourSolidBasic from '../../../snippets/tour/solid/basic.tsx?raw';
import tourSolidEvents from '../../../snippets/tour/solid/events.tsx?raw';
import tourSolidWaitForInput from '../../../snippets/tour/solid/wait-for-input.tsx?raw';
import tourSolidWithArrow from '../../../snippets/tour/solid/with-arrow.tsx?raw';

## API Reference

<PrimitiveReference href="https://ark-ui.com/docs/components/tour" />

## Basic


Create tour state with `useTour`, define its steps, and pass the returned object to `Tour`. The example starts with a
dialog, moves to a tooltip anchored to a page control, and finishes with a floating step. Keep the parts explicit so
you can adjust the composition when needed. `TourBody` keeps long copy scrollable inside the panel.

<ExampleFrame variant="component">
  <TourBasic />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/tour/basic.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={tourSolidBasic} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/tour/tour-basic.module.css"
```
</div>

</ExampleCode>

## Install with shadcn

See [Quick Start](/docs/quick-start) for full package setup, registry configuration, and styling guidance.

<Tabs groupId="framework">
  <Tab label="React" value="react">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-react/tour' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-react-tailwind/tour' },
  ]}
/>

  </Tab>
  <Tab label="Solid" value="solid">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-solid/tour' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-solid-tailwind/tour' },
  ]}
/>

  </Tab>
</Tabs>

## Anatomy

```text
Tour
└─ Overlay subtree (portalled by default)
   ├─ TourBackdrop
   ├─ TourSpotlight
   └─ TourPositioner
      └─ TourContent
         ├─ TourArrow
         ├─ TourCloseIcon / TourCloseTrigger
         ├─ TourBody
         │  ├─ TourTitle
         │  ├─ TourDescription
         │  └─ TourProgressText
         └─ TourControl
            ├─ TourActionList
            └─ TourActions
               └─ TourActionTrigger[action]
```

| Part | Role |
| --- | --- |
| `Tour` | Root bound to the object returned by `useTour`. |
| `TourBackdrop` / `TourSpotlight` | Optional page overlay and highlight for the current target. |
| `TourPositioner` / `TourContent` | Position and render Ark's `alertdialog` surface. |
| `TourArrow` | Renders moduix's `TourArrowTip` by default for tooltip steps. |
| `TourCloseIcon` / `TourCloseTrigger` | Default accessible dismiss control or a custom close control. |
| `TourBody` | Scrollable moduix wrapper for the title, description, and progress text. |
| `TourControl` / `TourActionList` | Recommended action row and the actions from the current step. |
| `TourActions` / `TourActionTrigger` | Custom action UI while preserving each Ark action object. |

## Composition

Create state with `useTour` and pass its return value to `Tour`; in Solid, the return value is an accessor, so call
`tour()` to use methods such as `start()` and `next()`. Keep the structural parts explicit and include `TourTitle` and
`TourDescription` so the alert dialog has an accessible name and description. `TourBackdrop`, `TourSpotlight`, and `TourPositioner`
are portalled by default; use `portalled={false}` or `portalRef` only when the overlay must stay in or target a specific
container. Use `TourActionList` for the actions declared in `steps`, or replace it with `TourActions` and
`TourActionTrigger asChild` when each action needs custom markup. `lazyMount` and `unmountOnExit` default to `true`; pass
callbacks to `useTour` when surrounding UI or analytics need to react to step or status changes.

## Examples

### Events

Use the detail objects from Ark callbacks to update surrounding UI or record analytics. Start the tour, choose Next,
then Done. The status output shows the latest step or final status event.

<ExampleFrame variant="component">
  <TourEvents />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/tour/events.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={tourSolidEvents} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/tour/tour-events.module.css"
```
</div>

</ExampleCode>

### Wait for input

Call `tour.next()` from an input handler after the value reaches two characters. For steps that depend on an
asynchronous condition, `waitForElement`, `waitForElementValue`, `waitForEvent`, and `waitForPromise` are also
exported from `@moduix/react/tour` and `@moduix/solid/tour`.

<ExampleFrame variant="component">
  <TourWaitForInput />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/tour/wait-for-input.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={tourSolidWaitForInput} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/tour/tour-wait-for-input.module.css"
```
</div>

</ExampleCode>

### With Arrow

Add `TourArrow` to a tooltip step when it needs a visual connection to its target, and enable the step's `arrow`
option so Ark can position it.

<ExampleFrame variant="component">
  <TourWithArrow />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/tour/with-arrow.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={tourSolidWithArrow} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/tour/tour-with-arrow.module.css"
```
</div>

</ExampleCode>

### Advanced customization

Replace `TourActionList` when each action needs its own markup. The example uses custom `Button` children for
Continue, Back, and Skip tour. Each `TourActionTrigger` still receives the original Ark action object, so navigation,
dismissal, and disabled state continue to work.

<ExampleFrame variant="component">
  <TourAdvancedCustomization />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/tour/advanced-customization.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={tourSolidAdvancedCustomization} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/tour/tour-advanced-customization.module.css"
```
</div>

</ExampleCode>

## Styling

### CSS Properties

<div className="docs-reference">
  <Tabs
    values={[{ label: 'CSS Variables', value: 'CSS Variables' }]}
    className="docs-reference-tabs"
    contentItemClassName="docs-reference-panel"
  >
    <Tab value="CSS Variables">
      <div className="docs-reference-panel docs-reference-panel--compact">
        <TourCssPropertiesPanel />
      </div>
    </Tab>
  </Tabs>
</div>

### Styling hooks

Use moduix `data-slot` hooks such as `tour-content`, `tour-body`, and `tour-action-trigger` for local styling. Ark also
provides `data-scope="tour"`, `data-part`, `data-state`, `data-type`, `data-placement`, `data-side`, `data-step`, and
`data-disabled`, plus runtime variables including `--tour-layer`, `--tour-z-index`, `--available-width`,
`--available-height`, `--transform-origin`, and the arrow variables. Pass `className` to `TourActionList` to style
every generated trigger. The stylesheet shortens overlay animations and removes action-button transitions for people
who prefer reduced motion.
