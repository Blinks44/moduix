---
title: Stack
component: stack
description: A lightweight flex layout primitive for arranging content vertically or horizontally in React, Solid, or Vue.
---

import * as StackDocs from '@/components/examples/stack/css-properties';
import StackBasic from '@/components/examples/stack/basic.tsx';
import StackFill from '@/components/examples/stack/growing-child-stack.tsx';
import StackLoadingLayout from '@/components/examples/stack/loading-layout.tsx';
import StackResponsiveDirection from '@/components/examples/stack/responsive-direction.tsx';
import StackRowLayout from '@/components/examples/stack/row-layout.tsx';
import StackSemanticElement from '@/components/examples/stack/semantic-element.tsx';
import StackSeparator from '@/components/examples/stack/separator.tsx';
import StackWrappedItems from '@/components/examples/stack/wrapped-items.tsx';
import stackSolidBasic from '../../../snippets/stack/solid/basic.tsx?raw';
import stackSolidFill from '../../../snippets/stack/solid/growing-child-stack.tsx?raw';
import stackSolidLoadingLayout from '../../../snippets/stack/solid/loading-layout.tsx?raw';
import stackSolidResponsiveDirection from '../../../snippets/stack/solid/responsive-direction.tsx?raw';
import stackSolidRowLayout from '../../../snippets/stack/solid/row-layout.tsx?raw';
import stackSolidSemanticElement from '../../../snippets/stack/solid/semantic-element.tsx?raw';
import stackSolidSeparator from '../../../snippets/stack/solid/separator.tsx?raw';
import stackSolidWrappedItems from '../../../snippets/stack/solid/wrapped-items.tsx?raw';
import stackVueBasic from '../../../snippets/stack/vue/basic.vue?raw';
import stackVueFill from '../../../snippets/stack/vue/growing-child-stack.vue?raw';
import stackVueLoadingLayout from '../../../snippets/stack/vue/loading-layout.vue?raw';
import stackVueResponsiveDirection from '../../../snippets/stack/vue/responsive-direction.vue?raw';
import stackVueRowLayout from '../../../snippets/stack/vue/row-layout.vue?raw';
import stackVueSemanticElement from '../../../snippets/stack/vue/semantic-element.vue?raw';
import stackVueSeparator from '../../../snippets/stack/vue/separator.vue?raw';
import stackVueWrappedItems from '../../../snippets/stack/vue/wrapped-items.vue?raw';

## API Reference

Ark UI does not include a dedicated `Stack` primitive. moduix implements this component with Ark's official
[factory](https://ark-ui.com/docs/guides/composition#the-ark-factory) and follows Ark's guidance for
[composition](https://ark-ui.com/docs/guides/composition), [styling](https://ark-ui.com/docs/guides/styling), and
[refs](https://ark-ui.com/docs/guides/ref). Chakra UI's [Stack](https://chakra-ui.com/docs/components/stack)
documentation informs the layout prop names only.

## Basic


Use `Stack` when a single flex container is enough and you only need to control direction, gap, and alignment.

<ExampleFrame variant="component">
  <StackBasic />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
      { label: 'Vue', value: 'vue' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/stack/basic.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={stackSolidBasic} />

    </Tab>
    <Tab value="vue">

<CodeBlockRuntime lang="vue" code={stackVueBasic} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

## Install with shadcn

See [Quick Start](/docs/quick-start) for full package setup, registry configuration, and styling guidance.

<Tabs groupId="framework">
  <Tab label="React" value="react">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-react/stack' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-react-tailwind/stack' },
  ]}
/>

  </Tab>
  <Tab label="Solid" value="solid">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-solid/stack' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-solid-tailwind/stack' },
  ]}
/>

  </Tab>
  <Tab label="Vue" value="vue">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-vue/stack' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-vue-tailwind/stack' },
  ]}
/>

  </Tab>
</Tabs>

## Anatomy

`Stack` renders one flex root that follows Ark's factory model. It controls direction, gap, alignment, wrapping, and
a simple responsive direction switch. Keep broader layout rules in your local CSS.

```text
Stack
└─ root[data-scope="stack"][data-part="root"][data-slot="stack-root"]
   └─ children
```

| Part                   | Role                                                                |
| ---------------------- | ------------------------------------------------------------------- |
| `Stack`                | Root flex wrapper for direction, gap, alignment, and child spacing. |

## Composition

`Stack` owns no managed state and exposes one root. Use `<Stack>` for regular `div` output. `fill` applies to the
`Stack` as a flex item, not to its children. Add `Separator` directly as a child when the layout needs a divider,
and use `asChild` when the root should be a semantic element.

## Examples

### Growing Child Stack

Use `fill` when a nested `Stack` should take the remaining space in a flex row.

<ExampleFrame variant="component">
  <StackFill />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
      { label: 'Vue', value: 'vue' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/stack/growing-child-stack.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={stackSolidFill} />

    </Tab>
    <Tab value="vue">

<CodeBlockRuntime lang="vue" code={stackVueFill} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Loading Layout

Combine `Stack` and `Skeleton` for a loading layout that only needs flex composition and spacing.

<ExampleFrame variant="component">
  <StackLoadingLayout />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
      { label: 'Vue', value: 'vue' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/stack/loading-layout.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={stackSolidLoadingLayout} />

    </Tab>
    <Tab value="vue">

<CodeBlockRuntime lang="vue" code={stackVueLoadingLayout} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Responsive Direction

Use responsive `direction` for a simple column-on-mobile, row-on-desktop layout. The desktop value starts at `640px`;
if only one responsive value is provided, Stack uses it at both breakpoints.

<ExampleFrame variant="component">
  <StackResponsiveDirection />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
      { label: 'Vue', value: 'vue' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/stack/responsive-direction.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={stackSolidResponsiveDirection} />

    </Tab>
    <Tab value="vue">

<CodeBlockRuntime lang="vue" code={stackVueResponsiveDirection} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Row Layout

Use `direction="row"` with alignment props for inline content.

<ExampleFrame variant="component">
  <StackRowLayout />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
      { label: 'Vue', value: 'vue' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/stack/row-layout.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={stackSolidRowLayout} />

    </Tab>
    <Tab value="vue">

<CodeBlockRuntime lang="vue" code={stackVueRowLayout} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Semantic Element

Use Ark's `asChild` when the layout wrapper should also express document structure.

<ExampleFrame variant="component">
  <StackSemanticElement />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
      { label: 'Vue', value: 'vue' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/stack/semantic-element.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={stackSolidSemanticElement} />

    </Tab>
    <Tab value="vue">

<CodeBlockRuntime lang="vue" code={stackVueSemanticElement} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Separator

Add `Separator` directly when a horizontal or vertical `Stack` needs a repeated visual divider between children. `gap`
applies to every child, including dividers.

<ExampleFrame variant="component">
  <StackSeparator />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
      { label: 'Vue', value: 'vue' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/stack/separator.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={stackSolidSeparator} />

    </Tab>
    <Tab value="vue">

<CodeBlockRuntime lang="vue" code={stackVueSeparator} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Wrapped Items

Use `wrap="wrap"` when items should flow onto the next line.

<ExampleFrame variant="component">
  <StackWrappedItems />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
      { label: 'Vue', value: 'vue' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/stack/wrapped-items.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={stackSolidWrappedItems} />

    </Tab>
    <Tab value="vue">

<CodeBlockRuntime lang="vue" code={stackVueWrappedItems} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

## Styling

Use `className` in React or `class` in Solid and Vue on the root for structural styles. Tailwind variants use native utility
classes and consumer overrides; the CSS variables below customize the CSS Modules variants. Data attributes and the
`data-slot` hook are shared by both styling tracks.

### CSS Properties

`Stack` exposes public CSS variables on its root. By default, they use `column` and `initial`; the `direction` and
`fill` props write inline values when provided, and `style` can override them for a single instance.

<div className="docs-reference">
  <Tabs
    values={[{ label: 'CSS Variables', value: 'CSS Variables' }]}
    className="docs-reference-tabs"
    contentItemClassName="docs-reference-panel"
  >
    <Tab>
      <StackDocs.StackCssPropertiesPanel />
    </Tab>
  </Tabs>
</div>

### Styling hooks

- `className` or `class` styles the single root for width, padding, borders, backgrounds, and other surface concerns.
- `data-scope="stack"` and `data-part="root"` align the wrapper with Ark styling conventions.
- `data-slot="stack-root"` is the stable moduix selector for targeting the root in local CSS.
- `style` is applied last, so it can override computed `gap`, `alignItems`, `justifyContent`, `flexWrap`, and any
  `--moduix-stack-*` variables written by props.
- There are no state attributes, CSS animation variables, or `slotProps` maps in the public contract.
