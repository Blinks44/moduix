---
title: Sidebar
component: sidebar
description: Resizable and collapsible application navigation built on the Ark UI Splitter.
---

import { SidebarCssPropertiesPanel } from '@/components/examples/sidebar';

## API Reference

`Sidebar` uses the [Ark UI Splitter](https://ark-ui.com/docs/components/splitter) for resizing,
collapse state, keyboard interaction, and accessibility. Its semantic navigation anatomy is
informed by the [shadcn/ui Sidebar](https://ui.shadcn.com/docs/components/sidebar), while the
state contract remains Ark-shaped.

<PrimitiveReference href="https://ark-ui.com/docs/components/splitter" />

## Choosing the right component

- Choose **`Sidebar`** for application navigation that should resize, collapse to an icon rail, and
  sit beside the main content.
- Choose **`Splitter`** when both panels are generic content regions rather than a navigation and
  inset pair.

## Basic

Render the navigation panel, Ark’s resize handle, and the main content area as separate parts. The
default ids and panel constraints cover the usual left-sidebar layout, so no setup is required.
The example also shows how to label icon-only navigation controls after collapse: wrap the button in
`Sidebar.Tooltip`. Tooltip keeps ownership of its own popup state.

<PreviewFrame>
  ```tsx preview file="./_snippets/sidebar/basic.tsx"

  ```
</PreviewFrame>

## Install with shadcn

<ShadcnInstall packageName="sidebar" />

## Anatomy

```text
Sidebar / Sidebar.Root
├─ Sidebar.Panel[id="sidebar"]
│  ├─ Sidebar.Header
│  │  ├─ Sidebar.Label
│  │  └─ Sidebar.Input
│  ├─ Sidebar.Content
│  │  └─ Sidebar.Group
│  │     ├─ Sidebar.GroupLabel
│  │     ├─ Sidebar.GroupAction
│  │     └─ Sidebar.NavigationList
│  │           └─ Sidebar.NavigationItem
│  │              ├─ Sidebar.Tooltip
│  │              │  └─ Sidebar.NavigationButton
│  │              ├─ Sidebar.NavigationAction
│  │              ├─ Sidebar.NavigationBadge
│  │              └─ Sidebar.NavigationSubList
│  │                 └─ Sidebar.NavigationSubItem
│  │                    └─ Sidebar.NavigationSubButton
│  └─ Sidebar.Footer
│     └─ Sidebar.Separator
├─ Sidebar.ResizeTrigger
├─ Sidebar.Trigger
└─ Sidebar.Inset[id="content"]
```

| Part                                    | Role                                                      |
| --------------------------------------- | --------------------------------------------------------- |
| `Sidebar` / `Sidebar.Root`              | Ark Splitter root with useful two-panel sidebar defaults. |
| `Sidebar.Panel`                         | Collapsible navigation panel; defaults to id `"sidebar"`. |
| `Sidebar.Inset`                         | Main content panel; defaults to id `"content"`.           |
| `Sidebar.ResizeTrigger`                 | Accessible neutral pointer and keyboard resize line.      |
| `Sidebar.Trigger`                       | Side-aware floating collapse/expand control.              |
| `Sidebar.Label`                         | Text removed from layout in the collapsed icon rail.      |
| `Sidebar.Input`                         | Full-width search or filter field styled for sidebar use. |
| `Sidebar.Header` / `Content` / `Footer` | Fixed top, scrolling middle, and fixed bottom regions.    |
| `Sidebar.Separator`                     | Sidebar-tinted divider between sections or footer blocks. |
| `Sidebar.Group`                         | Semantic navigation section with an optional heading.     |
| `Sidebar.GroupAction`                   | Compact action button aligned with a group heading.       |
| `Sidebar.GroupContent`                  | Optional wrapper for a custom group body.                 |
| `Sidebar.NavigationList` / `Sidebar.NavigationItem` | Navigation list and positioned list item.               |
| `Sidebar.Tooltip`                       | Collapsed-only label helper with side-aware placement.    |
| `Sidebar.NavigationButton`                    | Button or `asChild` link with active and size states.     |
| `Sidebar.NavigationAction`                    | Trailing icon action for a menu item.                     |
| `Sidebar.NavigationBadge`                     | Trailing count or status pill for a menu item.            |
| `Sidebar.NavigationSubList`                       | Nested navigation list with item and link parts.          |

## Composition

### Recommended composition

Keep each layout part as a direct child of the root. For a left sidebar, render `Panel`,
`ResizeTrigger`, `Trigger`, then `Inset`. For a right sidebar, use the reverse visual order:
`Inset`, `Trigger`, `ResizeTrigger`, then `Panel`. The trigger is positioned over the resize line
and the usual content-header divider; it does not take up layout width or get clipped by either
panel. Set `--moduix-sidebar-trigger-offset-y` if your content header has a different height.

Use `Collapsible` for nested menu items and `Menu + Avatar` for account actions. Place
`Menu.Indicator` directly in `Sidebar.NavigationButton`; it is aligned at the end of the row and hidden
in the collapsed rail. `Sidebar.Input`, `Sidebar.GroupAction`, `Sidebar.NavigationAction`, `Sidebar.NavigationBadge`, and
`Sidebar.Separator` cover common app-shell UI without adding another Sidebar state layer.

For icon-only buttons, wrap `Sidebar.NavigationButton` in `Sidebar.Tooltip` so their labels remain
available on hover. The two usual integration patterns are:

1. **Collapsed rail labels:** `Sidebar.Tooltip` around `Sidebar.NavigationButton`.
2. **Persistence:** keep width in controlled `size`, update it from `onResize(details)`, and persist
   it from `onResizeEnd(details)` in app state or storage.

`Sidebar` is shorthand for `Sidebar.Root`.

`Sidebar.NavigationList` is normally a direct child of `Sidebar.Group`; `Sidebar.GroupContent` is only
needed when an application needs an additional custom body wrapper. Sidebar never chooses how nested
links behave in the collapsed rail: when they must stay reachable, use `useSidebar().collapsed` and
explicitly render the application’s chosen popup `Menu` or another navigation pattern instead of the
inline `Collapsible` list.

### When to go custom

Use `defaultSize`, controlled `size`, and callbacks when the default CSS-length layout does not fit.
`useSidebar()` exposes `collapsed`, `state`, `side`, and `toggleSidebar`. Reach for `Splitter`
directly if you need different panel constraints, its lower-level provider or registry APIs, or more
than two panels.

`panelId` changes the default navigation id together with the matching resize handle; the inset id
remains `"content"`. The Sidebar layout parts intentionally do not accept individual ids, which
keeps the rendered panels, trigger, and `useSidebar()` on the same Ark panel contract. Use
`Splitter` directly when a layout needs a custom inset id or more than two panels.

Use `asChild` on navigation buttons and nested navigation buttons to render router links. Pass a single semantic child
that accepts the merged props and ref.

Keep the Splitter-bound pieces inside `Sidebar`: `Panel`, `Inset`, `ResizeTrigger`, `Trigger`, and
`useSidebar()`.

### Default props

| Value                | Default                                                         |
| -------------------- | --------------------------------------------------------------- |
| Panel ids            | `"sidebar"` and `"content"`                                     |
| Initial sidebar size | `"16rem"` (256px)                                               |
| Sidebar constraints  | `minSize: "3rem"`, `maxSize: "18rem"`, `collapsedSize: "3rem"`  |
| Content constraints  | No minimum                                                      |
| Side                 | `"left"`                                                        |
| Orientation          | `"horizontal"` (fixed for sidebar/inset layout)                 |
| Root `panelId`       | `"sidebar"`                                                     |
| Navigation button `size` | `"md"`                                                       |

### Behavioral notes

Ark accepts percentages and CSS lengths for panel sizes. CSS lengths are measured in the browser, so
a server-rendered layout can move after hydration. Prefer percentages when keeping SSR layout stable
is important. `Trigger` exposes `aria-expanded`, calls your `onClick` first, and does not toggle if
that handler calls `preventDefault()`.

Ark preserves pointer dragging, arrow-key resizing, focus state, and callback detail objects:
`onResize(details)`, `onResizeEnd(details)`, `onCollapse(details)`, and `onExpand(details)`.

Panel collapse and expansion follow Ark sizes immediately. Sidebar intentionally does not animate
panel width because a CSS flex transition would lag behind pointer and keyboard resizing.
Collapsible content, popup content, tooltips, and the trigger icon retain their own transitions.

## Examples

### Custom Scroll Area

Native scrolling is the default. If you need a styled thumb or an optional edge fade, place a
`ScrollArea` inside `Sidebar.Content`. Disable overflow on the outer content area so
`ScrollArea.Viewport` is the only scroll container.

<PreviewFrame>
  ```tsx preview file="./_snippets/sidebar/custom-scroll-area.tsx"

  ```
</PreviewFrame>

### Persisted Desktop Layout

Store a desktop sidebar width outside the component. Control `size`, update it while dragging with
`onResize(details)`, and save the final value in `onResizeEnd(details)`. This keeps `Splitter` in
charge of the layout while letting the width survive the next visit.

<PreviewFrame>
  ```tsx preview file="./_snippets/sidebar/persisted-layout.tsx"

  ```
</PreviewFrame>

### Recommended App Shell

Place `Sidebar.Input`, `Sidebar.GroupAction`, `Sidebar.NavigationAction`, and
`Sidebar.NavigationBadge` directly beside the navigation item they affect. Keep `Collapsible` and popup
`Menu` explicit: each primitive owns its own state. Each moduix primitive continues to manage its own state, focus, popup
positioning, and animation. This example also shows a long project list scrolling inside
`Sidebar.Content` and tooltips for the collapsed icon rail.

<PreviewFrame>
  ```tsx preview file="./_snippets/sidebar/nested-navigation-and-account-menu.tsx"

  ```
</PreviewFrame>

### Right Sidebar

Pass `side="right"` and render `Inset`, `Trigger`, `ResizeTrigger`, and `Panel` in that order.
Sidebar adjusts the panel setup, trigger id and position, and chevron direction automatically. The
Inspector example includes grouped tools, activity links, internal scrolling, and a footer menu.

<PreviewFrame>
  ```tsx preview file="./_snippets/sidebar/right-sidebar.tsx"

  ```
</PreviewFrame>

### Workspace Select

Use `Select.Trigger asChild` with `Sidebar.NavigationButton`. Put the text and indicators in
`Sidebar.Label`, then mark the compact visual anchor with `data-sidebar-icon`. The component can
now collapse without custom JavaScript, and the wrapped `Select.Indicator` stays aligned to the end
of the row. The example combines a workspace switcher with navigation, a scrollable content area,
and an account menu.

<PreviewFrame>
  ```tsx preview file="./_snippets/sidebar/workspace-select.tsx"

  ```
</PreviewFrame>

### Advanced Customization

Use `Splitter` directly when a layout needs different panel constraints, custom inset ids, more than
two panels, or provider and registry APIs.

<PreviewFrame>
  ```tsx preview file="./_snippets/sidebar/custom-sizes.tsx"

  ```
</PreviewFrame>

## Styling

The navigation panel uses card tokens; the inset uses the application background. The default panel
data configures a `3rem` rail. Collapsed panels move `Sidebar.Label` and group labels out of layout
with a visually-hidden pattern, hide nested navigation lists and `Sidebar.Input`, and center SVG and
`data-sidebar-icon` elements. Labels remain available to assistive technology without taking flex
space. Mark non-SVG anchors such as Avatar, a Select workspace mark, or a brand mark with
`data-sidebar-icon`; its own dimensions are preserved. Put collapsible text, values, badges, and
indicators in `Sidebar.Label` when they should leave the visual layout in the collapsed rail.
Trailing group and navigation affordances hide automatically in the collapsed icon-only state.
Navigation buttons and nested navigation buttons truncate direct text at any Sidebar width. Use `Sidebar.Label`
when text, values, badges, or indicators must also leave the collapsed rail.

At narrow expanded widths (below `7rem`), Sidebar also hides trailing group actions, navigation actions,
badges, and indicators before they can overlap the leading icon. The panel remains expanded: its Ark state
and labels are unchanged.

Panel widths are Ark state rather than visual CSS. Override `defaultSize` and controlled `size` for
the Sidebar's current width; use `Splitter` when the expanded, minimum, maximum, or collapsed
constraints must differ. `panelId` changes the default navigation id. Use the public
`--moduix-sidebar-*` variables for internal spacing, item dimensions, colors, and the trigger's vertical
offset.

The floating trigger sits at the intersection of the resize line and a typical inset topbar divider,
keeping clear of the navigation header. Its default `40px` offset suits a `3.5rem` inset topbar;
override `--moduix-sidebar-trigger-offset-y` for a custom header height. The resize UI is
intentionally only a line. Its idle color matches the border, hover and drag shift
it slightly toward `--moduix-color-muted-foreground`, and keyboard focus uses the Sidebar focus-ring
color. Override the `--moduix-splitter-resize-trigger-line-color-*` variables only when the product
needs stronger feedback.

### CSS Properties

<div className="docs-reference">
  <Tabs
    values={[{ label: 'CSS Variables', value: 'CSS Variables' }]}
    className="docs-reference-tabs"
    contentItemClassName="docs-reference-panel"
  >
    <Tab>
      <SidebarCssPropertiesPanel />
    </Tab>
  </Tabs>
</div>

### Styling hooks

Every semantic part accepts `className` and exposes a stable `data-slot="sidebar-*"` hook. The
Sidebar shell parts also expose `data-scope="sidebar"` and `data-part`. `Sidebar.Input` and
`Sidebar.Separator` retain the scopes of their underlying primitives, and Ark-backed layout parts
retain Splitter state attributes.

`Sidebar.Panel` adds `data-state="expanded"` or `"collapsed"`. Root, panel, inset, resize trigger,
and floating trigger expose `data-side`. `NavigationButton` and `NavigationSubButton` expose `data-active`,
`NavigationButton` exposes `data-size`, and the extra shell parts keep stable `data-slot="sidebar-*"`
hooks for custom layout or theme work. Resize state remains available through Ark's `data-focus`,
`data-dragging`, `data-disabled`, and `data-orientation`.
