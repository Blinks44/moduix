---
title: Toast
component: toast
description: Transient notifications for actions, status changes, and contextual feedback.
---

import * as ToastDocs from '@/components/examples/toast/css-properties';
import ToastAction from '@/components/examples/toast/action.tsx';
import ToastBasic from '@/components/examples/toast/basic.tsx';
import ToastCustomComposition from '@/components/examples/toast/custom-composition.tsx';
import ToastDuration from '@/components/examples/toast/duration.tsx';
import ToastMaximumVisible from '@/components/examples/toast/max-toasts.tsx';
import ToastNonOverlapping from '@/components/examples/toast/always-expanded.tsx';
import ToastPlacement from '@/components/examples/toast/placement.tsx';
import ToastPromise from '@/components/examples/toast/promise-toast.tsx';
import ToastTypes from '@/components/examples/toast/types.tsx';
import ToastUpdate from '@/components/examples/toast/update.tsx';
import toastSolidAction from '../../../snippets/toast/solid/action.tsx?raw';
import toastSolidBasic from '../../../snippets/toast/solid/basic.tsx?raw';
import toastSolidCustomComposition from '../../../snippets/toast/solid/custom-composition.tsx?raw';
import toastSolidDuration from '../../../snippets/toast/solid/duration.tsx?raw';
import toastSolidMaximumVisible from '../../../snippets/toast/solid/max-toasts.tsx?raw';
import toastSolidNonOverlapping from '../../../snippets/toast/solid/always-expanded.tsx?raw';
import toastSolidPlacement from '../../../snippets/toast/solid/placement.tsx?raw';
import toastSolidPromise from '../../../snippets/toast/solid/promise-toast.tsx?raw';
import toastSolidTypes from '../../../snippets/toast/solid/types.tsx?raw';
import toastSolidUpdate from '../../../snippets/toast/solid/update.tsx?raw';

## API Reference

<PrimitiveReference href="https://ark-ui.com/docs/components/toast" />

Moduix exports `Toast`, `ToastToaster`, `createToaster`, and `useToastContext` from
`@moduix/react/toast` and `@moduix/solid/toast`. The Ark reference contains the complete store and
part API.

## Basic


Create a toaster once with `createToaster()`, use its methods to add notifications, and render it
with `ToastToaster`.

<ExampleFrame variant="component">
  <ToastBasic />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/toast/basic.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={toastSolidBasic} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/toast/toast-basic.module.css"
```
</div>

</ExampleCode>

## Install with shadcn

See [Quick Start](/docs/quick-start) for full package setup, registry configuration, and styling guidance.

<Tabs groupId="framework">
  <Tab label="React" value="react">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-react/toast' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-react-tailwind/toast' },
  ]}
/>

  </Tab>
  <Tab label="Solid" value="solid">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-solid/toast' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-solid-tailwind/toast' },
  ]}
/>

  </Tab>
</Tabs>

## Anatomy

```text
createToaster()
└─ ToastToaster (portalled to document.body by default)
   └─ Toast
      ├─ ToastTitle
      ├─ ToastDescription
      ├─ ToastActionTrigger
      └─ ToastCloseTrigger
```

| Part                           | Role                                                                                   |
| ------------------------------ | -------------------------------------------------------------------------------------- |
| `Toast`               | Root of a visible toast.                                             |
| `ToastToaster`        | Renders the toaster store and manages the toast group.               |
| `ToastTitle`          | Title part. Uses the current toast `title` when no children are passed. |
| `ToastDescription`    | Description part. Uses the current toast `description` when no children are passed. |
| `ToastActionTrigger`  | Action button connected to `toast.action.onClick`.                   |
| `ToastCloseTrigger`   | Dismiss button. Uses `CloseButton` by default.                       |
| `ToastContext`        | Context part for custom composition and direct toast state access.   |

## Composition

Create the toaster outside the component render path when possible and pass the same store to
`ToastToaster`. The store owns placement, stacking, queue size, timing, and focus behavior. `ToastToaster`
portals the group to `document.body` by default; use `portalled={false}` or `portalRef` when it must
stay in another container. A separate `Portal` wrapper is not needed.

Use the default `ToastToaster` renderer for a title, description, action, and close button. For a different
layout or custom controls, pass a render prop, keep `Toast` as the outer item, and compose its parts
inside. Use `asChild` only with a semantic button child, and use `useToastContext()`
when a custom child needs direct access to the current toast.

## Examples

### Action

Add `action: { label, onClick }` when the notification should offer one direct follow-up action.

<ExampleFrame variant="component">
  <ToastAction />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs groupId="framework" values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]} className="docs-tabs">
    <Tab value="react">

```tsx pure file="../../../src/components/examples/toast/action.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={toastSolidAction} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/toast/toast-action.module.css"
```
</div>

</ExampleCode>

### Duration

Set `duration` in milliseconds when a notification should disappear sooner, stay visible longer, or
remain until the user dismisses it with `Infinity`.

<ExampleFrame variant="component">
  <ToastDuration />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs groupId="framework" values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]} className="docs-tabs">
    <Tab value="react">

```tsx pure file="../../../src/components/examples/toast/duration.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={toastSolidDuration} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/toast/toast-duration.module.css"
```
</div>

</ExampleCode>

### Maximum visible

Set `max` on the toaster when bursts of notifications should queue instead of filling the screen.

<ExampleFrame variant="component">
  <ToastMaximumVisible />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs groupId="framework" values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]} className="docs-tabs">
    <Tab value="react">

```tsx pure file="../../../src/components/examples/toast/max-toasts.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={toastSolidMaximumVisible} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/toast/toast-max-toasts.module.css"
```
</div>

</ExampleCode>

### Non-overlapping stack

Set `overlap: false` when every toast should remain fully visible. This is a toaster setting, not a
separate component mode.

<ExampleFrame variant="component">
  <ToastNonOverlapping />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs groupId="framework" values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]} className="docs-tabs">
    <Tab value="react">

```tsx pure file="../../../src/components/examples/toast/always-expanded.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={toastSolidNonOverlapping} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/toast/toast-always-expanded.module.css"
```
</div>

</ExampleCode>

### Placement

Choose one of Ark's six placements when the notification group should appear at a specific edge of
the viewport.

<ExampleFrame variant="component">
  <ToastPlacement />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs groupId="framework" values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]} className="docs-tabs">
    <Tab value="react">

```tsx pure file="../../../src/components/examples/toast/placement.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={toastSolidPlacement} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/toast/toast-placement.module.css"
```
</div>

</ExampleCode>

### Promise

Use `toaster.promise()` to move one notification from loading to success or error as an asynchronous
operation settles.

<ExampleFrame variant="component">
  <ToastPromise />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs groupId="framework" values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]} className="docs-tabs">
    <Tab value="react">

```tsx pure file="../../../src/components/examples/toast/promise-toast.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={toastSolidPromise} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/toast/toast-promise-toast.module.css"
```
</div>

</ExampleCode>

### Types

Use the typed toaster helpers for status-specific variants. Moduix styles `info`, `success`,
`warning`, and `error` through `data-type`.

<ExampleFrame variant="component">
  <ToastTypes />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs groupId="framework" values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]} className="docs-tabs">
    <Tab value="react">

```tsx pure file="../../../src/components/examples/toast/types.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={toastSolidTypes} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/toast/toast-types.module.css"
```
</div>

</ExampleCode>

### Update

Keep the id returned when a toast is created and pass it to `toaster.update()` when the existing
notification should change in place.

<ExampleFrame variant="component">
  <ToastUpdate />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs groupId="framework" values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]} className="docs-tabs">
    <Tab value="react">

```tsx pure file="../../../src/components/examples/toast/update.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={toastSolidUpdate} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/toast/toast-update.module.css"
```
</div>

</ExampleCode>

### Advanced Customization

Use the render prop to bypass the default `ToastToaster` renderer, add an icon, replace the close icon,
and apply component-specific CSS variables.

<ExampleFrame variant="component">
  <ToastCustomComposition />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs groupId="framework" values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]} className="docs-tabs">
    <Tab value="react">

```tsx pure file="../../../src/components/examples/toast/custom-composition.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={toastSolidCustomComposition} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/toast/toast-custom-composition.module.css"
```
</div>

</ExampleCode>

## Styling

Style `Toast` with Ark part attributes, `data-slot` hooks, and the public `--moduix-toast-*`
variables. Tailwind variants use native utilities and consumer classes; the CSS variables below apply
to the CSS Modules variants.

### CSS Variables

<div className="docs-reference">
  <Tabs
    values={[{ label: 'CSS Variables', value: 'CSS Variables' }]}
    className="docs-reference-tabs"
    contentItemClassName="docs-reference-panel"
  >
    <Tab>
      <ToastDocs.ToastCssPropertiesPanel />
    </Tab>
  </Tabs>
</div>

### Styling hooks

Use the toast root for the surface, border, motion, and type-based variants. Use `ToastTitle`,
`ToastDescription`, `ToastActionTrigger`, and `ToastCloseTrigger` for inner typography and
controls.

Useful Ark selectors:

- `[data-scope='toast'][data-part='group']` on `ToastToaster`
- `[data-scope='toast'][data-part='root']` on `Toast`
- `[data-scope='toast'][data-part='title']` on `ToastTitle`
- `[data-scope='toast'][data-part='description']` on `ToastDescription`
- `[data-scope='toast'][data-part='action-trigger']` on `ToastActionTrigger`
- `[data-scope='toast'][data-part='close-trigger']` on `ToastCloseTrigger`
- `--x`, `--y`, `--scale`, `--z-index`, `--height`, `--opacity`, and `--gap` on `Toast`

Moduix also adds stable hooks: `data-slot="toast-toaster"`, `data-slot="toast-root"`,
`data-slot="toast-title"`, `data-slot="toast-description"`, `data-slot="toast-action-trigger"`,
and `data-slot="toast-close-trigger"`.

At viewport widths up to `40rem`, the toaster group spans the viewport and each root uses Ark's
`--gap` variable to keep safe inline spacing.
