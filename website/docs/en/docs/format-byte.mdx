---
title: Format Byte
description: Format byte values as readable sizes in React and Solid with Ark UI, including decimal or binary units and locale-aware output.
---

import FormatByteBasic from '@/components/examples/format-byte/basic.tsx';
import FormatByteUnitSystem from '@/components/examples/format-byte/unit-system.tsx';
import formatByteSolidBasic from '../../../snippets/format-byte/solid/basic.tsx?raw';
import formatByteSolidUnitSystem from '../../../snippets/format-byte/solid/unit-system.tsx?raw';
import formatByteSolidLocale from '../../../snippets/format-byte/solid/locale.tsx?raw';

`Format.Byte` formats a byte count as readable text. Import it from the Ark package for your framework.

## API Reference

<PrimitiveReference href="https://ark-ui.com/docs/utilities/format-byte" />

Import `Format` from the matching Ark UI package:

<Tabs
  groupId="framework"
  values={[
    { label: 'React', value: 'react' },
    { label: 'Solid', value: 'solid' },
  ]}
  className="docs-tabs"
>
  <Tab value="react">

```tsx
import { Format } from '@ark-ui/react';
```

  </Tab>
  <Tab value="solid">

```tsx
import { Format } from '@ark-ui/solid';
```

  </Tab>
</Tabs>

## Basic

Pass the number of bytes to `value`. `Format.Byte` renders only the formatted text, so keep it next to a label or
another element that explains what the value represents. The examples use a paragraph for one value and a definition
list when several values need a shared context.

The preview uses the React source because the documentation app is React-based. The Solid tab contains the native
Solid source for the same output and API.

<ExampleFrame variant="component">
  <FormatByteBasic />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/format-byte/basic.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={formatByteSolidBasic} />

    </Tab>
  </Tabs>
</div>
</ExampleCode>

## Decimal and binary systems

Use `unitSystem="decimal"` for multiples of 1000 and `unitSystem="binary"` for multiples of 1024. Choose one
system deliberately when the product distinguishes advertised disk capacity from technical storage values.

<ExampleFrame variant="component">
  <FormatByteUnitSystem />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/format-byte/unit-system.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={formatByteSolidUnitSystem} />

    </Tab>
  </Tabs>
</div>
</ExampleCode>

## Locale

`Format.Byte` reads the locale from the nearest `LocaleProvider`. It does not accept `locale` as a prop. Wrap the
localized part of the interface with the provider from the matching Ark UI package:

<Tabs
  groupId="framework"
  values={[
    { label: 'React', value: 'react' },
    { label: 'Solid', value: 'solid' },
  ]}
  className="docs-tabs"
>
  <Tab value="react">

```tsx pure file="../../../src/components/examples/format-byte/locale.tsx"
```

  </Tab>
  <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={formatByteSolidLocale} />

  </Tab>
</Tabs>

## Options

| Prop | Values | Default | Purpose |
| --- | --- | --- | --- |
| `value` | `number` | required | Byte count to format. |
| `unit` | `'byte'` or `'bit'` | `'byte'` | Unit represented by `value`. |
| `unitDisplay` | `'short'`, `'narrow'`, or `'long'` | `'short'` | How the unit label is written. |
| `unitSystem` | `'decimal'` or `'binary'` | `'decimal'` | Base used to scale the value. |

The current public `FormatByteProps` type does not include `precision`, `locale`, or `sizes`. Use `LocaleProvider` for
locale changes and only the props listed above for component configuration.
