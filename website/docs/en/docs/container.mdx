---
title: Container
component: container
description: A responsive layout primitive for readable content widths and fluid horizontal padding.
---

import * as ContainerDocs from '@/components/examples/container/css-properties';
import ContainerBasic from '@/components/examples/container/basic.tsx';
import ContainerFullWidth from '@/components/examples/container/full-width.tsx';
import ContainerGutters from '@/components/examples/container/gutters.tsx';
import ContainerSemanticElement from '@/components/examples/container/semantic-element.tsx';
import ContainerWithBleed from '@/components/examples/container/with-bleed.tsx';
import containerSolidBasic from '../../../snippets/container/solid/basic.tsx?raw';
import containerSolidFullWidth from '../../../snippets/container/solid/full-width.tsx?raw';
import containerSolidGutters from '../../../snippets/container/solid/gutters.tsx?raw';
import containerSolidSemanticElement from '../../../snippets/container/solid/semantic-element.tsx?raw';
import containerSolidWithBleed from '../../../snippets/container/solid/with-bleed.tsx?raw';

## API Reference

Ark UI does not provide a dedicated `container` primitive. moduix implements `Container` with Ark's official
[factory](https://ark-ui.com/docs/guides/composition#the-ark-factory), so it follows Ark's guidance for
[composition](https://ark-ui.com/docs/guides/composition), [styling](https://ark-ui.com/docs/guides/styling), and
[ref forwarding](https://ark-ui.com/docs/guides/ref).

## Basic

Container ships for React and Solid in both the CSS Modules and Tailwind packages. The rendered preview on this page
uses the React CSS Modules package; the framework tabs show native source for the same public contract.

Import `Container` from the matching package: `@moduix/react/container` or `@moduix/solid/container` for CSS Modules,
and `@moduix/react-tailwind/container` or `@moduix/solid-tailwind/container` for Tailwind CSS.

Use `Container` for a centered content column with a readable width. It adds responsive horizontal gutters and leaves
vertical spacing to the surrounding layout.

<ExampleFrame variant="component">
  <ContainerBasic />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/container/basic.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={containerSolidBasic} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/container/container-basic.module.css"
```
</div>

</ExampleCode>

## Install with shadcn

<Tabs groupId="framework">
  <Tab label="React" value="react">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-react/container' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-react-tailwind/container' },
  ]}
/>

  </Tab>
  <Tab label="Solid" value="solid">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-solid/container' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-solid-tailwind/container' },
  ]}
/>

  </Tab>
</Tabs>

See [Quick start](/docs/quick-start) for full package setup and registry configuration.

## Anatomy

`Container` renders one root element and does not add an extra wrapper.

```text
Container / Container.Root
└─ children
```

| Part                           | Role                                                     |
| ------------------------------ | -------------------------------------------------------- |
| `Container` / `Container.Root` | Root layout element. Centers content and controls its width and horizontal padding. |

## Composition

`Container` has no managed state and no child parts to coordinate. Use it as the root and place the page or section
content directly inside it. `Container.Root` is an alias for the same root when that namespace form fits the rest of
your composition.

Use `asChild` when a semantic element should own the rendered root. Pass exactly one child: React nests that element
inside `Container`, while Solid uses the render-function form `asChild={(props) => <main {...props()} />}`. Use `Bleed`
inside `Container` when only one child needs to extend beyond the constrained text column.

## Examples

### Full Width

Use `size="full"` for a wide section that should use its parent width while keeping a responsive edge gutter.

<ExampleFrame variant="component">
  <ContainerFullWidth />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/container/full-width.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={containerSolidFullWidth} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/container/container-full-width.module.css"
```
</div>

</ExampleCode>

### Gutters

Use `gutter` to choose the amount of responsive horizontal padding.

<ExampleFrame variant="component">
  <ContainerGutters />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/container/gutters.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={containerSolidGutters} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/container/container-gutters.module.css"
```
</div>

</ExampleCode>

### Semantic Element

Use `asChild` when the layout wrapper should also describe document structure.

<ExampleFrame variant="component">
  <ContainerSemanticElement />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/container/semantic-element.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={containerSolidSemanticElement} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/container/container-semantic-element.module.css"
```
</div>

</ExampleCode>

### With Bleed

Keep the text column constrained and let only the wider child extend beyond it.

<ExampleFrame variant="component">
  <ContainerWithBleed />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/container/with-bleed.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={containerSolidWithBleed} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/container/container-with-bleed.module.css"
```
</div>

</ExampleCode>

## Styling

For CSS Modules packages, override the CSS variables on the root when a page shell needs different width or gutter
values. Tailwind packages use utility classes and consumer class overrides instead of these CSS variables. Use
`className` in React or `class` in Solid for local block styles such as padding, borders, or backgrounds.

### CSS Properties

<CssPropertiesSection properties={ContainerDocs.containerOverrideCssProperties} />

These variables apply to the CSS Modules packages. Tailwind packages expose the same `size` and `gutter` choices
through utilities.

### Styling hooks

- `Container` and `Container.Root` expose `data-slot="container-root"` on the root.
- `data-scope="container"` and `data-part="root"` follow Ark's styling conventions.
- `data-size` selects the width preset: `xs`, `sm`, `md`, `lg`, `xl`, `full`.
- `data-gutter` selects the gutter preset: `none`, `sm`, `md`, `lg`.
- `--moduix-container-*` variables control horizontal gutters and max-width math for CSS Modules. Vertical spacing and
  child layout remain the responsibility of the surrounding CSS.
