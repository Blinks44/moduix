---
title: Split Button
component: split-button
description: A grouped primary action with a menu of related actions, built from moduix Button and Menu parts.
---

import SplitButtonBasic from '@/components/examples/split-button/basic.tsx';
import SplitButtonControlledMenuState from '@/components/examples/split-button/controlled-menu-state.tsx';
import SplitButtonDisabledAction from '@/components/examples/split-button/disabled-action.tsx';
import SplitButtonLinkAction from '@/components/examples/split-button/link-action.tsx';
import SplitButtonMenuComposition from '@/components/examples/split-button/menu-composition.tsx';
import SplitButtonPositioning from '@/components/examples/split-button/positioning.tsx';
import SplitButtonSizes from '@/components/examples/split-button/sizes.tsx';
import SplitButtonVariants from '@/components/examples/split-button/variants.tsx';
import SplitButtonWithIcons from '@/components/examples/split-button/with-icons.tsx';
import * as SplitButtonDocs from '@/components/examples/split-button/css-properties';
import splitButtonSolidBasic from '../../../snippets/split-button/solid/basic.tsx?raw';
import splitButtonSolidControlledMenuState from '../../../snippets/split-button/solid/controlled-menu-state.tsx?raw';
import splitButtonSolidDisabledAction from '../../../snippets/split-button/solid/disabled-action.tsx?raw';
import splitButtonSolidLinkAction from '../../../snippets/split-button/solid/link-action.tsx?raw';
import splitButtonSolidMenuComposition from '../../../snippets/split-button/solid/menu-composition.tsx?raw';
import splitButtonSolidPositioning from '../../../snippets/split-button/solid/positioning.tsx?raw';
import splitButtonSolidSizes from '../../../snippets/split-button/solid/sizes.tsx?raw';
import splitButtonSolidVariants from '../../../snippets/split-button/solid/variants.tsx?raw';
import splitButtonSolidWithIcons from '../../../snippets/split-button/solid/with-icons.tsx?raw';

## API Reference

Ark UI does not provide a dedicated split-button primitive. moduix composes this component from the
[Ark factory](https://ark-ui.com/docs/guides/composition#the-ark-factory) and preserves the behavior of
[Ark Menu](https://ark-ui.com/docs/components/menu).

## Choosing the right component

Use `SplitButton` when one action should stay immediately available and related alternatives belong in a menu.
Use `Button` for a single action. Use `Menu` when the available actions are peers and none of them should be primary.

## Basic


Use the short `<SplitButton>` form, which is equivalent to `<SplitButton.Root>`, and keep the menu structure explicit
with `SplitButton.Action`, `SplitButton.Trigger`, `SplitButton.Positioner`, and `SplitButton.Content`.
The primary action follows the same `onClick` contract as `Button`.

<ExampleFrame variant="component">
  <SplitButtonBasic />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/split-button/basic.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={splitButtonSolidBasic} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/split-button/split-button-basic.module.css"
```
</div>

</ExampleCode>

## Install with shadcn

See [Quick Start](/docs/quick-start) for full package setup, registry configuration, and styling guidance.

<Tabs groupId="framework">
  <Tab label="React" value="react">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-react/split-button' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-react-tailwind/split-button' },
  ]}
/>

  </Tab>
  <Tab label="Solid" value="solid">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-solid/split-button' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-solid-tailwind/split-button' },
  ]}
/>

  </Tab>
</Tabs>

## Anatomy

```text
SplitButton / SplitButton.Root
├─ SplitButton.Action
├─ SplitButton.Trigger
└─ Popup subtree (automatically portalled)
   └─ SplitButton.Positioner
      └─ SplitButton.Content
         ├─ Menu.Item
         ├─ Menu.Separator
         └─ other shared Menu parts
```

| Part                               | Role                                                                  |
| ---------------------------------- | --------------------------------------------------------------------- |
| `SplitButton` / `SplitButton.Root` | Root that owns menu state and the semantic group around both buttons. |
| `SplitButton.Action`               | Primary Button action.                                                |
| `SplitButton.Trigger`              | Ark Menu trigger rendered as a moduix Button with a chevron by default. |
| `SplitButton.Positioner`           | Positions the Ark Menu popup.                                         |
| `SplitButton.Content`              | Ark Menu content surface for shared `Menu.*` parts.                   |

## Composition

`SplitButton` combines one primary `Action` with a `Trigger`; keep the popup tree explicit as
`Positioner` → `Content` and place shared `Menu.*` rows inside `Content`. The positioner is portalled automatically;
use `portalled={false}` or `portalRef` only when the popup must use a different container. Root `size` and `variant`
provide the defaults for both button parts. The root renders a semantic `role="group"`; provide `aria-label` or
`aria-labelledby` when the surrounding context does not already identify the group.

Menu state is internal by default. Pass `open` with `onOpenChange(details)` when another part of the application
owns the open state. For advanced state access, use the shared `Menu` API instead of adding a split-button-specific
state wrapper. Use `Action asChild` for a link; custom trigger content goes through `Trigger` `children` so Ark's
menu trigger and the moduix Button remain on one element.

## Examples

### Controlled Menu State

Use the same controlled open-state contract as Ark Menu when the dropdown state should be managed outside the
component.

<ExampleFrame variant="component">
  <SplitButtonControlledMenuState />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/split-button/controlled-menu-state.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={splitButtonSolidControlledMenuState} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Disabled Action

Disable the primary action while keeping the alternate-actions menu available when it still offers a useful path.

<ExampleFrame variant="component">
  <SplitButtonDisabledAction />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/split-button/disabled-action.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={splitButtonSolidDisabledAction} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Link Action

Use `asChild` on `SplitButton.Action` when the primary action should render as a link.

<ExampleFrame variant="component">
  <SplitButtonLinkAction />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/split-button/link-action.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={splitButtonSolidLinkAction} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Positioning

Configure popup placement with the Ark `positioning` root prop while keeping the popup parts explicit.

<ExampleFrame variant="component">
  <SplitButtonPositioning />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/split-button/positioning.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={splitButtonSolidPositioning} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Sizes

Use the root `size` when both sides should scale together.

<ExampleFrame variant="component">
  <SplitButtonSizes />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/split-button/sizes.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={splitButtonSolidSizes} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/split-button/split-button-sizes.module.css"
```
</div>

</ExampleCode>

### Variants

Use the root `variant` when both buttons should share the same visual style.

<ExampleFrame variant="component">
  <SplitButtonVariants />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/split-button/variants.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={splitButtonSolidVariants} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/split-button/split-button-variants.module.css"
```
</div>

</ExampleCode>

### With Icons

Add an icon to the primary action when it clarifies the default action without replacing the text label.

<ExampleFrame variant="component">
  <SplitButtonWithIcons />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/split-button/with-icons.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={splitButtonSolidWithIcons} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Advanced Customization

Use shared `Menu.*` parts inside `SplitButton.Content` when alternate actions need groups, separators, or grouped
rows.

<ExampleFrame variant="component">
  <SplitButtonMenuComposition />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/split-button/menu-composition.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={splitButtonSolidMenuComposition} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

## Styling

Use `className` in React or `class` in Solid on exported parts for structural styles. Tailwind variants use native
utilities and consumer classes; the CSS variables below customize the CSS Modules variants. Data attributes and
`data-slot` hooks are shared by both styling tracks.

The joined appearance comes from the shared `Button` and `Menu` styles plus SplitButton slots. The separator and
trigger padding use the `--moduix-split-button-separator-*` and `--moduix-split-button-trigger-padding-x-*` variables.

### CSS Properties

<CssPropertiesSection properties={SplitButtonDocs.splitButtonCssProperties} />

### Styling hooks

- `data-scope="split-button"` and `data-part="root"` target the semantic group.
- `data-slot="split-button-root"` targets the group wrapper.
- `data-slot="split-button-action"` targets the primary action.
- `data-slot="split-button-trigger"` targets the menu trigger.
- `data-slot="split-button-positioner"` targets the Ark Menu positioner.
- `data-slot="split-button-content"` targets the Ark Menu content surface.
- `SplitButton.Trigger` carries Ark Menu state attributes such as `aria-expanded`, `aria-controls`, and `data-state`.
- `SplitButton.Content` carries Ark Menu attributes such as `data-state`, `data-side`, `data-placement`, `data-nested`,
  and `data-has-nested`.
