---
title: Format Time
description: Format clock times for a specific locale in React or Solid with Ark UI Format.Time.
---

import FormatTimeBasic from '@/components/examples/format-time/basic.tsx';
import FormatTimeSecondsAndLabels from '@/components/examples/format-time/seconds-and-labels.tsx';
import FormatTimeLocale from '@/components/examples/format-time/locale.tsx';
import formatTimeSolidBasic from '../../../snippets/format-time/solid/basic.tsx?raw';
import formatTimeSolidSecondsAndLabels from '../../../snippets/format-time/solid/seconds-and-labels.tsx?raw';
import formatTimeSolidLocale from '../../../snippets/format-time/solid/locale.tsx?raw';

`Format.Time` turns a clock value into locale-aware text. The same props work in every shipped framework adapter.

## API Reference

<PrimitiveReference href="https://ark-ui.com/docs/utilities/format-time" />

Import the utility from the matching Ark UI package:

<Tabs
  groupId="framework"
  values={[
    { label: 'React', value: 'react' },
    { label: 'Solid', value: 'solid' },
  ]}
  className="docs-tabs"
>
  <Tab value="react">

```tsx
import { Format } from '@ark-ui/react';
```

  </Tab>
  <Tab value="solid">

```tsx
import { Format } from '@ark-ui/solid';
```

  </Tab>
</Tabs>

## Basic

Pass a string in `HH:mm` or `HH:mm:ss` form for a wall-clock value such as a scheduled departure. A string is not
converted between time zones.

<ExampleFrame variant="component">
  <PreviewFrame maxWidth="sm">
    <FormatTimeBasic />
  </PreviewFrame>
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/format-time/basic.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={formatTimeSolidBasic} />

    </Tab>
  </Tabs>
</div>
</ExampleCode>

## Formatting options

The default format is 24-hour. Set `format="12h"` for a 12-hour clock, `withSeconds` to include seconds, and
`amLabel` or `pmLabel` to replace the day-period labels in 12-hour output.

<ExampleFrame variant="component">
  <PreviewFrame maxWidth="sm">
    <FormatTimeSecondsAndLabels />
  </PreviewFrame>
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/format-time/seconds-and-labels.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={formatTimeSolidSecondsAndLabels} />

    </Tab>
  </Tabs>
</div>
</ExampleCode>

Use seconds only when they help people act on the information. Do not add them to a schedule just because the source
data contains them.

## Locale and Date values

`Format.Time` reads the locale from the nearest `LocaleProvider`. Without one, Ark UI uses `en-US`. Use a `Date` when
the value represents a specific moment, such as an API timestamp. Its displayed clock time uses the runtime's local
time zone. `LocaleProvider` changes the locale, not the time zone.

Keep scheduled wall-clock times as strings. Pass a `Date` only when the time-zone conversion is intended.

<ExampleFrame variant="component">
  <PreviewFrame maxWidth="sm">
    <FormatTimeLocale />
  </PreviewFrame>
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/format-time/locale.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={formatTimeSolidLocale} />

    </Tab>
  </Tabs>
</div>
</ExampleCode>
