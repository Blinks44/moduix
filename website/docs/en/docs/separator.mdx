---
title: Separator
component: separator
description: Separates related content or groups with an accessible horizontal or vertical line.
---

import * as SeparatorDocs from '@/components/examples/separator/css-properties';
import SeparatorBasic from '@/components/examples/separator/basic.tsx';
import SeparatorDecorative from '@/components/examples/separator/decorative.tsx';
import SeparatorSizes from '@/components/examples/separator/sizes.tsx';
import SeparatorVariants from '@/components/examples/separator/variants.tsx';
import SeparatorVertical from '@/components/examples/separator/vertical.tsx';
import SeparatorAsChild from '@/components/examples/separator/as-child.tsx';
import separatorSolidBasic from '../../../snippets/separator/solid/basic.tsx?raw';
import separatorSolidDecorative from '../../../snippets/separator/solid/decorative.tsx?raw';
import separatorSolidSizes from '../../../snippets/separator/solid/sizes.tsx?raw';
import separatorSolidVariants from '../../../snippets/separator/solid/variants.tsx?raw';
import separatorSolidVertical from '../../../snippets/separator/solid/vertical.tsx?raw';
import separatorSolidAsChild from '../../../snippets/separator/solid/as-child.tsx?raw';

## API Reference

Ark UI does not ship a dedicated separator primitive. moduix builds this component with the official
[Ark factory](https://ark-ui.com/docs/guides/composition#the-ark-factory) and follows Ark's guidance on
[composition](https://ark-ui.com/docs/guides/composition), [styling](https://ark-ui.com/docs/guides/styling), and
[refs](https://ark-ui.com/docs/guides/ref).

The `orientation`, `variant`, and `size` props follow the familiar Chakra UI Separator model. See the
[Chakra UI Separator docs](https://chakra-ui.com/docs/components/separator) for the upstream reference
that informed this API.

## Basic


Use `Separator` to make the boundary between related sections visible. The defaults are
`orientation="horizontal"`, `variant="solid"`, and `size="sm"`.

<ExampleFrame variant="component">
  <SeparatorBasic />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/separator/basic.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={separatorSolidBasic} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/separator/separator-basic.module.css"
```
</div>

</ExampleCode>

## Install with shadcn

See [Quick Start](/docs/quick-start) for full package setup, registry configuration, and styling guidance.

<Tabs groupId="framework">
  <Tab label="React" value="react">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-react/separator' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-react-tailwind/separator' },
  ]}
/>

  </Tab>
  <Tab label="Solid" value="solid">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-solid/separator' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-solid-tailwind/separator' },
  ]}
/>

  </Tab>
</Tabs>

## Anatomy

`Separator` exposes one root part.

```text
Separator / Separator.Root
└─ root[data-scope="separator"][data-part="root"][data-slot="separator-root"]
```

| Part                               | Role                                                                       |
| ---------------------------------- | -------------------------------------------------------------------------- |
| `Separator` / `Separator.Root` | Root divider element. Sets orientation, accessibility metadata, and style. |

## Composition

`Separator` is a root-only component, so it does not own state or require a provider. Use
`<Separator />` for the usual divider; it is the same root as `<Separator.Root />`. When another
element must host the divider, use `asChild` with one child that accepts the merged role, ARIA,
`data-*`, class, and ref props. React passes the child element directly, while Solid uses
`asChild={(props) => <hr {...props()} />}`.

## Examples

### Decorative

Use `role="presentation"` for a line that is only visual and carries no structural meaning for
assistive technology.

<ExampleFrame variant="component">
  <SeparatorDecorative />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/separator/decorative.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={separatorSolidDecorative} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/separator/separator-decorative.module.css"
```
</div>

</ExampleCode>

### Sizes

Use `size` to choose the separator thickness, then override it with CSS properties when needed.

<ExampleFrame variant="component">
  <SeparatorSizes />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/separator/sizes.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={separatorSolidSizes} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/separator/separator-sizes.module.css"
```
</div>

</ExampleCode>

### Variants

Use `variant` to choose a solid, dashed, or dotted line.

<ExampleFrame variant="component">
  <SeparatorVariants />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/separator/variants.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={separatorSolidVariants} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/separator/separator-variants.module.css"
```
</div>

</ExampleCode>

### Vertical

Use `orientation="vertical"` for inline groups such as navigation links.

<ExampleFrame variant="component">
  <SeparatorVertical />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/separator/vertical.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={separatorSolidVertical} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/separator/separator-vertical.module.css"
```
</div>

</ExampleCode>

### Advanced Customization

Use `asChild` when a specific host element needs to receive the separator role, styling hooks, and
forwarded ref. Otherwise, use `<Separator />` directly.

<ExampleFrame variant="component">
  <SeparatorAsChild />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/separator/as-child.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={separatorSolidAsChild} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/separator/separator-as-child.module.css"
```
</div>

</ExampleCode>

## Styling

Use `className` in React or `class` in Solid on the root. Tailwind variants use native utility
classes and consumer classes; the CSS properties below customize the CSS Modules variants. Data
attributes and `data-slot` hooks are shared by both styling tracks.

### CSS Properties

<div className="docs-reference">
  <Tabs
    values={[{ label: 'CSS Variables', value: 'CSS Variables' }]}
    className="docs-reference-tabs"
    contentItemClassName="docs-reference-panel"
  >
    <Tab>
      <SeparatorDocs.SeparatorCssPropertiesPanel />
    </Tab>
  </Tabs>
</div>

### Styling hooks

- `data-scope="separator"` and `data-part="root"` identify the Ark-style root part.
- `data-slot="separator-root"` is the stable moduix selector.
- `data-orientation="horizontal"` and `data-orientation="vertical"` target axis-specific styles.
- `data-size="xs|sm|md|lg"` and `data-variant="solid|dashed|dotted"` expose the visual settings.
- `className` or `class` and `style` are applied directly to the root separator element.
