---
title: Bleed
component: bleed
description: Layout primitive that lets selected content extend beyond a constrained container.
---

import * as BleedDocs from '@/components/examples/bleed/css-properties';
import BleedBasic from '@/components/examples/bleed/basic.tsx';
import BleedBlockBleed from '@/components/examples/bleed/block-bleed.tsx';
import BleedCustomStyling from '@/components/examples/bleed/custom-styling.tsx';
import BleedInlineAmounts from '@/components/examples/bleed/inline-amounts.tsx';
import BleedSemanticElement from '@/components/examples/bleed/semantic-element.tsx';
import bleedSolidBasic from '../../../snippets/bleed/solid/basic.tsx?raw';
import bleedSolidBlockBleed from '../../../snippets/bleed/solid/block-bleed.tsx?raw';
import bleedSolidCustomStyling from '../../../snippets/bleed/solid/custom-styling.tsx?raw';
import bleedSolidInlineAmounts from '../../../snippets/bleed/solid/inline-amounts.tsx?raw';
import bleedSolidSemanticElement from '../../../snippets/bleed/solid/semantic-element.tsx?raw';

## API Reference

Ark UI does not include a dedicated `bleed` primitive. moduix builds `Bleed` with the official
[Ark factory](https://ark-ui.com/docs/guides/composition#the-ark-factory) and follows Ark's guidance on
[composition](https://ark-ui.com/docs/guides/composition), [styling](https://ark-ui.com/docs/guides/styling), and
[refs](https://ark-ui.com/docs/guides/ref).

Use [Container](/docs/container) to keep the main content at a readable width. Put `Bleed` inside it when
only a media block, background, divider, or banner should extend beyond the container.

Import `Bleed` from the matching runtime and styling package: `@moduix/react/bleed` or
`@moduix/solid/bleed` for CSS Modules, and the corresponding `@moduix/react-tailwind/bleed` or
`@moduix/solid-tailwind/bleed` subpath for Tailwind.

## Basic


The default `inline="full"` makes media, banners, and section decorations reach the viewport edges while
remaining in the normal document flow.

<ExampleFrame variant="component">
  <BleedBasic />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/bleed/basic.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={bleedSolidBasic} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/bleed/bleed-basic.module.css"
```
</div>

</ExampleCode>

## Install with shadcn

See [Quick Start](/docs/quick-start) for full package setup, registry configuration, and styling guidance.

<Tabs groupId="framework">
  <Tab label="React" value="react">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-react/bleed' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-react-tailwind/bleed' },
  ]}
/>

  </Tab>
  <Tab label="Solid" value="solid">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-solid/bleed' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-solid-tailwind/bleed' },
  ]}
/>

  </Tab>
</Tabs>

## Anatomy

`Bleed` renders one root element that follows Ark's conventions. It uses negative margins on the inline
and/or block axis, without adding an inner wrapper.

```text
Bleed
└─ root[data-scope="bleed"][data-part="root"][data-slot="bleed-root"]
   └─ children
```

| Part                   | Role                                                                   |
| ---------------------- | ---------------------------------------------------------------------- |
| `Bleed`                | Root component for extending content along the inline and block axes. |

## Composition

`Bleed` is presentational and owns no state. It has one root part, so there are no child parts to coordinate.
The defaults are `inline="full"`, `block="none"`, and `asChild={false}`.
When `asChild` should give the rendered root to one semantic element, provide exactly one child that accepts DOM
props and a ref in React, or use the Solid render-function form. React refs target the rendered root or child;
the Solid form does not forward the component ref through the custom host.

Combine `inline` and `block` when a surface should extend through padding on both logical axes. `inline="full"`
is viewport-relative, so a nested shell may need local `--moduix-bleed-inline-full` and
`--moduix-bleed-inline-full-size` overrides.

## Examples

### Block Bleed

Add `block` when a surface should extend through the container's block-axis padding as well as its inline-axis padding.

<ExampleFrame variant="component">
  <BleedBlockBleed />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/bleed/block-bleed.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={bleedSolidBlockBleed} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/bleed/bleed-block-bleed.module.css"
```
</div>

</ExampleCode>

### Custom Styling

Override `--moduix-bleed-inline-full` and `--moduix-bleed-inline-full-size` locally when a nested shell
should size the bleed to its own content area instead of the viewport.

<ExampleFrame variant="component">
  <BleedCustomStyling />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/bleed/custom-styling.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={bleedSolidCustomStyling} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/bleed/bleed-custom-styling.module.css"
```
</div>

</ExampleCode>

### Inline Amounts

Set `inline` to control how far content extends along the inline axis. This example compares the fixed
`sm` and `lg` values with `full`, which reaches the viewport.

<ExampleFrame variant="component">
  <BleedInlineAmounts />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/bleed/inline-amounts.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={bleedSolidInlineAmounts} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/bleed/bleed-inline-amounts.module.css"
```
</div>

</ExampleCode>

### Semantic Element

Use `asChild` when the content should use a more specific semantic element. In this example, the `figure`
element becomes the `Bleed` root.

<ExampleFrame variant="component">
  <BleedSemanticElement />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/bleed/semantic-element.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={bleedSolidSemanticElement} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/bleed/bleed-semantic-element.module.css"
```
</div>

</ExampleCode>

## Styling

`Bleed` exposes CSS variables and stable data attributes on its root. Use `className` in React or `class` in
Solid for structural styles. Tailwind variants use native utilities and consumer classes; the variables below
customize the CSS Modules variants. Data attributes and `data-slot` hooks are shared by both styling tracks.
Ancestors with `overflow: hidden` or `overflow: clip` can crop the bleed.

### CSS Properties

<CssPropertiesSection properties={BleedDocs.bleedCssProperties} />

### Styling hooks

- `data-scope="bleed"` and `data-part="root"` provide Ark-aligned hooks on the exported root.
- `data-slot="bleed-root"` is the stable moduix selector on the exported root.
- `data-inline` shows the current inline bleed mode: `none`, `xs`, `sm`, `md`, `lg`, `xl`, or `full`.
- `data-block` shows the current block bleed mode: `none`, `xs`, `sm`, `md`, `lg`, or `xl`.
- The public `--moduix-bleed-*` CSS API covers both scale-based bleed amounts and the viewport sizing
  used by `inline="full"`.
