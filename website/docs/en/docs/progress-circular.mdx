---
title: Progress Circular
component: progress-circular
description: A circular progress indicator for determinate or indeterminate tasks.
---

import * as ProgressCircularDocs from '@/components/examples/progress-circular/css-properties';
import ProgressCircularBasic from '@/components/examples/progress-circular/basic.tsx';
import CirclePartsProgressCircular from '@/components/examples/progress-circular/circle-parts.tsx';
import ControlledProgressCircular from '@/components/examples/progress-circular/controlled.tsx';
import IndeterminateProgressCircular from '@/components/examples/progress-circular/indeterminate.tsx';
import MinMaxProgressCircular from '@/components/examples/progress-circular/min-max.tsx';
import RootProviderProgressCircular from '@/components/examples/progress-circular/root-provider.tsx';
import StateViewsProgressCircular from '@/components/examples/progress-circular/state-views.tsx';
import ValueTextProgressCircular from '@/components/examples/progress-circular/value-text.tsx';
import progressCircularSolidBasic from '../../../snippets/progress-circular/solid/basic.tsx?raw';
import progressCircularSolidCircleParts from '../../../snippets/progress-circular/solid/circle-parts.tsx?raw';
import progressCircularSolidControlled from '../../../snippets/progress-circular/solid/controlled.tsx?raw';
import progressCircularSolidIndeterminate from '../../../snippets/progress-circular/solid/indeterminate.tsx?raw';
import progressCircularSolidMinMax from '../../../snippets/progress-circular/solid/min-max.tsx?raw';
import progressCircularSolidRootProvider from '../../../snippets/progress-circular/solid/root-provider.tsx?raw';
import progressCircularSolidStateViews from '../../../snippets/progress-circular/solid/state-views.tsx?raw';
import progressCircularSolidValueText from '../../../snippets/progress-circular/solid/value-text.tsx?raw';

## API Reference

<PrimitiveReference href="https://ark-ui.com/docs/components/progress-circular" />

## Choosing the right component

Use `ProgressCircular` for compact progress feedback in inline content, cards, toolbar badges, and
small dashboards. Use `ProgressLinear` when progress is easier to compare on a horizontal track.

## Basic


Use `ProgressCircular.Ring` to render the standard circle with its track and range. Add
`ProgressCircular.ValueText` to show the current value. The example wraps the circle and value text
so the value appears in the center. Give `Ring` an `aria-label` that names the task and matches the
visible label.

<ExampleFrame variant="component">
  <ProgressCircularBasic />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/progress-circular/basic.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={progressCircularSolidBasic} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/progress-circular/component-basic.module.css"
```
</div>

</ExampleCode>

## Install with shadcn

See [Quick Start](/docs/quick-start) for full package setup, registry configuration, and styling guidance.

<Tabs groupId="framework">
  <Tab label="React" value="react">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-react/progress-circular' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-react-tailwind/progress-circular' },
  ]}
/>

  </Tab>
  <Tab label="Solid" value="solid">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-solid/progress-circular' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-solid-tailwind/progress-circular' },
  ]}
/>

  </Tab>
</Tabs>

## Anatomy

```text
ProgressCircular / ProgressCircular.Root
├─ ProgressCircular.Label
├─ ProgressCircular.Circle
│  ├─ ProgressCircular.CircleTrack
│  └─ ProgressCircular.CircleRange
├─ ProgressCircular.Ring
│  └─ fixed Circle, CircleTrack, and CircleRange subtree
├─ ProgressCircular.ValueText
└─ ProgressCircular.View

ProgressCircular.RootProvider
└─ same part tree connected to ProgressCircular.useProgress()
```

| Part                                         | Role                                                                                       |
| -------------------------------------------- | ------------------------------------------------------------------------------------------ |
| `ProgressCircular` / `ProgressCircular.Root` | Owns progress state, formatting, and generated ids.                                        |
| `ProgressCircular.RootProvider`              | Connects parts to state created with `ProgressCircular.useProgress()`.                     |
| `ProgressCircular.Context`                   | Exposes the current progress state through a render prop.                                  |
| `ProgressCircular.Label`                     | Provides visible context for the progress indicator.                                       |
| `ProgressCircular.Ring`                      | Convenience part that renders the default circle, track, and range.                       |
| `ProgressCircular.Circle`                    | SVG progressbar element with Ark's role and ARIA value attributes.                        |
| `ProgressCircular.CircleTrack`               | Background circle.                                                                         |
| `ProgressCircular.CircleRange`               | Foreground circle that reflects the current progress or indeterminate state.               |
| `ProgressCircular.ValueText`                 | Displays the formatted value and announces updates.                                       |
| `ProgressCircular.View`                      | Renders content for `indeterminate`, `loading`, or `complete`.                             |

## Composition

`ProgressCircular` is shorthand for `ProgressCircular.Root`. Use `ProgressCircular.Ring` when the
standard circle is enough: it combines `Circle`, `CircleTrack`, and `CircleRange`, but it does not
render or center `ValueText`. Keep the label, circle, and value text composition explicit when the
value should appear in the center.

Use `defaultValue` when the component owns progress state. Use `value` with
`onValueChange(details)` when the application owns it. Set `defaultValue={null}` or `value={null}`
for indeterminate progress. When state must be created outside the rendered tree, create it with
`ProgressCircular.useProgress()` and pass the store to `ProgressCircular.RootProvider` instead of
rendering another root for the same store.

## Examples

### Controlled

Keep `value` in application state when progress needs to stay synchronized with another control or
an external task.

<ExampleFrame variant="component">
  <ControlledProgressCircular />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/progress-circular/controlled.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={progressCircularSolidControlled} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/progress-circular/component-controlled.module.css"
```
</div>

</ExampleCode>

### Indeterminate

Set `defaultValue={null}` or `value={null}` when the task is running but its progress cannot be
measured yet.

<ExampleFrame variant="component">
  <IndeterminateProgressCircular />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/progress-circular/indeterminate.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={progressCircularSolidIndeterminate} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/progress-circular/component-indeterminate.module.css"
```
</div>

</ExampleCode>

### Min and max

Use `min` and `max` to map values to a range other than the default `0` to `100`.

<ExampleFrame variant="component">
  <MinMaxProgressCircular />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/progress-circular/min-max.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={progressCircularSolidMinMax} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/progress-circular/component-min-max.module.css"
```
</div>

</ExampleCode>

### Value text

Use `Context` with `translations.value(details)` when visible value text needs task-specific wording.
Render `progress.valueAsString` as the `ValueText` child so the visible text matches the accessible
value string.

<ExampleFrame variant="component">
  <ValueTextProgressCircular />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/progress-circular/value-text.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={progressCircularSolidValueText} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Advanced customization

`ProgressCircular.Ring` is the quick path. Compose `Circle`, `CircleTrack`, and `CircleRange`
directly when the SVG tree or state ownership needs to be customized. Only direct composition can
give `CircleTrack` and `CircleRange` their own classes, as in this example.

<ExampleFrame variant="component">
  <CirclePartsProgressCircular />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/progress-circular/circle-parts.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={progressCircularSolidCircleParts} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/progress-circular/component-circle-parts.module.css"
```
</div>

</ExampleCode>

#### RootProvider

Create the store with `ProgressCircular.useProgress()` and pass it to `RootProvider` when another
component needs to read or update progress outside the rendered tree.

<ExampleFrame variant="component">
  <RootProviderProgressCircular />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/progress-circular/root-provider.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={progressCircularSolidRootProvider} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/progress-circular/component-root-provider.module.css"
```
</div>

</ExampleCode>

#### State views

Use `ProgressCircular.View` when supporting text or UI should change with the Ark progress state.

<ExampleFrame variant="component">
  <StateViewsProgressCircular />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/progress-circular/state-views.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={progressCircularSolidStateViews} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/progress-circular/component-state-views.module.css"
```
</div>

</ExampleCode>

## Styling

Use `className` in React and `class` in Solid on exported parts for structural styling. Tailwind
variants use native utility classes and consumer class overrides. CSS Modules variants expose the
CSS variables below. For state-based styles, use Ark data attributes, Ark SVG variables, and moduix
`data-slot` hooks.

Use `--moduix-progress-circular-range-indeterminate-bg` to give indeterminate progress a different
semantic color from determinate progress. When reduced motion is preferred, the range neither
transitions nor spins. Use `--moduix-progress-circular-range-indeterminate-reduced-motion-dasharray`
to keep a visible static cue.

### CSS properties

<div className="docs-reference">
  <Tabs
    values={[{ label: 'CSS Variables', value: 'CSS Variables' }]}
    className="docs-reference-tabs"
    contentItemClassName="docs-reference-panel"
  >
    <Tab>
      <ProgressCircularDocs.ProgressCircularCssPropertiesPanel />
    </Tab>
  </Tabs>
</div>

### Styling hooks

- `data-slot="progress-circular-root"`, `data-slot="progress-circular-root-provider"`,
  `data-slot="progress-circular-label"`, `data-slot="progress-circular-circle"`,
  `data-slot="progress-circular-circle-track"`, `data-slot="progress-circular-circle-range"`,
  `data-slot="progress-circular-value-text"`, and `data-slot="progress-circular-view"`.
- `ProgressCircular.Ring` shares `ProgressCircular.Circle`'s `data-slot`, `className`, ref, and
  `--moduix-progress-circular-*` size and thickness variables.
- Ark hooks: `data-scope="progress"`, `data-part`, `data-state="indeterminate|loading|complete"`,
  `data-orientation`, `data-value`, and `data-max`.
- Ark circle variables: `--size`, `--thickness`, `--radius`, `--circumference`, `--percent`, and
  `--offset`.
