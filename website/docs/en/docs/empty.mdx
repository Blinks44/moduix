---
title: Empty
component: empty
description: A composable empty-state component for no-results, no-data, and first-run screens.
---

import * as EmptyDocs from '@/components/examples/empty/css-properties';
import EmptyAdvancedCustomization from '@/components/examples/empty/advanced-customization.tsx';
import EmptyBasic from '@/components/examples/empty/basic.tsx';
import EmptyWithoutActions from '@/components/examples/empty/without-actions.tsx';
import EmptyWithoutIcon from '@/components/examples/empty/without-icon.tsx';
import emptySolidAdvancedCustomization from '../../../snippets/empty/solid/advanced-customization.tsx?raw';
import emptySolidBasic from '../../../snippets/empty/solid/basic.tsx?raw';
import emptySolidWithoutActions from '../../../snippets/empty/solid/without-actions.tsx?raw';
import emptySolidWithoutIcon from '../../../snippets/empty/solid/without-icon.tsx?raw';

## API Reference

Ark UI does not include a dedicated empty-state primitive. `moduix` builds `Empty` with Ark's
official [factory](https://ark-ui.com/react/docs/guides/composition#the-ark-factory) and follows
Ark's guidance for [composition](https://ark-ui.com/react/docs/guides/composition),
[styling](https://ark-ui.com/react/docs/guides/styling), and
[refs](https://ark-ui.com/react/docs/guides/ref). Its composition also draws on Chakra UI's
[Empty State](https://chakra-ui.com/docs/components/empty-state). Unlike Chakra UI, `moduix`
exposes optional `Empty.Icon` and `Empty.Actions` as root-level parts alongside `Empty.Content`,
so you can arrange them in the order your screen needs.

## Basic


Start with `<Empty />` and add its namespaced parts. Add `Empty.Icon` and `Empty.Actions` only when
the state benefits from a visual cue or a clear next step.

<ExampleFrame variant="component">
  <EmptyBasic />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/empty/basic.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={emptySolidBasic} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/empty/empty-basic.module.css"
```
</div>

</ExampleCode>

## Install with shadcn

See [Quick Start](/docs/quick-start) for full package setup, registry configuration, and styling guidance.

<Tabs groupId="framework">
  <Tab label="React" value="react">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-react/empty' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-react-tailwind/empty' },
  ]}
/>

  </Tab>
  <Tab label="Solid" value="solid">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-solid/empty' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-solid-tailwind/empty' },
  ]}
/>

  </Tab>
</Tabs>

## Anatomy

```text
Empty / Empty.Root
├─ Empty.Icon (optional)
├─ Empty.Content
│  ├─ Empty.Title
│  └─ Empty.Description
└─ Empty.Actions (optional)
```

| Part                   | Role                                                                                          |
| ---------------------- | --------------------------------------------------------------------------------------------- |
| `Empty` / `Empty.Root` | Root container for padding, border, background, and alignment.                                |
| `Empty.Icon`           | Optional container for an icon or another small visual cue.                                   |
| `Empty.Content`        | Groups the title and description with tighter spacing.                                        |
| `Empty.Title`          | Main heading for the empty state.                                                             |
| `Empty.Description`    | Supporting content below the title. It can contain paragraphs, links, or other block content. |
| `Empty.Actions`        | Optional action row that wraps its children when needed.                                      |

## Composition

Compose `Empty` from its root-level parts. `Empty.Content` groups the title and description;
`Empty.Icon` and `Empty.Actions` are optional siblings, so you can omit or reorder them to match
the screen. `Empty` is presentational and does not own application state or live-region behavior.

Use `asChild` on `Empty` or `Empty.Title` when the surrounding section needs a semantic container or
a different heading level. Pass exactly one child to an `asChild` part so it can receive that part's
props and ref in React. In Solid, assign a ref inside the `asChild` render function. Then add any
required landmark, role, focus management, or `aria-live` behavior in the surrounding interface.

## Examples

### Without actions

Leave out `Empty.Actions` when the state is informational and does not need an immediate action.

<ExampleFrame variant="component">
  <EmptyWithoutActions />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/empty/without-actions.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={emptySolidWithoutActions} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/empty/empty-without-actions.module.css"
```
</div>

</ExampleCode>

### Without an icon

The icon is optional, so a text-only empty state can stay compact.

<ExampleFrame variant="component">
  <EmptyWithoutIcon />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/empty/without-icon.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={emptySolidWithoutIcon} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/empty/empty-without-icon.module.css"
```
</div>

</ExampleCode>

### Advanced customization

Use the `asChild` prop on `Empty` when the empty state needs to render as a semantic container such
as `section`. Use it on `Empty.Title` when the heading needs a different level. Because
`Empty.Description` renders as a `div`, it can contain paragraphs, links, and other block content.

<ExampleFrame variant="component">
  <EmptyAdvancedCustomization />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/empty/advanced-customization.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={emptySolidAdvancedCustomization} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/empty/empty-advanced-customization.module.css"
```
</div>

</ExampleCode>

## Styling

Customize `Empty` with `className`, Ark `data-scope="empty"` / `data-part` selectors, stable
`data-slot="empty-*"` hooks, and the public `--moduix-empty-*` CSS variables shown below.

### CSS Properties

<div className="docs-reference">
  <Tabs
    values={[{ label: 'CSS Variables', value: 'CSS Variables' }]}
    className="docs-reference-tabs"
    contentItemClassName="docs-reference-panel"
  >
    <Tab>
      <EmptyDocs.EmptyCssPropertiesPanel />
    </Tab>
  </Tabs>
</div>

### Styling hooks

Target the part that controls the visual property you want to change:

- `[data-scope="empty"][data-part="root"]` or `[data-slot="empty-root"]` for container layout,
  border, background, and shadow overrides
- `[data-scope="empty"][data-part="icon"]` or `[data-slot="empty-icon"]` for icon container
  size, padding, and colors
- `[data-scope="empty"][data-part="content"]` or `[data-slot="empty-content"]` for text block
  width and spacing
- `[data-scope="empty"][data-part="title"]` or `[data-slot="empty-title"]`, and
  `[data-scope="empty"][data-part="description"]` or `[data-slot="empty-description"]` for
  typography
- `[data-scope="empty"][data-part="actions"]` or `[data-slot="empty-actions"]` for action row
  spacing and alignment
