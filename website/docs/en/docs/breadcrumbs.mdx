---
title: Breadcrumbs
component: breadcrumbs
description: Semantic breadcrumb navigation with explicit parts and a compact API for short paths.
---

import * as BreadcrumbsDocs from '@/components/examples/breadcrumbs';

## API Reference

Ark UI does not provide a dedicated breadcrumbs primitive. moduix builds this component with the official
[Ark factory](https://ark-ui.com/docs/guides/composition#the-ark-factory) and follows Ark's guidance for
[composition](https://ark-ui.com/docs/guides/composition), [styling](https://ark-ui.com/docs/guides/styling), and
[refs](https://ark-ui.com/docs/guides/ref).

## Basic

Use `Breadcrumbs.Path` when the application already knows which path to display. It builds the ordered
list, items, links, current page, and separators from a small `items` array.

<PreviewFrame maxWidth="lg">
  ```tsx preview file="./_snippets/breadcrumbs/basic.tsx"

```

</PreviewFrame>

## Install with shadcn

<ShadcnInstall packageName="breadcrumbs" />

## Anatomy

`Breadcrumbs` exposes its parts through an Ark-style namespace.

```text
Breadcrumbs / Breadcrumbs.Root
└─ Breadcrumbs.List
   ├─ Breadcrumbs.Item
   │  └─ Breadcrumbs.Link
   ├─ Breadcrumbs.Separator
   └─ Breadcrumbs.Item
      └─ Breadcrumbs.Page
```

| Part                               | Role                                                              |
| ---------------------------------- | ----------------------------------------------------------------- |
| `Breadcrumbs` / `Breadcrumbs.Root` | Root navigation landmark with a default `aria-label`.             |
| `Breadcrumbs.List`                 | Ordered list of breadcrumb segments.                              |
| `Breadcrumbs.Item`                 | List item wrapper for a segment.                                  |
| `Breadcrumbs.Link`                 | Styled anchor for navigable segments; supports `asChild`.         |
| `Breadcrumbs.Page`                 | Current page text with `aria-current="page"`.                     |
| `Breadcrumbs.Separator`            | Visual delimiter hidden from assistive technology.               |
| `Breadcrumbs.Ellipsis`             | Compact marker for a menu trigger that reveals hidden segments.   |

`Breadcrumbs.Path` is a moduix shorthand that builds `Breadcrumbs.List`, `Breadcrumbs.Item`,
`Breadcrumbs.Link`, `Breadcrumbs.Page`, and `Breadcrumbs.Separator` from an `items` array.

## Composition

### Recommended composition

Use `Breadcrumbs.Path` for simple trails made of links. The last item becomes the current page, a
chevron is used between items by default, and `separator` replaces it in every visible gap.
The shorthand creates all of its children, so it does not accept `children` or `asChild`. Native
ordered-list props and the ref are forwarded to the generated `Breadcrumbs.List`.

### When to go custom

Use the explicit parts when you need framework router links, hidden middle segments, or different
markup for individual items. Ark `asChild` lets you plug in those router links. The child must be a
single semantic link that preserves the expected `href`, focus, and keyboard behavior.

Combine it with `Menu` when middle segments should be hidden behind an ellipsis trigger. Keep the
Menu structure explicit: `Menu.Positioner`, `Menu.Content`, and `Menu.Item asChild`. Menu handles the
portal boundary.

### Default props

| Surface                            | Default | Notes                                                  |
| ---------------------------------- | ------- | ------------------------------------------------------ |
| `Breadcrumbs` / `Breadcrumbs.Root` | -       | No additional behavioral defaults; Ark defaults apply. |
| `Breadcrumbs.Path`                 | chevron | Uses chevrons between items unless `separator` is set. |

### Behavioral notes

- `Breadcrumbs` renders a `nav` landmark and defaults `aria-label` to `"Breadcrumb"`.
- `Breadcrumbs.Path` builds an ordered list from `items`; the last item is always the current page.
- `Breadcrumbs.Path` forwards native ordered-list props and the ref, but its generated children are
  fixed, so it does not support `children` or `asChild`.
- `Breadcrumbs.Separator` is purely presentational and hidden from assistive technology.
- `Breadcrumbs.Ellipsis` is hidden from assistive technology because the trigger carries the label.
- Put the accessible label for a collapsed path on `Menu.Trigger`.
- Any visible breadcrumb item can shrink when space is tight. The current page remains the primary
  truncation target through `--moduix-breadcrumbs-item-max-width`.
- Breadcrumbs has no controlled props, callbacks, context hooks, `RootProvider`, ids, `HiddenInput`,
  or form-state integrations because it owns no interactive or form state.

## Examples

### Collapsed Path

Use the explicit Ark `Menu` parts when hidden middle segments need to be revealed. This example uses
explicit Breadcrumbs parts because the trigger and menu content provide the interaction.

<style>{`
      .collapsed-menu-trigger {
        min-width: auto;
        min-height: auto;
        padding: 0;
        border: none;
        background: transparent;
        color: inherit;
        cursor: pointer;
        border-radius: var(--moduix-radius-sm);
        transition: var(--moduix-transition-default);
      }

      @media (hover: hover) {
        .collapsed-menu-trigger:hover {
          color: var(--moduix-color-foreground);
          background: color-mix(in srgb, var(--moduix-color-muted) 70%, transparent);
        }
      }

      .collapsed-menu-trigger:focus-visible {
        outline: none;
        box-shadow: 0 0 0 var(--moduix-border-width-sm) var(--moduix-color-ring);
      }

      .collapsed-menu-content {
        --moduix-menu-popup-min-width: 8rem;
        --moduix-menu-item-font-size: var(--moduix-text-xs);
        --moduix-menu-item-padding-x-start: 0.5rem;
        --moduix-menu-item-padding-x-end: 0.5rem;
        --moduix-menu-item-padding-y: 0.25rem;
      }
    `}</style>

<PreviewFrame maxWidth="lg">
  ```tsx preview file="./_snippets/breadcrumbs/collapsed-path.tsx"

```

</PreviewFrame>

### Custom Separator

Use `Breadcrumbs.Path` to replace the default chevron with an icon or symbol when every visible gap
uses the same separator.

<style>{`
      .separator-icon {
        color: currentColor;
      }
    `}</style>

<PreviewFrame maxWidth="lg">
  ```tsx preview file="./_snippets/breadcrumbs/custom-separator.tsx"

```

</PreviewFrame>

### Framework Link

Use Ark `asChild` when the app uses links provided by a framework router.

<style>{`
      [data-framework-link] {
        text-decoration: none;
      }
    `}</style>

<PreviewFrame maxWidth="lg">
  ```tsx preview file="./_snippets/breadcrumbs/framework-link.tsx"

```

</PreviewFrame>

### Long Current Label

Pass a React node as the last `Breadcrumbs.Path` label when the current page needs extra attributes,
such as `title`. Ancestor items can shrink too, so long middle labels are less likely to make the trail
overflow.

<style>{`
      .breadcrumbs-long-label {
        --moduix-breadcrumbs-item-max-width: 12rem;
      }
    `}</style>

<PreviewFrame maxWidth="lg">
  ```tsx preview file="./_snippets/breadcrumbs/long-current-label.tsx"

```

</PreviewFrame>

### Advanced Customization

Use the explicit parts instead of `Breadcrumbs.Path` when a trail combines framework links, a custom
separator, and a current page label with extra markup.

<style>{`
      [data-framework-link] {
        text-decoration: none;
      }

      .separator-icon {
        color: currentColor;
      }

      .breadcrumbs-long-label {
        --moduix-breadcrumbs-item-max-width: 12rem;
      }
    `}</style>

<PreviewFrame maxWidth="lg">
  ```tsx preview file="./_snippets/breadcrumbs/advanced-customization.tsx"

```

</PreviewFrame>

## Styling

Each exported part exposes Ark-style `data-scope="breadcrumbs"` and `data-part` attributes, along with
stable moduix `data-slot` hooks. Public `--moduix-breadcrumbs-*` variables control layout, text styles,
the focus ring, separators, and ellipsis sizing.

### CSS Properties

<CssPropertiesSection properties={BreadcrumbsDocs.breadcrumbsCssProperties} />

### Styling hooks

- `data-scope="breadcrumbs"` is present on every exported part.
- `data-part="root"`, `list`, `item`, `link`, `page`, `separator`, and `ellipsis` follow the Ark factory
  contract used by this component.
- `data-slot="breadcrumbs-root"`, `breadcrumbs-list`, `breadcrumbs-item`, `breadcrumbs-link`,
  `breadcrumbs-page`, `breadcrumbs-separator`, and `breadcrumbs-ellipsis` remain stable moduix
  selectors.
- `Breadcrumbs.Link`, `Breadcrumbs.Page`, and `Breadcrumbs.Ellipsis` share the horizontal padding from
  `--moduix-breadcrumbs-item-padding-x`.
- Visible `Breadcrumbs.Item` parts can shrink to keep the trail in one line. The last visible item
  remains the primary truncation target and uses `--moduix-breadcrumbs-item-max-width`.
