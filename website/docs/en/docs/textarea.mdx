---
title: Textarea
component: textarea
description: A native multiline text input with Field integration and moduix styling.
---

import * as TextareaDocs from '@/components/examples/textarea/css-properties';
import TextareaAutoResize from '@/components/examples/textarea/auto-resize.tsx';
import TextareaBasic from '@/components/examples/textarea/basic.tsx';
import TextareaControlled from '@/components/examples/textarea/controlled.tsx';
import TextareaDisabledAndReadonly from '@/components/examples/textarea/disabled-and-readonly.tsx';
import TextareaFieldValidation from '@/components/examples/textarea/field-validation.tsx';
import TextareaNativeAttributes from '@/components/examples/textarea/native-attributes.tsx';
import textareaSolidAutoResize from '../../../snippets/textarea/solid/auto-resize.tsx?raw';
import textareaSolidBasic from '../../../snippets/textarea/solid/basic.tsx?raw';
import textareaSolidControlled from '../../../snippets/textarea/solid/controlled.tsx?raw';
import textareaSolidDisabledAndReadonly from '../../../snippets/textarea/solid/disabled-and-readonly.tsx?raw';
import textareaSolidFieldValidation from '../../../snippets/textarea/solid/field-validation.tsx?raw';
import textareaSolidNativeAttributes from '../../../snippets/textarea/solid/native-attributes.tsx?raw';

## API Reference

Ark UI does not ship a dedicated `Textarea` primitive. moduix builds this component on
[Ark's `Field.Textarea`](https://ark-ui.com/docs/components/field), preserving the native
`textarea` API, `autoresize`, ref forwarding, `asChild`, and Field state inheritance.

## Choosing the right component

Use `Textarea` for plain text that can span multiple lines and should behave like a native browser
`textarea`.

- Choose **`Input`** for short single-line values such as names, URLs, or compact filters.
- Choose **`Textarea`** for notes, comments, descriptions, and other longer free-form content.
- Choose **`Editable`** when text is shown first and edited on demand.
- Choose **`Field` + `Textarea`** when the control needs a declarative label, description, required
  state, or validation.

## Basic

Place `Textarea` inside `Field` when the control needs an accessible label or supporting text. This
is the recommended setup for comments, notes, issue descriptions, and similar content.


<ExampleFrame variant="component">
  <TextareaBasic />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/textarea/basic.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={textareaSolidBasic} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/textarea/textarea-basic.module.css"
```
</div>

</ExampleCode>

## Install with shadcn

See [Quick Start](/docs/quick-start) for full package setup, registry configuration, and styling guidance.

<Tabs groupId="framework">
  <Tab label="React" value="react">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-react/textarea' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-react-tailwind/textarea' },
  ]}
/>

  </Tab>
  <Tab label="Solid" value="solid">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-solid/textarea' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-solid-tailwind/textarea' },
  ]}
/>

  </Tab>
</Tabs>

## Anatomy

The public structure has one component part:

```text
Field (optional wrapper)
└─ Textarea
```

| Part       | Role                                                  |
| ---------- | ----------------------------------------------------- |
| `Textarea` | Native `textarea` with moduix styles and Field state. |

`Textarea` does not export an additional wrapper, compound subcomponents, provider or context hooks,
hidden inputs, or a label or character-counter API.

## Composition

Use `defaultValue` for an uncontrolled textarea. Use `value` with native `onChange` when the
application owns the content, using the matching framework's state primitive.

Wrap `Textarea` with `Field`, `Field.Label`, `Field.HelperText`, and `Field.ErrorText` when the
control needs a label, supporting text, or validation. Outside `Field`, provide an accessible name
with a native `<label>` or `aria-label`.

Set `autoresize` only when the control should grow with its content. Ark disables manual resizing in
that mode, so `--moduix-textarea-resize` applies only when `autoresize` is false. With `asChild`,
provide one semantic textarea host that can receive the merged props and ref.

## Examples

### Auto-resize

Set `autoresize` when the textarea should grow as the user types. Ark disables manual resizing while
this option is enabled.

<ExampleFrame variant="component">
  <TextareaAutoResize />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/textarea/auto-resize.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={textareaSolidAutoResize} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/textarea/textarea-auto-resize.module.css"
```
</div>

</ExampleCode>

### Controlled value

Use `value` and native `onChange` when application state should control the content.

<ExampleFrame variant="component">
  <TextareaControlled />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/textarea/controlled.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={textareaSolidControlled} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/textarea/textarea-controlled.module.css"
```
</div>

</ExampleCode>

### Disabled and read-only

Use `disabled` when the user must not interact with the control. Use `readOnly` when they can read
and select the value but must not edit it.

<ExampleFrame variant="component">
  <TextareaDisabledAndReadonly />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/textarea/disabled-and-readonly.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={textareaSolidDisabledAndReadonly} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/textarea/textarea-disabled-and-readonly.module.css"
```
</div>

</ExampleCode>

### Field validation

Set `invalid` on `Field`. Ark adds `aria-invalid` and `data-invalid` to the textarea and connects
its error text.

<ExampleFrame variant="component">
  <TextareaFieldValidation />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/textarea/field-validation.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={textareaSolidFieldValidation} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/textarea/textarea-field-validation.module.css"
```
</div>

</ExampleCode>

### Native attributes and form submission

Use native textarea props such as `rows`, `maxLength`, and `spellCheck` for browser behavior. This
example also submits the form and reads the current value from `FormData`.

<ExampleFrame variant="component">
  <TextareaNativeAttributes />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/textarea/native-attributes.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={textareaSolidNativeAttributes} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/textarea/textarea-native-attributes.module.css"
```
</div>

</ExampleCode>

## Styling

Style the control with `className` in React or `class` in Solid, together with
`data-slot="textarea-root"`. Tailwind variants use native utility and consumer classes. The public
`--moduix-textarea-*` variables below apply to the CSS Modules variants.

### CSS Properties

<div className="docs-reference">
  <Tabs
    values={[{ label: 'CSS Variables', value: 'CSS Variables' }]}
    className="docs-reference-tabs"
    contentItemClassName="docs-reference-panel"
  >
    <Tab>
      <TextareaDocs.TextareaCssPropertiesPanel />
    </Tab>
  </Tabs>
</div>

### Styling hooks

Use the `Textarea` root for the control's appearance and the surrounding `Field` for layout and
supporting text.

- `Textarea` root for the border, background, placeholder, resize behavior, and focus ring
- `Field` wrapper for width, max width, spacing, label layout, description, and validation text
- `data-slot="textarea-root"` for stable targeting in scoped styles
- `data-autoresize` when `autoresize` is enabled
- `data-invalid`, `[aria-invalid='true']`, `[disabled]`, `data-disabled`, `:read-only`, and
  `data-readonly` for state-aware styling
