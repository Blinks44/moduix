---
title: Listbox
component: listbox
description: A visible listbox for selecting one or more items, with optional filtering and extended selection.
---

import * as ListboxDocs from '@/components/examples/listbox/css-properties';
import ListboxBasic from '@/components/examples/listbox/basic.tsx';
import ListboxControlled from '@/components/examples/listbox/controlled.tsx';
import ListboxDisabledItem from '@/components/examples/listbox/disabled-item.tsx';
import ListboxExtendedSelection from '@/components/examples/listbox/extended-selection.tsx';
import ListboxFiltering from '@/components/examples/listbox/filtering.tsx';
import ListboxGrid from '@/components/examples/listbox/grid.tsx';
import ListboxGrouping from '@/components/examples/listbox/grouping.tsx';
import ListboxHorizontal from '@/components/examples/listbox/horizontal.tsx';
import ListboxMultiple from '@/components/examples/listbox/multiple.tsx';
import ListboxRootProvider from '@/components/examples/listbox/root-provider.tsx';
import ListboxSelectAll from '@/components/examples/listbox/select-all.tsx';
import ListboxStandaloneFilterInput from '@/components/examples/listbox/standalone-filter-input.tsx';
import ListboxValueText from '@/components/examples/listbox/value-text.tsx';
import listboxSolidBasic from '../../../snippets/listbox/solid/basic.tsx?raw';
import listboxSolidControlled from '../../../snippets/listbox/solid/controlled.tsx?raw';
import listboxSolidDisabledItem from '../../../snippets/listbox/solid/disabled-item.tsx?raw';
import listboxSolidExtendedSelection from '../../../snippets/listbox/solid/extended-selection.tsx?raw';
import listboxSolidFiltering from '../../../snippets/listbox/solid/filtering.tsx?raw';
import listboxSolidGrid from '../../../snippets/listbox/solid/grid.tsx?raw';
import listboxSolidGrouping from '../../../snippets/listbox/solid/grouping.tsx?raw';
import listboxSolidHorizontal from '../../../snippets/listbox/solid/horizontal.tsx?raw';
import listboxSolidMultiple from '../../../snippets/listbox/solid/multiple.tsx?raw';
import listboxSolidRootProvider from '../../../snippets/listbox/solid/root-provider.tsx?raw';
import listboxSolidSelectAll from '../../../snippets/listbox/solid/select-all.tsx?raw';
import listboxSolidStandaloneFilterInput from '../../../snippets/listbox/solid/standalone-filter-input.tsx?raw';
import listboxSolidValueText from '../../../snippets/listbox/solid/value-text.tsx?raw';

## API Reference

<PrimitiveReference href="https://ark-ui.com/docs/components/listbox" />

## Choosing the right component

Use `Listbox` when the options should stay visible and users choose from a collection. Use `Select`
when users should choose from a popup. Use `Combobox` when users need an input-driven popup for
searching or entering a value.

## Basic


Pass a `collection`, render `ListboxContent`, and pass each collection item to `ListboxItem`
through `item`. Add `ListboxItemIndicator` when rows should show the selected state.

<ExampleFrame variant="component">
  <ListboxBasic />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/listbox/basic.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={listboxSolidBasic} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/listbox/listbox-basic.module.css"
```
</div>

</ExampleCode>

## Install with shadcn

See [Quick Start](/docs/quick-start) for full package setup, registry configuration, and styling guidance.

<Tabs groupId="framework">
  <Tab label="React" value="react">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-react/listbox' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-react-tailwind/listbox' },
  ]}
/>

  </Tab>
  <Tab label="Solid" value="solid">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-solid/listbox' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-solid-tailwind/listbox' },
  ]}
/>

  </Tab>
</Tabs>

## Anatomy

```text
Listbox
├─ ListboxLabel
├─ ListboxFilter
│  ├─ ListboxInput
│  └─ ListboxClearTrigger
├─ ListboxContent
│  ├─ ListboxEmpty
│  ├─ ListboxItemGroup
│  │  ├─ ListboxItemGroupLabel
│  │  └─ ListboxItem[item]
│  │     ├─ ListboxItemText
│  │     └─ ListboxItemIndicator
│  └─ ListboxItem[item]
│     ├─ ListboxItemText
│     │  └─ ListboxItemTextContent (optional)
│     │     ├─ ListboxItemTextIcon (optional)
│     │     └─ ListboxItemTextLabel (optional)
│     └─ ListboxItemIndicator
└─ ListboxValueText

ListboxRootProvider
└─ same item tree connected to a useListbox() store
```

| Part                       | Role                                                                                   |
| -------------------------- | -------------------------------------------------------------------------------------- |
| `Listbox` | Manages the collection, selected values, highlighted item, and selection mode.         |
| `ListboxRootProvider`     | Renders the listbox tree from state returned by `useListbox`.                          |
| `ListboxLabel`            | Label that gives the listbox an accessible name.                                       |
| `ListboxFilter`           | Visual wrapper for a filter input with the default search icon. Place it before `ListboxContent`. |
| `ListboxInput`            | Filter input, usually placed inside `ListboxFilter`.                                         |
| `ListboxClearTrigger`     | Button for resetting the filter query, with the default close icon.                    |
| `ListboxContent`          | Focusable listbox container that handles keyboard navigation.                          |
| `ListboxEmpty`            | Content shown when the filtered collection has no items.                               |
| `ListboxItemGroup`        | Group wrapper for related collection items.                                            |
| `ListboxItemGroupLabel`   | Label for a collection group.                                                          |
| `ListboxItem`             | Selectable item. Pass its collection item through `item`.                              |
| `ListboxItemText`         | Text used for display and accessibility.                                               |
| `ListboxItemTextContent`  | Optional wrapper for laying out an icon and text inside `ListboxItemText`.                    |
| `ListboxItemTextIcon`     | Optional icon wrapper inside `ListboxItemTextContent`.                                       |
| `ListboxItemTextLabel`    | Text wrapper inside `ListboxItemTextContent` that can truncate long labels without breaking layout. |
| `ListboxItemIndicator`    | Selection indicator with the default moduix check icon.                                |
| `ListboxValueText`        | Summary of the selected values.                                                        |

## Composition

Use `defaultValue` for uncontrolled selection. When another part of the application owns the
selection, pass `value` and update it from `onValueChange(details.value)`. For filtering, set
`typeahead={false}`, keep the query in application state, and call the `filter` function returned by
`useListCollection` from the input handler. `ListboxFilter` only provides the joined search-field appearance, so a
clear action must reset both the query and the collection filter.

Use `useListbox` from the matching framework package with `ListboxRootProvider` when the store must
be created outside the rendered tree. Pass the returned store to `ListboxRootProvider` and do not render
`Listbox` for the same store. `ListboxInput` can be rendered directly when the
filter needs a separate wrapper, and `ListboxValueText` can be placed after `ListboxContent` for a selected
value summary.

## Examples

### Controlled

Keep the selected string array in application state and update it from `details.value`.

<ExampleFrame variant="component">
  <ListboxControlled />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs groupId="framework" values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]} className="docs-tabs">
    <Tab value="react">

```tsx pure file="../../../src/components/examples/listbox/controlled.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={listboxSolidControlled} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/listbox/listbox-controlled.module.css"
```
</div>

</ExampleCode>

### Disabled Item

Disable an individual item with its collection item's `disabled` field.

<ExampleFrame variant="component">
  <ListboxDisabledItem />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs groupId="framework" values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]} className="docs-tabs">
    <Tab value="react">

```tsx pure file="../../../src/components/examples/listbox/disabled-item.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={listboxSolidDisabledItem} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/listbox/listbox-disabled-item.module.css"
```
</div>

</ExampleCode>

### Extended Selection

Use `selectionMode="extended"` when users need modifier keys to select multiple items.

<ExampleFrame variant="component">
  <ListboxExtendedSelection />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs groupId="framework" values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]} className="docs-tabs">
    <Tab value="react">

```tsx pure file="../../../src/components/examples/listbox/extended-selection.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={listboxSolidExtendedSelection} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/listbox/listbox-extended-selection.module.css"
```
</div>

</ExampleCode>

### Filtering

Use `ListboxFilter` with `useListCollection`. It adds the default search icon and joins the filter
with `ListboxContent`; turn off typeahead when the input handles search.

<ExampleFrame variant="component">
  <ListboxFiltering />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs groupId="framework" values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]} className="docs-tabs">
    <Tab value="react">

```tsx pure file="../../../src/components/examples/listbox/filtering.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={listboxSolidFiltering} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/listbox/listbox-filtering.module.css"
```
</div>

</ExampleCode>

### Grid

Pass a collection created with `createGridCollection` to enable grid keyboard navigation. Ark adds
`data-layout="grid"` and the `--column-count` CSS variable to `ListboxContent`.

<ExampleFrame variant="component">
  <ListboxGrid />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs groupId="framework" values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]} className="docs-tabs">
    <Tab value="react">

```tsx pure file="../../../src/components/examples/listbox/grid.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={listboxSolidGrid} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/listbox/listbox-grid.module.css"
```
</div>

</ExampleCode>

### Grouping

Set `groupBy` when creating the collection and render `ListboxItemGroup` with `ListboxItemGroupLabel`.

<ExampleFrame variant="component">
  <ListboxGrouping />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs groupId="framework" values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]} className="docs-tabs">
    <Tab value="react">

```tsx pure file="../../../src/components/examples/listbox/grouping.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={listboxSolidGrouping} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/listbox/listbox-grouping.module.css"
```
</div>

</ExampleCode>

### Horizontal

Set `orientation="horizontal"` to lay out items horizontally and enable left and right arrow-key
navigation.

<ExampleFrame variant="component">
  <ListboxHorizontal />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs groupId="framework" values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]} className="docs-tabs">
    <Tab value="react">

```tsx pure file="../../../src/components/examples/listbox/horizontal.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={listboxSolidHorizontal} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/listbox/listbox-horizontal.module.css"
```
</div>

</ExampleCode>

### Multiple

Set `selectionMode="multiple"` to toggle multiple values without modifier keys.

<ExampleFrame variant="component">
  <ListboxMultiple />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs groupId="framework" values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]} className="docs-tabs">
    <Tab value="react">

```tsx pure file="../../../src/components/examples/listbox/multiple.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={listboxSolidMultiple} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/listbox/listbox-multiple.module.css"
```
</div>

</ExampleCode>

### Root Provider

Use `useListbox()` from the matching framework package with `ListboxRootProvider` when controls
outside the listbox need direct access to its store.

<ExampleFrame variant="component">
  <ListboxRootProvider />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs groupId="framework" values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]} className="docs-tabs">
    <Tab value="react">

```tsx pure file="../../../src/components/examples/listbox/root-provider.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={listboxSolidRootProvider} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/listbox/listbox-root-provider.module.css"
```
</div>

</ExampleCode>

### Select All

Use `useListboxContext` inside a child component to add a button that updates the listbox selection.

<ExampleFrame variant="component">
  <ListboxSelectAll />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs groupId="framework" values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]} className="docs-tabs">
    <Tab value="react">

```tsx pure file="../../../src/components/examples/listbox/select-all.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={listboxSolidSelectAll} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/listbox/listbox-select-all.module.css"
```
</div>

</ExampleCode>

### Standalone Filter Input

Render `ListboxInput` directly when the filter and results should remain separate surfaces.

<ExampleFrame variant="component">
  <ListboxStandaloneFilterInput />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs groupId="framework" values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]} className="docs-tabs">
    <Tab value="react">

```tsx pure file="../../../src/components/examples/listbox/standalone-filter-input.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={listboxSolidStandaloneFilterInput} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/listbox/listbox-standalone-filter-input.module.css"
```
</div>

</ExampleCode>

### Value Text

Render `ListboxValueText` when the selected-value summary should stay visible beside the list.

<ExampleFrame variant="component">
  <ListboxValueText />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs groupId="framework" values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]} className="docs-tabs">
    <Tab value="react">

```tsx pure file="../../../src/components/examples/listbox/value-text.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={listboxSolidValueText} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/listbox/listbox-value-text.module.css"
```
</div>

</ExampleCode>

## Styling

Style Listbox through `className` in React or `class` in Solid on exported parts. Tailwind variants
use native utilities and consumer classes, while CSS Modules variants use the public
`--moduix-listbox-*` CSS variables.

### CSS Properties

<CssPropertiesSection properties={ListboxDocs.listboxOverrideCssProperties} />

### Styling hooks

- Ark anatomy hooks: `data-scope="listbox"` and `data-part` on root, label, input, content, item
  group, item group label, item, item text, item indicator, value text, and empty.
- State hooks: `data-orientation`, `data-disabled`, `data-empty`, `data-layout`,
  `data-activedescendant`, `data-value`, `data-selected`, `data-state`, and `data-highlighted`.
- moduix hooks: `data-slot="listbox-root"`, `listbox-root-provider`, `listbox-label`,
  `listbox-filter`, `listbox-filter-icon`, `listbox-input`, `listbox-clear-trigger`,
  `listbox-content`, `listbox-empty`, `listbox-item-group`, `listbox-item-group-label`,
  `listbox-item`, `listbox-item-text`, `listbox-item-text-content`, `listbox-item-text-icon`,
  `listbox-item-text-label`, `listbox-item-indicator`, and `listbox-value-text`.
