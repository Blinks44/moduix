---
title: Hover Card
component: hover-card
description: An Ark UI hover card for supplementary previews opened when its trigger is hovered or focused.
---

import * as HoverCardDocs from '@/components/examples/hover-card/css-properties';
import BasicHoverCard from '@/components/examples/hover-card/basic.tsx';
import ContextHoverCard from '@/components/examples/hover-card/context.tsx';
import ControlledHoverCard from '@/components/examples/hover-card/controlled.tsx';
import DelayHoverCard from '@/components/examples/hover-card/delay.tsx';
import DialogHoverCard from '@/components/examples/hover-card/dialog.tsx';
import DisabledHoverCard from '@/components/examples/hover-card/disabled.tsx';
import MultipleTriggersHoverCard from '@/components/examples/hover-card/multiple-triggers.tsx';
import PositioningHoverCard from '@/components/examples/hover-card/positioning.tsx';
import RootProviderHoverCard from '@/components/examples/hover-card/root-provider.tsx';
import HoverCardWithArrow from '@/components/examples/hover-card/with-arrow.tsx';
import hoverCardSolidBasic from '../../../snippets/hover-card/solid/basic.tsx?raw';
import hoverCardSolidContext from '../../../snippets/hover-card/solid/context.tsx?raw';
import hoverCardSolidControlled from '../../../snippets/hover-card/solid/controlled.tsx?raw';
import hoverCardSolidDelay from '../../../snippets/hover-card/solid/delay.tsx?raw';
import hoverCardSolidDialog from '../../../snippets/hover-card/solid/dialog.tsx?raw';
import hoverCardSolidDisabled from '../../../snippets/hover-card/solid/disabled.tsx?raw';
import hoverCardSolidMultipleTriggers from '../../../snippets/hover-card/solid/multiple-triggers.tsx?raw';
import hoverCardSolidPositioning from '../../../snippets/hover-card/solid/positioning.tsx?raw';
import hoverCardSolidRootProvider from '../../../snippets/hover-card/solid/root-provider.tsx?raw';
import hoverCardSolidWithArrow from '../../../snippets/hover-card/solid/with-arrow.tsx?raw';

## API Reference

<PrimitiveReference href="https://ark-ui.com/docs/components/hover-card" />

## Choosing the right component

Use `Tooltip` for short, non-interactive hints. Use `Popover` when the popup contains richer
content or actions. Use `HoverCard` when you need a supplementary preview tied to a trigger, such as
a profile or article summary.

## Basic


Compose the card from explicit Ark parts. `HoverCard.Trigger` opens the card, while
`HoverCard.Positioner` and `HoverCard.Content` define its floating content. The basic composition
does not include an arrow. Put long content in `HoverCard.Body` so it can scroll within the
available height while `HoverCard.Content` stays overflow-visible for an optional arrow.

<ExampleFrame variant="component">
  <BasicHoverCard />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/hover-card/basic.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={hoverCardSolidBasic} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/hover-card/hover-card-basic.module.css"
```
</div>

</ExampleCode>

## Install with shadcn

See [Quick Start](/docs/quick-start) for full package setup, registry configuration, and styling guidance.

<Tabs groupId="framework">
  <Tab label="React" value="react">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-react/hover-card' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-react-tailwind/hover-card' },
  ]}
/>

  </Tab>
  <Tab label="Solid" value="solid">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-solid/hover-card' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-solid-tailwind/hover-card' },
  ]}
/>

  </Tab>
</Tabs>

## Anatomy

```text
HoverCard / HoverCard.Root
├─ HoverCard.Trigger
└─ Overlay subtree (automatically rendered in a portal)
   └─ HoverCard.Positioner
      └─ HoverCard.Content
         ├─ HoverCard.Arrow
         │  └─ HoverCard.ArrowTip
         └─ HoverCard.Body
            └─ content

HoverCard.RootProvider
└─ same part tree connected to a moduix useHoverCard() store
```

| Part                           | Role                                                                  |
| ------------------------------ | --------------------------------------------------------------------- |
| `HoverCard` / `HoverCard.Root`  | Owns state, lifecycle, delays, positioning, ids, and outside interaction. |
| `HoverCard.RootProvider`        | Connects the parts to state created with `useHoverCard()`.            |
| `HoverCard.Context`             | Exposes current state through a render prop for descendants.         |
| `HoverCard.Trigger`             | Interactive element that opens the card on hover or focus. Use `asChild` for a link or another semantic element. |
| `HoverCard.Positioner`          | Positions the floating layer and exposes Ark positioning variables.  |
| `HoverCard.Content`             | Visible card surface that closes on outside interaction.              |
| `HoverCard.Arrow`               | Optional positioned arrow wrapper that renders `HoverCard.ArrowTip` by default. |
| `HoverCard.ArrowTip`             | Arrow tip element styled through Ark arrow variables.                 |
| `HoverCard.Body`                | Optional scrollable content region for constrained previews.          |

`HoverCard` is the short root form, and `HoverCard.Root` is its namespace alias. Use the short form
in application code.

## Composition

`HoverCard` owns the state and portals `HoverCard.Positioner` by default. Set `portalled={false}`
inside another overlay, or pass `portalRef` for a custom container. Use `HoverCard.RootProvider`
with `useHoverCard()` when state belongs outside the card tree; React reads the store as an object,
while Solid reads it through its accessor.

Keep `HoverCard.Arrow` inside `HoverCard.Content` and wrap long previews in `HoverCard.Body`:
`Content` stays overflow-visible for the arrow, while `Body` provides the scroll region. Use
`asChild` with one focusable child when the trigger should be a link or another semantic element.
For several triggers sharing one content area, give each trigger a `value` and update external
content from `onTriggerValueChange(details)`.

## Examples

### Context

Use `HoverCard.Context` to read state inside the card tree without changing its visible part
structure.

<ExampleFrame variant="component">
  <ContextHoverCard />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/hover-card/context.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={hoverCardSolidContext} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/hover-card/hover-card-context.module.css"
```
</div>

</ExampleCode>

### Controlled

Control whether the card is open with `open` and `onOpenChange(details)`.

<ExampleFrame variant="component">
  <ControlledHoverCard />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/hover-card/controlled.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={hoverCardSolidControlled} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/hover-card/hover-card-controlled.module.css"
```
</div>

</ExampleCode>

### Delay

Set `openDelay` and `closeDelay` on the root to adjust hover and focus timing.

<ExampleFrame variant="component">
  <DelayHoverCard />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/hover-card/delay.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={hoverCardSolidDelay} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/hover-card/hover-card-delay.module.css"
```
</div>

</ExampleCode>

### Dialog

Render a hover card inline inside a dialog when it must stay in the dialog's overlay layer. The
dialog's initial focus can land on the Hover Card trigger and open the card after `openDelay`; set
`initialFocusEl` to another element when the preview should remain closed until hover or focus.

<ExampleFrame variant="component">
  <DialogHoverCard />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/hover-card/dialog.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={hoverCardSolidDialog} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/hover-card/hover-card-dialog.module.css"
```
</div>

</ExampleCode>

### Disabled

Set `disabled` to prevent the card from opening while keeping the trigger visible.

<ExampleFrame variant="component">
  <DisabledHoverCard />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/hover-card/disabled.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={hoverCardSolidDisabled} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/hover-card/hover-card-disabled.module.css"
```
</div>

</ExampleCode>

### Multiple Triggers

Give each trigger a `value` and use `onTriggerValueChange(details)` to share one content area
across several triggers.

<ExampleFrame variant="component">
  <MultipleTriggersHoverCard />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/hover-card/multiple-triggers.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={hoverCardSolidMultipleTriggers} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/hover-card/hover-card-multiple-triggers.module.css"
```
</div>

</ExampleCode>

### Positioning

Pass Ark positioning options through the `positioning` prop.

<ExampleFrame variant="component">
  <PositioningHoverCard />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/hover-card/positioning.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={hoverCardSolidPositioning} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/hover-card/hover-card-positioning.module.css"
```
</div>

</ExampleCode>

### Root Provider

Use `useHoverCard()` with `HoverCard.RootProvider` when controls or other UI outside the card tree
need to read or update its state.

<ExampleFrame variant="component">
  <RootProviderHoverCard />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/hover-card/root-provider.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={hoverCardSolidRootProvider} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/hover-card/hover-card-root-provider.module.css"
```
</div>

</ExampleCode>

### With Arrow

Add `HoverCard.Arrow` inside `HoverCard.Content` when the popup needs a visual pointer to its
trigger. The wrapper renders `HoverCard.ArrowTip` by default; keep the preview in
`HoverCard.Body` so the arrow remains part of the bordered surface while the preview can scroll.

<ExampleFrame variant="component">
  <HoverCardWithArrow />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/hover-card/with-arrow.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={hoverCardSolidWithArrow} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/hover-card/hover-card-with-arrow.module.css"
```
</div>

</ExampleCode>

## Styling

Use `className` in React or `class` in Solid on exported parts for structural styles. Tailwind
variants use native utilities and consumer classes; the variables below customize the CSS Modules
variants. Data attributes and `data-slot` hooks are shared by both styling tracks.

### CSS Properties

<div className="not-prose">
  <Tabs
    values={[{ label: 'CSS Variables', value: 'CSS Variables' }]}
    className="docs-reference-tabs"
    contentItemClassName="docs-reference-panel"
  >
    <Tab value="CSS Variables">
      <HoverCardDocs.HoverCardCssPropertiesPanel />
    </Tab>
  </Tabs>
</div>

### Styling hooks

Moduix adds `data-slot="hover-card-*"` hooks to the trigger, positioner, content, arrow, arrow tip,
and body.
`HoverCard` and `HoverCard.RootProvider` only provide state and do not render DOM elements, so they have no styling hooks. Ark adds
`data-scope="hover-card"` and `data-part` to its parts. For state-aware styling, target
`data-state="open|closed"`, `data-placement`, `data-side`, `data-value`, `data-current`,
`data-nested`, and `data-has-nested`.

`HoverCard.Positioner` exposes `--reference-width`, `--reference-height`, `--available-width`,
`--available-height`, `--x`, `--y`, `--z-index`, and `--transform-origin`. `HoverCard.Content`
exposes `--layer-index` and `--nested-layer-count`. `HoverCard.Arrow` exposes `--arrow-size`,
`--arrow-size-half`, `--arrow-background`, and `--arrow-offset`.
