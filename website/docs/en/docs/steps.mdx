---
title: Steps
component: steps
description: Guide users through a multi-step process with accessible Ark UI state and navigation.
---

import * as StepsDocs from '@/components/examples/steps/css-properties';
import StepsBasic from '@/components/examples/steps/basic.tsx';
import StepsControlled from '@/components/examples/steps/controlled.tsx';
import StepsValidation from '@/components/examples/steps/validation.tsx';
import StepsProgress from '@/components/examples/steps/progress.tsx';
import StepsRootProvider from '@/components/examples/steps/root-provider.tsx';
import StepsVertical from '@/components/examples/steps/vertical.tsx';
import StepsCustomComposition from '@/components/examples/steps/custom-composition.tsx';
import stepsSolidBasic from '../../../snippets/steps/solid/basic.tsx?raw';
import stepsSolidControlled from '../../../snippets/steps/solid/controlled.tsx?raw';
import stepsSolidValidation from '../../../snippets/steps/solid/validation.tsx?raw';
import stepsSolidProgress from '../../../snippets/steps/solid/progress.tsx?raw';
import stepsSolidRootProvider from '../../../snippets/steps/solid/root-provider.tsx?raw';
import stepsSolidVertical from '../../../snippets/steps/solid/vertical.tsx?raw';
import stepsSolidCustomComposition from '../../../snippets/steps/solid/custom-composition.tsx?raw';

## API Reference

<PrimitiveReference href="https://ark-ui.com/docs/components/steps" />

## Basic


Use `Steps` with `count`, a `StepsList` containing one `StepsItem` for each step, matching
`StepsContent` panels, and the built-in previous and next triggers. Step indexes start at zero.
For example, `count={3}` uses indexes `0`, `1`, and `2`. When the step value reaches `count`,
`StepsCompletedContent` is shown.

The `Steps` export is the root component. Use `StepsRootProvider` with `useSteps()` when state must
be created outside the rendered part tree.

<ExampleFrame variant="component">
  <StepsBasic />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/steps/basic.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={stepsSolidBasic} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/steps/steps-basic.module.css"
```
</div>

</ExampleCode>

## Install with shadcn

See [Quick Start](/docs/quick-start) for full package setup, registry configuration, and styling guidance.

<Tabs groupId="framework">
  <Tab label="React" value="react">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-react/steps' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-react-tailwind/steps' },
  ]}
/>

  </Tab>
  <Tab label="Solid" value="solid">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-solid/steps' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-solid-tailwind/steps' },
  ]}
/>

  </Tab>
</Tabs>

## Anatomy

```text
Steps
├─ StepsProgress (optional)
├─ StepsList
│  └─ StepsItem[index]
│     ├─ StepsTrigger
│     │  └─ StepsIndicator
│     └─ StepsSeparator
├─ StepsContent[index]
├─ StepsCompletedContent
├─ StepsPrevTrigger
└─ StepsNextTrigger

StepsRootProvider
└─ same part tree connected to a useSteps() store
```

| Part                              | Role                                                                              |
| --------------------------------- | --------------------------------------------------------------------------------- |
| `Steps`                    | Root state machine for step state, orientation, navigation, validation, and IDs. |
| `StepsRootProvider`         | Connects the parts to state created with `useSteps()`.                            |
| `StepsProgress`             | Optional visual progress track driven by Ark's `--percent` root variable.         |
| `StepsList`                 | Tablist containing the step triggers.                                             |
| `StepsItem`                 | Step item with a required zero-based `index`.                                      |
| `StepsTrigger`              | Tab trigger for a step; supports `asChild` for custom semantic elements.          |
| `StepsIndicator`            | Visual step marker with a number or check icon by default.                        |
| `StepsSeparator`            | Decorative connector between step items.                                          |
| `StepsContent`              | Content panel for one step, matched by `index`.                                    |
| `StepsCompletedContent`     | Panel shown when the current step value equals `count`.                           |
| `StepsPrevTrigger` / `StepsNextTrigger` | Buttons wired to Ark's previous and next actions.                    |

## Composition

Use `Steps` as the state owner. Give every `StepsItem` and matching
`StepsContent` the same zero-based `index`; `count` is the total number of steps and its value
represents completion. Use `StepsRootProvider` only with a store from `useSteps()` when
state needs to be created outside the part tree. Do not render the root and provider for the same
store.

For lower-level state reads, use `useStepsContext` and `useStepsItemContext`, or the flat context
values `StepsContext` and `StepsItemContext`.

`StepsTrigger`, `StepsPrevTrigger`, and `StepsNextTrigger` accept `asChild`. Pass one semantic
child when a control should render as an anchor, router link, or design-system button. When
`StepsIndicator` has no children, it renders the step number or the shared check icon for a
completed step; pass children to control the marker.

## Examples

### Controlled

Control `step` from framework state when the active step must stay synchronized with routing,
validation, or another part of the interface.

<ExampleFrame variant="component">
  <StepsControlled />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/steps/controlled.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={stepsSolidControlled} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/steps/steps-controlled.module.css"
```
</div>

</ExampleCode>

### Validation

Use `linear`, `isStepValid`, and `onStepInvalid` to prevent navigation until the current step
passes your validation rule.

<ExampleFrame variant="component">
  <StepsValidation />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/steps/validation.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={stepsSolidValidation} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/steps/steps-validation.module.css"
```
</div>

</ExampleCode>

### Progress

Add `StepsProgress` when the flow should show a visual progress track alongside the step list.
Add your own accessible status if the progress needs to be announced to assistive technology.

<ExampleFrame variant="component">
  <StepsProgress />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/steps/progress.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={stepsSolidProgress} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/steps/steps-progress.module.css"
```
</div>

</ExampleCode>

### RootProvider

Create the state with `useSteps()` outside the rendered part tree, then pass it to
`StepsRootProvider` when external controls need to work with the same state.

<ExampleFrame variant="component">
  <StepsRootProvider />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/steps/root-provider.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={stepsSolidRootProvider} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/steps/steps-root-provider.module.css"
```
</div>

</ExampleCode>

### Vertical

Set `orientation="vertical"` when the step list should sit beside the active content panel on
wide layouts and stack above it on narrow screens.

<ExampleFrame variant="component">
  <StepsVertical />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/steps/vertical.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={stepsSolidVertical} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/steps/steps-vertical.module.css"
```
</div>

</ExampleCode>

### Advanced Customization

Use `asChild` when a trigger must render as an anchor or router link instead of the default button.
Keep one semantic child so it receives Ark's ARIA, keyboard, and focus behavior.

<ExampleFrame variant="component">
  <StepsCustomComposition />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/steps/custom-composition.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={stepsSolidCustomComposition} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/steps/steps-custom-composition.module.css"
```
</div>

</ExampleCode>

## Styling

Style `Steps` with `className` in React or `class` in Solid, Ark's `data-scope="steps"` and
`data-part` attributes, Ark state attributes, moduix `data-slot` hooks, and the public
`--moduix-steps-*` CSS variables listed below.

### CSS variables

<div className="docs-reference">
  <Tabs
    values={[{ label: 'CSS Variables', value: 'CSS Variables' }]}
    className="docs-reference-tabs"
    contentItemClassName="docs-reference-panel"
  >
    <Tab>
      <StepsDocs.StepsCssPropertiesPanel />
    </Tab>
  </Tabs>
</div>

### Styling hooks

The useful public hooks are:

- `className` in React or `class` in Solid on `Steps`, `StepsRootProvider`, `StepsList`, `StepsItem`, `StepsTrigger`, `StepsIndicator`, `StepsSeparator`, `StepsContent`, `StepsCompletedContent`, `StepsPrevTrigger`, `StepsNextTrigger`, and `StepsProgress`
- `data-slot="steps-root"`, `steps-root-provider`, `steps-list`, `steps-item`, `steps-trigger`, `steps-indicator`, `steps-separator`, `steps-content`, `steps-completed-content`, `steps-prev-trigger`, `steps-next-trigger`, and `steps-progress`
- Ark's `data-scope="steps"` and `data-part` values for every primitive part
- `data-orientation="horizontal" | "vertical"` on the root, list, item, trigger, content, and separator parts
- `data-current`, `data-complete`, and `data-incomplete` on indicator, separator, and trigger state surfaces
- `data-skippable` on `StepsItem` when the step is marked skippable
- `data-state="open" | "closed"` on triggers and content panels
- `data-complete` on `StepsProgress`
- Ark's `--percent` variable on the root, consumed by `StepsProgress`
