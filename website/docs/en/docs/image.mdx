---
title: Image
component: image
description: Responsive images with automatic CDN detection and Unpic-generated srcset and sizes attributes.
---

import * as ImageDocs from '@/components/examples/image/css-properties';
import ImageArtDirection from '@/components/examples/image/art-direction.tsx';
import ImageBasic from '@/components/examples/image/basic.tsx';
import ImageFixed from '@/components/examples/image/fixed.tsx';
import ImageFullWidth from '@/components/examples/image/full-width.tsx';
import imageSolidArtDirection from '../../../snippets/image/solid/art-direction.tsx?raw';
import imageSolidBasic from '../../../snippets/image/solid/basic.tsx?raw';
import imageSolidFixed from '../../../snippets/image/solid/fixed.tsx?raw';
import imageSolidFullWidth from '../../../snippets/image/solid/full-width.tsx?raw';

## API Reference

`Image` is a thin moduix wrapper around the `<img>` component from [Unpic React](https://unpic.pics/img/react/).
Ark UI does not provide a dedicated image primitive, so `Image` uses native `<img>` and `<picture>`
composition instead of an Ark part. See Ark's [Composition guide](https://ark-ui.com/docs/guides/composition)
for more on its native-element model.

## Basic

Image ships for React and Solid in both the CSS Modules and Tailwind packages. The rendered preview
on this page uses the React CSS Modules package; the framework tabs show native source for the same
public contract.

Use `Image` with a URL from a supported image CDN or any regular image URL. For the default
`layout="constrained"`, provide `width` and `height`. When Unpic recognizes the provider, it
generates responsive `srcset` and `sizes` attributes. Otherwise, it keeps the source URL unchanged.
The browser reserves space for the image before it loads.

<ExampleFrame variant="component">
  <ImageBasic />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/image/basic.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={imageSolidBasic} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

## Install with shadcn

See [Quick Start](/docs/quick-start) for full package setup, registry configuration, and styling guidance.

<Tabs groupId="framework">
  <Tab label="React" value="react">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-react/image' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-react-tailwind/image' },
  ]}
/>

  </Tab>
  <Tab label="Solid" value="solid">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-solid/image' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-solid-tailwind/image' },
  ]}
/>

  </Tab>
</Tabs>

## Anatomy

```text
Image / Image.Root
└─ img

picture (native)
├─ Image.Source
└─ Image / Image.Root
```

| Part                   | Role                                                                       |
| ---------------------- | -------------------------------------------------------------------------- |
| `Image` / `Image.Root` | Responsive native `<img>` element; Unpic creates `srcset`, `sizes`, and styles. |
| `Image.Source`         | Responsive `<source>` for a native `<picture>` element.                    |

## Composition

`Image` is the root and can be rendered directly. Use `Image.Source` only as a child of a native
`<picture>`, and keep `Image` last so it remains the fallback when no source is selected. For
format selection, place a typed source before an equivalent source without `type`; the browser
chooses the first format it supports.

## Examples

### Art Direction

Use `Image.Source` to show a wider office view on larger screens and a portrait fallback on smaller
screens. The first large-screen source requests AVIF; the next source provides a fallback for
browsers that do not support AVIF. `Image.Source` must stay inside a native `<picture>` element.

<ExampleFrame variant="component">
  <ImageArtDirection />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/image/art-direction.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={imageSolidArtDirection} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/image/image-art-direction.module.css"
```
</div>

</ExampleCode>

### Fixed

Use `layout="fixed"` when the image must keep exact dimensions in CSS pixels. Make sure its parent
has enough room, since fixed images do not shrink to fit a narrower container.

<ExampleFrame variant="component">
  <ImageFixed />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/image/fixed.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={imageSolidFixed} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Full Width

Use `layout="fullWidth"` for a hero image or media strip that should fill its container's width.
Supply `height` when the crop height must remain fixed.

<ExampleFrame variant="component">
  <ImageFullWidth />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/image/full-width.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={imageSolidFullWidth} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

## Styling

Use `className` in React or `class` in Solid on `Image` and `Image.Source` for local styles.
Tailwind variants use native utilities and consumer classes. The `--moduix-image-radius` variable
customizes the CSS Modules variants, while `data-slot` hooks are shared by both styling tracks.

### CSS Properties

<CssPropertiesSection properties={ImageDocs.imageCssProperties} />

### Styling hooks

- `data-slot="image-root"` targets the rendered `<img>` for styling.
- `data-slot="image-source"` targets the source node inside a native `<picture>`.
- `className` in React and `class` in Solid are the recommended ways to apply local styles.
