---
title: Clipboard
component: clipboard
description: Build accessible clipboards in React, Solid, or Vue with package and shadcn installation options.
---

import AsChildClipboardDemo from '@/components/examples/clipboard/as-child.tsx';
import ClipboardBasic from '@/components/examples/clipboard/basic.tsx';
import ContextClipboardDemo from '@/components/examples/clipboard/context.tsx';
import ClipboardStatusDemo from '@/components/examples/clipboard/copy-status.tsx';
import CustomCopyTextClipboardDemo from '@/components/examples/clipboard/custom-copy-text.tsx';
import DisabledClipboardDemo from '@/components/examples/clipboard/disabled.tsx';
import RootProviderClipboardDemo from '@/components/examples/clipboard/root-provider.tsx';
import TimeoutClipboardDemo from '@/components/examples/clipboard/timeout.tsx';
import ValueTextClipboardDemo from '@/components/examples/clipboard/value-text.tsx';
import * as ClipboardDocs from '@/components/examples/clipboard/css-properties';
import clipboardSolidAsChild from '../../../snippets/clipboard/solid/as-child.tsx?raw';
import clipboardSolidBasic from '../../../snippets/clipboard/solid/basic.tsx?raw';
import clipboardSolidContext from '../../../snippets/clipboard/solid/context.tsx?raw';
import clipboardSolidCopyStatus from '../../../snippets/clipboard/solid/copy-status.tsx?raw';
import clipboardSolidCustomCopyText from '../../../snippets/clipboard/solid/custom-copy-text.tsx?raw';
import clipboardSolidDisabled from '../../../snippets/clipboard/solid/disabled.tsx?raw';
import clipboardSolidRootProvider from '../../../snippets/clipboard/solid/root-provider.tsx?raw';
import clipboardSolidTimeout from '../../../snippets/clipboard/solid/timeout.tsx?raw';
import clipboardSolidValueText from '../../../snippets/clipboard/solid/value-text.tsx?raw';
import clipboardVueAsChild from '../../../snippets/clipboard/vue/as-child.vue?raw';
import clipboardVueBasic from '../../../snippets/clipboard/vue/basic.vue?raw';
import clipboardVueContext from '../../../snippets/clipboard/vue/context.vue?raw';
import clipboardVueCopyStatus from '../../../snippets/clipboard/vue/copy-status.vue?raw';
import clipboardVueCustomCopyText from '../../../snippets/clipboard/vue/custom-copy-text.vue?raw';
import clipboardVueDisabled from '../../../snippets/clipboard/vue/disabled.vue?raw';
import clipboardVueRootProvider from '../../../snippets/clipboard/vue/root-provider.vue?raw';
import clipboardVueTimeout from '../../../snippets/clipboard/vue/timeout.vue?raw';
import clipboardVueValueText from '../../../snippets/clipboard/vue/value-text.vue?raw';

## API Reference

<PrimitiveReference href="https://ark-ui.com/docs/components/clipboard" />

## Choosing the right component

Use `Clipboard` when users need to copy a text value. The value can be rendered in a read-only
input or as ordinary text, depending on which parts you use.

## Basic

Use `Clipboard` as the root. Put one value surface, `ClipboardInput` or `ClipboardValueText`, and
`ClipboardTrigger` inside `ClipboardControl`. `ClipboardLabel` is optional. The trigger can contain
`ClipboardIndicator`, whose children and `copied` content accept text as well as icons in React and
Solid. In Vue, use its default and named `copied` slots for the same content.

React, Solid, and Vue use the same flat value names. Import `Clipboard` as the root and import each
additional part directly, such as `ClipboardInput` and `ClipboardTrigger`. The same names work with
npm packages and shadcn registry source.

<ExampleFrame variant="component">
  <ClipboardBasic />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
      { label: 'Vue', value: 'vue' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/clipboard/basic.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={clipboardSolidBasic} />

    </Tab>
    <Tab value="vue">

<CodeBlockRuntime lang="vue" code={clipboardVueBasic} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/clipboard/component-basic.module.css"
```
</div>

</ExampleCode>

## Install with shadcn

See [Quick Start](/docs/quick-start) for full package setup, registry configuration, and styling guidance.

<Tabs groupId="framework">
  <Tab label="React" value="react">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-react/clipboard' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-react-tailwind/clipboard' },
  ]}
/>

  </Tab>
  <Tab label="Solid" value="solid">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-solid/clipboard' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-solid-tailwind/clipboard' },
  ]}
/>

  </Tab>
  <Tab label="Vue" value="vue">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-vue/clipboard' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-vue-tailwind/clipboard' },
  ]}
/>

  </Tab>
</Tabs>

## Anatomy

```text
Clipboard | ClipboardRootProvider
├─ ClipboardLabel (optional)
├─ ClipboardControl
│  ├─ ClipboardInput or ClipboardValueText
│  └─ ClipboardTrigger
│     └─ ClipboardIndicator (optional)
```

| Part | Role |
| --- | --- |
| `Clipboard` | Owns the value and the temporary copied state. |
| `ClipboardRootProvider` | Connects the parts to a store created with `useClipboard()`. |
| `ClipboardLabel` | Provides an accessible label for the value. |
| `ClipboardControl` | Groups the value surface and the copy button. |
| `ClipboardInput` | Renders the value in a read-only input. Use controlled state or `ClipboardContext.setValue()` to change it. |
| `ClipboardTrigger` | Copies the current value when activated. It renders a button by default. |
| `ClipboardIndicator` | Shows different content for the idle and copied states. Its defaults are the copy/check icons; pass `copied="Copied"` and children in React/Solid, or use the matching Vue slots. |
| `ClipboardValueText` | Renders the value as text when an input is not the right surface. |

## Composition

`Clipboard` owns the value and copied state. Keep one value surface (`ClipboardInput` or
`ClipboardValueText`) and `ClipboardTrigger` inside `ClipboardControl`; `ClipboardIndicator` is
optional in the trigger. It follows the same copied state, and moduix supplies the default
copy/check icons when its content is omitted. Pass `copied="Copied"` and children for text labels
in React and Solid; in Vue, use the default and named `copied` slots.

Use `defaultValue` for an uncontrolled value. When another part of the application owns the value
or needs to react to copying, use `value` with `onValueChange(details)` and
`onStatusChange(details)`; `timeout` controls how long the copied state remains active.

Use `useClipboard()` with `ClipboardRootProvider` only when the store must be created or
shared outside the rendered tree. Descendants can read that state through `ClipboardContext` or
`useClipboardContext()`. In Solid, `useClipboard()` returns an accessor, so pass it as
`value={clipboard}` and read state as `clipboard().copied` or `clipboard().value`. In Vue,
`useClipboard()` returns the reactive store directly, so pass it with `:value="clipboard"` and read
state as `clipboard.copied` or `clipboard.value`. The root has no disabled state: pass the native
`disabled` prop to both `ClipboardInput` and `ClipboardTrigger`.
Use `asChild` on those parts only when another semantic element should own the rendered DOM node.

## Examples

### Context

Read clipboard state from nested content with `ClipboardContext`, or use
`useClipboardContext()` in a child component. The context exposes `copied`, `value`, `setValue`, and
`copy`.

<ExampleFrame variant="component">
  <ContextClipboardDemo />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
      { label: 'Vue', value: 'vue' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/clipboard/context.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={clipboardSolidContext} />

    </Tab>
    <Tab value="vue">

<CodeBlockRuntime lang="vue" code={clipboardVueContext} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/clipboard/component-context.module.css"
```
</div>

</ExampleCode>

### Copy Status

Use `onStatusChange(details)` when another part of the interface should react to a successful copy.
The example keeps a count based on `details.copied`.

<ExampleFrame variant="component">
  <ClipboardStatusDemo />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
      { label: 'Vue', value: 'vue' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/clipboard/copy-status.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={clipboardSolidCopyStatus} />

    </Tab>
    <Tab value="vue">

<CodeBlockRuntime lang="vue" code={clipboardVueCopyStatus} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/clipboard/component-copy-status.module.css"
```
</div>

</ExampleCode>

### Custom copy text

Pass your own children or `copied` content to `ClipboardIndicator` when the button needs labels
that match your product.

<ExampleFrame variant="component">
  <CustomCopyTextClipboardDemo />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
      { label: 'Vue', value: 'vue' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/clipboard/custom-copy-text.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={clipboardSolidCustomCopyText} />

    </Tab>
    <Tab value="vue">

<CodeBlockRuntime lang="vue" code={clipboardVueCustomCopyText} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/clipboard/component-custom-copy-text.module.css"
```
</div>

</ExampleCode>

### Disabled

Pass `disabled` to both the input and the trigger when users may view the value but must not edit or
copy it.

<ExampleFrame variant="component">
  <DisabledClipboardDemo />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
      { label: 'Vue', value: 'vue' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/clipboard/disabled.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={clipboardSolidDisabled} />

    </Tab>
    <Tab value="vue">

<CodeBlockRuntime lang="vue" code={clipboardVueDisabled} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/clipboard/component-disabled.module.css"
```
</div>

</ExampleCode>

### Root Provider

Create the store with `useClipboard()` and pass it to `ClipboardRootProvider` when state
needs to be shared or created separately from the rendered component tree.

<ExampleFrame variant="component">
  <RootProviderClipboardDemo />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
      { label: 'Vue', value: 'vue' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/clipboard/root-provider.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={clipboardSolidRootProvider} />

    </Tab>
    <Tab value="vue">

<CodeBlockRuntime lang="vue" code={clipboardVueRootProvider} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/clipboard/component-root-provider.module.css"
```
</div>

</ExampleCode>

### Timeout

Set `timeout` when the copied state should remain visible for longer or shorter than the default
three seconds.

<ExampleFrame variant="component">
  <TimeoutClipboardDemo />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
      { label: 'Vue', value: 'vue' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/clipboard/timeout.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={clipboardSolidTimeout} />

    </Tab>
    <Tab value="vue">

<CodeBlockRuntime lang="vue" code={clipboardVueTimeout} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/clipboard/component-timeout.module.css"
```
</div>

</ExampleCode>

### Value Text

Use `ClipboardValueText` when the value should appear as text instead of inside an input field.

<ExampleFrame variant="component">
  <ValueTextClipboardDemo />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
      { label: 'Vue', value: 'vue' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/clipboard/value-text.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={clipboardSolidValueText} />

    </Tab>
    <Tab value="vue">

<CodeBlockRuntime lang="vue" code={clipboardVueValueText} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/clipboard/component-value-text.module.css"
```
</div>

</ExampleCode>

### Advanced Customization

Use `asChild` to let your own input and button elements render the Clipboard parts while preserving
the component's state and behavior.

<ExampleFrame variant="component">
  <AsChildClipboardDemo />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
      { label: 'Vue', value: 'vue' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/clipboard/as-child.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={clipboardSolidAsChild} />

    </Tab>
    <Tab value="vue">

<CodeBlockRuntime lang="vue" code={clipboardVueAsChild} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/clipboard/component-advanced-customization.module.css"
```
</div>

</ExampleCode>

## Styling

Use `className` in React or `class` in Solid and Vue on exported parts for structural styles. Tailwind
variants use native utilities and consumer classes; the CSS Modules variants use the CSS variables
below. For copied-state styles, use Ark's `data-copied` attribute together with moduix's
`data-slot` hooks.

### CSS Properties

<CssPropertiesSection properties={ClipboardDocs.clipboardOverrideCssProperties} />

### Styling hooks

- `Clipboard` -> `data-slot="clipboard-root"`
- `ClipboardRootProvider` -> `data-slot="clipboard-root-provider"`
- `ClipboardLabel` -> `data-slot="clipboard-label"`
- `ClipboardControl` -> `data-slot="clipboard-control"`
- `ClipboardInput` -> `data-slot="clipboard-input"`
- `ClipboardTrigger` -> `data-slot="clipboard-trigger"`
- `ClipboardIndicator` -> `data-slot="clipboard-indicator"`
- `ClipboardValueText` -> `data-slot="clipboard-value-text"`
- Ark adds `data-copied` to `Clipboard`, `ClipboardLabel`, `ClipboardControl`, `ClipboardInput`, and
  `ClipboardTrigger` while the copied state is active.
- Ark adds `data-readonly` to `ClipboardInput` when the input is read-only.
- `ClipboardIndicator` adds `data-slot="clipboard-indicator-idle-icon"` and
  `data-slot="clipboard-indicator-copied-icon"` to its default icon nodes.
