---
title: Astro
description: Integrate moduix React or Solid components with Astro through packages or shadcn, including styles, aliases, and client directives.
---

# Astro

This guide assumes an existing Astro project with its official React or Solid integration already
configured. Components render as static HTML by default; add a `client:*` directive when they need
to become interactive in the browser.


## Choose your setup

- **Runtime:** React or Solid with the matching official Astro integration.
- **Styling:** CSS Modules or Tailwind CSS v4.
- **Distribution:** a package or source copied with the shadcn CLI.

This page focuses on Astro-specific file placement and island hydration. Complete the shared
[package setup](/docs/quick-start#install-as-a-package) or
[shadcn setup](/docs/quick-start#install-with-the-shadcn-cli) for your styling track first. The
examples below use CSS Modules package imports; the Astro entry points and `client:*` rules are the
same with the matching Tailwind package or registry.

The package setup below uses CSS Modules and includes the moduix reset. If you use the Tailwind
registry commands instead, keep the global stylesheet from
[Quick Start](/docs/quick-start#install-as-a-package) configured in your application.

## Install as a package

Install the framework-native package and its Ark UI peer dependency:

<Tabs groupId="framework">
  <Tab label="React" value="react">

<PackageManagerTabs command="install @moduix/react @ark-ui/react" />

  </Tab>
  <Tab label="Solid" value="solid">

<PackageManagerTabs command="install @moduix/solid @ark-ui/solid" />

  </Tab>
</Tabs>

Import the foundation stylesheet once in a shared layout:

<Tabs groupId="framework">
  <Tab label="React" value="react">

```astro title="src/layouts/Layout.astro"
---
import '@moduix/react/reset.css';
import '@moduix/react/style.css';
---

<html lang="en">
  <body><slot /></body>
</html>
```

  </Tab>
  <Tab label="Solid" value="solid">

```astro title="src/layouts/Layout.astro"
---
import '@moduix/solid/reset.css';
import '@moduix/solid/style.css';
---

<html lang="en">
  <body><slot /></body>
</html>
```

  </Tab>
</Tabs>

Create a framework component:

<Tabs groupId="framework">
  <Tab label="React" value="react">

```tsx title="src/components/ContinueButton.tsx"
import { Button } from '@moduix/react/button';

export function ContinueButton() {
  return <Button>Continue</Button>;
}
```

  </Tab>
  <Tab label="Solid" value="solid">

```tsx title="src/components/ContinueButton.tsx"
import { Button } from '@moduix/solid/button';

export function ContinueButton() {
  return <Button>Continue</Button>;
}
```

  </Tab>
</Tabs>

Import it directly into an Astro page and add a client directive:

```astro title="src/pages/index.astro"
---
import { ContinueButton } from '../components/ContinueButton';
---

<ContinueButton client:load />
```

Without a `client:*` directive, Astro sends static HTML without the component's client-side
JavaScript. Use `client:load` for immediately visible controls, `client:idle` when interaction can
wait, and `client:visible` for components below the fold. Interactive components such as `Dialog`,
`Menu`, and `Select` need a client directive.

## Install with the shadcn CLI

Complete the [shadcn CLI setup](/docs/quick-start#install-with-the-shadcn-cli), map `@/*` to `src/*`
in `tsconfig.json`, and import `@/lib/moduix/styles/style.css` from the shared layout. Then add the
component for your runtime:

<Tabs groupId="framework">
  <Tab label="React" value="react">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-react/button' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-react-tailwind/button' },
  ]}
/>

  </Tab>
  <Tab label="Solid" value="solid">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-solid/button' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-solid-tailwind/button' },
  ]}
/>

  </Tab>
</Tabs>

Import the generated component directly into the `.astro` file before applying a `client:*`
directive. Astro only accepts client directives on framework components imported directly into the
page.

## Verify

The page should contain styled server-rendered markup before hydration, then respond after the client
directive runs.

See Astro's [framework components guide](https://docs.astro.build/en/guides/framework-components/),
plus the official [React](https://docs.astro.build/en/guides/integrations-guide/react/) and
[Solid](https://docs.astro.build/en/guides/integrations-guide/solid-js/) integration guides.
