---
title: TanStack Start
description: Install moduix in TanStack Start for React or Solid with package and shadcn workflows.
---

# TanStack Start

TanStack Start has native React and Solid runtimes and can build with Vite or Rsbuild. It renders the
route tree on the server and hydrates it in the browser; moduix does not require a `use client`
directive in the standard setup.

The examples below use the CSS Modules packages, which contain the full component catalog. For a
component available in the Tailwind track, follow the [Tailwind package setup](/docs/quick-start#tailwind-package-setup)
and choose the matching React or Solid Tailwind package or registry.

## Install as a package

Install the package that matches your Start runtime:

<Tabs groupId="framework">
  <Tab label="React" value="react">

<PackageManagerTabs command="install @moduix/react @ark-ui/react" />

  </Tab>
  <Tab label="Solid" value="solid">

<PackageManagerTabs command="install @moduix/solid @ark-ui/solid solid-js" />

  </Tab>
</Tabs>

Import the foundation stylesheet once from `src/routes/__root.tsx`:

<Tabs groupId="framework">
  <Tab label="React" value="react">

```tsx title="src/routes/__root.tsx"
import '@moduix/react/style.css';
```

  </Tab>
  <Tab label="Solid" value="solid">

```tsx title="src/routes/__root.tsx"
import '@moduix/solid/style.css';
```

  </Tab>
</Tabs>

Use a component in a route:

<Tabs groupId="framework">
  <Tab label="React" value="react">

```tsx title="src/routes/index.tsx"
import { createFileRoute } from '@tanstack/react-router';
import { Button } from '@moduix/react/button';

export const Route = createFileRoute('/')({
  component: Home,
});

function Home() {
  return <Button>Continue</Button>;
}
```

  </Tab>
  <Tab label="Solid" value="solid">

```tsx title="src/routes/index.tsx"
import { createFileRoute } from '@tanstack/solid-router';
import { Button } from '@moduix/solid/button';

export const Route = createFileRoute('/')({
  component: Home,
});

function Home() {
  return <Button>Continue</Button>;
}
```

  </Tab>
</Tabs>

## Install with the shadcn CLI

Complete the [shadcn CLI setup](/docs/quick-start#install-with-the-shadcn-cli). If the starter does
not already map `@/*` to `src/*`, add the alias to the root `tsconfig.json`:

```json title="tsconfig.json"
{
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "@/*": ["./src/*"]
    }
  }
}
```

Make the same alias available to the selected bundler. Rsbuild reads the TypeScript paths directly;
for Vite, follow TanStack Start's path-alias guide. Then add the component:

<Tabs groupId="framework">
  <Tab label="React" value="react">

<PackageManagerTabs command="shadcn@latest add @moduix-react/button" dlx />

  </Tab>
  <Tab label="Solid" value="solid">

<PackageManagerTabs command="shadcn@latest add @moduix-solid/button" dlx />

  </Tab>
</Tabs>

Replace the package stylesheet with the generated stylesheet in the root route:

```tsx title="src/routes/__root.tsx"
import '@/lib/moduix/styles/style.css';
```

## Verify

Open `/` after starting the development server. The initial server-rendered page should include
moduix styles, hydrate without a mismatch, and remain interactive. Keep browser-only APIs and values
that may differ between server and client out of the server render.

Continue with the official TanStack Start guides for
[React](https://tanstack.com/start/latest/docs/framework/react/getting-started) or
[Solid](https://tanstack.com/start/latest/docs/framework/solid/getting-started).
