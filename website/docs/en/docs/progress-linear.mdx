---
title: Progress Linear
component: progress-linear
description: A linear progress indicator for tasks with known or unknown progress.
---

import * as ProgressLinearDocs from '@/components/examples/progress-linear/css-properties';
import ProgressLinearBasic from '@/components/examples/progress-linear/basic.tsx';
import ProgressLinearControlled from '@/components/examples/progress-linear/controlled.tsx';
import ProgressLinearIndeterminate from '@/components/examples/progress-linear/indeterminate.tsx';
import ProgressLinearInitialValue from '@/components/examples/progress-linear/initial-value.tsx';
import ProgressLinearMinMax from '@/components/examples/progress-linear/min-max.tsx';
import ProgressLinearStateViews from '@/components/examples/progress-linear/state-views.tsx';
import ProgressLinearValueText from '@/components/examples/progress-linear/value-text.tsx';
import ProgressLinearVertical from '@/components/examples/progress-linear/vertical.tsx';
import ProgressLinearRootProvider from '@/components/examples/progress-linear/root-provider.tsx';
import progressLinearSolidBasic from '../../../snippets/progress-linear/solid/basic.tsx?raw';
import progressLinearSolidControlled from '../../../snippets/progress-linear/solid/controlled.tsx?raw';
import progressLinearSolidIndeterminate from '../../../snippets/progress-linear/solid/indeterminate.tsx?raw';
import progressLinearSolidInitialValue from '../../../snippets/progress-linear/solid/initial-value.tsx?raw';
import progressLinearSolidMinMax from '../../../snippets/progress-linear/solid/min-max.tsx?raw';
import progressLinearSolidStateViews from '../../../snippets/progress-linear/solid/state-views.tsx?raw';
import progressLinearSolidValueText from '../../../snippets/progress-linear/solid/value-text.tsx?raw';
import progressLinearSolidVertical from '../../../snippets/progress-linear/solid/vertical.tsx?raw';
import progressLinearSolidRootProvider from '../../../snippets/progress-linear/solid/root-provider.tsx?raw';

## API Reference

<PrimitiveReference href="https://ark-ui.com/docs/components/progress-linear" />

## Choosing the right component

Use `ProgressLinear` when users need to follow progress along a bar. Use `ProgressCircular` for a
compact status indicator in a card, toolbar, or other space-constrained surface.

## Basic

`ProgressLinear` is available for React and Solid in both the CSS Modules and Tailwind packages. The
preview on this page uses the React CSS Modules package; the framework tabs show native source for the
same public contract.

Compose the progress bar from the explicit `Label`, `ValueText`, `Track`, and `Range` parts. Give
`Track` an accessible name that describes the task.

<ExampleFrame variant="component">
  <ProgressLinearBasic />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/progress-linear/basic.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={progressLinearSolidBasic} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

## Install with shadcn

See [Quick Start](/docs/quick-start) for full package setup, registry configuration, and styling guidance.

<Tabs groupId="framework">
  <Tab label="React" value="react">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-react/progress-linear' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-react-tailwind/progress-linear' },
  ]}
/>

  </Tab>
  <Tab label="Solid" value="solid">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-solid/progress-linear' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-solid-tailwind/progress-linear' },
  ]}
/>

  </Tab>
</Tabs>

## Anatomy

```text
ProgressLinear / ProgressLinear.Root
├─ ProgressLinear.Label
├─ ProgressLinear.ValueText
├─ ProgressLinear.Track
│  └─ ProgressLinear.Range
└─ ProgressLinear.View

ProgressLinear.RootProvider
└─ the same part tree connected to ProgressLinear.useProgress()
```

| Part                                     | Role                                                                       |
| ---------------------------------------- | -------------------------------------------------------------------------- |
| `ProgressLinear` / `ProgressLinear.Root` | Owns progress state and passes formatting, IDs, and other props to Ark.    |
| `ProgressLinear.RootProvider`             | Connects parts to the store returned by `ProgressLinear.useProgress()`.    |
| `ProgressLinear.Context`                 | Provides the current progress state to a render prop.                     |
| `ProgressLinear.Label`                   | Visible task label. It is not automatically used as `Track`'s accessible name. |
| `ProgressLinear.ValueText`                | Displays the formatted percentage and announces changes. Use `Context` for custom text. |
| `ProgressLinear.Track`                   | Progressbar surface. Ark adds the role and ARIA value attributes.         |
| `ProgressLinear.Range`                   | Filled portion that reflects the current progress.                        |
| `ProgressLinear.View`                    | Shows content only when its `state` matches the current progress state.   |

## Composition

Use `defaultValue` for uncontrolled progress. Use `value` with `onValueChange(details)` when your
application owns the current value, and use `defaultValue={null}` or `value={null}` when progress is
unknown. If state is created with `ProgressLinear.useProgress()`, render the parts inside
`ProgressLinear.RootProvider` instead of `ProgressLinear` or `ProgressLinear.Root`. Use
`ProgressLinear.View` for state-specific content and `ProgressLinear.Context` when custom visible text
needs the current state.

## Examples

### Controlled

Keep `value` in state when progress needs to stay in sync with another control or an external task.

<ExampleFrame variant="component">
  <ProgressLinearControlled />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/progress-linear/controlled.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={progressLinearSolidControlled} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Indeterminate

Set `defaultValue={null}` or `value={null}` when the task is running but its completion cannot be
measured yet.

<ExampleFrame variant="component">
  <ProgressLinearIndeterminate />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/progress-linear/indeterminate.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={progressLinearSolidIndeterminate} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Initial Value

Use `defaultValue` to set the initial value for uncontrolled progress.

<ExampleFrame variant="component">
  <ProgressLinearInitialValue />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/progress-linear/initial-value.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={progressLinearSolidInitialValue} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Min and Max

Use `min` and `max` when the progress range is different from the default range of `0` to `100`.

<ExampleFrame variant="component">
  <ProgressLinearMinMax />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/progress-linear/min-max.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={progressLinearSolidMinMax} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### State Views

Use `ProgressLinear.View` when supporting text or UI should change with the Ark progress state.

<ExampleFrame variant="component">
  <ProgressLinearStateViews />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/progress-linear/state-views.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={progressLinearSolidStateViews} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Value Text

Use `ProgressLinear.Context` with `translations.value(details)` when visible value text needs
task-specific wording.

<ExampleFrame variant="component">
  <ProgressLinearValueText />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/progress-linear/value-text.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={progressLinearSolidValueText} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Vertical

Set `orientation="vertical"` when the range should fill from bottom to top. Vertical progress uses
a default height of `12rem`; override `--moduix-progress-linear-height` when your layout needs a
different height.

<ExampleFrame variant="component">
  <ProgressLinearVertical />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/progress-linear/vertical.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={progressLinearSolidVertical} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Advanced Customization

Create the progress state with `ProgressLinear.useProgress()` in a parent component, then connect it
to the rendered parts with `ProgressLinear.RootProvider`.

<ExampleFrame variant="component">
  <ProgressLinearRootProvider />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/progress-linear/root-provider.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={progressLinearSolidRootProvider} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

## Styling

Use `className` in React or `class` in Solid on exported parts for structural styling. For state-based
styles, use Ark data attributes and moduix `data-slot` hooks. Tailwind variants use native utilities
and consumer classes; the variables below customize the CSS Modules variants.

The root has a default width of `12rem`. In vertical mode, its default height is also `12rem`. Use
the public `--moduix-progress-linear-*` variables to adapt the component to your layout.

### CSS Properties

<div className="docs-reference">
  <Tabs
    values={[{ label: 'CSS Variables', value: 'CSS Variables' }]}
    className="docs-reference-tabs"
    contentItemClassName="docs-reference-panel"
  >
    <Tab>
      <ProgressLinearDocs.ProgressLinearCssPropertiesPanel />
    </Tab>
  </Tabs>
</div>

### Styling Hooks

- `data-slot="progress-linear-root"`, `progress-linear-root-provider`, `progress-linear-label`,
  `progress-linear-value-text`, `progress-linear-track`, `progress-linear-range`, and
  `progress-linear-view`.
- Ark hooks: `data-scope="progress"`, `data-part`, `data-state="indeterminate|loading|complete"`,
  `data-orientation="horizontal|vertical"`, `data-value`, and `data-max`.
