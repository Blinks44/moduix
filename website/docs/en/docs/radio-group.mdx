---
title: Radio Group
component: radio-group
description: A radio group for choosing exactly one option from a visible set.
---

import * as RadioGroupDocs from '@/components/examples/radio-group/css-properties';
import RadioGroupAdvancedCustomization from '@/components/examples/radio-group/advanced-customization.tsx';
import RadioGroupAsChild from '@/components/examples/radio-group/aschild.tsx';
import RadioGroupBasic from '@/components/examples/radio-group/basic.tsx';
import RadioGroupControlled from '@/components/examples/radio-group/controlled.tsx';
import RadioGroupDisabled from '@/components/examples/radio-group/disabled.tsx';
import RadioGroupDisabledItem from '@/components/examples/radio-group/item-disabled.tsx';
import RadioGroupForm from '@/components/examples/radio-group/form.tsx';
import RadioGroupIndicator from '@/components/examples/radio-group/indicator.tsx';
import RadioGroupInitialValue from '@/components/examples/radio-group/initial-value.tsx';
import RadioGroupOrientation from '@/components/examples/radio-group/orientation.tsx';
import RadioGroupRootProvider from '@/components/examples/radio-group/root-provider.tsx';
import RadioGroupSizes from '@/components/examples/radio-group/sizes.tsx';
import RadioGroupField from '@/components/examples/radio-group/with-field.tsx';
import RadioGroupFieldset from '@/components/examples/radio-group/with-fieldset.tsx';

Use `RadioGroup` when people need to choose exactly one option from a visible set, such as a
framework, plan, or account type. Use `Checkbox` for independent choices and `Switch` for a setting
that takes effect immediately.

## API Reference

`RadioGroup` follows the Ark UI radio-group primitive and adds moduix styling, stable `data-slot`
attributes, and the `Option` convenience component. The component is exported from
`@moduix/react/radio-group`.

<PrimitiveReference href="https://ark-ui.com/docs/components/radio-group" />

## Basic

For most groups, use `RadioGroup.Option` to render a labelled item with a control and visible text.
`RadioGroup.Option` includes the native input in its fixed composition. When composing
`RadioGroup.Item` directly, add `RadioGroup.ItemHiddenInput` explicitly.

<ExampleFrame variant="component">
  <RadioGroupBasic />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/radio-group/basic.tsx"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

## Install with shadcn

<ShadcnInstall packageName="radio-group" />

## Anatomy

```text
RadioGroup.Root
├─ RadioGroup.Label
├─ RadioGroup.Indicator
└─ RadioGroup.Item[value]
   ├─ RadioGroup.ItemControl
   ├─ RadioGroup.ItemText
   └─ RadioGroup.ItemHiddenInput (explicit)

RadioGroup.RootProvider
└─ RadioGroup.* + useRadioGroup()

RadioGroup.Option
└─ RadioGroup.Item + ItemControl + ItemText + ItemHiddenInput (fixed composition)
```

| Part                           | Role                                                        |
| ------------------------------ | ----------------------------------------------------------- |
| `RadioGroup` / `.Root`         | Owns value state, keyboard navigation, orientation, and forms. |
| `RadioGroup.RootProvider`      | Connects parts to state created with `useRadioGroup()`.    |
| `RadioGroup.Label`             | Accessible label for the group.                             |
| `RadioGroup.Item`              | One selectable option; renders a `label` by default.        |
| `RadioGroup.Option`            | Convenience item with a control, text, and hidden input.   |
| `RadioGroup.ItemControl`       | Visual radio control; supports the moduix `size` prop.      |
| `RadioGroup.ItemText`          | Visible option text.                                        |
| `RadioGroup.Indicator`         | Optional active-item indicator using Ark measurement vars.  |

## Composition

### Recommended composition

`RadioGroup` is the short form of `RadioGroup.Root`. Use `RadioGroup.Option` for the common labelled
item. It accepts the Ark item props, the moduix `size` prop, and `className`. The class is applied to
the underlying `Item`, so nested parts remain targetable through their `data-slot` hooks.

### When to go custom

Use `RadioGroup.RootProvider` with `useRadioGroup()` from `@moduix/react` when the state must be
created before the group renders, shared with controls outside the rendered tree, or read by a
separate part of the interface. Pass the returned store to `RootProvider` and do not render `Root`
and `RootProvider` for the same state instance.

Use the explicit `Item` tree when nested parts need their own placement or class names, or when
`RadioGroup.Item asChild` must let another element own the item DOM node. The direct child of an
`Item` using `asChild` must still be a semantic `label`.

For advanced state reads, use `RadioGroup.Context`, `RadioGroup.ItemContext`,
`useRadioGroupContext()`, or `useRadioGroupItemContext()` from `@moduix/react`.

### Default props

| Prop                             | Default | Notes                                                   |
| -------------------------------- | ------- | ------------------------------------------------------- |
| `RadioGroup` / `RadioGroup.Root` | -       | Ark defaults apply; moduix adds no root behavior defaults. |
| `RadioGroup.Option size`         | `md`    | Control diameter and indicator-dot scale.               |
| `RadioGroup.ItemControl size`    | `md`    | Moduix control diameter and indicator-dot scale.        |

### Behavioral notes

`RadioGroup.Root` accepts Ark props such as `value`, `defaultValue`,
`onValueChange(details)`, `name`, `form`, `orientation`, `disabled`, `invalid`, `readOnly`,
`required`, `ids`, and `asChild`. In the callback, read the selected value from `details.value`.

`RadioGroup.Option` includes its native input. Direct `RadioGroup.Item` composition requires
`RadioGroup.ItemHiddenInput` for native submission, validation, and reset. Set `name` on the root
when the selected value should be included in `FormData`.

Ark manages the controlled and uncontrolled value state, roving focus, WAI-ARIA behavior, and
keyboard navigation. <kbd>Tab</kbd> moves focus to the checked item, or to the first item when none
is checked. <kbd>Space</kbd> checks the focused item, and the arrow keys move to and check the next
or previous item.

`Field.Root` provides a visible label, helper text, and error text, but it does not pass its state to
`RadioGroup`. Pass `disabled`, `invalid`, `readOnly`, and `required` to the group itself.
`Fieldset.Root` passes its Ark `disabled` and `invalid` state to nested radio groups.

Every styled part forwards its ref to the corresponding Ark DOM part, including `ItemHiddenInput`.

## Examples

### asChild

Use `RadioGroup.Item asChild` when a custom card or label layout should own the item DOM node. Keep a
semantic `label` as the direct child.

<ExampleFrame variant="component">
  <RadioGroupAsChild />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/radio-group/aschild.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/radio-group/radio-group-aschild.module.css"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Controlled

Use `value` with `onValueChange(details)` when the selected value is owned by React state or must
stay in sync with other parts of the interface.

<ExampleFrame variant="component">
  <RadioGroupControlled />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/radio-group/controlled.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/radio-group/radio-group-controlled.module.css"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Disabled

Set `disabled` on the root to make every item unavailable while keeping the selected value visible.

<ExampleFrame variant="component">
  <RadioGroupDisabled />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/radio-group/disabled.tsx"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Disabled Item

Set `disabled` on an individual `Item` or `Option` when only that choice is unavailable.

<ExampleFrame variant="component">
  <RadioGroupDisabledItem />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/radio-group/item-disabled.tsx"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Form

Pass `name` to the root when the selected value should be submitted. `RadioGroup.Option` already
contains the input; add `ItemHiddenInput` to every custom item.

<ExampleFrame variant="component">
  <RadioGroupForm />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/radio-group/form.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/radio-group/radio-group-form.module.css"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Indicator

Render `RadioGroup.Indicator` when the active item needs an additional measured highlight. Keep the
indicator and the radio items in the same positioning context, and keep unrelated content outside it,
so the indicator follows the item geometry.

<ExampleFrame variant="component">
  <RadioGroupIndicator />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/radio-group/indicator.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/radio-group/radio-group-indicator.module.css"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Initial Value

Set `defaultValue` when the initial checked item is known and the group should manage later changes
itself.

<ExampleFrame variant="component">
  <RadioGroupInitialValue />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/radio-group/initial-value.tsx"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Orientation

Set `orientation="horizontal"` when arrow-key behavior should follow a horizontal group. Arrange
the items in a row with an inline wrapper when the visual layout should be horizontal too.

<ExampleFrame variant="component">
  <RadioGroupOrientation />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/radio-group/orientation.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/radio-group/radio-group-orientation.module.css"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Root Provider

Use `RootProvider` when a `useRadioGroup()` store needs to be shared with controls outside the radio
group tree. The example also shows how to update the selected value from outside the group.

<ExampleFrame variant="component">
  <RadioGroupRootProvider />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/radio-group/root-provider.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/radio-group/radio-group-root-provider.module.css"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Sizes

Use the moduix `size` prop on `ItemControl` or `Option` when the radio control needs a different
diameter. Supported values are `xs`, `sm`, `md`, `lg`, and `xl`.

<ExampleFrame variant="component">
  <RadioGroupSizes />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/radio-group/sizes.tsx"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### With Field

Use `Field.Root` when a radio group needs a field-level label, helper text, or error message. Pass
`invalid` to `RadioGroup` as well, because `Field.Root` does not set the group's validation state.

<ExampleFrame variant="component">
  <RadioGroupField />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/radio-group/with-field.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/radio-group/radio-group-with-field.module.css"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### With Fieldset

Wrap the group in `Fieldset` when several radio items share a legend and belong to the same form
section. `Fieldset` can also provide its disabled and invalid state to the nested group.

<ExampleFrame variant="component">
  <RadioGroupFieldset />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/radio-group/with-fieldset.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/radio-group/radio-group-with-fieldset.module.css"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Advanced Customization

Bypass `Option` and use the full `Item` tree when each nested part needs custom placement, props, or
styling.

<ExampleFrame variant="component">
  <RadioGroupAdvancedCustomization />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/radio-group/advanced-customization.tsx"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

## Styling

Style `RadioGroup` through `className`, stable `data-slot="radio-group-*"` hooks, Ark
`data-scope`, `data-part`, and `data-state` attributes, and the public `--moduix-radio-*` CSS
variables below.

### CSS Properties

<div className="docs-reference">
  <Tabs
    values={[{ label: 'CSS Variables', value: 'CSS Variables' }]}
    className="docs-reference-tabs"
    contentItemClassName="docs-reference-panel"
  >
    <Tab>
      <RadioGroupDocs.RadioGroupCssPropertiesPanel />
    </Tab>
  </Tabs>
</div>

### Styling hooks

- Ark anatomy hooks: `data-scope="radio-group"` and `data-part` on the root, label, item,
  item-control, item-text, item-hidden-input, and indicator.
- Ark state hooks: `data-state="checked|unchecked"`, `data-disabled`, `data-readonly`,
  `data-invalid`, `data-required`, `data-focus`, `data-focus-visible`, `data-hover`,
  `data-active`, and `data-orientation`.
- Ark indicator variables: `--transition-property`, `--left`, `--top`, `--width`, and `--height`.
- moduix hooks: `data-slot="radio-group-root"`, `radio-group-root-provider`, `radio-group-label`,
  `radio-group-item`, `radio-group-item-control`, `radio-group-item-text`, `radio-group-item-hidden-input`, and
  `radio-group-indicator`.
