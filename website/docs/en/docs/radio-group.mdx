---
title: Radio Group
component: radio-group
description: A radio group for choosing exactly one option from a visible set.
---

import * as RadioGroupDocs from '@/components/examples/radio-group/css-properties';
import RadioGroupAdvancedCustomization from '@/components/examples/radio-group/advanced-customization.tsx';
import RadioGroupAsChild from '@/components/examples/radio-group/aschild.tsx';
import RadioGroupBasic from '@/components/examples/radio-group/basic.tsx';
import RadioGroupControlled from '@/components/examples/radio-group/controlled.tsx';
import RadioGroupDisabled from '@/components/examples/radio-group/disabled.tsx';
import RadioGroupDisabledItem from '@/components/examples/radio-group/item-disabled.tsx';
import RadioGroupForm from '@/components/examples/radio-group/form.tsx';
import RadioGroupIndicator from '@/components/examples/radio-group/indicator.tsx';
import RadioGroupInitialValue from '@/components/examples/radio-group/initial-value.tsx';
import RadioGroupOrientation from '@/components/examples/radio-group/orientation.tsx';
import RadioGroupRootProvider from '@/components/examples/radio-group/root-provider.tsx';
import RadioGroupSizes from '@/components/examples/radio-group/sizes.tsx';
import RadioGroupField from '@/components/examples/radio-group/with-field.tsx';
import RadioGroupFieldset from '@/components/examples/radio-group/with-fieldset.tsx';
import radioGroupSolidAdvancedCustomization from '../../../snippets/radio-group/solid/advanced-customization.tsx?raw';
import radioGroupSolidAsChild from '../../../snippets/radio-group/solid/aschild.tsx?raw';
import radioGroupSolidBasic from '../../../snippets/radio-group/solid/basic.tsx?raw';
import radioGroupSolidControlled from '../../../snippets/radio-group/solid/controlled.tsx?raw';
import radioGroupSolidDisabled from '../../../snippets/radio-group/solid/disabled.tsx?raw';
import radioGroupSolidDisabledItem from '../../../snippets/radio-group/solid/item-disabled.tsx?raw';
import radioGroupSolidForm from '../../../snippets/radio-group/solid/form.tsx?raw';
import radioGroupSolidIndicator from '../../../snippets/radio-group/solid/indicator.tsx?raw';
import radioGroupSolidInitialValue from '../../../snippets/radio-group/solid/initial-value.tsx?raw';
import radioGroupSolidOrientation from '../../../snippets/radio-group/solid/orientation.tsx?raw';
import radioGroupSolidRootProvider from '../../../snippets/radio-group/solid/root-provider.tsx?raw';
import radioGroupSolidSizes from '../../../snippets/radio-group/solid/sizes.tsx?raw';
import radioGroupSolidField from '../../../snippets/radio-group/solid/with-field.tsx?raw';
import radioGroupSolidFieldset from '../../../snippets/radio-group/solid/with-fieldset.tsx?raw';

## API Reference

`RadioGroup` follows the Ark UI radio-group primitive and adds moduix styling, stable `data-slot`
attributes, and the `RadioGroupOption` convenience component. It is exported from
`@moduix/react/radio-group` and `@moduix/solid/radio-group`.

<PrimitiveReference href="https://ark-ui.com/docs/components/radio-group" />

## Choosing the right component

Use `RadioGroup` when people need to choose exactly one option from a visible set, such as a
framework, plan, or account type. Use `Checkbox` for independent choices and `Switch` for a setting
that takes effect immediately.

## Basic


Use `RadioGroup` as the root and give every option a value. `RadioGroupOption` renders a labelled
item with its control, text, and native input.

<ExampleFrame variant="component">
  <RadioGroupBasic />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/radio-group/basic.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={radioGroupSolidBasic} />

    </Tab>
  </Tabs>
</div>
</ExampleCode>

## Install with shadcn

See [Quick Start](/docs/quick-start) for full package setup, registry configuration, and styling guidance.

<Tabs groupId="framework">
  <Tab label="React" value="react">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-react/radio-group' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-react-tailwind/radio-group' },
  ]}
/>

  </Tab>
  <Tab label="Solid" value="solid">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-solid/radio-group' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-solid-tailwind/radio-group' },
  ]}
/>

  </Tab>
</Tabs>

## Anatomy

```text
RadioGroup
├─ RadioGroupLabel
├─ RadioGroupIndicator (optional)
└─ RadioGroupItem[value]
   ├─ RadioGroupItemControl
   ├─ RadioGroupItemText
   └─ RadioGroupItemHiddenInput (explicit)

RadioGroupRootProvider
└─ the same item tree connected to useRadioGroup()

RadioGroupOption
└─ RadioGroupItem + RadioGroupItemControl + RadioGroupItemText + RadioGroupItemHiddenInput (fixed composition)
```

| Part                         | Role                                                            |
| ---------------------------- | --------------------------------------------------------------- |
| `RadioGroup`       | Owns value state, keyboard navigation, orientation, and forms. |
| `RadioGroupRootProvider`    | Connects parts to state created with `useRadioGroup()`.         |
| `RadioGroupLabel`           | Accessible label for the group.                                 |
| `RadioGroupItem`            | One selectable option; renders a `label` by default.            |
| `RadioGroupOption`          | Convenience item with a control, text, and hidden input.        |
| `RadioGroupItemControl`     | Visual radio control; supports the moduix `size` prop.          |
| `RadioGroupItemText`        | Visible option text.                                             |
| `RadioGroupItemHiddenInput` | Native form control for explicit `RadioGroupItem` composition.             |
| `RadioGroupIndicator`       | Optional active-item indicator using Ark measurement variables.  |

## Composition

Use `defaultValue` for an uncontrolled group. Use `value` with `onValueChange(details)` when another
part of the application owns the selected value. When state must be created outside the rendered
tree, use `RadioGroupRootProvider` with `useRadioGroup()` from the matching framework package. Do not render
`RadioGroup` and `RadioGroupRootProvider` for the same state instance. React reads the store as an
object; Solid reads its reactive state through the returned accessor.

`RadioGroupOption` includes `RadioGroupItemHiddenInput` in its fixed composition. When composing
`RadioGroupItem` directly, add `RadioGroupItemHiddenInput` yourself. Use the explicit item tree
when nested parts need custom placement or when `asChild` should let another element own the item
DOM node. In Solid, `asChild` uses a render function and the direct child must remain a semantic
`label`.

`Field` provides a visible label, helper text, and error text, but it does not pass its state to
the group. Pass `disabled`, `invalid`, `readOnly`, and `required` to `RadioGroup` itself.
`Fieldset` passes its Ark `disabled` and `invalid` state to nested radio groups.

## Examples

### asChild

Use `RadioGroupItem asChild` when a custom card or label layout should own the item DOM node. Keep a
semantic `label` as the direct child.

<ExampleFrame variant="component">
  <RadioGroupAsChild />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/radio-group/aschild.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={radioGroupSolidAsChild} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/radio-group/radio-group-aschild.module.css"
```
</div>
</ExampleCode>

### Controlled

Manage `value` with `useState` in React or `createSignal` in Solid when the selected value needs to
stay in sync with routing, filters, or surrounding UI.

<ExampleFrame variant="component">
  <RadioGroupControlled />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/radio-group/controlled.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={radioGroupSolidControlled} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/radio-group/radio-group-controlled.module.css"
```
</div>
</ExampleCode>

### Disabled

Set `disabled` on the root to make every item unavailable while keeping the selected value visible.

<ExampleFrame variant="component">
  <RadioGroupDisabled />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/radio-group/disabled.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={radioGroupSolidDisabled} />

    </Tab>
  </Tabs>
</div>
</ExampleCode>

### Disabled Item

Set `disabled` on an individual `RadioGroupItem` or `RadioGroupOption` when only that choice is unavailable.

<ExampleFrame variant="component">
  <RadioGroupDisabledItem />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/radio-group/item-disabled.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={radioGroupSolidDisabledItem} />

    </Tab>
  </Tabs>
</div>
</ExampleCode>

### Form

Pass `name` to the root when the selected value should be submitted. `RadioGroupOption` already
contains the input; add `RadioGroupItemHiddenInput` to every custom item.

<ExampleFrame variant="component">
  <RadioGroupForm />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/radio-group/form.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={radioGroupSolidForm} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/radio-group/radio-group-form.module.css"
```
</div>
</ExampleCode>

### RadioGroupIndicator

Render `RadioGroupIndicator` when the active item needs an additional measured highlight. Keep the
indicator and the radio items in the same positioning context so the indicator follows the item
geometry.

<ExampleFrame variant="component">
  <RadioGroupIndicator />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/radio-group/indicator.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={radioGroupSolidIndicator} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/radio-group/radio-group-indicator.module.css"
```
</div>
</ExampleCode>

### Initial Value

Set `defaultValue` when the initial checked item is known and the group should manage later changes
itself.

<ExampleFrame variant="component">
  <RadioGroupInitialValue />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/radio-group/initial-value.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={radioGroupSolidInitialValue} />

    </Tab>
  </Tabs>
</div>
</ExampleCode>

### Orientation

Set `orientation="horizontal"` when arrow-key behavior should follow a horizontal group. Arrange
the items in a row with an inline wrapper when the visual layout should be horizontal too.

<ExampleFrame variant="component">
  <RadioGroupOrientation />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/radio-group/orientation.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={radioGroupSolidOrientation} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/radio-group/radio-group-orientation.module.css"
```
</div>
</ExampleCode>

### Root Provider

Use `RadioGroupRootProvider` when a `useRadioGroup()` store needs to be shared with controls outside the radio
group tree. The example also shows how to update the selected value from outside the group.

<ExampleFrame variant="component">
  <RadioGroupRootProvider />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/radio-group/root-provider.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={radioGroupSolidRootProvider} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/radio-group/radio-group-root-provider.module.css"
```
</div>
</ExampleCode>

### Sizes

Use the moduix `size` prop on `RadioGroupOption` or `RadioGroupItemControl` when the radio control needs a different
diameter. Supported values are `xs`, `sm`, `md`, `lg`, and `xl`.

<ExampleFrame variant="component">
  <RadioGroupSizes />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/radio-group/sizes.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={radioGroupSolidSizes} />

    </Tab>
  </Tabs>
</div>
</ExampleCode>

### With Field

Use `Field` when a radio group needs a field-level label, helper text, or error message. Pass
`invalid` to `RadioGroup` as well, because `Field` does not set the group's validation state.

<ExampleFrame variant="component">
  <RadioGroupField />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/radio-group/with-field.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={radioGroupSolidField} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/radio-group/radio-group-with-field.module.css"
```
</div>
</ExampleCode>

### With Fieldset

Wrap the group in `Fieldset` when several radio items share a legend and belong to the same form
section. `Fieldset` can also provide its disabled and invalid state to the nested group.

<ExampleFrame variant="component">
  <RadioGroupFieldset />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/radio-group/with-fieldset.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={radioGroupSolidFieldset} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/radio-group/radio-group-with-fieldset.module.css"
```
</div>
</ExampleCode>

### Advanced Customization

Bypass `RadioGroupOption` and use the full `RadioGroupItem` tree when each nested part needs custom placement, props, or
styling.

<ExampleFrame variant="component">
  <RadioGroupAdvancedCustomization />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/radio-group/advanced-customization.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={radioGroupSolidAdvancedCustomization} />

    </Tab>
  </Tabs>
</div>
</ExampleCode>

## Styling

Use `className` in React or `class` in Solid on exported parts for structural styles. For state-based
styles, prefer Ark data attributes such as `data-state`, `data-disabled`, and `data-orientation`,
together with moduix `data-slot` selectors. Tailwind variants use native utilities and consumer
classes; the component variables below customize the CSS Modules variants. Data attributes and
`data-slot` hooks are shared by both styling tracks.

### CSS Properties

<CssPropertiesSection properties={RadioGroupDocs.radioGroupOverrideCssProperties} />

### Styling hooks

- Ark anatomy hooks: `data-scope="radio-group"` and `data-part` on the root, label, item,
  item-control, item-text, item-hidden-input, and indicator.
- Ark state hooks: `data-state="checked|unchecked"`, `data-disabled`, `data-readonly`,
  `data-invalid`, `data-required`, `data-focus`, `data-focus-visible`, `data-hover`,
  `data-active`, and `data-orientation`.
- Ark indicator variables: `--transition-property`, `--left`, `--top`, `--width`, and `--height`.
- moduix hooks: `data-slot="radio-group-root"`, `radio-group-root-provider`,
  `radio-group-label`, `radio-group-item`, `radio-group-item-control`, `radio-group-item-text`,
  and `radio-group-indicator`.
