---
title: Highlight
description: Highlight words and phrases inside existing text with native mark elements and moduix styles.
---

import * as HighlightDocs from '@/components/examples/highlight/css-properties';
import HighlightBasic from '@/components/examples/highlight/basic.tsx';
import HighlightDynamicQuery from '@/components/examples/highlight/dynamic-query.tsx';
import HighlightExactMatch from '@/components/examples/highlight/exact-match.tsx';
import HighlightIgnoreCase from '@/components/examples/highlight/ignore-case.tsx';
import HighlightMatchAll from '@/components/examples/highlight/match-all.tsx';
import HighlightMultipleQueries from '@/components/examples/highlight/multiple-queries.tsx';
import highlightSolidBasic from '../../../snippets/highlight/solid/basic.tsx?raw';
import highlightSolidDynamicQuery from '../../../snippets/highlight/solid/dynamic-query.tsx?raw';
import highlightSolidExactMatch from '../../../snippets/highlight/solid/exact-match.tsx?raw';
import highlightSolidIgnoreCase from '../../../snippets/highlight/solid/ignore-case.tsx?raw';
import highlightSolidMatchAll from '../../../snippets/highlight/solid/match-all.tsx?raw';
import highlightSolidMultipleQueries from '../../../snippets/highlight/solid/multiple-queries.tsx?raw';

## API Reference

<PrimitiveReference href="https://ark-ui.com/docs/utilities/highlight" />

## Choosing the right component

Use `Highlight` to emphasize search terms inside text that already has a semantic container. It
renders the matched ranges, but does not add a layout wrapper of its own.

- Choose **`Highlight`** when one or more terms in an existing sentence, label, or text block need emphasis.
- Choose **`Text`** when the whole string only needs typographic styling and no query-based matching.

## Basic


Pass the complete source string through `text` and the term to find through `query`. Place
`Highlight` inside the text component or HTML element that should provide the surrounding layout
and semantics.

<ExampleFrame variant="component">
  <HighlightBasic />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/highlight/basic.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={highlightSolidBasic} />

    </Tab>
  </Tabs>
</div>
</ExampleCode>

## Install with shadcn

See [Quick Start](/docs/quick-start) for full package setup, registry configuration, and styling guidance.

<Tabs groupId="framework">
  <Tab label="React" value="react">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-react/highlight' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-react-tailwind/highlight' },
  ]}
/>

  </Tab>
  <Tab label="Solid" value="solid">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-solid/highlight' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-solid-tailwind/highlight' },
  ]}
/>

  </Tab>
</Tabs>

## Anatomy

`Highlight` has one public root and returns a fragment, not an extra wrapper element. Each matched
range becomes a native `<mark>` element. Text that does not match stays as ordinary text in the
surrounding flow.

```text
Highlight / Highlight.Root
└─ matched ranges rendered as repeated <mark> elements
```

| Part                           | Role                                                                 |
| ------------------------------ | -------------------------------------------------------------------- |
| `Highlight` / `Highlight.Root` | Applies moduix styling to every matched range in the provided text. |

## Composition

`Highlight` is stateless and owns no layout wrapper. Place it inside an existing text container such
as `Text`, a heading, a list item, or a table cell. Control `query` from the parent when search input
changes, and use `Highlight.Root` only when the namespaced form is clearer. A string-array `query`
requires `matchAll` to remain `true`; use custom markup in your own component when each match needs a
different element or interactive behavior.

## Examples

### Dynamic Query

Control `query` from framework state to highlight the current term while a user searches or filters
visible text.

<ExampleFrame variant="component">
  <HighlightDynamicQuery />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/highlight/dynamic-query.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={highlightSolidDynamicQuery} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/highlight/highlight-dynamic-query.module.css"
```
</div>
</ExampleCode>

### Exact Match

Set `exactMatch` when partial-word matches are too broad. In this example, `box` matches the
standalone word but not `checkbox` or `combobox`. Matching follows JavaScript word boundaries,
which are reliable for Latin text but do not treat Cyrillic and some other scripts as whole words.

<ExampleFrame variant="component">
  <HighlightExactMatch />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/highlight/exact-match.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={highlightSolidExactMatch} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/highlight/highlight-exact-match.module.css"
```
</div>
</ExampleCode>

### Ignore Case

Set `ignoreCase` when the capitalization of the source text should not affect the result.

<ExampleFrame variant="component">
  <HighlightIgnoreCase />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/highlight/ignore-case.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={highlightSolidIgnoreCase} />

    </Tab>
  </Tabs>
</div>
</ExampleCode>

### Match All

Set `matchAll` when every occurrence should be highlighted instead of only the first one.

<ExampleFrame variant="component">
  <HighlightMatchAll />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/highlight/match-all.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={highlightSolidMatchAll} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/highlight/highlight-match-all.module.css"
```
</div>
</ExampleCode>

### Multiple Queries

Pass a string array when several terms should be highlighted in the same text. For an array,
`matchAll` defaults to `true`; setting `matchAll={false}` throws an error.

<ExampleFrame variant="component">
  <HighlightMultipleQueries />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/highlight/multiple-queries.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={highlightSolidMultipleQueries} />

    </Tab>
  </Tabs>
</div>
</ExampleCode>

## Styling

Matched ranges receive moduix's default `<mark>` styles. Use `className` in React or `class` in Solid
for a local adjustment. CSS Modules variants also expose the `--moduix-highlight-*` variables; use
Tailwind utilities or consumer classes for Tailwind variants.

### CSS Properties

<CssPropertiesSection properties={HighlightDocs.highlightCssProperties} />

### Styling hooks

- Every matched `<mark>` has `data-scope="highlight"` and `data-part="root"`.
- `data-slot="highlight-root"` is the stable moduix selector for each matched mark.
- Props such as `className`/`class`, `style`, `title`, `id`, and `data-*` are applied to every matched `<mark>`.
- There is no wrapper element, state attribute, or Ark runtime CSS variable.
