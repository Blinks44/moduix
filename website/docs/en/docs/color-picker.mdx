---
title: Color Picker
component: color-picker
description: An Ark UI-based color picker for choosing colors, editing color channels, and selecting swatches.
---

import * as ColorPickerDocs from '@/components/examples/color-picker/css-properties';
import ColorPickerAdvancedCustomization from '@/components/examples/color-picker/advanced-customization.tsx';
import ColorPickerBasic from '@/components/examples/color-picker/basic.tsx';
import ColorPickerCompactTrigger from '@/components/examples/color-picker/compact-trigger.tsx';
import ColorPickerControlled from '@/components/examples/color-picker/controlled.tsx';
import ColorPickerDisabled from '@/components/examples/color-picker/disabled.tsx';
import ColorPickerFieldState from '@/components/examples/color-picker/field-state.tsx';
import ColorPickerFormUsage from '@/components/examples/color-picker/form-usage.tsx';
import ColorPickerFormatViews from '@/components/examples/color-picker/format-views.tsx';
import ColorPickerInline from '@/components/examples/color-picker/inline.tsx';
import ColorPickerInsideDialog from '@/components/examples/color-picker/inside-dialog.tsx';
import ColorPickerInputOnly from '@/components/examples/color-picker/input-only.tsx';
import ColorPickerOpenControlled from '@/components/examples/color-picker/open-controlled.tsx';
import ColorPickerRootProvider from '@/components/examples/color-picker/root-provider.tsx';
import ColorPickerSliderOnly from '@/components/examples/color-picker/slider-only.tsx';
import ColorPickerSwatchOnly from '@/components/examples/color-picker/swatch-only.tsx';
import ColorPickerSwatches from '@/components/examples/color-picker/swatches.tsx';
import ColorPickerValueSwatch from '@/components/examples/color-picker/value-swatch.tsx';

import colorPickerSolidAdvancedCustomization from '../../../snippets/color-picker/solid/advanced-customization.tsx?raw';
import colorPickerSolidBasic from '../../../snippets/color-picker/solid/basic.tsx?raw';
import colorPickerSolidCompactTrigger from '../../../snippets/color-picker/solid/compact-trigger.tsx?raw';
import colorPickerSolidControlled from '../../../snippets/color-picker/solid/controlled.tsx?raw';
import colorPickerSolidDisabled from '../../../snippets/color-picker/solid/disabled.tsx?raw';
import colorPickerSolidFieldState from '../../../snippets/color-picker/solid/field-state.tsx?raw';
import colorPickerSolidFormUsage from '../../../snippets/color-picker/solid/form-usage.tsx?raw';
import colorPickerSolidFormatViews from '../../../snippets/color-picker/solid/format-views.tsx?raw';
import colorPickerSolidInline from '../../../snippets/color-picker/solid/inline.tsx?raw';
import colorPickerSolidInsideDialog from '../../../snippets/color-picker/solid/inside-dialog.tsx?raw';
import colorPickerSolidInputOnly from '../../../snippets/color-picker/solid/input-only.tsx?raw';
import colorPickerSolidOpenControlled from '../../../snippets/color-picker/solid/open-controlled.tsx?raw';
import colorPickerSolidRootProvider from '../../../snippets/color-picker/solid/root-provider.tsx?raw';
import colorPickerSolidSliderOnly from '../../../snippets/color-picker/solid/slider-only.tsx?raw';
import colorPickerSolidSwatchOnly from '../../../snippets/color-picker/solid/swatch-only.tsx?raw';
import colorPickerSolidSwatches from '../../../snippets/color-picker/solid/swatches.tsx?raw';
import colorPickerSolidValueSwatch from '../../../snippets/color-picker/solid/value-swatch.tsx?raw';

## API Reference

<PrimitiveReference href="https://ark-ui.com/docs/components/color-picker" />

## Choosing the right component

Choose `ColorPicker` when users need to pick or fine-tune a color. Choose `Input` when they only need
to enter a known value and do not need to manipulate the color directly.

## Basic

Start with a compact field that combines a root, one channel input, the trigger's default swatch,
explicit popup content, and the picker's default internal parts. Add `ColorPicker.HiddenInput`
explicitly when the color should be submitted with a form.

<ExampleFrame variant="component">
  <ColorPickerBasic />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/color-picker/basic.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={colorPickerSolidBasic} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/color-picker/color-picker-basic.module.css"
```
</div>

</ExampleCode>

## Install with shadcn

See [Quick Start](/docs/quick-start) for full package setup, registry configuration, and styling guidance.

<Tabs groupId="framework">
  <Tab label="React" value="react">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-react/color-picker' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-react-tailwind/color-picker' },
  ]}
/>

  </Tab>
  <Tab label="Solid" value="solid">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-solid/color-picker' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-solid-tailwind/color-picker' },
  ]}
/>

  </Tab>
</Tabs>

## Anatomy

```text
ColorPicker / ColorPicker.Root
├─ ColorPicker.Label
├─ ColorPicker.Control
│  ├─ ColorPicker.ChannelInput[channel]
│  └─ ColorPicker.Trigger
│     ├─ ColorPicker.TransparencyGrid
│     └─ ColorPicker.ValueSwatch
├─ Overlay subtree (automatically portalled)
│  └─ ColorPicker.Positioner
│     └─ ColorPicker.Content
│        ├─ ColorPicker.Area
│        │  ├─ ColorPicker.AreaBackground
│        │  └─ ColorPicker.AreaThumb
│        ├─ ColorPicker.EyeDropperTrigger
│        ├─ ColorPicker.ChannelSlider[channel]
│        │  ├─ ColorPicker.TransparencyGrid
│        │  ├─ ColorPicker.ChannelSliderTrack
│        │  └─ ColorPicker.ChannelSliderThumb
│        ├─ ColorPicker.SwatchGroup
│        │  └─ ColorPicker.SwatchTrigger[value]
│        │     └─ ColorPicker.Swatch[value]
│        │        └─ ColorPicker.SwatchIndicator
│        └─ ColorPicker.View[format]
│           └─ ColorPicker.ChannelInput[channel]
└─ ColorPicker.HiddenInput (explicit)
```

| Part                               | Role                                                                   |
| ---------------------------------- | ---------------------------------------------------------------------- |
| `ColorPicker` / `ColorPicker.Root` | Owns color value, format, popup state, and form integration.           |
| `ColorPicker.HiddenInput` | Native Ark form control; compose explicitly as described below. |
| `ColorPicker.Control`              | Groups visible inputs and trigger.                                     |
| `ColorPicker.Trigger`              | Button that opens the popup and renders the current swatch by default. |
| `ColorPicker.Content`              | Actual Ark popup content part.                                         |
| `ColorPicker.Area`                 | Two-dimensional color interaction surface.                             |
| `ColorPicker.ChannelSlider`        | One-dimensional channel control for hue, alpha, or other channels.     |
| `ColorPicker.Sliders`              | Styled hue and alpha slider pair.                                      |
| `ColorPicker.ChannelInput`         | Text or numeric input for a specific color channel.                    |
| `ColorPicker.SwatchTrigger`        | Selectable preset color button.                                        |
| `ColorPicker.ValueSwatch`          | Visual swatch for the current value.                                   |

`ColorPicker.RootProvider`, `parseColor`, `useColorPicker`, and `useColorPickerContext` cover the
normal advanced state path. Import Ark-only type helpers directly when needed as an escape hatch.

## Composition

Use `defaultValue` for local state. When application state owns the selected color or popup, pass `value` with `onValueChange(details)` or `open` with `onOpenChange(details)`. For state created outside the rendered tree, pass `useColorPicker()` to `ColorPicker.RootProvider`; do not render `ColorPicker` or `ColorPicker.Root` for the same store.

`ColorPicker.Positioner` is portalled by default. Set `portalled={false}` for an overlay inside another stacking context. To submit the color with a native form, set `name` on the root and render `ColorPicker.HiddenInput` explicitly. Parts such as `Area`, `ChannelSlider`, and `SwatchTrigger` provide default visual children; pass children only when changing their structure.

## Examples

### Color Area and Sliders

Use this compact composition when users need the color area and channel sliders but do not need
typed channel input.

<ExampleFrame variant="component">
  <ColorPickerSliderOnly />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/color-picker/slider-only.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={colorPickerSolidSliderOnly} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Compact Trigger

Use `data-fit-content` on `ColorPicker.Trigger` when a content-sized button is more useful than a
square swatch, such as when replacing a button-based color field.

<ExampleFrame variant="component">
  <ColorPickerCompactTrigger />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/color-picker/compact-trigger.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={colorPickerSolidCompactTrigger} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/color-picker/color-picker-compact-trigger.module.css"
```
</div>

</ExampleCode>

### Controlled

Use controlled state when the selected color must stay in sync with application state.

<ExampleFrame variant="component">
  <ColorPickerControlled />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/color-picker/controlled.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={colorPickerSolidControlled} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Disabled

Use `disabled` on the root when every color picker part should become non-interactive while keeping
the current color visible.

<ExampleFrame variant="component">
  <ColorPickerDisabled />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/color-picker/disabled.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={colorPickerSolidDisabled} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Field State

Use root form-state props such as `required` and `invalid` when the picker is rendered inside a
field container and should expose the same state through Ark data attributes.

<ExampleFrame variant="component">
  <ColorPickerFieldState />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/color-picker/field-state.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={colorPickerSolidFieldState} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/color-picker/color-picker-field-state.module.css"
```
</div>

</ExampleCode>

### Form Usage

Use `name` on `ColorPicker` and add `ColorPicker.HiddenInput` explicitly.

<ExampleFrame variant="component">
  <ColorPickerFormUsage />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/color-picker/form-usage.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={colorPickerSolidFormUsage} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Format Views

Use `format`, `onFormatChange`, `FormatSelect`, and `View` when users need to switch between channel
input groups without changing the picker structure. `FormatSelect` keeps Ark's native format
control, while its collapsed trigger is styled like the moduix Select field and consistently
presents the RGBA, HSLA, and HSBA options. The browser controls the options popup.

<ExampleFrame variant="component">
  <ColorPickerFormatViews />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/color-picker/format-views.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={colorPickerSolidFormatViews} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/color-picker/color-picker-format-views.module.css"
```
</div>

</ExampleCode>

### Inline

Use `inline` in settings panels where the color tools should stay visible without a popup trigger.

<ExampleFrame variant="component">
  <ColorPickerInline />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/color-picker/inline.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={colorPickerSolidInline} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/color-picker/color-picker-inline.module.css"
```
</div>

</ExampleCode>

### Input Only

Use channel inputs with a passive swatch when typed color entry is important and a popup would add
unnecessary complexity to the interface.

<ExampleFrame variant="component">
  <ColorPickerInputOnly />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/color-picker/input-only.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={colorPickerSolidInputOnly} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/color-picker/color-picker-input-only.module.css"
```
</div>

</ExampleCode>

### Inside Dialog

When a picker appears inside a modal dialog, render its popup without a portal so it shares the
dialog's stacking context and stays interactive above the dialog surface.

<ExampleFrame variant="component">
  <ColorPickerInsideDialog />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/color-picker/inside-dialog.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={colorPickerSolidInsideDialog} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/color-picker/color-picker-inside-dialog.module.css"
```
</div>

</ExampleCode>

### Open Controlled

Use controlled `open` state when another control or workflow owns the popup's visibility.

<ExampleFrame variant="component">
  <ColorPickerOpenControlled />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/color-picker/open-controlled.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={colorPickerSolidOpenControlled} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/color-picker/color-picker-open-controlled.module.css"
```
</div>

</ExampleCode>

### RootProvider

Use moduix `useColorPicker` with `RootProvider` when the color picker state must be created outside
the rendered tree and shared with nearby controls.

<ExampleFrame variant="component">
  <ColorPickerRootProvider />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/color-picker/root-provider.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={colorPickerSolidRootProvider} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/color-picker/color-picker-root-provider.module.css"
```
</div>

</ExampleCode>

### Swatch Only

Use swatch-only composition when a preset palette is enough and the area, sliders, and inputs are
unnecessary.

<ExampleFrame variant="component">
  <ColorPickerSwatchOnly />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/color-picker/swatch-only.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={colorPickerSolidSwatchOnly} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/color-picker/color-picker-swatch-only.module.css"
```
</div>

</ExampleCode>

### Swatches

Use popup swatches when the picker should offer a curated set of colors alongside freeform editing.
`closeOnSelect` follows Ark behavior and closes the popup after a swatch is chosen.

<ExampleFrame variant="component">
  <ColorPickerSwatches />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/color-picker/swatches.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={colorPickerSolidSwatches} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Value Swatch

Use `ValueSwatch` and `ValueText` when nearby UI needs to show the selected color without opening
the full picker.

<ExampleFrame variant="component">
  <ColorPickerValueSwatch />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/color-picker/value-swatch.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={colorPickerSolidValueSwatch} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/color-picker/color-picker-value-swatch.module.css"
```
</div>

</ExampleCode>

### Advanced Customization

Use explicit parts and children when the default composition needs a different visual arrangement.

<ExampleFrame variant="component">
  <ColorPickerAdvancedCustomization />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/color-picker/advanced-customization.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={colorPickerSolidAdvancedCustomization} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

## Styling

Use `className` in React or `class` in Solid on exported parts. Tailwind variants use native utilities and consumer classes; CSS Modules variants use the public `--moduix-color-picker-*` CSS variables shown below. Both styling tracks expose stable `data-slot="color-picker-*"` hooks and Ark `data-scope` / `data-part` attributes.
### CSS Properties

<CssPropertiesSection properties={ColorPickerDocs.colorPickerOverrideCssProperties} />

### Styling hooks

Use the part that owns the visual concern:

- `ColorPicker` for width, gap, shared color, and disabled opacity
- `ColorPicker.Control`, `ChannelInput`, `Trigger`, `FormatSelect`, and `FormatTrigger` for the
  visible field controls
- `ColorPicker.Positioner` and `ColorPicker.Content` for popup layering, width, padding, radius,
  shadow, and state animations
- `ColorPicker.Area`, `AreaBackground`, and `AreaThumb` for the two-dimensional color surface
- `ColorPicker.ChannelSlider`, `ChannelSliderTrack`, `ChannelSliderThumb`, and `Sliders` for hue,
  alpha, and other channel sliders
- `ColorPicker.SwatchGroup`, `SwatchTrigger`, `Swatch`, and `SwatchIndicator` for preset color
  grids
- `ColorPicker.ValueSwatch`, `ValueText`, `View`, and `TransparencyGrid` for value display and
  format-specific layouts

The main state hooks are `data-state`, `data-focus`, `data-invalid`, `data-disabled`,
`data-readonly`, `data-required`, `data-channel`, `data-orientation`, `data-value`,
`data-placement`, and `data-side`. Ark runtime variables include `--color`, `--value`,
`--available-width`, `--available-height`, and `--transform-origin`.

When `prefers-reduced-motion` is enabled, popup entry and exit animations use a 1ms duration so Ark
can complete its exit lifecycle without visible motion.
