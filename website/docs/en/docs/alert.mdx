---
title: Alert
component: alert
description: Inline messages for status updates, validation results, and system feedback.
---

import * as AlertDocs from '@/components/examples/alert/css-properties';
import AlertBasic from '@/components/examples/alert/basic.tsx';
import AlertCustomComposition from '@/components/examples/alert/custom-composition.tsx';
import AlertCustomHeading from '@/components/examples/alert/custom-heading.tsx';
import AlertError from '@/components/examples/alert/error.tsx';
import AlertStatuses from '@/components/examples/alert/statuses.tsx';
import AlertWithIcon from '@/components/examples/alert/with-icon.tsx';
import alertSolidBasic from '../../../snippets/alert/solid/basic.tsx?raw';
import alertSolidCustomComposition from '../../../snippets/alert/solid/custom-composition.tsx?raw';
import alertSolidCustomHeading from '../../../snippets/alert/solid/custom-heading.tsx?raw';
import alertSolidError from '../../../snippets/alert/solid/error.tsx?raw';
import alertSolidStatuses from '../../../snippets/alert/solid/statuses.tsx?raw';
import alertSolidWithIcon from '../../../snippets/alert/solid/with-icon.tsx?raw';
import alertVueBasic from '../../../snippets/alert/vue/basic.vue?raw';
import alertVueCustomHeading from '../../../snippets/alert/vue/custom-heading.vue?raw';
import alertVueError from '../../../snippets/alert/vue/error.vue?raw';
import alertVueStatuses from '../../../snippets/alert/vue/statuses.vue?raw';
import alertVueWithIcon from '../../../snippets/alert/vue/with-icon.vue?raw';

## API Reference

Ark UI does not provide a dedicated `alert` primitive. moduix builds `Alert` with Ark's official
[factory](https://ark-ui.com/docs/guides/composition#the-ark-factory), following Ark's guidance on
[composition](https://ark-ui.com/docs/guides/composition), [styling](https://ark-ui.com/docs/guides/styling), and
[refs](https://ark-ui.com/docs/guides/ref).

Import `Alert` from `@moduix/react/alert`, `@moduix/solid/alert`, or `@moduix/vue/alert`. `Alert` is
the root part, and the other public parts are `AlertIndicator`, `AlertContent`, `AlertTitle`,
`AlertDescription`, and `AlertActions`.

Every part accepts the usual props for its rendered element, including `className`/`class` and Ark's
`asChild`, and forwards refs to the rendered element. `status` defaults to `info` and controls visual
styling only. Add `role="status"` or `role="alert"` when a dynamically added or updated message needs a
live region. `AlertIndicator` is hidden from assistive technology, so keep the essential meaning in the
title or description.

## Choosing the right component

Use `Alert` when a message belongs in the page layout and should take up space in the normal document
flow.

- Use **`Alert`** for inline status updates, validation results, and system feedback in forms, panels, or page content.
- Use **`Toast`** for temporary notifications that appear outside the document flow and dismiss themselves.

## Basic


For a standard inline message, place `AlertTitle` and `AlertDescription` inside `AlertContent`.
Add `AlertIndicator` when an icon helps people scan the message.

<ExampleFrame variant="component">
  <AlertBasic />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
      { label: 'Vue', value: 'vue' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/alert/basic.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={alertSolidBasic} />

    </Tab>
    <Tab value="vue">

<CodeBlockRuntime lang="vue" code={alertVueBasic} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

## Install with shadcn

See [Quick Start](/docs/quick-start) for full package setup, registry configuration, and styling guidance.

<Tabs groupId="framework">
  <Tab label="React" value="react">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-react/alert' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-react-tailwind/alert' },
  ]}
/>

  </Tab>
  <Tab label="Solid" value="solid">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-solid/alert' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-solid-tailwind/alert' },
  ]}
/>

  </Tab>
  <Tab label="Vue" value="vue">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-vue/alert' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-vue-tailwind/alert' },
  ]}
/>

  </Tab>
</Tabs>

## Anatomy

Place message content inside `AlertContent`. `AlertIndicator` is optional and sits beside that
content when it helps people scan the message.

```text
Alert
├─ AlertIndicator (optional)
└─ AlertContent
   ├─ AlertTitle
   ├─ AlertDescription
   ├─ AlertActions (optional)
   └─ Extra custom blocks
```

| Part               | Role                                                               | `data-part`   |
| ------------------ | ------------------------------------------------------------------ | ------------- |
| `Alert`            | Root element. Applies status styles and automatic live-region semantics. | `root`        |
| `AlertIndicator`   | Optional visual cue. Child SVG icons receive a consistent size.   | `indicator`   |
| `AlertContent`     | Required column wrapper for message content and actions.           | `content`     |
| `AlertTitle`       | Alert title. Renders a `p` by default.                              | `title`       |
| `AlertDescription` | Supporting message text.                                           | `description` |
| `AlertActions`     | Optional wrapped row for actions inside `Content`.                 | `actions`     |

## Composition

`Alert` is presentational: the component that renders it owns visibility and dismissal state. It does
not manage focus or keyboard behavior for buttons and links placed inside it. `AlertContent` is
required and contains the title, description, controls, and other message content. Put controls inside
`AlertActions` when they need the default wrapped action-row styling.

When the title needs a different semantic element, use `AlertTitle asChild` with exactly one semantic
child. Solid uses Ark's render-function form of `asChild` as shown in the Custom Heading example. Vue
uses the `as-child` attribute with a single child element.

## Examples

### Custom Heading

Use `asChild` on `AlertTitle` when the alert title needs to participate in the page's heading hierarchy.

<ExampleFrame variant="component">
  <AlertCustomHeading />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
      { label: 'Vue', value: 'vue' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/alert/custom-heading.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={alertSolidCustomHeading} />

    </Tab>
    <Tab value="vue">

<CodeBlockRuntime lang="vue" code={alertVueCustomHeading} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Error

Set `status="error"` for stronger visual emphasis. Add `role="alert"` only when the message needs an
assertive announcement.

<ExampleFrame variant="component">
  <AlertError />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
      { label: 'Vue', value: 'vue' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/alert/error.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={alertSolidError} />

    </Tab>
    <Tab value="vue">

<CodeBlockRuntime lang="vue" code={alertVueError} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Statuses

Use the four statuses to distinguish informational, successful, warning, and error messages. Add
`AlertIndicator` with the icon of your choice when the visual cue is useful.

<ExampleFrame variant="component">
  <AlertStatuses />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
      { label: 'Vue', value: 'vue' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/alert/statuses.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={alertSolidStatuses} />

    </Tab>
    <Tab value="vue">

<CodeBlockRuntime lang="vue" code={alertVueStatuses} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/alert/alert-statuses.module.css"
```
</div>

</ExampleCode>

### With Icon

Place a moduix icon or custom SVG inside `AlertIndicator`.

<ExampleFrame variant="component">
  <AlertWithIcon />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
      { label: 'Vue', value: 'vue' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/alert/with-icon.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={alertSolidWithIcon} />

    </Tab>
    <Tab value="vue">

<CodeBlockRuntime lang="vue" code={alertVueWithIcon} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Advanced Customization

Use `AlertContent` and `AlertActions` when the message needs recovery or follow-up controls. Keep
dismissal state in the component that renders the alert when a local alert should be hidden.

<ExampleFrame variant="component">
  <AlertCustomComposition />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/alert/custom-composition.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={alertSolidCustomComposition} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/alert/alert-custom-composition.module.css"
```
</div>

</ExampleCode>

## Styling

Use `className` or `class` on the root or its parts for local styles. Tailwind variants use native
utilities and consumer classes. In the CSS Modules variants, override `--moduix-alert-*` variables
on the root when the alert needs a local theme.

In CSS Modules, the component-wide `--moduix-alert-bg`, `--moduix-alert-border-color`,
`--moduix-alert-color`, and `--moduix-alert-indicator-color` variables apply to every status. Use
the matching `--moduix-alert-<status>-*` variables when only one status should change.

Built-in statuses also read shared palette tokens:

| Status    | Shared token                 | Default                      |
| --------- | ---------------------------- | ---------------------------- |
| `info`    | `--moduix-color-card`        | theme card                   |
| `success` | `--moduix-color-success`     | `oklch(0.627 0.194 149.214)` |
| `warning` | `--moduix-color-warning`     | `oklch(0.795 0.184 86.047)`  |
| `error`   | `--moduix-color-destructive` | theme destructive            |

### CSS Properties

<CssPropertiesSection properties={AlertDocs.alertCssProperties} />

### Styling hooks

- Stable `data-slot` selectors: `alert-root`, `alert-indicator`, `alert-content`, `alert-title`, `alert-description`, and `alert-actions`
- Ark-style selectors: `data-scope="alert"` with `data-part="root"`, `indicator`, `content`, `title`, `description`, or `actions`
- `Alert` writes the selected status to `data-status`
