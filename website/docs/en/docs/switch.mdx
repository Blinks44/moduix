---
title: Switch
component: switch
description: A composable Ark UI switch for turning an immediate boolean setting on or off.
---

import * as SwitchDocs from '@/components/examples/switch/css-properties';
import SwitchAdvancedCustomization from '@/components/examples/switch/advanced-customization.tsx';
import SwitchAsChild from '@/components/examples/switch/as-child.tsx';
import SwitchBasic from '@/components/examples/switch/basic.tsx';
import SwitchContext from '@/components/examples/switch/context.tsx';
import SwitchControlled from '@/components/examples/switch/controlled.tsx';
import SwitchDisabled from '@/components/examples/switch/disabled.tsx';
import SwitchFormIntegration from '@/components/examples/switch/form-integration.tsx';
import SwitchInitialChecked from '@/components/examples/switch/initial-checked.tsx';
import SwitchReadOnly from '@/components/examples/switch/read-only.tsx';
import SwitchRootProvider from '@/components/examples/switch/root-provider.tsx';
import SwitchSizes from '@/components/examples/switch/sizes.tsx';
import switchSolidAdvancedCustomization from '../../../snippets/switch/solid/advanced-customization.tsx?raw';
import switchSolidAsChild from '../../../snippets/switch/solid/as-child.tsx?raw';
import switchSolidBasic from '../../../snippets/switch/solid/basic.tsx?raw';
import switchSolidContext from '../../../snippets/switch/solid/context.tsx?raw';
import switchSolidControlled from '../../../snippets/switch/solid/controlled.tsx?raw';
import switchSolidDisabled from '../../../snippets/switch/solid/disabled.tsx?raw';
import switchSolidFormIntegration from '../../../snippets/switch/solid/form-integration.tsx?raw';
import switchSolidInitialChecked from '../../../snippets/switch/solid/initial-checked.tsx?raw';
import switchSolidReadOnly from '../../../snippets/switch/solid/read-only.tsx?raw';
import switchSolidRootProvider from '../../../snippets/switch/solid/root-provider.tsx?raw';
import switchSolidSizes from '../../../snippets/switch/solid/sizes.tsx?raw';

## API Reference

<PrimitiveReference href="https://ark-ui.com/docs/components/switch" />

## Choosing the right component

Use `Switch` for a setting that takes effect as soon as the user changes it. A switch represents one
boolean value.

- Choose **`Switch`** for settings such as notifications, reminders, or feature flags.
- Choose **`Checkbox`** when the control represents a selection that users submit as part of a form,
  rather than an immediate setting change.
- Choose **`RadioGroup`** when users must choose one option from several mutually exclusive values.

## Basic


A basic switch has a root, a visual control, a label, and an explicit `Switch.HiddenInput` for native
interaction and form behavior. The control receives a default thumb when it has no children.

<ExampleFrame variant="component">
  <SwitchBasic />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/switch/basic.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={switchSolidBasic} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

## Install with shadcn

See [Quick Start](/docs/quick-start) for full package setup, registry configuration, and styling guidance.

<Tabs groupId="framework">
  <Tab label="React" value="react">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-react/switch' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-react-tailwind/switch' },
  ]}
/>

  </Tab>
  <Tab label="Solid" value="solid">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-solid/switch' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-solid-tailwind/switch' },
  ]}
/>

  </Tab>
</Tabs>

## Anatomy

```text
Switch / Switch.Root
├─ Switch.Control
│  └─ Switch.Thumb
├─ Switch.Label
└─ Switch.HiddenInput (explicit)
```

| Part                          | Role                                                                 |
| ----------------------------- | -------------------------------------------------------------------- |
| `Switch` / `Switch.Root`      | Root `label` element. Owns switch state, ids, form props, and `size`. |
| `Switch.HiddenInput`           | Native Ark form control; compose explicitly as described below.      |
| `Switch.RootProvider`          | Root `label` driven by state created with `useSwitch()`.             |
| `Switch.Control`               | Visual track and focus-ring target. Adds `Switch.Thumb` by default.  |
| `Switch.Thumb`                 | Movable thumb for the default visuals or custom thumb content.       |
| `Switch.Label`                 | Ark-connected label text.                                            |

`Switch` and `Switch.Root` are two names for the same root component. `Switch.Context`,
`useSwitchContext`, and `useSwitch` are exported from `@moduix/react` alongside the component.
The matching Solid exports are available from `@moduix/solid` and `@moduix/solid/switch`.

## Composition

Render `Switch.HiddenInput` explicitly inside `Switch` or `Switch.RootProvider`, including when the
switch is not in a form. It provides the native input that Ark uses for focus, keyboard interaction,
accessibility, and form submission.

When `Switch.Control` has no children, it adds the default `Switch.Thumb`. Add `Switch.Thumb`
explicitly only when it needs custom content or styling.

Use `Switch.Context` or `useSwitchContext` when nearby UI needs to read the switch state. Use
`useSwitch` with `Switch.RootProvider` when the state must be created outside the rendered switch
tree. Do not render `Switch` and `Switch.RootProvider` for the same state instance.

With `asChild`, provide one semantic element compatible with `label` so Ark can keep the switch
accessible.

## Examples

### asChild

Use `asChild` when you need a custom root element while Ark continues to manage the switch behavior.

<ExampleFrame variant="component">
  <SwitchAsChild />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/switch/as-child.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={switchSolidAsChild} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/switch/switch-as-child.module.css"
```
</div>

</ExampleCode>

### Context

Use `useSwitchContext` from `@moduix/react` when nearby UI needs to read the current switch state.

<ExampleFrame variant="component">
  <SwitchContext />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/switch/context.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={switchSolidContext} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Controlled

Use controlled mode when the switch needs to stay in sync with other UI or application state.

<ExampleFrame variant="component">
  <SwitchControlled />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/switch/controlled.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={switchSolidControlled} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/switch/switch-controlled.module.css"
```
</div>

</ExampleCode>

### Disabled

Use `disabled` to prevent interaction while keeping the current visual state.

<ExampleFrame variant="component">
  <SwitchDisabled />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/switch/disabled.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={switchSolidDisabled} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/switch/switch-disabled.module.css"
```
</div>

</ExampleCode>

### Form Integration

Use `Field` when the switch needs form labeling, validation, or submission. This example displays an
error after the form is submitted while the switch is still off.

<ExampleFrame variant="component">
  <SwitchFormIntegration />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/switch/form-integration.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={switchSolidFormIntegration} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/switch/switch-form-integration.module.css"
```
</div>

</ExampleCode>

### Initial Checked

Use `defaultChecked` when the switch should start enabled and manage its own state afterward.

<ExampleFrame variant="component">
  <SwitchInitialChecked />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/switch/initial-checked.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={switchSolidInitialChecked} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Read Only

Use `readOnly` when the current state should remain visible but users must not be able to change it.

<ExampleFrame variant="component">
  <SwitchReadOnly />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/switch/read-only.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={switchSolidReadOnly} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/switch/switch-read-only.module.css"
```
</div>

</ExampleCode>

### Root Provider

Import `useSwitch` from `@moduix/react` and pair it with `Switch.RootProvider` when you need to create
and control the state outside the rendered switch tree.

<ExampleFrame variant="component">
  <SwitchRootProvider />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/switch/root-provider.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={switchSolidRootProvider} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/switch/switch-root-provider.module.css"
```
</div>

</ExampleCode>

### Sizes

Use moduix `size` to match different form densities without changing the Ark part tree.

<ExampleFrame variant="component">
  <SwitchSizes />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/switch/sizes.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={switchSolidSizes} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/switch/switch-sizes.module.css"
```
</div>

</ExampleCode>

### Advanced Customization

Add `Switch.Thumb` explicitly when the thumb needs custom content or styling.

<ExampleFrame variant="component">
  <SwitchAdvancedCustomization />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/switch/advanced-customization.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={switchSolidAdvancedCustomization} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

## Styling

Style `Switch` with `className` in React or `class` in Solid, public `data-slot` hooks, Ark state
attributes, and the public `--moduix-switch-*` CSS variables shown below. Tailwind variants use
utilities and consumer classes; the variables below apply to CSS Modules, while data attributes are
shared by both approaches.

### CSS Properties

<div className="docs-reference">
  <Tabs
    values={[{ label: 'CSS Variables', value: 'CSS Variables' }]}
    className="docs-reference-tabs"
    contentItemClassName="docs-reference-panel"
  >
    <Tab>
      <SwitchDocs.SwitchCssPropertiesPanel />
    </Tab>
  </Tabs>
</div>

### Styling hooks

- `Switch` and `Switch.RootProvider` expose `data-slot="switch-root"` /
  `data-slot="switch-root-provider"` and set the moduix `data-size` attribute.
- `Switch.Control` exposes `data-slot="switch-control"` and receives Ark state attributes such as
  `data-state`, `data-focus-visible`, `data-hover`, `data-active`, `data-disabled`, `data-readonly`,
  `data-invalid`, and `data-required`.
- `Switch.Thumb` exposes `data-slot="switch-thumb"` and receives the same Ark state attributes for
  thumb movement and custom content styling.
- `Switch.Label` exposes `data-slot="switch-label"` for typography overrides.
