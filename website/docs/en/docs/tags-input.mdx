---
title: Tags Input
component: tags-input
description: An editable list of string tags with validation, form submission, and optional suggestions.
---

import * as TagsInputDocs from '@/components/examples/tags-input/css-properties';
import TagsInputAdvancedCustomization from '@/components/examples/tags-input/advanced-customization.tsx';
import TagsInputAllowDuplicates from '@/components/examples/tags-input/allow-duplicates.tsx';
import TagsInputBasic from '@/components/examples/tags-input/basic.tsx';
import TagsInputControlled from '@/components/examples/tags-input/controlled.tsx';
import TagsInputControlledInputValue from '@/components/examples/tags-input/controlled-input-value.tsx';
import TagsInputDelimiterAndPaste from '@/components/examples/tags-input/delimiter-and-paste.tsx';
import TagsInputDisableEditing from '@/components/examples/tags-input/disable-editing.tsx';
import TagsInputDisabledAndReadOnly from '@/components/examples/tags-input/disabled-and-read-only.tsx';
import TagsInputField from '@/components/examples/tags-input/field.tsx';
import TagsInputForm from '@/components/examples/tags-input/form.tsx';
import TagsInputMaxWithOverflow from '@/components/examples/tags-input/max-with-overflow.tsx';
import TagsInputRootProvider from '@/components/examples/tags-input/root-provider.tsx';
import TagsInputSanitizeAndBlur from '@/components/examples/tags-input/sanitize-and-blur.tsx';
import TagsInputValidationAndMax from '@/components/examples/tags-input/validation-and-max.tsx';
import TagsInputWithCombobox from '@/components/examples/tags-input/with-combobox.tsx';

## API Reference

<PrimitiveReference href="https://ark-ui.com/docs/components/tags-input" />

## Choosing the right component

Use `TagsInput` when users need to enter and manage a list of editable string values in one form
control.

- Choose **`TagsInput`** for labels, skills, filters, recipients, topics, and other free-form string lists.
- Choose **`Tag`** when the interface only displays an existing token and may offer a separate remove action.
- Choose **`Combobox`** when users should select from a known collection and filtering is the main interaction.

## Basic

Start with the root, label, control, `Items`, entry input, clear trigger, and an explicit
`TagsInput.HiddenInput` when the tags should be submitted with a form.

<ExampleFrame variant="component">
  <TagsInputBasic />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/tags-input/basic.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/tags-input/tags-input-basic.module.css"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

## Install with shadcn

<ShadcnInstall packageName="tags-input" />

## Anatomy

```text
TagsInput / TagsInput.Root
├─ TagsInput.Label
├─ TagsInput.Control
│  ├─ TagsInput.Items
│  └─ TagsInput.Item[index, value]
│     ├─ TagsInput.ItemPreview
│     │  ├─ TagsInput.ItemText
│     │  └─ TagsInput.ItemDeleteTrigger
│     └─ TagsInput.ItemInput
│  ├─ TagsInput.Input
│  └─ TagsInput.ClearTrigger
└─ TagsInput.HiddenInput (explicit)
```

| Part                                   | Role                                                                 |
| -------------------------------------- | -------------------------------------------------------------------- |
| `TagsInput` / `TagsInput.Root`         | Root state, tag values, entry input, validation, and form integration. |
| `TagsInput.HiddenInput` | Native Ark form control; compose explicitly as described below. |
| `TagsInput.RootProvider`               | Renders the tags input from state returned by Ark `useTagsInput`.     |
| `TagsInput.Label`                      | Accessible label for the tags input.                                 |
| `TagsInput.Control`                    | Visible shell containing tags, the entry input, and the clear trigger. |
| `TagsInput.Items`                      | Standard editable tag renderer for the current root values.          |
| `TagsInput.Item`                       | One tag. Pass `index` and `value`.                                    |
| `TagsInput.ItemContext`                | Render-prop access to the current tag's state.                       |
| `TagsInput.ItemPreview`                | Visible tag content outside edit mode.                                |
| `TagsInput.ItemText`                   | Visible tag text.                                                     |
| `TagsInput.ItemDeleteTrigger`          | Removes one tag; moduix supplies a compact close icon by default.    |
| `TagsInput.ItemInput`                  | Input used when a tag is edited in place.                             |
| `TagsInput.Input`                      | Entry input for new tags.                                             |
| `TagsInput.ClearTrigger`               | Clears all tags; moduix uses the shared `CloseButton.Root` by default. |
| `TagsInput.Context`                    | Render-prop access to the root state inside the component tree.       |
| `useTagsInputContext`                  | Accesses root state and methods from a descendant component.          |
| `useTagsInputItemContext`              | Accesses the current tag's state inside an item.                     |
| `moduix useTagsInput` + `RootProvider` | External state ownership and imperative methods.                     |

## Composition

### Native form control

`TagsInput.HiddenInput` is Ark's native input, exposed directly. Render it explicitly inside
`TagsInput` or `TagsInput.RootProvider`; the root does not add it. Set `name` on the root
to include the value in native form submission.

For a form outside the component tree, set `form` on the root.

### Recommended composition

Use the short `<TagsInput>` form, which is equivalent to `<TagsInput.Root>`. Place
`TagsInput.Items`, `TagsInput.Input`, and `TagsInput.ClearTrigger` inside `TagsInput.Control`.
This gives you the standard editable tag behavior. Add `TagsInput.HiddenInput` explicitly for forms.

To participate in a form, pass `name` to the root. Use `form` when the associated form is outside
the component's DOM subtree.

### When to go custom

Use `TagsInput.Context` and the lower-level item parts when tags need custom content or actions.
Map `tagsInput.value` to `TagsInput.Item`, pass its `index` and `value`, and keep `ItemInput` inside
the item so inline editing continues to work. Use `ItemContext` or `useTagsInputItemContext` when
custom item content depends on the current tag state.

Use `TagsInput.RootProvider` with moduix `useTagsInput` when state or imperative methods such as
`addValue`, `setValue`, `clearValue`, `setInputValue`, or `focus` must be available outside the
rendered tags input tree. Use `ids` when `TagsInput` shares an input or control with another Ark
primitive, such as `Combobox`.

Every primitive part supports `asChild` with one semantic child that forwards its props and `ref`.
`TagsInput.Items` is a convenience renderer and does not accept part
props; use the lower-level parts for custom item markup.

### Default props

| Prop                | Default                   | Notes                                                                 |
| ------------------- | ------------------------- | --------------------------------------------------------------------- |
| `defaultValue`      | `[]`                      | Initial tag array for uncontrolled usage.                             |
| `value`             | -                         | Controlled tag array. Update it from `onValueChange(details).value`.  |
| `defaultInputValue` | `''`                      | Initial entry input value for uncontrolled usage.                     |
| `inputValue`        | -                         | Controlled entry input value.                                         |
| `delimiter`         | `','`                     | String or regular expression used for delimiter keys and paste splitting. |
| `max`               | `Infinity`                | Maximum number of tags.                                               |
| `maxLength`         | -                         | Maximum number of characters accepted by the entry input.             |
| `allowOverflow`     | `false`                   | Allows more than `max` tags while marking the root invalid.           |
| `allowDuplicates`   | `false`                   | Allows the same string value to be added more than once.              |
| `editable`          | `true`                    | Allows a tag to enter edit mode after focus, Enter, or a double click. |
| `addOnPaste`        | `false`                   | Splits pasted text into tags using `delimiter`.                       |
| `blurBehavior`      | -                         | Set to `"add"` to commit the entry value on blur or `"clear"` to discard it. |
| `sanitizeValue`     | `(value) => value.trim()` | Normalizes each value before validation and addition.                 |

The `disabled`, `invalid`, `readOnly`, `required`, and `autoFocus` options are off unless you set
them. Use `placeholder` for the entry input and `translations` for accessible labels.

### Behavioral notes

`onValueChange(details)`, `onInputValueChange(details)`, `onHighlightChange(details)`, and
`onValueInvalid(details)` receive Ark detail objects. `validate(details)` accepts or rejects the
pending tag, while `sanitizeValue(value)` normalizes it before validation and addition. The
outside-interaction callbacks `onFocusOutside`, `onInteractOutside`, and `onPointerDownOutside`
receive their corresponding events.

`Field.Root` and `Fieldset.Root` can provide `disabled`, `invalid`, `required`, and `readOnly`
state through Ark context. The default clear trigger uses `CloseButton.Root` through Ark
`asChild`; item deletion keeps a smaller tag-level icon button.

## Examples

### Allow Duplicates

Set `allowDuplicates` when the same string may appear more than once, for example when repeated
values carry meaning.

<ExampleFrame variant="component">
  <TagsInputAllowDuplicates />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/tags-input/allow-duplicates.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/tags-input/tags-input-allow-duplicates.module.css"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Controlled

Keep the tag array in React state when another part of the screen depends on it or you need to
reset it programmatically.

<ExampleFrame variant="component">
  <TagsInputControlled />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/tags-input/controlled.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/tags-input/tags-input-controlled.module.css"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Controlled Input Value

Control `inputValue` separately when another control should prefill or clear the entry input.

<ExampleFrame variant="component">
  <TagsInputControlledInputValue />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/tags-input/controlled-input-value.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/tags-input/tags-input-controlled-input-value.module.css"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Delimiter and Paste

Use `delimiter` with `addOnPaste` when pasted text should create multiple tags. The example accepts
commas, semicolons, and spaces as separators.

<ExampleFrame variant="component">
  <TagsInputDelimiterAndPaste />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/tags-input/delimiter-and-paste.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/tags-input/tags-input-delimiter-and-paste.module.css"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Disable Editing

Set `editable={false}` when existing tags should remain removable but not editable in place.

<ExampleFrame variant="component">
  <TagsInputDisableEditing />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/tags-input/disable-editing.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/tags-input/tags-input-disable-editing.module.css"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Disabled and Read-only

Use `disabled` for a non-interactive control. Use `readOnly` when tags should remain visible but
users must not add, remove, or edit them.

<ExampleFrame variant="component">
  <TagsInputDisabledAndReadOnly />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/tags-input/disabled-and-read-only.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/tags-input/tags-input-disabled-and-read-only.module.css"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Field

Wrap `TagsInput` in `Field` when its label, helper text, invalid state, and error text should share
Ark's form accessibility wiring.

<ExampleFrame variant="component">
  <TagsInputField />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/tags-input/field.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/tags-input/tags-input-field.module.css"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Form

Pass `name` to the root and add `TagsInput.HiddenInput` to include the tag list in `FormData`. Ark
keeps the input synchronized with form reset.

<ExampleFrame variant="component">
  <TagsInputForm />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/tags-input/form.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/tags-input/tags-input-form.module.css"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Max With Overflow

Use `allowOverflow` with `max` when users may temporarily exceed the limit while the control reports
an invalid state.

<ExampleFrame variant="component">
  <TagsInputMaxWithOverflow />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/tags-input/max-with-overflow.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/tags-input/tags-input-max-with-overflow.module.css"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Root Provider

Use moduix `useTagsInput` with `RootProvider` when state and imperative methods need to be used
outside the rendered tags input tree.

<ExampleFrame variant="component">
  <TagsInputRootProvider />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/tags-input/root-provider.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/tags-input/tags-input-root-provider.module.css"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Sanitize and Blur

Use `sanitizeValue` to normalize incoming tags and `blurBehavior="add"` to commit the current
input when focus leaves the control.

<ExampleFrame variant="component">
  <TagsInputSanitizeAndBlur />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/tags-input/sanitize-and-blur.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/tags-input/tags-input-sanitize-and-blur.module.css"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Validation and Max

Use `validate`, `max`, and `onValueInvalid` to reject invalid values and observe why an addition was
rejected. Add `maxLength` when the entry input should stop accepting long values before validation
runs.

<ExampleFrame variant="component">
  <TagsInputValidationAndMax />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/tags-input/validation-and-max.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/tags-input/tags-input-validation-and-max.module.css"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### With Combobox

Compose `TagsInput` with `Combobox` when typed input should offer known options while still allowing
custom tags. The example shares IDs and composes both inputs with `asChild`.

<ExampleFrame variant="component">
  <TagsInputWithCombobox />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/tags-input/with-combobox.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/tags-input/tags-input-with-combobox.module.css"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Advanced Customization

Bypass `Items` when each tag needs custom content or actions. Map the root values to the full
Ark-shaped item tree and keep `ItemInput` in each item if inline editing is still needed.

<ExampleFrame variant="component">
  <TagsInputAdvancedCustomization />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/tags-input/advanced-customization.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/tags-input/tags-input-advanced-customization.module.css"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

## Styling

### CSS Properties

<div className="docs-reference">
  <Tabs
    values={[{ label: 'CSS Variables', value: 'CSS Variables' }]}
    className="docs-reference-tabs"
    contentItemClassName="docs-reference-panel"
  >
    <Tab>
      <TagsInputDocs.TagsInputCssPropertiesPanel />
    </Tab>
  </Tabs>
</div>

### Styling hooks

- The root and root provider expose `data-slot="tags-input-root"` and `data-slot="tags-input-root-provider"`.
- Ark parts keep `data-scope="tags-input"` and their `data-part` values from the anatomy.
- State styling can use `data-disabled`, `data-readonly`, `data-invalid`, `data-focus`, `data-empty`,
  `data-required`, `data-value`, and `data-highlighted` on the parts where Ark emits them.
- Delete and clear triggers expose stable `data-slot` hooks. Item deletion uses a compact close icon;
  clear-all uses the shared `CloseButton.Root` styling.
- `TagsInput.ClearTrigger` stays at the inline end of the control when tag rows wrap.
- In read-only state, moduix hides the entry input and clear trigger, leaving only the immutable tag
  values visible.

## Accessibility

Ark wires the label, tag highlighting, inline edit mode, deletion, paste handling, and hidden form
input. The explicit hidden input serializes the tag list for native form submission; pass `name` and,
when needed, `form` to associate it with a form.

### Keyboard support

- `Enter` in the entry input adds its current value. `Enter` on a highlighted tag enters edit mode.
- Double-clicking a tag also enters edit mode when `editable` is `true`.
- When the entry input is empty or the caret is at its start, `ArrowLeft` and `ArrowRight` move visual focus between tags.
- `Backspace` and `Delete` remove the highlighted tag. `Backspace` also removes the last tag when the entry input is empty.
- With `addOnPaste`, `Control+V` adds pasted values after splitting them with `delimiter`.
