---
title: Skeleton
component: skeleton
description: A loading placeholder for text, cards, media objects, and custom page sections.
---

import * as SkeletonDocs from '@/components/examples/skeleton/css-properties';
import SkeletonBasic from '@/components/examples/skeleton/basic.tsx';
import SkeletonCard from '@/components/examples/skeleton/card.tsx';
import SkeletonCustomHost from '@/components/examples/skeleton/custom-host.tsx';
import SkeletonLoadedContent from '@/components/examples/skeleton/loaded-content.tsx';
import SkeletonMediaObject from '@/components/examples/skeleton/media-object.tsx';
import SkeletonResponsiveLayout from '@/components/examples/skeleton/responsive-layout.tsx';
import SkeletonVariants from '@/components/examples/skeleton/variants.tsx';

## API Reference

Ark UI does not ship a dedicated `Skeleton` primitive. moduix builds `Skeleton` with the official
[Ark factory](https://ark-ui.com/docs/guides/composition#the-ark-factory) and follows Ark's guidance for
[composition](https://ark-ui.com/docs/guides/composition), [styling](https://ark-ui.com/docs/guides/styling), and
[refs](https://ark-ui.com/docs/guides/ref).

## Basic

Start with a few `Skeleton` lines when text is still loading.

<ExampleFrame variant="component">
  <SkeletonBasic />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/skeleton/basic.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/skeleton/skeleton-basic.module.css"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

## Install with shadcn

<ShadcnInstall packageName="skeleton" />

## Anatomy

`Skeleton` is a single visual part. Keep layout in the surrounding markup.

```text
Skeleton.Root
└─ children (optional, shown when loading={false})
```

| Part                         | Role                                                                                        |
| ---------------------------- | ------------------------------------------------------------------------------------------- |
| `Skeleton` / `Skeleton.Root` | Placeholder block. Renders an Ark factory element and accepts `asChild` when you need a different semantic root element. |

## Composition

### Recommended composition

Use `Skeleton` directly for text lines, cards, avatars, and media placeholders. While `loading` is true,
it renders a decorative block. Set `loading={false}` to show children in the same root element.

The default variant is `pulse`, which animates the placeholder. Use `variant="none"` for a static
placeholder. The component also disables the animation when the user prefers reduced motion.

### When to go custom

Use `asChild` when the root should be a semantic element other than `div`. The child must be one
semantic element, as required by Ark's composition model.

Use `width`, `height`, `boxSize`, and `borderRadius` for common dimensions. For a more specialized
appearance, use `className`, `style`, or the public CSS variables.

### Default props

| Prop           | Default                                | Notes                                                                                                       |
| -------------- | -------------------------------------- | ----------------------------------------------------------------------------------------------------------- |
| `loading`      | `true`                                 | Controls whether placeholder styles are active or children are shown normally.                              |
| `variant`      | `pulse`                                | Supports `pulse` and `none`.                                                                                |
| `height`       | `var(--moduix-spacing-4)` (while loading) | Loading CSS sets the default placeholder height. Loaded content uses natural height unless a height is set. |
| `width`        | `100%`                                 | CSS sets the default root width. Numeric prop values become `px`.                                           |
| `boxSize`      | Not set                              | Sets width and height together. An explicit `width` or `height` takes precedence on that axis.              |
| `borderRadius` | `var(--moduix-skeleton-border-radius, var(--moduix-radius-md))` | Numeric prop values become `px`; inline style overrides still win. |

### Behavioral notes

- `Skeleton.Root` is the same component as `Skeleton`, provided for namespace consistency.
- Refs are forwarded to the root element.
- `loading={true}` sets `aria-hidden`, `data-state="loading"`, and `data-loading` unless you explicitly set `aria-hidden`.
- `loading={false}` sets `data-state="loaded"` and lets children use their natural height unless `height`, `boxSize`, or `style.height` is set.
- `variant` is styling-only; no internal timers or state management are added.
- `Skeleton` is decorative and does not announce loading. Put `aria-busy` on the region that owns the loading state, or use `Spinner` when an inline status must be announced.

## Examples

### Card

Use a larger block and shorter text lines to reserve the card layout while content is loading.

<ExampleFrame variant="component">
  <SkeletonCard />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/skeleton/card.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/skeleton/skeleton-card.module.css"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Loaded Content

Use `loading` to keep the same root element in place while switching from the placeholder to real content.

<ExampleFrame variant="component">
  <SkeletonLoadedContent />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/skeleton/loaded-content.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/skeleton/skeleton-loaded-content.module.css"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Media Object

Use `boxSize` with a full radius for an avatar placeholder, then place text lines beside it.

<ExampleFrame variant="component">
  <SkeletonMediaObject />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/skeleton/media-object.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/skeleton/skeleton-media-object.module.css"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Responsive Layout

Keep responsive layout logic in the wrapper while each `Skeleton` remains a visual building block.

<ExampleFrame variant="component">
  <SkeletonResponsiveLayout />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/skeleton/responsive-layout.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/skeleton/skeleton-responsive-layout.module.css"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Variants

Use `variant` to choose between pulse and static placeholder rendering.

<ExampleFrame variant="component">
  <SkeletonVariants />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/skeleton/variants.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/skeleton/skeleton-variants.module.css"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Advanced Customization

Use `asChild` when a semantic element such as `section` should become the DOM root instead of the default `div`.

<ExampleFrame variant="component">
  <SkeletonCustomHost />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/skeleton/custom-host.tsx"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

## Styling

`Skeleton` exposes Ark-style part and state attributes plus public `--moduix-skeleton-*` variables for
its color, radius, height, and animation.

### CSS Variables

<div className="docs-reference">
  <Tabs
    values={[{ label: 'CSS Variables', value: 'CSS Variables' }]}
    className="docs-reference-tabs"
    contentItemClassName="docs-reference-panel"
  >
    <Tab>
      <SkeletonDocs.SkeletonCssPropertiesPanel />
    </Tab>
  </Tabs>
</div>

### Styling hooks

- `[data-scope="skeleton"][data-part="root"]` targets the exported root.
- `data-slot="skeleton-root"` is the stable moduix selector for the root.
- `data-state="loading"` and `data-state="loaded"` reflect the `loading` prop.
- `data-variant="pulse"` and `data-variant="none"` reflect the variant.
- `data-loading` is present only while placeholder styles are active.
