---
title: Skeleton
component: skeleton
description: A loading placeholder for text, cards, media, and custom page sections in React and Solid.
---

import * as SkeletonDocs from '@/components/examples/skeleton/css-properties';
import SkeletonBasic from '@/components/examples/skeleton/basic.tsx';
import SkeletonCard from '@/components/examples/skeleton/card.tsx';
import SkeletonCustomHost from '@/components/examples/skeleton/custom-host.tsx';
import SkeletonLoadedContent from '@/components/examples/skeleton/loaded-content.tsx';
import SkeletonMediaObject from '@/components/examples/skeleton/media-object.tsx';
import SkeletonResponsiveLayout from '@/components/examples/skeleton/responsive-layout.tsx';
import SkeletonVariants from '@/components/examples/skeleton/variants.tsx';
import skeletonSolidBasic from '../../../snippets/skeleton/solid/basic.tsx?raw';
import skeletonSolidCard from '../../../snippets/skeleton/solid/card.tsx?raw';
import skeletonSolidCustomHost from '../../../snippets/skeleton/solid/custom-host.tsx?raw';
import skeletonSolidLoadedContent from '../../../snippets/skeleton/solid/loaded-content.tsx?raw';
import skeletonSolidMediaObject from '../../../snippets/skeleton/solid/media-object.tsx?raw';
import skeletonSolidResponsiveLayout from '../../../snippets/skeleton/solid/responsive-layout.tsx?raw';
import skeletonSolidVariants from '../../../snippets/skeleton/solid/variants.tsx?raw';

## API Reference

Ark UI does not ship a dedicated `Skeleton` primitive. moduix builds `Skeleton` with the official
[Ark factory](https://ark-ui.com/docs/guides/composition#the-ark-factory) and follows Ark's guidance for
[composition](https://ark-ui.com/docs/guides/composition), [styling](https://ark-ui.com/docs/guides/styling), and
[refs](https://ark-ui.com/docs/guides/ref).

## Basic

Skeleton ships for React and Solid in both the CSS Modules and Tailwind packages. The rendered preview on this
page uses the React CSS Modules package; the framework tabs show native source for the same public contract.

Use `Skeleton` for text lines, cards, avatars, and media placeholders. The surrounding layout owns spacing and
responsive composition. See [Quick Start](/docs/quick-start) for package setup, registry configuration, and
choosing a styling track.

<ExampleFrame variant="component">
  <SkeletonBasic />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/skeleton/basic.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={skeletonSolidBasic} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/skeleton/skeleton-basic.module.css"
```
</div>

</ExampleCode>

## Install with shadcn

<Tabs groupId="framework">
  <Tab label="React" value="react">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-react/skeleton' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-react-tailwind/skeleton' },
  ]}
/>

  </Tab>
  <Tab label="Solid" value="solid">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-solid/skeleton' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-solid-tailwind/skeleton' },
  ]}
/>

  </Tab>
</Tabs>

## Anatomy

`Skeleton` is a single visual part. Keep layout in the surrounding markup.

```text
Skeleton.Root
└─ children (optional, shown when loading={false})
```

| Part                         | Role                                                                                        |
| ---------------------------- | ------------------------------------------------------------------------------------------- |
| `Skeleton` / `Skeleton.Root` | Placeholder block. Renders an Ark factory element and accepts `asChild` when you need a different semantic root element. |

## Composition

`Skeleton` does not own loading state. The data or page region that owns the request passes `loading` and should
carry `aria-busy`; the placeholder itself is decorative. Set `loading={false}` to reveal children through the
same root element.

`Skeleton` is a single visual part, so compose cards, media rows, and responsive layouts with your own wrapper.
CSS Modules packages can use `Stack`; Tailwind packages can use native flex and grid utilities. Use `asChild`
only when a different semantic root is required; Solid passes a render function to `asChild`.

## Examples

### Card

Use a larger block and shorter text lines to reserve the card layout while content is loading.

<ExampleFrame variant="component">
  <SkeletonCard />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/skeleton/card.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={skeletonSolidCard} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/skeleton/skeleton-card.module.css"
```
</div>

</ExampleCode>

### Loaded Content

Use `loading` to keep the same root element in place while switching from the placeholder to real content.

<ExampleFrame variant="component">
  <SkeletonLoadedContent />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/skeleton/loaded-content.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={skeletonSolidLoadedContent} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/skeleton/skeleton-loaded-content.module.css"
```
</div>

</ExampleCode>

### Media Object

Use `boxSize` with a full radius for an avatar placeholder, then place text lines beside it.

<ExampleFrame variant="component">
  <SkeletonMediaObject />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/skeleton/media-object.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={skeletonSolidMediaObject} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/skeleton/skeleton-media-object.module.css"
```
</div>

</ExampleCode>

### Responsive Layout

Keep responsive layout logic in the wrapper while each `Skeleton` remains a visual building block.

<ExampleFrame variant="component">
  <SkeletonResponsiveLayout />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/skeleton/responsive-layout.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={skeletonSolidResponsiveLayout} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/skeleton/skeleton-responsive-layout.module.css"
```
</div>

</ExampleCode>

### Variants

Use `variant` to choose between pulse and static placeholder rendering.

<ExampleFrame variant="component">
  <SkeletonVariants />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/skeleton/variants.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={skeletonSolidVariants} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/skeleton/skeleton-variants.module.css"
```
</div>

</ExampleCode>

### Advanced Customization

Use `asChild` when a semantic element such as `section` should become the DOM root instead of the default `div`.

<ExampleFrame variant="component">
  <SkeletonCustomHost />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/skeleton/custom-host.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={skeletonSolidCustomHost} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/skeleton/skeleton-custom-host.module.css"
```
</div>

</ExampleCode>

## Styling

Use `className` in React or `class` in Solid for structural styles and consumer utility classes. Tailwind
variants use native utilities and consumer classes; CSS Modules variants expose the variables below. State
attributes and `data-slot` hooks are shared by both styling tracks.

### CSS Variables

<div className="docs-reference">
  <Tabs
    values={[{ label: 'CSS Variables', value: 'CSS Variables' }]}
    className="docs-reference-tabs"
    contentItemClassName="docs-reference-panel"
  >
    <Tab>
      <SkeletonDocs.SkeletonCssPropertiesPanel />
    </Tab>
  </Tabs>
</div>

### Styling hooks

- `[data-scope="skeleton"][data-part="root"]` targets the exported root.
- `data-slot="skeleton-root"` is the stable moduix selector for the root.
- `data-state="loading"` and `data-state="loaded"` reflect the `loading` prop.
- `data-variant="pulse"` and `data-variant="none"` reflect the variant.
- `data-loading` is present only while placeholder styles are active.
