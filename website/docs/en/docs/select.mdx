---
title: Select
component: select
description: Ark UI select composition for choosing one or more values from a popup list.
---

import * as SelectDocs from '@/components/examples/select/css-properties';
import SelectAdvancedCustomization from '@/components/examples/select/advanced-customization.tsx';
import SelectBasic from '@/components/examples/select/basic.tsx';
import SelectControlled from '@/components/examples/select/controlled.tsx';
import SelectCustomItemLayout from '@/components/examples/select/custom-item-layout.tsx';
import SelectDynamicItems from '@/components/examples/select/dynamic-items.tsx';
import SelectField from '@/components/examples/select/field.tsx';
import SelectFormUsage from '@/components/examples/select/form-usage.tsx';
import SelectGrouping from '@/components/examples/select/grouping.tsx';
import SelectLazyMount from '@/components/examples/select/lazy-mount.tsx';
import SelectMaxSelection from '@/components/examples/select/max-selection.tsx';
import SelectMultiple from '@/components/examples/select/multiple.tsx';
import SelectNativeFormControl from '@/components/examples/select/native-form-control.tsx';
import SelectOverflow from '@/components/examples/select/overflow.tsx';
import SelectRootProvider from '@/components/examples/select/root-provider.tsx';
import SelectSearchInPopup from '@/components/examples/select/search-in-popup.tsx';
import SelectSelectAll from '@/components/examples/select/select-all.tsx';
import SelectSelectOnHighlight from '@/components/examples/select/select-on-highlight.tsx';

## API Reference

<PrimitiveReference href="https://ark-ui.com/docs/components/select" />

## Choosing the right component

Use `Select` when users choose from a fixed collection and the trigger stays read-only. Use
`Combobox` when typing helps find or create a value.

## Basic

Pass a collection to `Select`, use `Select.Field` for the standard visible control, and keep the
popup structure explicit. `clearLabel` adds an accessible clear action. Add `Select.HiddenSelect`
explicitly when native submission, autofill, validation, and reset are needed.

<ExampleFrame variant="component">
  <SelectBasic />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/select/basic.tsx"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

## Install with shadcn

<ShadcnInstall packageName="select" />

## Anatomy

```text
Select / Select.Root
├─ Select.Label
├─ Select.Control / Select.Field
│  ├─ Select.Trigger
│  │  ├─ Select.ValueText
│  ├─ Select.ClearTrigger
│  └─ Select.Indicator
├─ Overlay subtree (automatically portalled)
│  └─ Select.Positioner
│     └─ Select.Content
│        ├─ Select.List
│        └─ Select.ItemGroup
│           ├─ Select.ItemGroupLabel
│           └─ Select.Item[item]
│              ├─ Select.ItemText
│              │  └─ Select.ItemTextContent
│              │     ├─ Select.ItemTextIcon
│              │     └─ Select.ItemTextLabel
│              └─ Select.ItemIndicator
└─ Select.HiddenSelect (explicit)
```

| Part                     | Role                                                                   |
| ------------------------ | ---------------------------------------------------------------------- |
| `Select` / `Select.Root` | Root state, collection, selection, popup lifecycle, and form data.     |
| `Select.RootProvider`    | Renders a select tree from state returned by `Select.useSelect`.       |
| `Select.Label`           | Accessible label inherited by the trigger and native form control.     |
| `Select.Control`         | Visual field wrapper that receives focus, invalid, and open state.     |
| `Select.Field`           | moduix helper that renders the standard control, trigger, and actions. |
| `Select.Trigger`         | Button that opens the popup and owns keyboard entry.                   |
| `Select.ValueText`       | Placeholder or selected item labels.                                   |
| `Select.ClearTrigger`    | Button that clears the current value.                                  |
| `Select.Indicator`       | Trigger indicator with the moduix default chevron icon.                |
| `Select.Positioner`      | Floating layer that exposes Ark positioning CSS variables.             |
| `Select.Content`         | Popup surface and listbox container.                                   |
| `Select.List`            | Optional list wrapper for virtualized or custom list layouts.          |
| `Select.ItemGroup`       | Group wrapper for related collection items.                            |
| `Select.ItemGroupLabel`  | Label for a collection group.                                          |
| `Select.Item`            | Selectable row. Pass the collection item through `item`.               |
| `Select.ItemText`        | Text used for display and accessibility.                               |
| `Select.ItemTextContent` | Layout helper for rich item text.                                     |
| `Select.ItemTextIcon`    | Icon wrapper for rich item text.                                      |
| `Select.ItemTextLabel`   | Label wrapper for rich item text.                                    |
| `Select.ItemIndicator`   | Selected-state indicator with the moduix default check icon.           |

## Composition

The root portals `Positioner` automatically. Use `portalled={false}` for inline rendering and
`portalRef` for a custom container; `Positioner` and `Content` remain explicit and styleable.

### Recommended composition

`Select.Field` renders `Control`, `Trigger`, `ValueText`, and the default indicator. Pass
`clearLabel` to include a labelled clear action, or `indicator={<Icon />}` to replace only the
chevron. Keep `Positioner`, `Content`, and items visible in the composition.

`Select` is the concise root form. `Select.Root` is the equivalent member of the namespace.

### When to go custom

Use explicit `Control`, `Trigger`, and visual parts when their layout needs independent
composition. `asChild` requires one semantic container that can accept Ark props and render its
children; use a container such as `div` or `section`, not an interactive element such as `button`.
Use `Select.List` for custom list wrappers and virtualization paths.

### Default props

The portalled overlay content lazily mounts and unmounts after its exit animation by default. Set
`unmountOnExit={false}` to retain it after first open; set both lifecycle props to `false` only when
eager initial rendering is needed.

| Prop                    | Default | Notes                                                          |
| ----------------------- | ------- | -------------------------------------------------------------- |
| `closeOnSelect`         | `true`  | Closes the popup after item selection.                         |
| `composite`             | `true`  | Enables listbox keyboard composition.                          |
| `defaultValue`          | `[]`    | Values are string arrays, including single selection.          |
| `lazyMount`             | `true`  | Defers popup content mounting until opened.                    |
| `loopFocus`             | `false` | Keeps arrow navigation from wrapping by default.               |
| `multiple`              | `false` | Enables multiple values when set.                              |
| `unmountOnExit`         | `true`  | Removes popup content after its exit animation.                |
| `ValueText.placeholder` | -       | Text shown when the value array is empty.                      |

### Behavioral notes

`value`, `defaultValue`, and `onValueChange(details)` use string arrays. Read `details.value` for
selected keys and `details.items` for collection items. `onOpenChange(details)`,
`onHighlightChange(details)`, and `onSelect(details)` keep Ark detail objects unchanged.

Use `Select.useSelect` with `Select.RootProvider` when state is needed outside the select tree.
`Select.Context`, `Select.ItemContext`, `Select.useSelectContext`, and
`Select.useSelectItemContext` are available for in-tree state reads. `Field.Root` and
`Fieldset.Root` provide disabled, invalid, required, and read-only state through Ark context.

`Select.HiddenSelect` preserves browser validation, autofill, reset, and progressive enhancement.
For a virtualized collection where rendering every native option is undesirable, provide
application-owned form integration.

Select removes trigger transitions and popup enter/exit animation when the user requests reduced
motion; selection, focus restoration, and mount lifecycle remain unchanged.

## Examples

### Controlled

Control the selected string array from React state and update it from `details.value`.

<ExampleFrame variant="component">
  <SelectControlled />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/select/controlled.tsx"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Custom Item Layout

Use `Select.ItemTextContent`, `Select.ItemTextIcon`, and `Select.ItemTextLabel` inside `ItemText`
when an item needs a stable icon and an ellipsized label.

<ExampleFrame variant="component">
  <SelectCustomItemLayout />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/select/custom-item-layout.tsx"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Dynamic Items

Update the collection when available items change; the existing Select composition stays the same.

<ExampleFrame variant="component">
  <SelectDynamicItems />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/select/dynamic-items.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/select/select-dynamic-items.module.css"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Field

Wrap Select in `Field.Root` when label, helper text, invalid, required, or disabled state belongs
to a form field.

<ExampleFrame variant="component">
  <SelectField />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/select/field.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/select/select-field.module.css"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Form Usage

The explicitly composed `Select.HiddenSelect` keeps native form submission working. Read submitted values from
`FormData`.

<ExampleFrame variant="component">
  <SelectFormUsage />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/select/form-usage.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/select/select-form-usage.module.css"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Grouping

Use `groupBy` on the collection and render `collection.group()` with `ItemGroup` and
`ItemGroupLabel`.

<ExampleFrame variant="component">
  <SelectGrouping />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/select/grouping.tsx"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Lazy Mount

Use `lazyMount` and `unmountOnExit` when popup content should exist only while the Select is used.

<ExampleFrame variant="component">
  <SelectLazyMount />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/select/lazy-mount.tsx"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Max Selection

Limit multiple selection by rejecting oversized value arrays and disabling the remaining items.

<ExampleFrame variant="component">
  <SelectMaxSelection />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/select/max-selection.tsx"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Multiple

Set `multiple` to allow several selected values. `ValueText` renders selected labels from the
collection.

<ExampleFrame variant="component">
  <SelectMultiple />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/select/multiple.tsx"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Native Form Control

Compose `Select.HiddenSelect` explicitly when the selection should participate in a native form.
This example focuses on the submitted `FormData`.

<ExampleFrame variant="component">
  <SelectNativeFormControl />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/select/native-form-control.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/select/select-native-form-control.module.css"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Overflow

Use `positioning.fitViewport` for long lists. The content already limits its height with Ark's
`--available-height` variable and remains scrollable.

<ExampleFrame variant="component">
  <SelectOverflow />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/select/overflow.tsx"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Root Provider

Use `Select.useSelect` with `Select.RootProvider` when state is read or controlled outside the
rendered Select tree.

<ExampleFrame variant="component">
  <SelectRootProvider />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/select/root-provider.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/select/select-root-provider.module.css"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Search in Popup

Place a filter control beside the listbox inside the popup. Keep the query state and filtered
collection in your component rather than extending the Select API.

<ExampleFrame variant="component">
  <SelectSearchInPopup />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/select/search-in-popup.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/select/select-search-in-popup.module.css"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Select All

Keep bulk actions outside the listbox. `Select.useSelect` keeps the action and the Select backed by
the same Ark state instance.

<ExampleFrame variant="component">
  <SelectSelectAll />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/select/select-all.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/select/select-select-all.module.css"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Select On Highlight

Use `onHighlightChange(details)` when moving through options should immediately update selection.

<ExampleFrame variant="component">
  <SelectSelectOnHighlight />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/select/select-on-highlight.tsx"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Advanced Customization

Bypass `Select.Field` when the trigger, value text, or indicator needs independent composition and
styling.

<ExampleFrame variant="component">
  <SelectAdvancedCustomization />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }]} className="docs-tabs">
    <Tab value="React">

```tsx pure file="../../../src/components/examples/select/advanced-customization.tsx"
```

    </Tab>
  </Tabs>
</div>

</ExampleCode>

## Styling

Style Select through `className`, stable `data-slot="select-*"` hooks, Ark attributes, and the
public `--moduix-select-*` CSS variables.

### CSS Properties

<CssPropertiesSection properties={SelectDocs.selectOverrideCssProperties} />

### Styling hooks

`Select.Field` renders the existing `select-control`, `select-trigger`, `select-value-text`,
`select-indicator`, and optional `select-clear-trigger` slots. Its `indicator` prop supplies the
child of the existing indicator slot. Rich item text helpers expose `select-item-text-content`,
`select-item-text-icon`, and `select-item-text-label` slots.

- Roots expose `data-slot="select-root"` and `data-slot="select-root-provider"`.
- Ark anatomy uses `data-scope="select"` and `data-part`.
- State styling uses `data-state="open" | "closed"`, `data-focus`, `data-invalid`,
  `data-disabled`, `data-readonly`, `data-required`, `data-placeholder-shown`, `data-highlighted`,
  and item `data-state="checked" | "unchecked"`.
- `Select.Positioner` exposes `--reference-width`, `--reference-height`, `--available-width`,
  `--available-height`, `--x`, `--y`, `--z-index`, and `--transform-origin`.
