---
title: Text
component: text
description: A typography component for body text, inline content, semantic emphasis, and truncated previews.
---

import * as TextDocs from '@/components/examples/text/css-properties';
import TextAlignment from '@/components/examples/text/alignment.tsx';
import TextBasic from '@/components/examples/text/basic.tsx';
import TextCustomComposition from '@/components/examples/text/custom-composition.tsx';
import TextSemantics from '@/components/examples/text/semantics.tsx';
import TextSize from '@/components/examples/text/size.tsx';
import TextTone from '@/components/examples/text/tone.tsx';
import TextTruncation from '@/components/examples/text/truncation.tsx';
import TextWeight from '@/components/examples/text/weight.tsx';
import textSolidAlignment from '../../../snippets/text/solid/alignment.tsx?raw';
import textSolidBasic from '../../../snippets/text/solid/basic.tsx?raw';
import textSolidCustomComposition from '../../../snippets/text/solid/custom-composition.tsx?raw';
import textSolidSemantics from '../../../snippets/text/solid/semantics.tsx?raw';
import textSolidSize from '../../../snippets/text/solid/size.tsx?raw';
import textSolidTone from '../../../snippets/text/solid/tone.tsx?raw';
import textSolidTruncation from '../../../snippets/text/solid/truncation.tsx?raw';
import textSolidWeight from '../../../snippets/text/solid/weight.tsx?raw';

## API Reference

Ark UI does not provide a dedicated `Text` component. moduix provides this typography component
with the official [Ark factory](https://ark-ui.com/docs/guides/composition#the-ark-factory) and follows Ark's
[composition](https://ark-ui.com/docs/guides/composition), [styling](https://ark-ui.com/docs/guides/styling), and
[ref](https://ark-ui.com/docs/guides/ref) guidance.

## Choosing the right component

Use `Text` for body copy, helper text, inline emphasis, and other supporting UI text that belongs
on moduix's typography scale.

- Choose **`Heading`** for document structure and heading hierarchy.
- Choose **`Text`** when content should keep paragraph or inline semantics while using body-text styles.
- Use local CSS when the concern is rich text layout, spacing, or disclosure around clamped text.

## Basic


Use the default `p` root for a paragraph. Pass `as` when the content needs a different semantic HTML
element but should keep `Text`'s typography styles.

<ExampleFrame variant="component">
  <TextBasic />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/text/basic.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={textSolidBasic} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/text/text-basic.module.css"
```
</div>

</ExampleCode>

## Install with shadcn

See [Quick Start](/docs/quick-start) for full package setup, registry configuration, and styling guidance.

<Tabs groupId="framework">
  <Tab label="React" value="react">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-react/text' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-react-tailwind/text' },
  ]}
/>

  </Tab>
  <Tab label="Solid" value="solid">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-solid/text' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-solid-tailwind/text' },
  ]}
/>

  </Tab>
</Tabs>

## Anatomy

`Text` renders a single root element using Ark's factory. The short `<Text>` form and `<Text.Root>`
are equivalent.

```text
Text / Text.Root
└─ text or inline content
```

| Part                 | Role                                                             |
| -------------------- | ---------------------------------------------------------------- |
| `Text` / `Text.Root` | Typography root with semantic HTML and stable data attributes. |

## Composition

`Text` is root-only and owns no state or child parts. Use `<Text>` or `<Text.Root>` for the same root,
`as` for a supported semantic element, and `asChild` only when one existing element should receive
the text props and ref. Truncation only constrains rendering, so disclosure state and its control stay
in the surrounding UI.

## Examples

### Alignment

Use `align` when alignment belongs to the text rather than its surrounding layout container.

<ExampleFrame variant="component">
  <TextAlignment />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/text/alignment.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={textSolidAlignment} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/text/text-alignment.module.css"
```
</div>

</ExampleCode>

### Semantics

Use `as` to choose a semantic HTML element while keeping `Text`'s typography styles.

<ExampleFrame variant="component">
  <TextSemantics />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/text/semantics.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={textSolidSemantics} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/text/text-semantics.module.css"
```
</div>

</ExampleCode>

### Size

Use `size` to change the visual hierarchy without switching to a different typography component.

<ExampleFrame variant="component">
  <TextSize />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/text/size.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={textSolidSize} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/text/text-size.module.css"
```
</div>

</ExampleCode>

### Tone

Use `tone` to express the role of the copy without adding local color classes.

<ExampleFrame variant="component">
  <TextTone />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/text/tone.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={textSolidTone} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/text/text-tone.module.css"
```
</div>

</ExampleCode>

### Truncation

Use `truncate` for a single-line ellipsis and `lineClamp` for dense multi-line previews. Add the
expansion or disclosure behavior in the surrounding UI.

<ExampleFrame variant="component">
  <TextTruncation />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/text/truncation.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={textSolidTruncation} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/text/text-truncation.module.css"
```
</div>

</ExampleCode>

### Weight

Use `weight` when the emphasis should stay within the same semantic element.

<ExampleFrame variant="component">
  <TextWeight />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/text/weight.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={textSolidWeight} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/text/text-weight.module.css"
```
</div>

</ExampleCode>

### Advanced Customization

Use `asChild` when the root should be an existing custom element instead of one of the supported
intrinsic `as` values. The child must be a single element that accepts the forwarded props and ref.

<ExampleFrame variant="component">
  <TextCustomComposition />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/text/custom-composition.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={textSolidCustomComposition} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

## Styling

Style `Text` through `className`, root data attributes, and the public CSS API. CSS Modules packages
expose the `--moduix-text-*` variables below; Tailwind packages use utility classes and consumer
class overrides.

### CSS Properties

<div className="not-prose docs-reference">
  <Tabs
    values={[{ label: 'CSS Variables', value: 'CSS Variables' }]}
    className="docs-reference-tabs"
    contentItemClassName="docs-reference-panel"
  >
    <Tab>
      <TextDocs.TextCssPropertiesPanel />
    </Tab>
  </Tabs>
</div>

### Styling hooks

- `data-scope="text"` and `data-part="root"` identify the Ark-style root.
- `data-slot="text-root"` is the stable moduix selector on the exported root.
- `data-size` reflects the resolved visual size: `xs`, `sm`, `md`, `lg`, or `xl`.
- `data-weight` reflects the resolved weight preset: `regular`, `medium`, `semibold`, or `bold`.
- `data-tone` reflects the active tone preset: `default`, `muted`, `subtle`, `primary`, or
  `destructive`.
- `data-align` appears only when `align` is explicitly set to `start`, `center`, `end`, `left`,
  `right`, or `justify`.
- `data-truncate` appears when single-line ellipsis is active.
- `data-line-clamp` appears when `lineClamp` is a positive integer. In that case, `lineClamp` sets
  the fallback line count. Set `--moduix-text-line-clamp` to override it through the public CSS API.
