---
title: Editable
component: editable
description: Inline text editing with Ark UI, preview and input states, edit controls, and native form support.
---

import * as EditableDocs from '@/components/examples/editable/css-properties';
import EditableAdvancedCustomization from '@/components/examples/editable/advanced-customization.tsx';
import EditableBasic from '@/components/examples/editable/basic.tsx';
import EditableControlled from '@/components/examples/editable/controlled.tsx';
import EditableField from '@/components/examples/editable/field.tsx';
import EditableForm from '@/components/examples/editable/form.tsx';
import EditableGuides from '@/components/examples/editable/guides.tsx';
import EditableRootProvider from '@/components/examples/editable/root-provider.tsx';
import EditableStates from '@/components/examples/editable/disabled-and-read-only.tsx';
import EditableTextarea from '@/components/examples/editable/textarea.tsx';

## API Reference

<PrimitiveReference href="https://ark-ui.com/docs/components/editable" />

## Choosing the right component

- Choose **`Input`** or **`Textarea`** when the value should always be available for editing.
- Choose **`Editable`** when the value is usually read-only and editing should be an explicit action.
- Wrap **`Editable`** in **`Field`** when helper text, error text, or shared form state should appear
  outside the component.

## Basic

Use `Editable` as the root and compose it from `Editable.Label`, `Editable.Area`, `Editable.Input`,
and `Editable.Preview`. Add `Editable.Controls` for the standard edit, submit, and cancel flow.
The moduix wrapper adds styles, `data-slot` hooks, and default trigger icons. By default, users
enter edit mode with a double-click, which helps prevent accidental changes.

<ExampleFrame variant="component">
  <EditableBasic />
</ExampleFrame>

<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }]} className="docs-tabs">
    <Tab value="React">

```tsx file="../../../src/components/examples/editable/basic.tsx"
```

    </Tab>
  </Tabs>
</div>

## Install with shadcn

<ShadcnInstall packageName="editable" />

## Anatomy

```text
Editable.Root
├─ Editable.Label
├─ Editable.Area
│  ├─ Editable.Input
│  └─ Editable.Preview
├─ Editable.Control or Editable.Controls
│  ├─ Editable.EditTrigger
│  ├─ Editable.SubmitTrigger
│  └─ Editable.CancelTrigger

Editable.RootProvider
└─ same part tree connected to Ark `useEditable()` state
```

| Part                         | Role                                                   |
| ---------------------------- | ------------------------------------------------------ |
| `Editable` / `Editable.Root` | Component state, callbacks, form props, and accessibility wiring. |
| `Editable.RootProvider`      | Root connected to a `useEditable()` instance.                    |
| `Editable.Label`             | Visible label for the input and preview.                          |
| `Editable.Area`              | Shared container for the input and preview.                       |
| `Editable.Input`             | Text input shown while editing.                                   |
| `Editable.Preview`           | Read-only value or placeholder shown outside edit mode.           |
| `Editable.Control`           | Optional container for the edit, submit, and cancel buttons.      |
| `Editable.Controls`          | Convenience wrapper for the standard edit, submit, and cancel flow. |
| `Editable.EditTrigger`       | Button that enters edit mode.                                     |
| `Editable.SubmitTrigger`     | Button that commits the current value.                            |
| `Editable.CancelTrigger`     | Button that reverts the current value.                            |

## Composition

### Recommended composition

Place `Editable.Input` and `Editable.Preview` as siblings inside `Editable.Area`. Put
`Editable.Controls` after the area to use the standard trigger layout. For a single-line input,
the controls appear on the right and are vertically centered. Textarea compositions use the same
structure. Set `--moduix-editable-control-align: start` to align the controls with the top of the
multiline area.

### When to go custom

Use moduix `Editable.Context` or `useEditableContext()` when reusable child parts or custom content
need access to the current edit state. Use `useEditable()` with `Editable.RootProvider` when the
state or methods need to live outside the rendered `Editable` tree. `RootProvider` replaces
`Editable.Root` for that state instance. Use `asChild` when the input should render as another
semantic element, such as a native `<textarea>`.

### Default props

| Prop             | Default    | Notes                                                                                                                 |
| ---------------- | ---------- | --------------------------------------------------------------------------------------------------------------------- |
| `activationMode` | `dblclick` | moduix uses double-click by default. Pass `focus`, `click`, or `none` to choose a different Ark activation mode. |
| `selectOnFocus`  | `true`     | Ark selects input text when the input receives focus.                                                                 |
| `submitMode`     | `both`     | `Enter` and blur commit by default. Use `none` when only explicit submit controls should commit the value. |

### Behavioral notes

The `value` and `defaultValue` props set the text value. The `edit` and `defaultEdit` props control
edit mode. Ark callback props keep their `details` objects, so `onValueChange`, `onValueCommit`,
`onValueRevert`, and `onEditChange` receive the same payload shapes as in Ark UI.

Press `Enter` to commit the value and `Escape` to revert it. When `Editable.Input` renders as a
textarea through `asChild`, Cmd/Ctrl + Enter commits the value. `name` and `form` are forwarded to
Ark for native form submission. `disabled` prevents interaction, while `readOnly` keeps the value
visible without allowing edits.

Set `translations.input` to provide a context-specific accessible name for the input.
`Editable.Label` remains the visible label.

## Examples

### Controlled

Use `value` and `onValueChange` when application state should be the source of truth for the value.

<ExampleFrame variant="component">
  <EditableControlled />
</ExampleFrame>

<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx file="../../../src/components/examples/editable/controlled.tsx"
```

    </Tab>
  </Tabs>
</div>

### Field

Wrap `Editable` in `Field` when validation state or an error message should appear outside the component.

<ExampleFrame variant="component">
  <EditableField />
</ExampleFrame>

<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }]} className="docs-tabs">
    <Tab value="React">

```tsx file="../../../src/components/examples/editable/field.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/editable/editable-field.module.css"
```

    </Tab>
  </Tabs>
</div>

### Ark options

The root accepts common Ark options such as activation mode, auto-resize, maximum length,
mode-specific placeholders, and localized control labels. This example sets `activationMode` to
`click` to show how to replace moduix's default double-click activation.

<ExampleFrame variant="component">
  <EditableGuides />
</ExampleFrame>

<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }]} className="docs-tabs">
    <Tab value="React">

```tsx file="../../../src/components/examples/editable/guides.tsx"
```

    </Tab>
  </Tabs>
</div>

### Root Provider

Use moduix `useEditable()` and `Editable.RootProvider` when controls outside the component tree
need access to `Editable` state and methods. Pass `activationMode: 'dblclick'` to the hook to match
the root component's default activation behavior. The provider replaces `Editable.Root` for this
state instance.

<ExampleFrame variant="component">
  <EditableRootProvider />
</ExampleFrame>

<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx file="../../../src/components/examples/editable/root-provider.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/editable/editable-root-provider.module.css"
```

    </Tab>
  </Tabs>
</div>

### Textarea

Use `Editable.Input asChild` with a native `<textarea>` for multiline content. The example sets the
textarea size so the copied snippet works as-is.

<ExampleFrame variant="component">
  <EditableTextarea />
</ExampleFrame>

<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx file="../../../src/components/examples/editable/textarea.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/editable/editable-textarea.module.css"
```

    </Tab>
  </Tabs>
</div>

### Advanced Customization

Use `Editable.Context` directly when a custom state-dependent layout needs access to the current
edit state. The example skips `Editable.Controls` and shows keyboard instructions while editing.

<ExampleFrame variant="component">
  <EditableAdvancedCustomization />
</ExampleFrame>

<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }]} className="docs-tabs">
    <Tab value="React">

```tsx file="../../../src/components/examples/editable/advanced-customization.tsx"
```

    </Tab>
  </Tabs>
</div>

### Disabled and read-only

Use `disabled` when the field should be unavailable for interaction. Use `readOnly` when the value
should remain visible but must not be editable.

<ExampleFrame variant="component">
  <EditableStates />
</ExampleFrame>

<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx file="../../../src/components/examples/editable/disabled-and-read-only.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/editable/editable-disabled-and-read-only.module.css"
```

    </Tab>
  </Tabs>
</div>

### In a form

Pass `name` to include the current value in a native form submission.

<ExampleFrame variant="component">
  <EditableForm />
</ExampleFrame>

<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx file="../../../src/components/examples/editable/form.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/editable/editable-form.module.css"
```

    </Tab>
  </Tabs>
</div>

## Styling

### CSS Properties

<div className="docs-reference">
  <Tabs
    values={[{ label: 'CSS Variables', value: 'CSS Variables' }]}
    className="docs-reference-tabs"
    contentItemClassName="docs-reference-panel"
  >
    <Tab value="CSS Variables">
      <EditableDocs.EditableCssPropertiesPanel />
    </Tab>
  </Tabs>
</div>

### Styling hooks

- Target parts with `data-slot="editable-root"`, `editable-root-provider`, `editable-label`,
  `editable-area`, `editable-input`, `editable-preview`, `editable-control`,
  `editable-edit-trigger`, `editable-submit-trigger`, and `editable-cancel-trigger`.
- Ark also emits `data-scope="editable"` and `data-part` attributes for each part.
- State selectors include `data-focus` on `Label` and `Area`; `data-disabled` on `Area`, `Input`,
  and `Preview`; `data-readonly` / `aria-readonly` on read-only controls; `data-invalid` on
  `Label`, `Input`, and `Preview`; `data-required` on `Label`; `data-placeholder-shown` on `Area`
  and `Preview`; and `data-autoresize` on `Input` and `Preview`.
