---
title: Dialog
component: dialog
description: A modal window for focused tasks such as forms, confirmations, and details.
---

import { DialogCssPropertiesPanel } from '@/components/examples/dialog/css-properties';
import DialogAdvancedCustomization from '@/components/examples/dialog/advanced-customization.tsx';
import DialogAlertDialog from '@/components/examples/dialog/alert-dialog.tsx';
import DialogBasic from '@/components/examples/dialog/basic.tsx';
import DialogConfirmation from '@/components/examples/dialog/confirmation.tsx';
import DialogControlled from '@/components/examples/dialog/controlled.tsx';
import DialogInsideScroll from '@/components/examples/dialog/inside-scroll.tsx';
import DialogMountingAndFocusLifecycle from '@/components/examples/dialog/mounting-and-focus-lifecycle.tsx';
import DialogMultipleTriggers from '@/components/examples/dialog/multiple-triggers.tsx';
import DialogNested from '@/components/examples/dialog/nested.tsx';
import DialogNonModal from '@/components/examples/dialog/non-modal.tsx';
import DialogOpenFromMenu from '@/components/examples/dialog/open-from-menu.tsx';
import DialogOutsideScroll from '@/components/examples/dialog/outside-scroll.tsx';
import DialogRootProvider from '@/components/examples/dialog/root-provider-and-ark-context-hooks.tsx';
import dialogSolidAdvancedCustomization from '../../../snippets/dialog/solid/advanced-customization.tsx?raw';
import dialogSolidAlertDialog from '../../../snippets/dialog/solid/alert-dialog.tsx?raw';
import dialogSolidBasic from '../../../snippets/dialog/solid/basic.tsx?raw';
import dialogSolidConfirmation from '../../../snippets/dialog/solid/confirmation.tsx?raw';
import dialogSolidControlled from '../../../snippets/dialog/solid/controlled.tsx?raw';
import dialogSolidInsideScroll from '../../../snippets/dialog/solid/inside-scroll.tsx?raw';
import dialogSolidMountingAndFocusLifecycle from '../../../snippets/dialog/solid/mounting-and-focus-lifecycle.tsx?raw';
import dialogSolidMultipleTriggers from '../../../snippets/dialog/solid/multiple-triggers.tsx?raw';
import dialogSolidNested from '../../../snippets/dialog/solid/nested.tsx?raw';
import dialogSolidNonModal from '../../../snippets/dialog/solid/non-modal.tsx?raw';
import dialogSolidOpenFromMenu from '../../../snippets/dialog/solid/open-from-menu.tsx?raw';
import dialogSolidOutsideScroll from '../../../snippets/dialog/solid/outside-scroll.tsx?raw';
import dialogSolidRootProvider from '../../../snippets/dialog/solid/root-provider-and-ark-context-hooks.tsx?raw';

## API Reference

<PrimitiveReference href="https://ark-ui.com/docs/components/dialog" />

## Choosing the right component

Use `Dialog` for forms, confirmations, details, and other tasks that should keep the user on the
current page. Use `Drawer` when the panel should stay attached to the edge of the viewport or
support dragging.

## Basic

Dialog ships for React and Solid in both the CSS Modules and Tailwind packages. The rendered
preview on this page uses the React CSS Modules package; the framework tabs show native source for
the same public contract.

Use `Dialog` as the root and compose its structural parts explicitly. `Dialog.Backdrop` is the
modal background, `Dialog.Positioner` places the dialog, and `Dialog.Content` contains its surface.

<ExampleFrame variant="component">
  <DialogBasic />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/dialog/basic.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={dialogSolidBasic} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

## Install with shadcn

See [Quick Start](/docs/quick-start) for package installation and choosing between CSS Modules and
Tailwind. The commands below add Dialog to an existing setup.

<Tabs groupId="framework">
  <Tab label="React" value="react">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-react/dialog' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-react-tailwind/dialog' },
  ]}
/>

  </Tab>
  <Tab label="Solid" value="solid">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-solid/dialog' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-solid-tailwind/dialog' },
  ]}
/>

  </Tab>
</Tabs>

## Anatomy

```text
Dialog / Dialog.Root
├─ Dialog.Trigger
└─ Overlay content (automatically portalled)
   ├─ Dialog.Backdrop (optional)
   └─ Dialog.Positioner
      └─ Dialog.Content
         ├─ Dialog.CloseTrigger or Dialog.CloseIcon
         ├─ Dialog.Title
         ├─ Dialog.Description
         ├─ Dialog.Header (moduix layout helper)
         ├─ Dialog.Body (moduix layout helper)
         └─ Dialog.Footer (moduix layout helper)

Dialog.RootProvider
└─ the same part tree connected to state from moduix useDialog()

Dialog.Context
└─ a render function with the current dialog state and methods
```

| Part                     | Role                                                                |
| ------------------------ | ------------------------------------------------------------------- |
| `Dialog` / `Dialog.Root` | Owns open state, focus management, and dismissal behavior.          |
| `Dialog.RootProvider`    | Connects parts to state created with `useDialog()`.                 |
| `Dialog.Trigger`         | Opens the dialog.                                                    |
| `Dialog.Backdrop`        | Optional background layer for a modal dialog.                       |
| `Dialog.Positioner`      | Positions the dialog content.                                        |
| `Dialog.Content`         | Contains the visible dialog surface.                                 |
| `Dialog.Title`           | Gives the dialog an accessible title.                                |
| `Dialog.Description`     | Provides supporting accessible text.                                 |
| `Dialog.CloseTrigger`    | Closes the dialog.                                                   |
| `Dialog.CloseIcon`       | Moduix close-button helper that also closes the dialog.              |
| `Dialog.Header`          | Moduix layout helper for the header.                                 |
| `Dialog.Body`            | Moduix layout helper for the main content.                           |
| `Dialog.Footer`          | Moduix layout helper for actions.                                    |
| `Dialog.Context`         | Renders children with current state and dialog methods.              |

Import `useDialog()` when the dialog state needs to live outside the rendered dialog tree. Read or
update that state with `Dialog.Context` or `useDialogContext()` from the matching package.

## Composition

Dialog owns its open state by default. Use `open` with `onOpenChange(details)` when another part of
the application owns that state. When state must live outside the dialog tree, create one
`useDialog()` store and pass it to `Dialog.RootProvider`; do not render another root for the same
store.

The root automatically portals `Dialog.Backdrop` and `Dialog.Positioner`. Set `portalled={false}`
to keep them in the current tree, or pass `portalRef` to use a custom container. Keep the part tree
explicit, and use `Dialog.Header`, `Dialog.Body`, and `Dialog.Footer` only when their layout
helpers fit the composition.

## Examples

### Alert Dialog

Use `role="alertdialog"` for decisions with serious consequences. Include a clear cancel action
and a separate action that confirms the decision.

<ExampleFrame variant="component">
  <DialogAlertDialog />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/dialog/alert-dialog.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={dialogSolidAlertDialog} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Confirmation

Intercept the parent dialog's close request when a form contains unsaved changes and ask whether the
user wants to discard them.

<ExampleFrame variant="component">
  <DialogConfirmation />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/dialog/confirmation.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={dialogSolidConfirmation} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Controlled

Keep `open` in React state or a Solid signal and read `details.open` from `onOpenChange` to
synchronize the dialog with the rest of the application.

<ExampleFrame variant="component">
  <DialogControlled />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/dialog/controlled.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={dialogSolidControlled} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Inside Scroll

Make only the main content scrollable when the title and actions should remain visible.

<ExampleFrame variant="component">
  <DialogInsideScroll />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/dialog/inside-scroll.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={dialogSolidInsideScroll} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/dialog/dialog-inside-scroll.module.css"
```
</div>

</ExampleCode>

### Mounting and Focus Lifecycle

Use `lazyMount` and `unmountOnExit` for the presence lifecycle. Use `initialFocusEl` and
`finalFocusEl` when focus should move to specific elements on open and close.

<ExampleFrame variant="component">
  <DialogMountingAndFocusLifecycle />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/dialog/mounting-and-focus-lifecycle.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={dialogSolidMountingAndFocusLifecycle} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Multiple Triggers

Share one dialog between several triggers by giving each trigger a `value` and handling
`onTriggerValueChange(details)`.

<ExampleFrame variant="component">
  <DialogMultipleTriggers />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/dialog/multiple-triggers.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={dialogSolidMultipleTriggers} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Nested

Use separate `useDialog()` stores for nested dialogs. Ark UI manages the layer stack, and moduix
uses the resulting state to make the parent recede while the nested dialog is open.

<ExampleFrame variant="component">
  <DialogNested />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/dialog/nested.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={dialogSolidNested} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/dialog/dialog-nested.module.css"
```
</div>

</ExampleCode>

### Non-Modal

Set `modal={false}` and omit the backdrop when users should still be able to interact with the page
outside the dialog.

<ExampleFrame variant="component">
  <DialogNonModal />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/dialog/non-modal.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={dialogSolidNonModal} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Open from Menu

Control `open` from a menu item when a contextual action should open a confirmation dialog.

<ExampleFrame variant="component">
  <DialogOpenFromMenu />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/dialog/open-from-menu.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={dialogSolidOpenFromMenu} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Outside Scroll

Let `Dialog.Positioner` scroll the whole dialog when long content should keep its natural height.

<ExampleFrame variant="component">
  <DialogOutsideScroll />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/dialog/outside-scroll.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={dialogSolidOutsideScroll} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/dialog/dialog-outside-scroll.module.css"
```
</div>

</ExampleCode>

### Root Provider

Create the state with `useDialog()` when controls live outside the dialog. Connect it with
`Dialog.RootProvider`, then read it with `Dialog.Context` or `useDialogContext()` in reusable
components.

<ExampleFrame variant="component">
  <DialogRootProvider />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/dialog/root-provider-and-ark-context-hooks.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={dialogSolidRootProvider} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Advanced Customization

Skip `Header`, `Body`, `Footer`, and `CloseIcon` when a custom layout needs direct control over every
Ark dialog part.

<ExampleFrame variant="component">
  <DialogAdvancedCustomization />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/dialog/advanced-customization.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={dialogSolidAdvancedCustomization} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/dialog/dialog-advanced-customization.module.css"
```
</div>

</ExampleCode>

## Styling

Use `className` in React or `class` in Solid on exported parts for structural styles. For state-based
styles, prefer Ark data attributes such as `data-state` and `data-has-nested`, together with moduix
`data-slot` selectors. Tailwind variants use native utilities and consumer classes; the component
variables below customize the CSS Modules variants. Data attributes and `data-slot` hooks are shared
by both styling tracks.

### CSS Properties

<div className="docs-reference">
  <Tabs
    values={[{ label: 'CSS Variables', value: 'CSS Variables' }]}
    className="docs-reference-tabs"
    contentItemClassName="docs-reference-panel"
  >
    <Tab>
      <DialogCssPropertiesPanel />
    </Tab>
  </Tabs>
</div>

### Styling hooks

- Moduix `data-slot` values: `dialog-trigger`, `dialog-backdrop`, `dialog-positioner`,
  `dialog-content`, `dialog-title`, `dialog-description`, `dialog-close-trigger`,
  `dialog-close-icon`, `dialog-header`, `dialog-body`, and `dialog-footer`.
- Ark attributes: `data-scope="dialog"` and
  `data-part="trigger|backdrop|positioner|content|title|description|close-trigger"`.
- State attributes: `data-state="open|closed"`, `data-has-nested`, `data-nested`, `data-current`,
  and `data-value`.
- Runtime variables: `--layer-index` and `--nested-layer-count`.
- `Dialog.CloseIcon` is positioned against the corner of `Dialog.Content`, so its placement does
  not depend on `Dialog.Header`.
