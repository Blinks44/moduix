---
title: Swap
component: swap
description: Animate the transition between two visual states with moduix presets built on Ark UI.
---

import SwapAnimationPresets from '@/components/examples/swap/animation-presets.tsx';
import SwapBasic from '@/components/examples/swap/basic.tsx';
import * as SwapDocs from '@/components/examples/swap/css-properties';
import SwapCustomAnimation from '@/components/examples/swap/custom-animation.tsx';
import SwapRootProvider from '@/components/examples/swap/root-provider.tsx';
import swapSolidAnimationPresets from '../../../snippets/swap/solid/animation-presets.tsx?raw';
import swapSolidBasic from '../../../snippets/swap/solid/basic.tsx?raw';
import swapSolidCustomAnimation from '../../../snippets/swap/solid/custom-animation.tsx?raw';
import swapSolidRootProvider from '../../../snippets/swap/solid/root-provider.tsx?raw';

## API Reference

<PrimitiveReference href="https://ark-ui.com/docs/utilities/swap" />

## Basic


Use `Swap` when one piece of UI needs to switch between two visual states, such as an icon that
changes after an action. Pass the current boolean to `swap`: `false` shows the `off` indicator and
`true` shows the `on` indicator.

<ExampleFrame variant="component">
  <SwapBasic />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/swap/basic.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={swapSolidBasic} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

## Install with shadcn

See [Quick Start](/docs/quick-start) for full package setup, registry configuration, and styling guidance.

<Tabs groupId="framework">
  <Tab label="React" value="react">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-react/swap' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-react-tailwind/swap' },
  ]}
/>

  </Tab>
  <Tab label="Solid" value="solid">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-solid/swap' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-solid-tailwind/swap' },
  ]}
/>

  </Tab>
</Tabs>

## Anatomy

```text
Swap
├─ SwapIndicator type="off"
└─ SwapIndicator type="on"

SwapRootProvider
└─ same indicator tree connected to a useSwap() store
```

| Part                 | Role                                                               |
| -------------------- | ------------------------------------------------------------------ |
| `Swap` | Supplies the `swap` state and stacks both visual states.           |
| `SwapRootProvider`  | Connects the parts to state created with `useSwap()`.         |
| `SwapIndicator`     | Renders the `on` or `off` state selected by its `type` prop.        |

## Composition

`Swap` is presentational. Keep state, interaction, focus, and the accessible name on the surrounding
semantic control. For icon-only indicators, set `aria-hidden="true"`. Render both indicators as
direct children so they share one grid cell and the built-in recipes stay scoped to that `Swap`
instance.

`Swap` uses the `scale` animation by default. Pass `fade`, `scale`, `rotate`, or `flip` to `animation`,
or pass a custom name and define its CSS with the root's `data-animation` and the indicators' Ark
`data-state` attributes. When state must be created outside the rendered tree, pass
`useSwap()` to `SwapRootProvider` and do not render another root for the same store. With
`asChild`, provide exactly one semantic child.

## Examples

### Animation presets

Choose a built-in recipe, then activate the button to see the selected transition. The current value
and recipe controls are shown below the demo.

<ExampleFrame variant="component">
  <SwapAnimationPresets />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/swap/animation-presets.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={swapSolidAnimationPresets} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/swap/swap-animation-presets.module.css"
```
</div>

</ExampleCode>

### Custom animation

Pass a custom `animation` name and target the root's `data-animation` together with the indicators'
Ark `data-state`. The `bounce` recipe below is plain CSS. A custom recipe must define both enter and
exit animations and include a reduced-motion fallback.

<ExampleFrame variant="component">
  <SwapCustomAnimation />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/swap/custom-animation.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={swapSolidCustomAnimation} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/swap/swap-custom-animation.module.css"
```
</div>

</ExampleCode>

### Root Provider

Create the Swap state separately with `useSwap()`, then pass it to `SwapRootProvider` through
the `value` prop. This example also uses `asChild`, so the semantic button becomes the rendered Swap
root.

<ExampleFrame variant="component">
  <SwapRootProvider />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/swap/root-provider.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={swapSolidRootProvider} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/swap/swap-root-provider.module.css"
```
</div>

</ExampleCode>

## Styling

Built-in recipes use the CSS variables below in the CSS Modules variants. Tailwind variants use native
utility and consumer class overrides. A custom animation name disables the built-in recipes, but you
can still reuse `--moduix-swap-transition` in your own CSS.

Ark pins the layout with inline styles: the root gets `display: inline-grid`, and each indicator gets
`grid-area: 1 / 1` and `display: inline-flex`. Class-based `display` and `grid-area` overrides are
no-ops in both tracks, so change these properties through the `style` prop (for example
`style={{ display: 'grid' }}`) instead.

In the Tailwind variants the flip recipe applies `data-[animation=flip]:[perspective:24rem]` on the
root, which out-specifies a plain consumer `perspective` utility, so repeat that documented variant
(for example `data-[animation=flip]:[perspective:32rem]`) to override it - the CSS Modules track
instead exposes `--moduix-swap-flip-perspective`.

### CSS Properties

<CssPropertiesSection properties={SwapDocs.swapOverrideCssProperties} />

### Styling hooks

- `Swap` uses `data-slot="swap-root"`.
- `SwapRootProvider` uses `data-slot="swap-root-provider"`.
- `SwapIndicator` uses `data-slot="swap-indicator"`.
- `animation` is exposed as `data-animation` on `Swap` and `SwapRootProvider`.
- Built-in recipes target only direct `SwapIndicator` children of their root.
- Ark adds `data-scope="swap"` and `data-part="root" | "indicator"`.
- Ark adds `data-state="open" | "closed"` to indicators. moduix uses those attributes for its
  built-in animations and for custom recipes.
