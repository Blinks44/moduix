---
title: Collapsible
component: collapsible
description: An accessible disclosure control for showing and hiding one content region in React or Solid.
---

import * as CollapsibleDocs from '@/components/examples/collapsible/css-properties';
import CollapsibleBasic from '@/components/examples/collapsible/basic.tsx';
import CollapsibleContext from '@/components/examples/collapsible/context.tsx';
import CollapsibleControlled from '@/components/examples/collapsible/controlled.tsx';
import CollapsibleDefaultOpen from '@/components/examples/collapsible/default-open.tsx';
import CollapsibleDisabled from '@/components/examples/collapsible/disabled.tsx';
import CollapsibleLazyMount from '@/components/examples/collapsible/lazy-mount.tsx';
import CollapsibleNested from '@/components/examples/collapsible/nested.tsx';
import CollapsiblePartialCollapse from '@/components/examples/collapsible/partial-collapse.tsx';
import CollapsiblePartialCollapseWidth from '@/components/examples/collapsible/partial-collapse-width.tsx';
import CollapsibleRootProvider from '@/components/examples/collapsible/root-provider.tsx';
import CollapsibleCustomComposition from '@/components/examples/collapsible/custom-composition.tsx';
import collapsibleSolidBasic from '../../../snippets/collapsible/solid/basic.tsx?raw';
import collapsibleSolidContext from '../../../snippets/collapsible/solid/context.tsx?raw';
import collapsibleSolidControlled from '../../../snippets/collapsible/solid/controlled.tsx?raw';
import collapsibleSolidDefaultOpen from '../../../snippets/collapsible/solid/default-open.tsx?raw';
import collapsibleSolidDisabled from '../../../snippets/collapsible/solid/disabled.tsx?raw';
import collapsibleSolidLazyMount from '../../../snippets/collapsible/solid/lazy-mount.tsx?raw';
import collapsibleSolidNested from '../../../snippets/collapsible/solid/nested.tsx?raw';
import collapsibleSolidPartialCollapse from '../../../snippets/collapsible/solid/partial-collapse.tsx?raw';
import collapsibleSolidPartialCollapseWidth from '../../../snippets/collapsible/solid/partial-collapse-width.tsx?raw';
import collapsibleSolidRootProvider from '../../../snippets/collapsible/solid/root-provider.tsx?raw';
import collapsibleSolidCustomComposition from '../../../snippets/collapsible/solid/custom-composition.tsx?raw';

## API Reference

<PrimitiveReference href="https://ark-ui.com/docs/components/collapsible" />

## Choosing the right component

Use `Collapsible` for one independent disclosure. Use `Accordion` when several related sections
need shared keyboard navigation or coordinated state.

## Basic


Use `Collapsible` with `CollapsibleTrigger`, the optional `CollapsibleIndicator`, and `CollapsibleContent` to show and hide one
content region. Put the content inside `CollapsibleBody` when you need the recommended inner
wrapper for spacing and layout.

<ExampleFrame variant="component">
  <CollapsibleBasic />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/collapsible/basic.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={collapsibleSolidBasic} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/collapsible/collapsible-basic.module.css"
```

</div>

</ExampleCode>

## Install with shadcn

See [Quick Start](/docs/quick-start) for full package setup, registry configuration, and styling guidance.

<Tabs groupId="framework">
  <Tab label="React" value="react">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-react/collapsible' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-react-tailwind/collapsible' },
  ]}
/>

  </Tab>
  <Tab label="Solid" value="solid">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-solid/collapsible' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-solid-tailwind/collapsible' },
  ]}
/>

  </Tab>
</Tabs>

## Anatomy

```text
Collapsible
├─ CollapsibleTrigger
│  ├─ label
│  └─ CollapsibleIndicator (optional)
└─ CollapsibleContent
   └─ CollapsibleBody
      └─ content

CollapsibleRootProvider
└─ same part tree connected to a useCollapsible() store
```

| Part                      | Role                                                               |
| ------------------------- | ------------------------------------------------------------------ |
| `Collapsible`             | Owns the open state for one disclosure.                            |
| `CollapsibleRootProvider` | Connects the parts to state created with `useCollapsible`.         |
| `CollapsibleTrigger`      | Accessible button that opens or closes the content.                 |
| `CollapsibleIndicator`    | Optional state indicator that renders a moduix chevron by default. |
| `CollapsibleContent`      | Animated region whose dimensions Ark measures.                     |
| `CollapsibleBody`         | Inner wrapper for spacing and content layout.                       |

## Composition

`Collapsible` owns state inside the root. Use `defaultOpen` for an uncontrolled disclosure; use
`open` with `onOpenChange(details)` when another part of the application owns it. The callback keeps
Ark's details object, so controlled state reads `details.open`.

Use `CollapsibleRootProvider` with `useCollapsible()` from the matching framework package when the
store must be created outside the rendered tree or shared across composition boundaries. Pass that
store to `CollapsibleRootProvider` and do not render a second `Collapsible` root for the same store.

Keep padding and surface styles on `CollapsibleBody`, not `CollapsibleContent`, so Ark can measure
the animated content part correctly. `CollapsibleIndicator` is optional and defaults to the moduix
chevron.

Use `asChild` only when a consumer-owned semantic element should receive the trigger behavior; it
must remain a keyboard-accessible button. In Solid, pass a render function such as
`asChild={(props) => <button {...props()} />}`.

## Examples

### Context

Use `useCollapsibleContext()` inside a descendant when it needs to read state or close the disclosure
programmatically.

<ExampleFrame variant="component">
  <CollapsibleContext />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/collapsible/context.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={collapsibleSolidContext} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/collapsible/collapsible-context.module.css"
```

</div>

</ExampleCode>

### Controlled

Use `open` with the Ark `onOpenChange(details)` callback when another part of your UI owns the state.

<ExampleFrame variant="component">
  <CollapsibleControlled />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/collapsible/controlled.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={collapsibleSolidControlled} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/collapsible/collapsible-controlled.module.css"
```

</div>

</ExampleCode>

### Default Open

Set `defaultOpen` to start an uncontrolled disclosure in the open state.

<ExampleFrame variant="component">
  <CollapsibleDefaultOpen />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/collapsible/default-open.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={collapsibleSolidDefaultOpen} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/collapsible/collapsible-default-open.module.css"
```

</div>

</ExampleCode>

### Disabled

Set `disabled` on the root to prevent users from opening or closing the disclosure.

<ExampleFrame variant="component">
  <CollapsibleDisabled />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/collapsible/disabled.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={collapsibleSolidDisabled} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/collapsible/collapsible-disabled.module.css"
```

</div>

</ExampleCode>

### Lazy Mount

Set `lazyMount` to mount content only after its first opening. Add `unmountOnExit` to remove it from
the DOM after it closes.

<ExampleFrame variant="component">
  <CollapsibleLazyMount />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/collapsible/lazy-mount.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={collapsibleSolidLazyMount} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/collapsible/collapsible-lazy-mount.module.css"
```

</div>

</ExampleCode>

### Nested

Nest independent roots when a revealed section contains another optional detail.

<ExampleFrame variant="component">
  <CollapsibleNested />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/collapsible/nested.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={collapsibleSolidNested} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/collapsible/collapsible-nested.module.css"
```

</div>

</ExampleCode>

### Partial Collapse

Use `collapsedHeight` or `collapsedWidth` to keep part of the content visible while closed.
Interactive elements in that area become inert until the disclosure opens, so keep actions that must
stay available outside `CollapsibleContent`.

<ExampleFrame variant="component">
  <CollapsiblePartialCollapse />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/collapsible/partial-collapse.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={collapsibleSolidPartialCollapse} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/collapsible/collapsible-partial-collapse.module.css"
```

</div>

</ExampleCode>

### Partial Width

Use `collapsedWidth` when a horizontal preview is more useful than a height-limited preview.

<ExampleFrame variant="component">
  <CollapsiblePartialCollapseWidth />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/collapsible/partial-collapse-width.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={collapsibleSolidPartialCollapseWidth} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/collapsible/collapsible-partial-collapse-width.module.css"
```

</div>

</ExampleCode>

### Root Provider

Create the state with `useCollapsible()` and pass it to `CollapsibleRootProvider` when code outside
the rendered component tree needs access to the state.

<ExampleFrame variant="component">
  <CollapsibleRootProvider />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/collapsible/root-provider.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={collapsibleSolidRootProvider} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/collapsible/collapsible-root-provider.module.css"
```

</div>

</ExampleCode>

### Advanced Customization

Use `asChild` to apply the trigger behavior to a button you own. This skips the default
`CollapsibleTrigger` styling while preserving Ark behavior and state attributes.

<ExampleFrame variant="component">
  <CollapsibleCustomComposition />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/collapsible/custom-composition.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={collapsibleSolidCustomComposition} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/collapsible/collapsible-custom-composition.module.css"
```

</div>

</ExampleCode>

## Styling

Use `className` in React or `class` in Solid on exported parts for structural styles. For state-based
styles, prefer Ark data attributes such as `data-state` and `data-disabled`, together with moduix
`data-slot` selectors. Tailwind variants use native utilities and consumer classes; the component
variables below customize the CSS Modules variants. Data attributes and `data-slot` hooks are shared
by both styling tracks.

Put spacing on `CollapsibleBody`, not `CollapsibleContent`, so Ark can measure and animate the
content correctly.

### CSS Properties

<CssPropertiesSection properties={CollapsibleDocs.collapsibleOverrideCssProperties} />

### Styling hooks

- DOM parts expose `data-slot="collapsible-root"`, `collapsible-root-provider`,
  `collapsible-trigger`, `collapsible-indicator`, `collapsible-content`, and `collapsible-body`.
- Ark parts expose `data-scope="collapsible"` and `data-part` values for the root, trigger, indicator,
  and content.
- Ark exposes `data-state="open" | "closed"` on root, trigger, indicator, and content.
- Ark adds `data-collapsible` to the content part.
- `data-disabled` marks disabled trigger, indicator, and content parts.
- `data-has-collapsed-size` marks content configured with `collapsedHeight` or `collapsedWidth`.
- Content animation uses Ark `--height`, `--width`, `--collapsed-height`, and `--collapsed-width`.
