---
title: Input
component: input
description: A styled native single-line input built on Ark FieldInput, with native events and moduix styling.
---

import * as InputDocs from '@/components/examples/input/css-properties';
import InputAsChild from '@/components/examples/input/as-child.tsx';
import InputBasic from '@/components/examples/input/basic.tsx';
import InputControlled from '@/components/examples/input/controlled.tsx';
import InputFieldState from '@/components/examples/input/field-state.tsx';
import InputFile from '@/components/examples/input/file-input.tsx';
import InputNativeAttributes from '@/components/examples/input/native-attributes.tsx';
import InputSizes from '@/components/examples/input/sizes.tsx';
import InputStandalone from '@/components/examples/input/standalone-input.tsx';
import InputStates from '@/components/examples/input/disabled-and-readonly.tsx';
import inputSolidAsChild from '../../../snippets/input/solid/as-child.tsx?raw';
import inputSolidBasic from '../../../snippets/input/solid/basic.tsx?raw';
import inputSolidControlled from '../../../snippets/input/solid/controlled.tsx?raw';
import inputSolidFieldState from '../../../snippets/input/solid/field-state.tsx?raw';
import inputSolidFile from '../../../snippets/input/solid/file-input.tsx?raw';
import inputSolidNativeAttributes from '../../../snippets/input/solid/native-attributes.tsx?raw';
import inputSolidSizes from '../../../snippets/input/solid/sizes.tsx?raw';
import inputSolidStandalone from '../../../snippets/input/solid/standalone-input.tsx?raw';
import inputSolidStates from '../../../snippets/input/solid/disabled-and-readonly.tsx?raw';

## API Reference

`Input` is a styled wrapper around [Ark's `FieldInput`](https://ark-ui.com/docs/components/field).
It follows Ark's guidance for [composition](https://ark-ui.com/docs/guides/composition),
[styling](https://ark-ui.com/docs/guides/styling), and [ref forwarding](https://ark-ui.com/docs/guides/ref).
It renders a native single-line `<input>` and preserves browser form behavior and each framework's
native event API.

Use `Field` when the control needs a label, helper or error text, generated ids, or shared state.

## Choosing the right component

- Use **`Input`** for an ordinary single-line input.
- Use **`InputGroup`** when the input needs inline content such as a prefix, suffix, or action.
- Use **`PasswordInput`** for a password field with a visibility toggle.
- Use **`NumberInput`** when you need numeric parsing and step controls.
- Use **`Textarea`** for multi-line content.
- Use **`Editable`** when text should appear as a value first and switch into edit mode on demand.

## Basic


Place `Input` inside `Field` when you need a label, helper text, or an error message. `Field`
connects that content to the control, generates the necessary ids, and provides its state.

<ExampleFrame variant="component">
  <InputBasic />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/input/basic.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={inputSolidBasic} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/input/input-basic.module.css"
```
</div>

</ExampleCode>

## Install with shadcn

See [Quick Start](/docs/quick-start) for full package setup, registry configuration, and styling guidance.

<Tabs groupId="framework">
  <Tab label="React" value="react">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-react/input' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-react-tailwind/input' },
  ]}
/>

  </Tab>
  <Tab label="Solid" value="solid">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-solid/input' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-solid-tailwind/input' },
  ]}
/>

  </Tab>
</Tabs>

## Anatomy

```text
Field (optional wrapper)
└─ Input / Input.Root
```

| Part                   | Role                                                                  |
| ---------------------- | --------------------------------------------------------------------- |
| `Input` / `Input.Root` | Styled version of Ark's `FieldInput` that renders a native single-line `<input>`. |

`Input.Root` is an alias of `Input`; both render the same control.

## Composition

Use `Field` as the owner of the label, helper or error text, generated ids, and shared `disabled`,
`invalid`, `required`, or `readOnly` state. `Input` remains the native control: use `value` with
`onChange(event)` for controlled React state or `defaultValue` for an uncontrolled input. It does
not manage validation, focus, or keyboard navigation.

For a standalone control, provide its accessible name yourself. Use `asChild` only with one
focusable, input-compatible child; Ark merges the `Input` and `Field` props, class, and ref into
that DOM node.

## Examples

### Controlled value

Use this pattern when the surrounding component owns the current value. In React, read the next
value from native `onChange(event)`; in Solid, use `onInput`. Pass the current value back through
`value`.

<ExampleFrame variant="component">
  <InputControlled />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/input/controlled.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={inputSolidControlled} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/input/input-controlled.module.css"
```
</div>

</ExampleCode>

### Disabled and read-only

Use `disabled` when the user must not interact with the control. Use `readOnly` when they can read
and select the value but must not edit it. Set them directly on `Input` for one control, or on
`Field` when the label, supporting text, and input should share the same state.

<ExampleFrame variant="component">
  <InputStates />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/input/disabled-and-readonly.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={inputSolidStates} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/input/input-disabled-and-readonly.module.css"
```
</div>

</ExampleCode>

### Field validation

Set `invalid` on `Field` when validation fails. Ark adds `aria-invalid` and `data-invalid` to the
input and connects it to the error text.

<ExampleFrame variant="component">
  <InputFieldState />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/input/field-state.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={inputSolidFieldState} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/input/input-field-state.module.css"
```
</div>

</ExampleCode>

### File input

`Input` is enough for a native file chooser in a simple form. Use `FileUpload` when the application
needs to keep selected files in state, show a dropzone, or manage an upload flow.

<ExampleFrame variant="component">
  <InputFile />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/input/file-input.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={inputSolidFile} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/input/input-file-input.module.css"
```
</div>

</ExampleCode>

### Native attributes

Use native attributes for browser behavior such as validation, input mode, autocomplete, and form
submission. Use `htmlSize` for a character-based width and `size` for visual density in both React
and Solid.

<ExampleFrame variant="component">
  <InputNativeAttributes />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/input/native-attributes.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={inputSolidNativeAttributes} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/input/input-native-attributes.module.css"
```
</div>

</ExampleCode>

### Sizes

The moduix `size` prop controls visual density. It is separate from `htmlSize`, which controls the
native character-based width.

<ExampleFrame variant="component">
  <InputSizes />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/input/sizes.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={inputSolidSizes} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/input/input-sizes.module.css"
```
</div>

</ExampleCode>

### Standalone input

`Field` is optional. When `Input` is used on its own, provide an accessible name with an HTML
`<label>`, `aria-label`, or `aria-labelledby`. Native props still work, but without `Field` context,
ids are not generated automatically and helper or error text is not connected to the control.

<ExampleFrame variant="component">
  <InputStandalone />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/input/standalone-input.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={inputSolidStandalone} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/input/input-standalone-input.module.css"
```
</div>

</ExampleCode>

### Advanced Customization

Use `asChild` only when another DOM node must be rendered as the input. Keep one semantic,
focusable input element that can receive the merged props and `ref`.

<ExampleFrame variant="component">
  <InputAsChild />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/input/as-child.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={inputSolidAsChild} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/input/input-as-child.module.css"
```
</div>

</ExampleCode>

## Styling

Use `className` in React or `class` in Solid on `Input`. CSS Modules variants expose the variables
below, while Tailwind variants use native utility classes and consumer classes. Use the shared data
attributes and native state selectors when styling field state.

### CSS Properties

<div className="docs-reference">
  <Tabs
    values={[{ label: 'CSS Variables', value: 'CSS Variables' }]}
    className="docs-reference-tabs"
    contentItemClassName="docs-reference-panel"
  >
    <Tab>
      <InputDocs.InputCssPropertiesPanel />
    </Tab>
  </Tabs>
</div>

### Styling hooks

- `data-slot="input-root"`
- `data-size="xs" | "sm" | "md" | "lg" | "xl"`
- `data-scope="field"` and `data-part="input"`
- Ark `data-invalid`, `data-required`, and `data-readonly` inside `Field`
- native `:disabled`, `:read-only`, `:focus-visible`, and `[aria-invalid="true"]`
