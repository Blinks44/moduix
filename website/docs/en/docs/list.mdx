---
title: List
component: list
description: A semantic list with configurable spacing, typography, tones, and native markers.
---

import * as ListDocs from '@/components/examples/list/css-properties';
import ListBasic from '@/components/examples/list/basic.tsx';
import ListCustomComposition from '@/components/examples/list/custom-composition.tsx';
import ListMarkerless from '@/components/examples/list/markerless.tsx';
import ListNativeItems from '@/components/examples/list/native-items.tsx';
import ListOrdered from '@/components/examples/list/ordered.tsx';
import ListRootAsChild from '@/components/examples/list/root-as-child.tsx';
import ListTone from '@/components/examples/list/tone.tsx';
import listSolidBasic from '../../../snippets/list/solid/basic.tsx?raw';
import listSolidCustomComposition from '../../../snippets/list/solid/custom-composition.tsx?raw';
import listSolidMarkerless from '../../../snippets/list/solid/markerless.tsx?raw';
import listSolidNativeItems from '../../../snippets/list/solid/native-items.tsx?raw';
import listSolidOrdered from '../../../snippets/list/solid/ordered.tsx?raw';
import listSolidRootAsChild from '../../../snippets/list/solid/root-as-child.tsx?raw';
import listSolidTone from '../../../snippets/list/solid/tone.tsx?raw';

## API Reference

Ark UI does not provide a dedicated list primitive. moduix uses the official
[Ark factory](https://ark-ui.com/docs/guides/composition#the-ark-factory) to compose native `ul`, `ol`, and
`li` elements. See Ark's guides to [composition](https://ark-ui.com/docs/guides/composition),
[styling](https://ark-ui.com/docs/guides/styling), and [refs](https://ark-ui.com/docs/guides/ref) for the
underlying model.

## Choosing the right component

Use `List` for static grouped content such as steps, features, or metadata. It keeps native `ul` and `ol`
semantics while adding controls for spacing, typography, tone, and markers.

If users need to navigate with the keyboard and select one or more options, use [Listbox](/docs/listbox).
`Listbox` manages focus, the collection, and selection.

## Basic

`List` ships for React and Solid in both the CSS Modules and Tailwind packages. The rendered preview on this
page uses the React CSS Modules package; the framework tabs show native source for the same public contract.

`List` renders a semantic `<ul>` by default. Use `List.Item` when you need the stable item part and its
styling hooks, or use native `<li>` children when you do not need the wrapper. The root controls spacing,
typography, tone, and markers. Leave `marker` unset for semantic defaults, or set it to `disc`, `decimal`, or
`none` explicitly.

<ExampleFrame variant="component">
  <ListBasic />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/list/basic.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={listSolidBasic} />

    </Tab>
  </Tabs>
</div>
</ExampleCode>

## Install with shadcn

<Tabs groupId="framework">
  <Tab label="React" value="react">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-react/list' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-react-tailwind/list' },
  ]}
/>

  </Tab>
  <Tab label="Solid" value="solid">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-solid/list' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-solid-tailwind/list' },
  ]}
/>

  </Tab>
</Tabs>

## Anatomy

```text
List / List.Root
└─ List.Item | li
```

| Part                 | Role                                                                                              |
| -------------------- | ------------------------------------------------------------------------------------------------- |
| `List` / `List.Root` | Semantic list root that renders `ul` or `ol` and controls markers, spacing, size, and tone.      |
| `List.Item`          | Optional item part that renders `li`, forwards native attributes and refs, and supports `asChild`. |

## Composition

Use `List` or `List.Root` for the root. Its direct children should be `List.Item` or native `li` elements,
because root spacing and marker styling target direct list items. Use `List.Item` when you need its stable
slot, `className`/`class`, or `asChild`; native `li` is enough otherwise. Use `asChild` only when an application
component owns one semantic `ul`, `ol`, or `li` and forwards the received props and ref. In Solid, pass the
render function form of `asChild`. `List` has no state machine or callbacks, so keyboard selection belongs to
`Listbox`.

## Examples

### Custom Root

Use `List asChild` when a component from your application must render the semantic list element. It must
forward the received props and ref to one `<ul>` or `<ol>` in React, or use the render-function form in Solid.

<ExampleFrame variant="component">
  <ListRootAsChild />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/list/root-as-child.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={listSolidRootAsChild} />

    </Tab>
  </Tabs>
</div>
</ExampleCode>

### Markerless

Use `marker="none"` when you need list semantics without visible markers. `List` adds `role="list"` by default
in this mode unless you provide another `role`.

<ExampleFrame variant="component">
  <ListMarkerless />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/list/markerless.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={listSolidMarkerless} />

    </Tab>
  </Tabs>
</div>
</ExampleCode>

### Native Items

Use native `li` elements when you do not need the exported `List.Item` part.

<ExampleFrame variant="component">
  <ListNativeItems />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/list/native-items.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={listSolidNativeItems} />

    </Tab>
  </Tabs>
</div>
</ExampleCode>

### Ordered

Use `as="ol"` to render an ordered list while keeping the styling applied by the moduix root. Native props such
as `start`, `reversed`, and `type` are forwarded to the ordered-list root.

<ExampleFrame variant="component">
  <ListOrdered />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/list/ordered.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={listSolidOrdered} />

    </Tab>
  </Tabs>
</div>
</ExampleCode>

### Tone

Use `tone` to apply a semantic text and marker color to the list.

<ExampleFrame variant="component">
  <ListTone />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/list/tone.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={listSolidTone} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/list/list-tone.module.css"
```
</div>
</ExampleCode>

### Advanced Customization

Use `List.Item asChild` when a custom component must render the `<li>` itself. It must forward the received
props and ref and preserve the semantic `<li>` element. In other cases, use `List.Item className` or `class` directly.

<ExampleFrame variant="component">
  <ListCustomComposition />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/list/custom-composition.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={listSolidCustomComposition} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/list/list-custom-composition.module.css"
```
</div>
</ExampleCode>

## Styling

Style `List` with the framework's class prop (`className` in React and `class` in Solid), the exported Ark-style
`data-*` attributes, native `li::marker`, and the public `--moduix-list-*` CSS variables shown below. This list
covers the public CSS API.

### CSS Properties

<div className="docs-reference">
  <Tabs
    values={[{ label: 'CSS Variables', value: 'CSS Variables' }]}
    className="docs-reference-tabs"
    contentItemClassName="docs-reference-panel"
  >
    <Tab>
      <ListDocs.ListCssPropertiesPanel />
    </Tab>
  </Tabs>
</div>

### Styling Hooks

`List` exposes these public styling hooks:

- root: `data-scope="list"`, `data-part="root"`, `data-slot="list-root"`
- item: `data-scope="list"`, `data-part="item"`, `data-slot="list-item"`
- `data-gap="xs" | "sm" | "md" | "lg" | "xl" | "2xl"` for spacing-related styles
- `data-marker="auto" | "disc" | "decimal" | "none"` for marker-related styles
- `data-size="xs" | "sm" | "md" | "lg" | "xl"` for typography size styles
- `data-tone="default" | "muted" | "subtle" | "primary" | "destructive"` for semantic colors
- native `li::marker` for overriding marker color or weight
