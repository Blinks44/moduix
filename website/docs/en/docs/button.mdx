---
title: Button
component: button
description: A styled single-part action control for React and Solid, with CSS Modules and Tailwind packages.
---

import * as ButtonDocs from '@/components/examples/button/css-properties';
import ButtonAsChildLink from '@/components/examples/button/as-child-link.tsx';
import ButtonBasic from '@/components/examples/button/basic.tsx';
import ButtonDisabled from '@/components/examples/button/disabled.tsx';
import ButtonIcons from '@/components/examples/button/icons.tsx';
import ButtonPendingState from '@/components/examples/button/pending-state.tsx';
import ButtonRef from '@/components/examples/button/ref.tsx';
import ButtonSizes from '@/components/examples/button/sizes.tsx';
import ButtonVariants from '@/components/examples/button/variants.tsx';
import buttonSolidAsChildLink from '../../../snippets/button/solid/as-child-link.tsx?raw';
import buttonSolidBasic from '../../../snippets/button/solid/basic.tsx?raw';
import buttonSolidDisabled from '../../../snippets/button/solid/disabled.tsx?raw';
import buttonSolidIcons from '../../../snippets/button/solid/icons.tsx?raw';
import buttonSolidPendingState from '../../../snippets/button/solid/pending-state.tsx?raw';
import buttonSolidRef from '../../../snippets/button/solid/ref.tsx?raw';
import buttonSolidSizes from '../../../snippets/button/solid/sizes.tsx?raw';
import buttonSolidVariants from '../../../snippets/button/solid/variants.tsx?raw';

## API Reference

Ark UI does not provide a dedicated `button` primitive. moduix builds this component with the official
[Ark factory](https://ark-ui.com/docs/guides/composition#the-ark-factory) and follows Ark's guidance for
[composition](https://ark-ui.com/docs/guides/composition), [styling](https://ark-ui.com/docs/guides/styling), and
[refs](https://ark-ui.com/docs/guides/ref).

## Basic

Button ships in React and Solid packages for both CSS Modules and Tailwind. The rendered preview on this page
uses the React CSS Modules package; the framework tabs show native source for the same public contract.

For most actions, use the short `<Button>...</Button>` form. `Button.Root` is the equivalent namespaced form.

<ExampleFrame variant="component">
  <ButtonBasic />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/button/basic.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={buttonSolidBasic} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

## Install with shadcn

<Tabs groupId="framework">
  <Tab label="React" value="react">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-react/button' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-react-tailwind/button' },
  ]}
/>

  </Tab>
  <Tab label="Solid" value="solid">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-solid/button' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-solid-tailwind/button' },
  ]}
/>

  </Tab>
</Tabs>

See [Quick Start](/docs/quick-start) for package setup and registry configuration.

## Anatomy

`Button` has a single part.

```text
Button / Button.Root
└─ root[data-scope="button"][data-part="root"][data-slot="button-root"]
   └─ children
```

| Part                     | Role                                             |
| ------------------------ | ------------------------------------------------ |
| `Button` / `Button.Root` | Single interactive root element with moduix styles. |
| `Button`                 | Callable alias for `Button.Root`.                |

## Composition

`Button` is a single-part root. Use `asChild` when another semantic element should own the rendered DOM node:
React passes exactly one child, while Solid uses an `asChild` render function. When an Ark part composes Button,
the parent keeps ownership of `data-scope` and `data-part`; Button keeps its recipe classes and stable `data-slot`.

The `loading` prop owns only busy and disabled state (`data-loading`, `aria-busy`, and `data-disabled`); your
application owns the spinner, label, and async lifecycle. Native roots default to `type="button"`, while
`asChild` targets do not receive that default.

## Examples

### Disabled

Use `disabled` with native buttons and elements used with `asChild`.

<ExampleFrame variant="component">
  <ButtonDisabled />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/button/disabled.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={buttonSolidDisabled} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/button/button-disabled.module.css"
```
</div>

</ExampleCode>

### Icons

Icons can appear before or after the label. Their order in JSX and the Button root's gap determine the layout.

<ExampleFrame variant="component">
  <ButtonIcons />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/button/icons.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={buttonSolidIcons} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/button/button-icons.module.css"
```
</div>

</ExampleCode>

### Link with `asChild`

Use `asChild` for navigation so the anchor keeps its link semantics while using button styles.

<ExampleFrame variant="component">
  <ButtonAsChildLink />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/button/as-child-link.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={buttonSolidAsChildLink} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Loading State

Use `loading` to expose the busy state while keeping the label, spinner, and async lifecycle explicit in your
application code. It also prevents click activation on elements used with `asChild`.

<ExampleFrame variant="component">
  <ButtonPendingState />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/button/pending-state.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={buttonSolidPendingState} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Ref

The forwarded ref points to the rendered root and can be used to focus or measure it programmatically.

<ExampleFrame variant="component">
  <ButtonRef />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/button/ref.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={buttonSolidRef} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/button/button-ref.module.css"
```
</div>

</ExampleCode>

### Sizes

Text and icon-only buttons use the same root API.

<ExampleFrame variant="component">
  <ButtonSizes />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/button/sizes.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={buttonSolidSizes} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/button/button-sizes.module.css"
```
</div>

</ExampleCode>

### Variants

Use variants to communicate an action's priority and intent.

<ExampleFrame variant="component">
  <ButtonVariants />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/button/variants.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={buttonSolidVariants} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/button/button-variants.module.css"
```
</div>

</ExampleCode>

## Styling

Use `className` in React or `class` in Solid on the root for consumer styles. CSS Modules variants expose the
Button variables below; Tailwind variants use native utilities and consumer classes. State attributes and
`data-slot` hooks are shared by both styling tracks.

### CSS Properties

<CssPropertiesSection properties={ButtonDocs.buttonCssProperties} />

### Styling hooks

- `data-slot="button-root"` is the stable moduix selector.
- Standalone roots default to `data-scope="button"` and `data-part="root"`. A composing Ark parent may replace
  them with its own anatomy values.
- `data-variant` and `data-size` reflect the active visual recipe.
- `data-disabled` is present for native `disabled`, `aria-disabled="true"`, and `loading`.
- `data-loading` is present when `loading={true}`.
- `[data-disabled]` drives disabled visuals for native `disabled`, `aria-disabled="true"`, and `loading`.
- Enabled button variants use a subtle press movement; `link` keeps opacity feedback only, and the movement is
  omitted when the user prefers reduced motion.
