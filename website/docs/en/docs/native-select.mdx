---
title: Native Select
component: native-select
description: A styled native `<select>` for simple option lists that keeps the browser in control of selection.
---

import * as NativeSelectDocs from '@/components/examples/native-select/css-properties';
import NativeSelectBasic from '@/components/examples/native-select/basic.tsx';
import NativeSelectControlled from '@/components/examples/native-select/controlled.tsx';
import NativeSelectField from '@/components/examples/native-select/field-integration.tsx';
import NativeSelectMultiple from '@/components/examples/native-select/multiple-selection.tsx';
import nativeSelectSolidBasic from '../../../snippets/native-select/solid/basic.tsx?raw';
import nativeSelectSolidControlled from '../../../snippets/native-select/solid/controlled.tsx?raw';
import nativeSelectSolidField from '../../../snippets/native-select/solid/field-integration.tsx?raw';
import nativeSelectSolidMultiple from '../../../snippets/native-select/solid/multiple-selection.tsx?raw';

## API Reference

`NativeSelect` is a styled wrapper around Ark UI's
[`Field.Select`](https://ark-ui.com/docs/components/field). It renders a real HTML `<select>`, so the
browser remains responsible for option rendering, keyboard interaction, form submission, autofill,
validation, and reset.

## Choosing the right component

Use `NativeSelect` for simple option lists when you want native browser behavior, including autofill,
form reset, keyboard interaction, and the platform picker on mobile. Choose [`Select`](/docs/select)
when you need a custom popup, collection APIs, rich option content, clearing, or programmatic control
of the popup.

## Basic

NativeSelect ships for React and Solid in both the CSS Modules and Tailwind packages. The rendered
preview on this page uses the React CSS Modules package; the framework tabs show native source for
the same public contract.

Add native `option` or `optgroup` elements as children and pass the same props you would pass to an
HTML `<select>`.

<ExampleFrame variant="component">
  <NativeSelectBasic />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/native-select/basic.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={nativeSelectSolidBasic} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

## Install with shadcn

<Tabs groupId="framework">
  <Tab label="React" value="react">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-react/native-select' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-react-tailwind/native-select' },
  ]}
/>

  </Tab>
  <Tab label="Solid" value="solid">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-solid/native-select' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-solid-tailwind/native-select' },
  ]}
/>

  </Tab>
</Tabs>

## Anatomy

`NativeSelect` renders the real `<select>` inside an outer wrapper `<span>`. Collapsed single-select
controls also get a decorative `ChevronDownIcon`; the indicator is hidden for `multiple` and
`size > 1`, when the browser displays a list.

```text
NativeSelect / NativeSelect.Root
└─ span[data-scope="native-select"][data-part="control"][data-slot="native-select-control"]
   ├─ select[data-scope="field"][data-part="select"][data-slot="native-select-root"]
   │  ├─ option
   │  └─ optgroup
   └─ span[data-scope="native-select"][data-part="indicator"][data-slot="native-select-indicator"]
      └─ ChevronDownIcon
```

| Part | Role |
| ---- | ---- |
| `NativeSelect` / `NativeSelect.Root` | Public root component. Renders the native `<select>` and decorative indicator. |

## Composition

Use `defaultValue` for an uncontrolled select. Use `value` with `onChange` when application state owns
the current value. Wrap `NativeSelect` in `Field.Root` when the label, supporting text, validation, or
disabled state should be shared with the control. The `Field` parts connect that content to the native
select while the browser still owns option selection. With `multiple` or `size > 1`, the browser owns
the list interaction and the decorative indicator is hidden. `asChild` can replace the rendered select,
but the child must remain one semantic HTML `<select>`.

## Examples

### Controlled

Use `value` and `onChange` when the application owns the current value. Read the new value from the
native event and pass it back through `value`.

<ExampleFrame variant="component">
  <NativeSelectControlled />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/native-select/controlled.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={nativeSelectSolidControlled} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Field

Use `Field.Root` when the select needs a visible label, helper text, error message, or shared field
state. Field connects that content to the native control.

<ExampleFrame variant="component">
  <NativeSelectField />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/native-select/field-integration.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={nativeSelectSolidField} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/native-select/native-select-field-integration.module.css"
```
</div>

</ExampleCode>

### Multiple

Pass the native `multiple` and `size` props to render a multi-select list. The browser manages the
selected options and their keyboard interaction.

<ExampleFrame variant="component">
  <NativeSelectMultiple />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/native-select/multiple-selection.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={nativeSelectSolidMultiple} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

## Styling

`NativeSelect` reuses the `--moduix-select-*` variables from the CSS Modules variants of `Select`, so
dimensions, indicator spacing, borders, colors, radius, focus ring, disabled opacity, and transitions
can be tuned consistently. Tailwind variants use native utility classes and consumer class overrides.

### CSS Properties

The following variables are available in the CSS Modules variants. Default padding and indicator
geometry use the shared 4px spacing scale.

<div className="docs-reference">
  <Tabs
    values={[{ label: 'CSS Variables', value: 'CSS Variables' }]}
    className="docs-reference-tabs"
    contentItemClassName="docs-reference-panel"
  >
    <Tab>
      <NativeSelectDocs.NativeSelectCssPropertiesPanel />
    </Tab>
  </Tabs>
</div>

### Styling hooks

- In React use `className` on the select, and in Solid use `class`. Use `controlProps.className` or
  `controlProps.class` for the outer wrapper `<span>`.
- Set indicator-related `--moduix-select-*` variables in `controlProps.style` when both the select and
  indicator should inherit them.
- `data-scope="field"` and `data-part="select"` preserve the Ark Field styling contract.
- `data-slot="native-select-root"` is the stable moduix selector for the `<select>` control.
- `data-slot="native-select-control"` and `data-slot="native-select-indicator"` target the internal
  layout and icon.
- Native `:focus-visible`, `:disabled`, `[aria-invalid="true"]`, `[multiple]`, and `[size]` selectors
  control the visual states.
