---
title: Table of Contents
component: table-of-contents
description: Keep an accessible table of contents in sync with the section a reader is viewing.
---

import TocBasic from '@/components/examples/table-of-contents/basic.tsx';
import TocRail from '@/components/examples/table-of-contents/rail.tsx';
import TocRootProvider from '@/components/examples/table-of-contents/root-provider.tsx';
import TocWithCollapsible from '@/components/examples/table-of-contents/with-collapsible.tsx';
import * as TocDocs from '@/components/examples/table-of-contents/css-properties';
import tocSolidBasic from '../../../snippets/table-of-contents/solid/basic.tsx?raw';
import tocSolidRail from '../../../snippets/table-of-contents/solid/rail.tsx?raw';
import tocSolidRootProvider from '../../../snippets/table-of-contents/solid/root-provider.tsx?raw';
import tocSolidWithCollapsible from '../../../snippets/table-of-contents/solid/with-collapsible.tsx?raw';

## API Reference

<PrimitiveReference href="https://ark-ui.com/docs/components/toc" />

## Basic

`Toc` is the shorthand root component. Pass an `items` array with the headings you want to track.
Each item needs a `value` that matches a heading's `id` and a `depth` that matches its semantic
level, such as `2` for `<h2>`. Use the same item when rendering `TocItem`, then build the matching
`TocLink` fragment from its `value`.


On a regular document page, omit `scrollEl` and the component observes the page viewport. The
preview below uses an inner scroll container, so it passes that element to `scrollEl` to keep
tracking and link navigation inside the preview.

<ExampleFrame variant="component">
  <TocBasic />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/table-of-contents/basic.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={tocSolidBasic} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/table-of-contents/table-of-contents-basic.module.css"
```
</div>

</ExampleCode>

## Install with shadcn

See [Quick Start](/docs/quick-start) for full package setup, registry configuration, and styling guidance.

<Tabs groupId="framework">
  <Tab label="React" value="react">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-react/toc' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-react-tailwind/toc' },
  ]}
/>

  </Tab>
  <Tab label="Solid" value="solid">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-solid/toc' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-solid-tailwind/toc' },
  ]}
/>

  </Tab>
</Tabs>

## Anatomy

```text
Toc
├─ TocContent
└─ TocNav
   ├─ TocTitle
   └─ TocList
      ├─ TocIndicator (optional)
      └─ TocItem[item]
         └─ TocLink
            └─ TocRail (optional)

TocRootProvider
└─ same part tree connected to useToc()
```

| Part               | Role                                                                 |
| ------------------ | -------------------------------------------------------------------- |
| `Toc`             | Root part that owns tracking state from the item list.               |
| `TocContent`      | Article containing headings whose IDs match item values.             |
| `TocNav`          | Navigation region connected to the table of contents title.          |
| `TocTitle`        | Accessible name and visible heading for the navigation.              |
| `TocList`         | List containing heading links and the optional indicator.             |
| `TocItem`         | List item bound to one heading, with depth and active-state metadata. |
| `TocLink`         | Same-page anchor that scrolls to its matching heading.               |
| `TocIndicator`    | Subtle marker that follows the active range in the list.              |
| `TocRootProvider` | Connects the parts to tracking state created outside the part tree.   |
| `TocRail`         | Optional SVG branch connecting neighbouring heading depths.            |

## Composition

Pass one item for every tracked heading. Keep `item.value`, the heading `id`, and the `TocLink`
fragment identical, and use the heading's semantic level for `depth`. Omit `scrollEl` when the page
viewport owns scrolling; pass the reading pane when the content has its own scroll container.

Unlike Ark UI, `autoScroll` defaults to `false` for both `Toc` and `useToc()`. Pass `autoScroll` to
opt in only when automatically revealing the active navigation item cannot scroll an ancestor
container.

Use `TocRootProvider` with `useToc()` only when controls outside the part tree need the same
tracking state, such as `activeIds` or `scrollTo`. React reads the store as an object; Solid reads
its reactive state through the returned accessor. Use `TocContext` when a surrounding composition
such as `Collapsible` needs the active items. For router links, use `TocLink asChild` with one
semantic `<a>` child; in Solid, pass that child through the render function expected by `asChild`.
Choose either `TocIndicator` for an active-range marker or `TocRail` for per-link hierarchy; do
not render both in the same list.

## Examples

### Root Provider

Use `useToc()` when another control needs to read the active headings or scroll the reading pane.

<ExampleFrame variant="component">
  <TocRootProvider />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/table-of-contents/root-provider.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={tocSolidRootProvider} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/table-of-contents/table-of-contents-root-provider.module.css"
```
</div>

</ExampleCode>

### Rail

Place `TocRail` inside each link to draw an SVG connector between neighbouring heading depths.
Pass the current, previous, and next depths from the same ordered `items` array. The rail on the
active link uses the indicator colour. Do not render `TocIndicator` in the same list.

<ExampleFrame variant="component">
  <TocRail />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/table-of-contents/rail.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={tocSolidRail} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/table-of-contents/table-of-contents-rail.module.css"
```
</div>

</ExampleCode>

### With Collapsible

Place the navigation list inside `Collapsible` when readers should be able to hide the full table
of contents. `TocContext` supplies the active item for the trigger label and progress ring, while
the links continue to target the same reading pane.

<ExampleFrame variant="component">
  <TocWithCollapsible />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/table-of-contents/with-collapsible.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={tocSolidWithCollapsible} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/table-of-contents/table-of-contents-with-collapsible.module.css"
```
</div>

</ExampleCode>

## Styling

Style the component through `className` in React or `class` in Solid, Ark attributes, moduix
`data-slot` hooks, and the public `--moduix-table-of-contents-*` variables. The CSS Modules
packages use these variables; the Tailwind packages use native utility classes and consumer classes
for overrides.

### CSS Properties

<CssPropertiesSection properties={TocDocs.tableOfContentsOverrideCssProperties} />

### Styling hooks

- `data-slot="toc-root"`, `toc-root-provider`, `toc-content`, `toc-nav`, `toc-title`, `toc-list`,
  `toc-item`, `toc-link`, `toc-indicator`, and `toc-rail`
- Ark `data-scope="toc"` and `data-part` attributes on primitive parts
- `TocItem` attributes and variables: `data-value`, `data-depth`, `data-active`, `data-first`,
  `data-last`, and `--depth`; `TocLink` exposes `data-value`, `data-active`, and `aria-current`
- Ark runtime `--top`, `--left`, `--width`, and `--height` variables on the root
