---
title: Frame
description: Render React or Solid content in an isolated iframe with Ark UI Frame.
---

import FrameBasic from '@/components/examples/frame/basic.tsx';
import FrameSizing from '@/components/examples/frame/sizing.tsx';
import frameSolidBasic from '../../../snippets/frame/solid/basic.tsx?raw';
import frameSolidSizing from '../../../snippets/frame/solid/sizing.tsx?raw';
import frameSolidLifecycle from '../../../snippets/frame/solid/lifecycle.tsx?raw';
import frameSolidStaticDocument from '../../../snippets/frame/solid/static-document.tsx?raw';

`Frame` renders React or Solid content in an isolated iframe document.

## API Reference

<PrimitiveReference href="https://ark-ui.com/docs/utilities/frame" />

Import the utility from the matching Ark UI package:

<Tabs
  groupId="framework"
  values={[
    { label: 'React', value: 'react' },
    { label: 'Solid', value: 'solid' },
  ]}
  className="docs-tabs"
>
  <Tab value="react">

```tsx
import { Frame } from '@ark-ui/react';
```

  </Tab>
  <Tab value="solid">

```tsx
import { Frame } from '@ark-ui/solid';
```

  </Tab>
</Tabs>

## Basic

`Frame` renders its children inside the iframe document. Always provide a descriptive `title`, because it becomes the
iframe's accessible name. The example also passes a `<style>` element through `head`, since styles from the host
document do not cross the iframe boundary.

<ExampleFrame variant="component">
  <PreviewFrame maxWidth="sm">
    <FrameBasic />
  </PreviewFrame>
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/frame/basic.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={frameSolidBasic} />

    </Tab>
  </Tabs>
</div>
</ExampleCode>

## Content styles and size

The `head` prop accepts JSX content and inserts it into the iframe's `<head>`. Use it for embedded styles, fonts, or
metadata. Styles needed by the embedded UI must also be loaded through this prop.

`Frame` observes the mounted content and writes its size to the iframe as `--width` and `--height` in pixels. Use
`block-size: var(--height, 10rem)` when the iframe should grow with its content. The `--width` variable can be used in
the same way for inline size.

<ExampleFrame variant="component">
  <PreviewFrame maxWidth="sm">
    <FrameSizing />
  </PreviewFrame>
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/frame/sizing.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={frameSolidSizing} />

    </Tab>
  </Tabs>
</div>
</ExampleCode>

## Lifecycle callbacks

Use `onMount` and `onUnmount` for resources owned by the embedded experience. Both callbacks are called without
arguments. A ref gives the application access to the iframe document when setup needs to interact with it.

Only inject scripts that your application controls. Prefer `head` for styles and metadata.

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/frame/lifecycle.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={frameSolidLifecycle} />

    </Tab>
  </Tabs>
</div>
</ExampleCode>

## Static HTML documents

Use `srcDoc` in React or `srcdoc` in Solid when the iframe should start from a complete HTML document instead of
rendered framework children. These are the native spellings of the same iframe attribute in each framework.

For untrusted HTML, set the native `sandbox` attribute and grant only the permissions the content needs. Keep
`allow-scripts` and `allow-same-origin` out of the same sandbox for untrusted content.

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/frame/static-document.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={frameSolidStaticDocument} />

    </Tab>
  </Tabs>
</div>
</ExampleCode>
