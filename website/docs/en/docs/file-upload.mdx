---
title: File Upload
component: file-upload
description: Ark UI file upload component for file selection, drag-and-drop, validation, and previews.
---

import * as FileUploadDocs from '@/components/examples/file-upload/css-properties';
import FileUploadAcceptedFileTypes from '@/components/examples/file-upload/accepted-file-types.tsx';
import FileUploadAdvancedCustomization from '@/components/examples/file-upload/advanced-customization.tsx';
import FileUploadBasic from '@/components/examples/file-upload/basic.tsx';
import FileUploadClearTrigger from '@/components/examples/file-upload/clear-trigger.tsx';
import FileUploadControlled from '@/components/examples/file-upload/controlled.tsx';
import FileUploadDirectoryUpload from '@/components/examples/file-upload/directory-upload.tsx';
import FileUploadDropzone from '@/components/examples/file-upload/dropzone.tsx';
import FileUploadErrorHandling from '@/components/examples/file-upload/error-handling.tsx';
import FileUploadFilePreviews from '@/components/examples/file-upload/file-previews.tsx';
import FileUploadFormUsage from '@/components/examples/file-upload/form-usage.tsx';
import FileUploadInitialFiles from '@/components/examples/file-upload/initial-files.tsx';
import FileUploadMediaCapture from '@/components/examples/file-upload/media-capture.tsx';
import FileUploadRejectedFiles from '@/components/examples/file-upload/rejected-files.tsx';
import FileUploadRootProviderAndPaste from '@/components/examples/file-upload/root-provider-and-paste.tsx';
import FileUploadTransformFiles from '@/components/examples/file-upload/transform-files.tsx';
import FileUploadWithField from '@/components/examples/file-upload/with-field.tsx';
import fileUploadSolidAcceptedFileTypes from '../../../snippets/file-upload/solid/accepted-file-types.tsx?raw';
import fileUploadSolidAdvancedCustomization from '../../../snippets/file-upload/solid/advanced-customization.tsx?raw';
import fileUploadSolidBasic from '../../../snippets/file-upload/solid/basic.tsx?raw';
import fileUploadSolidClearTrigger from '../../../snippets/file-upload/solid/clear-trigger.tsx?raw';
import fileUploadSolidControlled from '../../../snippets/file-upload/solid/controlled.tsx?raw';
import fileUploadSolidDirectoryUpload from '../../../snippets/file-upload/solid/directory-upload.tsx?raw';
import fileUploadSolidDropzone from '../../../snippets/file-upload/solid/dropzone.tsx?raw';
import fileUploadSolidErrorHandling from '../../../snippets/file-upload/solid/error-handling.tsx?raw';
import fileUploadSolidFilePreviews from '../../../snippets/file-upload/solid/file-previews.tsx?raw';
import fileUploadSolidFormUsage from '../../../snippets/file-upload/solid/form-usage.tsx?raw';
import fileUploadSolidInitialFiles from '../../../snippets/file-upload/solid/initial-files.tsx?raw';
import fileUploadSolidMediaCapture from '../../../snippets/file-upload/solid/media-capture.tsx?raw';
import fileUploadSolidRejectedFiles from '../../../snippets/file-upload/solid/rejected-files.tsx?raw';
import fileUploadSolidRootProviderAndPaste from '../../../snippets/file-upload/solid/root-provider-and-paste.tsx?raw';
import fileUploadSolidTransformFiles from '../../../snippets/file-upload/solid/transform-files.tsx?raw';
import fileUploadSolidWithField from '../../../snippets/file-upload/solid/with-field.tsx?raw';

## API Reference

<PrimitiveReference href="https://ark-ui.com/docs/components/file-upload" />

`FileUpload` is available from `@moduix/react/file-upload`. `FileUpload`, `useFileUpload`, and
`useFileUploadContext` are also exported from `@moduix/react`.

The same component is available from `@moduix/solid/file-upload`, `@moduix/react-tailwind/file-upload`, and
`@moduix/solid-tailwind/file-upload`. The four package variants expose the same component parts and state model.
Choose the package that matches your framework and styling path.

## Choosing the right component

Use `FileUpload` when the application needs to keep selected files in state, show a dropzone, or
manage an upload flow. `Input` with `type="file"` is enough for a native chooser in a simple form.

## Basic


Start with `FileUpload` as the root. Add a `FileUploadLabel`, then use `FileUploadTrigger` for a compact control or `FileUploadDropzone` for
drag-and-drop. Add `FileUploadHiddenInput` explicitly to open the native picker. Put `FileUploadItems` inside
`FileUploadItemGroup` to show accepted files. `FileUploadItems` uses an image thumbnail only when the file has an `image/*` MIME type; otherwise it uses the
generic file preview.

<ExampleFrame variant="component">
  <FileUploadBasic />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/file-upload/basic.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={fileUploadSolidBasic} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/file-upload/file-upload-basic.module.css"
```
</div>

</ExampleCode>

## Install with shadcn

See [Quick Start](/docs/quick-start) for full package setup, registry configuration, and styling guidance.

<Tabs groupId="framework">
  <Tab label="React" value="react">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-react/file-upload' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-react-tailwind/file-upload' },
  ]}
/>

  </Tab>
  <Tab label="Solid" value="solid">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-solid/file-upload' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-solid-tailwind/file-upload' },
  ]}
/>

  </Tab>
</Tabs>

## Anatomy

```text
FileUpload / FileUploadRootProvider
├─ FileUploadLabel
├─ FileUploadDropzone (optional)
│  ├─ FileUploadDropzoneIcon
│  └─ FileUploadTrigger
├─ FileUploadTrigger (when no dropzone is used)
├─ FileUploadItemGroup[type]
│  ├─ FileUploadItems
│  └─ FileUploadContext
│     └─ FileUploadItem[file]
│        ├─ FileUploadItemPreview[type]
│        │  ├─ FileUploadItemPreviewImage
│        │  └─ FileUploadItemPreviewIcon
│        ├─ FileUploadItemName
│        ├─ FileUploadItemMetadata[file]
│        ├─ FileUploadItemSizeText
│        └─ FileUploadItemDeleteTrigger
├─ FileUploadClearTrigger
└─ FileUploadHiddenInput (explicit)
```

| Part                             | Role                                                                   |
| -------------------------------- | ---------------------------------------------------------------------- |
| `FileUpload` / `FileUploadRootProvider` | Owns file state and handles validation, drag-and-drop, paste, and form integration. |
| `FileUploadHiddenInput` | Native Ark form control; compose explicitly as described below. |
| `FileUploadRootProvider`        | Connects the upload tree to state returned by the `useFileUpload` hook.              |
| `FileUploadLabel`               | Accessible label for the hidden file input and upload controls.                     |
| `FileUploadDropzone`            | Drag-and-drop target. Pass `disableClick` when it contains a nested trigger.        |
| `FileUploadDropzoneIcon`        | Decorative upload icon for a `FileUploadDropzone`.                                            |
| `FileUploadTrigger`             | Button that opens the native file picker.                                           |
| `FileUploadItemGroup`           | List for accepted or rejected files.                                                 |
| `FileUploadItems`               | Convenience component that renders image cards and file rows with previews, metadata, and delete controls. |
| `FileUploadContext`             | Ark-compatible render-prop context for custom file rows.                             |
| `FileUploadItem`                | File row. Pass it the current `file` object.                                         |
| `FileUploadItemPreview`         | Preview slot that matches a file by MIME type.                                       |
| `FileUploadItemPreviewImage`    | Image preview inside `FileUploadItemPreview`.                                                  |
| `FileUploadItemPreviewIcon`     | Generic file icon for a fallback preview inside `FileUploadItemPreview`.                      |
| `FileUploadItemName`            | File name text.                                                                      |
| `FileUploadItemMetadata`        | File type and localized size. Pass it the current `file` object.                    |
| `FileUploadItemSizeText`        | Localized file-size text.                                                            |
| `FileUploadItemDeleteTrigger`   | Removes one file. Defaults to the moduix trash icon.                                |
| `FileUploadClearTrigger`        | Clears selected files. Defaults to the moduix close icon.                           |

## Composition

`FileUpload` keeps file state internally by default. Use `defaultAcceptedFiles` for an uncontrolled initial value, or pair
`acceptedFiles` with `onFileChange(details)` when the application owns the files. Use `useFileUpload()` with
`FileUploadRootProvider` when state or methods must be created outside the rendered tree.

Render `FileUploadHiddenInput` explicitly inside `FileUpload` or `FileUploadRootProvider`, even outside a form, because `FileUploadTrigger` uses it
to open the native picker. Set `name` on the root for native form submission. For a dropzone with a nested `FileUploadTrigger`, pass
`disableClick` so the picker opens only once.

Use `FileUploadItems` inside `FileUploadItemGroup` for the default file rows. For custom rows, render `FileUploadContext` and
choose one preview per file from `acceptedFiles` or `rejectedFiles`.

## Examples

### Accepted File Types

Use `accept` to restrict both the native file picker and drop validation. Pass MIME types or file extensions.

<ExampleFrame variant="component">
  <FileUploadAcceptedFileTypes />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/file-upload/accepted-file-types.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={fileUploadSolidAcceptedFileTypes} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/file-upload/file-upload-accepted-file-types.module.css"
```
</div>

</ExampleCode>

### Clear Trigger

Use `FileUploadClearTrigger` to remove all accepted files at once.

<ExampleFrame variant="component">
  <FileUploadClearTrigger />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/file-upload/clear-trigger.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={fileUploadSolidClearTrigger} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/file-upload/file-upload-clear-trigger.module.css"
```
</div>

</ExampleCode>

### Controlled

Control `acceptedFiles` when selected files need to stay in sync with external state or a multi-step flow. Update the
value from `onFileChange(details)`.

<ExampleFrame variant="component">
  <FileUploadControlled />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/file-upload/controlled.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={fileUploadSolidControlled} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/file-upload/file-upload-controlled.module.css"
```
</div>

</ExampleCode>

### Directory Upload

Use `directory` to let users select a folder. Supported browsers expose each file's relative path through
`file.webkitRelativePath`. Set `maxFiles` high enough for the folder or omit it when the number of files is unknown.

<ExampleFrame variant="component">
  <FileUploadDirectoryUpload />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/file-upload/directory-upload.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={fileUploadSolidDirectoryUpload} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/file-upload/file-upload-directory-upload.module.css"
```
</div>

</ExampleCode>

### Dropzone

Use `FileUploadDropzone` for drag-and-drop uploads. Keep a nested `FileUploadTrigger` so users can also open the picker with the keyboard.

<ExampleFrame variant="component">
  <FileUploadDropzone />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/file-upload/dropzone.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={fileUploadSolidDropzone} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/file-upload/file-upload-dropzone.module.css"
```
</div>

</ExampleCode>

### Error Handling

Use `onFileReject(details)` to show application-specific feedback. Keep built-in constraints such as `accept`,
`maxFiles`, and `maxFileSize` on the root. Add `validate(file, details)` for application-specific checks.

<ExampleFrame variant="component">
  <FileUploadErrorHandling />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/file-upload/error-handling.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={fileUploadSolidErrorHandling} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/file-upload/file-upload-error-handling.module.css"
```
</div>

</ExampleCode>

### File Previews

This example starts with an SVG image and a PDF, so you can see both layouts immediately. File-row preview boxes are
2.5rem square by default, and long names stay on one line. Remove either item or add files from your device to try
the same layouts with your own files.

<ExampleFrame variant="component">
  <FileUploadFilePreviews />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/file-upload/file-previews.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={fileUploadSolidFilePreviews} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/file-upload/file-upload-file-previews.module.css"
```
</div>

</ExampleCode>

### Form Usage

Pass `name` to the root and render `FileUploadHiddenInput`; native form submission and reset then include the selected files.

<ExampleFrame variant="component">
  <FileUploadFormUsage />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/file-upload/form-usage.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={fileUploadSolidFormUsage} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/file-upload/file-upload-form-usage.module.css"
```
</div>

</ExampleCode>

### Initial Files

Use `defaultAcceptedFiles` to preload files and then let the component manage the selection.

<ExampleFrame variant="component">
  <FileUploadInitialFiles />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/file-upload/initial-files.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={fileUploadSolidInitialFiles} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/file-upload/file-upload-initial-files.module.css"
```
</div>

</ExampleCode>

### Media Capture

Use `capture` with an image or video `accept` value to suggest opening the camera on mobile devices. Set it to
`"user"` or `"environment"` to suggest the front or rear camera. This is a hint to the browser, not a guarantee that
the camera will open.

<ExampleFrame variant="component">
  <FileUploadMediaCapture />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/file-upload/media-capture.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={fileUploadSolidMediaCapture} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/file-upload/file-upload-media-capture.module.css"
```
</div>

</ExampleCode>

### Rejected Files

Render `rejectedFiles` in a separate `FileUploadItemGroup type="rejected"` when users need to see why files were rejected.

<ExampleFrame variant="component">
  <FileUploadRejectedFiles />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/file-upload/rejected-files.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={fileUploadSolidRejectedFiles} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/file-upload/file-upload-rejected-files.module.css"
```
</div>

</ExampleCode>

### Root Provider and Pasting Files

Use `useFileUpload` with `FileUploadRootProvider` when state or methods need to live outside the rendered upload tree. In this
example, `setClipboardFiles` forwards files pasted into a text area to the upload state.

<ExampleFrame variant="component">
  <FileUploadRootProviderAndPaste />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/file-upload/root-provider-and-paste.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={fileUploadSolidRootProviderAndPaste} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/file-upload/file-upload-root-provider-and-paste.module.css"
```
</div>

</ExampleCode>

### Transform Files

Use `transformFiles(files)` to process files asynchronously before adding them to accepted state.

<ExampleFrame variant="component">
  <FileUploadTransformFiles />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/file-upload/transform-files.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={fileUploadSolidTransformFiles} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/file-upload/file-upload-transform-files.module.css"
```
</div>

</ExampleCode>

### With Field

Wrap the upload in `Field` when helper text, an error message, required state, or invalid state belongs to a form field.

<ExampleFrame variant="component">
  <FileUploadWithField />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/file-upload/with-field.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={fileUploadSolidWithField} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/file-upload/file-upload-with-field.module.css"
```
</div>

</ExampleCode>

### Advanced Customization

Use custom rows instead of `FileUploadItems` when you need MIME-specific previews, extra metadata, or a different layout. The
explicit `FileUploadContext` composition keeps Ark state visible and lets you choose one image or fallback preview
for each file.

<ExampleFrame variant="component">
  <FileUploadAdvancedCustomization />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/file-upload/advanced-customization.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={fileUploadSolidAdvancedCustomization} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/file-upload/file-upload-advanced-customization.module.css"
```
</div>

</ExampleCode>

## Styling

Use `className` in React or `class` in Solid on exported parts for structural styles. Tailwind variants use native
utilities and consumer classes; CSS Modules variants expose the variables below. Both styling tracks share Ark state
attributes and moduix `data-slot` hooks.

### CSS Properties

<div className="docs-reference">
  <Tabs
    values={[{ label: 'CSS Variables', value: 'CSS Variables' }]}
    className="docs-reference-tabs"
    contentItemClassName="docs-reference-panel"
  >
    <Tab value="CSS Variables">
      <div>
        <FileUploadDocs.FileUploadCssPropertiesPanel />
      </div>
    </Tab>
  </Tabs>
</div>

### Styling hooks

Target stable moduix slots such as `[data-slot="file-upload-dropzone"]`,
`[data-slot="file-upload-item"]`, and `[data-slot="file-upload-item-delete-trigger"]`.
Ark also exposes `data-scope="file-upload"`, part-specific `data-part`, and state attributes such as
`data-disabled`, `data-readonly`, `data-invalid`, `data-required`, `data-dragging`, and `data-type`.
