---
title: Splitter
component: splitter
description: Build accessible resizable layouts in React or Solid with package and shadcn installation options.
---

import { CssPropertiesSection } from '@/components/mdx/Components';
import { splitterOverrideCssProperties } from '@/components/examples/splitter/css-properties';
import SplitterBasic from '@/components/examples/splitter/basic.tsx';
import SplitterCollapsiblePanels from '@/components/examples/splitter/collapsible-panels.tsx';
import SplitterControlled from '@/components/examples/splitter/controlled.tsx';
import SplitterDynamicCollapsible from '@/components/examples/splitter/dynamic-collapsible.tsx';
import SplitterMultiplePanels from '@/components/examples/splitter/multiple-panels.tsx';
import SplitterNested from '@/components/examples/splitter/nested.tsx';
import SplitterRootProvider from '@/components/examples/splitter/root-provider.tsx';
import SplitterVertical from '@/components/examples/splitter/vertical.tsx';
import splitterSolidBasic from '../../../snippets/splitter/solid/basic.tsx?raw';
import splitterSolidCollapsiblePanels from '../../../snippets/splitter/solid/collapsible-panels.tsx?raw';
import splitterSolidControlled from '../../../snippets/splitter/solid/controlled.tsx?raw';
import splitterSolidDynamicCollapsible from '../../../snippets/splitter/solid/dynamic-collapsible.tsx?raw';
import splitterSolidMultiplePanels from '../../../snippets/splitter/solid/multiple-panels.tsx?raw';
import splitterSolidNested from '../../../snippets/splitter/solid/nested.tsx?raw';
import splitterSolidRootProvider from '../../../snippets/splitter/solid/root-provider.tsx?raw';
import splitterSolidVertical from '../../../snippets/splitter/solid/vertical.tsx?raw';

## API Reference

<PrimitiveReference href="https://ark-ui.com/docs/components/splitter" />

## Basic


Build a splitter from `Splitter`, `Splitter.Panel`, and `Splitter.ResizeTrigger`. The `panels` prop
lists the panel ids and their size constraints. Each rendered `Splitter.Panel` must use one of those
ids. When you omit the trigger's children, moduix renders a resize indicator for you.

<ExampleFrame variant="component">
  <SplitterBasic />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/splitter/basic.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={splitterSolidBasic} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/splitter/splitter-basic.module.css"
```
</div>

</ExampleCode>

## Install with shadcn

See [Quick Start](/docs/quick-start) for full package setup, registry configuration, and styling guidance.

<Tabs groupId="framework">
  <Tab label="React" value="react">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-react/splitter' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-react-tailwind/splitter' },
  ]}
/>

  </Tab>
  <Tab label="Solid" value="solid">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-solid/splitter' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-solid-tailwind/splitter' },
  ]}
/>

  </Tab>
</Tabs>

## Anatomy

```text
Splitter / Splitter.Root
├─ Splitter.Panel[id]
├─ Splitter.ResizeTrigger[id="panel-a:panel-b"]
│  └─ Splitter.ResizeTriggerIndicator (default, customizable)
└─ Splitter.Panel[id]

Splitter.RootProvider
└─ same panel tree connected to a useSplitter() store
```

| Part                              | Role                                                                           |
| --------------------------------- | ------------------------------------------------------------------------------ |
| `Splitter` / `Splitter.Root`      | Manages panel definitions, sizes, orientation, callbacks, and splitter state. |
| `Splitter.Panel`                  | Resizable region whose `id` must match an entry in `panels`.                  |
| `Splitter.ResizeTrigger`          | Accessible button for resizing the two adjacent panels.                       |
| `Splitter.ResizeTriggerIndicator` | Default visual handle. Custom trigger children replace it.                   |
| `Splitter.RootProvider`            | Renders the same parts using state created with `useSplitter()`.              |

## Composition

Use `size` with `onResize(details)` when the application owns the current panel sizes. Use
`Splitter.RootProvider` with `useSplitter()` when the store or its controls must live outside the
rendered panel tree, and choose one root for each store. For nested splitters that should resize
together at handle intersections, create one `createSplitterRegistry()` and pass it to every root.

Use `asChild` only with one interactive child that keeps button semantics, focus behavior, and
keyboard interaction; it replaces the default resize indicator. In Solid, `useSplitter()` returns an
accessor, so pass it to `value` and read the API as `splitter()`.

## Examples

### Collapsible panels

Set `collapsible: true` and choose a `collapsedSize` for a panel that can turn into a narrow rail. Set
`minSize` to the same value when pointer resizing should remain continuous to the collapsed size. If
`minSize` is larger, the splitter snaps between the expanded minimum and the collapsed rail.

<ExampleFrame variant="component">
  <SplitterCollapsiblePanels />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/splitter/collapsible-panels.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={splitterSolidCollapsiblePanels} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/splitter/splitter-collapsible-panels.module.css"
```
</div>

</ExampleCode>

### Controlled

Keep panel sizes in application state and update that state from `details.size` in `onResize`.

<ExampleFrame variant="component">
  <SplitterControlled />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/splitter/controlled.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={splitterSolidControlled} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/splitter/splitter-controlled.module.css"
```
</div>

</ExampleCode>

### Multiple panels

Add another `Panel` and an adjacent `ResizeTrigger` for a three-panel layout. Every panel needs a
matching entry in `panels`, and every trigger id must connect its two neighbors.

<ExampleFrame variant="component">
  <SplitterMultiplePanels />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/splitter/multiple-panels.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={splitterSolidMultiplePanels} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/splitter/splitter-multiple-panels.module.css"
```
</div>

</ExampleCode>

### Nested

Pass the same registry to nested splitters to support multi-drag at intersections between horizontal
and vertical handles.

<ExampleFrame variant="component">
  <SplitterNested />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/splitter/nested.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={splitterSolidNested} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/splitter/splitter-nested.module.css"
```
</div>

</ExampleCode>

### Responsive collapse

Use `collapsePanel()` and `expandPanel()` when a sidebar should react to the available width measured
by the application.

<ExampleFrame variant="component">
  <SplitterDynamicCollapsible />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/splitter/dynamic-collapsible.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={splitterSolidDynamicCollapsible} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/splitter/splitter-dynamic-collapsible.module.css"
```
</div>

</ExampleCode>

### RootProvider

Use `useSplitter()` with `Splitter.RootProvider` when state or controls need to live outside the
rendered splitter parts. The returned API includes methods such as `getSizes()`, `setSizes()`,
`resizePanel()`, `collapsePanel()`, `expandPanel()`, and `resetSizes()`.

<ExampleFrame variant="component">
  <SplitterRootProvider />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/splitter/root-provider.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={splitterSolidRootProvider} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/splitter/splitter-root-provider.module.css"
```
</div>

</ExampleCode>

### Vertical

Set `orientation="vertical"` and give the root a stable height so stacked panels have room to
resize.

<ExampleFrame variant="component">
  <SplitterVertical />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/splitter/vertical.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={splitterSolidVertical} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/splitter/splitter-vertical.module.css"
```
</div>

</ExampleCode>

## Accessibility

`ResizeTrigger` renders a button and provides pointer and keyboard resizing according to the WAI-ARIA
Window Splitter pattern. Give each trigger an accessible name, for example with
`aria-label="Resize panels"`, when its visual handle has no text label. If you use `asChild`, the
custom host must keep button semantics, focus behavior, and keyboard interaction.

Ark exposes state through attributes such as `data-orientation`, `data-dragging`, `data-focus`, and
`data-disabled`. Use these attributes when styling interaction states.

## Styling

Use `className` in React or `class` in Solid on exported parts for structural styles. Tailwind
variants use native utilities and consumer classes; the CSS Modules variants expose the component
variables below. Data attributes and `data-slot` hooks are shared by both styling tracks.

The resize trigger reserves a `1px` divider in the layout and draws a thinner `0.5px` line over it.
Its transparent hit area makes the handle easier to use, and the rounded indicator sits on top of the
line. Hover and dragging states strengthen the line, border, and shadow. Override the corresponding
`--moduix-splitter-resize-trigger-*` variables when the surrounding theme needs a different treatment.

### CSS Properties

<CssPropertiesSection properties={splitterOverrideCssProperties} />

### Styling hooks

Ark writes `data-scope="splitter"` and `data-part` to the rendered parts. moduix also writes these
`data-slot` values: `splitter-root`, `splitter-root-provider`, `splitter-panel`,
`splitter-resize-trigger`, and `splitter-resize-trigger-indicator`.

Useful part-specific attributes include `data-id` and `data-index` on panels, plus
`data-orientation`, `data-dragging`, `data-focus`, and `data-disabled` on the relevant parts.
