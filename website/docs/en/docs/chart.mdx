---
title: Chart
component: chart
description: Render accessible, responsive TanStack Charts definitions with a composed moduix figure, tooltip, legend, and motion.
---

import * as ChartDocs from '@/components/examples/chart';

## API Reference

Ark UI does not include a chart primitive. moduix provides the figure, title, legend, and tooltip
around the [TanStack Charts React host](https://tanstack.com/charts/latest/docs/framework/react/reference/chart).
You still define marks, scales, interactions, and callbacks with TanStack Charts. TanStack Charts
0.14 requires React 19.

## Basic

Create a TanStack definition and pass it to `Chart.Plot`. Add the visible title, description, and
legend around the plot, then provide a concise `ariaLabel` for people using assistive technology.

<PreviewFrame maxWidth="lg">
  ```tsx preview file="./_snippets/chart/basic.tsx"

```
</PreviewFrame>

## Install with shadcn

<ShadcnInstall packageName="chart" />

## Anatomy

```text
Chart / Chart.Root
├─ Chart.Header (optional figcaption)
│  ├─ Chart.Title
│  └─ Chart.Description
├─ Chart.Plot
│  └─ TanStack chart surface (SVG by default)
└─ Chart.Legend (optional)
   └─ Chart.LegendItem
```

| Part | Role |
| --- | --- |
| `Chart` / `Chart.Root` | Composed `figure` surface. Accepts native props, `asChild`, and an `HTMLElement` ref. |
| `Chart.Plot` | TanStack React host. Requires `definition` and `ariaLabel`; defaults to the Moduix motion preset and accepts `motion={false}`. |
| `Chart.Header` | `figcaption` layout for the title and description. |
| `Chart.Title` | Chart heading; renders `h3` by default. |
| `Chart.Description` | Supporting visible context. |
| `Chart.Legend` | Semantic list of visible series. |
| `Chart.LegendItem` | Legend item with an optional indicator `color`. |

## Composition

### Recommended composition

Keep a stable TanStack definition outside the component, then assemble `Chart.Header`,
`Chart.Plot`, and `Chart.Legend` in the root component. Define `barY`, `areaY`, `lineY`, stacking,
scales, guides, and color channels directly with TanStack. moduix does not duplicate them as React
parts.

Use the definition's `color` scale for marks and tooltip swatches. Reuse the corresponding moduix
chart tokens in `Chart.LegendItem` so the visible legend stays aligned with the chart.

### Choosing marks

`Chart.Plot` is the renderer host, while the marks inside `definition` determine what is drawn. All
variants use the same `<Chart.Plot definition={definition} />` composition.

| Goal | Marks | Example |
| --- | --- | --- |
| Vertical bars | `barY` | Basic, Stacked bars |
| Horizontal bars | `barX` | Horizontal bars |
| Line or area | `lineY`, `areaY` | Animated area, Chart colors |
| Pie or donut | `pie`, `polar`, `radialArc` | Donut chart |
| Scatter plot | `dot` | Scatter plot |
| Heatmap | `cell` | Heatmap |

### When to go custom

`Chart.Plot` uses TanStack's responsive `motion()` preset by default. Set `motion={false}` for a
static SVG chart, or pass a different `renderer` to control animation yourself. An explicit
`renderer` takes precedence over `motion`. If your application owns the rendering surface, use
TanStack's canvas or low-level renderer directly.

```tsx
<Chart.Plot definition={definition} ariaLabel="Monthly revenue" motion={false} />
```

Chart uses a compact Moduix tooltip by default, so rows without a swatch do not leave an empty
column. Use `renderTooltipBody` only to add React content. Its callback receives TanStack's native
`defaultBody` with the title, rows, formatting, and swatches. TanStack continues to handle the
tooltip's anchor, position, portal, pinned state, and dismissal. Only render interactive controls
when `pinned` is true.

### Default props

| Part | Prop | Default | Notes |
| --- | --- | --- | --- |
| `Chart.Root` | element | `figure` | `Chart.Header` is its `figcaption`. |
| `Chart.Plot` | `definition` | - | Required TanStack chart definition. |
| `Chart.Plot` | `ariaLabel` | - | Required accessible chart name. |
| `Chart.Plot` | `motion` | `true` | Set to `false` to use TanStack's static SVG renderer. |
| `Chart.Plot` | `renderer` | Moduix motion preset | Animates geometry and tooltip; respects reduced motion. |
| `Chart.Plot` | `height` | `320` | Used when `aspectRatio` is absent. |
| `Chart.Plot` | `width` | responsive | Follows the host width. |
| `Chart.Plot` | `initialWidth` | `640` | Server and pre-measurement width. |
| `Chart.Plot` | `tabIndex` | `0` | Keeps TanStack keyboard navigation in the tab order. |

### Behavioral notes

- Keep a static definition at module scope. When it captures props or state, memoize the complete
  definition with `useMemo`.
- The default motion renderer animates initial geometry, keyed data updates, focus guides, and the
  native tooltip. It snaps when `prefers-reduced-motion: reduce` matches; `motion={false}` disables
  it independently of the user's media preference.
- Pointer focus, arrow-key navigation, Enter/Space activation, `onFocusChange`, and `onSelect`
  remain owned by TanStack.
- The plot's focusable SVG uses the moduix focus ring. Do not set `focusRing: false` unless custom
  focus geometry replaces TanStack's point indicator.
- `ariaDescription` adds detail when the visible title and description are not sufficient.
- `className` and `style` on `Chart.Plot` target the outer TanStack host. The host has no ref API.

## Examples

### Animated area

Area, line, points, grouped focus, and tooltip motion all remain native TanStack behavior.

<PreviewFrame maxWidth="lg">
  ```tsx preview file="./_snippets/chart/animated-area.tsx"

```
</PreviewFrame>

### Chart colors

Use moduix chart tokens in the TanStack color scale. CSS custom properties are valid SVG paint
values and automatically follow the active theme.

<PreviewFrame maxWidth="lg">
  ```tsx preview file="./_snippets/chart/line-colors.tsx"

```
</PreviewFrame>

### Donut chart

Compose TanStack's opt-in `pie`, `polar`, and `radialArc` primitives for part-to-whole data. The
same `Chart.Plot`, motion, focus, tooltip, and legend composition work outside Cartesian charts.

<PreviewFrame maxWidth="lg">
  ```tsx preview file="./_snippets/chart/donut.tsx"

```
</PreviewFrame>

### Heatmap

Use `cell` with band scales on both axes to build a compact categorical matrix. A color channel
turns each cell's level into a theme-aware paint value.

<PreviewFrame maxWidth="lg">
  ```tsx preview file="./_snippets/chart/heatmap.tsx"

```
</PreviewFrame>

### Horizontal bars

Use `barX` when category labels are long or the ranking should read from top to bottom. The numeric
value belongs on x and the category belongs on y.

<PreviewFrame maxWidth="lg">
  ```tsx preview file="./_snippets/chart/horizontal-bars.tsx"

```
</PreviewFrame>

### Scatter plot

Use `dot` to compare two quantitative values. Color can carry one additional categorical dimension
without changing the x and y relationship.

<PreviewFrame maxWidth="lg">
  ```tsx preview file="./_snippets/chart/scatter.tsx"

```
</PreviewFrame>

### Stacked bars

Use TanStack's tidy color channel and `stack()` layout for multi-series bars. `focus: 'group-x'`
keeps the tooltip and keyboard focus grouped by quarter.

<PreviewFrame maxWidth="lg">
  ```tsx preview file="./_snippets/chart/stacked-bars.tsx"

```
</PreviewFrame>

### Advanced Customization

The Ark factory parts accept `asChild`, so they can apply their styles to semantic elements in your
layout. This dashboard section combines Chart with Button, replaces the default `figure` and
`figcaption` with a labelled `section` and `header`, and places the legend in the header.

`Chart.Plot` is still the TanStack host, not a slot. Use its `className` and `style` on the outer
element, and use `renderTooltipBody` to extend the tooltip. Render `defaultBody` inside your own
wrapper while TanStack continues to position and portal it. Add interactive controls only to a
pinned tooltip.

<PreviewFrame maxWidth="lg">
  ```tsx preview file="./_snippets/chart/advanced-customization.tsx"

```
</PreviewFrame>

## Styling

Style the figure with `Chart.Root` and the TanStack host with `Chart.Plot`. Public CSS properties
control the surface, focus ring, tooltip shell, and default tooltip layout. Keep chart geometry in
the TanStack definition, using mark options, scales, and color channels.

Give the root a class when a chart needs local overrides:

```tsx
<Chart className="sales-chart">...</Chart>
```

```css
.sales-chart {
  --moduix-chart-tooltip-bg: var(--moduix-color-card);
  --moduix-chart-tooltip-font-size: var(--moduix-text-sm);
  --moduix-chart-tooltip-line-height: var(--moduix-line-height-text-sm);
}
```

### CSS Properties

<CssPropertiesSection properties={ChartDocs.chartCssProperties} />

### Styling hooks

- `Chart.Root`, `Chart.Header`, `Chart.Title`, `Chart.Description`, `Chart.Legend`,
  and `Chart.LegendItem` expose `data-scope="chart"`, `data-part`, and `data-slot`.
- `className` on `Chart.Plot` reaches the outer `.ts-chart-host` element.
- The default tooltip uses `chart-tooltip-body`, `chart-tooltip-title`,
  `chart-tooltip-rows`, `chart-tooltip-row`, `chart-tooltip-swatch`, `chart-tooltip-label`, and
  `chart-tooltip-value` data slots.
- Use `--moduix-color-chart-1` through `--moduix-color-chart-5` in TanStack color scales.
- The tooltip shell is styled through the documented `--ts-chart-tooltip-*` variables mapped from
  Moduix properties. Use TanStack's `tooltip.className` for custom selectors, or
  `renderTooltipBody` when you need React content and the native `defaultBody`.
