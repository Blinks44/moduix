---
title: Chart
component: chart
description: Build accessible, responsive charts with TanStack Charts and moduix's figure, tooltip, legend, and motion defaults.
---

import * as ChartDocs from '@/components/examples/chart/css-properties';
import ChartAdvancedCustomization from '@/components/examples/chart/advanced-customization.tsx';
import ChartAnimatedArea from '@/components/examples/chart/animated-area.tsx';
import ChartBasic from '@/components/examples/chart/basic.tsx';
import ChartDonut from '@/components/examples/chart/donut.tsx';
import ChartHeatmap from '@/components/examples/chart/heatmap.tsx';
import ChartHorizontalBars from '@/components/examples/chart/horizontal-bars.tsx';
import ChartColors from '@/components/examples/chart/line-colors.tsx';
import ChartScatter from '@/components/examples/chart/scatter.tsx';
import ChartStackedBars from '@/components/examples/chart/stacked-bars.tsx';
import chartSolidAdvancedCustomization from '../../../snippets/chart/solid/advanced-customization.tsx?raw';
import chartSolidAnimatedArea from '../../../snippets/chart/solid/animated-area.tsx?raw';
import chartSolidBasic from '../../../snippets/chart/solid/basic.tsx?raw';
import chartSolidDonut from '../../../snippets/chart/solid/donut.tsx?raw';
import chartSolidHeatmap from '../../../snippets/chart/solid/heatmap.tsx?raw';
import chartSolidHorizontalBars from '../../../snippets/chart/solid/horizontal-bars.tsx?raw';
import chartSolidColors from '../../../snippets/chart/solid/line-colors.tsx?raw';
import chartSolidScatter from '../../../snippets/chart/solid/scatter.tsx?raw';
import chartSolidStackedBars from '../../../snippets/chart/solid/stacked-bars.tsx?raw';

## API Reference

Chart does not map to an Ark UI primitive. moduix adds a semantic `figure`, title, description,
legend, and compact tooltip body around the [TanStack Charts chart
API](https://tanstack.com/charts/latest/docs/framework/react/reference/chart). Define marks,
scales, interactions, and callbacks with TanStack Charts; moduix keeps the accessible composition
around them.

`@tanstack/charts` is an optional peer dependency of moduix. The React and Solid packages expose the
same chart definition API; the Solid package uses TanStack's renderer adapter.

## Basic


Create a TanStack definition and pass it to `ChartPlot`. Add a visible title, description, and
legend around the plot, then provide a concise `ariaLabel` for assistive technology.

<ExampleFrame variant="component">
  <ChartBasic />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/chart/basic.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={chartSolidBasic} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

## Install with shadcn

See [Quick Start](/docs/quick-start) for full package setup, registry configuration, and styling guidance.

<Tabs groupId="framework">
  <Tab label="React" value="react">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-react/chart' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-react-tailwind/chart' },
  ]}
/>

  </Tab>
  <Tab label="Solid" value="solid">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-solid/chart' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-solid-tailwind/chart' },
  ]}
/>

  </Tab>
</Tabs>

## Anatomy

```text
Chart
├─ ChartHeader (optional figcaption)
│  ├─ ChartTitle
│  └─ ChartDescription
├─ ChartPlot
│  └─ TanStack chart surface (SVG by default)
└─ ChartLegend (optional)
   └─ ChartLegendItem
```

| Part | Role |
| --- | --- |
| `Chart` | Composed `figure` root. Accepts native props, `asChild`, and an `HTMLElement` ref. |
| `ChartPlot` | TanStack Charts host. Requires `definition` and `ariaLabel`; uses the moduix motion preset by default and accepts `motion={false}`. |
| `ChartHeader` | `figcaption` layout for the title and description. |
| `ChartTitle` | Chart heading; renders `h3` by default. |
| `ChartDescription` | Supporting visible context. |
| `ChartLegend` | Semantic list of visible series. |
| `ChartLegendItem` | Legend item with an optional indicator `color`. |

## Composition

`Chart` is a presentational shell. TanStack's `definition` owns marks, scales, interactions, and
callbacks, so keep it at module scope or memoize the complete definition when it depends on props
or state. Compose `ChartHeader`, `ChartPlot`, and `ChartLegend` under the root; `ChartPlot`
continues to own rendering, focus, and tooltip state through TanStack.

Use `renderTooltipBody` only when you need to wrap or extend the native tooltip: render the supplied
`defaultBody` and add interactive controls only while `pinned` is true. `Chart` and
`ChartHeader` support `asChild` when the semantic wrapper or layout needs to be owned by your
application.

## Examples

### Animated area

Area, line, points, grouped focus, and tooltip motion all remain native TanStack behavior.

<ExampleFrame variant="component">
  <ChartAnimatedArea />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs groupId="framework" values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]} className="docs-tabs">
    <Tab value="react">

```tsx pure file="../../../src/components/examples/chart/animated-area.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={chartSolidAnimatedArea} />

    </Tab>
  </Tabs>
</div>
</ExampleCode>

### Chart colors

Use moduix chart tokens in the TanStack color scale. CSS custom properties work as SVG paint values
and automatically follow the active theme.

<ExampleFrame variant="component">
  <ChartColors />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs groupId="framework" values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]} className="docs-tabs">
    <Tab value="react">

```tsx pure file="../../../src/components/examples/chart/line-colors.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={chartSolidColors} />

    </Tab>
  </Tabs>
</div>
</ExampleCode>

### Donut chart

Compose TanStack's opt-in `pie`, `polar`, and `radialArc` primitives for part-to-whole data. The
same composition of `ChartPlot`, motion, focus, tooltip, and legend works outside Cartesian charts.

<ExampleFrame variant="component">
  <ChartDonut />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs groupId="framework" values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]} className="docs-tabs">
    <Tab value="react">

```tsx pure file="../../../src/components/examples/chart/donut.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={chartSolidDonut} />

    </Tab>
  </Tabs>
</div>
</ExampleCode>

### Heatmap

Use `cell` with band scales on both axes to build a compact categorical matrix. A color channel
turns each cell's level into a theme-aware paint value.

<ExampleFrame variant="component">
  <ChartHeatmap />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs groupId="framework" values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]} className="docs-tabs">
    <Tab value="react">

```tsx pure file="../../../src/components/examples/chart/heatmap.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={chartSolidHeatmap} />

    </Tab>
  </Tabs>
</div>
</ExampleCode>

### Horizontal bars

Use `barX` when category labels are long or the ranking should read from top to bottom. The numeric
value belongs on x and the category belongs on y.

<ExampleFrame variant="component">
  <ChartHorizontalBars />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs groupId="framework" values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]} className="docs-tabs">
    <Tab value="react">

```tsx pure file="../../../src/components/examples/chart/horizontal-bars.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={chartSolidHorizontalBars} />

    </Tab>
  </Tabs>
</div>
</ExampleCode>

### Scatter plot

Use `dot` to compare two quantitative values. Color can carry one additional categorical dimension
without changing the x and y relationship.

<ExampleFrame variant="component">
  <ChartScatter />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs groupId="framework" values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]} className="docs-tabs">
    <Tab value="react">

```tsx pure file="../../../src/components/examples/chart/scatter.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={chartSolidScatter} />

    </Tab>
  </Tabs>
</div>
</ExampleCode>

### Stacked bars

Use a TanStack color channel and the `stack()` layout for multi-series bars. `focus: 'group-x'`
keeps the tooltip and keyboard focus grouped by quarter.

<ExampleFrame variant="component">
  <ChartStackedBars />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs groupId="framework" values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]} className="docs-tabs">
    <Tab value="react">

```tsx pure file="../../../src/components/examples/chart/stacked-bars.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={chartSolidStackedBars} />

    </Tab>
  </Tabs>
</div>
</ExampleCode>

### Advanced Customization

The Ark factory parts accept `asChild`, so they can apply their styles to semantic elements in your
layout. This dashboard example combines Chart with Button, replaces the default `figure` and
`figcaption` with a labelled `section` and `header`, and places the legend in the header.

`ChartPlot` is still the TanStack host, not a slot. Use its `class` or `className` and `style` on
the outer element, and use `renderTooltipBody` to extend the tooltip. Render `defaultBody` inside
your own wrapper while TanStack continues to position and portal it. Add interactive controls only
to a pinned tooltip.

<ExampleFrame variant="component">
  <ChartAdvancedCustomization />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs groupId="framework" values={[{ label: 'React', value: 'react' }, { label: 'Solid', value: 'solid' }]} className="docs-tabs">
    <Tab value="react">

```tsx pure file="../../../src/components/examples/chart/advanced-customization.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={chartSolidAdvancedCustomization} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/chart/chart-advanced-customization.module.css"
```
</div>
</ExampleCode>

## Styling

Style the figure with `Chart` and the TanStack host with `ChartPlot`. Public CSS properties
control the surface, focus ring, tooltip shell, and default tooltip layout. Keep chart geometry in
the TanStack definition through mark options, scales, and color channels.

Tailwind variants use native utilities and consumer classes; the CSS properties below customize the
CSS Modules variants. Data attributes and `data-slot` hooks are shared by both styling tracks.

Give the root a local class (`className` in React or `class` in Solid) when a chart needs overrides:

```css
.sales-chart {
  --moduix-chart-tooltip-bg: var(--moduix-color-card);
  --moduix-chart-tooltip-font-size: var(--moduix-text-sm);
  --moduix-chart-tooltip-line-height: var(--moduix-line-height-text-sm);
}
```

### CSS Properties

<CssPropertiesSection properties={ChartDocs.chartCssProperties} />

### Styling hooks

- `Chart`, `ChartHeader`, `ChartTitle`, `ChartDescription`, `ChartLegend`, and
  `ChartLegendItem` expose `data-scope="chart"`, `data-part`, and `data-slot`.
- `className` in React and `class` in Solid on `ChartPlot` reach the outer `.ts-chart-host` element.
- The default tooltip uses `chart-tooltip-body`, `chart-tooltip-title`, `chart-tooltip-rows`,
  `chart-tooltip-row`, `chart-tooltip-swatch`, `chart-tooltip-label`, `chart-tooltip-value`, and
  `chart-tooltip-text` data slots.
- Use `--moduix-color-chart-1` through `--moduix-color-chart-5` in TanStack color scales.
- The tooltip shell is styled through the documented `--ts-chart-tooltip-*` variables mapped from
  moduix properties. Use TanStack's `tooltip.className` for custom selectors, or
  `renderTooltipBody` when you need custom content and the native `defaultBody`.
