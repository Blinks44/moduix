---
title: Date Input
component: date-input
description: A segmented date input built on Ark UI for single dates, ranges, locale-aware editing, and native form submission.
---

import * as DateInputDocs from '@/components/examples/date-input';

## API Reference

<PrimitiveReference href="https://ark-ui.com/docs/components/date-input" />

## Choosing the right component

- Choose **`Input`** for plain text values.
- Choose **`DateInput`** when users need keyboard-editable date segments and native form submission.
- Choose **`DatePicker`** when users need a calendar popup. `DateInput` is keyboard-first and does not
  include a calendar.

## Basic

Use `DateInput.Segments` to render date segments in the order defined by the current locale. Ark
handles segment order, keyboard editing, focus, placeholders, and form synchronization. moduix
provides the styled parts and removes the repeated segment-rendering code needed for the common case.

<style>{DateInputDocs.dateInputExampleCss}</style>

<PreviewFrame contentWidth="fit-content" maxWidth="sm">
  ```tsx preview file="./_snippets/date-input/basic.tsx"

```
</PreviewFrame>

## Install with shadcn

<ShadcnInstall packageName="date-input" />

## Anatomy

```text
DateInput.Root
├─ DateInput.Label
├─ DateInput.Control
│  └─ DateInput.Segments
└─ native input(s) (automatic)

Advanced customization:

DateInput.SegmentGroup
└─ DateInput.SegmentContext
   └─ DateInput.Segment

DateInput.RootProvider
└─ same part tree connected to a useDateInput() store
```

| Part                           | Role                                                               |
| ------------------------------ | ------------------------------------------------------------------ |
| `DateInput` / `DateInput.Root` | Controls the date value, locale formatting, segment focus, and validation. |
| `DateInput.RootProvider`       | Connects the parts to state created by `useDateInput()`.           |
| `DateInput.Label`              | Accessible label for the date input.                               |
| `DateInput.Control`            | Visual frame around one or more segment groups.                    |
| `DateInput.SegmentGroup`       | Groups segments for one date index.                                |
| `DateInput.Segment`            | Editable or fixed date segment returned by Ark.                    |
| `DateInput.Segments`           | Renders one locale-aware segment group for the common case.        |
| `DateInput.Separator`          | Presentational text used between range groups.                     |
| `DateInput.Context`            | Renderless access to the current date input state.                 |
| `DateInput.SegmentContext`     | Ark render helper for locale-aware segment rendering.              |

## Composition

### Recommended composition

Use the shorthand root form, `DateInput`. Render `DateInput.Segments` inside
`DateInput.Control` for a standard locale-aware segment group.

The root automatically renders native inputs for form submission and reset. For ranges, render
indexed `DateInput.Segments`. With a shared `name="date"`, the submitted names are `date[0]` and
`date[1]`. To use different base names, pass the two-item `names` tuple, for example
`names={['check-in', 'check-out']}`, which submits `check-in[0]` and `check-out[1]`.

### When to go custom

Use `DateInput.SegmentGroup`, `DateInput.SegmentContext`, and `DateInput.Segment` directly when
segment classes or rendering need to vary by segment.
Use `asChild` on Ark parts when you need a custom host element. Keep the child as one semantic element.
Use `ids` when an external label, helper text, or another component must share exact IDs.

### Default props

| Prop                              | Default        | Notes                                                 |
| --------------------------------- | -------------- | ----------------------------------------------------- |
| `DateInput` / `DateInput.Root`    | -              | No local behavioral defaults; Ark defaults apply.     |
| `DateInput.Segments`              | -              | Renders one locale-aware segment group for one index. |
| `DateInput.names`                 | -              | Two base names for range inputs; Ark appends indexes. |
| `DateInput.Separator.aria-hidden` | `true`         | Presentational text is hidden from assistive tech.    |
| `DateInput.Separator.role`        | `presentation` | Keeps range separators out of the accessibility tree. |

### Behavioral notes

- Values are `DateInputDateValue[]` objects from Ark and `@internationalized/date`, not strings.
- `onValueChange(details)` retains Ark's details object with `details.value` and `details.valueAsString`.
- `focused`, `defaultFocused`, and `onFocusChange(details)` control focus on the root.
- `placeholderValue`, `defaultPlaceholderValue`, and `onPlaceholderChange(details)` control the placeholder date.
- `selectionMode="range"` enables indexed segment groups and automatic native inputs.
- `shouldForceLeadingZeros` controls whether numeric segments are padded.
- `min`, `max`, and `isDateUnavailable` validate entered dates.
- `locale`, `timeZone`, `hourCycle`, `granularity`, `formatter`, `format`, `translations`, `dir`, and
  `createCalendar` are passed through to Ark. Use a formatter configured for time only with
  `granularity="minute"` or `"second"` when dates are not needed.
- Import `useDateInput()`, `useDateInputContext()`, and `DateInputDateValue` from `@moduix/react`.
  Use `DateInput.Context` or `useDateInputContext()` to read state from within the component tree, and
  use `DateInput.SegmentContext` for segments. Direct Ark imports are for lower-level types and helpers
  that moduix does not export.

## Examples

### Controlled

Control the selected date array by updating it with `details.value`.

<style>{DateInputDocs.dateInputExampleCss}</style>

<PreviewFrame contentWidth="fit-content" maxWidth="sm">
  ```tsx preview file="./_snippets/date-input/controlled.tsx"

```

</PreviewFrame>

### Disabled and Read Only

Use `disabled` to prevent interaction. Use `readOnly` when the value should remain focusable but not
editable.

<style>{DateInputDocs.dateInputExampleCss}</style>

<PreviewFrame contentWidth="fit-content" maxWidth="sm">
  ```tsx preview file="./_snippets/date-input/disabled-and-read-only.tsx"

```

</PreviewFrame>

### Field State

Wrap `DateInput` in `Field` to provide surrounding form text. Pass `required`, `disabled`, or `invalid`
directly to `DateInput` when Ark state attributes should appear on the date input itself.

<style>{DateInputDocs.dateInputExampleCss}</style>

<PreviewFrame contentWidth="fit-content" maxWidth="sm">
  ```tsx preview file="./_snippets/date-input/field-state.tsx"

```

</PreviewFrame>

### Granularity

Set `granularity` to include time segments in the input.

<style>{DateInputDocs.dateInputExampleCss}</style>

<PreviewFrame contentWidth="fit-content" maxWidth="sm">
  ```tsx preview file="./_snippets/date-input/granularity.tsx"

```

</PreviewFrame>

### Leading Zeros

Set `shouldForceLeadingZeros={false}` when numeric segments should not include leading zeros.

<style>{DateInputDocs.dateInputExampleCss}</style>

<PreviewFrame contentWidth="fit-content" maxWidth="sm">
  ```tsx preview file="./_snippets/date-input/leading-zeros.tsx"

```

</PreviewFrame>

### Locale

Set `locale`, `timeZone`, `hourCycle`, or `granularity` on the root when segment order and labels
should follow regional conventions.

<style>{DateInputDocs.dateInputExampleCss}</style>

<PreviewFrame contentWidth="fit-content" maxWidth="sm">
  ```tsx preview file="./_snippets/date-input/locale.tsx"

```

</PreviewFrame>

### Min, Max, and Unavailable Dates

Use `min`, `max`, and `isDateUnavailable` to mark entered dates outside business rules as invalid.

<style>{DateInputDocs.dateInputExampleCss}</style>

<PreviewFrame contentWidth="fit-content" maxWidth="sm">
  ```tsx preview file="./_snippets/date-input/min-max-and-unavailable-dates.tsx"

```

</PreviewFrame>

### Range

Use `selectionMode="range"` and pass `index` to each segment group. moduix automatically renders
the indexed native inputs.

<style>{DateInputDocs.dateInputExampleCss}</style>

<PreviewFrame contentWidth="fit-content" maxWidth="sm">
  ```tsx preview file="./_snippets/date-input/range.tsx"

```

</PreviewFrame>

### Right to Left

Set `dir="rtl"` with an RTL locale to keep the locale's segment order and direction.

<style>{DateInputDocs.dateInputExampleCss}</style>

<PreviewFrame contentWidth="fit-content" maxWidth="sm">
  ```tsx preview file="./_snippets/date-input/right-to-left.tsx"

```

</PreviewFrame>

### Root Provider

Use `useDateInput()` from `@moduix/react` with `DateInput.RootProvider` when state actions need to
be available outside the rendered root tree.

<style>{DateInputDocs.dateInputExampleCss}</style>

<PreviewFrame contentWidth="fit-content" maxWidth="sm">
  ```tsx preview file="./_snippets/date-input/root-provider.tsx"

```

</PreviewFrame>

### Time Only

Use a formatter configured for time only with minute or second granularity when the date is not part
of the value users edit.

<style>{DateInputDocs.dateInputExampleCss}</style>

<PreviewFrame contentWidth="fit-content" maxWidth="sm">
  ```tsx preview file="./_snippets/date-input/time-only.tsx"

```

</PreviewFrame>

### Advanced Customization

Use the full segment composition when rendering needs to vary based on Ark segment data.
`DateInput.Segments` remains the default path for standard rendering.

<style>{DateInputDocs.dateInputExampleCss}</style>

<PreviewFrame contentWidth="fit-content" maxWidth="sm">
  ```tsx preview file="./_snippets/date-input/advanced-customization.tsx"

```

</PreviewFrame>

## Styling

For most styling changes, start with `--moduix-date-input-width`, `--moduix-date-input-control-width`,
`--moduix-date-input-border-color`, `--moduix-date-input-radius`, `--moduix-date-input-focus-ring-color`, and
`--moduix-date-input-segment-bg-focus`. The full public variable contract is listed below.

### CSS Properties

<div className="docs-reference">
  <Tabs
    values={[{ label: 'CSS Variables', value: 'CSS Variables' }]}
    className="docs-reference-tabs"
    contentItemClassName="docs-reference-panel"
  >
    <Tab>
      <DateInputDocs.DateInputCssPropertiesPanel />
    </Tab>
  </Tabs>
</div>

### Styling hooks

- `data-slot="date-input-root"` on the root.
- `data-slot="date-input-label"` on the label.
- `data-slot="date-input-control"` on the visual frame.
- `data-slot="date-input-segment-group"` on each segment group.
- `data-slot="date-input-segment"` on editable and fixed segments.
- `data-slot="date-input-hidden-input"` on the hidden native input.
- `data-slot="date-input-separator"` on moduix range separator sugar.
- Ark state attributes include `data-disabled`, `data-readonly`, `data-invalid`, `data-focus` on
  control and segment groups, `data-type`, `data-editable`, and `data-placeholder-shown`.
