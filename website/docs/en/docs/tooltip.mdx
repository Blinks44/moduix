---
title: Tooltip
component: tooltip
description: A short, supplementary label that appears when its trigger is hovered or focused.
---

import * as TooltipDocs from '@/components/examples/tooltip/css-properties';
import TooltipAdvancedCustomization from '@/components/examples/tooltip/advanced-customization.tsx';
import TooltipBasic from '@/components/examples/tooltip/basic.tsx';
import TooltipContext from '@/components/examples/tooltip/context.tsx';
import TooltipControlled from '@/components/examples/tooltip/controlled.tsx';
import TooltipDelay from '@/components/examples/tooltip/delay.tsx';
import TooltipDisabledTrigger from '@/components/examples/tooltip/disabled-trigger.tsx';
import TooltipMultipleTriggers from '@/components/examples/tooltip/multiple-triggers.tsx';
import TooltipPositioning from '@/components/examples/tooltip/positioning.tsx';
import TooltipRootProvider from '@/components/examples/tooltip/root-provider.tsx';
import TooltipWithArrow from '@/components/examples/tooltip/with-arrow.tsx';
import TooltipWithinFixedContainers from '@/components/examples/tooltip/within-fixed-containers.tsx';
import tooltipSolidAdvancedCustomization from '../../../snippets/tooltip/solid/advanced-customization.tsx?raw';
import tooltipSolidBasic from '../../../snippets/tooltip/solid/basic.tsx?raw';
import tooltipSolidContext from '../../../snippets/tooltip/solid/context.tsx?raw';
import tooltipSolidControlled from '../../../snippets/tooltip/solid/controlled.tsx?raw';
import tooltipSolidDelay from '../../../snippets/tooltip/solid/delay.tsx?raw';
import tooltipSolidDisabledTrigger from '../../../snippets/tooltip/solid/disabled-trigger.tsx?raw';
import tooltipSolidMultipleTriggers from '../../../snippets/tooltip/solid/multiple-triggers.tsx?raw';
import tooltipSolidPositioning from '../../../snippets/tooltip/solid/positioning.tsx?raw';
import tooltipSolidRootProvider from '../../../snippets/tooltip/solid/root-provider.tsx?raw';
import tooltipSolidWithArrow from '../../../snippets/tooltip/solid/with-arrow.tsx?raw';
import tooltipSolidWithinFixedContainers from '../../../snippets/tooltip/solid/within-fixed-containers.tsx?raw';

## API Reference

<PrimitiveReference href="https://ark-ui.com/docs/components/tooltip" />

## Choosing the right component

Use `Tooltip` for short, non-essential information that appears on hover or focus.

- Choose **`Tooltip`** for brief labels and contextual hints without interaction.
- Choose **`Popover`** when the popup needs richer content, actions, or form controls.
- Choose **`HoverCard`** when the popup should show a supplementary preview tied to its trigger.

## Basic


Use `Tooltip.Trigger` for the element that opens the tooltip and `Tooltip.Body` for its content.
`Tooltip.Body` combines `Tooltip.Positioner` and `Tooltip.Content`. Give the trigger an accessible
name of its own. Tooltip content is supplemental and does not replace visible text or an
`aria-label`.

<ExampleFrame variant="component">
  <TooltipBasic />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/tooltip/basic.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={tooltipSolidBasic} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

## Install with shadcn

See [Quick Start](/docs/quick-start) for full package setup, registry configuration, and styling guidance.

<Tabs groupId="framework">
  <Tab label="React" value="react">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-react/tooltip' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-react-tailwind/tooltip' },
  ]}
/>

  </Tab>
  <Tab label="Solid" value="solid">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-solid/tooltip' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-solid-tailwind/tooltip' },
  ]}
/>

  </Tab>
</Tabs>

## Anatomy

```text
Tooltip / Tooltip.Root
├─ Tooltip.Trigger
└─ Overlay subtree (automatically rendered in a portal)
   └─ Tooltip.Positioner
      └─ Tooltip.Content
         └─ content
```

Add `Tooltip.Arrow` as the first child of the content when the popup should point to its trigger.
`Tooltip.Arrow` renders `Tooltip.ArrowTip` by default.

```text
Tooltip / Tooltip.Root
├─ Tooltip.Trigger
└─ Overlay subtree (automatically rendered in a portal)
   └─ Tooltip.Positioner
      └─ Tooltip.Content
         ├─ Tooltip.Arrow
         │  └─ Tooltip.ArrowTip
         └─ content
```

| Part | Role |
| --- | --- |
| `Tooltip` / `Tooltip.Root` | Owns tooltip state, opening and closing, delays, positioning, and lifecycle. |
| `Tooltip.RootProvider` | Renders a tooltip from a `useTooltip()` state object created outside the tree. |
| `Tooltip.Context` | Exposes current tooltip state through a render prop for custom descendants. |
| `Tooltip.Body` | Convenience wrapper that renders `Tooltip.Positioner` and `Tooltip.Content` together. |
| `Tooltip.Trigger` | Interactive element that opens the tooltip on hover or focus. |
| `Tooltip.DisabledTrigger` | Focusable wrapper for one disabled native control that still needs an explanation. |
| `Tooltip.Positioner` | Positions the content and exposes Ark positioning variables. |
| `Tooltip.Content` | Visible tooltip surface. |
| `Tooltip.Arrow` | Optional positioned arrow wrapper that renders `Tooltip.ArrowTip` by default. |
| `Tooltip.ArrowTip` | Arrow tip element. |

## Composition

Use `Tooltip.Body` for the standard `Trigger` plus content composition. It combines
`Tooltip.Positioner` and `Tooltip.Content`; use those parts explicitly when they need independent
styling or structure. The root owns state, so use `defaultOpen` for an uncontrolled tooltip or
`open` with `onOpenChange(details)` when another part of the application owns it. Use
`Tooltip.RootProvider` with `useTooltip()` when the store is created outside the rendered tree, and
use `Tooltip.Context` or `useTooltipContext()` when a descendant needs that state. `asChild` requires
one focusable child; Solid uses a render function. `Tooltip.DisabledTrigger` is for one disabled
native control. Portalling is enabled by default, so set `portalled={false}` or pass `portalRef`
only when the tooltip must stay within a specific DOM boundary.

## Examples

### Context

Use `useTooltipContext()` inside a descendant that needs the current tooltip state.

<ExampleFrame variant="component">
  <TooltipContext />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/tooltip/context.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={tooltipSolidContext} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Controlled

Control whether the tooltip is open with `open` and `onOpenChange(details)`.

<ExampleFrame variant="component">
  <TooltipControlled />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/tooltip/controlled.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={tooltipSolidControlled} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Delay

Set `openDelay` and `closeDelay` on the root to adjust hover and focus timing.

<ExampleFrame variant="component">
  <TooltipDelay />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/tooltip/delay.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={tooltipSolidDelay} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Disabled Trigger

Use `Tooltip.DisabledTrigger` when a disabled native control still needs a short explanation. It
keeps a focusable wrapper around the disabled child without changing its disabled state.

<ExampleFrame variant="component">
  <TooltipDisabledTrigger />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/tooltip/disabled-trigger.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={tooltipSolidDisabledTrigger} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Multiple Triggers

Give each trigger a `value` and use `onTriggerValueChange(details)` to share one tooltip across
several triggers.

<ExampleFrame variant="component">
  <TooltipMultipleTriggers />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/tooltip/multiple-triggers.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={tooltipSolidMultipleTriggers} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/tooltip/component-multiple-triggers.module.css"
```
</div>

</ExampleCode>

### Positioning

Pass Ark positioning options to `Tooltip`, including placement and offset values.

<ExampleFrame variant="component">
  <TooltipPositioning />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/tooltip/positioning.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={tooltipSolidPositioning} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/tooltip/component-positioning.module.css"
```
</div>

</ExampleCode>

### Root Provider

Use `useTooltip()` with `Tooltip.RootProvider` when the tooltip state is created outside the
rendered tooltip tree.

<ExampleFrame variant="component">
  <TooltipRootProvider />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/tooltip/root-provider.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={tooltipSolidRootProvider} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### With Arrow

Add `Tooltip.Arrow` as the first child of the tooltip content when it needs a visual pointer. With
`Tooltip.Body`, place it directly inside `Tooltip.Body`; the wrapper supplies the default
`Tooltip.ArrowTip`.

<ExampleFrame variant="component">
  <TooltipWithArrow />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/tooltip/with-arrow.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={tooltipSolidWithArrow} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Within Fixed Containers

Use `positioning.strategy: "fixed"` when the trigger is inside a fixed or sticky interface region.
This example sets `closeOnScroll={false}` so you can focus the trigger and scroll the panel while
checking the tooltip's alignment.

<ExampleFrame variant="component">
  <TooltipWithinFixedContainers />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/tooltip/within-fixed-containers.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={tooltipSolidWithinFixedContainers} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/tooltip/component-within-fixed-containers.module.css"
```
</div>

</ExampleCode>

### Advanced Customization

Use the explicit `Tooltip.Positioner` and `Tooltip.Content` parts when they need independent
composition or styling.

<ExampleFrame variant="component">
  <TooltipAdvancedCustomization />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/tooltip/advanced-customization.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={tooltipSolidAdvancedCustomization} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

## Styling

Use `className` in React or `class` in Solid on exported parts for structural styles. For state-based
styles, prefer Ark data attributes such as `data-state`, `data-placement`, and `data-side`, together
with moduix `data-slot` selectors. Tailwind variants use native utilities and consumer classes; the
CSS variables below customize the CSS Modules variants.

### CSS Properties

<div className="not-prose">
  <Tabs
    values={[{ label: 'CSS Variables', value: 'CSS Variables' }]}
    className="docs-reference-tabs"
    contentItemClassName="docs-reference-panel"
  >
    <Tab>
      <TooltipDocs.TooltipCssPropertiesPanel />
    </Tab>
  </Tabs>
</div>

### Styling hooks

- `Tooltip.Body` does not create a DOM node. When using the explicit composition, style
  `Tooltip.Positioner` and `Tooltip.Content` separately.
- `Tooltip.Trigger` controls trigger chrome, hover and open feedback, and focus styling.
- `Tooltip.DisabledTrigger` controls the disabled-control wrapper and its direct disabled child.
- `Tooltip.Positioner` controls floating layout constraints.
- `Tooltip.Content` controls the popup surface.
- `Tooltip.Arrow` and `Tooltip.ArrowTip` control the arrow size, background, and stroke.

The main public hooks are `data-slot="tooltip-trigger"`, `data-slot="tooltip-positioner"`,
`data-slot="tooltip-content"`, `data-slot="tooltip-disabled-trigger"`,
`data-slot="tooltip-arrow"`, and `data-slot="tooltip-arrow-tip"`.

Ark also adds `data-scope="tooltip"`, `data-part`, `data-state="open" | "closed"`, `data-value`,
`data-current`, `data-expanded`, `data-instant`, `data-placement`, and `data-side` to the relevant
parts. The positioner exposes runtime CSS variables including `--available-width`,
`--available-height`, `--reference-width`, `--reference-height`, `--x`, `--y`, `--z-index`, and
`--transform-origin`.
