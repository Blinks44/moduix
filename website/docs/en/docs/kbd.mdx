---
title: Kbd
component: kbd
description: A compact component for keyboard shortcuts, command hints, and inline key labels.
---

import * as KbdDocs from '@/components/examples/kbd/css-properties';
import KbdBasic from '@/components/examples/kbd/basic.tsx';
import KbdCompactDensity from '@/components/examples/kbd/compact-density.tsx';
import KbdCustomElement from '@/components/examples/kbd/custom-element.tsx';
import KbdShortcutList from '@/components/examples/kbd/shortcut-list.tsx';
import KbdSingleKey from '@/components/examples/kbd/single-key.tsx';
import kbdSolidBasic from '../../../snippets/kbd/solid/basic.tsx?raw';
import kbdSolidCompactDensity from '../../../snippets/kbd/solid/compact-density.tsx?raw';
import kbdSolidCustomElement from '../../../snippets/kbd/solid/custom-element.tsx?raw';
import kbdSolidShortcutList from '../../../snippets/kbd/solid/shortcut-list.tsx?raw';
import kbdSolidSingleKey from '../../../snippets/kbd/solid/single-key.tsx?raw';

## API Reference

Ark UI does not ship a dedicated primitive for displaying keyboard keys. moduix builds `Kbd` with the official
[Ark factory](https://ark-ui.com/docs/guides/composition#the-ark-factory), so it follows Ark's familiar composition,
styling, and ref patterns. See the Ark guides for [composition](https://ark-ui.com/docs/guides/composition),
[styling](https://ark-ui.com/docs/guides/styling), and [refs](https://ark-ui.com/docs/guides/ref) when you need to
customize the rendered elements.

## Basic


Use `Kbd` to show one key. For a shortcut made of several keys, place the keycaps and their text separators inside
`KbdGroup`.

<ExampleFrame variant="component">
  <KbdBasic />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/kbd/basic.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={kbdSolidBasic} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/kbd/kbd-basic.module.css"
```
</div>

</ExampleCode>

## Install with shadcn

See [Quick Start](/docs/quick-start) for full package setup, registry configuration, and styling guidance.

<Tabs groupId="framework">
  <Tab label="React" value="react">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-react/kbd' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-react-tailwind/kbd' },
  ]}
/>

  </Tab>
  <Tab label="Solid" value="solid">

<ShadcnInstallOptions
  options={[
    { label: 'CSS Modules', command: 'shadcn@latest add @moduix-solid/kbd' },
    { label: 'Tailwind CSS', command: 'shadcn@latest add @moduix-solid-tailwind/kbd' },
  ]}
/>

  </Tab>
</Tabs>

## Anatomy

The structure of a shortcut is simple. The `+` in this example is ordinary text, not a component part.

```text
KbdGroup
├─ Kbd
├─ +
└─ Kbd
```

| Part               | Role                                                                          |
| ------------------ | ----------------------------------------------------------------------------- |
| `Kbd`              | Root keycap; renders a `<kbd>` element by default.                            |
| `KbdGroup`         | Inline wrapper for related keycaps; uses the `group` role by default.         |

## Composition

Use `Kbd` when you need an individual key, and use `KbdGroup` to combine related keycaps. Keep separators such as
`+` as plain text inside the group, and give the group an `aria-label` when visual
symbols or abbreviations need a clearer accessible name.

Kbd is static and owns no state or keyboard interaction. Use `asChild` only when another component must render the
semantic `<kbd>` element, and pass exactly one child.

## Examples

### Compact Density

Use local CSS variables when one part of your interface needs more compact keycaps, without adding component props.

<ExampleFrame variant="component">
  <KbdCompactDensity />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/kbd/compact-density.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={kbdSolidCompactDensity} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/kbd/kbd-compact-density.module.css"
```
</div>

</ExampleCode>

### Custom Element

Use `asChild` when another component renders the semantic `<kbd>` element and you need to apply Kbd's styles to it.

<ExampleFrame variant="component">
  <KbdCustomElement />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/kbd/custom-element.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={kbdSolidCustomElement} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

### Shortcut List

Combine `KbdGroup` with explanatory text to present a list of available shortcuts.

<ExampleFrame variant="component">
  <KbdShortcutList />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/kbd/shortcut-list.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={kbdSolidShortcutList} />

    </Tab>
  </Tabs>

**Styles**

```css file="../../../src/components/examples/kbd/kbd-shortcut-list.module.css"
```
</div>

</ExampleCode>

### Single Key

Use `Kbd` directly when you need to show one key, such as `Esc`.

<ExampleFrame variant="component">
  <KbdSingleKey />
</ExampleFrame>

<ExampleCode>
<div className="docs-reference">
  <Tabs
    groupId="framework"
    values={[
      { label: 'React', value: 'react' },
      { label: 'Solid', value: 'solid' },
    ]}
    className="docs-tabs"
  >
    <Tab value="react">

```tsx pure file="../../../src/components/examples/kbd/single-key.tsx"
```

    </Tab>
    <Tab value="solid">

<CodeBlockRuntime lang="tsx" code={kbdSolidSingleKey} />

    </Tab>
  </Tabs>
</div>

</ExampleCode>

## Styling

Customize `Kbd` and `KbdGroup` with `className` in React or `class` in Solid, Ark anatomy attributes, stable
`data-slot` hooks, and the public `--moduix-kbd-*` CSS variables listed below. Tailwind variants use native utility
classes, while CSS Modules variants use the component variables and consumer CSS.

### CSS Properties

<CssPropertiesSection properties={KbdDocs.kbdOverrideCssProperties} />

### Styling hooks

- `data-slot="kbd-root"` targets each keycap element
- `data-slot="kbd-group"` targets the inline wrapper for grouped shortcuts
- `[data-scope="kbd"][data-part="root"]` and `[data-part="group"]` expose Ark-style anatomy selectors
- Separators in `KbdGroup` inherit the group's text color. Use `--moduix-kbd-group-separator-color` to set it
