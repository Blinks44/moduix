---
title: Lightbox
component: lightbox
description: Dialog-based image previews built on Ark UI.
---

import * as LightboxDocs from '@/components/examples/lightbox/css-properties';
import LightboxAdvancedCustomization from '@/components/examples/lightbox/advanced-customization.tsx';
import LightboxBasic from '@/components/examples/lightbox/basic.tsx';
import LightboxBindCmsContent from '@/components/examples/lightbox/bind-cms-content.tsx';
import LightboxClickToCloseImage from '@/components/examples/lightbox/click-to-close-image.tsx';
import LightboxControlled from '@/components/examples/lightbox/controlled.tsx';
import LightboxFocusAndIds from '@/components/examples/lightbox/focus-and-ids.tsx';
import LightboxGallery from '@/components/examples/lightbox/gallery.tsx';
import LightboxMultipleTriggers from '@/components/examples/lightbox/multiple-triggers.tsx';
import LightboxNonModal from '@/components/examples/lightbox/non-modal.tsx';
import LightboxRootProviderAndContext from '@/components/examples/lightbox/root-provider-and-context.tsx';

## API Reference

<PrimitiveReference href="https://ark-ui.com/docs/components/dialog" />

Ark UI does not provide a dedicated `lightbox` primitive. moduix builds this component on top of
`Dialog`, follows Ark's guidance for [composition](https://ark-ui.com/docs/guides/composition),
[styling](https://ark-ui.com/docs/guides/styling), and
[refs](https://ark-ui.com/docs/guides/ref), and uses `Dialog` as the source of truth for behavior.

## Choosing the right component

Use `Lightbox` when you primarily need to view images or other media in a modal window.

- Choose **`Lightbox`** for zoomed-in image previews, product galleries, and other media-focused views.
- Choose **`Dialog`** for general application UI such as forms, actions, or mixed content.
- Compose **`Carousel` inside `Lightbox.Gallery`** when the preview needs slides or navigation arrows.

## Basic

Build the explicit Ark UI dialog structure with `Lightbox`, `Lightbox.Trigger`,
`Lightbox.Backdrop`, `Lightbox.Positioner`, and `Lightbox.Content`. Use `asChild` with a single
semantic trigger element.

<ExampleFrame variant="component">
  <LightboxBasic />
</ExampleFrame>

<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx file="../../../src/components/examples/lightbox/basic.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/lightbox/lightbox-basic.module.css"
```

    </Tab>
  </Tabs>
</div>

## Install with shadcn

<ShadcnInstall packageName="lightbox" />

## Anatomy

```text
Lightbox / Lightbox.Root
├─ Lightbox.Trigger
├─ Lightbox.Bind
└─ Overlay subtree (automatically portalled)
   ├─ Lightbox.Backdrop
   └─ Lightbox.Positioner
      ├─ Lightbox.CloseTrigger or Lightbox.CloseIcon
      └─ Lightbox.Content
         ├─ Lightbox.Header (optional)
         │  ├─ Lightbox.Title
         │  └─ Lightbox.Description
         ├─ Lightbox.Body
         │  └─ Lightbox.Image
         └─ Lightbox.Footer (optional)

Lightbox.RootProvider
└─ the same part tree connected to useLightbox()
```

| Part                         | Role                                                                           |
| ---------------------------- | ------------------------------------------------------------------------------ |
| `Lightbox` / `Lightbox.Root` | Ark Dialog root that manages open state, focus lifecycle, dismissal, and accessibility. |
| `Lightbox.RootProvider`      | Provides state created with `useLightbox()`.                                   |
| `Lightbox.Trigger`           | Ark trigger. Use `asChild` for custom semantic buttons or links.               |
| `Lightbox.Backdrop`          | Modal backdrop with Ark `data-state` and layer variables.                      |
| `Lightbox.Positioner`        | Fixed, centered overlay container.                                             |
| `Lightbox.Content`           | The actual Ark dialog content node.                                            |
| `Lightbox.Title`             | Optional accessible title.                                                     |
| `Lightbox.Description`       | Optional accessible description.                                               |
| `Lightbox.CloseTrigger`      | Low-level Ark close trigger.                                                   |
| `Lightbox.CloseIcon`         | Convenience wrapper that combines `CloseButton.Root` with the Ark close trigger. |
| `Lightbox.Header` / `Body`   | Optional layout helpers for text and media regions inside the content.         |
| `Lightbox.Footer`            | Optional layout helper for actions or status below the media.                  |
| `Lightbox.Image`             | Styled native image that forwards an `HTMLImageElement` ref.                   |
| `Lightbox.Gallery`           | Layout and styling boundary for a composed `Carousel`.                         |
| `Lightbox.Bind`              | Renderless behavior part that connects external image markup to the root.       |

## Composition

By default, the root renders `Backdrop` and `Positioner` through a portal. Use `portalled={false}`
to render them in place, or pass `portalRef` to target a custom container. The structural parts
remain explicit and can be styled independently.

### Recommended composition

Keep the modal structure explicit. `Lightbox.Content` is the actual Ark content part. It does not
render a backdrop, positioner, image, gallery, or close button for you.

Use `Lightbox.Image` for a single image. It renders a native `<img>`, accepts native image props,
and forwards its ref to the `HTMLImageElement`. `Lightbox.Header`, `Body`, and `Footer` are optional
layout helpers that keep text, media, and actions visually separated.

Wrap `Carousel` in `Lightbox.Gallery` for a known image collection. `Gallery` provides the
lightbox-specific viewport, arrow alignment, media sizing, and indicator layout while `Carousel`
continues to manage all slide behavior.

Render `Lightbox.CloseIcon` as a sibling of `Lightbox.Content` inside `Lightbox.Positioner` when
you want it pinned to the viewport corner. Rendering it inside `Lightbox.Content` makes it inherit
content transforms during entry and exit motion.

### When to go custom

Render custom content directly inside `Lightbox.Content` when it is not a single media frame or a
`Carousel` gallery. Use `Lightbox.Bind` only when the source markup cannot render
`Lightbox.Trigger`. For a structured image array, use explicit triggers and compose `Carousel`
inside `Lightbox.Gallery`.

### Default props

By default, portalled content mounts lazily and unmounts after its exit animation. Set
`unmountOnExit={false}` to keep it mounted after the first open. Set both lifecycle props to `false`
only when the content must render eagerly on the initial page load.

| Prop                     | Default    | Notes                                                               |
| ------------------------ | ---------- | ------------------------------------------------------------------- |
| `closeOnEscape`          | `true`     | Inherited from Ark Dialog.                                          |
| `closeOnInteractOutside` | `true`     | Inherited from Ark Dialog.                                          |
| `defaultOpen`            | `false`    | Uncontrolled initial state.                                         |
| `modal`                  | `true`     | Prevents outside interaction and hides content below the lightbox.  |
| `preventScroll`          | `true`     | Locks page scroll while the lightbox is open.                       |
| `role`                   | `"dialog"` | Use `aria-label` or `Lightbox.Title` for an accessible name; `ids` only makes those relationships stable. |
| `trapFocus`              | `true`     | Keeps keyboard focus inside the lightbox while it is open.          |
| `lazyMount`              | `true`     | Defers heavy media until the first open.                             |
| `unmountOnExit`          | `true`     | Removes overlay nodes after the exit animation.                     |

### Behavioral notes

`onOpenChange` and `onTriggerValueChange` use Ark's detail-object callback signature. Read
`details.open` for controlled open state and `details.value` for galleries with multiple triggers.

Use `useLightbox()` with `Lightbox.RootProvider` when controls live outside the rendered root.
Read dialog state in child components with `useLightboxContext()`. Both APIs are available from
`@moduix/react` and on the `Lightbox` namespace.

`Lightbox.Image closeOnClick` is the supported shortcut for dismissing the overlay when the image is
clicked. `Lightbox.Bind` connects CMS or third-party markup that cannot be wrapped in
`Lightbox.Trigger`. Render it inside `Lightbox`; it reports the selected image through
`onImageSelect(details)`. The details contain `src`, optional `alt`, and the source
`HTMLImageElement` as `element`. `selector` is required and should match a semantic button or link
so every preview remains keyboard-accessible.

Other inherited Dialog props pass through unchanged, including `id`, `ids`, `initialFocusEl`,
`finalFocusEl`, `persistentElements`, `restoreFocus`, `present`, `immediate`,
`skipAnimationOnMount`, and outside-interaction callbacks.

## Examples

### Bind CMS Content

Use `Lightbox.Bind` when preview images come from a CMS or third-party markup that you cannot render
directly with `Lightbox.Trigger`. Point it at a container with `rootRef` or `rootSelector`, choose a
`selector`, and handle the resolved `{ src, alt, element }` through `onImageSelect`. `Bind` opens its
parent Lightbox but renders no overlay structure, so the backdrop, content, close controls, and image
layout remain yours to compose. It reads the source from `data-lightbox-src`, then `currentSrc`, then
`src`, and preloads it on pointer hover or keyboard focus. Use a semantic button or link for the
matched `selector` so native keyboard activation produces the click that opens the Lightbox. If your
server already returns a structured image array, prefer the `Lightbox.Gallery + Carousel` composition
above.

<ExampleFrame variant="component">
  <LightboxBindCmsContent />
</ExampleFrame>

<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx file="../../../src/components/examples/lightbox/bind-cms-content.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/lightbox/lightbox-bind-cms-content.module.css"
```

    </Tab>
  </Tabs>
</div>

### Click to Close Image

Use `Lightbox.Image closeOnClick` when clicking the previewed image should dismiss the overlay. The
close icon remains available and keeps the same Ark close semantics.

<ExampleFrame variant="component">
  <LightboxClickToCloseImage />
</ExampleFrame>

<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx file="../../../src/components/examples/lightbox/click-to-close-image.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/lightbox/lightbox-click-to-close-image.module.css"
```

    </Tab>
  </Tabs>
</div>

### Controlled

Use Ark's `onOpenChange(details)` callback when synchronizing lightbox state with React.

<ExampleFrame variant="component">
  <LightboxControlled />
</ExampleFrame>

<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx file="../../../src/components/examples/lightbox/controlled.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/lightbox/lightbox-controlled.module.css"
```

    </Tab>
  </Tabs>
</div>

### Focus and IDs

Use Ark's `initialFocusEl`, `finalFocusEl`, and `ids` when a lightbox needs explicit focus targets
or stable element IDs for external accessibility relationships. The return target is highlighted
after close so the focus handoff is visible.

<ExampleFrame variant="component">
  <LightboxFocusAndIds />
</ExampleFrame>

<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx file="../../../src/components/examples/lightbox/focus-and-ids.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/lightbox/lightbox-focus-and-ids.module.css"
```

    </Tab>
  </Tabs>
</div>

### Gallery

When a CMS or API returns a structured array of images, keep the selected image and Carousel page in
React state, then compose `Carousel` inside `Lightbox.Gallery`. Use `Lightbox.Trigger.value` to choose
the initial slide and keep the Carousel page in sync as the user navigates.

<ExampleFrame variant="component">
  <LightboxGallery />
</ExampleFrame>

<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx file="../../../src/components/examples/lightbox/gallery.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/lightbox/lightbox-gallery.module.css"
```

    </Tab>
  </Tabs>
</div>

### Multiple Triggers

Use `Lightbox.Trigger.value` and `onTriggerValueChange(details)` for gallery thumbnails. The
component does not maintain a hidden image registry.

<ExampleFrame variant="component">
  <LightboxMultipleTriggers />
</ExampleFrame>

<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx file="../../../src/components/examples/lightbox/multiple-triggers.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/lightbox/lightbox-multiple-triggers.module.css"
```

    </Tab>
  </Tabs>
</div>

### Non-modal

Set `modal={false}` for rare previews that should leave the page interactive. Ark disables focus
trapping and scroll prevention in this mode. Omit `Backdrop` so background actions remain available
while the image is open.

<ExampleFrame variant="component">
  <LightboxNonModal />
</ExampleFrame>

<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx file="../../../src/components/examples/lightbox/non-modal.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/lightbox/lightbox-non-modal.module.css"
```

    </Tab>
  </Tabs>
</div>

### Root Provider

Create state with `useLightbox()` when the opener is outside the root. Read the state inside a child
component with `useLightboxContext()`. The external opener is a regular moduix `Button`.

<ExampleFrame variant="component">
  <LightboxRootProviderAndContext />
</ExampleFrame>

<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx file="../../../src/components/examples/lightbox/root-provider-and-context.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/lightbox/lightbox-root-provider-and-context.module.css"
```

    </Tab>
  </Tabs>
</div>

### Advanced Customization

Skip the optional layout helpers when the media surface needs custom slot classes and token
overrides. `Backdrop`, `Positioner`, and `Content` remain explicit.

<ExampleFrame variant="component">
  <LightboxAdvancedCustomization />
</ExampleFrame>

<div className="docs-reference">
  <Tabs values={[{ label: 'React', value: 'React' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="React">

```tsx file="../../../src/components/examples/lightbox/advanced-customization.tsx"
```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/examples/lightbox/lightbox-advanced-customization.module.css"
```

    </Tab>
  </Tabs>
</div>

## Styling

### CSS Properties

<div className="docs-reference">
  <Tabs
    values={[{ label: 'CSS Variables', value: 'CSS Variables' }]}
    className="docs-reference-tabs"
    contentItemClassName="docs-reference-panel"
  >
    <Tab>
      <LightboxDocs.LightboxCssPropertiesPanel />
    </Tab>
  </Tabs>
</div>

### Styling hooks

- moduix slots: `lightbox-trigger`, `lightbox-backdrop`, `lightbox-positioner`,
  `lightbox-content`, `lightbox-title`, `lightbox-description`, `lightbox-close-trigger`,
  `lightbox-close-icon`, `lightbox-header`, `lightbox-body`, `lightbox-footer`, `lightbox-image`,
  and `lightbox-gallery`.
- Ark state and anatomy hooks: `data-scope="dialog"`, `data-part`, `data-state="open|closed"`,
  `data-nested`, `data-has-nested`, `--layer-index`, and `--nested-layer-count`.
- Local hooks: `Lightbox.Image` exposes `data-slot="lightbox-image"` and optional
  `data-close-on-click`; `Lightbox.Gallery` exposes `data-slot="lightbox-gallery"` and styles
  nested Carousel slots.
- Motion is driven by Ark `data-state`; use `present` only when JavaScript animation control needs
  to keep the content mounted.
- With `prefers-reduced-motion: reduce`, the backdrop and content use a 1ms animation so exit
  lifecycle cleanup remains intact without visible motion.
