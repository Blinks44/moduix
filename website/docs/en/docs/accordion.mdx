---
title: Accordion
component: accordion
description: A vertically stacked set of interactive headings that each reveal a section of content.
---

import * as AccordionDocs from '@/components/examples/accordion';

## API Reference

<PrimitiveReference href="https://ark-ui.com/docs/components/accordion" />

## Basic

`Accordion` groups related sections behind headings that can be expanded in place. Use it for FAQs,
settings groups, or filters where people may need to compare sections; use `Collapsible` for one
standalone disclosure.

Use `Accordion` with Ark-style parts: `Accordion.Item`, `Accordion.ItemTrigger`,
`Accordion.ItemIndicator`, `Accordion.ItemContent`, and `Accordion.ItemBody`.

<PreviewFrame maxWidth="sm">
  ```tsx preview file="./_snippets/accordion/basic.tsx"

```
</PreviewFrame>

## Install with shadcn

<ShadcnInstall packageName="accordion" />

## Anatomy

```text
Accordion.Root
└─ Accordion.Item[value]
 ├─ Accordion.ItemTrigger
 │  ├─ label
 │  └─ Accordion.ItemIndicator (optional)
 └─ Accordion.ItemContent
    └─ Accordion.ItemBody
       └─ content

Accordion.RootProvider
└─ same item tree connected to a useAccordion() store
```

| Part                           | Role                                                                                   |
| ------------------------------ | -------------------------------------------------------------------------------------- |
| `Accordion` / `Accordion.Root` | Controls accordion state and keyboard behavior.                                        |
| `Accordion.RootProvider`       | Connects parts to state created with `useAccordion()`.                                 |
| `Accordion.Item`               | One collapsible item with a required `value`.                                          |
| `Accordion.ItemTrigger`        | Interactive button that opens or closes the item.                                      |
| `Accordion.ItemIndicator`      | Visual open/close cue. Defaults to the moduix plus icon when no children are provided. |
| `Accordion.ItemContent`        | Collapsible content region that keeps Ark size animation behavior intact.              |
| `Accordion.ItemBody`           | Default inner spacing wrapper for panel content.                                       |

## Composition

### Recommended composition

`Accordion` owns keyboard navigation, focus movement, and open item state. Use it for
`value`, `defaultValue`, `onValueChange(details)`, `onFocusChange(details)`, `multiple`,
`collapsible`, `orientation`, `lazyMount`, `unmountOnExit`, and `ids`.

Stay with `Accordion`, `Accordion.Item`, `Accordion.ItemTrigger`, `Accordion.ItemIndicator`,
`Accordion.ItemContent`, and `Accordion.ItemBody` for the standard Ark composition path.

### Custom composition

Use a custom composition when state must live outside the rendered subtree, surrounding UI needs to
read it, or you need to replace the default parts.

`value` and `defaultValue` are always arrays, including single-open mode. `onValueChange` follows
the Ark callback shape and exposes the current item values as `details.value`.

Use `Accordion.RootProvider` together with `useAccordion()` from `@moduix/react` when accordion state must be created
outside the rendered subtree.

Use `Accordion.Context`, `Accordion.ItemContext`, `useAccordionContext()`, and
`useAccordionItemContext()` from moduix when surrounding UI needs to read root or item state. Item
state exposes Ark's `expanded`, `focused`, and `disabled` booleans.

`Accordion.Item` requires a stable `value`. Use `disabled` on the item when one section should stay
present but unavailable.

`Accordion.ItemIndicator` is optional. In moduix it renders `PlusIcon` by default when no children
are provided, but you can replace the indicator content entirely.

Use `asChild` only when another element must own the rendered node. Keep the Ark part tree visible
instead of hiding it behind convenience wrappers.

### Default props

| Prop                   | Default    | Notes                                                 |
| ---------------------- | ---------- | ----------------------------------------------------- |
| `collapsible`          | `false`    | Keeps one expanded item open until another is chosen. |
| `lazyMount`            | `false`    | Mounts item content immediately.                      |
| `multiple`             | `false`    | Allows a single expanded item by default.             |
| `orientation`          | `vertical` | Uses vertical keyboard and layout behavior.           |
| `unmountOnExit`        | `false`    | Keeps collapsed content mounted.                      |
| `value`/`defaultValue` | `[]`       | Values are string arrays, including single-open mode. |

### Behavioral notes

Put spacing on `Accordion.ItemBody` instead of on `Accordion.ItemContent`. Ark measurement variables
drive the open/close animation: `--height` for vertical accordions and `--width` for horizontal
accordions.

Each trigger is a native button connected to its content with `aria-expanded`, `aria-controls`, and
the matching content label. `Enter` or `Space` toggles an item. In vertical orientation, `ArrowDown`
and `ArrowUp` move between triggers; horizontal orientation uses `ArrowRight` and `ArrowLeft`.
`Home` and `End` move to the first and last trigger.

Every styled part forwards its ref and supports `asChild` for cases where another semantic element
must own the rendered node. Keep trigger replacements keyboard-focusable and button-like.

When the user requests reduced motion, Accordion removes trigger and indicator transitions and
reduces panel motion to an effectively instant lifecycle-safe animation.

## Examples

### Collapsible

Add `collapsible` when the currently open item should be allowed to close without selecting another item first.

<PreviewFrame maxWidth="sm">
  ```tsx preview file="./_snippets/accordion/collapsible.tsx"

```
</PreviewFrame>

### Controlled

Control `value` from React state when the open items need to stay in sync with routing, filters, or surrounding UI.

<PreviewFrame maxWidth="sm">
```tsx preview file="./_snippets/accordion/controlled.tsx"

```

</PreviewFrame>

### Disabled Item

Set `disabled` on an item when it should stay visible in the list but remain unavailable for interaction.

<PreviewFrame maxWidth="sm">
  ```tsx preview file="./_snippets/accordion/disabled-item.tsx"

```
</PreviewFrame>

### Horizontal

Switch to `orientation="horizontal"` when the accordion should follow a horizontal interaction pattern.

<PreviewFrame maxWidth="sm">
```tsx preview file="./_snippets/accordion/horizontal.tsx"

```

</PreviewFrame>

### Lazy Mount

Use `lazyMount` and `unmountOnExit` when hidden panel content is expensive and should only render while needed.

<PreviewFrame maxWidth="sm">
  ```tsx preview file="./_snippets/accordion/lazy-mount.tsx"

```
</PreviewFrame>

### Multiple

Use `multiple` when several sections should stay open at the same time instead of collapsing to a single active item.

<PreviewFrame maxWidth="sm">
```tsx preview file="./_snippets/accordion/multiple.tsx"

```

</PreviewFrame>

### Root Provider

Use `useAccordion()` from `@moduix/react` with `Accordion.RootProvider` when the state store must be created outside
the rendered accordion tree. Do not render `Accordion` or `Accordion.Root` for the same store.

<PreviewFrame maxWidth="sm">
  ```tsx preview file="./_snippets/accordion/root-provider.tsx"

```
</PreviewFrame>

### Customization

Accordion content can host another Ark-based composite widget. Keep each primitive's part tree
explicit so keyboard handling and hidden form inputs stay owned by the correct component.

<PreviewFrame maxWidth="sm">
```tsx preview file="./_snippets/accordion/with-slider.tsx"

```

</PreviewFrame>

## Styling

Use `className` on exported parts for structural styling changes. For state styling, prefer Ark
data attributes (`data-state`, `data-disabled`, `data-orientation`) and moduix `data-slot` hooks.

### CSS Properties

<CssPropertiesSection properties={AccordionDocs.accordionOverrideCssProperties} />

### Styling hooks

- `data-slot="accordion-root"`, `accordion-root-provider`, `accordion-item`,
  `accordion-item-trigger`, `accordion-item-indicator`, `accordion-item-content`, and
  `accordion-item-body`.
- Ark anatomy hooks: `data-scope="accordion"` and `data-part="root|item|item-trigger|item-indicator|item-content"`.
- moduix sugar hook: `data-part="item-body"` on `Accordion.ItemBody`.
- Ark state hooks: `data-state="open|closed"`, `data-disabled`, `data-focus`,
  `data-controls`, and `data-orientation="horizontal|vertical"`.
- Ark content variables: `--height` for vertical animation and `--width` for horizontal animation.
