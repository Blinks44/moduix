# ProgressCircular (Solid)

`ProgressCircular` preserves the React component's circular Ark anatomy, state, accessibility
attributes, data hooks, CSS variables, RootProvider, Context, and namespace hooks.

Ark Solid uses render-function `asChild` composition and does not forward `ref` through that path.
Ordinary refs and `asChild` composition are covered independently in the Solid tests.