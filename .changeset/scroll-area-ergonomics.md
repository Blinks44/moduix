---
'@moduix/react': patch
---

ScrollArea: Add persistent scrollbar visibility and moduix state access.
Smooths scrollbar hover growth and adds `ScrollArea.useScrollArea()` for RootProvider composition.