---
'@moduix/react': patch
'@moduix/solid': patch
---

Navigation Menu: Align the viewport enter/exit keyframes with the shared popup motion so reduced-motion and preset tuning follow foundation tokens, and document the `ViewportPositioner` `align` prop.