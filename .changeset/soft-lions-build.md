---
'@moduix/react': patch
---

DatePicker: add `Field`, `RangeField`, and `DayTable` sugar and align docs around the shorter default composition.
Document the advanced low-level composition path separately and update examples to keep the basic path first.