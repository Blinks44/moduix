---
'@moduix/react': major
'@moduix/react-tailwind': patch
'@moduix/solid': major
'@moduix/solid-tailwind': patch
---

Badge: Keep direct children unchanged and limit built-in interaction styling to links.
Use `Badge.Label` explicitly for truncation; button hover and disabled presentation are now consumer-owned, and `--moduix-badge-opacity-disabled` is removed.