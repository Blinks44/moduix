---
title: Tag
description: A compact token for selected values, filters, and assignments with optional leading, trailing, and close parts.
icon: Component
---

import {
  CustomTagExample,
  RemovableTagExample,
  TagAsChildExample,
  TagCssPropertiesPanel,
  TagExample,
  TagSizesExample,
  TagTruncatedExample,
  TagVariantsExample,
  TagWithLeadingIconExample,
} from '@/components/examples/tag';

## API Reference

There is no dedicated Ark UI `Tag` primitive, so moduix owns this component contract and builds it with the Ark [`ark` factory](https://ark-ui.com/docs/guides/composition) and Ark [styling attributes](https://ark-ui.com/docs/guides/styling). Its part model follows the Chakra UI [`Tag`](https://chakra-ui.com/docs/components/tag) recipe: `Root`, `Label`, `StartElement`, `EndElement`, and `CloseTrigger`.

## Choosing the right component

Use `Tag` when the UI is showing **the selected value itself** and that value may need to be
removed, reordered, or managed as part of the current interface state.

- Choose **`Badge`** when the UI is showing short metadata such as status, category, version, or count.
- Choose **`Tag`** when the UI is showing selected values, filters, assignments, or labels that belong to the current state of the interface.

## Basic

Start with the short root form and `Tag.Label`. Add other parts only when the token needs icons or
an inline close action.

<Preview>
  <TagExample />

  <Preview.Code>
    {`
      import { Tag } from "moduix";

      export function TagDemo() {
        return (
          <Tag>
            <Tag.Label>TypeScript</Tag.Label>
          </Tag>
        );
      }
    `}

  </Preview.Code>

  <Preview.Data>
    {`      const label = "TypeScript";`}

  </Preview.Data>

  <Preview.CSS>
    {`      /* No custom styles are required. */`}

  </Preview.CSS>
</Preview>

## Install with shadcn (optional)

If you want `Tag` to become part of your own project source instead of staying in `node_modules`,
install it from the moduix GitHub registry:

<Tabs items={['npm', 'pnpm', 'bun', 'deno', 'aube']}>
  <Tab value="npm">

```bash
npx shadcn@latest add Blinks44/moduix/tag
```

  </Tab>
  <Tab value="pnpm">

```bash
pnpm dlx shadcn@latest add Blinks44/moduix/tag
```

  </Tab>
  <Tab value="bun">

```bash
bunx shadcn@latest add Blinks44/moduix/tag
```

  </Tab>
  <Tab value="deno">

```bash
deno x -A npm:shadcn@latest add Blinks44/moduix/tag
```

  </Tab>
  <Tab value="aube">

```bash
aubx shadcn@latest add Blinks44/moduix/tag
```

  </Tab>
</Tabs>

This install path also pulls in the shared moduix foundation files that `Tag` depends on, such as
styles, icons, and small utilities.

## Anatomy

The root is also exported as `Tag.Root` for namespace consistency with multipart Ark-style
components.

```text
Tag / Tag.Root
├─ Tag.StartElement (optional)
├─ Tag.Label
└─ Tag.EndElement (optional)
   └─ Tag.CloseTrigger (optional)
```

| Part               | Role                                                                               |
| ------------------ | ---------------------------------------------------------------------------------- |
| `Tag` / `Tag.Root` | Root slot. Applies token layout, border, size, variant, and `asChild` composition. |
| `Tag.Label`        | Truncation wrapper for the visible token value.                                    |
| `Tag.StartElement` | Optional leading slot for icons, avatars, or status markers.                       |
| `Tag.EndElement`   | Optional trailing slot for icons or actions.                                       |
| `Tag.CloseTrigger` | Optional button action for dismissing or removing the token.                       |

## Composition

### Recommended composition

Keep token text in `Tag.Label`. Put decorative or supporting elements in `Tag.StartElement` and
`Tag.EndElement`, and put `Tag.CloseTrigger` inside `Tag.EndElement` when the token is removable.

### When to go custom

Use `asChild` when the root itself needs native button or link semantics. The child must be a single
semantic element, and it owns the interaction behavior.

### Default props

| Surface            | Prop         | Default      | Notes                                                          |
| ------------------ | ------------ | ------------ | -------------------------------------------------------------- |
| `Tag`              | `variant`    | `default`    | `default`, `secondary`, `outline`, `ghost`, `destructive`      |
| `Tag`              | `size`       | `md`         | `sm`, `md`                                                     |
| `Tag.CloseTrigger` | `type`       | `button`     | Prevents accidental form submission when not using `asChild`.  |
| `Tag.CloseTrigger` | `children`   | close icon   | Replaced when custom children are passed.                      |
| `Tag.CloseTrigger` | `aria-label` | `Remove tag` | Applied only with no custom children and no `aria-labelledby`. |

### Behavioral notes

- `Tag` has no owned state, callbacks, keyboard roving, or form behavior.
- `Tag.CloseTrigger` is explicit composition, not a root-level `onClose` prop.
- Built-in variants use the same color semantics as `Badge`; `Tag` differs by part anatomy and close affordance.
- Give each close trigger a specific accessible name when several removable tags appear together.
- `asChild` follows Ark factory composition and requires one child element.

## Examples

### Variants

Use the built-in variants for common moduix tones while keeping the same part structure.

<Preview>
  <TagVariantsExample />

  <Preview.Code>
    {`
      import { Tag } from "moduix";
      import styles from "./tag-demo.module.css";

      const variants = ["default", "secondary", "outline", "ghost", "destructive"] as const;

      export function TagVariantsDemo() {
        return (
          <div className={styles.row}>
            {variants.map((variant) => (
              <Tag key={variant} variant={variant}>
                <Tag.Label>{variant}</Tag.Label>
              </Tag>
            ))}
          </div>
        );
      }
    `}

  </Preview.Code>

  <Preview.Data>
    {`      const variants = ["default", "secondary", "outline", "ghost", "destructive"] as const;`}

  </Preview.Data>

  <Preview.CSS>
    {`
      .row {
        display: flex;
        flex-wrap: wrap;
        align-items: center;
        gap: var(--spacing-3);
      }
    `}

  </Preview.CSS>
</Preview>

### Sizes

Use `size="sm"` for dense token rows and keep the default `md` size for standalone tokens.

<Preview>
  <TagSizesExample />

  <Preview.Code>
    {`
      import { Tag } from "moduix";
      import styles from "./tag-demo.module.css";

      const sizes = [
        { label: "Compact", size: "sm" },
        { label: "Default", size: "md" },
      ] as const;

      export function TagSizesDemo() {
        return (
          <div className={styles.row}>
            {sizes.map((tag) => (
              <Tag key={tag.size} size={tag.size}>
                <Tag.Label>{tag.label}</Tag.Label>
              </Tag>
            ))}
          </div>
        );
      }
    `}

  </Preview.Code>

  <Preview.Data>
    {`
      const sizes = [
        { label: "Compact", size: "sm" },
        { label: "Default", size: "md" },
      ] as const;
    `}

  </Preview.Data>

  <Preview.CSS>
    {`
      .row {
        display: flex;
        flex-wrap: wrap;
        align-items: center;
        gap: var(--spacing-3);
      }
    `}

  </Preview.CSS>
</Preview>

### Closable

Use `Tag.EndElement` with `Tag.CloseTrigger` for removable values. The parent widget owns removal
state and click handling.

<Preview>
  <RemovableTagExample />

  <Preview.Code>
    {`
      import { Tag } from "moduix";
      import styles from "./tag-demo.module.css";

      const tags = [
        { label: "TypeScript", variant: "default" },
        { label: "Design review", variant: "secondary" },
        { label: "Needs approval", variant: "outline", disabled: true },
      ] as const;

      export function RemovableTagDemo() {
        return (
          <div className={styles.row}>
            {tags.map((tag) => (
              <Tag key={tag.label} variant={tag.variant}>
                <Tag.Label>{tag.label}</Tag.Label>
                <Tag.EndElement>
                  <Tag.CloseTrigger disabled={tag.disabled} aria-label={\`Remove \${tag.label} tag\`} />
                </Tag.EndElement>
              </Tag>
            ))}
          </div>
        );
      }
    `}

  </Preview.Code>

  <Preview.Data>
    {`
      const tags = [
        { label: "TypeScript", variant: "default" },
        { label: "Design review", variant: "secondary" },
        { label: "Needs approval", variant: "outline", disabled: true },
      ] as const;
    `}

  </Preview.Data>

  <Preview.CSS>
    {`
      .row {
        display: flex;
        flex-wrap: wrap;
        align-items: center;
        gap: var(--spacing-3);
      }
    `}

  </Preview.CSS>
</Preview>

### Icon

Use `Tag.StartElement` and `Tag.EndElement` for icon slots so label truncation and action sizing stay
independent.

<Preview>
  <TagWithLeadingIconExample />

  <Preview.Code>
    {`
      import { CheckIcon, Tag } from "moduix";
      import styles from "./tag-demo.module.css";

      const tags = [
        { label: "Selected", variant: "default", removable: false },
        { label: "Deployed", variant: "outline", removable: true },
      ] as const;

      export function TagWithIconDemo() {
        return (
          <div className={styles.row}>
            {tags.map((tag) => (
              <Tag key={tag.label} variant={tag.variant}>
                <Tag.StartElement>
                  <CheckIcon />
                </Tag.StartElement>
                <Tag.Label>{tag.label}</Tag.Label>
                {tag.removable ? (
                  <Tag.EndElement>
                    <Tag.CloseTrigger aria-label={\`Remove \${tag.label} tag\`} />
                  </Tag.EndElement>
                ) : null}
              </Tag>
            ))}
          </div>
        );
      }
    `}

  </Preview.Code>

  <Preview.Data>
    {`
      const tags = [
        { label: "Selected", variant: "default", removable: false },
        { label: "Deployed", variant: "outline", removable: true },
      ] as const;
    `}

  </Preview.Data>

  <Preview.CSS>
    {`
      .row {
        display: flex;
        flex-wrap: wrap;
        align-items: center;
        gap: var(--spacing-3);
      }
    `}

  </Preview.CSS>
</Preview>

### Overflow

Use `Tag.Label` as the truncation point for long values. Add `title` when the full text should
remain discoverable.

<Preview>
  <TagTruncatedExample />

  <Preview.Code>
    {`
      import { Tag } from "moduix";
      import styles from "./tag-demo.module.css";

      const label = "Ready for stakeholder review after legal approval";

      export function TagTruncatedDemo() {
        return (
          <Tag className={styles.constrained}>
            <Tag.Label title={label}>{label}</Tag.Label>
            <Tag.EndElement>
              <Tag.CloseTrigger aria-label="Remove long tag" />
            </Tag.EndElement>
          </Tag>
        );
      }
    `}

  </Preview.Code>

  <Preview.Data>
    {`      const label = "Ready for stakeholder review after legal approval";`}

  </Preview.Data>

  <Preview.CSS>
    {`
      .constrained {
        max-width: 18rem;
      }
    `}

  </Preview.CSS>
</Preview>

### Render as Button

Use `asChild` when the whole token is an action. Keep the child as the semantic element that owns
keyboard and click behavior.

<Preview>
  <TagAsChildExample />

  <Preview.Code>
    {`
      import { Tag } from "moduix";
      import styles from "./tag-demo.module.css";

      export function TagAsChildDemo() {
        return (
          <Tag asChild variant="outline">
            <button className={styles.buttonTag} type="button">
              <Tag.Label>Open filter</Tag.Label>
            </button>
          </Tag>
        );
      }
    `}

  </Preview.Code>

  <Preview.Data>
    {`      const label = "Open filter";`}

  </Preview.Data>

  <Preview.CSS>
    {`
      .buttonTag {
        appearance: none;
        cursor: pointer;
      }
    `}

  </Preview.CSS>
</Preview>

### Custom Styling

Use local CSS variables when a token needs custom tone or spacing without changing the public API.

<Preview>
  <CustomTagExample />

  <Preview.Code>
    {`
      import { CheckIcon, Tag } from "moduix";
      import styles from "./tag-demo.module.css";

      export function CustomTagDemo() {
        return (
          <div className={styles.row}>
            <Tag className={styles.customSoft}>
              <Tag.StartElement>
                <CheckIcon />
              </Tag.StartElement>
              <Tag.Label>Priority</Tag.Label>
              <Tag.EndElement>
                <Tag.CloseTrigger aria-label="Remove priority tag" />
              </Tag.EndElement>
            </Tag>
            <Tag className={styles.customOutline} variant="outline">
              <Tag.Label>Customer-facing</Tag.Label>
            </Tag>
          </div>
        );
      }
    `}

  </Preview.Code>

  <Preview.Data>
    {`
      const labels = {
        priority: "Priority",
        customer: "Customer-facing",
      };
    `}

  </Preview.Data>

  <Preview.CSS>
    {`
      .customSoft {
        --tag-bg: color-mix(in oklab, var(--color-primary) 16%, var(--color-background));
        --tag-border-color: color-mix(in oklab, var(--color-primary) 35%, transparent);
        --tag-color: var(--color-primary);
        --tag-close-trigger-bg-hover: color-mix(in oklab, var(--color-primary) 18%, transparent);
      }

      .customOutline {
        --tag-border-color: color-mix(in oklab, var(--color-foreground) 18%, transparent);
        --tag-padding-x-md: 0.625rem;
      }

      .row {
        display: flex;
        flex-wrap: wrap;
        align-items: center;
        gap: var(--spacing-3);
      }
    `}

  </Preview.CSS>
</Preview>

## Styling

Prefer built-in variants first, then local CSS variables for one-off sizing and tone changes.

### CSS Properties

<div className="not-prose">
  <Tabs items={['CSS Variables']} className="mb-0 mt-3">
    <Tab className="max-h-150 overflow-auto">
      <TagCssPropertiesPanel />
    </Tab>
  </Tabs>
</div>

### Styling hooks

- `data-scope="tag"` and `data-part="root" | "label" | "start-element" | "end-element" | "close-trigger"` are emitted on the exported parts.
- `data-slot="tag-root"`, `data-slot="tag-label"`, `data-slot="tag-start-element"`, `data-slot="tag-end-element"`, and `data-slot="tag-close-trigger"` are stable moduix selectors.
- `Tag` writes the selected size and tone to `data-size` and `data-variant`.
- `Tag.CloseTrigger` exposes `data-disabled` for disabled or `aria-disabled` states and inherits the token color.