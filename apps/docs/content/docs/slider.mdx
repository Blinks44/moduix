---
title: Slider
description: A range input for selecting one value or a range of values.
icon: Component
---

import {
  ControlledSliderExample,
  CustomCompositionSliderExample,
  DisabledSliderExample,
  RangeSliderExample,
  SliderCssPlaygroundPanel,
  SliderCssPropertiesPanel,
  SliderExample,
  SteppedRangeSliderExample,
  VerticalSliderExample,
  sliderPlaygroundCssProperties,
} from '@/components/examples/slider';

## API Reference

<BaseUIReference href="https://base-ui.com/react/components/slider" />

## Default Path

Use `Slider` for the common case. It keeps Base UI state and behavior intact, while rendering the
control, track, and indicator for you.

<Preview cssProperties={sliderPlaygroundCssProperties}>
  <SliderExample />

  <Preview.Code>
    {`
      import {
        Slider,
        SliderLabel,
        SliderThumb,
        SliderValue,
      } from "moduix";

      export function SliderDemo() {
        return (
          <Slider defaultValue={40}>
            <SliderLabel>Volume</SliderLabel>
            <SliderValue>{([formattedValue]) => \`\${formattedValue}%\`}</SliderValue>
            <SliderThumb aria-label="Volume" />
          </Slider>
        );
      }
    `}

  </Preview.Code>
  <Preview.CSSProperties>{(context) => <SliderCssPropertiesPanel {...context} />}</Preview.CSSProperties>
  <Preview.CSSPlayground>{(context) => <SliderCssPlaygroundPanel {...context} />}</Preview.CSSPlayground>
</Preview>

## Install with shadcn (optional)

If you want `Slider` to become part of your own project source instead of staying in
`node_modules`, install it from the moduix GitHub registry:

<Tabs items={['npm', 'pnpm', 'bun']}>
  <Tab value="npm">

```bash
npx shadcn@latest add Blinks44/moduix/slider
```

  </Tab>
  <Tab value="pnpm">

```bash
pnpm dlx shadcn@latest add Blinks44/moduix/slider
```

  </Tab>
  <Tab value="bun">

```bash
bunx shadcn@latest add Blinks44/moduix/slider
```

  </Tab>
</Tabs>

This install path also pulls in the shared moduix foundation files that `Slider` depends on, such
as styles, icons, and small utilities.

`SliderLabel` gives the slider group a visible label. Each `SliderThumb` still needs its own
accessible name through `aria-label`, especially for range sliders.

## Anatomy

The default wrapper keeps the recommended structure short:

```text
Slider
├─ SliderLabel
├─ SliderValue
└─ SliderThumb[index]
```

Internally, `Slider` renders:

```text
SliderRoot
├─ SliderLabel
├─ SliderValue
└─ SliderControl
   └─ SliderTrack
      ├─ SliderIndicator
      └─ SliderThumb[index]
```

| Part              | Role                                                                 |
| ----------------- | -------------------------------------------------------------------- |
| `Slider`          | Default high-level wrapper for the common path.                      |
| `SliderRoot`      | Low-level root for manual composition.                               |
| `SliderLabel`     | Visible label for the slider group.                                  |
| `SliderValue`     | Optional formatted value output.                                     |
| `SliderControl`   | Interaction area for pointer and keyboard input.                     |
| `SliderTrack`     | Visual track that contains the indicator and thumbs.                 |
| `SliderIndicator` | Filled portion of the track.                                         |
| `SliderThumb`     | Interactive handle. Render one thumb per value and index range ones. |

## Composition

Use `Slider` unless you need to restyle or rearrange the internal structural parts directly.

The high-level wrapper accepts the same behavior props as `SliderRoot`, including:

- `value` and `defaultValue`
- `onValueChange`
- `min`, `max`, `step`, and `largeStep`
- `minStepsBetweenValues`
- `orientation`
- `thumbAlignment`
- `thumbCollisionBehavior`

For the default path, put `className` on:

- `Slider` for root width and layout
- `SliderLabel` and `SliderValue` for text styling
- `SliderThumb` for thumb styling

Switch to `SliderRoot` composition when the styling needs to reach `SliderControl`,
`SliderTrack`, or `SliderIndicator`.

### Defaults

| Prop                     | Default      | Values                               |
| ------------------------ | ------------ | ------------------------------------ |
| `orientation`            | `horizontal` | `horizontal`, `vertical`             |
| `min`                    | `0`          | number                               |
| `max`                    | `100`        | number                               |
| `step`                   | `1`          | number                               |
| `largeStep`              | `10`         | number                               |
| `minStepsBetweenValues`  | `0`          | number                               |
| `thumbAlignment`         | `center`     | `center`, `edge`, `edge-client-only` |
| `thumbCollisionBehavior` | `push`       | `push`, `swap`, `none`               |

For range sliders, pass an array value and render the same number of `SliderThumb` elements. Keep
`index` aligned with the value array for SSR and stable thumb identity.

## Examples

### Range

Pass an array value and render one `SliderThumb` for each selected value.

<Preview>
  <RangeSliderExample />

  <Preview.Code>
    {`
      import {
        Slider,
        SliderLabel,
        SliderThumb,
        SliderValue,
      } from "moduix";
      import { useState } from "react";

      export function RangeSliderDemo() {
        const [value, setValue] = useState([20, 70] as readonly number[]);

        return (
          <Slider value={value} min={0} max={100} onValueChange={setValue}>
            <SliderLabel>Price range</SliderLabel>
            <SliderValue>{([minValue, maxValue]) => \`\${minValue} - \${maxValue}\`}</SliderValue>
            <SliderThumb index={0} aria-label="Minimum price" />
            <SliderThumb index={1} aria-label="Maximum price" />
          </Slider>
        );
      }
    `}

  </Preview.Code>
</Preview>

### Steps and Constraints

Use `step`, `largeStep`, `minStepsBetweenValues`, and `thumbCollisionBehavior` when the slider
represents a constrained numeric domain.

<Preview>
  <SteppedRangeSliderExample />

  <Preview.Code>
    {`
      import {
        Slider,
        SliderLabel,
        SliderThumb,
        SliderValue,
      } from "moduix";
      import { useState } from "react";

      export function SteppedRangeSliderDemo() {
        const [value, setValue] = useState([250, 750] as readonly number[]);

        return (
          <Slider
            value={value}
            min={0}
            max={1000}
            step={50}
            largeStep={200}
            minStepsBetweenValues={2}
            thumbCollisionBehavior="none"
            format={{
              style: "currency",
              currency: "USD",
              maximumFractionDigits: 0,
            }}
            onValueChange={setValue}
          >
            <SliderLabel>Budget</SliderLabel>
            <SliderValue>
              {(_, [minValue, maxValue]) =>
                \`\${priceFormatter.format(minValue)} - \${priceFormatter.format(maxValue)}\`
              }
            </SliderValue>
            <SliderThumb index={0} aria-label="Minimum budget" />
            <SliderThumb index={1} aria-label="Maximum budget" />
          </Slider>
        );
      }
    `}

  </Preview.Code>

  <Preview.Data>
    {`      const priceFormatter = new Intl.NumberFormat("en-US", {
        style: "currency",
        currency: "USD",
        maximumFractionDigits: 0,
      });`}

  </Preview.Data>
</Preview>

### Controlled

Control the value from React state when the slider needs to stay in sync with another input or
derived UI.

<Preview>
  <ControlledSliderExample />

  <Preview.Code>
    {`
      import {
        Slider,
        SliderLabel,
        SliderThumb,
        SliderValue,
      } from "moduix";
      import { useState } from "react";
      import styles from "./slider.module.css";

      export function ControlledSliderDemo() {
        const [value, setValue] = useState(24);

        return (
          <div className={styles.stack}>
            <Slider value={value} onValueChange={setValue}>
              <SliderLabel>Brightness</SliderLabel>
              <SliderValue>{([formattedValue]) => \`\${formattedValue}%\`}</SliderValue>
              <SliderThumb aria-label="Brightness" />
            </Slider>
            <input
              className={styles.range}
              type="range"
              min={0}
              max={100}
              value={value}
              onChange={(event) => {
                setValue(Number(event.target.value));
              }}
            />
          </div>
        );
      }
    `}

  </Preview.Code>

  <Preview.CSS>
    {`
      .stack {
        display: grid;
        gap: var(--spacing-4);
      }

      .range {
        width: 12rem;
      }
    `}

  </Preview.CSS>
</Preview>

### Vertical

Set `orientation="vertical"` and size the wrapper with the public CSS custom properties.

<Preview>
  <VerticalSliderExample />

  <Preview.Code>
    {`
      import {
        Slider,
        SliderLabel,
        SliderThumb,
        SliderValue,
      } from "moduix";
      import styles from "./slider.module.css";

      export function VerticalSliderDemo() {
        return (
          <div className={styles.verticalContainer}>
            <Slider orientation="vertical" defaultValue={60} className={styles.verticalSlider}>
              <SliderLabel>Output</SliderLabel>
              <SliderValue>{([formattedValue]) => \`\${formattedValue}%\`}</SliderValue>
              <SliderThumb aria-label="Output" />
            </Slider>
          </div>
        );
      }
    `}

  </Preview.Code>

  <Preview.CSS>
    {`
      .verticalContainer {
        display: flex;
        height: 14rem;
      }

      .verticalSlider {
        --slider-height: 12rem;
        --slider-width-vertical: auto;
      }
    `}

  </Preview.CSS>
</Preview>

### Disabled

Use `disabled` to preserve the current value while preventing interaction.

<Preview>
  <DisabledSliderExample />

  <Preview.Code>
    {`
      import {
        Slider,
        SliderLabel,
        SliderThumb,
        SliderValue,
      } from "moduix";

      export function DisabledSliderDemo() {
        return (
          <Slider defaultValue={32} disabled>
            <SliderLabel>Notifications</SliderLabel>
            <SliderValue>{([formattedValue]) => \`\${formattedValue}%\`}</SliderValue>
            <SliderThumb aria-label="Notifications" />
          </Slider>
        );
      }
    `}

  </Preview.Code>
</Preview>

## Custom Composition

Use `SliderRoot` when the default wrapper is still the right behavior model, but the visible track
structure needs direct control.

<Preview>
  <CustomCompositionSliderExample />

  <Preview.Code>
    {`
      import {
        SliderControl,
        SliderIndicator,
        SliderLabel,
        SliderRoot,
        SliderThumb,
        SliderTrack,
        SliderValue,
      } from "moduix";
      import styles from "./slider.module.css";

      export function CustomCompositionSliderDemo() {
        return (
          <SliderRoot defaultValue={56}>
            <SliderLabel>Temperature</SliderLabel>
            <SliderValue>{([formattedValue]) => \`\${formattedValue}%\`}</SliderValue>
            <SliderControl className={styles.customControl}>
              <SliderTrack className={styles.customTrack}>
                <SliderIndicator className={styles.customIndicator} />
                <SliderThumb aria-label="Temperature" className={styles.customThumb} />
              </SliderTrack>
            </SliderControl>
          </SliderRoot>
        );
      }
    `}

  </Preview.Code>

  <Preview.CSS>
    {`
      .customControl {
        --slider-control-padding-y: var(--spacing-3);
      }

      .customTrack {
        --slider-track-bg: color-mix(in oklab, var(--color-chart-4) 18%, var(--color-muted));
        --slider-track-border-color: transparent;
        --slider-track-size: 0.625rem;
      }

      .customIndicator {
        --slider-indicator-bg: var(--color-chart-4);
      }

      .customThumb {
        --slider-thumb-bg: var(--color-chart-4);
        --slider-thumb-border-color: var(--color-background);
        --slider-thumb-size: 1.25rem;
      }
    `}

  </Preview.CSS>
</Preview>