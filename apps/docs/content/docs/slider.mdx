---
title: Slider
description: A control for selecting one value or a range of values.
icon: Component
---

import {
  CenterOriginSliderExample,
  ContextSliderExample,
  ControlledSliderExample,
  CustomStylingSliderExample,
  DisabledSliderExample,
  DraggingIndicatorSliderExample,
  EventsSliderExample,
  MarksSliderExample,
  MinMaxSliderExample,
  RangeSliderExample,
  RootProviderSliderExample,
  SliderCssPropertiesPanel,
  SliderExample,
  StepSliderExample,
  ThumbAlignmentSliderExample,
  ThumbCollisionSliderExample,
  ThumbOverlapSliderExample,
  VerticalSliderExample,
  sliderBasicCode,
  sliderCenterOriginCode,
  sliderContextCode,
  sliderContextCss,
  sliderControlledCode,
  sliderCustomStylingCode,
  sliderCustomStylingCss,
  sliderDisabledCode,
  sliderDraggingIndicatorCode,
  sliderEventsCode,
  sliderEventsCss,
  sliderHeaderCss,
  sliderMarksCode,
  sliderMarksData,
  sliderMinMaxCode,
  sliderNoCss,
  sliderNoData,
  sliderRangeCode,
  sliderRootProviderCode,
  sliderStackCss,
  sliderStepCode,
  sliderThumbAlignmentCode,
  sliderThumbCollisionCode,
  sliderThumbOverlapCode,
  sliderVerticalCode,
  sliderVerticalCss,
} from '@/components/examples/slider';

## API Reference

<PrimitiveReference href="https://ark-ui.com/docs/components/slider" />

## Choosing the right component

Use `Slider` when users should adjust a bounded numeric value directly with pointer or keyboard
input.

- Choose **`Slider`** for volume, brightness, budgets, thresholds, and multi-thumb ranges.
- Choose **`Input`** or **`NumberInput`** when exact typed entry matters more than scrubbing.
- Choose **`ProgressLinear`** or **`ProgressCircular`** when the value is display-only.

## Basic

`Slider` is the styled Ark root. Compose the Ark parts explicitly and keep a `Slider.HiddenInput`
inside each thumb when the value should participate in forms and resets.

<Preview>
  <SliderExample />

  <Preview.Code>{sliderBasicCode}</Preview.Code>
  <Preview.CSS>{sliderHeaderCss}</Preview.CSS>
  <Preview.Data>{sliderNoData}</Preview.Data>
</Preview>

## Install with shadcn

If you want `Slider` to become part of your own project source instead of staying in
`node_modules`, install it from the moduix GitHub registry:

<Tabs items={['npm', 'pnpm', 'bun', 'deno', 'aube']}>
  <Tab value="npm">

```bash
npx shadcn@latest add Blinks44/moduix/react-slider
```

  </Tab>
  <Tab value="pnpm">

```bash
pnpm dlx shadcn@latest add Blinks44/moduix/react-slider
```

  </Tab>
  <Tab value="bun">

```bash
bunx shadcn@latest add Blinks44/moduix/react-slider
```

  </Tab>
  <Tab value="deno">

```bash
deno x -A npm:shadcn@latest add Blinks44/moduix/react-slider
```

  </Tab>
  <Tab value="aube">

```bash
aubx shadcn@latest add Blinks44/moduix/react-slider
```

  </Tab>
</Tabs>

## Anatomy

```text
Slider / Slider.Root
├─ Slider.Label
├─ Slider.ValueText
├─ Slider.Control
│  ├─ Slider.Track
│  │  └─ Slider.Range
│  └─ Slider.Thumb[index]
│     ├─ Slider.DraggingIndicator
│     └─ Slider.HiddenInput
└─ Slider.MarkerGroup
   └─ Slider.Marker[value]
```

| Part                                   | Role                                                        |
| -------------------------------------- | ----------------------------------------------------------- |
| `Slider` / `Slider.Root`               | Styled Ark root for controlled and uncontrolled state.      |
| `Slider.RootProvider`                  | Root that receives an externally owned `useSlider` machine. |
| `Slider.Label`                         | Visible label for the slider group.                         |
| `Slider.ValueText`                     | Current value text generated by Ark.                        |
| `Slider.Control`                       | Pointer interaction area.                                   |
| `Slider.Track`                         | Full visual rail.                                           |
| `Slider.Range`                         | Filled portion from the configured origin to the value.     |
| `Slider.Thumb`                         | Focusable slider handle.                                    |
| `Slider.HiddenInput`                   | Native input for form submission and reset behavior.        |
| `Slider.MarkerGroup` / `Slider.Marker` | Optional tick mark group and individual mark.               |
| `Slider.DraggingIndicator`             | Optional value bubble while dragging.                       |
| `Slider.Context`                       | Render-prop access to Ark slider state.                     |

## Composition

### Recommended composition

Use the explicit Ark part tree: root, label, optional value text, control, track, range, one thumb
per value, and `HiddenInput` inside each thumb when forms or resets matter.

`Slider` is the short root form and is equivalent to `Slider.Root`.

### When to go custom

Use `asChild` when a part must render a custom host element, but keep a single semantic child that
can receive Ark props. Use `Slider.Context` for inline state reads and `useSlider` with
`Slider.RootProvider` when state must be owned outside the rendered tree.

### Default props

| Prop                     | Default      | Notes                                      |
| ------------------------ | ------------ | ------------------------------------------ |
| `defaultValue`           | `[50]`       | Ark stores slider values as `number[]`.    |
| `orientation`            | `horizontal` | Use `vertical` for vertical keyboard axis. |
| `min`                    | `0`          | Lower bound.                               |
| `max`                    | `100`        | Upper bound.                               |
| `step`                   | `1`          | Pointer and keyboard increment.            |
| `minStepsBetweenThumbs`  | `0`          | Minimum gap between range thumbs.          |
| `origin`                 | `start`      | Fill start for single-value ranges.        |
| `thumbAlignment`         | `contain`    | Use `center` to let the thumb overflow.    |
| `thumbCollisionBehavior` | `none`       | Use `push` or `swap` for range dragging.   |
| `thumbSize`              | measured     | Override Ark's measured thumb dimensions.  |

`value`, `defaultValue`, `onValueChange(details)`, and `onValueChangeEnd(details)` use Ark detail
objects. Use `details.value` to read the array.

### Behavioral notes

`Slider` follows Ark's WAI-ARIA slider behavior. Each thumb needs an accessible name through
`Slider.Label`, `aria-label`, or `aria-labelledby`. Horizontal sliders use left/right arrow keys;
vertical sliders use up/down arrow keys. Home and End move to bounds.

Keep `Slider.HiddenInput` inside every `Slider.Thumb` when native form submission, form
association, or form reset synchronization matters. `disabled`, `invalid`, `readOnly`, `name`,
`form`, `ids`, `aria-label`, `aria-labelledby`, and `getAriaValueText(details)` pass through to
Ark.

## Examples

### Range

Pass an array with one value per thumb and render matching `Slider.Thumb` parts.

<Preview>
  <RangeSliderExample />

  <Preview.Code>{sliderRangeCode}</Preview.Code>
  <Preview.CSS>{sliderNoCss}</Preview.CSS>
  <Preview.Data>{sliderNoData}</Preview.Data>
</Preview>

### Min and Max

Use `min` and `max` for domains that do not start at `0` or end at `100`.

<Preview>
  <MinMaxSliderExample />

  <Preview.Code>{sliderMinMaxCode}</Preview.Code>
  <Preview.CSS>{sliderNoCss}</Preview.CSS>
  <Preview.Data>{sliderNoData}</Preview.Data>
</Preview>

### Step

Use `step` for fractional or constrained increments.

<Preview>
  <StepSliderExample />

  <Preview.Code>{sliderStepCode}</Preview.Code>
  <Preview.CSS>{sliderHeaderCss}</Preview.CSS>
  <Preview.Data>{sliderNoData}</Preview.Data>
</Preview>

### Controlled

Control the value from React state by storing Ark's `details.value` array.

<Preview>
  <ControlledSliderExample />

  <Preview.Code>{sliderControlledCode}</Preview.Code>
  <Preview.CSS>{sliderHeaderCss}</Preview.CSS>
  <Preview.Data>{sliderNoData}</Preview.Data>
</Preview>

### Change Events

Use `onValueChange` for live updates and `onValueChangeEnd` when a drag or keyboard interaction is
committed.

<Preview>
  <EventsSliderExample />

  <Preview.Code>{sliderEventsCode}</Preview.Code>
  <Preview.CSS>{sliderEventsCss}</Preview.CSS>
  <Preview.Data>{sliderNoData}</Preview.Data>
</Preview>

### Vertical

Set `orientation="vertical"` and give the root a stable height.

<Preview>
  <VerticalSliderExample />

  <Preview.Code>{sliderVerticalCode}</Preview.Code>
  <Preview.CSS>{sliderVerticalCss}</Preview.CSS>
  <Preview.Data>{sliderNoData}</Preview.Data>
</Preview>

### Center Origin

Set `origin="center"` when the selected value represents a positive or negative offset.

<Preview>
  <CenterOriginSliderExample />

  <Preview.Code>{sliderCenterOriginCode}</Preview.Code>
  <Preview.CSS>{sliderHeaderCss}</Preview.CSS>
  <Preview.Data>{sliderNoData}</Preview.Data>
</Preview>

### Marks

Use `Slider.MarkerGroup` and `Slider.Marker` to render tick marks. Ark marks expose
`data-state="under-value | at-value | over-value"` for styling.

<Preview>
  <MarksSliderExample />

  <Preview.Code>{sliderMarksCode}</Preview.Code>
  <Preview.CSS>{sliderHeaderCss}</Preview.CSS>
  <Preview.Data>{sliderMarksData}</Preview.Data>
</Preview>

### Dragging Indicator

Place `Slider.DraggingIndicator` inside a thumb to show the active value while dragging.

<Preview>
  <DraggingIndicatorSliderExample />

  <Preview.Code>{sliderDraggingIndicatorCode}</Preview.Code>
  <Preview.CSS>{sliderNoCss}</Preview.CSS>
  <Preview.Data>{sliderNoData}</Preview.Data>
</Preview>

### Thumb Alignment

Use `thumbAlignment="center"` when the thumb should extend beyond the track edges.

<Preview>
  <ThumbAlignmentSliderExample />

  <Preview.Code>{sliderThumbAlignmentCode}</Preview.Code>
  <Preview.CSS>{sliderHeaderCss}</Preview.CSS>
  <Preview.Data>{sliderNoData}</Preview.Data>
</Preview>

### Thumb Collision

Use `thumbCollisionBehavior="push"` or `"swap"` to change how range thumbs behave when they meet.

<Preview>
  <ThumbCollisionSliderExample />

  <Preview.Code>{sliderThumbCollisionCode}</Preview.Code>
  <Preview.CSS>{sliderHeaderCss}</Preview.CSS>
  <Preview.Data>{sliderNoData}</Preview.Data>
</Preview>

### Thumb Overlap

Use `minStepsBetweenThumbs` to keep a minimum gap between range thumbs.

<Preview>
  <ThumbOverlapSliderExample />

  <Preview.Code>{sliderThumbOverlapCode}</Preview.Code>
  <Preview.CSS>{sliderHeaderCss}</Preview.CSS>
  <Preview.Data>{sliderNoData}</Preview.Data>
</Preview>

### Disabled

Use `disabled` to keep the value visible while preventing interaction.

<Preview>
  <DisabledSliderExample />

  <Preview.Code>{sliderDisabledCode}</Preview.Code>
  <Preview.CSS>{sliderHeaderCss}</Preview.CSS>
  <Preview.Data>{sliderNoData}</Preview.Data>
</Preview>

### Context

Use `Slider.Context` for inline reads of Ark state such as `value`, `dragging`, and focus state.

<Preview>
  <ContextSliderExample />

  <Preview.Code>{sliderContextCode}</Preview.Code>
  <Preview.CSS>{sliderContextCss}</Preview.CSS>
  <Preview.Data>{sliderNoData}</Preview.Data>
</Preview>

### Root Provider

Use `useSlider` with `Slider.RootProvider` when state must be owned outside the rendered tree.

<Preview>
  <RootProviderSliderExample />

  <Preview.Code>{sliderRootProviderCode}</Preview.Code>
  <Preview.CSS>{sliderStackCss}</Preview.CSS>
  <Preview.Data>{sliderNoData}</Preview.Data>
</Preview>

### Custom Styling

Customize visuals with CSS variables, `className`, `data-slot` hooks, and Ark state attributes.

<Preview>
  <CustomStylingSliderExample />

  <Preview.Code>{sliderCustomStylingCode}</Preview.Code>
  <Preview.CSS>{sliderCustomStylingCss}</Preview.CSS>
  <Preview.Data>{sliderNoData}</Preview.Data>
</Preview>

## Styling

Style `Slider` through `className`, Ark attributes, stable `data-slot="slider-*"` hooks, Ark CSS
variables, and the public Moduix CSS variables below.

### CSS Properties

<div className="not-prose">
  <Tabs items={['CSS Variables']} className="mb-0 mt-3">
    <Tab className="max-h-150 overflow-auto">
      <SliderCssPropertiesPanel />
    </Tab>
  </Tabs>
</div>

### Styling hooks

Ark writes `data-scope="slider"` and `data-part` to all rendered parts. Moduix also writes
`data-slot` values: `slider-root`, `slider-root-provider`, `slider-label`, `slider-value-text`,
`slider-control`, `slider-track`, `slider-range`, `slider-thumb`, `slider-hidden-input`,
`slider-marker-group`, `slider-marker`, and `slider-dragging-indicator`.

Important Ark state hooks include `data-orientation`, `data-disabled`, `data-invalid`,
`data-focus`, `data-dragging`, marker `data-state`, and dragging-indicator
`data-state="open | closed"`. Ark runtime CSS variables include `--slider-thumb-width`,
`--slider-thumb-height`, `--slider-thumb-transform`, `--slider-range-start`,
`--slider-range-end`, `--translate-x`, and `--translate-y`.