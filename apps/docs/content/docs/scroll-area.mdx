---
title: Scroll Area
description: A native scroll container with custom Ark UI scrollbars.
icon: Component
---

import {
  BothDirectionsScrollAreaExample,
  CustomStylingScrollAreaExample,
  FadeScrollAreaExample,
  HorizontalScrollAreaExample,
  NestedScrollAreaExample,
  RootProviderScrollAreaExample,
  ScrollAreaCssPropertiesPanel,
  ScrollAreaExample,
} from '@/components/examples/scroll-area';

## API Reference

<PrimitiveReference href="https://ark-ui.com/docs/components/scroll-area" />

## Basic

Use `ScrollArea` as the Ark root and compose the viewport, content, scrollbar, thumb, and corner
parts explicitly. The viewport keeps native wheel, touch, trackpad, and keyboard scrolling while the
scrollbar parts provide the styled controls. Moduix adds one small extra: `fade`, which applies a
top and bottom mask to the viewport using Ark overflow measurements.

<Preview>
  <ScrollAreaExample />

  <Preview.Code>
    {`
      import { ScrollArea } from "@moduix/react";

      export function ScrollAreaDemo() {
        return (
          <ScrollArea className="root">
            <ScrollArea.Viewport>
              <ScrollArea.Content>
                <div className="textContent">
                  {sections.map((item) => (
                    <section key={item.title}>
                      <h3>{item.title}</h3>
                      <p className="paragraph">{item.body}</p>
                    </section>
                  ))}
                </div>
              </ScrollArea.Content>
            </ScrollArea.Viewport>
            <ScrollArea.Scrollbar>
              <ScrollArea.Thumb />
            </ScrollArea.Scrollbar>
            <ScrollArea.Corner />
          </ScrollArea>
        );
      }
    `}

  </Preview.Code>

  <Preview.CSS>
    {`
      .root {
        --scroll-area-width: 24rem;
        --scroll-area-height: 13rem;
        max-width: calc(100vw - 2rem);
        border: var(--border-width-sm) solid var(--color-border);
        border-radius: var(--radius-lg);
      }

      .textContent {
        display: grid;
        gap: var(--spacing-3);
        padding: var(--spacing-3);
        padding-right: var(--spacing-6);
      }

      .paragraph {
        margin: 0;
        color: var(--color-foreground);
        font-size: var(--text-sm);
        line-height: var(--line-height-text-sm);
      }
    `}

  </Preview.CSS>

  <Preview.Data>
    {`
      const sections = [
        {
          title: "Overview",
          body: "Use ScrollArea when content needs a bounded viewport without giving up native scrolling behavior.",
        },
        {
          title: "Dense panels",
          body: "Long settings forms, sidebars, drawers, and inspectors often need a stable frame with scrollable content inside.",
        },
        {
          title: "Readable overflow",
          body: "A visible scrollbar makes it clear that more content exists outside the current viewport.",
        },
        {
          title: "Keyboard support",
          body: "The viewport remains focusable, so keyboard scrolling works without extra wrapper logic.",
        },
      ];
    `}

  </Preview.Data>
</Preview>

## Install with shadcn

If you want `Scroll Area` to become part of your own project source instead of staying in
`node_modules`, install it from the moduix GitHub registry:

<Tabs items={['npm', 'pnpm', 'bun', 'deno', 'aube']}>
  <Tab value="npm">

```bash
npx shadcn@latest add Blinks44/moduix/react-scroll-area
```

  </Tab>
  <Tab value="pnpm">

```bash
pnpm dlx shadcn@latest add Blinks44/moduix/react-scroll-area
```

  </Tab>
  <Tab value="bun">

```bash
bunx shadcn@latest add Blinks44/moduix/react-scroll-area
```

  </Tab>
  <Tab value="deno">

```bash
deno x -A npm:shadcn@latest add Blinks44/moduix/react-scroll-area
```

  </Tab>
  <Tab value="aube">

```bash
aubx shadcn@latest add Blinks44/moduix/react-scroll-area
```

  </Tab>
</Tabs>

This install path also pulls in the shared moduix foundation files that `Scroll Area` depends on.

## Anatomy

```text
ScrollArea
├─ ScrollArea.Viewport
│  └─ ScrollArea.Content
│     └─ children
├─ ScrollArea.Scrollbar
│  └─ ScrollArea.Thumb
└─ ScrollArea.Corner
```

| Part                             | Role                                                        |
| -------------------------------- | ----------------------------------------------------------- |
| `ScrollArea` / `ScrollArea.Root` | State owner and outer positioning box.                      |
| `ScrollArea.RootProvider`        | Root that receives a `useScrollArea()` state object.        |
| `ScrollArea.Viewport`            | Native scroll viewport and focus target.                    |
| `ScrollArea.Content`             | Inner content wrapper measured by Ark UI.                   |
| `ScrollArea.Scrollbar`           | Custom scrollbar track for one axis.                        |
| `ScrollArea.Thumb`               | Draggable thumb inside a scrollbar.                         |
| `ScrollArea.Corner`              | Bottom-end intersection filler when both axes can overflow. |
| `ScrollArea.Context`             | Render-prop access to the current scroll area API.          |

## Composition

### Default props

| Prop                                                 | Default    | Notes                                                                                  |
| ---------------------------------------------------- | ---------- | -------------------------------------------------------------------------------------- |
| `fade` on `ScrollArea` and `ScrollArea.RootProvider` | `false`    | Adds a top and bottom viewport mask driven by Ark `--scroll-area-overflow-y-*` values. |
| `orientation` on `ScrollArea.Scrollbar`              | `vertical` | Set `orientation="horizontal"` for a horizontal track.                                 |
| `asChild`                                            | `false`    | Available on each Ark part for single-child host composition.                          |
| `ids` on `ScrollArea`                                | generated  | Use when separate code needs stable root, viewport, content, scrollbar, or thumb IDs.  |

`ScrollArea` is the styled root. It does not render viewport, content, or scrollbar parts for you;
compose the Ark parts explicitly so each structural node remains configurable.

### State access

Use `useScrollArea()` with `ScrollArea.RootProvider` when controls outside the root need to call the
scroll area API. Use `ScrollArea.Context` or `useScrollAreaContext()` for state reads inside the
rendered tree. Do not render `ScrollArea` and `ScrollArea.RootProvider` for the same state object.

### Behavior notes

- Render one `ScrollArea.Scrollbar` per axis you want to expose.
- Ark adds `data-overflow-x` and `data-overflow-y`; moduix hides scrollbar tracks until the matching
  axis actually overflows.
- The viewport CSS hides native browser scrollbars with `scrollbar-width: none` and the WebKit
  scrollbar rule required by Ark UI.
- The ref on `ScrollArea` points to the root element; the ref on `ScrollArea.Viewport` points to the
  native scroll container.

## Examples

### Horizontal

Use a horizontal scrollbar when the content is intentionally wider than the frame.

<Preview>
  <HorizontalScrollAreaExample />

  <Preview.Code>
    {`
      import { ScrollArea } from "@moduix/react";

      export function HorizontalScrollAreaDemo() {
        return (
          <ScrollArea className="horizontalRoot">
            <ScrollArea.Viewport>
              <ScrollArea.Content>
                <p className="wideParagraph">{copy}</p>
              </ScrollArea.Content>
            </ScrollArea.Viewport>
            <ScrollArea.Scrollbar orientation="horizontal">
              <ScrollArea.Thumb />
            </ScrollArea.Scrollbar>
            <ScrollArea.Corner />
          </ScrollArea>
        );
      }
    `}

  </Preview.Code>

  <Preview.CSS>
    {`
      .horizontalRoot {
        --scroll-area-width: 24rem;
        --scroll-area-height: auto;
        max-width: calc(100vw - 2rem);
        border: var(--border-width-sm) solid var(--color-border);
        border-radius: var(--radius-lg);
      }

      .wideParagraph {
        width: 50rem;
        margin: 0;
        padding: var(--spacing-3);
        padding-bottom: var(--spacing-6);
        font-size: var(--text-sm);
        line-height: var(--line-height-text-sm);
      }
    `}

  </Preview.CSS>

  <Preview.Data>
    {`
      const copy = "Long single-line or wide content can stay in one native scroll viewport while the custom horizontal track remains visible only when x-axis overflow exists.";
    `}

  </Preview.Data>
</Preview>

### Fade

Use `fade` when long vertical content should hint that more content exists above and below the
current viewport without introducing another wrapper.

<Preview>
  <FadeScrollAreaExample />

  <Preview.Code>
    {`
      import { ScrollArea } from "@moduix/react";

      export function FadeScrollAreaDemo() {
        return (
          <ScrollArea className="root" fade>
            <ScrollArea.Viewport>
              <ScrollArea.Content>
                <div className="textContent">
                  {sections.map((item) => (
                    <section key={item.title}>
                      <h3>{item.title}</h3>
                      <p className="paragraph">{item.body}</p>
                    </section>
                  ))}
                </div>
              </ScrollArea.Content>
            </ScrollArea.Viewport>
            <ScrollArea.Scrollbar>
              <ScrollArea.Thumb />
            </ScrollArea.Scrollbar>
            <ScrollArea.Corner />
          </ScrollArea>
        );
      }
    `}

  </Preview.Code>

  <Preview.CSS>
    {`
      .root {
        --scroll-area-width: 24rem;
        --scroll-area-height: 13rem;
        --scroll-area-fade-size: var(--spacing-8);
        max-width: calc(100vw - 2rem);
        border: var(--border-width-sm) solid var(--color-border);
        border-radius: var(--radius-lg);
      }

      .textContent {
        display: grid;
        gap: var(--spacing-3);
        padding: var(--spacing-3);
        padding-right: var(--spacing-6);
      }

      .paragraph {
        margin: 0;
        color: var(--color-foreground);
        font-size: var(--text-sm);
        line-height: var(--line-height-text-sm);
      }
    `}

  </Preview.CSS>

  <Preview.Data>
    {`
      const sections = [
        { title: "Overview", body: "The mask becomes shallower near the top and bottom because it uses Ark's measured overflow distances." },
        { title: "Long content", body: "Use it for settings panels, drawers, and release notes where scrollbar visibility alone is too subtle." },
        { title: "Tuning", body: "Adjust --scroll-area-fade-size, --scroll-area-fade-start-size, and --scroll-area-fade-end-size on the root." },
      ];
    `}

  </Preview.Data>
</Preview>

### Both Directions

Render vertical and horizontal scrollbars when content can overflow on both axes.

<Preview>
  <BothDirectionsScrollAreaExample />

  <Preview.Code>
    {`
      import { ScrollArea } from "@moduix/react";

      export function BothDirectionsScrollAreaDemo() {
        return (
          <ScrollArea className="root">
            <ScrollArea.Viewport>
              <ScrollArea.Content>
                <div className="gridContent">
                  {cells.map((cell) => (
                    <div key={cell} className="cell">
                      {cell}
                    </div>
                  ))}
                </div>
              </ScrollArea.Content>
            </ScrollArea.Viewport>
            <ScrollArea.Scrollbar>
              <ScrollArea.Thumb />
            </ScrollArea.Scrollbar>
            <ScrollArea.Scrollbar orientation="horizontal">
              <ScrollArea.Thumb />
            </ScrollArea.Scrollbar>
            <ScrollArea.Corner />
          </ScrollArea>
        );
      }
    `}

  </Preview.Code>

  <Preview.CSS>
    {`
      .root {
        --scroll-area-width: 24rem;
        --scroll-area-height: 13rem;
        max-width: calc(100vw - 2rem);
        border: var(--border-width-sm) solid var(--color-border);
        border-radius: var(--radius-lg);
      }

      .gridContent {
        display: grid;
        grid-template-columns: repeat(12, 5rem);
        grid-template-rows: repeat(8, 5rem);
        width: max-content;
        gap: var(--spacing-2);
        padding: var(--spacing-3);
      }

      .cell {
        display: grid;
        place-items: center;
        border-radius: var(--radius-sm);
        background-color: var(--color-muted);
        color: var(--color-foreground);
        font-size: var(--text-sm);
        font-weight: var(--weight-medium);
      }
    `}

  </Preview.CSS>

  <Preview.Data>
    {`
      const cells = Array.from({ length: 96 }, (_, index) => index + 1);
    `}

  </Preview.Data>
</Preview>

### Nested

Nested scroll areas use the same composition. Keep each root, viewport, content, and scrollbar tree
complete so Ark can measure each area independently.

<Preview>
  <NestedScrollAreaExample />

  <Preview.Code>
    {`
      import { ScrollArea } from "@moduix/react";

      export function NestedScrollAreaDemo() {
        return (
          <ScrollArea className="root">
            <ScrollArea.Viewport>
              <ScrollArea.Content>
                <div className="textContent">
                  <section>
                    <h3>Outer release notes</h3>
                    <p className="paragraph">{sections[0].body}</p>
                  </section>
                  <ScrollArea className="nestedRoot">
                    <ScrollArea.Viewport>
                      <ScrollArea.Content>
                        <div className="textContent">
                          {sections.map((item) => (
                            <section key={item.title}>
                              <h3>{item.title}</h3>
                              <p className="paragraph">{item.body}</p>
                            </section>
                          ))}
                        </div>
                      </ScrollArea.Content>
                    </ScrollArea.Viewport>
                    <ScrollArea.Scrollbar>
                      <ScrollArea.Thumb />
                    </ScrollArea.Scrollbar>
                    <ScrollArea.Corner />
                  </ScrollArea>
                </div>
              </ScrollArea.Content>
            </ScrollArea.Viewport>
            <ScrollArea.Scrollbar>
              <ScrollArea.Thumb />
            </ScrollArea.Scrollbar>
            <ScrollArea.Corner />
          </ScrollArea>
        );
      }
    `}

  </Preview.Code>

  <Preview.CSS>
    {`
      .root {
        --scroll-area-width: 24rem;
        --scroll-area-height: 13rem;
        max-width: calc(100vw - 2rem);
        border: var(--border-width-sm) solid var(--color-border);
        border-radius: var(--radius-lg);
      }

      .nestedRoot {
        --scroll-area-width: 100%;
        --scroll-area-height: 8rem;
        border: var(--border-width-sm) solid var(--color-border);
        border-radius: var(--radius-lg);
      }

      .textContent {
        display: grid;
        gap: var(--spacing-3);
        padding: var(--spacing-3);
        padding-right: var(--spacing-6);
      }

      .paragraph {
        margin: 0;
        color: var(--color-foreground);
        font-size: var(--text-sm);
        line-height: var(--line-height-text-sm);
      }
    `}

  </Preview.CSS>

  <Preview.Data>
    {`
      const sections = [
        { title: "Outer release notes", body: "The outer viewport can contain normal content and another complete ScrollArea tree." },
        { title: "Nested details", body: "The nested root owns its own measurements, overflow state, and scrollbar parts." },
      ];
    `}

  </Preview.Data>
</Preview>

### Root Provider

Use `useScrollArea()` with `ScrollArea.RootProvider` when external controls need access to the
scrolling API.

<Preview>
  <RootProviderScrollAreaExample />

  <Preview.Code>
    {`
      import { Button, ScrollArea, useScrollArea } from "@moduix/react";

      export function RootProviderScrollAreaDemo() {
        const scrollArea = useScrollArea();

        return (
          <div className="providerStack">
            <div className="actions">
              <Button onClick={() => scrollArea.scrollToEdge({ edge: "top" })}>
                Top
              </Button>
              <Button onClick={() => scrollArea.scrollToEdge({ edge: "bottom" })}>
                Bottom
              </Button>
            </div>
            <ScrollArea.RootProvider value={scrollArea} className="root">
              <ScrollArea.Viewport>
                <ScrollArea.Content>
                  <div className="textContent">
                    {sections.map((item) => (
                      <section key={item.title}>
                        <h3>{item.title}</h3>
                        <p className="paragraph">{item.body}</p>
                      </section>
                    ))}
                  </div>
                </ScrollArea.Content>
              </ScrollArea.Viewport>
              <ScrollArea.Scrollbar>
                <ScrollArea.Thumb />
              </ScrollArea.Scrollbar>
              <ScrollArea.Corner />
            </ScrollArea.RootProvider>
          </div>
        );
      }
    `}

  </Preview.Code>

  <Preview.CSS>
    {`
      .providerStack {
        display: grid;
        gap: var(--spacing-3);
      }

      .root {
        --scroll-area-width: 24rem;
        --scroll-area-height: 13rem;
        max-width: calc(100vw - 2rem);
        border: var(--border-width-sm) solid var(--color-border);
        border-radius: var(--radius-lg);
      }

      .actions {
        display: flex;
        gap: var(--spacing-2);
        justify-content: flex-end;
      }

      .textContent {
        display: grid;
        gap: var(--spacing-3);
        padding: var(--spacing-3);
        padding-right: var(--spacing-6);
      }

      .paragraph {
        margin: 0;
        color: var(--color-foreground);
        font-size: var(--text-sm);
        line-height: var(--line-height-text-sm);
      }
    `}

  </Preview.CSS>

  <Preview.Data>
    {`
      const sections = [
        { title: "Overview", body: "External controls can call the same scroll area API that Ark provides to the root provider." },
        { title: "Scroll targets", body: "Use scrollToEdge for simple top and bottom controls, or the lower-level scroll methods for custom targets." },
      ];
    `}

  </Preview.Data>
</Preview>

### Custom Styling

Customize the Moduix visual layer with CSS variables and per-part classes while keeping Ark's
composition and state attributes intact.

<Preview>
  <CustomStylingScrollAreaExample />

  <Preview.Code>
    {`
      import { ScrollArea } from "@moduix/react";

      export function CustomStylingScrollAreaDemo() {
        return (
          <ScrollArea className="customRoot">
            <ScrollArea.Viewport>
              <ScrollArea.Content>
                <div className="gridContent">
                  {cells.map((cell) => (
                    <div key={cell} className="cell">
                      {cell}
                    </div>
                  ))}
                </div>
              </ScrollArea.Content>
            </ScrollArea.Viewport>
            <ScrollArea.Scrollbar>
              <ScrollArea.Thumb />
            </ScrollArea.Scrollbar>
            <ScrollArea.Scrollbar orientation="horizontal" className="accentScrollbar">
              <ScrollArea.Thumb className="accentThumb" />
            </ScrollArea.Scrollbar>
            <ScrollArea.Corner className="customCorner" />
          </ScrollArea>
        );
      }
    `}

  </Preview.Code>

  <Preview.CSS>
    {`
      .customRoot {
        --scroll-area-width: 24rem;
        --scroll-area-height: 13rem;
        --scroll-area-scrollbar-size: var(--spacing-2);
        --scroll-area-scrollbar-margin: var(--spacing-1);
        --scroll-area-scrollbar-bg: color-mix(in srgb, var(--color-primary) 12%, transparent);
        --scroll-area-thumb-bg: var(--color-primary);
        --scroll-area-corner-bg: color-mix(in srgb, var(--color-primary) 12%, transparent);
        max-width: calc(100vw - 2rem);
        border: var(--border-width-sm) solid var(--color-border);
        border-radius: var(--radius-lg);
      }

      .accentScrollbar {
        background-color: color-mix(in srgb, var(--color-chart-3) 18%, transparent);
      }

      .accentThumb {
        background-color: var(--color-chart-3);
      }

      .gridContent {
        display: grid;
        grid-template-columns: repeat(12, 5rem);
        grid-template-rows: repeat(8, 5rem);
        width: max-content;
        gap: var(--spacing-2);
        padding: var(--spacing-3);
      }

      .cell {
        display: grid;
        place-items: center;
        border-radius: var(--radius-sm);
        background-color: var(--color-muted);
        color: var(--color-foreground);
        font-size: var(--text-sm);
        font-weight: var(--weight-medium);
      }
    `}

  </Preview.CSS>

  <Preview.Data>
    {`
      const cells = Array.from({ length: 96 }, (_, index) => index + 1);
    `}

  </Preview.Data>
</Preview>

## Styling

Use CSS variables on the root when scrollbar thickness, focus ring treatment, or surface styling
needs to change without replacing the structure. The default thumb keeps the border token color and
grows by `2px` across its track on hover or drag; customize that amount with
`--scroll-area-thumb-hover-increase`.

### CSS Properties

<div className="not-prose">
  <Tabs items={['CSS Variables']} className="mb-0 mt-3">
    <Tab className="max-h-150 overflow-auto">
      <ScrollAreaCssPropertiesPanel />
    </Tab>
  </Tabs>
</div>

### Styling hooks

- `data-slot="scroll-area-root"`, `scroll-area-root-provider`, `scroll-area-viewport`,
  `scroll-area-content`, `scroll-area-scrollbar`, `scroll-area-thumb`, and `scroll-area-corner` are
  the stable Moduix hooks.
- `data-fade` is added by the `fade` prop on `ScrollArea` and `ScrollArea.RootProvider`.
- Ark adds `data-scope="scroll-area"` and `data-part` for every part.
- `data-overflow-x` and `data-overflow-y` are present when the measured content overflows on the
  corresponding axis.
- `data-at-top`, `data-at-bottom`, `data-at-left`, and `data-at-right` are available on the viewport.
- `data-orientation="vertical"` and `data-orientation="horizontal"` are exposed on scrollbars and
  thumbs.
- `data-hover` and `data-dragging` are available on scrollbar and thumb parts; `data-scrolling` is
  available on scrollbar parts.
- Ark root variables `--corner-width`, `--corner-height`, `--thumb-width`, and `--thumb-height` drive
  the corner offset and thumb dimensions. Ark viewport variables
  `--scroll-area-overflow-x-start`, `--scroll-area-overflow-x-end`,
  `--scroll-area-overflow-y-start`, and `--scroll-area-overflow-y-end` expose edge distances.