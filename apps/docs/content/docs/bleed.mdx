---
title: Bleed
description: A layout primitive that lets content intentionally escape a constrained container.
icon: Component
---

import {
  BleedBlockExample,
  BleedCssPropertiesPanel,
  BleedExample,
  BleedInlineAmountsExample,
  BleedSemanticExample,
  CustomStylingBleedExample,
  bleedBasicData,
  bleedBlockData,
  bleedCustomStylingData,
  bleedInlineAmountsData,
  bleedSemanticData,
} from '@/components/examples/bleed';

## API Reference

Ark UI does not ship a dedicated `bleed` primitive. Moduix builds this component with the official
[Ark factory](https://ark-ui.com/docs/guides/composition#the-ark-factory), follows Ark's
[composition](https://ark-ui.com/docs/guides/composition), [styling](https://ark-ui.com/docs/guides/styling), and
[ref](https://ark-ui.com/docs/guides/ref) guidance.

## Basic

Start with the default `inline="full"` behavior when media, banners, or section accents should
break out of a constrained container while staying in normal document flow.

<Preview>
  <BleedExample />

  <Preview.Code>
    {`
      import { Bleed, Text } from "@moduix/react";

      export function BleedDemo() {
        return (
          <div className="bleed-demo-container">
            <Text tone="muted">{content.before}</Text>
            <Bleed className="bleed-demo-surface">
              <Text weight="semibold">
                {content.surface}
              </Text>
            </Bleed>
            <Text tone="muted">
              {content.after}
            </Text>
          </div>
        );
      }
    `}

  </Preview.Code>

  <Preview.CSS>
    {`
      .bleed-demo-container {
        display: flex;
        width: min(28rem, calc(100vw - var(--spacing-8)));
        flex-direction: column;
        gap: var(--spacing-4);
        padding: var(--spacing-4);
        border: var(--border-width-sm) dashed var(--color-border);
      }

      .bleed-demo-surface {
        display: flex;
        flex-direction: column;
        align-items: center;
        padding: var(--spacing-4);
        background-color: var(--color-muted);
        text-align: center;
      }
    `}

  </Preview.CSS>
  <Preview.Data>{bleedBasicData}</Preview.Data>
</Preview>

## Install with shadcn (optional)

If you want `Bleed` to become part of your own project source instead of staying in
`node_modules`, install it from the moduix GitHub registry:

<Tabs items={['npm', 'pnpm', 'bun', 'deno', 'aube']}>
  <Tab value="npm">

```bash
npx shadcn@latest add Blinks44/moduix/react-bleed
```

  </Tab>
  <Tab value="pnpm">

```bash
pnpm dlx shadcn@latest add Blinks44/moduix/react-bleed
```

  </Tab>
  <Tab value="bun">

```bash
bunx shadcn@latest add Blinks44/moduix/react-bleed
```

  </Tab>
  <Tab value="deno">

```bash
deno x -A npm:shadcn@latest add Blinks44/moduix/react-bleed
```

  </Tab>
  <Tab value="aube">

```bash
aubx shadcn@latest add Blinks44/moduix/react-bleed
```

  </Tab>
</Tabs>

This install path also pulls in the shared moduix foundation files that `Bleed` depends on, such
as styles, icons, and small utilities.

## Anatomy

`Bleed` has a single Ark-aligned root element. It applies negative inline and/or block margins on
that root.

```text
Bleed.Root
└─ root[data-scope="bleed"][data-part="root"][data-slot="bleed-root"]
   └─ children
```

| Part                   | Role                                            |
| ---------------------- | ----------------------------------------------- |
| `Bleed` / `Bleed.Root` | Root layout wrapper for inline and block bleed. |
| `Bleed`                | Callable alias of `Bleed.Root`.                 |

## Composition

### Recommended composition

Use the shorter `Bleed` form when the layout only needs controlled negative margins on one wrapper.
`Bleed.Root` is the equivalent Ark-aligned namespace form.
This is the intended path for full-bleed media, inset section backgrounds, dividers, and banners
inside centered or padded shells.

### When to go custom

Use `className` and local `--bleed-*` overrides when a drawer, nested scroll region, or app shell
needs different full-bleed math than the default viewport-based values.

### Default props

| Entry       | Default | Values / Notes                               |
| ----------- | ------- | -------------------------------------------- |
| `inline`    | `full`  | `none`, `xs`, `sm`, `md`, `lg`, `xl`, `full` |
| `block`     | `none`  | `none`, `xs`, `sm`, `md`, `lg`, `xl`         |
| `asChild`   | `false` | Ark factory composition                      |
| `className` | -       | Applied to the root                          |

### Behavioral notes

- `Bleed.Root` renders exactly one root and forwards Ark factory div props to it.
- `ref` targets the rendered root element. With `asChild`, it targets the single child element.
- `asChild` requires one semantic child that can accept DOM props and a ref.
- Use `inline` for horizontal escape, and add `block` when a surface should also escape vertical
  padding.
- The old `as` prop was removed during the Ark migration. Use `asChild` when a semantic element
  should own the DOM node.
- Use one of the fixed scale values when content only needs to escape container padding instead of
  reaching the viewport edge.
- `inline="full"` uses viewport math, so nested shells may need local overrides for
  `--bleed-inline-full` and `--bleed-inline-full-size`.
- Ancestors with `overflow: hidden` or `overflow: clip` can crop the bleed.
- Unlike Chakra's layout primitive, moduix intentionally exposes token-based `inline` and `block`
  modes rather than one-sided `inlineStart`, `inlineEnd`, `blockStart`, and `blockEnd` props.

## Examples

### Inline Amounts

Use `inline` to choose how far content escapes the container on the inline axis.

<Preview>
  <BleedInlineAmountsExample />

  <Preview.Code>
    {`
      import { Bleed, Text } from "@moduix/react";

      export function BleedInlineAmountsDemo() {
        return (
          <div className="bleed-demo-container">
            {inlineAmounts.map((amount) => (
              <Bleed
                key={amount.value}
                inline={amount.value}
                className="bleed-demo-panel"
              >
                <Text>{amount.label}</Text>
              </Bleed>
            ))}
          </div>
        );
      }
    `}

  </Preview.Code>

  <Preview.CSS>
    {`
      .bleed-demo-container {
        display: flex;
        width: min(28rem, calc(100vw - var(--spacing-8)));
        flex-direction: column;
        gap: var(--spacing-4);
        padding: var(--spacing-4);
        border: var(--border-width-sm) dashed var(--color-border);
      }

      .bleed-demo-panel {
        display: flex;
        flex-direction: column;
        align-items: center;
        padding: var(--spacing-4);
        background-color: var(--color-muted);
        text-align: center;
      }
    `}

  </Preview.CSS>
  <Preview.Data>{bleedInlineAmountsData}</Preview.Data>
</Preview>

### Block Bleed

Use `block` when a surface should also escape vertical container padding.

<Preview>
  <BleedBlockExample />

  <Preview.Code>
    {`
      import { Bleed, Text } from "@moduix/react";

      export function BleedBlockDemo() {
        return (
          <div className="bleed-demo-padded-container">
            <Text tone="muted">{content.before}</Text>
            <Bleed inline="md" block="md" className="bleed-demo-panel">
              <Text>{content.surface}</Text>
            </Bleed>
            <Text tone="muted">{content.after}</Text>
          </div>
        );
      }
    `}

  </Preview.Code>

  <Preview.CSS>
    {`
      .bleed-demo-padded-container {
        display: flex;
        width: min(28rem, calc(100vw - var(--spacing-8)));
        flex-direction: column;
        gap: var(--spacing-4);
        padding: var(--spacing-6);
        border: var(--border-width-sm) dashed var(--color-border);
      }

      .bleed-demo-panel {
        display: flex;
        flex-direction: column;
        align-items: center;
        padding: var(--spacing-4);
        background-color: var(--color-muted);
        text-align: center;
      }
    `}

  </Preview.CSS>
  <Preview.Data>{bleedBlockData}</Preview.Data>
</Preview>

### Semantic Element

Use `asChild` when the escaping content has a more specific semantic role.

<Preview>
  <BleedSemanticExample />

  <Preview.Code>
    {`
      import { Bleed, Text } from "@moduix/react";

      export function BleedSemanticDemo() {
        return (
          <div className="bleed-demo-container">
            <Bleed asChild className="bleed-demo-figure">
              <figure>
                <div className="bleed-demo-media" />
                <Text tone="muted" size="sm">
                  {content.caption}
                </Text>
              </figure>
            </Bleed>
          </div>
        );
      }
    `}

  </Preview.Code>

  <Preview.CSS>
    {`
      .bleed-demo-container {
        display: flex;
        width: min(28rem, calc(100vw - var(--spacing-8)));
        flex-direction: column;
        gap: var(--spacing-4);
        padding: var(--spacing-4);
        border: var(--border-width-sm) dashed var(--color-border);
      }

      .bleed-demo-figure {
        display: flex;
        flex-direction: column;
        align-items: center;
        gap: var(--spacing-3);
        padding: var(--spacing-4);
        background-color: var(--color-muted);
        text-align: center;
      }

      .bleed-demo-media {
        width: min(28rem, 100%);
        min-height: 8rem;
        border-radius: var(--radius-md);
        background: linear-gradient(135deg, rgb(0 0 0 / 0.14), transparent 45%), var(--color-accent);
      }
    `}

  </Preview.CSS>
  <Preview.Data>{bleedSemanticData}</Preview.Data>
</Preview>

### Custom Styling

Pass `className` when styling the root or overriding bleed variables in local CSS.
This keeps the public API small while still allowing local full-bleed math for shells, drawers,
and inset layouts that do not align with the viewport.

<Preview>
  <CustomStylingBleedExample />

  <Preview.Code>
    {`
      import { Bleed, Text } from "@moduix/react";

      export function CustomStylingBleedDemo() {
        return (
          <div className="bleed-demo-container">
            <Bleed className="bleed-demo-custom-surface">
              <Text weight="semibold">{content.surface}</Text>
            </Bleed>
          </div>
        );
      }
    `}

  </Preview.Code>

  <Preview.CSS>
    {`
      .bleed-demo-container {
        display: flex;
        width: min(28rem, calc(100vw - var(--spacing-8)));
        flex-direction: column;
        gap: var(--spacing-4);
        padding: var(--spacing-4);
        border: var(--border-width-sm) dashed var(--color-border);
      }

      .bleed-demo-custom-surface {
        --bleed-inline-full: calc(var(--spacing-4) * -1);
        --bleed-inline-full-size: calc(100% + var(--spacing-8));
        display: flex;
        flex-direction: column;
        align-items: center;
        padding: var(--spacing-4);
        background-color: var(--color-muted);
        text-align: center;
      }
    `}

  </Preview.CSS>
  <Preview.Data>{bleedCustomStylingData}</Preview.Data>
</Preview>

## Styling

`Bleed.Root` exposes a small CSS variable contract and stable root data attributes. Use variables when
the layout math needs to change, and use `className` for the visual surface inside the bleed.

### CSS Properties

<div className="not-prose">
  <Tabs items={['CSS Variables']} className="mb-0 mt-3">
    <Tab className="max-h-150 overflow-auto">
      <BleedCssPropertiesPanel />
    </Tab>
  </Tabs>
</div>

### Styling hooks

- `data-scope="bleed"` and `data-part="root"` provide Ark-aligned hooks on the exported root.
- `data-slot="bleed-root"` is the stable moduix selector on the exported root.
- `data-inline` reflects the current inline bleed mode: `none`, `xs`, `sm`, `md`, `lg`, `xl`, or
  `full`.
- `data-block` reflects the current block bleed mode: `none`, `xs`, `sm`, `md`, `lg`, or `xl`.
- The public `--bleed-*` variables in `theme.css` cover both scale-based bleed amounts and the
  viewport math used by `inline="full"`.