---
title: Tabs
description: A set of tab triggers that switch between related content panels on the same page.
icon: Component
---

import {
  ControlledTabsExample,
  CustomStylingTabsExample,
  DisabledTabTabsExample,
  IconTabsExample,
  IndicatorTabsExample,
  LazyMountTabsExample,
  LineTabsExample,
  LinkTabsExample,
  ManualActivationTabsExample,
  RootProviderTabsExample,
  TabsCssPropertiesPanel,
  TabsExample,
  VerticalTabsExample,
  tabsCustomStylingCss,
  tabsExampleCss,
  tabsItemsData,
} from '@/components/examples/tabs';

## API Reference

<PrimitiveReference href="https://ark-ui.com/docs/components/tabs" />

## Choosing the right component

Use `Tabs` when users switch between mutually exclusive views that share one page region.

- Choose **`Tabs`** when one selected trigger should reveal one selected content panel.
- Choose **`Accordion`** when sections should stay stacked and expand independently.
- Choose **`ToggleGroup`** when the control changes state or mode, but does not own panel content.

## Basic

Use Ark-style parts: `Tabs.Root`, `Tabs.List`, `Tabs.Trigger`, `Tabs.Indicator`, and
`Tabs.Content`. The short `<Tabs>` form is the same root component.

<Preview>
  <TabsExample />

  <Preview.Code>
    {`
      import { Tabs } from "@moduix/react";

      export function TabsDemo() {
        return (
          <Tabs defaultValue="overview">
            <Tabs.List>
              {items.map((item) => (
                <Tabs.Trigger key={item.value} value={item.value}>
                  {item.title}
                </Tabs.Trigger>
              ))}
              <Tabs.Indicator />
            </Tabs.List>

            {items.map((item) => (
              <Tabs.Content key={item.value} value={item.value}>
                {item.content}
              </Tabs.Content>
            ))}
          </Tabs>
        );
      }
    `}

  </Preview.Code>

  <Preview.CSS>{tabsExampleCss}</Preview.CSS>
  <Preview.Data>{tabsItemsData}</Preview.Data>
</Preview>

## Install with shadcn (optional)

If you want `Tabs` to become part of your own project source instead of staying in
`node_modules`, install it from the moduix GitHub registry:

<Tabs items={['npm', 'pnpm', 'bun', 'deno', 'aube']}>
  <Tab value="npm">

```bash
npx shadcn@latest add Blinks44/moduix/react-tabs
```

  </Tab>
  <Tab value="pnpm">

```bash
pnpm dlx shadcn@latest add Blinks44/moduix/react-tabs
```

  </Tab>
  <Tab value="bun">

```bash
bunx shadcn@latest add Blinks44/moduix/react-tabs
```

  </Tab>
  <Tab value="deno">

```bash
deno x -A npm:shadcn@latest add Blinks44/moduix/react-tabs
```

  </Tab>
  <Tab value="aube">

```bash
aubx shadcn@latest add Blinks44/moduix/react-tabs
```

  </Tab>
</Tabs>

## Anatomy

```text
Tabs.Root
├─ Tabs.Context (optional render-prop access)
├─ Tabs.List
│  ├─ Tabs.Trigger[value]
│  ├─ Tabs.Trigger[value]
│  └─ Tabs.Indicator (optional)
└─ Tabs.Content[value]

Tabs.RootProvider
└─ same part tree connected to a useTabs() store
```

| Part                 | Role                                                            |
| -------------------- | --------------------------------------------------------------- |
| `Tabs` / `Tabs.Root` | Controls selected value, focus movement, orientation, and mode. |
| `Tabs.RootProvider`  | Connects parts to state created with `useTabs()`.               |
| `Tabs.List`          | Groups tab triggers and owns roving-focus list semantics.       |
| `Tabs.Trigger`       | Interactive tab button or `asChild` host with a required value. |
| `Tabs.Indicator`     | Optional active-trigger indicator positioned by Ark variables.  |
| `Tabs.Content`       | Content region for the matching trigger value.                  |
| `Tabs.Context`       | Reads root state through an Ark render prop.                    |

## Composition

### Recommended Composition

`Tabs.Root` owns keyboard navigation, focus movement, and selected value. Use it for `value`,
`defaultValue`, `onValueChange(details)`, `onFocusChange(details)`, `activationMode`,
`orientation`, `loopFocus`, `deselectable`, `lazyMount`, `unmountOnExit`, `ids`, and `navigate`.

Keep the Ark part tree visible. `Tabs.Indicator` is optional and should be rendered explicitly when
you want the active-trigger indicator.

### When To Go Custom

Use `asChild` on `Tabs.Trigger` when a trigger should render a link or router component. The child
must be a single semantic element that can preserve Ark's keyboard, focus, and ARIA behavior.

Use `useTabs()` with `Tabs.RootProvider` when tab state must be created outside the rendered tree.
Do not render `Tabs.Root` around the same state instance.

Use `Tabs.Context` or `useTabsContext()` when surrounding UI needs selected-value state without
adding a parallel React state layer.

### Default Props

| Prop                       | Default      | Notes                                                        |
| -------------------------- | ------------ | ------------------------------------------------------------ |
| `Tabs.variant`             | `default`    | moduix visual sugar: `default` or `line`.                    |
| `Tabs.Root.activationMode` | `automatic`  | Focus selects automatically unless set to `manual`.          |
| `Tabs.Root.orientation`    | `horizontal` | `horizontal` uses arrow left/right; `vertical` uses up/down. |
| `Tabs.Root.loopFocus`      | `true`       | Roving focus wraps around the trigger list.                  |
| `Tabs.Root.lazyMount`      | `false`      | Defers content mounting until selected.                      |
| `Tabs.Root.unmountOnExit`  | `false`      | Removes inactive content when combined with lazy paths.      |

### Behavioral Notes

- `onValueChange` follows Ark and receives `details`; read the selected value from `details.value`.
- `Tabs.Trigger` and `Tabs.Content` connect through matching string `value` props.
- `Tabs.List` no longer auto-renders an indicator. Render `Tabs.Indicator` explicitly when needed.
- Ark controls accessibility attributes, tabpanel relationships, roving focus, and disabled trigger behavior.

## Examples

### Controlled Value

Control the selected tab from React state when the active panel coordinates with other UI.

<Preview>
  <ControlledTabsExample />

  <Preview.Code>
    {`
      import { Tabs } from "@moduix/react";
      import { useState } from "react";

      export function ControlledTabsDemo() {
        const [value, setValue] = useState("projects");

        return (
          <Tabs value={value} onValueChange={(details) => setValue(details.value)}>
            <Tabs.List>
              {items.map((item) => (
                <Tabs.Trigger key={item.value} value={item.value}>
                  {item.title}
                </Tabs.Trigger>
              ))}
              <Tabs.Indicator />
            </Tabs.List>

            {items.map((item) => (
              <Tabs.Content key={item.value} value={item.value}>
                {item.content}
              </Tabs.Content>
            ))}
          </Tabs>
        );
      }
    `}

  </Preview.Code>

  <Preview.CSS>{tabsExampleCss}</Preview.CSS>
  <Preview.Data>{tabsItemsData}</Preview.Data>
</Preview>

### Disabled Tab

Disable triggers that are visible but not available yet.

<Preview>
  <DisabledTabTabsExample />

  <Preview.Code>
    {`
      import { Tabs } from "@moduix/react";

      export function DisabledTabTabsDemo() {
        return (
          <Tabs defaultValue="overview">
            <Tabs.List>
              <Tabs.Trigger value="overview">Overview</Tabs.Trigger>
              <Tabs.Trigger value="projects" disabled>
                Projects
              </Tabs.Trigger>
              <Tabs.Trigger value="account">Account</Tabs.Trigger>
            </Tabs.List>

            {items.map((item) => (
              <Tabs.Content key={item.value} value={item.value}>
                {item.content}
              </Tabs.Content>
            ))}
          </Tabs>
        );
      }
    `}

  </Preview.Code>

  <Preview.CSS>{tabsExampleCss}</Preview.CSS>
  <Preview.Data>{tabsItemsData}</Preview.Data>
</Preview>

### Indicator

Add `Tabs.Indicator` inside `Tabs.List` when you want an animated active-trigger highlight.

<Preview>
  <IndicatorTabsExample />

  <Preview.Code>
    {`
      import { Tabs } from "@moduix/react";

      export function IndicatorTabsDemo() {
        return (
          <Tabs defaultValue="overview">
            <Tabs.List>
              {items.map((item) => (
                <Tabs.Trigger key={item.value} value={item.value}>
                  {item.title}
                </Tabs.Trigger>
              ))}
              <Tabs.Indicator />
            </Tabs.List>

            {items.map((item) => (
              <Tabs.Content key={item.value} value={item.value}>
                {item.content}
              </Tabs.Content>
            ))}
          </Tabs>
        );
      }
    `}

  </Preview.Code>

  <Preview.CSS>{tabsExampleCss}</Preview.CSS>
  <Preview.Data>{tabsItemsData}</Preview.Data>
</Preview>

### Lazy Mount

Use `lazyMount` and `unmountOnExit` when inactive content should not stay in the DOM.

<Preview>
  <LazyMountTabsExample />

  <Preview.Code>
    {`
      import { Tabs } from "@moduix/react";

      export function LazyMountTabsDemo() {
        return (
          <Tabs defaultValue="overview" lazyMount unmountOnExit>
            <Tabs.List>
              {items.map((item) => (
                <Tabs.Trigger key={item.value} value={item.value}>
                  {item.title}
                </Tabs.Trigger>
              ))}
              <Tabs.Indicator />
            </Tabs.List>

            {items.map((item) => (
              <Tabs.Content key={item.value} value={item.value}>
                {item.content}
              </Tabs.Content>
            ))}
          </Tabs>
        );
      }
    `}

  </Preview.Code>

  <Preview.CSS>{tabsExampleCss}</Preview.CSS>
  <Preview.Data>{tabsItemsData}</Preview.Data>
</Preview>

### Links

Use `asChild` when triggers should render anchors or router links.

<Preview>
  <LinkTabsExample />

  <Preview.Code>
    {`
      import { Tabs } from "@moduix/react";

      export function LinkTabsDemo() {
        return (
          <Tabs defaultValue="overview">
            <Tabs.List>
              {items.map((item) => (
                <Tabs.Trigger key={item.value} value={item.value} asChild>
                  <a href={"#" + item.value}>{item.title}</a>
                </Tabs.Trigger>
              ))}
            </Tabs.List>

            {items.map((item) => (
              <Tabs.Content key={item.value} value={item.value}>
                <span id={item.value}>{item.content}</span>
              </Tabs.Content>
            ))}
          </Tabs>
        );
      }
    `}

  </Preview.Code>

  <Preview.CSS>{tabsExampleCss}</Preview.CSS>
  <Preview.Data>{tabsItemsData}</Preview.Data>
</Preview>

### Manual Activation

Set `activationMode="manual"` when arrow-key focus should move without selecting until click,
Enter, or Space.

<Preview>
  <ManualActivationTabsExample />

  <Preview.Code>
    {`
      import { Tabs } from "@moduix/react";

      export function ManualActivationTabsDemo() {
        return (
          <Tabs defaultValue="overview" activationMode="manual">
            <Tabs.List>
              {items.map((item) => (
                <Tabs.Trigger key={item.value} value={item.value}>
                  {item.title}
                </Tabs.Trigger>
              ))}
            </Tabs.List>

            {items.map((item) => (
              <Tabs.Content key={item.value} value={item.value}>
                {item.content}
              </Tabs.Content>
            ))}
          </Tabs>
        );
      }
    `}

  </Preview.Code>

  <Preview.CSS>{tabsExampleCss}</Preview.CSS>
  <Preview.Data>{tabsItemsData}</Preview.Data>
</Preview>

### Root Provider

Create state with `useTabs()` when the selected value must be read before rendering the part tree.

<Preview>
  <RootProviderTabsExample />

  <Preview.Code>
    {`
      import { Tabs, useTabs } from "@moduix/react";

      export function RootProviderTabsDemo() {
        const tabs = useTabs({ defaultValue: "overview" });

        return (
          <div>
            <output>selected: {tabs.value}</output>
            <Tabs.RootProvider value={tabs}>
              <Tabs.List>
                {items.map((item) => (
                  <Tabs.Trigger key={item.value} value={item.value}>
                    {item.title}
                  </Tabs.Trigger>
                ))}
              </Tabs.List>

              {items.map((item) => (
                <Tabs.Content key={item.value} value={item.value}>
                  {item.content}
                </Tabs.Content>
              ))}
            </Tabs.RootProvider>
          </div>
        );
      }
    `}

  </Preview.Code>

  <Preview.CSS>{tabsExampleCss}</Preview.CSS>
  <Preview.Data>{tabsItemsData}</Preview.Data>
</Preview>

### Vertical

Use `orientation="vertical"` when the trigger list should sit beside the active content.

<Preview>
  <VerticalTabsExample />

  <Preview.Code>
    {`
      import { Tabs } from "@moduix/react";

      export function VerticalTabsDemo() {
        return (
          <Tabs defaultValue="overview" orientation="vertical">
            <Tabs.List>
              {items.map((item) => (
                <Tabs.Trigger key={item.value} value={item.value}>
                  {item.title}
                </Tabs.Trigger>
              ))}
            </Tabs.List>

            {items.map((item) => (
              <Tabs.Content key={item.value} value={item.value}>
                {item.content}
              </Tabs.Content>
            ))}
          </Tabs>
        );
      }
    `}

  </Preview.Code>

  <Preview.CSS>{tabsExampleCss}</Preview.CSS>
  <Preview.Data>{tabsItemsData}</Preview.Data>
</Preview>

### Line

Use moduix `variant="line"` when the indicator should sit on the active edge instead of filling the
trigger.

<Preview>
  <LineTabsExample />

  <Preview.Code>
    {`
      import { Tabs } from "@moduix/react";

      export function LineTabsDemo() {
        return (
          <Tabs defaultValue="overview" variant="line">
            <Tabs.List>
              {items.map((item) => (
                <Tabs.Trigger key={item.value} value={item.value}>
                  {item.title}
                </Tabs.Trigger>
              ))}
              <Tabs.Indicator />
            </Tabs.List>

            {items.map((item) => (
              <Tabs.Content key={item.value} value={item.value}>
                {item.content}
              </Tabs.Content>
            ))}
          </Tabs>
        );
      }
    `}

  </Preview.Code>

  <Preview.CSS>{tabsExampleCss}</Preview.CSS>
  <Preview.Data>{tabsItemsData}</Preview.Data>
</Preview>

### Custom Icons

Compose icons and labels directly inside `Tabs.Trigger`.

<Preview>
  <IconTabsExample />

  <Preview.Code>
    {`
      import { HandshakeIcon, MapIcon, PresentIcon, Tabs } from "@moduix/react";

      export function IconTabsDemo() {
        return (
          <Tabs defaultValue="overview">
            <Tabs.List>
              <Tabs.Trigger value="overview">
                <HandshakeIcon />
                <span>Overview</span>
              </Tabs.Trigger>
              <Tabs.Trigger value="projects">
                <PresentIcon />
                <span>Projects</span>
              </Tabs.Trigger>
              <Tabs.Trigger value="account">
                <MapIcon />
                <span>Account</span>
              </Tabs.Trigger>
            </Tabs.List>

            {items.map((item) => (
              <Tabs.Content key={item.value} value={item.value}>
                {item.content}
              </Tabs.Content>
            ))}
          </Tabs>
        );
      }
    `}

  </Preview.Code>

  <Preview.CSS>{tabsExampleCss}</Preview.CSS>
  <Preview.Data>{tabsItemsData}</Preview.Data>
</Preview>

### Custom Styling

Use `className`, `Tabs.Indicator`, and CSS variables when the default styling needs to fit a
lighter inline layout.

<Preview>
  <CustomStylingTabsExample />

  <Preview.Code>
    {`
      import { Tabs } from "@moduix/react";
      import styles from "./custom-styling-tabs-demo.module.css";

      export function CustomStylingTabsDemo() {
        return (
          <Tabs defaultValue="name" className={styles.inlineRoot}>
            <Tabs.List className={styles.inlineList}>
              {fields.map((field) => (
                <Tabs.Trigger key={field.value} value={field.value} className={styles.inlineTrigger}>
                  {field.label}
                </Tabs.Trigger>
              ))}
              <Tabs.Indicator className={styles.inlineIndicator} />
            </Tabs.List>
            {fields.map((field) => (
              <Tabs.Content key={field.value} value={field.value} className={styles.inlineContent}>
                <input
                  className={styles.inlineInput}
                  placeholder={field.placeholder}
                  aria-label={field.placeholder}
                />
              </Tabs.Content>
            ))}
          </Tabs>
        );
      }
    `}

  </Preview.Code>

  <Preview.CSS>{tabsCustomStylingCss}</Preview.CSS>
  <Preview.Data>
    {`const fields = [
  { value: "name", label: "Name", placeholder: "Full name" },
  { value: "email", label: "Email", placeholder: "Email" },
];`}
  </Preview.Data>
</Preview>

## Styling

Style `Tabs` through `className`, stable `data-slot="tabs-*"` hooks, Ark `data-scope` and
`data-part` attributes, `data-variant`, and the public `--tabs-*` CSS variables shown below.

### CSS Properties

<div className="not-prose">
  <Tabs items={['CSS Variables']} className="mb-0 mt-3">
    <Tab className="max-h-150 overflow-auto">
      <TabsCssPropertiesPanel />
    </Tab>
  </Tabs>
</div>

### Styling Hooks

Use the part that owns the visual concern:

- `Tabs.Root` for layout width, overall gap, orientation, and variant-level overrides
- `Tabs.List` for list chrome, borders, background, and indicator container spacing
- `Tabs.Trigger` for label layout, icon spacing, selected state, disabled state, and focus styling
- `Tabs.Indicator` for custom indicator color, size, and transition behavior
- `Tabs.Content` for content surface, padding, and focus styling

The main public hooks include `data-slot="tabs-root"`, `data-slot="tabs-root-provider"`,
`data-slot="tabs-list"`, `data-slot="tabs-trigger"`, `data-slot="tabs-indicator"`, and
`data-slot="tabs-content"`.

Useful Ark hooks include `data-scope="tabs"`, `data-part`, `data-orientation`, `data-selected`,
`data-disabled`, `data-focus`, and indicator variables `--left`, `--top`, `--width`, and `--height`.