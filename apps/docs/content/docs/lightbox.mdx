---
title: Lightbox
description: Ark UI dialog composition for image-first modal previews.
icon: Component
---

import {
  ClickToCloseLightboxExample,
  ControlledLightboxExample,
  BoundLightboxExample,
  CustomizedLightboxExample,
  FocusLightboxExample,
  GalleryLightboxExample,
  LazyMountLightboxExample,
  LightboxCssPropertiesPanel,
  LightboxExample,
  MultipleTriggersLightboxExample,
  NonModalLightboxExample,
  RootProviderLightboxExample,
  lightboxEmptyData,
  lightboxExampleCss,
  lightboxImagesData,
} from '@/components/examples/lightbox';

## API Reference

<PrimitiveReference href="https://ark-ui.com/docs/components/dialog" />

Ark UI does not ship a dedicated `lightbox` primitive. Moduix builds this component on Ark UI
`Dialog`, follows Ark's [composition](https://ark-ui.com/docs/guides/composition),
[styling](https://ark-ui.com/docs/guides/styling), and
[ref](https://ark-ui.com/docs/guides/ref) guidance, and keeps Dialog as the behavior source of
truth.

## Choosing the right component

Use `Lightbox` when the overlay is primarily about previewing image or media content.

- Choose **`Lightbox`** for zoomed image previews, product image galleries, and media-first modal
  surfaces.
- Choose **`Dialog`** when the surface contains general application UI such as forms, actions, or
  mixed content.
- Compose **`Carousel` inside `Lightbox.Gallery`** when the preview needs slides or arrows.

## Basic

Compose the explicit Ark UI dialog structure: `Lightbox`, `Lightbox.Trigger`, `Portal`,
`Lightbox.Backdrop`, `Lightbox.Positioner`, and `Lightbox.Content`. Use `asChild` with one
semantic trigger element.

<Preview>
  <LightboxExample />

  <Preview.Code>
    {`
      import { Lightbox, Portal } from "@moduix/react";

      export function LightboxDemo() {
        return (
          <Lightbox>
            <Lightbox.Trigger asChild>
              <button type="button" className="lightbox-trigger">
                <img src={images[0].thumbnail} alt={images[0].alt} />
              </button>
            </Lightbox.Trigger>
            <Portal>
              <Lightbox.Backdrop />
              <Lightbox.Positioner>
                <Lightbox.CloseIcon />
                <Lightbox.Content aria-label={images[0].alt}>
                  <Lightbox.Image src={images[0].src} alt={images[0].alt} />
                </Lightbox.Content>
              </Lightbox.Positioner>
            </Portal>
          </Lightbox>
        );
      }
    `}

  </Preview.Code>
  <Preview.CSS>{lightboxExampleCss}</Preview.CSS>
  <Preview.Data>{lightboxImagesData}</Preview.Data>
</Preview>

## Install with shadcn (optional)

<Tabs items={['npm', 'pnpm', 'bun', 'deno', 'aube']}>
  <Tab value="npm">

```bash
npx shadcn@latest add Blinks44/moduix/react-lightbox
```

  </Tab>
  <Tab value="pnpm">

```bash
pnpm dlx shadcn@latest add Blinks44/moduix/react-lightbox
```

  </Tab>
  <Tab value="bun">

```bash
bunx shadcn@latest add Blinks44/moduix/react-lightbox
```

  </Tab>
  <Tab value="deno">

```bash
deno x -A npm:shadcn@latest add Blinks44/moduix/react-lightbox
```

  </Tab>
  <Tab value="aube">

```bash
aubx shadcn@latest add Blinks44/moduix/react-lightbox
```

  </Tab>
</Tabs>

## Anatomy

```text
Lightbox.Root
├─ Lightbox.Trigger
├─ Lightbox.Bind
└─ Portal
   ├─ Lightbox.Backdrop
   └─ Lightbox.Positioner
      ├─ Lightbox.CloseTrigger or Lightbox.CloseIcon
      └─ Lightbox.Content
         ├─ Lightbox.Title
         ├─ Lightbox.Description
         └─ Lightbox.Image

Lightbox.RootProvider
└─ the same part tree connected to useLightbox()
```

| Part                         | Role                                                                           |
| ---------------------------- | ------------------------------------------------------------------------------ |
| `Lightbox` / `Lightbox.Root` | Ark Dialog root for open state, focus lifecycle, dismissal, and accessibility. |
| `Lightbox.RootProvider`      | Provider for state created with `useLightbox()`.                               |
| `Lightbox.Trigger`           | Ark trigger. Use `asChild` for custom semantic buttons or links.               |
| `Portal`                     | Renders overlay structure outside the local DOM flow.                          |
| `Lightbox.Backdrop`          | Modal backdrop with Ark `data-state` and layer variables.                      |
| `Lightbox.Positioner`        | Fixed centered overlay container.                                              |
| `Lightbox.Content`           | Actual Ark dialog content node.                                                |
| `Lightbox.Title`             | Optional accessible title.                                                     |
| `Lightbox.Description`       | Optional accessible description.                                               |
| `Lightbox.CloseTrigger`      | Low-level Ark close trigger.                                                   |
| `Lightbox.CloseIcon`         | Moduix `CloseButton.Root` sugar over Ark close trigger.                        |
| `Lightbox.Image`             | Styled native image with an `HTMLImageElement` ref.                            |
| `Lightbox.Gallery`           | Layout and styling boundary for a composed `Carousel`.                         |
| `Lightbox.Bind`              | Zero-render behavior part that binds external image markup to the root.        |
| `Lightbox.Context`           | Inline Ark dialog state reader.                                                |

## Composition

### Recommended composition

Keep the overlay structure explicit. `Lightbox.Content` is the real Ark content part; it does not
render a portal, backdrop, positioner, image, gallery, or close button for you.

Use `Lightbox.Image` for a single image. It renders a native `<img>`, accepts native image props,
and forwards its ref to the `HTMLImageElement`. Place custom media layouts directly inside
`Lightbox.Content`.

Use `Lightbox.Gallery` around `Carousel.Root` for a known image collection. It provides the
lightbox-specific viewport, arrow alignment, media sizing, and indicator layout while Carousel
continues to own all slide behavior.

Render `Lightbox.CloseIcon` as a sibling of `Lightbox.Content` inside `Lightbox.Positioner` when
you want it pinned to the viewport corner. Rendering it inside `Lightbox.Content` makes it inherit
content transforms during entry and exit motion.

### When to go custom

Render custom content directly inside `Lightbox.Content` when it is not a single media frame or a
Carousel gallery. Use `Lightbox.Bind` only when the source markup cannot render
`Lightbox.Trigger`; structured arrays should use explicit triggers and `Lightbox.Gallery`.

### Default props

| Prop                     | Default    | Notes                                                               |
| ------------------------ | ---------- | ------------------------------------------------------------------- |
| `closeOnEscape`          | `true`     | Inherited from Ark Dialog.                                          |
| `closeOnInteractOutside` | `true`     | Inherited from Ark Dialog.                                          |
| `defaultOpen`            | `false`    | Uncontrolled initial state.                                         |
| `modal`                  | `true`     | Prevents outside interaction and hides content below the lightbox.  |
| `preventScroll`          | `true`     | Locks page scroll while the lightbox is open.                       |
| `role`                   | `"dialog"` | Use `aria-label`, `Lightbox.Title`, or `ids` for accessible naming. |
| `trapFocus`              | `true`     | Keeps keyboard focus inside the lightbox while open.                |
| `lazyMount`              | `false`    | Use with `unmountOnExit` for heavy media.                           |
| `unmountOnExit`          | `false`    | Removes overlay nodes after exit.                                   |

### Behavioral notes

`onOpenChange` and `onTriggerValueChange` keep Ark detail-object callback shapes. Read
`details.open` for controlled open state and `details.value` for multiple-trigger galleries.

Use `useLightbox()` with `Lightbox.RootProvider` when controls live outside the rendered root. Use
`Lightbox.Context` or `useLightboxContext()` to read the current state from child components.

`Lightbox.Image closeOnClick` is the supported image-preview shortcut when the image itself should
dismiss the overlay. `Lightbox.Bind` is the zero-render binding helper for CMS or third-party
markup you cannot wrap with `Lightbox.Trigger`. It must be rendered inside `Lightbox` and reports
the selected image through `onImageSelect(details)`. Details contain `src`, optional `alt`, and
the source `HTMLImageElement` as `element`.

Other inherited Dialog props pass through unchanged, including `id`, `ids`, `initialFocusEl`,
`finalFocusEl`, `persistentElements`, `restoreFocus`, `present`, `immediate`,
`skipAnimationOnMount`, and outside-interaction callbacks.

## Examples

### Controlled

Use the Ark `onOpenChange(details)` callback when synchronizing lightbox state with React.

<Preview>
  <ControlledLightboxExample />

  <Preview.Code>
    {`
      import { Lightbox, Portal } from "@moduix/react";
      import { useState } from "react";

      export function ControlledLightboxDemo() {
        const [open, setOpen] = useState(false);

        return (
          <Lightbox open={open} onOpenChange={(details) => setOpen(details.open)}>
            <Lightbox.Trigger className="lightbox-button">
              Open controlled lightbox
            </Lightbox.Trigger>
            <Portal>
              <Lightbox.Backdrop />
              <Lightbox.Positioner>
                <Lightbox.CloseIcon />
                <Lightbox.Content aria-label={images[1].alt}>
                  <Lightbox.Image src={images[1].src} alt={images[1].alt} />
                </Lightbox.Content>
              </Lightbox.Positioner>
            </Portal>
          </Lightbox>
        );
      }
    `}

  </Preview.Code>
  <Preview.CSS>{lightboxExampleCss}</Preview.CSS>
  <Preview.Data>{lightboxImagesData}</Preview.Data>
</Preview>

### Multiple Triggers

Use `Trigger.value` and `onTriggerValueChange(details)` for gallery thumbnails. The component does
not maintain a hidden image registry.

<Preview>
  <MultipleTriggersLightboxExample />

  <Preview.Code>
    {`
      import { Lightbox, Portal } from "@moduix/react";
      import { useState } from "react";

      export function MultipleTriggersLightboxDemo() {
        const [activeImage, setActiveImage] = useState(images[0]);

        return (
          <Lightbox
            onTriggerValueChange={(details) => {
              setActiveImage(images.find((image) => image.id === details.value) ?? images[0]);
            }}
          >
            <div className="lightbox-gallery">
              {images.map((image) => (
                <Lightbox.Trigger key={image.id} value={image.id} asChild>
                  <button type="button" className="lightbox-gallery-trigger">
                    <img src={image.thumbnail} alt={image.alt} />
                  </button>
                </Lightbox.Trigger>
              ))}
            </div>
            <Portal>
              <Lightbox.Backdrop />
              <Lightbox.Positioner>
                <Lightbox.CloseIcon />
                <Lightbox.Content aria-label={activeImage.alt}>
                  <Lightbox.Image src={activeImage.src} alt={activeImage.alt} />
                </Lightbox.Content>
              </Lightbox.Positioner>
            </Portal>
          </Lightbox>
        );
      }
    `}

  </Preview.Code>
  <Preview.CSS>{lightboxExampleCss}</Preview.CSS>
  <Preview.Data>{lightboxImagesData}</Preview.Data>
</Preview>

### Gallery

When a CMS or API gives you a structured array of images, keep that array in React state and
compose `Carousel` inside `Lightbox.Gallery`. Use `Lightbox.Trigger value` to choose the initial
slide and keep `Carousel` page state in sync as the user navigates.

<Preview>
  <GalleryLightboxExample />

  <Preview.Code>
    {`
      import { Carousel, Lightbox, Portal } from "@moduix/react";
      import { useState } from "react";

      export function GalleryLightboxDemo() {
        const [activeIndex, setActiveIndex] = useState(0);
        const activeImage = images[activeIndex] ?? images[0];

        return (
          <Lightbox
            onTriggerValueChange={(details) => {
              const nextIndex = images.findIndex((image) => image.id === details.value);
              setActiveIndex(nextIndex >= 0 ? nextIndex : 0);
            }}
          >
            <div className="lightbox-gallery">
              {images.map((image) => (
                <Lightbox.Trigger key={image.id} value={image.id} asChild>
                  <button type="button" className="lightbox-gallery-trigger">
                    <img src={image.thumbnail} alt={image.alt} />
                  </button>
                </Lightbox.Trigger>
              ))}
            </div>

            <Portal>
              <Lightbox.Backdrop />
              <Lightbox.Positioner>
                <Lightbox.CloseIcon />
                <Lightbox.Content aria-label={activeImage.alt}>
                  <Lightbox.Gallery>
                    <Carousel.Root
                      page={activeIndex}
                      onPageChange={(details) => setActiveIndex(details.page)}
                      slideCount={images.length}
                    >
                      <Carousel.Control>
                        <Carousel.PrevTrigger />
                        <Carousel.ItemGroup aria-label="Server-driven image carousel">
                          {images.map((image, index) => (
                            <Carousel.Item key={image.id} index={index}>
                              <img src={image.src} alt={image.alt} />
                            </Carousel.Item>
                          ))}
                        </Carousel.ItemGroup>
                        <Carousel.NextTrigger />
                      </Carousel.Control>

                      <Carousel.IndicatorGroup>
                        {images.map((image, index) => (
                          <Carousel.Indicator key={image.id} index={index}>
                            <img src={image.thumbnail} alt="" />
                          </Carousel.Indicator>
                        ))}
                      </Carousel.IndicatorGroup>
                    </Carousel.Root>
                  </Lightbox.Gallery>
                </Lightbox.Content>
              </Lightbox.Positioner>
            </Portal>
          </Lightbox>
        );
      }
    `}

  </Preview.Code>
  <Preview.CSS>{lightboxExampleCss}</Preview.CSS>
  <Preview.Data>{lightboxImagesData}</Preview.Data>
</Preview>

### Bind CMS Content

Use `Lightbox.Bind` when previewable images come from CMS or third-party markup that you do not
render directly with `Lightbox.Trigger`. Point it at a container with `rootRef` or `rootSelector`,
choose a `selector`, and handle the resolved `{ src, alt, element }` through `onImageSelect`.
`Bind` opens its parent Lightbox but renders no overlay structure, so backdrop, content, close
controls, and image layout remain fully composable. It reads `data-lightbox-src`, then `currentSrc`,
then `src`, and preloads that source on pointer hover or keyboard focus. Use a semantic button or
link as the matched `selector` so native keyboard activation produces the click that opens the
Lightbox. If your server already returns a real image array, prefer the
`Lightbox.Gallery + Carousel` composition above.

<Preview>
  <BoundLightboxExample />

  <Preview.Code>
    {`
      import { Lightbox, Portal } from "@moduix/react";
      import type { LightboxImageSelectDetails } from "@moduix/react";
      import { useRef, useState } from "react";

      export function CmsLightboxDemo() {
        const rootRef = useRef<HTMLDivElement | null>(null);
        const [activeImage, setActiveImage] = useState(
          null as LightboxImageSelectDetails | null
        );

        return (
          <>
            <div ref={rootRef} className="lightbox-gallery">
              {images.map((image) => (
                <button key={image.id} type="button" className="lightbox-gallery-trigger">
                  <img src={image.thumbnail} data-lightbox-src={image.src} alt={image.alt} />
                </button>
              ))}
            </div>

            <Lightbox lazyMount unmountOnExit>
              <Lightbox.Bind
                rootRef={rootRef}
                selector="button"
                onImageSelect={setActiveImage}
              />
              <Portal>
                <Lightbox.Backdrop />
                <Lightbox.Positioner>
                  <Lightbox.CloseIcon />
                  <Lightbox.Content aria-label={activeImage?.alt ?? "Image preview"}>
                    {activeImage ? (
                      <Lightbox.Image src={activeImage.src} alt={activeImage.alt ?? ""} />
                    ) : null}
                  </Lightbox.Content>
                </Lightbox.Positioner>
              </Portal>
            </Lightbox>
          </>
        );
      }
    `}

  </Preview.Code>
  <Preview.CSS>{lightboxExampleCss}</Preview.CSS>
  <Preview.Data>{lightboxImagesData}</Preview.Data>
</Preview>

### Focus and IDs

Use Ark `initialFocusEl`, `finalFocusEl`, and `ids` when a lightbox needs explicit focus targets or
stable element ids for external accessibility composition.

<Preview>
  <FocusLightboxExample />

  <Preview.Code>
    {`
      import { Lightbox, Portal } from "@moduix/react";
      import { useRef } from "react";

      export function FocusLightboxDemo() {
        const closeRef = useRef<HTMLButtonElement | null>(null);
        const triggerRef = useRef<HTMLButtonElement | null>(null);

        return (
          <>
            <button ref={triggerRef} type="button" className="lightbox-button">
              Focus returns here
            </button>
            <Lightbox
              initialFocusEl={() => closeRef.current}
              finalFocusEl={() => triggerRef.current}
              ids={{
                content: "lightbox-focus-content",
                title: "lightbox-focus-title",
              }}
            >
              <Lightbox.Trigger className="lightbox-button">
                Open focus-managed lightbox
              </Lightbox.Trigger>
              <Portal>
                <Lightbox.Backdrop />
                <Lightbox.Positioner>
                  <Lightbox.CloseIcon ref={closeRef} />
                  <Lightbox.Content>
                    <Lightbox.Title className="lightbox-status">
                      Mountain ridge at sunset
                    </Lightbox.Title>
                    <Lightbox.Image src={images[0].src} alt={images[0].alt} />
                  </Lightbox.Content>
                </Lightbox.Positioner>
              </Portal>
            </Lightbox>
          </>
        );
      }
    `}

  </Preview.Code>
  <Preview.CSS>{lightboxExampleCss}</Preview.CSS>
  <Preview.Data>{lightboxImagesData}</Preview.Data>
</Preview>

### Non Modal

Set `modal={false}` for rare previews that should leave the page interactive. Pair it with
`trapFocus={false}` and `preventScroll={false}` when the background needs to remain reachable.

<Preview>
  <NonModalLightboxExample />

  <Preview.Code>
    {`
      import { Lightbox, Portal } from "@moduix/react";

      export function NonModalLightboxDemo() {
        return (
          <>
            <Lightbox modal={false} trapFocus={false} preventScroll={false}>
              <Lightbox.Trigger className="lightbox-button">
                Open non-modal lightbox
              </Lightbox.Trigger>
              <Portal>
                <Lightbox.Backdrop />
                <Lightbox.Positioner>
                  <Lightbox.CloseIcon />
                  <Lightbox.Content aria-label={images[2].alt}>
                    <Lightbox.Image src={images[2].src} alt={images[2].alt} />
                  </Lightbox.Content>
                </Lightbox.Positioner>
              </Portal>
            </Lightbox>
            <button type="button" className="lightbox-button">
              Background action
            </button>
          </>
        );
      }
    `}

  </Preview.Code>
  <Preview.CSS>{lightboxExampleCss}</Preview.CSS>
  <Preview.Data>{lightboxImagesData}</Preview.Data>
</Preview>

### Root Provider and Context

Create state with `useLightbox()` when the opener is outside the root. Read inline state with
`Lightbox.Context`.

<Preview>
  <RootProviderLightboxExample />

  <Preview.Code>
    {`
      import { Lightbox, Portal, useLightbox } from "@moduix/react";

      export function LightboxStateDemo() {
        const lightbox = useLightbox();

        return (
          <>
            <button type="button" onClick={() => lightbox.setOpen(true)}>
              Lightbox is {lightbox.open ? "open" : "closed"}
            </button>
            <Lightbox.RootProvider value={lightbox}>
              <Portal>
                <Lightbox.Backdrop />
                <Lightbox.Positioner>
                  <Lightbox.CloseIcon />
                  <Lightbox.Content aria-label={images[2].alt}>
                    <Lightbox.Image src={images[2].src} alt={images[2].alt} />
                    <Lightbox.Context>
                      {(state) => <>Preview is {state.open ? "open" : "closed"}</>}
                    </Lightbox.Context>
                  </Lightbox.Content>
                </Lightbox.Positioner>
              </Portal>
            </Lightbox.RootProvider>
          </>
        );
      }
    `}

  </Preview.Code>
  <Preview.CSS>{lightboxExampleCss}</Preview.CSS>
  <Preview.Data>{lightboxImagesData}</Preview.Data>
</Preview>

### Lazy Mount

Use Ark presence props for heavy media. `lazyMount` delays rendering until the first open, and
`unmountOnExit` removes the overlay after close animations finish.

<Preview>
  <LazyMountLightboxExample />

  <Preview.Code>
    {`
      import { Lightbox, Portal } from "@moduix/react";

      export function LazyLightboxDemo() {
        return (
          <Lightbox lazyMount unmountOnExit>
            <Lightbox.Trigger className="lightbox-button">Open lazy lightbox</Lightbox.Trigger>
            <Portal>
              <Lightbox.Backdrop />
              <Lightbox.Positioner>
                <Lightbox.CloseIcon />
                <Lightbox.Content aria-label={images[0].alt}>
                  <Lightbox.Image src={images[0].src} alt={images[0].alt} />
                </Lightbox.Content>
              </Lightbox.Positioner>
            </Portal>
          </Lightbox>
        );
      }
    `}

  </Preview.Code>
  <Preview.CSS>{lightboxExampleCss}</Preview.CSS>
  <Preview.Data>{lightboxImagesData}</Preview.Data>
</Preview>

### Click To Close Image

Use `Lightbox.Image closeOnClick` when clicking the previewed image should dismiss the overlay. The
close icon remains available and keeps the same Ark close semantics.

<Preview>
  <ClickToCloseLightboxExample />

  <Preview.Code>
    {`
      import { Lightbox, Portal } from "@moduix/react";

      export function ClickToCloseLightboxDemo() {
        return (
          <Lightbox>
            <Lightbox.Trigger className="lightbox-button">
              Open click-to-close lightbox
            </Lightbox.Trigger>
            <Portal>
              <Lightbox.Backdrop />
              <Lightbox.Positioner>
                <Lightbox.CloseIcon />
                <Lightbox.Content aria-label={images[1].alt}>
                  <Lightbox.Image src={images[1].src} alt={images[1].alt} closeOnClick />
                </Lightbox.Content>
              </Lightbox.Positioner>
            </Portal>
          </Lightbox>
        );
      }
    `}

  </Preview.Code>
  <Preview.CSS>{lightboxExampleCss}</Preview.CSS>
  <Preview.Data>{lightboxImagesData}</Preview.Data>
</Preview>

### Custom Styling

Customize the backdrop, content sizing, media radius, and close icon through classes and
`--lightbox-*` variables while keeping Ark parts intact.

<Preview>
  <CustomizedLightboxExample />

  <Preview.Code>
    {`
      import { Lightbox, Portal } from "@moduix/react";

      export function StyledLightboxDemo() {
        return (
          <Lightbox>
            <Lightbox.Trigger className="lightbox-button">Open styled lightbox</Lightbox.Trigger>
            <Portal>
              <Lightbox.Backdrop className="lightbox-custom-backdrop" />
              <Lightbox.Positioner>
                <Lightbox.CloseIcon className="lightbox-custom-close" />
                <Lightbox.Content className="lightbox-custom-content" aria-label={images[1].alt}>
                  <Lightbox.Image src={images[1].src} alt={images[1].alt} />
                </Lightbox.Content>
              </Lightbox.Positioner>
            </Portal>
          </Lightbox>
        );
      }
    `}

  </Preview.Code>
  <Preview.CSS>{lightboxExampleCss}</Preview.CSS>
  <Preview.Data>{lightboxImagesData}</Preview.Data>
</Preview>

## Styling

### CSS Properties

<div className="not-prose">
  <Tabs items={['CSS Variables']} className="mb-0 mt-3">
    <Tab className="max-h-150 overflow-auto">
      <LightboxCssPropertiesPanel />
    </Tab>
  </Tabs>
</div>

### Styling hooks

- Moduix slots: `lightbox-trigger`, `lightbox-backdrop`, `lightbox-positioner`,
  `lightbox-content`, `lightbox-title`, `lightbox-description`, `lightbox-close-trigger`,
  `lightbox-close-icon`, `lightbox-image`, and `lightbox-gallery`.
- Ark state and anatomy hooks: `data-scope="dialog"`, `data-part`, `data-state="open|closed"`,
  `data-nested`, `data-has-nested`, `--layer-index`, and `--nested-layer-count`.
- Local hooks: `Lightbox.Image` exposes `data-slot="lightbox-image"` and optional
  `data-close-on-click`; `Lightbox.Gallery` exposes `data-slot="lightbox-gallery"` and styles
  nested Carousel slots.
- Motion is driven by Ark `data-state`; use `present` only when JavaScript animation control needs
  to keep the content mounted.