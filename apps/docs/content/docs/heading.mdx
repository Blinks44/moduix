---
title: Heading
description: A semantic heading primitive with Ark factory composition and moduix typography tokens.
icon: Component
---

import {
  CustomStylingHeadingExample,
  HeadingCssPropertiesPanel,
  HeadingElementsExample,
  HeadingExample,
  HeadingSemanticLevelExample,
  HeadingSizesExample,
  HeadingWeightsExample,
} from '@/components/examples/heading';

## API Reference

Ark UI has no dedicated heading primitive. `Heading` follows the
[Ark composition and factory model](https://ark-ui.com/docs/guides/composition).

## Basic

`Heading` renders an `h1` by default. Use it for page titles and keep the semantic heading level
tied to the document outline.

<Preview>
  <HeadingExample />

  <Preview.Code>
    {`
      import { Heading } from "@moduix/react";
      import styles from "./heading.module.css";

      export function HeadingDemo() {
        return <Heading className={styles.demo}>{headingText}</Heading>;
      }
    `}

  </Preview.Code>

  <Preview.CSS>
    {`
      .demo {
        max-width: 28rem;
      }
    `}

  </Preview.CSS>

  <Preview.Data>
    {`
      const headingText = "Build reliable interfaces";
    `}

  </Preview.Data>
</Preview>

## Install with shadcn (optional)

Install the source component from the moduix registry:

<Tabs items={['npm', 'pnpm', 'bun', 'deno', 'aube']}>
  <Tab value="npm">

```bash
npx shadcn@latest add Blinks44/moduix/react-heading
```

  </Tab>
  <Tab value="pnpm">

```bash
pnpm dlx shadcn@latest add Blinks44/moduix/react-heading
```

  </Tab>
  <Tab value="bun">

```bash
bunx shadcn@latest add Blinks44/moduix/react-heading
```

  </Tab>
  <Tab value="deno">

```bash
deno x -A npm:shadcn@latest add Blinks44/moduix/react-heading
```

  </Tab>
  <Tab value="aube">

```bash
aubx shadcn@latest add Blinks44/moduix/react-heading
```

  </Tab>
</Tabs>

## Anatomy

`Heading` is a root-only component. `Heading.Root` is the equivalent Ark-aligned namespace form.

```text
Heading / Heading.Root
└─ text or inline content
```

| Part                       | Role                                                    |
| -------------------------- | ------------------------------------------------------- |
| `Heading` / `Heading.Root` | Native semantic heading with moduix typography styling. |

## Composition

### Recommended composition

Use `<Heading>` for an `h1`. For another semantic level, use Ark factory `asChild` with exactly one
native heading child:

```tsx
<Heading asChild size="2xl">
  <h2>Page title rendered as h2</h2>
</Heading>
```

The child owns the DOM node while receiving the component props, class name, data attributes, and
forwarded ref.

### When to go custom

Use `className` and the public `--heading-*` variables for visual overrides. Use `Text` instead when
the content is body copy or general typography rather than a document heading.

### Default props

| Entry     | Default             | Values / Notes                          |
| --------- | ------------------- | --------------------------------------- |
| element   | `h1`                | Use `asChild` for `h2` through `h6`     |
| `size`    | by rendered element | `xs`, `sm`, `md`, `lg`, `xl`, `2xl`     |
| `weight`  | `semibold`          | `regular`, `medium`, `semibold`, `bold` |
| `asChild` | `false`             | Ark factory composition                 |

### Behavioral notes

- Default visual sizes follow the rendered element: `h1 -> 2xl`, `h2 -> xl`, `h3 -> lg`,
  `h4 -> md`, `h5 -> sm`, and `h6 -> xs`.
- An explicit `size` overrides the element-based default without changing semantics.
- The root forwards its ref to the rendered heading element.
- Native headings need no additional ARIA. Do not use a non-heading child with `asChild`.
- `Heading` is stateless and has no callbacks, keyboard behavior, providers, or context hooks.

## Examples

### Semantic Level and Visual Size

Keep document structure and visual emphasis independent by composing a semantic child and applying
an explicit visual size.

<Preview>
  <HeadingSemanticLevelExample />

  <Preview.Code>
    {`
      import { Heading } from "@moduix/react";
      import styles from "./heading.module.css";

      export function SemanticHeadingDemo() {
        return (
          <Heading asChild size="2xl" className={styles.demo}>
            <h2>{headingText}</h2>
          </Heading>
        );
      }
    `}

  </Preview.Code>

  <Preview.CSS>
    {`
      .demo {
        max-width: 28rem;
      }
    `}

  </Preview.CSS>

  <Preview.Data>
    {`
      const headingText = "Page title rendered as h2";
    `}

  </Preview.Data>
</Preview>

### Heading Levels

Use `asChild` for `h2` through `h6`. The default visual scale follows the actual heading element.

<Preview>
  <HeadingElementsExample />

  <Preview.Code>
    {`
      import { Heading } from "@moduix/react";
      import styles from "./heading.module.css";

      export function HeadingLevelsDemo() {
        return (
          <div className={styles.stack}>
            <Heading>{headingLevels[0].label}</Heading>
            <Heading asChild><h2>{headingLevels[1].label}</h2></Heading>
            <Heading asChild><h3>{headingLevels[2].label}</h3></Heading>
            <Heading asChild><h4>{headingLevels[3].label}</h4></Heading>
            <Heading asChild><h5>{headingLevels[4].label}</h5></Heading>
            <Heading asChild><h6>{headingLevels[5].label}</h6></Heading>
          </div>
        );
      }
    `}

  </Preview.Code>

  <Preview.CSS>
    {`
      .stack {
        display: flex;
        width: fit-content;
        max-width: min(32rem, calc(100vw - var(--spacing-8)));
        flex-direction: column;
        gap: var(--spacing-3);
      }
    `}

  </Preview.CSS>

  <Preview.Data>
    {`
      const headingLevels = [
        { level: 1, label: "Heading level 1" },
        { level: 2, label: "Heading level 2" },
        { level: 3, label: "Heading level 3" },
        { level: 4, label: "Heading level 4" },
        { level: 5, label: "Heading level 5" },
        { level: 6, label: "Heading level 6" },
      ];
    `}

  </Preview.Data>
</Preview>

### Visual Sizes

Use `size` when multiple headings share one semantic level but need different visual emphasis.

<Preview>
  <HeadingSizesExample />

  <Preview.Code>
    {`
      import { Heading } from "@moduix/react";
      import styles from "./heading.module.css";

      export function HeadingSizesDemo() {
        return (
          <div className={styles.stack}>
            {headingSizes.map((item) => (
              <Heading key={item.size} asChild size={item.size}>
                <h2>{item.label}</h2>
              </Heading>
            ))}
          </div>
        );
      }
    `}

  </Preview.Code>

  <Preview.CSS>
    {`
      .stack {
        display: flex;
        width: fit-content;
        max-width: min(32rem, calc(100vw - var(--spacing-8)));
        flex-direction: column;
        gap: var(--spacing-3);
      }
    `}

  </Preview.CSS>

  <Preview.Data>
    {`
      const headingSizes = [
        { size: "2xl", label: "Extra-large heading" },
        { size: "xl", label: "Large heading" },
        { size: "lg", label: "Medium-large heading" },
        { size: "md", label: "Medium heading" },
        { size: "sm", label: "Small heading" },
        { size: "xs", label: "Extra-small heading" },
      ];
    `}

  </Preview.Data>
</Preview>

### Weights

Use `weight` for the built-in typography emphasis presets.

<Preview>
  <HeadingWeightsExample />

  <Preview.Code>
    {`
      import { Heading } from "@moduix/react";
      import styles from "./heading.module.css";

      export function HeadingWeightsDemo() {
        return (
          <div className={styles.stack}>
            {headingWeights.map((item) => (
              <Heading key={item.weight} asChild weight={item.weight}>
                <h2>{item.label}</h2>
              </Heading>
            ))}
          </div>
        );
      }
    `}

  </Preview.Code>

  <Preview.CSS>
    {`
      .stack {
        display: flex;
        width: fit-content;
        max-width: min(32rem, calc(100vw - var(--spacing-8)));
        flex-direction: column;
        gap: var(--spacing-3);
      }
    `}

  </Preview.CSS>

  <Preview.Data>
    {`
      const headingWeights = [
        { weight: "regular", label: "Regular weight" },
        { weight: "medium", label: "Medium weight" },
        { weight: "semibold", label: "Semibold weight" },
        { weight: "bold", label: "Bold weight" },
      ];
    `}

  </Preview.Data>
</Preview>

### Custom Styling

Pass `className` to override public typography variables on one heading.

<Preview>
  <CustomStylingHeadingExample />

  <Preview.Code>
    {`
      import { Heading } from "@moduix/react";
      import styles from "./heading.module.css";

      export function CustomHeadingDemo() {
        return (
          <Heading asChild className={styles.custom}>
            <h2>{headingText}</h2>
          </Heading>
        );
      }
    `}

  </Preview.Code>

  <Preview.CSS>
    {`
      .custom {
        --heading-color: var(--color-primary);
        --heading-font-size-xl: var(--text-3xl);
        --heading-line-height-xl: var(--line-height-text-3xl);
        --heading-font-weight-semibold: var(--weight-bold);
      }
    `}

  </Preview.CSS>

  <Preview.Data>
    {`
      const headingText = "Customized heading";
    `}

  </Preview.Data>
</Preview>

## Styling

### CSS Properties

<div className="not-prose">
  <Tabs items={['CSS Variables']} className="mb-0 mt-3">
    <Tab className="max-h-150 overflow-auto">
      <HeadingCssPropertiesPanel />
    </Tab>
  </Tabs>
</div>

### Styling hooks

- `data-scope="heading"` identifies the Ark-aligned component scope.
- `data-part="root"` identifies the single root part.
- `data-slot="heading-root"` is the stable moduix selector.
- `data-size` is present when `size` is explicitly set.
- `data-weight` reflects `regular`, `medium`, `semibold`, or `bold`.
- Use `className` and `--heading-*` variables for scoped visual overrides.