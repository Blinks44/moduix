---
title: Checkbox
description: A composable checkbox primitive for standalone and grouped selection.
icon: Component
---

import {
  CheckboxContextExample,
  CheckboxCssPropertiesPanel,
  CheckboxDefaultCheckedExample,
  CheckboxDisabledExample,
  CheckboxExample,
  CheckboxGroupExample,
  CheckboxGroupFieldsetExample,
  CheckboxGroupInvalidExample,
  CheckboxGroupMaxSelectedExample,
  CheckboxGroupProviderExample,
  CheckboxGroupSelectAllExample,
  CheckboxGroupWithFormExample,
  CheckboxIndeterminateExample,
  CheckboxReadOnlyExample,
  CheckboxRootProviderExample,
  CheckboxSizesExample,
  CheckboxWithFieldExample,
  CheckboxWithFormExample,
  ControlledCheckboxExample,
  ControlledCheckboxGroupExample,
  CustomIndicatorCheckboxExample,
  CustomStylingCheckboxExample,
  checkboxCustomStylingCss,
  checkboxExampleCss,
  checkboxLabelsData,
  frameworkOptionsData,
  frameworkOptionsWithSvelteData,
  notificationOptionsData,
} from '@/components/examples/checkbox';

## API Reference

<PrimitiveReference href="https://ark-ui.com/docs/components/checkbox" />

## Basic

Use the Ark part tree directly: `Checkbox.Root`, `Checkbox.Control`, `Checkbox.Indicator`,
`Checkbox.Label`, and `Checkbox.HiddenInput`.

<Preview>
  <CheckboxExample />

  <Preview.Code>
    {`
      import { Checkbox } from "moduix";

      export function CheckboxDemo() {
        return (
          <Checkbox.Root>
            <Checkbox.Control>
              <Checkbox.Indicator />
            </Checkbox.Control>
            <Checkbox.Label>Enable notifications</Checkbox.Label>
            <Checkbox.HiddenInput />
          </Checkbox.Root>
        );
      }
    `}

  </Preview.Code>
  <Preview.CSS>{checkboxExampleCss}</Preview.CSS>
  <Preview.Data>{checkboxLabelsData}</Preview.Data>
</Preview>

## Install with shadcn (optional)

If you want `Checkbox` to become part of your own project source instead of staying in
`node_modules`, install it from the moduix GitHub registry:

<Tabs items={['npm', 'pnpm', 'bun', 'deno', 'aube']}>
  <Tab value="npm">

```bash
npx shadcn@latest add Blinks44/moduix/checkbox
```

  </Tab>
  <Tab value="pnpm">

```bash
pnpm dlx shadcn@latest add Blinks44/moduix/checkbox
```

  </Tab>
  <Tab value="bun">

```bash
bunx shadcn@latest add Blinks44/moduix/checkbox
```

  </Tab>
  <Tab value="deno">

```bash
deno x -A npm:shadcn@latest add Blinks44/moduix/checkbox
```

  </Tab>
  <Tab value="aube">

```bash
aubx shadcn@latest add Blinks44/moduix/checkbox
```

  </Tab>
</Tabs>

## Anatomy

```text
Checkbox.Root
├─ Checkbox.Control
│  ├─ Checkbox.Indicator
│  └─ Checkbox.Indicator[indeterminate] (optional)
├─ Checkbox.Label
├─ Checkbox.HiddenInput
└─ Checkbox.Context (optional render-prop access)

Checkbox.RootProvider
└─ same part tree connected to useCheckbox()

Checkbox.Group
└─ Checkbox.Root[value]
   ├─ Checkbox.Control
   ├─ Checkbox.Label
   └─ Checkbox.HiddenInput

Checkbox.GroupProvider
└─ same grouped item tree connected to useCheckboxGroup()
```

| Part                     | Role                                                                  |
| ------------------------ | --------------------------------------------------------------------- |
| `Checkbox.Root`          | Owns one checkbox state and renders the wrapping label element.       |
| `Checkbox.RootProvider`  | Connects parts to state created with `useCheckbox()`.                 |
| `Checkbox.Control`       | Visual control surface. Styles checked, indeterminate, invalid state. |
| `Checkbox.Indicator`     | Checked or indeterminate icon slot. Use `indeterminate` when needed.  |
| `Checkbox.Label`         | Visible label text inside the root.                                   |
| `Checkbox.HiddenInput`   | Native input for forms, validation, form reset, and submission.       |
| `Checkbox.Context`       | Reads checkbox state through an Ark render prop.                      |
| `Checkbox.Group`         | Shares value state across multiple checkbox roots.                    |
| `Checkbox.GroupProvider` | Connects grouped items to state created with `useCheckboxGroup()`.    |

## Composition

### Recommended path

Keep the Ark part tree visible. `Checkbox.Root` accepts Ark root props such as `checked`,
`defaultChecked`, `onCheckedChange(details)`, `disabled`, `readOnly`, `invalid`, `required`,
`name`, `value`, `form`, `ids`, and `asChild`.

`Checkbox.Group` accepts `value`, `defaultValue`, `onValueChange(value)`, `disabled`, `readOnly`,
`invalid`, `maxSelectedValues`, and `name`.

`size` is the only moduix DX prop. Apply it on `Checkbox.Root` or `Checkbox.RootProvider`.

### Form controls

Render `Checkbox.HiddenInput` for native form participation, validation, form reset, and
submission. For a standalone checkbox, pass `name` and `value` to `Checkbox.Root`. For a group,
pass `name` to `Checkbox.Group` and `value` to every `Checkbox.Root`.

### Custom host elements

Ark `Checkbox.Root` renders a `label`. If you use `asChild`, the direct child must also be a
semantic `label`, otherwise the checkbox no longer has valid form-control labelling.

## Examples

### Default Checked

Use `defaultChecked` when the checkbox should start checked and then manage its own state.

<Preview>
  <CheckboxDefaultCheckedExample />

  <Preview.Code>
    {`
      <Checkbox.Root defaultChecked>
        <Checkbox.Control>
          <Checkbox.Indicator />
        </Checkbox.Control>
        <Checkbox.Label>Enable notifications</Checkbox.Label>
        <Checkbox.HiddenInput />
      </Checkbox.Root>
    `}
  </Preview.Code>
  <Preview.CSS>{checkboxExampleCss}</Preview.CSS>
  <Preview.Data>{checkboxLabelsData}</Preview.Data>
</Preview>

### Controlled

Control `checked` from React state when the checkbox should coordinate with other UI or derived logic.

<Preview>
  <ControlledCheckboxExample />

  <Preview.Code>
    {`
      import { Checkbox } from "moduix";
      import { useState } from "react";

      export function ControlledCheckboxDemo() {
        const [checked, setChecked] = useState(true);

        return (
          <Checkbox.Root checked={checked} onCheckedChange={(details) => setChecked(details.checked === true)}>
            <Checkbox.Control>
              <Checkbox.Indicator />
            </Checkbox.Control>
            <Checkbox.Label>{checked ? "Enabled" : "Disabled"}</Checkbox.Label>
            <Checkbox.HiddenInput />
          </Checkbox.Root>
        );
      }
    `}

  </Preview.Code>
  <Preview.CSS>{checkboxExampleCss}</Preview.CSS>
  <Preview.Data>{`const initialChecked = true;`}</Preview.Data>
</Preview>

### Root Provider

Use `useCheckbox()` with `Checkbox.RootProvider` when checkbox state needs to live outside the rendered subtree.

<Preview>
  <CheckboxRootProviderExample />

  <Preview.Code>
    {`
      import { Checkbox, useCheckbox } from "moduix";

      export function RootProviderCheckboxDemo() {
        const checkbox = useCheckbox({ defaultChecked: true });

        return (
          <>
            <Checkbox.RootProvider value={checkbox}>
              <Checkbox.Control>
                <Checkbox.Indicator />
              </Checkbox.Control>
              <Checkbox.Label>Managed outside the tree</Checkbox.Label>
              <Checkbox.HiddenInput />
            </Checkbox.RootProvider>
            <button type="button" onClick={() => checkbox.setChecked(!checkbox.checked)}>
              {checkbox.checked ? "Uncheck" : "Check"}
            </button>
          </>
        );
      }
    `}

  </Preview.Code>
  <Preview.CSS>{checkboxExampleCss}</Preview.CSS>
  <Preview.Data>{`const initialChecked = true;`}</Preview.Data>
</Preview>

### Disabled

Use `disabled` when the option is unavailable and should not accept focus or input.

<Preview>
  <CheckboxDisabledExample />

  <Preview.Code>
    {`
      <Checkbox.Root disabled>
        <Checkbox.Control>
          <Checkbox.Indicator />
        </Checkbox.Control>
        <Checkbox.Label>Receive weekly summary</Checkbox.Label>
        <Checkbox.HiddenInput />
      </Checkbox.Root>
    `}
  </Preview.Code>
  <Preview.CSS>{checkboxExampleCss}</Preview.CSS>
  <Preview.Data>{checkboxLabelsData}</Preview.Data>
</Preview>

### Read Only

Use `readOnly` when the value should remain visible but user interaction should not change it.

<Preview>
  <CheckboxReadOnlyExample />

  <Preview.Code>
    {`
      <Checkbox.Root readOnly defaultChecked>
        <Checkbox.Control>
          <Checkbox.Indicator />
        </Checkbox.Control>
        <Checkbox.Label>Preserve existing setting</Checkbox.Label>
        <Checkbox.HiddenInput />
      </Checkbox.Root>
    `}
  </Preview.Code>
  <Preview.CSS>{checkboxExampleCss}</Preview.CSS>
  <Preview.Data>{checkboxLabelsData}</Preview.Data>
</Preview>

### Indeterminate

Use the indeterminate state when one checkbox reflects a partial selection instead of a simple on or off value.

<Preview>
  <CheckboxIndeterminateExample />

  <Preview.Code>
    {`
      <Checkbox.Root checked="indeterminate">
        <Checkbox.Control>
          <Checkbox.Indicator />
          <Checkbox.Indicator indeterminate />
        </Checkbox.Control>
        <Checkbox.Label>Select all team members</Checkbox.Label>
        <Checkbox.HiddenInput />
      </Checkbox.Root>
    `}
  </Preview.Code>
  <Preview.CSS>{checkboxExampleCss}</Preview.CSS>
  <Preview.Data>{`const checked = 'indeterminate';`}</Preview.Data>
</Preview>

### With Field

Use `Field` around one checkbox when it needs helper text, validation text, or shared field state.

<Preview>
  <CheckboxWithFieldExample />

  <Preview.Code>
    {`
      import { Checkbox, Field } from "moduix";

      export function CheckboxFieldDemo() {
        return (
          <Field className="checkbox-field">
            <Checkbox.Root required >
              <Checkbox.Control>
                <Checkbox.Indicator />
              </Checkbox.Control>
              <Checkbox.Label>Accept terms</Checkbox.Label>
              <Checkbox.HiddenInput />
            </Checkbox.Root>
            <Field.HelperText>Required to continue.</Field.HelperText>
            <Field.ErrorText>Please accept the terms.</Field.ErrorText>
          </Field>
        );
      }
    `}

  </Preview.Code>
  <Preview.CSS>{checkboxExampleCss}</Preview.CSS>
  <Preview.Data>{`const fieldName = 'terms';`}</Preview.Data>
</Preview>

### With Form

Pass `name` and `value` to `Checkbox.Root` so the hidden input submits with native `FormData`.

<Preview>
  <CheckboxWithFormExample />

  <Preview.Code>
    {`
      <form onSubmit={(event) => {
        event.preventDefault();
        const formData = new FormData(event.currentTarget);
        console.log(formData.get("terms"));
      }}>
        <Checkbox.Root  value="accepted">
          <Checkbox.Control>
            <Checkbox.Indicator />
          </Checkbox.Control>
          <Checkbox.Label>I agree to the terms and conditions</Checkbox.Label>
          <Checkbox.HiddenInput />
        </Checkbox.Root>
        <button type="submit">Submit</button>
      </form>
    `}
  </Preview.Code>
  <Preview.CSS>{checkboxExampleCss}</Preview.CSS>
  <Preview.Data>{`const formField = { name: 'terms', value: 'accepted' };`}</Preview.Data>
</Preview>

### Context

Read Ark checkbox state with `Checkbox.Context` when nearby UI needs the current checked value.

<Preview>
  <CheckboxContextExample />

  <Preview.Code>
    {`
      <Checkbox.Root defaultChecked>
        <Checkbox.Control>
          <Checkbox.Indicator />
        </Checkbox.Control>
        <Checkbox.Label>Context reader</Checkbox.Label>
        <Checkbox.HiddenInput />
        <Checkbox.Context>
          {(checkbox) => <span>Checked: {String(checkbox.checked)}</span>}
        </Checkbox.Context>
      </Checkbox.Root>
    `}
  </Preview.Code>
  <Preview.CSS>{checkboxExampleCss}</Preview.CSS>
  <Preview.Data>{`const initialChecked = true;`}</Preview.Data>
</Preview>

### Sizes

Apply moduix `size` to match checkbox density to the surrounding form or settings surface.

<Preview>
  <CheckboxSizesExample />

  <Preview.Code>
    {`
      <Checkbox.Root size="lg" defaultChecked>
        <Checkbox.Control>
          <Checkbox.Indicator />
        </Checkbox.Control>
        <Checkbox.Label>Large</Checkbox.Label>
        <Checkbox.HiddenInput />
      </Checkbox.Root>
    `}
  </Preview.Code>
  <Preview.CSS>{checkboxExampleCss}</Preview.CSS>
  <Preview.Data>{`const sizes = ['xs', 'sm', 'md', 'lg', 'xl'];`}</Preview.Data>
</Preview>

### Custom Indicator

Replace the default checkmark when the product needs a different icon treatment while keeping Ark checkbox behavior.

<Preview>
  <CustomIndicatorCheckboxExample />

  <Preview.Code>
    {`
      <Checkbox.Root defaultChecked>
        <Checkbox.Control>
          <Checkbox.Indicator>
            <CustomPlusIcon />
          </Checkbox.Indicator>
        </Checkbox.Control>
        <Checkbox.Label>Use a custom indicator icon</Checkbox.Label>
        <Checkbox.HiddenInput />
      </Checkbox.Root>
    `}
  </Preview.Code>
  <Preview.CSS>{checkboxCustomStylingCss}</Preview.CSS>
  <Preview.Data>{`const icon = 'custom plus';`}</Preview.Data>
</Preview>

### Group

Use `Checkbox.Group` when several options belong to one shared array of selected values.

<Preview>
  <CheckboxGroupExample />

  <Preview.Code>
    {`
      <Checkbox.Group defaultValue={["email"]} >
        {options.map((option) => (
          <Checkbox.Root key={option.value} value={option.value}>
            <Checkbox.Control>
              <Checkbox.Indicator />
            </Checkbox.Control>
            <Checkbox.Label>{option.label}</Checkbox.Label>
            <Checkbox.HiddenInput />
          </Checkbox.Root>
        ))}
      </Checkbox.Group>
    `}
  </Preview.Code>
  <Preview.CSS>{checkboxExampleCss}</Preview.CSS>
  <Preview.Data>{notificationOptionsData}</Preview.Data>
</Preview>

### Controlled Group

Control the group value from React state when selected options need to drive filters, summaries, or actions.

<Preview>
  <ControlledCheckboxGroupExample />

  <Preview.Code>
    {`
      const [value, setValue] = useState(["push"] as string[]);

      <Checkbox.Group value={value} onValueChange={setValue} >
        {options.map((option) => (
          <Checkbox.Root key={option.value} value={option.value}>
            <Checkbox.Control>
              <Checkbox.Indicator />
            </Checkbox.Control>
            <Checkbox.Label>{option.label}</Checkbox.Label>
            <Checkbox.HiddenInput />
          </Checkbox.Root>
        ))}
      </Checkbox.Group>
    `}

  </Preview.Code>
  <Preview.CSS>{checkboxExampleCss}</Preview.CSS>
  <Preview.Data>{notificationOptionsData}</Preview.Data>
</Preview>

### Group Provider

Use `useCheckboxGroup()` with `Checkbox.GroupProvider` when group state is created outside the rendered tree.

<Preview>
  <CheckboxGroupProviderExample />

  <Preview.Code>
    {`
      import { Checkbox, useCheckboxGroup } from "moduix";

      export function CheckboxGroupProviderDemo() {
        const group = useCheckboxGroup({ defaultValue: ["react"], name: "framework" });

        return (
          <Checkbox.GroupProvider value={group}>
            {options.map((option) => (
              <Checkbox.Root key={option.value} value={option.value}>
                <Checkbox.Control>
                  <Checkbox.Indicator />
                </Checkbox.Control>
                <Checkbox.Label>{option.label}</Checkbox.Label>
                <Checkbox.HiddenInput />
              </Checkbox.Root>
            ))}
          </Checkbox.GroupProvider>
        );
      }
    `}

  </Preview.Code>
  <Preview.CSS>{checkboxExampleCss}</Preview.CSS>
  <Preview.Data>{frameworkOptionsData}</Preview.Data>
</Preview>

### Invalid Group

Mark the group invalid when the whole set participates in validation and requires a valid selection state.

<Preview>
  <CheckboxGroupInvalidExample />

  <Preview.Code>
    {`
      <Checkbox.Group invalid defaultValue={["email"]} >
        {options.map((option) => (
          <Checkbox.Root key={option.value} value={option.value}>
            <Checkbox.Control>
              <Checkbox.Indicator />
            </Checkbox.Control>
            <Checkbox.Label>{option.label}</Checkbox.Label>
            <Checkbox.HiddenInput />
          </Checkbox.Root>
        ))}
      </Checkbox.Group>
    `}
  </Preview.Code>
  <Preview.CSS>{checkboxExampleCss}</Preview.CSS>
  <Preview.Data>{notificationOptionsData}</Preview.Data>
</Preview>

### Max Selected

Use `maxSelectedValues` when only a fixed number of options can be selected.

<Preview>
  <CheckboxGroupMaxSelectedExample />

  <Preview.Code>
    {`
      <Checkbox.Group defaultValue={["react", "solid"]} maxSelectedValues={2} >
        {options.map((option) => (
          <Checkbox.Root key={option.value} value={option.value}>
            <Checkbox.Control>
              <Checkbox.Indicator />
            </Checkbox.Control>
            <Checkbox.Label>{option.label}</Checkbox.Label>
            <Checkbox.HiddenInput />
          </Checkbox.Root>
        ))}
      </Checkbox.Group>
    `}
  </Preview.Code>
  <Preview.CSS>{checkboxExampleCss}</Preview.CSS>
  <Preview.Data>{frameworkOptionsWithSvelteData}</Preview.Data>
</Preview>

### Select All

Compose a parent checkbox and a controlled group when one control should toggle the entire selection set.

<Preview>
  <CheckboxGroupSelectAllExample />

  <Preview.Code>
    {`
      const [value, setValue] = useState(["react"] as string[]);
      const allValues = options.map((option) => option.value);
      const allSelected = value.length === allValues.length;
      const indeterminate = value.length > 0 && value.length < allValues.length;

      <Checkbox.Root
        checked={indeterminate ? "indeterminate" : allSelected}
        onCheckedChange={(details) => setValue(details.checked === true ? allValues : [])}
      >
        <Checkbox.Control>
          <Checkbox.Indicator />
          <Checkbox.Indicator indeterminate />
        </Checkbox.Control>
        <Checkbox.Label>Select all</Checkbox.Label>
        <Checkbox.HiddenInput />
      </Checkbox.Root>
    `}

  </Preview.Code>
  <Preview.CSS>{checkboxExampleCss}</Preview.CSS>
  <Preview.Data>{frameworkOptionsData}</Preview.Data>
</Preview>

### Group With Form

Use `Checkbox.Group` inside a form and read selected values with `FormData.getAll(name)`.

<Preview>
  <CheckboxGroupWithFormExample />

  <Preview.Code>
    {`
      <form onSubmit={(event) => {
        event.preventDefault();
        console.log(new FormData(event.currentTarget).getAll("framework"));
      }}>
        <Checkbox.Group defaultValue={["react"]} >
          {options.map((option) => (
            <Checkbox.Root key={option.value} value={option.value}>
              <Checkbox.Control>
                <Checkbox.Indicator />
              </Checkbox.Control>
              <Checkbox.Label>{option.label}</Checkbox.Label>
              <Checkbox.HiddenInput />
            </Checkbox.Root>
          ))}
        </Checkbox.Group>
        <button type="submit">Submit</button>
      </form>
    `}
  </Preview.Code>
  <Preview.CSS>{checkboxExampleCss}</Preview.CSS>
  <Preview.Data>{frameworkOptionsData}</Preview.Data>
</Preview>

### Group With Fieldset

Wrap the group in `Fieldset` when the checkbox set is one semantic form field with a shared legend.

<Preview>
  <CheckboxGroupFieldsetExample />

  <Preview.Code>
    {`
      import { Checkbox, Fieldset, FieldsetLegend } from "moduix";

      export function CheckboxGroupFieldsetDemo() {
        return (
          <Fieldset className="checkbox-fieldset">
            <FieldsetLegend>Frameworks</FieldsetLegend>
            <Checkbox.Group defaultValue={["react"]} >
              {options.map((option) => (
                <Checkbox.Root key={option.value} value={option.value}>
                  <Checkbox.Control>
                    <Checkbox.Indicator />
                  </Checkbox.Control>
                  <Checkbox.Label>{option.label}</Checkbox.Label>
                  <Checkbox.HiddenInput />
                </Checkbox.Root>
              ))}
            </Checkbox.Group>
          </Fieldset>
        );
      }
    `}

  </Preview.Code>
  <Preview.CSS>{checkboxExampleCss}</Preview.CSS>
  <Preview.Data>{frameworkOptionsData}</Preview.Data>
</Preview>

### Custom Styling

Use local classes and CSS variables when the checkbox visuals need a custom tone without changing the part tree.

<Preview>
  <CustomStylingCheckboxExample />

  <Preview.Code>
    {`
      <Checkbox.Group defaultValue={["email"]} className="checkbox-custom-group" >
        {options.map((option) => (
          <Checkbox.Root key={option.value} value={option.value} className="checkbox-custom-root">
            <Checkbox.Control className="checkbox-custom-control">
              <Checkbox.Indicator>
                <CustomPlusIcon className="checkbox-custom-icon" />
              </Checkbox.Indicator>
            </Checkbox.Control>
            <Checkbox.Label className="checkbox-custom-label">{option.label}</Checkbox.Label>
            <Checkbox.HiddenInput />
          </Checkbox.Root>
        ))}
      </Checkbox.Group>
    `}
  </Preview.Code>
  <Preview.CSS>{checkboxCustomStylingCss}</Preview.CSS>
  <Preview.Data>{notificationOptionsData}</Preview.Data>
</Preview>

## Styling

### CSS Properties

<div className="not-prose">
  <Tabs items={['CSS Variables']} className="mb-0 mt-3">
    <Tab className="max-h-150 overflow-auto">
      <CheckboxCssPropertiesPanel />
    </Tab>
  </Tabs>
</div>

### Styling Hooks

- `Checkbox.Root` exposes `data-slot="checkbox-root"`, `data-size`, and Ark state attributes such as `data-state`, `data-disabled`, `data-readonly`, `data-invalid`, `data-required`, `data-hover`, `data-focus`, and `data-focus-visible`.
- `Checkbox.RootProvider` exposes `data-slot="checkbox-root-provider"` and the same root styling contract.
- `Checkbox.Control` exposes `data-slot="checkbox-control"` and Ark state attributes including `data-state="checked" | "indeterminate" | "unchecked"`.
- `Checkbox.Indicator` exposes `data-slot="checkbox-indicator"` and inherits Ark checkbox state attributes.
- `Checkbox.Label` exposes `data-slot="checkbox-label"` and Ark checkbox state attributes.
- `Checkbox.HiddenInput` exposes `data-slot="checkbox-hidden-input"`.
- `Checkbox.Group` exposes `data-slot="checkbox-group"`.
- `Checkbox.GroupProvider` exposes `data-slot="checkbox-group-provider"`.