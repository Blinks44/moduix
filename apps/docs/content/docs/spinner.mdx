---
title: Spinner
description: A compact loading indicator with one default ring and a small customization surface.
icon: Component
---

import {
  SpinnerClassNameExample,
  SpinnerCssPlaygroundPanel,
  SpinnerCssPropertiesPanel,
  SpinnerCustomIndicatorExample,
  SpinnerExample,
  SpinnerSizesExample,
  spinnerPlaygroundCssProperties,
} from '@/components/examples/spinner';

## API Reference

`Spinner` is a compact loading primitive. Base UI does not provide a dedicated spinner primitive, so
moduix keeps this one intentionally small: one default ring indicator, size control, and standard
status semantics.

Use it for pending controls, inline loading feedback, and compact empty states. By default it
announces loading with `role="status"` and `aria-label="Loading"`. Set `decorative` when nearby
text already covers that.

## Basic

<Preview cssProperties={spinnerPlaygroundCssProperties}>
  <SpinnerExample />

  <Preview.Code>
    {`
      import { Spinner } from "moduix";

      export function SpinnerDemo() {
        return <Spinner />;
      }
    `}

  </Preview.Code>
  <Preview.CSSProperties>{(context) => <SpinnerCssPropertiesPanel {...context} />}</Preview.CSSProperties>
  <Preview.CSSPlayground>{(context) => <SpinnerCssPlaygroundPanel {...context} />}</Preview.CSSPlayground>
</Preview>

## Anatomy

`Spinner` exposes one public root and one internal visual slot.

```text
Spinner
└─ indicator
```

```tsx
<Spinner size="md" />
```

| Part                | Role                                                                    |
| ------------------- | ----------------------------------------------------------------------- |
| `Spinner`           | Root status element. Use `size`, `decorative`, and standard span props. |
| `spinner-indicator` | Internal visual slot that hosts the default ring or custom children.    |

## Composition

`Spinner` forwards standard `span` props. Use `size` for common density changes and `decorative`
when the spinner is paired with visible loading text. Override the announcement with `aria-label`
when you need a more specific status message.

### Defaults

| Prop         | Default   | Values                       |
| ------------ | --------- | ---------------------------- |
| `size`       | `md`      | `xs`, `sm`, `md`, `lg`, `xl` |
| `decorative` | `false`   | `true`, `false`              |
| `aria-label` | `Loading` | String                       |

## Examples

### Sizes

Use smaller sizes inside compact controls and larger sizes for standalone loading states.

<Preview>
  <SpinnerSizesExample />

  <Preview.Code>
    {`
      import { Spinner } from "moduix";

      export function SpinnerSizesDemo() {
        return (
          <>
            <Spinner size="xs" />
            <Spinner size="sm" />
            <Spinner size="md" />
            <Spinner size="lg" />
            <Spinner size="xl" />
          </>
        );
      }
    `}

  </Preview.Code>
</Preview>

### Custom Composition

Pass custom children when you need a different indicator while keeping Spinner sizing and status
semantics.

<Preview>
  <SpinnerCustomIndicatorExample />

  <Preview.Code>
    {`
      import { Spinner } from "moduix";

      export function SpinnerCustomIndicatorDemo() {
        return (
          <Spinner size="lg" aria-label="Syncing">
            <svg viewBox="0 0 24 24" fill="none" aria-hidden="true">
              <path
                d="M12 3v4M12 17v4M3 12h4M17 12h4M5.64 5.64l2.83 2.83M15.53 15.53l2.83 2.83M18.36 5.64l-2.83 2.83M8.47 15.53l-2.83 2.83"
                stroke="currentColor"
                strokeWidth="2"
                strokeLinecap="round"
              />
            </svg>
          </Spinner>
        );
      }
    `}

  </Preview.Code>
</Preview>

### Styling

Use `className` or CSS variables for targeted color changes.

<Preview>
  <SpinnerClassNameExample />

  <Preview.Code>
    {`
      import { Spinner } from "moduix";
      import styles from "./spinner-demo.module.css";

      export function SpinnerStylingDemo() {
        return (
          <div className={styles.brand}>
            <Spinner size="lg" />
          </div>
        );
      }
    `}

  </Preview.Code>

  <Preview.CSS>
    {`
      .brand {
        color: var(--color-primary);
      }
    `}

  </Preview.CSS>
</Preview>