---
title: Select
description: Ark UI select composition for choosing one or more values from a popup list.
icon: Component
---

import {
  ControlledSelectExample,
  CustomItemSelectExample,
  DynamicItemsSelectExample,
  FieldSelectExample,
  FormSelectExample,
  GroupedSelectExample,
  LazyMountSelectExample,
  MaxSelectionSelectExample,
  MultipleSelectExample,
  OverflowSelectExample,
  RootProviderSelectExample,
  SelectAllExample,
  SelectCssPropertiesPanel,
  SelectExample,
  SelectOnHighlightExample,
} from '@/components/examples/select';

## API Reference

<PrimitiveReference href="https://ark-ui.com/docs/components/select" />

## Choosing the right component

Use `Select` when users choose from a fixed collection and the trigger should stay read-only.

- Choose **`Combobox`** when typing is part of finding or creating the value.
- Choose **`Select`** when the user picks from known values, including grouped or multiple values.

## Basic

Start with `Select`, pass a `collection`, render the visible field through `Label`, `Control`,
`Trigger`, `ValueText`, `ClearTrigger`, and `Indicator`, then render the popup structure explicitly.
Keep `HiddenSelect` in the tree for native form submission and browser autofill.

<Preview>
  <SelectExample />

  <Preview.Code>
    {`
      import { Select } from "@moduix/react";

      export function SelectDemo() {
        return (
          <Select collection={fruits}>
            <Select.Label>Choose fruit</Select.Label>
            <Select.Control>
              <Select.Trigger>
                <Select.ValueText placeholder="Select an option" />
              </Select.Trigger>
              <Select.Indicators>
                <Select.ClearTrigger aria-label="Clear selection" />
                <Select.Indicator />
              </Select.Indicators>
            </Select.Control>
            <Select.Positioner>
              <Select.Content>
                <Select.ItemGroup>
                  <Select.ItemGroupLabel>Fruits</Select.ItemGroupLabel>
                  {fruits.items.map((item) => (
                    <Select.Item key={item.value} item={item}>
                      <Select.ItemText>{item.label}</Select.ItemText>
                      <Select.ItemIndicator />
                    </Select.Item>
                  ))}
                </Select.ItemGroup>
              </Select.Content>
            </Select.Positioner>
            <Select.HiddenSelect />
          </Select>
        );
      }
    `}

  </Preview.Code>

  <Preview.CSS>
    {`
      [data-slot="select-root"] {
        --select-width: 16rem;
      }
    `}

  </Preview.CSS>

  <Preview.Data>
    {`
      const fruits = createListCollection({
        items: [
          { label: "Apple", value: "apple" },
          { label: "Banana", value: "banana" },
          { label: "Blueberry", value: "blueberry" },
        ],
      });
    `}

  </Preview.Data>
</Preview>

## Install with shadcn

If you want `Select` to become part of your own project source instead of staying in
`node_modules`, install it from the moduix GitHub registry:

<Tabs items={['npm', 'pnpm', 'bun', 'deno', 'aube']}>
  <Tab value="npm">

```bash
npx shadcn@latest add Blinks44/moduix/react-select
```

  </Tab>
  <Tab value="pnpm">

```bash
pnpm dlx shadcn@latest add Blinks44/moduix/react-select
```

  </Tab>
  <Tab value="bun">

```bash
bunx shadcn@latest add Blinks44/moduix/react-select
```

  </Tab>
  <Tab value="deno">

```bash
deno x -A npm:shadcn@latest add Blinks44/moduix/react-select
```

  </Tab>
  <Tab value="aube">

```bash
aubx shadcn@latest add Blinks44/moduix/react-select
```

  </Tab>
</Tabs>

## Anatomy

```text
Select / Select.Root
├─ Select.Label
├─ Select.Control
│  ├─ Select.Trigger
│  │  └─ Select.ValueText
│  └─ Select.Indicators
│     ├─ Select.ClearTrigger
│     └─ Select.Indicator
├─ Overlay subtree (automatically portalled)
│  └─ Select.Positioner
│     └─ Select.Content
│        ├─ Select.List
│        └─ Select.ItemGroup
│           ├─ Select.ItemGroupLabel
│           └─ Select.Item[item]
│              ├─ Select.ItemText
│              └─ Select.ItemIndicator
└─ Select.HiddenSelect
```

| Part                             | Role                                                               |
| -------------------------------- | ------------------------------------------------------------------ |
| `Select` / `Select.Root`         | Root state, collection, selection, popup lifecycle, and form data. |
| `Select.RootProvider`            | Renders a select tree from state returned by `useSelect`.          |
| `Select.Label`                   | Accessible label inherited by the trigger and hidden select.       |
| `Select.Control`                 | Visual field wrapper that receives focus, invalid, and open state. |
| `Select.Trigger`                 | Button that opens the popup and owns keyboard entry.               |
| `Select.ValueText`               | Placeholder or selected item labels.                               |
| `Select.ClearTrigger`            | Button that clears the current value.                              |
| `Select.Indicator`               | Trigger indicator with the moduix default chevron icon.            |
| `Select.Indicators`              | Moduix wrapper that aligns clear and indicator buttons.            |
| `Select.Positioner`              | Floating layer that exposes Ark positioning CSS variables.         |
| `Select.Content`                 | Popup surface and listbox container.                               |
| `Select.List`                    | Optional list wrapper for virtualized or custom list layouts.      |
| `Select.ItemGroup`               | Group wrapper for related collection items.                        |
| `Select.ItemGroupLabel`          | Label for a collection group.                                      |
| `Select.Item`                    | Selectable row. Pass the collection item through `item`.           |
| `Select.ItemText`                | Text used for display and accessibility.                           |
| `Select.ItemIndicator`           | Selected-state indicator with the moduix default check icon.       |
| `Select.HiddenSelect`            | Native hidden `<select>` for forms, autofill, and reset behavior.  |
| `Select.Context` / `ItemContext` | Inline access to root and item state.                              |

## Composition

The root portals `Positioner` automatically. Use `portalled={false}` for inline rendering and `portalRef` for a custom container; structural parts remain explicit and styleable.

### Recommended composition

Use the explicit Ark part tree: root, label, control, trigger, value text, optional clear and
indicator buttons, automatically portalled positioner/content, items, and `HiddenSelect`.

`Select` is the short root form and is equivalent to `Select.Root`.

### When to go custom

Use `asChild` when a part must render a custom host element, but keep a single semantic child that
can receive Ark props. Use `Select.List` for custom list wrappers and virtualization paths; regular
selects can render items directly in `Select.Content`.

### Default props

| Prop                    | Default | Notes                                                          |
| ----------------------- | ------- | -------------------------------------------------------------- |
| `closeOnSelect`         | `true`  | Closes the popup after item selection.                         |
| `composite`             | `true`  | Allows composition with nested composite widgets.              |
| `defaultValue`          | `[]`    | Values are string arrays, including single selection.          |
| `lazyMount`             | `false` | Defers popup content mounting until opened.                    |
| `loopFocus`             | `false` | Keeps arrow navigation from wrapping by default.               |
| `multiple`              | `false` | Enables multiple values when set.                              |
| `positioning`           | -       | Ark floating positioning options.                              |
| `unmountOnExit`         | `false` | Removes popup content after close when set with lazy mounting. |
| `ValueText.placeholder` | -       | Text shown when the value array is empty.                      |

### Behavioral notes

`value`, `defaultValue`, and `onValueChange(details)` use string arrays. Read
`details.value` for selected keys and `details.items` for the selected collection items.

`onOpenChange(details)`, `onHighlightChange(details)`, and `onSelect(details)` keep Ark's detail
object signatures. `Field.Root` and `Fieldset.Root` can provide disabled, invalid, required, and
read-only state through Ark context.

Keep `HiddenSelect` in normal form usage for native submission, autofill, and reset behavior. For
very large virtualized collections, compose a lightweight hidden input with `useSelectContext`
instead of rendering one native option per item.

## Examples

### Controlled

Control the selected string array from React state and update it from `details.value`.

<Preview>
  <ControlledSelectExample />

  <Preview.Code>
    {`
      const [value, setValue] = useState(["light"]);

      <Select
        collection={themes}
        value={value}
        onValueChange={(details) => setValue(details.value)}
      >
        <Select.Label>Theme</Select.Label>
        <Select.Control>
          <Select.Trigger>
            <Select.ValueText placeholder="Select theme" />
          </Select.Trigger>
          <Select.Indicators>
            <Select.ClearTrigger aria-label="Clear selection" />
            <Select.Indicator />
          </Select.Indicators>
        </Select.Control>
        <Select.Positioner>
          <Select.Content>
            {themes.items.map((item) => (
              <Select.Item key={item.value} item={item}>
                <Select.ItemText>{item.label}</Select.ItemText>
                <Select.ItemIndicator />
              </Select.Item>
            ))}
          </Select.Content>
        </Select.Positioner>
        <Select.HiddenSelect />
      </Select>
    `}

  </Preview.Code>
  <Preview.CSS>{`[data-slot="select-root"] { --select-width: 14rem; }`}</Preview.CSS>
  <Preview.Data>
    {`
      const value = ["light"];

      const themes = createListCollection({
        items: [
          { label: "System", value: "system" },
          { label: "Light", value: "light" },
          { label: "Dark", value: "dark" },
        ],
      });
    `}

  </Preview.Data>
</Preview>

### Root Provider

Use `useSelect` with `Select.RootProvider` when state needs to be read or controlled outside the
rendered select tree.

<Preview>
  <RootProviderSelectExample />

  <Preview.Code>
    {`
      export function SelectRootProviderDemo() {
        const select = useSelect({ collection: fruits, defaultValue: ["banana"] });

        return (
          <>
            <output>Selected: {select.valueAsString}</output>
            <Select.RootProvider value={select}>
              <Select.Label>Choose fruit</Select.Label>
              <Select.Control>
                <Select.Trigger>
                  <Select.ValueText placeholder="Select an option" />
                </Select.Trigger>
                <Select.Indicators>
                  <Select.ClearTrigger aria-label="Clear selection" />
                  <Select.Indicator />
                </Select.Indicators>
              </Select.Control>
              <Select.Positioner>
                <Select.Content>
                  {fruits.items.map((item) => (
                    <Select.Item key={item.value} item={item}>
                      <Select.ItemText>{item.label}</Select.ItemText>
                      <Select.ItemIndicator />
                    </Select.Item>
                  ))}
                </Select.Content>
              </Select.Positioner>
              <Select.HiddenSelect />
            </Select.RootProvider>
          </>
        );
      }
    `}

  </Preview.Code>
  <Preview.CSS>{`[data-slot="select-root-provider"] { --select-width: 14rem; }`}</Preview.CSS>
  <Preview.Data>
    {`
      const fruits = createListCollection({
        items: [
          { label: "Apple", value: "apple" },
          { label: "Banana", value: "banana" },
          { label: "Blueberry", value: "blueberry" },
          { label: "Grape", value: "grape" },
          { label: "Kiwi", value: "kiwi" },
          { label: "Mango", value: "mango" },
          { label: "Orange", value: "orange" },
          { label: "Pineapple", value: "pineapple" },
          { label: "Strawberry", value: "strawberry" },
          { label: "Watermelon", value: "watermelon" },
        ],
      });
    `}

  </Preview.Data>
</Preview>

### Multiple

Set `multiple` to allow several selected values. `ValueText` renders the selected item labels from
the collection.

<Preview>
  <MultipleSelectExample />

  <Preview.Code>
    {`
      <Select collection={languages} multiple defaultValue={["javascript", "typescript"]}>
        <Select.Label>Languages</Select.Label>
        <Select.Control>
          <Select.Trigger>
            <Select.ValueText placeholder="Select languages" />
          </Select.Trigger>
          <Select.Indicators>
            <Select.ClearTrigger aria-label="Clear selection" />
            <Select.Indicator />
          </Select.Indicators>
        </Select.Control>
        <Select.Positioner>
          <Select.Content>
            {languages.items.map((item) => (
              <Select.Item key={item.value} item={item}>
                <Select.ItemText>{item.label}</Select.ItemText>
                <Select.ItemIndicator />
              </Select.Item>
            ))}
          </Select.Content>
        </Select.Positioner>
        <Select.HiddenSelect />
      </Select>
    `}

  </Preview.Code>
  <Preview.CSS>{`[data-slot="select-item"][data-state="checked"] { font-weight: var(--weight-semibold); }`}</Preview.CSS>
  <Preview.Data>
    {`
      const languages = createListCollection({
        items: [
          { label: "C#", value: "csharp" },
          { label: "Go", value: "go" },
          { label: "JavaScript", value: "javascript" },
          { label: "Python", value: "python" },
          { label: "Rust", value: "rust" },
          { label: "TypeScript", value: "typescript" },
        ],
      });
    `}

  </Preview.Data>
</Preview>

### Grouping

Use `groupBy` on the collection and render `collection.group()` with `ItemGroup` and
`ItemGroupLabel`.

<Preview>
  <GroupedSelectExample />

  <Preview.Code>
    {`
      {produce.group().map(([type, group]) => (
        <Select.ItemGroup key={type}>
          <Select.ItemGroupLabel>{type}</Select.ItemGroupLabel>
          {group.map((item) => (
            <Select.Item key={item.value} item={item}>
              <Select.ItemText>{item.label}</Select.ItemText>
              <Select.ItemIndicator />
            </Select.Item>
          ))}
        </Select.ItemGroup>
      ))}
    `}

  </Preview.Code>
  <Preview.CSS>{`[data-slot="select-item-group-label"] { color: var(--color-muted-foreground); }`}</Preview.CSS>
  <Preview.Data>
    {`
      const produce = createListCollection({
        items: [
          { label: "Apple", value: "apple", type: "Fruits" },
          { label: "Mango", value: "mango", type: "Fruits" },
          { label: "Orange", value: "orange", type: "Fruits" },
          { label: "Broccoli", value: "broccoli", type: "Vegetables" },
          { label: "Carrot", value: "carrot", type: "Vegetables" },
          { label: "Spinach", value: "spinach", type: "Vegetables" },
        ],
        groupBy: (item) => item.type,
      });
    `}

  </Preview.Data>
</Preview>

### Field

Wrap `Select` in `Field.Root` when label, helper text, invalid, required, or disabled state belongs
to a form field.

<Preview>
  <FieldSelectExample />

  <Preview.Code>
    {`
      <Field.Root required>
        <Select collection={frameworks} name="framework">
          <Select.Label>Framework</Select.Label>
          <Select.Control>
            <Select.Trigger>
              <Select.ValueText placeholder="Select framework" />
            </Select.Trigger>
            <Select.Indicators>
              <Select.ClearTrigger aria-label="Clear selection" />
              <Select.Indicator />
            </Select.Indicators>
          </Select.Control>
          <Select.Positioner>
            <Select.Content>
              {frameworks.items.map((item) => (
                <Select.Item key={item.value} item={item}>
                  <Select.ItemText>{item.label}</Select.ItemText>
                  <Select.ItemIndicator />
                </Select.Item>
              ))}
            </Select.Content>
          </Select.Positioner>
          <Select.HiddenSelect />
        </Select>
        <Field.HelperText>Pick the framework used by this project.</Field.HelperText>
      </Field.Root>
    `}

  </Preview.Code>
  <Preview.CSS>{`[data-slot="select-label"][data-required]::after { content: " *"; }`}</Preview.CSS>
  <Preview.Data>
    {`
      const frameworks = createListCollection({
        items: [
          { label: "React", value: "react" },
          { label: "Solid", value: "solid" },
          { label: "Vue", value: "vue" },
          { label: "Svelte", value: "svelte", disabled: true },
        ],
      });
    `}

  </Preview.Data>
</Preview>

### Form Usage

`HiddenSelect` keeps native submission working. Read submitted values from `FormData`.

<Preview>
  <FormSelectExample />

  <Preview.Code>
    {`
      <form onSubmit={handleSubmit}>
        <Select collection={themes} name="theme" required>
          <Select.Label>Theme</Select.Label>
          <Select.Control>
            <Select.Trigger>
              <Select.ValueText placeholder="Select theme" />
            </Select.Trigger>
            <Select.Indicators>
              <Select.ClearTrigger aria-label="Clear selection" />
              <Select.Indicator />
            </Select.Indicators>
          </Select.Control>
          <Select.Positioner>
            <Select.Content>
              {themes.items.map((item) => (
                <Select.Item key={item.value} item={item}>
                  <Select.ItemText>{item.label}</Select.ItemText>
                  <Select.ItemIndicator />
                </Select.Item>
              ))}
            </Select.Content>
          </Select.Positioner>
          <Select.HiddenSelect />
        </Select>
        <button type="submit">Submit</button>
      </form>
    `}

  </Preview.Code>
  <Preview.CSS>{`form [data-slot="select-root"] { --select-width: 16rem; }`}</Preview.CSS>
  <Preview.Data>
    {`
      const themes = createListCollection({
        items: [
          { label: "System", value: "system" },
          { label: "Light", value: "light" },
          { label: "Dark", value: "dark" },
        ],
      });

      const fieldName = "theme";
    `}

  </Preview.Data>
</Preview>

### Lazy Mount

Use `lazyMount` and `unmountOnExit` when popup content should only exist while the select is used.

<Preview>
  <LazyMountSelectExample />

  <Preview.Code>
    {`
      <Select collection={fruits} lazyMount unmountOnExit>
        <Select.Label>Choose fruit</Select.Label>
        <Select.Control>
          <Select.Trigger>
            <Select.ValueText placeholder="Select an option" />
          </Select.Trigger>
          <Select.Indicators>
            <Select.ClearTrigger aria-label="Clear selection" />
            <Select.Indicator />
          </Select.Indicators>
        </Select.Control>
        <Select.Positioner>
          <Select.Content>
            {fruits.items.map((item) => (
              <Select.Item key={item.value} item={item}>
                <Select.ItemText>{item.label}</Select.ItemText>
                <Select.ItemIndicator />
              </Select.Item>
            ))}
          </Select.Content>
        </Select.Positioner>
        <Select.HiddenSelect />
      </Select>
    `}

  </Preview.Code>
  <Preview.CSS>{`[data-slot="select-content"] { --select-content-closed-scale: 0.96; }`}</Preview.CSS>
  <Preview.Data>
    {`
      const fruits = createListCollection({
        items: [
          { label: "Apple", value: "apple" },
          { label: "Banana", value: "banana" },
          { label: "Blueberry", value: "blueberry" },
          { label: "Grape", value: "grape" },
          { label: "Kiwi", value: "kiwi" },
          { label: "Mango", value: "mango" },
          { label: "Orange", value: "orange" },
          { label: "Pineapple", value: "pineapple" },
          { label: "Strawberry", value: "strawberry" },
          { label: "Watermelon", value: "watermelon" },
        ],
      });
    `}

  </Preview.Data>
</Preview>

### Select On Highlight

Use `onHighlightChange(details)` when moving through options should immediately update selection.

<Preview>
  <SelectOnHighlightExample />

  <Preview.Code>
    {`
      const select = useSelect({
        collection: fruits,
        onHighlightChange({ highlightedValue }) {
          if (highlightedValue) select.selectValue(highlightedValue);
        },
      });

      <Select.RootProvider value={select}>
        <Select.Label>Choose fruit</Select.Label>
        <Select.Control>
          <Select.Trigger>
            <Select.ValueText placeholder="Select an option" />
          </Select.Trigger>
          <Select.Indicators>
            <Select.ClearTrigger aria-label="Clear selection" />
            <Select.Indicator />
          </Select.Indicators>
        </Select.Control>
        <Select.Positioner>
          <Select.Content>
            {fruits.items.map((item) => (
              <Select.Item key={item.value} item={item}>
                <Select.ItemText>{item.label}</Select.ItemText>
                <Select.ItemIndicator />
              </Select.Item>
            ))}
          </Select.Content>
        </Select.Positioner>
        <Select.HiddenSelect />
      </Select.RootProvider>
    `}

  </Preview.Code>
  <Preview.CSS>{`[data-slot="select-item"][data-highlighted] { color: var(--select-highlight-color); }`}</Preview.CSS>
  <Preview.Data>
    {`
      const fruits = createListCollection({
        items: [
          { label: "Apple", value: "apple" },
          { label: "Banana", value: "banana" },
          { label: "Blueberry", value: "blueberry" },
          { label: "Grape", value: "grape" },
          { label: "Kiwi", value: "kiwi" },
          { label: "Mango", value: "mango" },
          { label: "Orange", value: "orange" },
          { label: "Pineapple", value: "pineapple" },
          { label: "Strawberry", value: "strawberry" },
          { label: "Watermelon", value: "watermelon" },
        ],
      });
    `}

  </Preview.Data>
</Preview>

### Max Selection

Limit multiple selection by rejecting oversized value arrays and marking remaining items disabled.

<Preview>
  <MaxSelectionSelectExample />

  <Preview.Code>
    {`
      <Select
        collection={collection}
        multiple
        value={value}
        onValueChange={(details) => {
          if (details.value.length <= 3) setValue(details.value);
        }}
      >
        <Select.Label>Languages</Select.Label>
        <Select.Control>
          <Select.Trigger>
            <Select.ValueText placeholder="Select up to 3" />
          </Select.Trigger>
          <Select.Indicators>
            <Select.ClearTrigger aria-label="Clear selection" />
            <Select.Indicator />
          </Select.Indicators>
        </Select.Control>
        <Select.Positioner>
          <Select.Content>
            {collection.items.map((item) => (
              <Select.Item key={item.value} item={item}>
                <Select.ItemText>{item.label}</Select.ItemText>
                <Select.ItemIndicator />
              </Select.Item>
            ))}
          </Select.Content>
        </Select.Positioner>
        <Select.HiddenSelect />
      </Select>
    `}

  </Preview.Code>
  <Preview.CSS>{`[data-slot="select-item"][data-disabled] { opacity: var(--opacity-disabled); }`}</Preview.CSS>
  <Preview.Data>
    {`
      const value = ["javascript"];

      const collection = createListCollection({
        items: [
          { label: "C#", value: "csharp", disabled: false },
          { label: "Go", value: "go", disabled: false },
          { label: "JavaScript", value: "javascript", disabled: false },
          { label: "Python", value: "python", disabled: false },
          { label: "Rust", value: "rust", disabled: false },
          { label: "TypeScript", value: "typescript", disabled: false },
        ],
      });
    `}

  </Preview.Data>
</Preview>

### Select All

Read root state with `Select.Context` and call `selectAll()` for bulk selection.

<Preview>
  <SelectAllExample />

  <Preview.Code>
    {`
      <Select collection={languages} multiple>
        <Select.Label>Languages</Select.Label>
        <Select.Control>
          <Select.Trigger>
            <Select.ValueText placeholder="Select languages" />
          </Select.Trigger>
          <Select.Indicators>
            <Select.ClearTrigger aria-label="Clear selection" />
            <Select.Indicator />
          </Select.Indicators>
        </Select.Control>
        <Select.Positioner>
          <Select.Content>
            <Select.Context>
              {(select) => (
                <button
                  className="select-bulk-action"
                  type="button"
                  onClick={() => {
                    select.selectAll();
                    select.setOpen(false);
                  }}
                >
                  Select all
                </button>
              )}
            </Select.Context>
            {languages.items.map((item) => (
              <Select.Item key={item.value} item={item}>
                <Select.ItemText>{item.label}</Select.ItemText>
                <Select.ItemIndicator />
              </Select.Item>
            ))}
          </Select.Content>
        </Select.Positioner>
        <Select.HiddenSelect />
      </Select>
    `}

  </Preview.Code>
  <Preview.CSS>
    {`
      .select-bulk-action {
        width: 100%;
        margin-bottom: var(--spacing-2);
      }
    `}
  </Preview.CSS>
  <Preview.Data>
    {`
      const languages = createListCollection({
        items: [
          { label: "C#", value: "csharp" },
          { label: "Go", value: "go" },
          { label: "JavaScript", value: "javascript" },
          { label: "Python", value: "python" },
          { label: "Rust", value: "rust" },
          { label: "TypeScript", value: "typescript" },
        ],
      });
    `}

  </Preview.Data>
</Preview>

### Overflow

Use `positioning.fitViewport` and the exposed `--available-height` variable for long lists.

<Preview>
  <OverflowSelectExample />

  <Preview.Code>
    {`
      <Select
        collection={fruits}
        positioning={{ fitViewport: true, placement: "bottom-start", sameWidth: true }}
      >
        <Select.Label>Choose fruit</Select.Label>
        <Select.Control>
          <Select.Trigger>
            <Select.ValueText placeholder="Select an option" />
          </Select.Trigger>
          <Select.Indicators>
            <Select.ClearTrigger aria-label="Clear selection" />
            <Select.Indicator />
          </Select.Indicators>
        </Select.Control>
        <Select.Positioner>
          <Select.Content>
            {fruits.items.map((item) => (
              <Select.Item key={item.value} item={item}>
                <Select.ItemText>{item.label}</Select.ItemText>
                <Select.ItemIndicator />
              </Select.Item>
            ))}
          </Select.Content>
        </Select.Positioner>
        <Select.HiddenSelect />
      </Select>
    `}

  </Preview.Code>
  <Preview.CSS>{`[data-slot="select-content"] { max-height: min(24rem, var(--available-height)); }`}</Preview.CSS>
  <Preview.Data>
    {`
      const fruits = createListCollection({
        items: [
          { label: "Apple", value: "apple" },
          { label: "Banana", value: "banana" },
          { label: "Blueberry", value: "blueberry" },
          { label: "Grape", value: "grape" },
          { label: "Kiwi", value: "kiwi" },
          { label: "Mango", value: "mango" },
          { label: "Orange", value: "orange" },
          { label: "Pineapple", value: "pineapple" },
          { label: "Strawberry", value: "strawberry" },
          { label: "Watermelon", value: "watermelon" },
        ],
      });
    `}

  </Preview.Data>
</Preview>

### Dynamic Items

Recreate or update the collection when the available items change.

<Preview>
  <DynamicItemsSelectExample />

  <Preview.Code>
    {`
      const collection = createListCollection({
        items: fruits.items.filter((item) => item.label.toLowerCase().includes(query)),
      });

      <Select collection={collection}>
        <Select.Label>Choose fruit</Select.Label>
        <Select.Control>
          <Select.Trigger>
            <Select.ValueText placeholder="Select an option" />
          </Select.Trigger>
          <Select.Indicators>
            <Select.ClearTrigger aria-label="Clear selection" />
            <Select.Indicator />
          </Select.Indicators>
        </Select.Control>
        <Select.Positioner>
          <Select.Content>
            {collection.items.map((item) => (
              <Select.Item key={item.value} item={item}>
                <Select.ItemText>{item.label}</Select.ItemText>
                <Select.ItemIndicator />
              </Select.Item>
            ))}
          </Select.Content>
        </Select.Positioner>
        <Select.HiddenSelect />
      </Select>
    `}

  </Preview.Code>
  <Preview.CSS>{`input + [data-slot="select-root"] { margin-top: var(--spacing-3); }`}</Preview.CSS>
  <Preview.Data>
    {`
      const query = "";

      const fruits = createListCollection({
        items: [
          { label: "Apple", value: "apple" },
          { label: "Banana", value: "banana" },
          { label: "Blueberry", value: "blueberry" },
          { label: "Grape", value: "grape" },
          { label: "Kiwi", value: "kiwi" },
          { label: "Mango", value: "mango" },
          { label: "Orange", value: "orange" },
          { label: "Pineapple", value: "pineapple" },
          { label: "Strawberry", value: "strawberry" },
          { label: "Watermelon", value: "watermelon" },
        ],
      });
    `}

  </Preview.Data>
</Preview>

### Custom Item Layout

Use the moduix leaf helpers inside `ItemText` when items need a stable icon and ellipsized label
layout. These helpers are plain spans; selection behavior stays on Ark `Select.Item`.

<Preview>
  <CustomItemSelectExample />

  <Preview.Code>
    {`
      <Select.Item item={item}>
        <Select.ItemText>
          <Select.ItemTextContent>
            <Select.ItemTextIcon aria-hidden>i</Select.ItemTextIcon>
            <Select.ItemTextLabel>{item.label}</Select.ItemTextLabel>
          </Select.ItemTextContent>
        </Select.ItemText>
        <Select.ItemIndicator />
      </Select.Item>
    `}

  </Preview.Code>
  <Preview.CSS>{`[data-slot="select-item-text-content"] { gap: var(--spacing-2); }`}</Preview.CSS>
  <Preview.Data>
    {`
      const item = {
        label: "Apple",
        value: "apple",
      };
    `}

  </Preview.Data>
</Preview>

## Styling

Style `Select` through `className`, stable `data-slot="select-*"` hooks, Ark attributes, and the
public `--select-*` CSS variables shown below.

### CSS Properties

<div className="not-prose">
  <Tabs items={['CSS Variables']} className="mb-0 mt-3">
    <Tab className="max-h-150 overflow-auto">
      <SelectCssPropertiesPanel />
    </Tab>
  </Tabs>
</div>

### Styling hooks

- Root and root provider expose `data-slot="select-root"` and `data-slot="select-root-provider"`.
- Ark parts keep `data-scope="select"` and `data-part` for anatomy selectors.
- State styling uses `data-state="open" | "closed"`, `data-focus`, `data-invalid`,
  `data-disabled`, `data-readonly`, `data-placeholder-shown`, `data-highlighted`, and
  `data-state="checked" | "unchecked"` on items.
- `Select.Positioner` exposes `--reference-width`, `--reference-height`, `--available-width`,
  `--available-height`, `--x`, `--y`, `--z-index`, and `--transform-origin`.