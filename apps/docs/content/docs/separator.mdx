---
title: Separator
description: Separates related content or groups with an accessible horizontal or vertical line.
icon: Component
---

import {
  SeparatorCssPlaygroundPanel,
  SeparatorCssPropertiesPanel,
  SeparatorExample,
  StyledSeparatorExample,
  VerticalSeparatorExample,
  separatorPlaygroundCssProperties,
} from '@/components/examples/separator';

## API Reference

<BaseUIReference href="https://base-ui.com/react/components/separator" />

## Default Path

<Preview cssProperties={separatorPlaygroundCssProperties}>
  <SeparatorExample />

  <Preview.Code>
    {`
      import { Separator } from "moduix";
      import styles from "./separator-demo.module.css";

      export function SeparatorDemo() {
        return (
          <div className={styles.card}>
            <div className={styles.stack}>
              <span className={styles.text}>Account settings</span>
              <Separator />
              <span className={styles.text}>Billing details</span>
            </div>
          </div>
        );
      }
    `}

  </Preview.Code>
  <Preview.CSS>
    {`
      .card {
        width: min(18rem, 100%);
        padding: var(--spacing-4);
        border: var(--border-width-sm) solid var(--color-border);
        border-radius: var(--radius-md);
        background-color: var(--color-background);
      }

      .stack {
        display: grid;
        gap: var(--spacing-4);
      }

      .text {
        color: var(--color-foreground);
        font-size: var(--text-sm);
        line-height: var(--line-height-text-sm);
      }
    `}

  </Preview.CSS>
  <Preview.CSSProperties>{(context) => <SeparatorCssPropertiesPanel {...context} />}</Preview.CSSProperties>
  <Preview.CSSPlayground>{(context) => <SeparatorCssPlaygroundPanel {...context} />}</Preview.CSSPlayground>
</Preview>

Use `Separator` directly for non-interactive visual division between related pieces of content.
The default horizontal path already fills the available width, so the common case stays short.

## Anatomy

`Separator` exposes a single root slot with stable `data-slot` and `data-orientation` hooks.

```text
Separator
└─ root[data-slot="separator-root"][data-orientation]
```

```tsx
<Separator orientation="horizontal" />
```

| Part        | Role                                                                                                   |
| ----------- | ------------------------------------------------------------------------------------------------------ |
| `Separator` | Root divider element. Controls `orientation`, accessibility attributes, styling, and element override. |

Style the root directly with `className` or the `--separator-*` CSS properties.

## Composition

`Separator` stays intentionally thin:

- one exported part
- primitive `orientation`, `className`, `style`, and `render` props passed straight through
- direct root styling with `className`
- a small `--separator-*` CSS contract for common visual tuning

Horizontal separators fill the container by default. Vertical separators are intended for inline
groups and default to `1em` height so they align with surrounding text. The primitive keeps
semantic separator output for assistive technology; there are no interactive or disabled states.

Pass `orientation="vertical"` for inline layouts. Use `className` for direct styling,
or the small `--separator-*` CSS contract when you want to tune color, thickness, or length.

### Defaults

| Prop          | Default      | Values                                 |
| ------------- | ------------ | -------------------------------------- |
| `orientation` | `horizontal` | `horizontal`, `vertical`               |
| `className`   | `undefined`  | `string` or state-based class function |
| `style`       | `undefined`  | style object or state-based style fn   |

## Examples

### Vertical

Use `orientation="vertical"` for inline groups such as navigation actions.

<Preview>
  <VerticalSeparatorExample />

  <Preview.Code>
    {`
      import { Separator } from "moduix";
      import styles from "./separator-demo.module.css";

      export function VerticalSeparatorDemo() {
        return (
          <nav className={styles.nav} aria-label="Main navigation">
            <a href="#" className={styles.link}>
              Home
            </a>
            <a href="#" className={styles.link}>
              Pricing
            </a>
            <Separator orientation="vertical" />
            <a href="#" className={styles.link}>
              Sign in
            </a>
          </nav>
        );
      }
    `}

  </Preview.Code>
  <Preview.CSS>
    {`
      .nav {
        display: inline-flex;
        align-items: center;
        gap: var(--spacing-3);
        text-wrap: nowrap;
      }

      .link {
        color: var(--color-foreground);
        font-size: var(--text-sm);
        line-height: var(--line-height-text-sm);
        text-decoration: none;
      }
    `}

  </Preview.CSS>
</Preview>

### Styling

Use `className` or `--separator-*` custom properties when the divider needs a stronger visual
weight or a fixed length.

<Preview>
  <StyledSeparatorExample />

  <Preview.Code>
    {`
      import { Separator } from "moduix";
      import styles from "./separator-demo.module.css";

      export function CustomSeparatorDemo() {
        return (
          <div className={styles.section}>
            <span className={styles.text}>Completed profile</span>
            <Separator className={styles.customSeparator} />
            <span className={styles.text}>Next step: billing details</span>
          </div>
        );
      }
    `}

  </Preview.Code>

  <Preview.CSS>
    {`
      .section {
        display: grid;
        gap: var(--spacing-2);
      }

      .text {
        color: var(--color-foreground);
        font-size: var(--text-sm);
        line-height: var(--line-height-text-sm);
      }

      .customSeparator {
        --separator-color: var(--color-primary);
        --separator-thickness: 2px;
        --separator-length-horizontal: 8rem;
      }
    `}

  </Preview.CSS>
</Preview>