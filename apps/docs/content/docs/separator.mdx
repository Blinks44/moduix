---
title: Separator
description: Separates related content or groups with an accessible horizontal or vertical line.
icon: Component
---

import * as SeparatorDocs from '@/components/examples/separator';

## API Reference

Ark UI does not ship a dedicated separator primitive. Moduix builds this component with the official
[Ark factory](https://ark-ui.com/docs/guides/composition#the-ark-factory), follows Ark's
[composition](https://ark-ui.com/docs/guides/composition), [styling](https://ark-ui.com/docs/guides/styling), and
[ref](https://ark-ui.com/docs/guides/ref) guidance.

## Basic

Use `Separator` when the line itself communicates structure between related content.

<Preview>
  <SeparatorDocs.SeparatorExample />

  <Preview.Code>
  <include lang="tsx">./_snippets/separator/basic.tsx#demo</include>
</Preview.Code>
  <Preview.CSS>
    {`
      .card {
        width: min(18rem, 100%);
        padding: var(--spacing-4);
        border: var(--border-width-sm) solid var(--color-border);
        border-radius: var(--radius-md);
        background-color: var(--color-background);
      }

      .stack {
        display: grid;
        gap: var(--spacing-4);
      }

      .text {
        color: var(--color-foreground);
        font-size: var(--text-sm);
        line-height: var(--line-height-text-sm);
      }
    `}

  </Preview.CSS>
  
</Preview>

## Install with shadcn

<ShadcnInstall packageName="react-separator" />

## Anatomy

`Separator` exposes one root part.

```text
Separator / Separator.Root
└─ root[data-scope="separator"][data-part="root"][data-slot="separator-root"]
```

| Part                           | Role                                                                                   |
| ------------------------------ | -------------------------------------------------------------------------------------- |
| `Separator` / `Separator.Root` | Root divider element. Controls orientation, accessibility metadata, and visual recipe. |

Style the root directly with `className`, `asChild`, or the `--separator-*` CSS properties.

## Composition

### Recommended composition

Use the short `<Separator />` form for most dividers. It is equivalent to
`<Separator.Root />` and keeps the public root form consistent with multipart Ark-style
components.

### When to go custom

Use `asChild` when the host must be a semantic element such as `hr`. The child must be one element
that can receive the merged role, ARIA, data attributes, class name, and ref.

### Default props

| Prop          | Default      | Notes                                  |
| ------------- | ------------ | -------------------------------------- |
| `orientation` | `horizontal` | `horizontal`, `vertical`               |
| `variant`     | `solid`      | `solid`, `dashed`, `dotted`            |
| `size`        | `sm`         | `xs`, `sm`, `md`, `lg`                 |
| `asChild`     | `false`      | Ark factory single-child composition   |
| `role`        | `separator`  | Pass `presentation` for decorative use |

### Behavioral notes

- `Separator` writes `aria-orientation` when its role is `separator`.
- Horizontal separators fill the available width by default.
- Vertical separators default to `1em` height so they align naturally with inline text and compact
  action rows.
- `Separator` is not interactive and does not expose state, callbacks, disabled behavior, or focus
  management.
- Use layout primitives or spacing tokens for whitespace-only gaps.

## Examples

### Vertical

Use `orientation="vertical"` for inline groups such as navigation actions.

<Preview>
  <SeparatorDocs.VerticalSeparatorExample />

  <Preview.Code>
  <include lang="tsx">./_snippets/separator/vertical.tsx#demo</include>
</Preview.Code>
  <Preview.CSS>
    {`
      .nav {
        display: inline-flex;
        align-items: center;
        gap: var(--spacing-3);
        text-wrap: nowrap;
      }

      .link {
        color: var(--color-foreground);
        font-size: var(--text-sm);
        line-height: var(--line-height-text-sm);
        text-decoration: none;
      }
    `}

  </Preview.CSS>
  
</Preview>

### Variants

Use `variant` when the separator needs a solid, dashed, or dotted stroke.

<Preview>
  <SeparatorDocs.SeparatorVariantsExample />

  <Preview.Code>
  <include lang="tsx">./_snippets/separator/variants.tsx#demo</include>
</Preview.Code>
  <Preview.CSS>
    {`
      .section {
        display: grid;
        gap: var(--spacing-2);
      }

      .exampleRow {
        display: grid;
        width: min(16rem, 100%);
        grid-template-columns: 4rem minmax(6rem, 1fr);
        align-items: center;
        gap: var(--spacing-3);
      }

      .text {
        color: var(--color-foreground);
        font-size: var(--text-sm);
        line-height: var(--line-height-text-sm);
      }
    `}

  </Preview.CSS>
  
</Preview>

### Sizes

Use `size` to select the recipe thickness before applying any custom CSS variable overrides.

<Preview>
  <SeparatorDocs.SeparatorSizesExample />

  <Preview.Code>
  <include lang="tsx">./_snippets/separator/sizes.tsx#demo</include>
</Preview.Code>
  <Preview.CSS>
    {`
      .section {
        display: grid;
        gap: var(--spacing-2);
      }

      .exampleRow {
        display: grid;
        width: min(16rem, 100%);
        grid-template-columns: 4rem minmax(6rem, 1fr);
        align-items: center;
        gap: var(--spacing-3);
      }

      .text {
        color: var(--color-foreground);
        font-size: var(--text-sm);
        line-height: var(--line-height-text-sm);
      }
    `}

  </Preview.CSS>
  
</Preview>

### As Child

Use `asChild` to render a native host while keeping the moduix separator styling hooks.

<Preview>
  <SeparatorDocs.SeparatorAsChildExample />

  <Preview.Code>
  <include lang="tsx">./_snippets/separator/as-child.tsx#demo</include>
</Preview.Code>
  <Preview.CSS>
    {`
      .section {
        display: grid;
        gap: var(--spacing-2);
      }

      .text {
        color: var(--color-foreground);
        font-size: var(--text-sm);
        line-height: var(--line-height-text-sm);
      }

      .nativeRule {
        margin: 0;
      }
    `}

  </Preview.CSS>
  
</Preview>

### Custom Styling

Use `className` or `--separator-*` custom properties when the divider needs stronger visual weight
or a fixed length.

<Preview>
  <SeparatorDocs.StyledSeparatorExample />

  <Preview.Code>
  <include lang="tsx">./_snippets/separator/custom-styling.tsx#demo</include>
</Preview.Code>
  <Preview.CSS>
    {`
      .section {
        display: grid;
        gap: var(--spacing-2);
      }

      .text {
        color: var(--color-foreground);
        font-size: var(--text-sm);
        line-height: var(--line-height-text-sm);
      }

      .customSeparator {
        --separator-color: var(--color-primary);
        --separator-thickness: 2px;
        --separator-length-horizontal: 8rem;
      }
    `}

  </Preview.CSS>
  
</Preview>

## Styling

`Separator` exposes Ark-style part selectors and a small `--separator-*` CSS contract for color,
border style, thickness, and orientation-specific length.

### CSS Properties

<div className="not-prose">
  <Tabs items={['CSS Variables']} className="mb-0 mt-3">
    <Tab className="max-h-150 overflow-auto">
      <SeparatorDocs.SeparatorCssPropertiesPanel />
    </Tab>
  </Tabs>
</div>

### Styling hooks

- `data-scope="separator"` and `data-part="root"` identify the Ark-style root part.
- `data-slot="separator-root"` is the stable moduix selector.
- `data-orientation="horizontal"` and `data-orientation="vertical"` target axis-specific styling.
- `data-size="xs|sm|md|lg"` and `data-variant="solid|dashed|dotted"` expose the visual recipe.
- `className` and `style` are applied directly to the root separator element.