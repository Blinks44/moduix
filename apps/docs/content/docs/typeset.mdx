---
title: Typeset
description: A CSS-first reading rhythm for semantic HTML, rendered Markdown, and streaming content.
icon: Component
---

import * as TypesetDocs from '@/components/examples/typeset';

## API Reference

Ark UI does not ship a dedicated typesetting primitive. Moduix builds `Typeset` with the
[Ark factory](https://ark-ui.com/docs/guides/composition#the-ark-factory), so the root keeps
standard HTML props, `asChild`, and a forwarded ref while its CSS styles semantic descendants.

## Basic

Use the short `<Typeset>` root for rendered HTML. Add `asChild` when the container itself should
be semantic, such as an `article` or `section`. Use plain `<ul>` and `<ol>` elements: Typeset adds
the bullets, numbering, nested markers, and reading rhythm without utility classes.

<Preview>
  <TypesetDocs.TypesetExample />

<Preview.Code>
  <include lang="tsx">./_snippets/typeset/basic.tsx#demo</include>
</Preview.Code>

  <Preview.CSS>
    {`
      .article {
        width: min(42rem, 100%);
      }
    `}
  </Preview.CSS>
</Preview>

## Install with shadcn

```bash
npx shadcn@latest add @moduix-react/typeset
```

## Anatomy

```text
Typeset
└─ Typeset.Scroll (optional)
```

## Composition

### Recommended composition

Keep Markdown parsing, sanitization, rendering, and reading measure in the application. `Typeset`
only supplies the scoped visual rhythm and deliberately does not set a reading width. Use
`Typeset.Scroll` around a wide table or another block that should scroll horizontally.

### Behavioral notes

`Typeset` has no state machine, controlled state, keyboard behavior, form integration, or hidden
inputs. Its styles use `margin-block-start` and avoid `:last-child`, `:has()`, and `:empty`, so
appending a streaming Markdown block does not restyle earlier blocks.

## Examples

### Reading rhythm

Use CSS variables to create roomier or denser presets without adding component props.

<Preview>
  <TypesetDocs.TypesetReadingExample />

<Preview.Code>
  <include lang="tsx">./_snippets/typeset/reading-rhythm.tsx#demo</include>
</Preview.Code>

  <Preview.CSS>
    {`
      .typeset-reading {
        --typeset-size: 1.125rem;
        --typeset-leading: 1.9;
        --typeset-flow: 1.75em;
      }
    `}
  </Preview.CSS>
</Preview>

### Streaming content

Use a tighter rhythm for chat messages. New blocks contribute only their own spacing as the stream
grows.

<Preview>
  <TypesetDocs.TypesetStreamingExample />

  <Preview.CSS>
    {`
      .typeset-chat {
        --typeset-leading: 1.6;
        --typeset-flow: 0.875em;
      }
    `}
  </Preview.CSS>
</Preview>

### Scrollable table

Wrap a table generated by the renderer in `Typeset.Scroll` when it should scroll instead of
squeezing into a narrow container.

<Preview>
  <TypesetDocs.TypesetScrollableTableExample />

<Preview.Code>
  <include lang="tsx">./_snippets/typeset/scrollable-table.tsx#demo</include>
</Preview.Code>

  <Preview.CSS>
    {`
      .wide-table {
        min-width: 36rem;
      }
    `}
  </Preview.CSS>
</Preview>

### Markdown extensions

Task lists, disclosures, definition lists, and GFM footnotes use the same tokens and one-direction
block spacing as the core HTML elements.

<Preview>
  <TypesetDocs.TypesetMarkdownExtensionsExample />

  <Preview.Code>
    <include lang="tsx">./_snippets/typeset/markdown-extensions.tsx#demo</include>
  </Preview.Code>
</Preview>

### Excluding application surfaces

Add `.not-typeset` or `data-not-typeset` to a nested subtree with its own visual contract. Both
exclude the marked element and everything inside it, including a nested `Typeset` root.

<Preview>
  <TypesetDocs.TypesetExcludedSubtreeExample />

<Preview.Code>
  <include lang="tsx">./_snippets/typeset/excluded-subtree.tsx#demo</include>
</Preview.Code>

  <Preview.CSS>
    {`
      .app-surface {
        padding: var(--spacing-4);
        border: var(--border-width-sm) solid var(--color-border);
        border-radius: var(--radius-md);
        background: var(--color-muted);
      }
    `}
  </Preview.CSS>
</Preview>

## Styling

### CSS Properties

<div className="not-prose">
  <Tabs items={['CSS Variables']} className="mb-0 mt-3">
    <Tab className="max-h-150 overflow-auto">
      <TypesetDocs.TypesetCssPropertiesPanel />
    </Tab>
  </Tabs>
</div>

The three rhythm controls are `--typeset-size`, `--typeset-leading`, and `--typeset-flow`. Font
variables are optional; colors, borders, and radius come straight from the active moduix theme.
Element rules live in the `ui.components` layer and use low-specificity `:where()` selectors, so
normal application CSS can override individual elements without `!important`.

### Styling hooks

- `data-slot="typeset"` targets the root container.
- `data-slot="typeset-scroll"` targets the optional overflow wrapper.
- `[data-scope="typeset"][data-part="root"]` and `[data-part="scroll"]` expose stable anatomy
  hooks.