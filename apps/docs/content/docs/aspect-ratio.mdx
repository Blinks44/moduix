---
title: Aspect Ratio
description: Constrains media and embed content inside a responsive box with a fixed aspect ratio.
icon: Component
---

import {
  AspectRatioAsChildExample,
  AspectRatioCardGridExample,
  AspectRatioCssPropertiesPanel,
  AspectRatioCustomRadiusExample,
  AspectRatioCustomRatioExample,
  AspectRatioEmbedExample,
  AspectRatioExample,
  AspectRatioPhotoExample,
  AspectRatioPortraitExample,
  AspectRatioSquareExample,
  aspectRatioFrameCss,
  aspectRatioGridCss,
  aspectRatioImageCss,
  aspectRatioNarrowImageCss,
  aspectRatioRoundedCss,
} from '@/components/examples/aspect-ratio';

## API Reference

Ark UI does not ship a dedicated `aspect-ratio` primitive. Moduix builds this component with the official
[Ark factory](https://ark-ui.com/docs/guides/composition#the-ark-factory), follows Ark's
[composition](https://ark-ui.com/docs/guides/composition), [styling](https://ark-ui.com/docs/guides/styling), and
[ref](https://ark-ui.com/docs/guides/ref) guidance.

## Basic

Use `AspectRatio` with a numeric `ratio` for responsive images, videos, embeds, and canvas content.

<Preview>
  <AspectRatioExample />

  <Preview.Code>
    {`
      import { AspectRatio } from "@moduix/react";

      export function AspectRatioDemo() {
        return (
          <div className="aspect-ratio-demo">
            <AspectRatio ratio={16 / 9}>
              <img
                src={image.src}
                alt={image.alt}
                className="aspect-ratio-demo__image"
              />
            </AspectRatio>
          </div>
        );
      }
    `}

  </Preview.Code>

<Preview.CSS>{aspectRatioImageCss}</Preview.CSS>

  <Preview.Data>
    {`
      const image = {
        src: "https://images.unsplash.com/photo-1506905925346-21bda4d32df4?w=800&q=80",
        alt: "Mountain landscape",
      };
    `}

  </Preview.Data>
</Preview>

## Install with shadcn

<Tabs items={['npm', 'pnpm', 'bun', 'deno', 'aube']}>
  <Tab value="npm">

```bash
npx shadcn@latest add Blinks44/moduix/react-aspect-ratio
```

  </Tab>
  <Tab value="pnpm">

```bash
pnpm dlx shadcn@latest add Blinks44/moduix/react-aspect-ratio
```

  </Tab>
  <Tab value="bun">

```bash
bunx shadcn@latest add Blinks44/moduix/react-aspect-ratio
```

  </Tab>
  <Tab value="deno">

```bash
deno x -A npm:shadcn@latest add Blinks44/moduix/react-aspect-ratio
```

  </Tab>
  <Tab value="aube">

```bash
aubx shadcn@latest add Blinks44/moduix/react-aspect-ratio
```

  </Tab>
</Tabs>

## Anatomy

```text
AspectRatio.Root
└─ media | iframe | canvas | svg | custom content
```

| Part                               | Role                            |
| ---------------------------------- | ------------------------------- |
| `AspectRatio` / `AspectRatio.Root` | Responsive frame with clipping. |

## Composition

### Recommended composition

Use `AspectRatio` for most media frames. `AspectRatio.Root` is the same root exposed for namespace consistency.

- `ratio` is required and numeric; pass a finite value greater than zero for valid CSS behavior
- `asChild` uses Ark factory composition and requires exactly one React element
- direct `img`, `video`, `iframe`, `canvas`, and `svg` children fill the frame automatically
- `className` styles the root directly
- `--aspect-ratio-radius` controls the default rounded frame

### When to go custom

Wrap `AspectRatio` when shadows, focus rings, or layout decoration need to escape the clipped
frame. Keep the clipping on the root itself.

### Default props

| Entry                   | Default            | Values / Notes                    |
| ----------------------- | ------------------ | --------------------------------- |
| `ratio`                 | -                  | Required positive finite `number` |
| `className`             | `undefined`        | `string`                          |
| `style`                 | `undefined`        | Can override inline `aspectRatio` |
| `asChild`               | `undefined`        | Ark factory composition           |
| `--aspect-ratio-radius` | `var(--radius-md)` | CSS length or token value         |

### Behavioral notes

- The root renders with `data-scope="aspect-ratio"`, `data-part="root"`, and
  `data-slot="aspect-ratio-root"`.
- `position: relative` on the root is intentional and supports absolute-fill child patterns such
  as Next.js `Image fill`.
- The previous preset strings (`square`, `video`, `portrait`, `photo`) were removed during the Ark
  migration. Use numbers such as `1`, `16 / 9`, `9 / 16`, and `4 / 3`.

## Examples

### Square

Use a square ratio for avatars, thumbnails, and other media that should remain visually balanced across layouts.

<Preview>
  <AspectRatioSquareExample />

  <Preview.Code>
    {`
      import { AspectRatio } from "@moduix/react";

      export function SquareAspectRatioDemo() {
        return (
          <div className="aspect-ratio-demo">
            <AspectRatio ratio={ratio}>
              <img
                src={media.src}
                alt={media.alt}
                className="aspect-ratio-demo__image"
              />
            </AspectRatio>
          </div>
        );
      }
    `}

  </Preview.Code>

<Preview.CSS>{aspectRatioImageCss}</Preview.CSS>

  <Preview.Data>
    {`
      const ratio = 1;
      const media = {
        src: "https://images.unsplash.com/photo-1506905925346-21bda4d32df4?w=800&q=80",
        alt: "Mountain landscape",
      };
    `}

  </Preview.Data>
</Preview>

### Portrait

Choose a portrait ratio when the frame should favor tall media such as profile photography or editorial art.

<Preview>
  <AspectRatioPortraitExample />

  <Preview.Code>
    {`
      import { AspectRatio } from "@moduix/react";

      export function PortraitAspectRatioDemo() {
        return (
          <div className="aspect-ratio-demo aspect-ratio-demo--narrow">
            <AspectRatio ratio={ratio}>
              <img
                src={media.src}
                alt={media.alt}
                className="aspect-ratio-demo__image"
              />
            </AspectRatio>
          </div>
        );
      }
    `}

  </Preview.Code>

<Preview.CSS>{aspectRatioNarrowImageCss}</Preview.CSS>

  <Preview.Data>
    {`
      const ratio = 9 / 16;
      const media = {
        src: "https://images.unsplash.com/photo-1506905925346-21bda4d32df4?w=800&q=80",
        alt: "Mountain landscape",
      };
    `}

  </Preview.Data>
</Preview>

### Photo

Use a photo-friendly ratio such as `4 / 3` when landscape imagery needs a familiar editorial crop.

<Preview>
  <AspectRatioPhotoExample />

  <Preview.Code>
    {`
      import { AspectRatio } from "@moduix/react";

      export function PhotoAspectRatioDemo() {
        return (
          <div className="aspect-ratio-demo">
            <AspectRatio ratio={ratio}>
              <img
                src={media.src}
                alt={media.alt}
                className="aspect-ratio-demo__image"
              />
            </AspectRatio>
          </div>
        );
      }
    `}

  </Preview.Code>

<Preview.CSS>{aspectRatioImageCss}</Preview.CSS>

  <Preview.Data>
    {`
      const ratio = 4 / 3;
      const media = {
        src: "https://images.unsplash.com/photo-1506905925346-21bda4d32df4?w=800&q=80",
        alt: "Mountain landscape",
      };
    `}

  </Preview.Data>
</Preview>

### Card Grid

Keep repeated cards visually aligned by wrapping their media in the same aspect ratio even when the source assets differ.

<Preview>
  <AspectRatioCardGridExample />

  <Preview.Code>
    {`
      import { AspectRatio, Card } from "@moduix/react";

      export function AspectRatioCardGridDemo() {
        return (
          <div className="aspect-ratio-grid">
            {stories.map((story) => (
              <Card key={story.title} className="aspect-ratio-grid__card">
                <AspectRatio ratio={16 / 9} className="aspect-ratio-grid__media">
                  <img
                    src={story.src}
                    alt={story.alt}
                    className="aspect-ratio-grid__image"
                  />
                </AspectRatio>
                <div className="aspect-ratio-grid__body">
                  <h3 className="aspect-ratio-grid__title">{story.title}</h3>
                  <p className="aspect-ratio-grid__description">
                    {story.description}
                  </p>
                </div>
              </Card>
            ))}
          </div>
        );
      }
    `}

  </Preview.Code>

<Preview.CSS>{aspectRatioGridCss}</Preview.CSS>

  <Preview.Data>
    {`
      const stories = [
        {
          src: "https://images.unsplash.com/photo-1500530855697-b586d89ba3ee?w=1200&h=720&q=80",
          alt: "Evening city skyline",
          title: "City center opens a redesigned pedestrian corridor",
          description: "The landscape source stays aligned with the neighboring card.",
        },
        {
          src: "https://images.unsplash.com/photo-1515886657613-9f3515b0c78f?w=720&h=1200&q=80",
          alt: "Reporter portrait on a street background",
          title: "Editors uploaded a tall portrait photo for this story",
          description: "AspectRatio and object-fit keep the media height stable.",
        },
      ];
    `}

  </Preview.Data>
</Preview>

### Custom Ratio

Pass a custom numeric ratio when the design calls for a format outside the usual square, portrait, or widescreen values.

<Preview>
  <AspectRatioCustomRatioExample />

  <Preview.Code>
    {`
      import { AspectRatio } from "@moduix/react";

      export function CustomRatioAspectRatioDemo() {
        return (
          <div className="aspect-ratio-demo">
            <AspectRatio ratio={ratio}>
              <img
                src={media.src}
                alt={media.alt}
                className="aspect-ratio-demo__image"
              />
            </AspectRatio>
          </div>
        );
      }
    `}

  </Preview.Code>

<Preview.CSS>{aspectRatioImageCss}</Preview.CSS>

  <Preview.Data>
    {`
      const ratio = 2;
      const media = {
        src: "https://images.unsplash.com/photo-1506905925346-21bda4d32df4?w=800&q=80",
        alt: "Mountain landscape",
      };
    `}

  </Preview.Data>
</Preview>

### Embedded Content

Wrap video or other embeds in `AspectRatio` to keep the frame stable before the external content finishes loading.

<Preview>
  <AspectRatioEmbedExample />

  <Preview.Code>
    {`
      import { AspectRatio } from "@moduix/react";

      export function AspectRatioEmbedDemo() {
        return (
          <div className="aspect-ratio-demo">
            <AspectRatio ratio={embed.ratio}>
              <iframe
                src={embed.src}
                title={embed.title}
                allow="autoplay; encrypted-media"
                allowFullScreen
                className="aspect-ratio-demo__frame"
              />
            </AspectRatio>
          </div>
        );
      }
    `}

  </Preview.Code>

<Preview.CSS>{aspectRatioFrameCss}</Preview.CSS>

  <Preview.Data>
    {`
      const embed = {
        ratio: 16 / 9,
        src: "https://www.youtube.com/embed/dQw4w9WgXcQ",
        title: "Video embed",
      };
    `}

  </Preview.Data>
</Preview>

### Custom Composition

Use `asChild` when a semantic element such as `figure` should own the frame. Ark merges the
component props into the single child element.

<Preview>
  <AspectRatioAsChildExample />

  <Preview.Code>
    {`
      import { AspectRatio } from "@moduix/react";

      export function AspectRatioAsChildDemo() {
        return (
          <div className="aspect-ratio-demo">
            <AspectRatio ratio={media.ratio} asChild>
              <figure>
                <img
                  src={media.src}
                  alt={media.alt}
                  className="aspect-ratio-demo__image"
                />
              </figure>
            </AspectRatio>
          </div>
        );
      }
    `}

  </Preview.Code>

<Preview.CSS>{aspectRatioImageCss}</Preview.CSS>

  <Preview.Data>
    {`
      const media = {
        ratio: 16 / 9,
        src: "https://images.unsplash.com/photo-1506905925346-21bda4d32df4?w=800&q=80",
        alt: "Mountain landscape",
      };
    `}

  </Preview.Data>
</Preview>

### Custom Styling

Use `className` and `--aspect-ratio-radius` when the frame needs local radius changes without affecting the child content.

<Preview>
  <AspectRatioCustomRadiusExample />

  <Preview.Code>
    {`
      import { AspectRatio } from "@moduix/react";

      export function CustomRadiusAspectRatioDemo() {
        return (
          <div className="aspect-ratio-demo">
            <AspectRatio ratio={media.ratio} className="aspect-ratio-demo__rounded">
              <img
                src={media.src}
                alt={media.alt}
                className="aspect-ratio-demo__image"
              />
            </AspectRatio>
          </div>
        );
      }
    `}

  </Preview.Code>

<Preview.CSS>{aspectRatioRoundedCss}</Preview.CSS>

  <Preview.Data>
    {`
      const media = {
        ratio: 16 / 9,
        src: "https://images.unsplash.com/photo-1506905925346-21bda4d32df4?w=800&q=80",
        alt: "Mountain landscape",
      };
    `}

  </Preview.Data>
</Preview>

## Styling

### CSS Properties

<div className="not-prose">
  <Tabs items={['CSS Variables']} className="mb-0 mt-3">
    <Tab className="max-h-150 overflow-auto">
      <AspectRatioCssPropertiesPanel />
    </Tab>
  </Tabs>
</div>

### Styling hooks

- `data-scope="aspect-ratio"` and `data-part="root"` are the Ark-aligned root hooks.
- `data-slot="aspect-ratio-root"` is the stable moduix root hook.
- `overflow: hidden` is intentional. If visible decoration must escape the frame, put that
  decoration on an outer wrapper instead of removing root clipping.