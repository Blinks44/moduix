---
title: Aspect Ratio
description: Constrains media and embed content inside a responsive box with a fixed aspect ratio.
icon: Component
---

import {
  AspectRatioCardGridExample,
  AspectRatioCssPropertiesPanel,
  AspectRatioCustomRadiusExample,
  AspectRatioCustomRatioExample,
  AspectRatioEmbedExample,
  AspectRatioExample,
  AspectRatioPhotoExample,
  AspectRatioPortraitExample,
  AspectRatioSquareExample,
} from '@/components/examples/aspect-ratio';

## API Reference

`AspectRatio` is a standalone moduix component and does not wrap a Base UI primitive.

## Basic

Start with `ratio="video"` for the most common responsive media frame. This is the recommended
path for images, videos, embeds, and canvas-based content that should keep a stable shape while the
parent width changes.

<Preview>
  <AspectRatioExample />

  <Preview.Code>
    {`
      import { AspectRatio } from "moduix";
      import styles from "./aspect-ratio-demo.module.css";

      export function AspectRatioDemo() {
        return (
          <div className={styles.container}>
            <AspectRatio ratio="video">
              <img
                src="https://images.unsplash.com/photo-1506905925346-21bda4d32df4?w=800&q=80"
                alt="Mountain landscape"
                className={styles.image}
              />
            </AspectRatio>
          </div>
        );
      }
    `}

  </Preview.Code>

  <Preview.CSS>
    {`
      .container {
        width: min(30rem, calc(100vw - var(--spacing-8)));
      }

      .image {
        object-fit: cover;
      }
    `}

  </Preview.CSS>
</Preview>

## Install with shadcn (optional)

If you want `Aspect Ratio` to become part of your own project source instead of staying in
`node_modules`, install it from the moduix GitHub registry:

<Tabs items={['npm', 'pnpm', 'bun', 'deno', 'aube']}>
  <Tab value="npm">

```bash
npx shadcn@latest add Blinks44/moduix/aspect-ratio
```

  </Tab>
  <Tab value="pnpm">

```bash
pnpm dlx shadcn@latest add Blinks44/moduix/aspect-ratio
```

  </Tab>
  <Tab value="bun">

```bash
bunx shadcn@latest add Blinks44/moduix/aspect-ratio
```

  </Tab>
  <Tab value="deno">

```bash
deno x -A npm:shadcn@latest add Blinks44/moduix/aspect-ratio
```

  </Tab>
  <Tab value="aube">

```bash
aubx shadcn@latest add Blinks44/moduix/aspect-ratio
```

  </Tab>
</Tabs>

This install path also pulls in the shared moduix foundation files that `Aspect Ratio` depends on,
such as styles, icons, and small utilities.

## Anatomy

`AspectRatio` exposes one root. There are no sub-parts or slot prop systems.

```text
AspectRatio
└─ root[data-slot="aspect-ratio-root"]
```

| Part          | Role                                                                 |
| ------------- | -------------------------------------------------------------------- |
| `AspectRatio` | Root media frame. Controls the ratio, clipping, and rounded surface. |

## Composition

### When to stay high-level

Stay with `AspectRatio` directly for most image, video, iframe, canvas, and SVG frames.

The component stays intentionally small:

- `ratio` is required; there is no default that could hide layout mistakes
- direct `img`, `video`, `iframe`, `canvas`, and `svg` children fill the box automatically
- `className` styles the root directly
- `--aspect-ratio-radius` customizes the default rounded surface without extra wrapper markup

### When to go custom

Switch to a custom wrapper only when something outside the clipped frame needs to escape the box,
such as a shadow, focus treatment, or outer layout decoration. Keep the built-in clipping on
`AspectRatio` itself.

### Default props

| Entry                   | Default            | Values / Notes                                    |
| ----------------------- | ------------------ | ------------------------------------------------- |
| `ratio`                 | -                  | `number`, `square`, `video`, `portrait`, `photo`  |
| `className`             | `undefined`        | `string`                                          |
| `style`                 | `undefined`        | Style object. Can override the inline ratio style |
| `--aspect-ratio-radius` | `var(--radius-md)` | CSS length or token value                         |

### Behavioral notes

- Named presets resolve to numeric ratios at render time: `square`, `video`, `portrait`, `photo`.
- The resolved ratio is written as inline `aspectRatio` CSS.
- Direct media children auto-fill the box, but `object-fit` remains the consumer's responsibility.
- `position: relative` on the root is intentional and supports absolute-fill child patterns such as
  Next.js `<Image fill>`.

## Examples

### Square

Use `ratio="square"` for avatars, thumbnails, and grid media.

<Preview>
  <AspectRatioSquareExample />

  <Preview.Code>
    {`
      import { AspectRatio } from "moduix";
      import styles from "./aspect-ratio-demo.module.css";

      export function SquareAspectRatioDemo() {
        return (
          <div className={styles.container}>
            <AspectRatio ratio="square">
              <img
                src="https://images.unsplash.com/photo-1506905925346-21bda4d32df4?w=800&q=80"
                alt="Mountain landscape"
                className={styles.image}
              />
            </AspectRatio>
          </div>
        );
      }
    `}

  </Preview.Code>

  <Preview.CSS>
    {`
      .container {
        width: min(30rem, calc(100vw - var(--spacing-8)));
      }

      .image {
        object-fit: cover;
      }
    `}

  </Preview.CSS>
</Preview>

### Portrait

Use `ratio="portrait"` when the content should read as a tall phone or poster frame.

<Preview>
  <AspectRatioPortraitExample />

  <Preview.Code>
    {`
      import { AspectRatio } from "moduix";
      import styles from "./aspect-ratio-demo.module.css";

      export function PortraitAspectRatioDemo() {
        return (
          <div className={styles.narrowContainer}>
            <AspectRatio ratio="portrait">
              <img
                src="https://images.unsplash.com/photo-1506905925346-21bda4d32df4?w=800&q=80"
                alt="Mountain landscape"
                className={styles.image}
              />
            </AspectRatio>
          </div>
        );
      }
    `}

  </Preview.Code>

  <Preview.CSS>
    {`
      .narrowContainer {
        width: min(15rem, calc(100vw - var(--spacing-8)));
      }

      .image {
        object-fit: cover;
      }
    `}

  </Preview.CSS>
</Preview>

### Photo

Use `ratio="photo"` for a classic `4 / 3` media frame.

<Preview>
  <AspectRatioPhotoExample />

  <Preview.Code>
    {`
      import { AspectRatio } from "moduix";
      import styles from "./aspect-ratio-demo.module.css";

      export function PhotoAspectRatioDemo() {
        return (
          <div className={styles.container}>
            <AspectRatio ratio="photo">
              <img
                src="https://images.unsplash.com/photo-1506905925346-21bda4d32df4?w=800&q=80"
                alt="Mountain landscape"
                className={styles.image}
              />
            </AspectRatio>
          </div>
        );
      }
    `}

  </Preview.Code>

  <Preview.CSS>
    {`
      .container {
        width: min(30rem, calc(100vw - var(--spacing-8)));
      }

      .image {
        object-fit: cover;
      }
    `}

  </Preview.CSS>
</Preview>

### Card Grid

This is the common editorial case: two cards sit side by side, but the uploaded images have very
different source proportions. Giving both media blocks the same `AspectRatio` and `object-fit: cover`
keeps the visual media height aligned instead of letting the cards jump.

<Preview>
  <AspectRatioCardGridExample />

  <Preview.Code>
    {`
      import { AspectRatio, Card } from "moduix";
      import styles from "./aspect-ratio-demo.module.css";

      export function AspectRatioCardGridDemo() {
        return (
          <div className={styles.cardGrid}>
            <Card className={styles.newsCard}>
              <AspectRatio ratio="video" className={styles.newsMedia}>
                <img
                  src="https://images.unsplash.com/photo-1500530855697-b586d89ba3ee?w=1200&h=720&q=80"
                  alt="Evening city skyline"
                  className={styles.image}
                />
              </AspectRatio>
              <div className={styles.newsBody}>
                <h3 className={styles.newsTitle}>City center opens a redesigned pedestrian corridor</h3>
                <p className={styles.newsDescription}>
                  The source image is landscape, but the media frame stays aligned with the card beside it.
                </p>
              </div>
            </Card>

            <Card className={styles.newsCard}>
              <AspectRatio ratio="video" className={styles.newsMedia}>
                <img
                  src="https://images.unsplash.com/photo-1515886657613-9f3515b0c78f?w=720&h=1200&q=80"
                  alt="Reporter portrait on a street background"
                  className={styles.image}
                />
              </AspectRatio>
              <div className={styles.newsBody}>
                <h3 className={styles.newsTitle}>
                  Editors uploaded a tall portrait photo for this story
                </h3>
                <p className={styles.newsDescription}>
                  AspectRatio plus object-fit: cover keeps the media height the same instead of letting the card jump.
                </p>
              </div>
            </Card>
          </div>
        );
      }
    `}

  </Preview.Code>
</Preview>

### Custom Ratio

Use a numeric ratio when the named presets are not specific enough.

<Preview>
  <AspectRatioCustomRatioExample />

  <Preview.Code>
    {`
      import { AspectRatio } from "moduix";
      import styles from "./aspect-ratio-demo.module.css";

      export function CustomRatioAspectRatioDemo() {
        return (
          <div className={styles.container}>
            <AspectRatio ratio={2.35}>
              <img
                src="https://images.unsplash.com/photo-1506905925346-21bda4d32df4?w=800&q=80"
                alt="Mountain landscape"
                className={styles.image}
              />
            </AspectRatio>
          </div>
        );
      }
    `}

  </Preview.Code>

  <Preview.CSS>
    {`
      .container {
        width: min(30rem, calc(100vw - var(--spacing-8)));
      }

      .image {
        object-fit: cover;
      }
    `}

  </Preview.CSS>
</Preview>

### Embedded Content

Use `AspectRatio` for iframe-based video or map embeds when the frame should scale responsively.

<Preview>
  <AspectRatioEmbedExample />

  <Preview.Code>
    {`
      import { AspectRatio } from "moduix";
      import styles from "./aspect-ratio-demo.module.css";

      export function AspectRatioEmbedDemo() {
        return (
          <div className={styles.container}>
            <AspectRatio ratio="video">
              <iframe
                src="https://www.youtube.com/embed/dQw4w9WgXcQ"
                title="Video embed"
                allow="autoplay; encrypted-media"
                allowFullScreen
                className={styles.frame}
              />
            </AspectRatio>
          </div>
        );
      }
    `}

  </Preview.Code>
</Preview>

### Custom Radius

Override `--aspect-ratio-radius` when the frame should be sharper, larger, or fully square.

<Preview>
  <AspectRatioCustomRadiusExample />

  <Preview.Code>
    {`
      import { AspectRatio } from "moduix";
      import styles from "./aspect-ratio-demo.module.css";

      export function CustomRadiusAspectRatioDemo() {
        return (
          <div className={styles.container}>
            <AspectRatio ratio="video" className={styles.roundedFrame}>
              <img
                src="https://images.unsplash.com/photo-1506905925346-21bda4d32df4?w=800&q=80"
                alt="Mountain landscape"
                className={styles.image}
              />
            </AspectRatio>
          </div>
        );
      }
    `}

  </Preview.Code>

  <Preview.CSS>
    {`
      .roundedFrame {
        --aspect-ratio-radius: var(--radius-xl);
      }

      .image {
        object-fit: cover;
      }
    `}

  </Preview.CSS>
</Preview>

## Styling

`AspectRatio` has one public styling token, and its root is the only stable styling surface.

### CSS Properties

<div className="not-prose">
  <Tabs items={['CSS Variables']} className="mb-0 mt-3">
    <Tab className="max-h-150 overflow-auto">
      <AspectRatioCssPropertiesPanel />
    </Tab>
  </Tabs>
</div>

### Styling hooks

- `data-slot="aspect-ratio-root"` is the stable selector on the exported root.
- `overflow: hidden` and the default clipping are intentional. If a child needs a visible shadow
  or focus treatment outside the frame, put that treatment on an outer wrapper instead of removing
  root clipping.