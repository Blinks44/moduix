---
title: Split Button
description: A grouped primary action and Ark Menu dropdown trigger built from moduix Button and Menu parts.
icon: Component
---

import {
  SplitButtonControlledExample,
  SplitButtonCustomPositioningExample,
  SplitButtonExample,
  SplitButtonIconExample,
  SplitButtonLinkActionExample,
  SplitButtonSizesExample,
  SplitButtonStylingExample,
  SplitButtonVariantsExample,
  splitButtonCustomStylingCss,
  splitButtonExampleCss,
  splitButtonItemsData,
  splitButtonSizesData,
  splitButtonVariantsData,
} from '@/components/examples/split-button';

## API Reference

Ark UI does not provide a dedicated Split Button primitive. `SplitButton` is a moduix-owned
composition built on [Ark Menu](https://ark-ui.com/docs/components/menu), the
[Ark `asChild` composition model](https://ark-ui.com/docs/guides/composition), and the
[Chakra Button split-menu recipe](https://chakra-ui.com/docs/components/button#split-menu).

## Basic

Use `SplitButton` as the short root form, then keep the Ark-style structure visible with
`SplitButton.Action`, `SplitButton.Trigger`, `Portal`, `SplitButton.Positioner`, and
`SplitButton.Content`.

<Preview>
  <SplitButtonExample />

  <Preview.Code>
    {`
      import { Menu, Portal, SplitButton } from "@moduix/react";

      export function SplitButtonDemo() {
        return (
          <SplitButton>
            <SplitButton.Action>Save Changes</SplitButton.Action>
            <SplitButton.Trigger />
            <Portal>
              <SplitButton.Positioner>
                <SplitButton.Content>
                  {items.slice(0, 2).map((item) => (
                    <Menu.Item key={item.value} value={item.value}>
                      {item.label}
                    </Menu.Item>
                  ))}
                  <Menu.Separator />
                  <Menu.Item value={items[2].value}>{items[2].label}</Menu.Item>
                </SplitButton.Content>
              </SplitButton.Positioner>
            </Portal>
          </SplitButton>
        );
      }
    `}

  </Preview.Code>

  <Preview.CSS>{splitButtonExampleCss}</Preview.CSS>
  <Preview.Data>{splitButtonItemsData}</Preview.Data>
</Preview>

## Install with shadcn (optional)

If you want `Split Button` to become part of your own project source instead of staying in
`node_modules`, install it from the moduix GitHub registry:

<Tabs items={['npm', 'pnpm', 'bun', 'deno', 'aube']}>
  <Tab value="npm">

```bash
npx shadcn@latest add Blinks44/moduix/react-split-button
```

  </Tab>
  <Tab value="pnpm">

```bash
pnpm dlx shadcn@latest add Blinks44/moduix/react-split-button
```

  </Tab>
  <Tab value="bun">

```bash
bunx shadcn@latest add Blinks44/moduix/react-split-button
```

  </Tab>
  <Tab value="deno">

```bash
deno x -A npm:shadcn@latest add Blinks44/moduix/react-split-button
```

  </Tab>
  <Tab value="aube">

```bash
aubx shadcn@latest add Blinks44/moduix/react-split-button
```

  </Tab>
</Tabs>

## Anatomy

```text
SplitButton.Root
├─ SplitButton.Action
├─ SplitButton.Trigger
└─ Portal
   └─ SplitButton.Positioner
      └─ SplitButton.Content
         ├─ Menu.Item
         ├─ Menu.Separator
         └─ other shared Menu parts
```

| Part                               | Role                                                                  |
| ---------------------------------- | --------------------------------------------------------------------- |
| `SplitButton` / `SplitButton.Root` | Owns Ark Menu root state and the grouped visual wrapper.              |
| `SplitButton.Action`               | Primary Button action. Inherits root `variant` and `size` by default. |
| `SplitButton.Trigger`              | Ark Menu trigger rendered as a moduix Button with a chevron fallback. |
| `Portal`                           | Portal boundary for the menu popup.                                   |
| `SplitButton.Positioner`           | Ark Menu positioner with placement variables and layer z-index.       |
| `SplitButton.Content`              | Ark Menu content surface for shared `Menu.*` rows.                    |

## Composition

### Recommended composition

`SplitButton.Root` forwards Ark `Menu.Root` props such as `open`, `defaultOpen`,
`onOpenChange(details)`, `onSelect(details)`, `closeOnSelect`, `positioning`, `ids`, `lazyMount`,
`unmountOnExit`, and `typeahead`.

Use `SplitButton.Action` for the primary path. Put secondary actions in `SplitButton.Content` with
the shared `Menu.Item`, `Menu.Separator`, `Menu.ItemGroup`, `Menu.CheckboxItem`, and `Menu.RadioItem`
parts.

### When to go custom

Use `asChild` on `SplitButton.Action` when the primary action should render as a semantic link or
another compatible element.

Use `asChild` on `SplitButton.Trigger` only with a single semantic child that can receive Ark Menu
trigger props and keyboard behavior.

Use `positioning` on `SplitButton.Root` for popup placement. Keep `Portal`,
`SplitButton.Positioner`, and `SplitButton.Content` explicit so consumers can style or configure the
popup structure without rewriting the component.

When the menu itself needs advanced state access, compose directly with `Menu.RootProvider` and
`useMenu()` from the shared `Menu` API rather than adding split-button-specific state wrappers.

### Default props

| Surface               | Prop          | Default                                  | Notes                                                  |
| --------------------- | ------------- | ---------------------------------------- | ------------------------------------------------------ |
| `SplitButton.Root`    | `variant`     | `'default'`                              | Shared default for `Action` and `Trigger`.             |
| `SplitButton.Root`    | `size`        | `'md'`                                   | Shared default for `Action` and `Trigger`.             |
| `SplitButton.Root`    | `positioning` | `{ placement: 'bottom-end', gutter: 4 }` | Keeps the popup aligned to the trigger edge.           |
| `SplitButton.Trigger` | `aria-label`  | `'More actions'` when icon-only          | Override for a more specific accessible name.          |
| `SplitButton.Trigger` | `children`    | `ChevronDownIcon`                        | Replace when the trigger needs custom visible content. |

### Behavioral notes

- `SplitButton` has no legacy render contract and no flat part exports.
- `SplitButton.Action` follows the shared moduix `Button` `asChild` contract.
- `SplitButton.Trigger` receives Ark Menu trigger data attributes such as `data-state`, `data-side`,
  `data-placement`, and `data-controls`.
- `SplitButton.Positioner` receives Ark Menu CSS variables such as `--reference-width`,
  `--available-height`, `--x`, `--y`, `--z-index`, and `--transform-origin`.
- The root `variant` excludes the `link` button variant because grouped link-style split buttons do
  not form a coherent control.
- The root `size` excludes icon-only button sizes because the default path expects a visible label on
  the primary action.

## Examples

### Variants

Use the root `variant` when both grouped buttons should share the same visual tone.

<Preview>
  <SplitButtonVariantsExample />

  <Preview.Code>
    {`
      import { Menu, Portal, SplitButton } from "@moduix/react";

      export function SplitButtonVariantsDemo() {
        return (
          <div className="row">
            {variants.map((variant) => (
              <SplitButton key={variant} variant={variant}>
                <SplitButton.Action>{variant}</SplitButton.Action>
                <SplitButton.Trigger />
                <Portal>
                  <SplitButton.Positioner>
                    <SplitButton.Content>
                      <Menu.Item value={\`\${variant}-edit\`}>Edit</Menu.Item>
                      <Menu.Item value={\`\${variant}-duplicate\`}>Duplicate</Menu.Item>
                    </SplitButton.Content>
                  </SplitButton.Positioner>
                </Portal>
              </SplitButton>
            ))}
          </div>
        );
      }
    `}

  </Preview.Code>

  <Preview.CSS>{splitButtonExampleCss}</Preview.CSS>
  <Preview.Data>{splitButtonVariantsData}</Preview.Data>
</Preview>

### Sizes

Use the root `size` when both sides should scale together.

<Preview>
  <SplitButtonSizesExample />

  <Preview.Code>
    {`
      import { Menu, Portal, SplitButton } from "@moduix/react";

      export function SplitButtonSizesDemo() {
        return (
          <div className="row">
            {sizes.map((size) => (
              <SplitButton key={size} size={size} variant="outline">
                <SplitButton.Action>{size}</SplitButton.Action>
                <SplitButton.Trigger />
                <Portal>
                  <SplitButton.Positioner>
                    <SplitButton.Content>
                      <Menu.Item value={\`\${size}-create\`}>Create</Menu.Item>
                      <Menu.Item value={\`\${size}-create-open\`}>Create and Open</Menu.Item>
                    </SplitButton.Content>
                  </SplitButton.Positioner>
                </Portal>
              </SplitButton>
            ))}
          </div>
        );
      }
    `}

  </Preview.Code>

  <Preview.CSS>{splitButtonExampleCss}</Preview.CSS>
  <Preview.Data>{splitButtonSizesData}</Preview.Data>
</Preview>

### With Icons

Add an icon to the primary side when it clarifies the default action without replacing the text
label.

<Preview>
  <SplitButtonIconExample />

  <Preview.Code>
    {`
      import { Menu, PlusIcon, Portal, SplitButton } from "@moduix/react";

      export function SplitButtonIconsDemo() {
        return (
          <SplitButton>
            <SplitButton.Action>
              <PlusIcon />
              Create Item
            </SplitButton.Action>
            <SplitButton.Trigger aria-label="More create actions" />
            <Portal>
              <SplitButton.Positioner>
                <SplitButton.Content>
                  <Menu.Item value="create-blank">Create Blank</Menu.Item>
                  <Menu.Item value="create-template">Create From Template</Menu.Item>
                  <Menu.Separator />
                  <Menu.Item value="import-existing">Import Existing</Menu.Item>
                </SplitButton.Content>
              </SplitButton.Positioner>
            </Portal>
          </SplitButton>
        );
      }
    `}

  </Preview.Code>

  <Preview.CSS>{splitButtonExampleCss}</Preview.CSS>
  <Preview.Data>{`const createItems = ["Create Blank", "Create From Template", "Import Existing"];`}</Preview.Data>
</Preview>

### Controlled Menu State

Use the same controlled open-state contract as Ark Menu when outside state should own the dropdown.

<Preview>
  <SplitButtonControlledExample />

  <Preview.Code>
    {`
      import { Menu, Portal, SplitButton } from "@moduix/react";
      import { useState } from "react";

      export function ControlledSplitButton() {
        const [open, setOpen] = useState(false);

        return (
          <SplitButton open={open} onOpenChange={(details) => setOpen(details.open)} variant="outline">
            <SplitButton.Action>Share</SplitButton.Action>
            <SplitButton.Trigger aria-label="More share actions" />
            <Portal>
              <SplitButton.Positioner>
                <SplitButton.Content>
                  <Menu.Item value="copy-link">Copy Link</Menu.Item>
                  <Menu.Item value="invite-email">Invite by Email</Menu.Item>
                  <Menu.Separator />
                  <Menu.Item value="close-menu" onSelect={() => setOpen(false)}>
                    Close Menu
                  </Menu.Item>
                </SplitButton.Content>
              </SplitButton.Positioner>
            </Portal>
          </SplitButton>
        );
      }
    `}

  </Preview.Code>

  <Preview.CSS>{splitButtonExampleCss}</Preview.CSS>
  <Preview.Data>{`const [open, setOpen] = useState(false);`}</Preview.Data>
</Preview>

### Positioning

Configure popup placement with the Ark `positioning` root prop while keeping the structural popup
parts explicit.

<Preview>
  <SplitButtonCustomPositioningExample />

  <Preview.Code>
    {`
      import { Menu, Portal, SplitButton } from "@moduix/react";

      export function SplitButtonPositioningDemo() {
        return (
          <SplitButton positioning={{ placement: "bottom-start", gutter: 8 }} variant="outline">
            <SplitButton.Action>Export</SplitButton.Action>
            <SplitButton.Trigger aria-label="More export actions" />
            <Portal>
              <SplitButton.Positioner>
                <SplitButton.Content>
                  <Menu.Item value="export-pdf">Export PDF</Menu.Item>
                  <Menu.Item value="export-csv">Export CSV</Menu.Item>
                </SplitButton.Content>
              </SplitButton.Positioner>
            </Portal>
          </SplitButton>
        );
      }
    `}

  </Preview.Code>

  <Preview.CSS>{splitButtonExampleCss}</Preview.CSS>
  <Preview.Data>{`const positioning = { placement: "bottom-start", gutter: 8 };`}</Preview.Data>
</Preview>

### Custom Styling

Use root `className` and stable `data-slot` hooks when the grouped control needs a custom tone.

<Preview>
  <SplitButtonStylingExample />

  <Preview.Code>
    {`
      import { Menu, Portal, SplitButton } from "@moduix/react";
      import styles from "./split-button.module.css";

      export function SplitButtonStylingDemo() {
        return (
          <SplitButton className={styles.brandSplitButton} variant="outline">
            <SplitButton.Action>Review</SplitButton.Action>
            <SplitButton.Trigger aria-label="More review actions" />
            <Portal>
              <SplitButton.Positioner>
                <SplitButton.Content>
                  <Menu.Item value="approve">Approve</Menu.Item>
                  <Menu.Item value="request-changes">Request Changes</Menu.Item>
                </SplitButton.Content>
              </SplitButton.Positioner>
            </Portal>
          </SplitButton>
        );
      }
    `}

  </Preview.Code>

  <Preview.CSS>{splitButtonCustomStylingCss}</Preview.CSS>
  <Preview.Data>{`const reviewActions = ["Approve", "Request Changes"];`}</Preview.Data>
</Preview>

### Link Action

Use `asChild` on `SplitButton.Action` when the primary action should render as a link.

<Preview>
  <SplitButtonLinkActionExample />

  <Preview.Code>
    {`
      import { ArrowUpRightIcon, Menu, Portal, SplitButton } from "@moduix/react";

      export function SplitButtonLinkActionDemo() {
        return (
          <SplitButton variant="outline">
            <SplitButton.Action asChild>
              <a href="#split-button">
                Open Docs
                <ArrowUpRightIcon />
              </a>
            </SplitButton.Action>
            <SplitButton.Trigger aria-label="More docs actions" />
            <Portal>
              <SplitButton.Positioner>
                <SplitButton.Content>
                  <Menu.Item value="copy-link">Copy Link</Menu.Item>
                  <Menu.Item value="open-new-tab">Open in New Tab</Menu.Item>
                </SplitButton.Content>
              </SplitButton.Positioner>
            </Portal>
          </SplitButton>
        );
      }
    `}

  </Preview.Code>

  <Preview.CSS>{splitButtonExampleCss}</Preview.CSS>
  <Preview.Data>{`const docsActions = ["Copy Link", "Open in New Tab"];`}</Preview.Data>
</Preview>

## Styling

`SplitButton` uses moduix `Button` and `Menu` styles, plus split-button slots for the grouped
visual treatment.

### Styling hooks

- `data-scope="split-button"` and `data-part="root"` target the grouped wrapper.
- `data-slot="split-button-root"` targets the grouped wrapper.
- `data-slot="split-button-action"` targets the primary action side.
- `data-slot="split-button-trigger"` targets the dropdown trigger side.
- `data-slot="split-button-positioner"` targets the Ark Menu positioner wrapper.
- `data-slot="split-button-content"` targets the Ark Menu content surface.
- `SplitButton.Trigger` carries Ark Menu trigger state attributes including `data-state="open" |
"closed"`.
- `SplitButton.Content` carries Ark Menu content attributes including `data-state`, `data-side`,
  `data-placement`, `data-nested`, and `data-has-nested`.