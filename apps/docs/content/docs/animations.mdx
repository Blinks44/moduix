---
title: Animations
description: Motion token recipes for fade, slide, zoom, and directional appearance transitions in moduix.
icon: Sparkles
---

import {
  AnimationMotionPlaygroundPanel,
  AnimationMotionPropertiesPanel,
  MotionPlaygroundExample,
  MotionRecipesExample,
  animationMotionCssProperties,
} from '@/components/examples/animations';

# Animations

moduix appearance transitions expose phase-specific motion tokens for opacity, scale, and translate.
Shared `--duration-*`, `--ease-*`, `--transition-*`, and `--scale-*` tokens affect multiple
components; component-local tokens are preferable for isolated changes.

`*-starting-*` values apply during entry and `*-ending-*` values apply during exit. For the
Ark-backed `Popover`, the visual part is `Content`, so its token family is
`--popover-content-*`. Other component families may still use `popup`, `content`, or another
part-specific name.

## Live Preview

The preview uses Ark-aligned `Popover.Content` composition and CSS animations tied to its
`data-state` attribute.

<Preview cssProperties={animationMotionCssProperties} cssVariableScope="root">
  <MotionPlaygroundExample />

  <Preview.Code>
    {`
      import { Button, Popover } from "moduix";

      export function MotionPlaygroundDemo() {
        return (
          <Popover positioning={{ gutter: 12 }}>
            <Popover.Trigger asChild>
              <Button>Open motion playground</Button>
            </Popover.Trigger>
            <Popover.Portal>
              <Popover.Positioner>
                <Popover.Content>
                  Tune the motion tokens in the Playground tab.
                </Popover.Content>
              </Popover.Positioner>
            </Popover.Portal>
          </Popover>
        );
      }
    `}

  </Preview.Code>

  <Preview.CSSProperties>{(context) => <AnimationMotionPropertiesPanel {...context} />}</Preview.CSSProperties>
  <Preview.CSSPlayground>{(context) => <AnimationMotionPlaygroundPanel {...context} />}</Preview.CSSPlayground>
</Preview>

## Ready Recipes

Apply these variables to `Popover.Content` through `className`.

### Fade

```css
.fadeMotion {
  --popover-content-starting-opacity: 0;
  --popover-content-ending-opacity: 0;
  --popover-content-starting-scale: 1;
  --popover-content-ending-scale: 1;
  --popover-content-starting-translate-y: 0;
  --popover-content-ending-translate-y: 0;
}
```

### Slide Up

```css
.slideUpMotion {
  --popover-content-starting-opacity: 0;
  --popover-content-ending-opacity: 0;
  --popover-content-starting-scale: 1;
  --popover-content-ending-scale: 1;
  --popover-content-starting-translate-y: 0.75rem;
  --popover-content-ending-translate-y: 0.75rem;
}
```

### Slide Down

```css
.slideDownMotion {
  --popover-content-starting-opacity: 0;
  --popover-content-ending-opacity: 0;
  --popover-content-starting-scale: 1;
  --popover-content-ending-scale: 1;
  --popover-content-starting-translate-y: -0.75rem;
  --popover-content-ending-translate-y: -0.75rem;
}
```

### Slide Left

```css
.slideLeftMotion {
  --popover-content-starting-opacity: 0;
  --popover-content-ending-opacity: 0;
  --popover-content-starting-scale: 1;
  --popover-content-ending-scale: 1;
  --popover-content-starting-translate-x: 0.75rem;
  --popover-content-ending-translate-x: 0.75rem;
}
```

### Slide Right

```css
.slideRightMotion {
  --popover-content-starting-opacity: 0;
  --popover-content-ending-opacity: 0;
  --popover-content-starting-scale: 1;
  --popover-content-ending-scale: 1;
  --popover-content-starting-translate-x: -0.75rem;
  --popover-content-ending-translate-x: -0.75rem;
}
```

### Zoom

```css
.zoomMotion {
  --popover-content-starting-opacity: 0;
  --popover-content-ending-opacity: 0;
  --popover-content-starting-scale: 0.94;
  --popover-content-ending-scale: 0.94;
}
```

### Lift

```css
.liftMotion {
  --popover-content-starting-opacity: 0;
  --popover-content-ending-opacity: 0;
  --popover-content-starting-scale: 0.96;
  --popover-content-ending-scale: 0.96;
  --popover-content-starting-translate-y: 0.5rem;
  --popover-content-ending-translate-y: 0.5rem;
}
```

### Drop In

```css
.dropInMotion {
  --popover-content-starting-opacity: 0;
  --popover-content-ending-opacity: 0;
  --popover-content-starting-scale: 0.98;
  --popover-content-ending-scale: 0.98;
  --popover-content-starting-translate-y: -0.5rem;
  --popover-content-ending-translate-y: -0.5rem;
}
```

### Recipe Gallery

<Preview>
  <MotionRecipesExample />
</Preview>

## Usage Rules

- Put motion variables on the visual part that owns the animation.
- Keep entry and exit values symmetric unless asymmetric motion is intentional.
- Set scale to `1` for pure fade or slide motion.
- Ark presence keeps closed content mounted while CSS exit animations run. Use `present` only when
  JavaScript must control that lifecycle.

## Related

- [Tokens](/docs/tokens)
- [Composition Patterns](/docs/composition-patterns)