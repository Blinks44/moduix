---
title: Introduction
description: Why moduix exists and what it is built for.
icon: BookOpen
---

# moduix

Moduix is a component system for product interfaces. It keeps the interaction layer close to
[Ark UI](https://ark-ui.com/), ships ready-made CSS and tokens, and favors explicit named parts
over large configuration surfaces. Version 2 completes the React package's migration to Ark UI:
`@moduix/react` exposes that Ark-aligned component model directly.

The library supports two workflows:

- use it as a normal npm package when you want package-managed updates
- install component source into your own project with `shadcn` when you want local ownership and direct customization

For installation and first-use, start with [Quick start](/docs/quick-start). It covers both
delivery paths, the current `shadcn` preparation steps, and the required stylesheet imports.

<div className="not-prose my-8 overflow-hidden rounded-lg border bg-fd-card">
  <div className="grid gap-0 md:grid-cols-[0.92fr_1.08fr]">
    <div className="border-b bg-fd-muted/35 p-5 md:border-b-0 md:border-r">
      <p className="text-xs font-medium uppercase tracking-wide text-fd-muted-foreground">
        {'Dependency philosophy'}
      </p>
      <h2 className="mt-3 text-2xl font-semibold tracking-normal text-fd-foreground">
        {'One primitive foundation'}
      </h2>
      <p className="mt-3 text-sm leading-6 text-fd-muted-foreground">
        {
          'I built moduix with the idea that the component layer should stay quiet: one external primitive foundation, plain CSS, and explicit composition instead of a stack of UI runtime dependencies.'
        }
      </p>
    </div>
    <div className="grid gap-3 p-5">
      <div className="rounded-md border bg-fd-background p-4">
        <div className="flex items-center justify-between gap-3">
          <span className="text-sm font-medium text-fd-foreground">{'moduix'}</span>
          <span className="rounded-full bg-fd-primary px-2.5 py-1 text-xs font-medium text-fd-primary-foreground">
            {'components'}
          </span>
        </div>
        <div className="mt-4 h-2 rounded-full bg-fd-muted">
          <div className="h-full w-[68%] rounded-full bg-fd-primary" />
        </div>
      </div>
      <div className="grid gap-2 sm:grid-cols-3">
        <div className="rounded-md border bg-fd-background p-3">
          <p className="text-xs text-fd-muted-foreground">{'Behavior'}</p>
          <p className="mt-1 text-sm font-medium text-fd-foreground">{'Ark UI'}</p>
        </div>
        <div className="rounded-md border bg-fd-background p-3">
          <p className="text-xs text-fd-muted-foreground">{'Styling'}</p>
          <p className="mt-1 text-sm font-medium text-fd-foreground">{'native CSS'}</p>
        </div>
        <div className="rounded-md border bg-fd-background p-3">
          <p className="text-xs text-fd-muted-foreground">{'API'}</p>
          <p className="mt-1 text-sm font-medium text-fd-foreground">{'named parts'}</p>
        </div>
      </div>
      <p className="text-xs leading-5 text-fd-muted-foreground">
        {
          'Ark UI defines the target primitive model for accessibility and interaction; moduix keeps the product surface, defaults, tokens, and examples close to your application code.'
        }
      </p>
    </div>
  </div>
</div>

moduix is not trying to replace [shadcn/ui](https://ui.shadcn.com/). shadcn/ui is a strong
inspiration for the composition-first API and the idea that components should stay easy to read,
own, and customize. moduix explores that model with Ark UI primitives and a plain CSS styling
layer.

## Start Here

Start with setup and the shared design foundation.

| Page                             | Use it for                                                                       |
| -------------------------------- | -------------------------------------------------------------------------------- |
| [Quick start](/docs/quick-start) | Pick the install path, set up styles, and render the first components.           |
| [Themes](/docs/themes)           | Start with a named token preset and keep its CSS in your project.                |
| [Tokens](/docs/tokens)           | Learn the shared design token system, global overrides, and component fallbacks. |

## Guides

Use these pages when you are past installation and need composition rules, Ark-side helpers, or
motion tuning.

| Page                                               | Use it for                                                                         |
| -------------------------------------------------- | ---------------------------------------------------------------------------------- |
| [Animations](/docs/animations)                     | Tune motion with shared timing tokens and component-local appearance variables.    |
| [Composition Patterns](/docs/composition-patterns) | Understand explicit parts, product wrappers, and low-level escape hatches.         |
| [Utilities](/docs/utilities)                       | Use Ark utilities directly when moduix does not ship a styled wrapper for the job. |

## Workflow References

These sections live alongside the component catalog and are best read when the use case appears.

| Page                             | Use it for                                                                 |
| -------------------------------- | -------------------------------------------------------------------------- |
| [Collections](/docs/collections) | Model list, tree, async, and selection data used by collection components. |
| [Forms](/docs/forms)             | Use native forms or integrate moduix controls with a form-state library.   |

## What You Get

- Components built on accessible Ark UI primitives.
- Default styles that work immediately, without owning your application reset.
- CSS variables for fast theming and focused per-component customization.
- A composition-first API with named parts instead of one large configuration surface.
- A dependency-light design centered on Ark UI as the primary primitive foundation.
- A path for both package-managed usage and copy-owned component code.
- Documentation examples that show practical usage and customization patterns.
- A CSS playground in component previews, so you can test style changes live.

## Acknowledgements

This project could not exist without the work of these teams and communities:

- [Ark UI](https://ark-ui.com/) for the accessible primitives that anchor the library direction.
- [shadcn/ui](https://ui.shadcn.com/) for the API inspiration and the culture of practical,
  readable component composition.
- [UnoCSS](https://unocss.dev/) and [Tailwind CSS](https://tailwindcss.com/) for the optional
  `reset.css`.
- [Fumadocs](https://fumadocs.dev/) for the documentation foundation.
- [TanStack](https://tanstack.com/) for the application tooling used by the docs.
- [Voidzero](https://voidzero.dev/) for awesome JS tools