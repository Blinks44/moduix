---
title: Accordion
description: A vertically stacked set of interactive headings that each reveal a section of content.
icon: Component
---

import {
  AccordionCssPropertiesPanel,
  AccordionExample,
  CollapsibleAccordionExample,
  ControlledAccordionExample,
  ContextAccordionExample,
  CustomStylingAccordionExample,
  DisabledItemAccordionExample,
  HorizontalAccordionExample,
  ItemStateAccordionExample,
  LazyMountAccordionExample,
  MultipleAccordionExample,
  RootProviderAccordionExample,
  accordionCustomStylingCss,
  accordionExampleCss,
  accordionItemsData,
} from '@/components/examples/accordion';

## API Reference

<BaseUIReference href="https://ark-ui.com/docs/components/accordion" />

## Basic

Use Ark-style parts: `Accordion.Root`, `Accordion.Item`, `Accordion.ItemTrigger`,
`Accordion.ItemIndicator`, and `Accordion.ItemContent`.

<Preview>
  <AccordionExample defaultValue={["what-is-ark-ui"]} />

  <Preview.Code>
    {`
      import { Accordion } from "moduix";

      export function AccordionDemo() {
        return (
          <Accordion.Root defaultValue={["what-is-ark-ui"]}>
            {items.map((item) => (
              <Accordion.Item key={item.value} value={item.value}>
                <Accordion.ItemTrigger>
                  {item.title}
                  <Accordion.ItemIndicator />
                </Accordion.ItemTrigger>
                <Accordion.ItemContent>
                  <div className="accordion-panel-content">{item.description}</div>
                </Accordion.ItemContent>
              </Accordion.Item>
            ))}
          </Accordion.Root>
        );
      }
    `}

  </Preview.Code>

  <Preview.CSS>{accordionExampleCss}</Preview.CSS>
  <Preview.Data>{accordionItemsData}</Preview.Data>
</Preview>

## Install with shadcn (optional)

If you want `Accordion` to become part of your own project source instead of staying in
`node_modules`, install it from the moduix GitHub registry:

<Tabs items={['npm', 'pnpm', 'bun', 'deno', 'aube']}>
  <Tab value="npm">

```bash
npx shadcn@latest add Blinks44/moduix/accordion
```

  </Tab>
  <Tab value="pnpm">

```bash
pnpm dlx shadcn@latest add Blinks44/moduix/accordion
```

  </Tab>
  <Tab value="bun">

```bash
bunx shadcn@latest add Blinks44/moduix/accordion
```

  </Tab>
  <Tab value="deno">

```bash
deno x -A npm:shadcn@latest add Blinks44/moduix/accordion
```

  </Tab>
  <Tab value="aube">

```bash
aubx shadcn@latest add Blinks44/moduix/accordion
```

  </Tab>
</Tabs>

## Anatomy

```text
Accordion.Root
└─ Accordion.Context (optional render-prop access)
└─ Accordion.Item[value]
   ├─ Accordion.ItemTrigger
   │  ├─ label
   │  ├─ Accordion.ItemContext (optional render-prop access)
   │  └─ Accordion.ItemIndicator (optional)
   └─ Accordion.ItemContent
      └─ content

Accordion.RootProvider
└─ same item tree connected to a useAccordion() store
```

| Part                      | Role                                                                                      |
| ------------------------- | ----------------------------------------------------------------------------------------- |
| `Accordion.Root`          | Controls accordion state and keyboard behavior.                                           |
| `Accordion.RootProvider`  | Connects parts to state created with `useAccordion()`.                                    |
| `Accordion.Item`          | One collapsible item with a required `value`.                                             |
| `Accordion.ItemTrigger`   | Interactive button that opens or closes the item.                                         |
| `Accordion.ItemIndicator` | Visual open/close cue. Defaults to the moduix plus icon when no children are provided.    |
| `Accordion.ItemContent`   | Collapsible content region. Keep spacing wrappers inside for clean `--height` animations. |
| `Accordion.Context`       | Reads root state through an Ark render prop.                                              |
| `Accordion.ItemContext`   | Reads item state through an Ark render prop.                                              |

## Composition

### Recommended composition

`Accordion.Root` owns keyboard navigation, focus movement, and open item state. Use it for
`value`, `defaultValue`, `onValueChange(details)`, `onFocusChange(details)`, `multiple`,
`collapsible`, `orientation`, `lazyMount`, `unmountOnExit`, and `ids`.

Stay with `Accordion.Root`, `Accordion.Item`, `Accordion.ItemTrigger`, `Accordion.ItemIndicator`,
and `Accordion.ItemContent` for the standard Ark composition path.

### When to go custom

`value` and `defaultValue` are always arrays, including single-open mode. `onValueChange` follows
the Ark callback shape and exposes the current item values as `details.value`.

Use the Ark `RootProvider` and `useAccordion()` pattern when accordion state must be created
outside the rendered subtree.

Use `Accordion.Context`, `Accordion.ItemContext`, `useAccordionContext()`, or
`useAccordionItemContext()` when surrounding UI needs to react to root or item state without adding
a separate state layer.

`Accordion.Item` requires a stable `value`. Use `disabled` on the item when one section should stay
present but unavailable.

`Accordion.ItemIndicator` is optional. In moduix it renders `PlusIcon` by default when no children
are provided, but you can replace the indicator content entirely.

Use `asChild` only when another element must own the rendered node. Keep the Ark part tree visible
instead of hiding it behind convenience wrappers.

### Behavioral notes

Keep spacing wrappers inside `Accordion.ItemContent` instead of on the content part itself. The Ark
measurement variable `--height` drives the open/close animation, so inner padding keeps the motion
clean.

All exported parts support `asChild` for cases where another element must own the rendered node.

## Examples

### Default Value

Set `defaultValue` to specify which item should be expanded when the accordion first renders.

<Preview>
  <AccordionExample defaultValue={["what-is-ark-ui"]} />

  <Preview.Code>
    {`
      import { Accordion } from "moduix";

      export function DefaultValueAccordionDemo() {
        return (
          <Accordion.Root defaultValue={["what-is-ark-ui"]}>
            {items.map((item) => (
              <Accordion.Item key={item.value} value={item.value}>
                <Accordion.ItemTrigger>
                  {item.title}
                  <Accordion.ItemIndicator />
                </Accordion.ItemTrigger>
                <Accordion.ItemContent>
                  <div className="accordion-panel-content">{item.description}</div>
                </Accordion.ItemContent>
              </Accordion.Item>
            ))}
          </Accordion.Root>
        );
      }
    `}

  </Preview.Code>

  <Preview.CSS>{accordionExampleCss}</Preview.CSS>
  <Preview.Data>{accordionItemsData}</Preview.Data>
</Preview>

### Multiple

Use `multiple` when several sections should stay open at the same time instead of collapsing to a single active item.

<Preview>
  <MultipleAccordionExample />
  <Preview.Code>
    {`
      import { Accordion } from "moduix";

      export function MultipleAccordionDemo() {
        return (
          <Accordion.Root multiple defaultValue={["what-is-ark-ui", "can-i-use-it"]}>
            {items.map((item) => (
              <Accordion.Item key={item.value} value={item.value}>
                <Accordion.ItemTrigger>
                  {item.title}
                  <Accordion.ItemIndicator />
                </Accordion.ItemTrigger>
                <Accordion.ItemContent>
                  <div className="accordion-panel-content">{item.description}</div>
                </Accordion.ItemContent>
              </Accordion.Item>
            ))}
          </Accordion.Root>
        );
      }
    `}

  </Preview.Code>
  <Preview.CSS>{accordionExampleCss}</Preview.CSS>
  <Preview.Data>{accordionItemsData}</Preview.Data>
</Preview>

### Controlled

Control `value` from React state when the open items need to stay in sync with routing, filters, or surrounding UI.

<Preview>
  <ControlledAccordionExample />
  <Preview.Code>
    {`
      import { Accordion } from "moduix";
      import { useState } from "react";

      export function ControlledAccordionDemo() {
        const [value, setValue] = useState(["getting-started"] as string[]);

        return (
          <Accordion.Root value={value} onValueChange={(details) => setValue(details.value)}>
            {items.map((item) => (
              <Accordion.Item key={item.value} value={item.value}>
                <Accordion.ItemTrigger>
                  {item.title}
                  <Accordion.ItemIndicator />
                </Accordion.ItemTrigger>
                <Accordion.ItemContent>
                  <div className="accordion-panel-content">{item.description}</div>
                </Accordion.ItemContent>
              </Accordion.Item>
            ))}
          </Accordion.Root>
        );
      }
    `}

  </Preview.Code>
  <Preview.CSS>{accordionExampleCss}</Preview.CSS>
  <Preview.Data>{accordionItemsData}</Preview.Data>
</Preview>

### Root Provider

Use `useAccordion()` with `Accordion.RootProvider` when the state store must be created outside
the rendered accordion tree. Do not render `Accordion.Root` for the same store.

<Preview>
  <RootProviderAccordionExample />
  <Preview.Code>
    {`
      import { Accordion, useAccordion } from "moduix";

      export function RootProviderAccordionDemo() {
        const accordion = useAccordion({ defaultValue: ["what-is-ark-ui"] });

        return (
          <Accordion.RootProvider value={accordion}>
            {items.map((item) => (
              <Accordion.Item key={item.value} value={item.value}>
                <Accordion.ItemTrigger>
                  {item.title}
                  <Accordion.ItemIndicator />
                </Accordion.ItemTrigger>
                <Accordion.ItemContent>
                  <div className="accordion-panel-content">{item.description}</div>
                </Accordion.ItemContent>
              </Accordion.Item>
            ))}
          </Accordion.RootProvider>
        );
      }
    `}

  </Preview.Code>
  <Preview.CSS>{accordionExampleCss}</Preview.CSS>
  <Preview.Data>{accordionItemsData}</Preview.Data>
</Preview>

### Collapsible

Add `collapsible` when the currently open item should be allowed to close without selecting another item first.

<Preview>
  <CollapsibleAccordionExample />
  <Preview.Code>
    {`
      import { Accordion } from "moduix";

      export function CollapsibleAccordionDemo() {
        return (
          <Accordion.Root collapsible defaultValue={["what-is-ark-ui"]}>
            {items.map((item) => (
              <Accordion.Item key={item.value} value={item.value}>
                <Accordion.ItemTrigger>
                  {item.title}
                  <Accordion.ItemIndicator />
                </Accordion.ItemTrigger>
                <Accordion.ItemContent>
                  <div className="accordion-panel-content">{item.description}</div>
                </Accordion.ItemContent>
              </Accordion.Item>
            ))}
          </Accordion.Root>
        );
      }
    `}

  </Preview.Code>
  <Preview.CSS>{accordionExampleCss}</Preview.CSS>
  <Preview.Data>{accordionItemsData}</Preview.Data>
</Preview>

### Horizontal

Switch to `orientation="horizontal"` when the accordion should follow a horizontal interaction pattern.

<Preview>
  <HorizontalAccordionExample />
  <Preview.Code>
    {`
      import { Accordion } from "moduix";

      export function HorizontalAccordionDemo() {
        return (
          <Accordion.Root orientation="horizontal" defaultValue={["what-is-ark-ui"]}>
            {items.map((item) => (
              <Accordion.Item key={item.value} value={item.value}>
                <Accordion.ItemTrigger>
                  {item.title}
                  <Accordion.ItemIndicator />
                </Accordion.ItemTrigger>
                <Accordion.ItemContent>
                  <div className="accordion-panel-content">{item.description}</div>
                </Accordion.ItemContent>
              </Accordion.Item>
            ))}
          </Accordion.Root>
        );
      }
    `}

  </Preview.Code>
  <Preview.CSS>{accordionExampleCss}</Preview.CSS>
  <Preview.Data>{accordionItemsData}</Preview.Data>
</Preview>

### Lazy Mount

Use `lazyMount` and `unmountOnExit` when hidden panel content is expensive and should only render while needed.

<Preview>
  <LazyMountAccordionExample />
  <Preview.Code>
    {`
      import { Accordion } from "moduix";

      export function LazyMountAccordionDemo() {
        return (
          <Accordion.Root lazyMount unmountOnExit>
            {items.map((item) => (
              <Accordion.Item key={item.value} value={item.value}>
                <Accordion.ItemTrigger>
                  {item.title}
                  <Accordion.ItemIndicator />
                </Accordion.ItemTrigger>
                <Accordion.ItemContent>
                  <div className="accordion-panel-content">{item.description}</div>
                </Accordion.ItemContent>
              </Accordion.Item>
            ))}
          </Accordion.Root>
        );
      }
    `}

  </Preview.Code>
  <Preview.CSS>{accordionExampleCss}</Preview.CSS>
  <Preview.Data>{accordionItemsData}</Preview.Data>
</Preview>

### Context

Use `Accordion.Context` when adjacent UI needs to read root state without duplicating accordion
state in React.

<Preview>
  <ContextAccordionExample />
  <Preview.Code>
    {`
      import { Accordion } from "moduix";

      export function ContextAccordionDemo() {
        return (
          <Accordion.Root defaultValue={["what-is-ark-ui"]}>
            {items.map((item) => (
              <Accordion.Item key={item.value} value={item.value}>
                <Accordion.ItemTrigger>
                  {item.title}
                  <Accordion.ItemIndicator />
                </Accordion.ItemTrigger>
                <Accordion.ItemContent>
                  <div className="accordion-panel-content">{item.description}</div>
                </Accordion.ItemContent>
              </Accordion.Item>
            ))}
            <Accordion.Context>
              {(accordion) => (
                <div className="accordion-state">Open sections: {accordion.value.join(", ")}</div>
              )}
            </Accordion.Context>
          </Accordion.Root>
        );
      }
    `}

  </Preview.Code>
  <Preview.CSS>{accordionExampleCss}</Preview.CSS>
  <Preview.Data>{accordionItemsData}</Preview.Data>
</Preview>

### Item State

Use `Accordion.ItemContext` when a custom child part needs the current item state.

<Preview>
  <ItemStateAccordionExample />
  <Preview.Code>
    {`
      import { Accordion } from "moduix";

      export function ItemStateAccordionDemo() {
        return (
          <Accordion.Root defaultValue={["what-is-ark-ui"]}>
            {items.map((item) => (
              <Accordion.Item key={item.value} value={item.value}>
                <Accordion.ItemTrigger>
                  {item.title}
                  <Accordion.ItemContext>
                    {(itemState) => (
                      <span className="accordion-item-state">
                        {itemState.expanded ? "Open" : "Closed"}
                      </span>
                    )}
                  </Accordion.ItemContext>
                  <Accordion.ItemIndicator />
                </Accordion.ItemTrigger>
                <Accordion.ItemContent>
                  <div className="accordion-panel-content">{item.description}</div>
                </Accordion.ItemContent>
              </Accordion.Item>
            ))}
          </Accordion.Root>
        );
      }
    `}

  </Preview.Code>
  <Preview.CSS>{accordionExampleCss}</Preview.CSS>
  <Preview.Data>{accordionItemsData}</Preview.Data>
</Preview>

### Disabled Item

Set `disabled` on an item when it should stay visible in the list but remain unavailable for interaction.

<Preview>
  <DisabledItemAccordionExample />
  <Preview.Code>
    {`
      import { Accordion } from "moduix";

      export function DisabledItemAccordionDemo() {
        return (
          <Accordion.Root defaultValue={["what-is-ark-ui"]}>
            {items.map((item) => (
              <Accordion.Item
                key={item.value}
                value={item.value}
                disabled={item.value === "getting-started"}
              >
                <Accordion.ItemTrigger>
                  {item.title}
                  <Accordion.ItemIndicator />
                </Accordion.ItemTrigger>
                <Accordion.ItemContent>
                  <div className="accordion-panel-content">{item.description}</div>
                </Accordion.ItemContent>
              </Accordion.Item>
            ))}
          </Accordion.Root>
        );
      }
    `}

  </Preview.Code>
  <Preview.CSS>{accordionExampleCss}</Preview.CSS>
  <Preview.Data>{accordionItemsData}</Preview.Data>
</Preview>

### Custom Styling

Override the indicator content or local CSS variables when the accordion should match a product-specific visual language.

<Preview>
  <CustomStylingAccordionExample />

  <Preview.Code>
    {`
      import { Accordion, ChevronDownIcon } from "moduix";

      export function CustomStylingAccordionDemo() {
        return (
          <Accordion.Root defaultValue={["what-is-ark-ui"]}>
            {items.map((item) => (
              <Accordion.Item key={item.value} value={item.value}>
                <Accordion.ItemTrigger>
                  {item.title}
                  <Accordion.ItemIndicator className="accordion-custom-icon">
                    <ChevronDownIcon />
                  </Accordion.ItemIndicator>
                </Accordion.ItemTrigger>
                <Accordion.ItemContent>
                  <div className="accordion-panel-content">{item.description}</div>
                </Accordion.ItemContent>
              </Accordion.Item>
            ))}
          </Accordion.Root>
        );
      }
    `}

  </Preview.Code>

  <Preview.CSS>{accordionCustomStylingCss}</Preview.CSS>
  <Preview.Data>{accordionItemsData}</Preview.Data>
</Preview>

## Styling

Use `className` on exported parts for structural styling changes. For state styling, prefer Ark
data attributes (`data-state`, `data-disabled`, `data-orientation`) and moduix `data-slot` hooks.

### CSS Properties

<div className="not-prose">
  <Tabs items={['CSS Variables']} className="mb-0 mt-3">
    <Tab className="max-h-150 overflow-auto">
      <AccordionCssPropertiesPanel />
    </Tab>
  </Tabs>
</div>

### Styling hooks

- `data-slot="accordion-root"`, `accordion-root-provider`, `accordion-item`,
  `accordion-item-trigger`, `accordion-item-indicator`, and `accordion-item-content`.
- Ark state hooks: `data-state="open|closed"`, `data-disabled`, `data-orientation`.