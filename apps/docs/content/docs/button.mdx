---
title: Button
description: A styled action control built with the Ark factory composition model.
icon: Component
---

import {
  ButtonCssPropertiesPanel,
  ButtonDisabledExample,
  ButtonExample,
  ButtonIconExample,
  ButtonLinkCompositionExample,
  ButtonLoadingExample,
  ButtonRefExample,
  ButtonSizesExample,
  ButtonVariantsExample,
  CustomStylingButtonExample,
} from '@/components/examples/button';

## API Reference

Ark UI does not ship a dedicated `button` primitive. Moduix builds this component with the official
[Ark factory](https://ark-ui.com/docs/guides/composition#the-ark-factory), follows Ark's
[composition](https://ark-ui.com/docs/guides/composition), [styling](https://ark-ui.com/docs/guides/styling), and
[ref](https://ark-ui.com/docs/guides/ref) guidance.

## Basic

Use the short `<Button />` form for normal actions. `Button.Root` is the equivalent namespaced
form.

<Preview>
  <ButtonExample />

  <Preview.Code>
    {`
      import { Button } from "@moduix/react";

      export function ButtonDemo() {
        return <Button>{labels.save}</Button>;
      }
    `}

  </Preview.Code>

<Preview.CSS>
  {`
      .button-demo-row {
        display: flex;
        justify-content: center;
      }
    `}
</Preview.CSS>

  <Preview.Data>
    {`
      const labels = { save: "Save Changes" };
    `}
  </Preview.Data>
</Preview>

## Install with shadcn (optional)

<Tabs items={['npm', 'pnpm', 'bun', 'deno', 'aube']}>
  <Tab value="npm">

```bash
npx shadcn@latest add Blinks44/moduix/react-button
```

  </Tab>
  <Tab value="pnpm">

```bash
pnpm dlx shadcn@latest add Blinks44/moduix/react-button
```

  </Tab>
  <Tab value="bun">

```bash
bunx shadcn@latest add Blinks44/moduix/react-button
```

  </Tab>
  <Tab value="deno">

```bash
deno x -A npm:shadcn@latest add Blinks44/moduix/react-button
```

  </Tab>
  <Tab value="aube">

```bash
aubx shadcn@latest add Blinks44/moduix/react-button
```

  </Tab>
</Tabs>

## Anatomy

`Button` is a single-part component.

```text
Button.Root
└─ root[data-scope="button"][data-part="root"][data-slot="button-root"]
   └─ children
```

| Part                     | Role                                          |
| ------------------------ | --------------------------------------------- |
| `Button` / `Button.Root` | Root interactive surface with moduix styling. |
| `Button`                 | Callable alias of `Button.Root`.              |

## Composition

### Recommended composition

Use a native root for actions. It defaults to `type="button"` to avoid accidental form submission;
set `type="submit"` explicitly for a submit action.

### Default props

| Prop      | Default   | Values                                                                                   |
| --------- | --------- | ---------------------------------------------------------------------------------------- |
| `variant` | `default` | `default`, `outline`, `secondary`, `destructive`, `destructive-outline`, `ghost`, `link` |
| `size`    | `md`      | `xs`, `sm`, `md`, `lg`, `xl`, `icon-sm`, `icon-md`, `icon-lg`                            |
| `type`    | `button`  | Native roots only                                                                        |

### When to go custom

Use `asChild` when one semantic child should own the DOM node. Ark composition requires exactly one
child. The default `type="button"` is not injected into an `asChild` target.

Use a real anchor for navigation. `aria-disabled="true"` is presentational on links, so application
code must also prevent navigation or activation.

### Behavioral notes

- Native attributes, events, `className`, and `style` pass through without callback translation.
- The forwarded ref targets the rendered root.
- Native `disabled` and `aria-disabled="true"` expose `data-disabled`.
- Loading UI stays compositional: combine `Spinner`, `disabled`, and `aria-busy`.
- Direct child `svg` elements inherit `currentColor` and use `--button-icon-size`.
- Icon-only buttons require an accessible name.
- Chakra-only `colorPalette`, responsive recipe props, `ButtonGroup`, and managed loading props are
  intentionally outside this root-only moduix contract.

## Examples

### Variants

Use variants to communicate action priority and intent.

<Preview>
  <ButtonVariantsExample />

  <Preview.Code>
    {`
      import { Button } from "@moduix/react";

      export function ButtonVariantsDemo() {
        return (
          <div className="button-demo-row">
            {variants.map((variant) => (
              <Button key={variant} variant={variant}>
                {variant}
              </Button>
            ))}
          </div>
        );
      }
    `}

  </Preview.Code>

<Preview.CSS>
  {`
      .button-demo-row {
        display: flex;
        flex-wrap: wrap;
        align-items: center;
        gap: var(--spacing-3);
      }
    `}
</Preview.CSS>

  <Preview.Data>
    {`
      const variants = [
        "default",
        "outline",
        "secondary",
        "destructive",
        "destructive-outline",
        "ghost",
        "link",
      ] as const;
    `}
  </Preview.Data>
</Preview>

### Sizes

Text and icon-only sizes share the same root API.

<Preview>
  <ButtonSizesExample />

  <Preview.Code>
    {`
      import { Button, StarIcon } from "@moduix/react";

      export function ButtonSizesDemo() {
        return (
          <div className="button-demo-row">
            {sizes.map((size) => (
              <Button key={size} size={size}>
                {size}
              </Button>
            ))}
            {iconSizes.map((item) => (
              <Button
                key={item.size}
                size={item.size}
                variant="outline"
                aria-label={item.label}
              >
                <StarIcon />
              </Button>
            ))}
          </div>
        );
      }
    `}

  </Preview.Code>

<Preview.CSS>
  {`
      .button-demo-row {
        display: flex;
        flex-wrap: wrap;
        align-items: center;
        gap: var(--spacing-3);
      }
    `}
</Preview.CSS>

  <Preview.Data>
    {`
      const sizes = ["xs", "sm", "md", "lg", "xl"] as const;
      const iconSizes = [
        { label: "Small favorite", size: "icon-sm" },
        { label: "Favorite", size: "icon-md" },
        { label: "Large favorite", size: "icon-lg" },
      ] as const;
    `}
  </Preview.Data>
</Preview>

### Icons

Icons can appear before or after text. Icon-only controls use an `icon-*` size and an accessible
name.

<Preview>
  <ButtonIconExample />

  <Preview.Code>
    {`
      import { ArrowUpRightIcon, Button, PlusIcon, StarIcon } from "@moduix/react";

      export function ButtonIconsDemo() {
        return (
          <div className="button-demo-row">
            <Button>
              <PlusIcon />
              {labels.create}
            </Button>
            <Button size="icon-md" variant="outline" aria-label={labels.favorite}>
              <StarIcon />
            </Button>
            <Button variant="link">
              {labels.docs}
              <ArrowUpRightIcon />
            </Button>
          </div>
        );
      }
    `}

  </Preview.Code>

<Preview.CSS>
  {`
      .button-demo-row {
        display: flex;
        flex-wrap: wrap;
        align-items: center;
        gap: var(--spacing-3);
      }
    `}
</Preview.CSS>

  <Preview.Data>
    {`
      const labels = {
        create: "Create Item",
        docs: "Open Button Docs",
        favorite: "Favorites",
      };
    `}
  </Preview.Data>
</Preview>

### Disabled

Use native `disabled` for buttons. Composed links need both `aria-disabled` and application-level
activation prevention.

<Preview>
  <ButtonDisabledExample />

  <Preview.Code>
    {`
      import { Button } from "@moduix/react";

      export function ButtonDisabledDemo() {
        return (
          <div className="button-demo-row">
            <Button disabled>{labels.button}</Button>
            <Button asChild aria-disabled="true" variant="outline">
              <a href="#button" onClick={(event) => event.preventDefault()}>
                {labels.link}
              </a>
            </Button>
          </div>
        );
      }
    `}

  </Preview.Code>

<Preview.CSS>
  {`
      .button-demo-row {
        display: flex;
        flex-wrap: wrap;
        align-items: center;
        gap: var(--spacing-3);
      }
    `}
</Preview.CSS>

  <Preview.Data>
    {`
      const labels = {
        button: "Disabled",
        link: "Disabled Link",
      };
    `}
  </Preview.Data>
</Preview>

### Pending State

Compose pending content explicitly so product code controls the label, spinner, and async
lifecycle.

<Preview>
  <ButtonLoadingExample />

  <Preview.Code>
    {`
      import { Button, Spinner } from "@moduix/react";
      import { useState } from "react";

      export function PendingButton() {
        const [pending, setPending] = useState(false);

        return (
          <Button
            disabled={pending}
            aria-busy={pending || undefined}
            onClick={() => {
              setPending(true);
              setTimeout(() => setPending(false), delay);
            }}
          >
            {pending ? (
              <>
                <Spinner decorative size="sm" />
                {labels.pending}
              </>
            ) : (
              labels.idle
            )}
          </Button>
        );
      }
    `}

  </Preview.Code>

<Preview.CSS>
  {`
      [data-slot="button-root"][aria-busy="true"] {
        cursor: progress;
      }
    `}
</Preview.CSS>

  <Preview.Data>
    {`
      const delay = 1800;
      const labels = {
        idle: "Save Changes",
        pending: "Saving",
      };
    `}
  </Preview.Data>
</Preview>

### AsChild Link

Use `asChild` for navigation so the anchor retains link semantics while receiving button styling.

<Preview>
  <ButtonLinkCompositionExample />

  <Preview.Code>
    {`
      import { Button } from "@moduix/react";

      export function ButtonLinkDemo() {
        return (
          <Button asChild variant="outline">
            <a href={link.href}>{link.label}</a>
          </Button>
        );
      }
    `}

  </Preview.Code>

<Preview.CSS>
  {`
      [data-slot="button-root"] {
        text-decoration: none;
      }
    `}
</Preview.CSS>

  <Preview.Data>
    {`
      const link = {
        href: "#button",
        label: "Open Button Docs",
      };
    `}
  </Preview.Data>
</Preview>

### Ref

The forwarded ref points at the rendered root and can be used for imperative focus or measurement.

<Preview>
  <ButtonRefExample />

  <Preview.Code>
    {`
      import { Button } from "@moduix/react";
      import { useRef } from "react";

      export function ButtonRefDemo() {
        const buttonRef = useRef(null as HTMLButtonElement | null);

        return (
          <div className="button-demo-row">
            <Button ref={buttonRef}>{labels.target}</Button>
            <Button variant="outline" onClick={() => buttonRef.current?.focus()}>
              {labels.trigger}
            </Button>
          </div>
        );
      }
    `}

  </Preview.Code>

<Preview.CSS>
  {`
      .button-demo-row {
        display: flex;
        flex-wrap: wrap;
        align-items: center;
        gap: var(--spacing-3);
      }
    `}
</Preview.CSS>

  <Preview.Data>
    {`
      const labels = {
        target: "Focus target",
        trigger: "Focus first button",
      };
    `}
  </Preview.Data>
</Preview>

### Custom Styling

Use `className`, CSS variables, and stable data hooks for local visual changes.

<Preview>
  <CustomStylingButtonExample />

  <Preview.Code>
    {`
      import { Button, Spinner } from "@moduix/react";

      export function CustomStylingButtonDemo() {
        return (
          <Button className="customButton" disabled aria-busy>
            <Spinner decorative size="sm" className="customSpinner" />
            {labels.pending}
          </Button>
        );
      }
    `}

  </Preview.Code>

  <Preview.CSS>
    {`
      .customButton {
        --button-radius: var(--radius-full);
        --button-default-bg: var(--color-foreground);
        --button-default-border-color: var(--color-foreground);
        box-shadow: 0 1px 2px rgb(0 0 0 / 0.08);
      }

      .customSpinner {
        color: currentColor;
      }
    `}

  </Preview.CSS>

  <Preview.Data>
    {`
      const labels = { pending: "Publishing" };
    `}
  </Preview.Data>
</Preview>

## Styling

### CSS Properties

<div className="not-prose">
  <Tabs items={['CSS Variables']} className="mb-0 mt-3">
    <Tab className="max-h-150 overflow-auto">
      <ButtonCssPropertiesPanel />
    </Tab>
  </Tabs>
</div>

### Styling hooks

- `data-slot="button-root"` is the stable moduix selector.
- `data-scope="button"` and `data-part="root"` support Ark-style targeting.
- `data-variant` and `data-size` reflect the active visual recipe.
- `data-disabled` is present for both native `disabled` and `aria-disabled="true"`.
- `[data-disabled]` drives disabled visuals for native `disabled` and `aria-disabled="true"`.