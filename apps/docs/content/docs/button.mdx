---
title: Button
description: A styled action control built with the Ark factory composition model.
icon: Component
---

import * as ButtonDocs from '@/components/examples/button';

## API Reference

Ark UI does not ship a dedicated `button` primitive. Moduix builds this component with the official
[Ark factory](https://ark-ui.com/docs/guides/composition#the-ark-factory), follows Ark's
[composition](https://ark-ui.com/docs/guides/composition), [styling](https://ark-ui.com/docs/guides/styling), and
[ref](https://ark-ui.com/docs/guides/ref) guidance.

## Basic

Use the short `<Button />` form for normal actions. `Button.Root` is the equivalent namespaced
form.

<Preview>
  <ButtonDocs.ButtonExample />

<Preview.Code>
  <include lang="tsx">./_snippets/button/basic.tsx#demo</include>
</Preview.Code>

<Preview.CSS>
  {`
      .button-demo-row {
        display: flex;
        justify-content: center;
      }
    `}
</Preview.CSS>
</Preview>

## Install with shadcn

<ShadcnInstall packageName="react-button" />

## Anatomy

`Button` is a single-part component.

```text
Button.Root
└─ root[data-scope="button"][data-part="root"][data-slot="button-root"]
   └─ children
```

| Part                     | Role                                          |
| ------------------------ | --------------------------------------------- |
| `Button` / `Button.Root` | Root interactive surface with moduix styling. |
| `Button`                 | Callable alias of `Button.Root`.              |

## Composition

### Recommended composition

Use a native root for actions. It defaults to `type="button"` to avoid accidental form submission;
set `type="submit"` explicitly for a submit action.

### Default props

| Prop      | Default   | Values                                                                                   |
| --------- | --------- | ---------------------------------------------------------------------------------------- |
| `variant` | `default` | `default`, `outline`, `secondary`, `destructive`, `destructive-outline`, `ghost`, `link` |
| `size`    | `md`      | `xs`, `sm`, `md`, `lg`, `xl`, `icon-sm`, `icon-md`, `icon-lg`                            |
| `type`    | `button`  | Native roots only                                                                        |

### When to go custom

Use `asChild` when one semantic child should own the DOM node. Ark composition requires exactly one
child. The default `type="button"` is not injected into an `asChild` target.

Use a real anchor for navigation. `aria-disabled="true"` is presentational on links, so application
code must also prevent navigation or activation.

### Behavioral notes

- Native attributes, events, `className`, and `style` pass through without callback translation.
- The forwarded ref targets the rendered root.
- Native `disabled` and `aria-disabled="true"` expose `data-disabled`.
- Loading UI stays compositional: combine `Spinner`, `disabled`, and `aria-busy`.
- Direct child `svg` elements inherit `currentColor` and use `--button-icon-size`.
- Icon-only buttons require an accessible name.
- Chakra-only `colorPalette`, responsive recipe props, `ButtonGroup`, and managed loading props are
  intentionally outside this root-only moduix contract.

## Examples

### Variants

Use variants to communicate action priority and intent.

<Preview>
  <ButtonDocs.ButtonVariantsExample />

<Preview.Code>
  <include lang="tsx">./_snippets/button/variants.tsx#demo</include>
</Preview.Code>

<Preview.CSS>
  {`
      .button-demo-row {
        display: flex;
        flex-wrap: wrap;
        align-items: center;
        gap: var(--spacing-3);
      }
    `}
</Preview.CSS>
</Preview>

### Sizes

Text and icon-only sizes share the same root API.

<Preview>
  <ButtonDocs.ButtonSizesExample />

<Preview.Code>
  <include lang="tsx">./_snippets/button/sizes.tsx#demo</include>
</Preview.Code>

<Preview.CSS>
  {`
      .button-demo-row {
        display: flex;
        flex-wrap: wrap;
        align-items: center;
        gap: var(--spacing-3);
      }
    `}
</Preview.CSS>
</Preview>

### Icons

Icons can appear before or after text. Icon-only controls use an `icon-*` size and an accessible
name.

<Preview>
  <ButtonDocs.ButtonIconExample />

<Preview.Code>
  <include lang="tsx">./_snippets/button/icons.tsx#demo</include>
</Preview.Code>

<Preview.CSS>
  {`
      .button-demo-row {
        display: flex;
        flex-wrap: wrap;
        align-items: center;
        gap: var(--spacing-3);
      }
    `}
</Preview.CSS>
</Preview>

### Disabled

Use native `disabled` for buttons. Composed links need both `aria-disabled` and application-level
activation prevention.

<Preview>
  <ButtonDocs.ButtonDisabledExample />

<Preview.Code>
  <include lang="tsx">./_snippets/button/disabled.tsx#demo</include>
</Preview.Code>

<Preview.CSS>
  {`
      .button-demo-row {
        display: flex;
        flex-wrap: wrap;
        align-items: center;
        gap: var(--spacing-3);
      }
    `}
</Preview.CSS>
</Preview>

### Pending State

Compose pending content explicitly so product code controls the label, spinner, and async
lifecycle.

<Preview>
  <ButtonDocs.ButtonLoadingExample />

<Preview.Code>
  <include lang="tsx">./_snippets/button/pending-state.tsx#demo</include>
</Preview.Code>

<Preview.CSS>
  {`
      [data-slot="button-root"][aria-busy="true"] {
        cursor: progress;
      }
    `}
</Preview.CSS>
</Preview>

### AsChild Link

Use `asChild` for navigation so the anchor retains link semantics while receiving button styling.

<Preview>
  <ButtonDocs.ButtonLinkCompositionExample />

<Preview.Code>
  <include lang="tsx">./_snippets/button/as-child-link.tsx#demo</include>
</Preview.Code>

<Preview.CSS>
  {`
      [data-slot="button-root"] {
        text-decoration: none;
      }
    `}
</Preview.CSS>
</Preview>

### Ref

The forwarded ref points at the rendered root and can be used for imperative focus or measurement.

<Preview>
  <ButtonDocs.ButtonRefExample />

<Preview.Code>
  <include lang="tsx">./_snippets/button/ref.tsx#demo</include>
</Preview.Code>

<Preview.CSS>
  {`
      .button-demo-row {
        display: flex;
        flex-wrap: wrap;
        align-items: center;
        gap: var(--spacing-3);
      }
    `}
</Preview.CSS>
</Preview>

## Styling

### CSS Properties

<CssPropertiesSection properties={ButtonDocs.buttonCssProperties} />

### Styling hooks

- `data-slot="button-root"` is the stable moduix selector.
- `data-scope="button"` and `data-part="root"` support Ark-style targeting.
- `data-variant` and `data-size` reflect the active visual recipe.
- `data-disabled` is present for both native `disabled` and `aria-disabled="true"`.
- `[data-disabled]` drives disabled visuals for native `disabled` and `aria-disabled="true"`.