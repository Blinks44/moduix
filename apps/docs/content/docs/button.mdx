---
title: Button
description: A thin styled wrapper over the Base UI button primitive.
icon: Component
---

import {
  ButtonCssPlaygroundPanel,
  ButtonCssPropertiesPanel,
  ButtonCustomTagExample,
  ButtonDisabledExample,
  ButtonExample,
  ButtonIconExample,
  ButtonLoadingExample,
  ButtonSizesExample,
  ButtonVariantsExample,
  CustomStylesButtonExample,
  buttonPlaygroundCssProperties,
} from '@/components/examples/button';

## API Reference

<BaseUIReference href="https://base-ui.com/react/components/button" />

## Basic

<Preview cssProperties={buttonPlaygroundCssProperties}>
  <ButtonExample />

  <Preview.Code>
    {`
      import { Button } from "moduix";

      export function ButtonDemo() {
        return <Button>Save Changes</Button>;
      }
    `}

  </Preview.Code>

<Preview.CSSProperties>
  {(context) => <ButtonCssPropertiesPanel {...context} />}
</Preview.CSSProperties>

  <Preview.CSSPlayground>
    {(context) => <ButtonCssPlaygroundPanel {...context} />}
  </Preview.CSSPlayground>
</Preview>

## Anatomy

`Button` is a single styled Base UI primitive root.

```text
Button
```

| Part     | Role                                                       |
| -------- | ---------------------------------------------------------- |
| `Button` | Interactive root with moduix styles plus Base UI behavior. |

The root exposes `data-slot="button-root"`, `data-variant`, `data-size`, and Base UI
`data-disabled`.

## Composition

`Button` keeps the primitive API intact. Use Base UI props such as `render`, `nativeButton`,
`disabled`, and `focusableWhenDisabled` directly on the component.

moduix only adds two visual props:

| Prop      | Default   | Values                                                                                   |
| --------- | --------- | ---------------------------------------------------------------------------------------- |
| `variant` | `default` | `default`, `outline`, `secondary`, `destructive`, `destructive-outline`, `ghost`, `link` |
| `size`    | `md`      | `xs`, `sm`, `md`, `lg`, `xl`, `icon-sm`, `icon-md`, `icon-lg`                            |

Use `className` for root-level overrides. Compose icons, spinners, and labels through
`children`.

For submit buttons, pass `type="submit"`. For links, style an `<a>` directly instead of using
button semantics.

## Examples

### Variants

<Preview>
  <ButtonVariantsExample />

  <Preview.Code>
    {`
      import { Button } from "moduix";

      export function ButtonVariantsDemo() {
        return (
          <div>
            <Button>Default</Button>
            <Button variant="outline">Outline</Button>
            <Button variant="secondary">Secondary</Button>
            <Button variant="destructive">Destructive</Button>
            <Button variant="destructive-outline">Destructive Outline</Button>
            <Button variant="ghost">Ghost</Button>
            <Button variant="link">Link</Button>
          </div>
        );
      }
    `}

  </Preview.Code>
</Preview>

### Sizes

<Preview>
  <ButtonSizesExample />

  <Preview.Code>
    {`
      import { Button, StarIcon } from "moduix";

      export function ButtonSizesDemo() {
        return (
          <div>
            <Button size="xs">Extra-small</Button>
            <Button size="sm">Small</Button>
            <Button size="md">Medium</Button>
            <Button size="lg">Large</Button>
            <Button size="xl">Extra-large</Button>
            <Button size="icon-sm" variant="outline" aria-label="Small favorite">
              <StarIcon />
            </Button>
            <Button size="icon-md" variant="outline" aria-label="Favorite">
              <StarIcon />
            </Button>
            <Button size="icon-lg" variant="outline" aria-label="Large favorite">
              <StarIcon />
            </Button>
          </div>
        );
      }
    `}

  </Preview.Code>
</Preview>

### Icons

<Preview>
  <ButtonIconExample />

  <Preview.Code>
    {`
      import { ArrowUpRightIcon, Button, PlusIcon, StarIcon } from "moduix";

      export function ButtonIconsDemo() {
        return (
          <div>
            <Button>
              <PlusIcon />
              Create Item
            </Button>
            <Button size="icon-md" variant="outline" aria-label="Favorites">
              <StarIcon />
            </Button>
            <Button variant="link">
              Open Docs
              <ArrowUpRightIcon />
            </Button>
          </div>
        );
      }
    `}

  </Preview.Code>
</Preview>

### Disabled

<Preview>
  <ButtonDisabledExample />

  <Preview.Code>
    {`
      import { Button } from "moduix";

      export function ButtonDisabledDemo() {
        return (
          <div>
            <Button disabled>Disabled</Button>
            <Button disabled focusableWhenDisabled variant="outline">
              Focusable Disabled
            </Button>
          </div>
        );
      }
    `}

  </Preview.Code>
</Preview>

### Custom Tag

<Preview>
  <ButtonCustomTagExample />

  <Preview.Code>
    {`
      import { Button } from "moduix";

      export function ButtonCustomTagDemo() {
        return (
          <Button render={<div />} nativeButton={false}>
            Complex Trigger
          </Button>
        );
      }
    `}

  </Preview.Code>
</Preview>

### Pending State

Compose pending UI with `disabled`, `focusableWhenDisabled`, `aria-busy`, and a child spinner.

<Preview>
  <ButtonLoadingExample />

  <Preview.Code>
    {`
      import { Button, Spinner } from "moduix";
      import { useState } from "react";

      export function PendingButton() {
        const [pending, setPending] = useState(false);

        return (
          <Button
            disabled={pending}
            focusableWhenDisabled
            aria-busy={pending || undefined}
            onClick={() => {
              setPending(true);
              setTimeout(() => setPending(false), 1800);
            }}
          >
            {pending ? (
              <>
                <Spinner decorative size="sm" />
                Saving
              </>
            ) : (
              "Save Changes"
            )}
          </Button>
        );
      }
    `}

  </Preview.Code>
</Preview>

### Custom Styles

<Preview>
  <CustomStylesButtonExample />

  <Preview.Code>
    {`
      import { Button, Spinner } from "moduix";

      export function CustomStylesButtonDemo() {
        return (
          <Button
            className={styles.customButton}
            disabled
            focusableWhenDisabled
            aria-busy
          >
            <Spinner decorative size="sm" className={styles.customSpinner} />
            Publishing
          </Button>
        );
      }
    `}

  </Preview.Code>

  <Preview.CSS>
    {`
      .customButton {
        border-color: var(--color-primary);
        background-color: var(--color-primary);
        color: var(--color-primary-foreground);
        box-shadow: 0 1px 2px rgb(0 0 0 / 0.08);
      }

      .customSpinner {
        color: currentColor;
      }
    `}

  </Preview.CSS>
</Preview>