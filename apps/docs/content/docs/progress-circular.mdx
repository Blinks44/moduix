---
title: Progress Circular
description: A circular progressbar for determinate or indeterminate progress.
icon: Component
---

import {
  ControlledProgressCircularExample,
  CustomStylingProgressCircularExample,
  IndeterminateProgressCircularExample,
  InitialValueProgressCircularExample,
  MinMaxProgressCircularExample,
  ProgressCircularCssPropertiesPanel,
  ProgressCircularExample,
  RootProviderProgressCircularExample,
  StateViewsProgressCircularExample,
  ValueTextProgressCircularExample,
  progressCircularBasicData,
  progressCircularControlledData,
  progressCircularCustomStylingData,
  progressCircularCustomStylingCss,
  progressCircularIndeterminateData,
  progressCircularInitialValueData,
  progressCircularMinMaxData,
  progressCircularExampleCss,
  progressCircularRootProviderData,
  progressCircularStateViewsData,
} from '@/components/examples/progress-circular';

## API Reference

<PrimitiveReference href="https://ark-ui.com/docs/components/progress-circular" />

## Choosing the right component

Choose `ProgressCircular` for compact inline status, cards, toolbar badges, and small dashboards.
Choose `ProgressLinear` when horizontal comparison against a full track matters.

## Basic

Use Ark-style parts: `ProgressCircular.Root`, `ProgressCircular.Circle`,
`ProgressCircular.CircleTrack`, `ProgressCircular.CircleRange`, and `ProgressCircular.ValueText`.

<Preview>
  <ProgressCircularExample />

  <Preview.Code>
    {`
      import { ProgressCircular } from "@moduix/react";

      export function ProgressCircularDemo() {
        return (
          <ProgressCircular defaultValue={42}>
            <ProgressCircular.Label>Export data</ProgressCircular.Label>
            <div className="progress-circular-circle-container">
              <ProgressCircular.Circle>
                <ProgressCircular.CircleTrack />
                <ProgressCircular.CircleRange />
              </ProgressCircular.Circle>
              <ProgressCircular.ValueText />
            </div>
          </ProgressCircular>
        );
      }
    `}

  </Preview.Code>

  <Preview.CSS>{progressCircularExampleCss}</Preview.CSS>
  <Preview.Data>{progressCircularBasicData}</Preview.Data>
</Preview>

## Install with shadcn

If you want `ProgressCircular` to become part of your own project source instead of staying in
`node_modules`, install it from the moduix GitHub registry:

<Tabs items={['npm', 'pnpm', 'bun', 'deno', 'aube']}>
  <Tab value="npm">

```bash
npx shadcn@latest add Blinks44/moduix/react-progress-circular
```

  </Tab>
  <Tab value="pnpm">

```bash
pnpm dlx shadcn@latest add Blinks44/moduix/react-progress-circular
```

  </Tab>
  <Tab value="bun">

```bash
bunx shadcn@latest add Blinks44/moduix/react-progress-circular
```

  </Tab>
  <Tab value="deno">

```bash
deno x -A npm:shadcn@latest add Blinks44/moduix/react-progress-circular
```

  </Tab>
  <Tab value="aube">

```bash
aubx shadcn@latest add Blinks44/moduix/react-progress-circular
```

  </Tab>
</Tabs>

## Anatomy

```text
ProgressCircular.Root
├─ ProgressCircular.Label
├─ ProgressCircular.Circle
│  ├─ ProgressCircular.CircleTrack
│  └─ ProgressCircular.CircleRange
└─ ProgressCircular.ValueText

ProgressCircular.RootProvider
└─ same part tree connected to useProgress() from @ark-ui/react/progress
```

| Part                                         | Role                                                            |
| -------------------------------------------- | --------------------------------------------------------------- |
| `ProgressCircular` / `ProgressCircular.Root` | Owns progress state, formatting, and ids.                       |
| `ProgressCircular.RootProvider`              | Connects parts to state created with Ark `useProgress()`.       |
| `ProgressCircular.Label`                     | Accessible label for the progressbar.                           |
| `ProgressCircular.Circle`                    | SVG progressbar surface with ARIA value.                        |
| `ProgressCircular.CircleTrack`               | Background circle.                                              |
| `ProgressCircular.CircleRange`               | Foreground circle that reflects the current percent/state.      |
| `ProgressCircular.ValueText`                 | Visible value text generated from Ark translations.             |
| `ProgressCircular.View`                      | Conditional view for `indeterminate`, `loading`, or `complete`. |

## Composition

### Recommended composition

`ProgressCircular` is the short root form. It is equivalent to `ProgressCircular.Root`, and the SVG
circle parts stay explicit so the rendered anatomy matches Ark UI.

Use `defaultValue` for uncontrolled progress, `value` with `onValueChange(details)` for controlled
progress, and `defaultValue={null}` or `value={null}` for indeterminate progress.

### When to go custom

Use `ProgressCircular.RootProvider` with `useProgress()` from `@ark-ui/react/progress` when
progress state must be created outside the rendered subtree.

Use `translations.value(details)` for custom visible and accessible value text. Wrap the circle and
value text in your own layout element when the number should sit in the center.

Use `ids` when the label, circle, or surrounding layout need stable explicit IDs across a more
custom tree. Use `ProgressCircular.View` when supporting UI should switch with the Ark progress
state instead of duplicating state checks outside the component. If you need Ark context APIs or
hooks, import them directly from `@ark-ui/react/progress`.

### Default props

| Prop            | Default                | Notes                                   |
| --------------- | ---------------------- | --------------------------------------- |
| `defaultValue`  | `50`                   | Initial uncontrolled value.             |
| `min`           | `0`                    | Lower bound for percent and ARIA value. |
| `max`           | `100`                  | Upper bound for percent and ARIA value. |
| `formatOptions` | `{ style: 'percent' }` | Controls `ValueText` formatting.        |
| `locale`        | `'en-US'`              | Locale used by `Intl.NumberFormat`.     |

### Behavioral notes

`ProgressCircular.Circle` receives the progressbar role and ARIA value attributes from Ark. Ark
also owns the SVG variables for radius, circumference, percent, and stroke offset.

All exported parts support `asChild` through Ark polymorphic composition. Keep replacement children
semantic and preserve the progressbar semantics on the circle.

`ProgressCircular.ValueText` announces changes with `aria-live="polite"`. Keep the visible label in
the tree, or provide `aria-label`/`aria-labelledby` when composing without `ProgressCircular.Label`.

## Examples

### Controlled

Keep `value` in React state when circular progress mirrors another control or external task state.

<Preview>
  <ControlledProgressCircularExample />

  <Preview.Code>
    {`
      import { ProgressCircular, Slider } from "@moduix/react";
      import { useState } from "react";

      export function ControlledProgressCircularDemo() {
        const [value, setValue] = useState(42 as number | null);

        return (
          <div className="progress-circular-stack">
            <ProgressCircular value={value} onValueChange={(details) => setValue(details.value)}>
              <ProgressCircular.Label>Upload status</ProgressCircular.Label>
              <div className="progress-circular-circle-container">
                <ProgressCircular.Circle>
                  <ProgressCircular.CircleTrack />
                  <ProgressCircular.CircleRange />
                </ProgressCircular.Circle>
                <ProgressCircular.ValueText />
              </div>
            </ProgressCircular>
            <Slider
              className="progress-circular-slider"
              min={0}
              max={100}
              value={[value ?? 0]}
              onValueChange={(details) => setValue(details.value[0] ?? 0)}
            >
              <Slider.Label>Progress value</Slider.Label>
              <Slider.ValueText />
              <Slider.Control>
                <Slider.Track>
                  <Slider.Range />
                </Slider.Track>
                <Slider.Thumb index={0} aria-label="Progress value">
                  <Slider.HiddenInput />
                </Slider.Thumb>
              </Slider.Control>
            </Slider>
          </div>
        );
      }
    `}

  </Preview.Code>

  <Preview.CSS>{progressCircularExampleCss}</Preview.CSS>
  <Preview.Data>{progressCircularControlledData}</Preview.Data>
</Preview>

### Initial Value

Use `defaultValue` to set the initial uncontrolled progress value.

<Preview>
  <InitialValueProgressCircularExample />

  <Preview.Code>
    {`
      import { ProgressCircular } from "@moduix/react";

      export function InitialValueProgressCircularDemo() {
        return (
          <ProgressCircular defaultValue={70}>
            <ProgressCircular.Label>Import data</ProgressCircular.Label>
            <div className="progress-circular-circle-container">
              <ProgressCircular.Circle>
                <ProgressCircular.CircleTrack />
                <ProgressCircular.CircleRange />
              </ProgressCircular.Circle>
              <ProgressCircular.ValueText />
            </div>
          </ProgressCircular>
        );
      }
    `}

  </Preview.Code>

  <Preview.CSS>{progressCircularExampleCss}</Preview.CSS>
  <Preview.Data>{progressCircularInitialValueData}</Preview.Data>
</Preview>

### Min Max

Use `min` and `max` when the value does not use the default `0` to `100` range.

<Preview>
  <MinMaxProgressCircularExample />

  <Preview.Code>
    {`
      import { ProgressCircular } from "@moduix/react";

      export function MinMaxProgressCircularDemo() {
        return (
          <ProgressCircular defaultValue={420} min={200} max={800}>
            <ProgressCircular.Label>Requests per minute</ProgressCircular.Label>
            <div className="progress-circular-circle-container">
              <ProgressCircular.Circle>
                <ProgressCircular.CircleTrack />
                <ProgressCircular.CircleRange />
              </ProgressCircular.Circle>
              <ProgressCircular.ValueText />
            </div>
          </ProgressCircular>
        );
      }
    `}

  </Preview.Code>

  <Preview.CSS>{progressCircularExampleCss}</Preview.CSS>
  <Preview.Data>{progressCircularMinMaxData}</Preview.Data>
</Preview>

### Indeterminate

Pass `defaultValue={null}` or `value={null}` when progress is running but completion is unknown.

<Preview>
  <IndeterminateProgressCircularExample />

  <Preview.Code>
    {`
      import { ProgressCircular } from "@moduix/react";

      export function IndeterminateProgressCircularDemo() {
        return (
          <ProgressCircular defaultValue={null}>
            <ProgressCircular.Label>Preparing report</ProgressCircular.Label>
            <div className="progress-circular-circle-container">
              <ProgressCircular.Circle>
                <ProgressCircular.CircleTrack />
                <ProgressCircular.CircleRange />
              </ProgressCircular.Circle>
              <ProgressCircular.ValueText />
            </div>
          </ProgressCircular>
        );
      }
    `}

  </Preview.Code>

  <Preview.CSS>{progressCircularExampleCss}</Preview.CSS>
  <Preview.Data>{progressCircularIndeterminateData}</Preview.Data>
</Preview>

### Value Text

Use `translations.value(details)` when the formatted percent is not the wording users need.

<Preview>
  <ValueTextProgressCircularExample />

  <Preview.Code>
    {`
      import { ProgressCircular } from "@moduix/react";

      export function ValueTextProgressCircularDemo() {
        return (
          <ProgressCircular
            translations={{
              value({ value, max }) {
                if (value === null) return "Loading...";
                return \`\${value} of \${max}\`;
              },
            }}
          >
            <ProgressCircular.Label>Migration</ProgressCircular.Label>
            <div className="progress-circular-circle-container">
              <ProgressCircular.Circle>
                <ProgressCircular.CircleTrack />
                <ProgressCircular.CircleRange />
              </ProgressCircular.Circle>
              <ProgressCircular.ValueText />
            </div>
          </ProgressCircular>
        );
      }
    `}

  </Preview.Code>

  <Preview.CSS>{progressCircularExampleCss}</Preview.CSS>
  <Preview.Data>{`const fallbackText = 'Loading...';`}</Preview.Data>
</Preview>

### Root Provider

Create the Ark progress store outside the rendered tree when another component needs to own state.

<Preview>
  <RootProviderProgressCircularExample />

  <Preview.Code>
    {`
      import { useProgress } from "@ark-ui/react/progress";
      import { ProgressCircular } from "@moduix/react";

      export function RootProviderProgressCircularDemo() {
        const progress = useProgress({ defaultValue: 58 });

        return (
          <ProgressCircular.RootProvider value={progress}>
            <ProgressCircular.Label>Team rollout</ProgressCircular.Label>
            <div className="progress-circular-circle-container">
              <ProgressCircular.Circle>
                <ProgressCircular.CircleTrack />
                <ProgressCircular.CircleRange />
              </ProgressCircular.Circle>
              <ProgressCircular.ValueText />
            </div>
          </ProgressCircular.RootProvider>
        );
      }
    `}

  </Preview.Code>

  <Preview.CSS>{progressCircularExampleCss}</Preview.CSS>
  <Preview.Data>{progressCircularRootProviderData}</Preview.Data>
</Preview>

### State Views

Use `ProgressCircular.View` when supporting text or UI should switch with the Ark progress state
instead of duplicating state checks outside the component.

<Preview>
  <StateViewsProgressCircularExample />

  <Preview.Code>
    {`
      import { ProgressCircular } from "@moduix/react";

      export function StateViewsProgressCircularDemo() {
        return (
          <ProgressCircular defaultValue={null}>
            <ProgressCircular.Label>Preparing report</ProgressCircular.Label>
            <div className="progress-circular-circle-container">
              <ProgressCircular.Circle>
                <ProgressCircular.CircleTrack />
                <ProgressCircular.CircleRange />
              </ProgressCircular.Circle>
              <ProgressCircular.ValueText />
            </div>
            <ProgressCircular.View className="progress-circular-state" state="indeterminate">
              {progressCircularStateMessages.indeterminate}
            </ProgressCircular.View>
            <ProgressCircular.View className="progress-circular-state" state="loading">
              {progressCircularStateMessages.loading}
            </ProgressCircular.View>
            <ProgressCircular.View className="progress-circular-state" state="complete">
              {progressCircularStateMessages.complete}
            </ProgressCircular.View>
          </ProgressCircular>
        );
      }
    `}

  </Preview.Code>

  <Preview.CSS>{progressCircularExampleCss}</Preview.CSS>
  <Preview.Data>{progressCircularStateViewsData}</Preview.Data>
</Preview>

### Custom Styling

Override public CSS variables and target SVG `data-slot` hooks when only the visuals need to change.

<Preview>
  <CustomStylingProgressCircularExample />

  <Preview.Code>
    {`
      import { ProgressCircular } from "@moduix/react";

      export function CustomStylingProgressCircularDemo() {
        return (
          <ProgressCircular defaultValue={72} className="progress-circular-custom">
            <ProgressCircular.Label>Monthly quota</ProgressCircular.Label>
            <div className="progress-circular-circle-container">
              <ProgressCircular.Circle>
                <ProgressCircular.CircleTrack />
                <ProgressCircular.CircleRange />
              </ProgressCircular.Circle>
              <ProgressCircular.ValueText />
            </div>
          </ProgressCircular>
        );
      }
    `}

  </Preview.Code>

  <Preview.CSS>{progressCircularCustomStylingCss}</Preview.CSS>
  <Preview.Data>{progressCircularCustomStylingData}</Preview.Data>
</Preview>

## Styling

Use `className` on exported parts for structural styling changes. For state styling, prefer Ark
data attributes, Ark SVG variables, and moduix `data-slot` hooks.

### CSS Properties

<div className="not-prose">
  <Tabs items={['CSS Variables']} className="mb-0 mt-3">
    <Tab className="max-h-150 overflow-auto">
      <ProgressCircularCssPropertiesPanel />
    </Tab>
  </Tabs>
</div>

### Styling hooks

- `data-slot="progress-circular-root"`, `progress-circular-root-provider`,
  `progress-circular-label`, `progress-circular-circle`, `progress-circular-circle-track`,
  `progress-circular-circle-range`, `progress-circular-value-text`, and `progress-circular-view`.
- Ark hooks: `data-scope="progress"`, `data-part`, `data-state="indeterminate|loading|complete"`,
  `data-orientation`, `data-value`, and `data-max`.
- Ark circle variables: `--size`, `--thickness`, `--radius`, `--circumference`, `--percent`, and
  stroke offset values generated by Ark.