---
title: Container
description: A responsive layout primitive for consistent content width and fluid inline gutters.
icon: Component
---

import {
  ContainerBleedExample,
  ContainerCssPropertiesPanel,
  ContainerExample,
  ContainerGuttersExample,
  ContainerSemanticExample,
  ContainerSizesExample,
  CustomStylingContainerExample,
} from '@/components/examples/container';

## API Reference

Ark UI does not provide a dedicated `Container` primitive. moduix implements `Container` as an
Ark factory wrapper that follows the Ark composition guide:
https://ark-ui.com/docs/guides/composition.

## Basic

Use `Container` when the main concern is readable inline width. It centers a content column,
applies responsive inline gutters, and stays out of vertical spacing decisions.

<Preview cssVariableScope="local">
  <ContainerExample />

  <Preview.Code>
    {`
      import { Container, Heading, Text } from "moduix";

      export function ContainerDemo() {
        return (
          <Container.Root className="container">
            <Heading as="h3" size="lg">
              Responsive page content
            </Heading>
            <Text tone="muted">
              The content column stays readable while inline gutters fluidly adapt.
            </Text>
          </Container.Root>
        );
      }
    `}

  </Preview.Code>

  <Preview.CSS>
    {`
      .container {
        padding-block: var(--spacing-4);
        border-block: var(--border-width-sm) dashed var(--color-border);
        background-color: var(--color-muted);
      }

      .container > * {
        max-width: 42rem;
      }
    `}

  </Preview.CSS>
</Preview>

## Install with shadcn (optional)

If you want `Container` to become part of your own project source instead of staying in
`node_modules`, install it from the moduix GitHub registry:

<Tabs items={['npm', 'pnpm', 'bun', 'deno', 'aube']}>
  <Tab value="npm">

```bash
npx shadcn@latest add Blinks44/moduix/container
```

  </Tab>
  <Tab value="pnpm">

```bash
pnpm dlx shadcn@latest add Blinks44/moduix/container
```

  </Tab>
  <Tab value="bun">

```bash
bunx shadcn@latest add Blinks44/moduix/container
```

  </Tab>
  <Tab value="deno">

```bash
deno x -A npm:shadcn@latest add Blinks44/moduix/container
```

  </Tab>
  <Tab value="aube">

```bash
aubx shadcn@latest add Blinks44/moduix/container
```

  </Tab>
</Tabs>

This install path also pulls in the shared moduix foundation files that `Container` depends on, such
as styles, icons, and small utilities.

## Anatomy

`Container` is a single root layout primitive built on Ark factory composition.

```text
Container.Root
└─ children
```

| Part             | Role                                                     |
| ---------------- | -------------------------------------------------------- |
| `Container.Root` | Root layout wrapper. Controls content width and gutters. |

## Composition

### Recommended composition

Use `Container.Root` for page shells, article bodies, docs sections, settings screens, and other
layouts where the main requirement is a centered content column with predictable gutters.

Use `size` to choose the content width and `gutter` to control the edge padding. Use `asChild` when
the wrapper itself should also carry semantic meaning such as `main`, `section`, or `article`.

### When to go custom

`Container` is intentionally root-only. There are no extra subparts, slot props, or nested
layout helpers.

The custom path here is small:

- pass `className` for local layout or surface styling;
- override `--container-*` variables when a page needs different width or gutter math;
- use `asChild` when another element should own the DOM node;
- compose with `Bleed` when a child should escape the constrained text column.

### Default props

| Prop      | Default | Values / Notes                       |
| --------- | ------- | ------------------------------------ |
| `size`    | `lg`    | `xs`, `sm`, `md`, `lg`, `xl`, `full` |
| `gutter`  | `md`    | `none`, `sm`, `md`, `lg`             |
| `asChild` | `false` | Ark factory composition              |

### Behavioral notes

- `Container` does not manage vertical rhythm. Add section spacing outside it or on its children.
- `size="full"` removes only the max-width cap. The selected gutter still applies unless
  `gutter="none"` is used.
- The root exposes `data-scope="container"`, `data-part="root"`, `data-slot="container-root"`,
  `data-size`, and `data-gutter` for styling and tests.

## Examples

### Sizes

Use `size` to choose the content column width. `full` fills the available inline space while
preserving the selected gutter.

<Preview>
  <ContainerSizesExample />

  <Preview.Code>
    {`
      import { Container, Text } from "moduix";

      export function ContainerSizesDemo() {
        return (
          <div className="stack">
            <Container.Root size="xs" className="container"><Text>xs</Text></Container.Root>
            <Container.Root size="sm" className="container"><Text>sm</Text></Container.Root>
            <Container.Root size="md" className="container"><Text>md</Text></Container.Root>
            <Container.Root size="lg" className="container"><Text>lg</Text></Container.Root>
            <Container.Root size="xl" className="container"><Text>xl</Text></Container.Root>
            <Container.Root size="full" className="container"><Text>full</Text></Container.Root>
          </div>
        );
      }
    `}

  </Preview.Code>

  <Preview.CSS>
    {`
      .stack {
        display: flex;
        width: 100%;
        flex-direction: column;
        gap: var(--spacing-4);
      }

      .container {
        padding-block: var(--spacing-4);
        border-block: var(--border-width-sm) dashed var(--color-border);
        background-color: var(--color-muted);
      }
    `}

  </Preview.CSS>
</Preview>

### Gutters

Use `gutter` to control responsive inline padding.

<Preview>
  <ContainerGuttersExample />

  <Preview.Code>
    {`
      import { Container, Text } from "moduix";

      export function ContainerGuttersDemo() {
        return (
          <div className="stack">
            <Container.Root gutter="none" className="container"><Text>No gutter</Text></Container.Root>
            <Container.Root gutter="sm" className="container"><Text>Small gutter</Text></Container.Root>
            <Container.Root gutter="md" className="container"><Text>Medium gutter</Text></Container.Root>
            <Container.Root gutter="lg" className="container"><Text>Large gutter</Text></Container.Root>
          </div>
        );
      }
    `}

  </Preview.Code>

  <Preview.CSS>
    {`
      .stack {
        display: flex;
        width: 100%;
        flex-direction: column;
        gap: var(--spacing-4);
      }

      .container {
        padding-block: var(--spacing-4);
        border-block: var(--border-width-sm) dashed var(--color-border);
        background-color: var(--color-muted);
      }
    `}

  </Preview.CSS>
</Preview>

### Semantic Element

Use `asChild` when the layout wrapper should also describe document structure.

<Preview>
  <ContainerSemanticExample />

  <Preview.Code>
    {`
      import { Container, Heading, Text } from "moduix";

      export function ContainerSemanticDemo() {
        return (
          <Container.Root asChild size="md" className="container">
            <main>
              <Heading as="h3" size="lg">Main content area</Heading>
              <Text tone="muted">Use asChild when the wrapper should carry semantic meaning.</Text>
            </main>
          </Container.Root>
        );
      }
    `}

  </Preview.Code>

  <Preview.CSS>
    {`
      .container {
        padding-block: var(--spacing-4);
        border-block: var(--border-width-sm) dashed var(--color-border);
        background-color: var(--color-muted);
      }

      .container > * {
        max-width: 42rem;
      }
    `}

  </Preview.CSS>
</Preview>

### With Bleed

Keep the text column constrained and let only the wider child opt out.

<Preview>
  <ContainerBleedExample />

  <Preview.Code>
    {`
      import { Bleed, Container, Heading, Text } from "moduix";

      export function ContainerBleedDemo() {
        return (
          <Container.Root className="container">
            <Heading as="h3" size="lg">Article body</Heading>
            <Text tone="muted">
              Keep the reading width constrained, then use Bleed for content that should stretch wider.
            </Text>
            <Bleed.Root inline="md">
              <div className="bleedSurface">Bleed content escapes the constrained column.</div>
            </Bleed.Root>
          </Container.Root>
        );
      }
    `}

  </Preview.Code>

  <Preview.CSS>
    {`
      .container {
        padding-block: var(--spacing-4);
        border-block: var(--border-width-sm) dashed var(--color-border);
        background-color: var(--color-muted);
      }

      .container > * {
        max-width: 42rem;
      }

      .bleedSurface {
        padding: var(--spacing-4);
        border: var(--border-width-sm) solid var(--color-border);
        background-color: var(--color-background);
      }
    `}

  </Preview.CSS>
</Preview>

### Custom Styling

Pass `className` when styling the root or overriding container variables locally.

<Preview>
  <CustomStylingContainerExample />

  <Preview.Code>
    {`
      import { Container, Text } from "moduix";

      export function CustomStyledContainerDemo() {
        return (
          <Container.Root className="customContainer">
            <Text weight="semibold">Customized max width and gutters</Text>
          </Container.Root>
        );
      }
    `}

  </Preview.Code>

  <Preview.CSS>
    {`
      .customContainer {
        padding-block: var(--spacing-4);
        border-block: var(--border-width-sm) dashed var(--color-border);
        background-color: var(--color-muted);
        --container-gutter-md: clamp(1.25rem, 6vw, 4rem);
        --container-max-width-lg: 54rem;
      }

      .customContainer > * {
        max-width: 42rem;
      }
    `}

  </Preview.CSS>
</Preview>

## Styling

Use CSS variables on the root when a page shell needs different gutter math or a different width
scale. Use `className` for local block styling such as padding, borders, or backgrounds.

### CSS Properties

<div className="not-prose">
  <Tabs items={['CSS Variables']} className="mb-0 mt-3">
    <Tab className="max-h-150 overflow-auto">
      <ContainerCssPropertiesPanel />
    </Tab>
  </Tabs>
</div>

### Styling hooks

- `Container.Root` exposes `data-slot="container-root"` on the root.
- `data-scope="container"` and `data-part="root"` provide Ark-aligned root hooks.
- `data-size` selects the width preset: `xs`, `sm`, `md`, `lg`, `xl`, `full`.
- `data-gutter` selects the gutter preset: `none`, `sm`, `md`, `lg`.
- `--container-*` variables control only inline gutter and max-width math; vertical spacing and
  child layout remain the responsibility of surrounding layout CSS.