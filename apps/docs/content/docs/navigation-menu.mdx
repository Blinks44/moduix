---
title: Navigation Menu
description: A collection of links and menus for website and application navigation.
icon: Component
---

import {
  CustomCompositionNavigationMenuExample,
  CustomIconNavigationMenuExample,
  FullWidthNavigationMenuExample,
  NavigationMenuCssPlaygroundPanel,
  NavigationMenuCssPropertiesPanel,
  NavigationMenuExample,
  NestedInlineNavigationMenuExample,
  NestedNavigationMenuExample,
  navigationMenuPlaygroundCssProperties,
} from '@/components/examples/navigation-menu';

## API Reference

<BaseUIReference href="https://base-ui.com/react/components/navigation-menu" />

## Basic

<Preview cssProperties={navigationMenuPlaygroundCssProperties}>
  <NavigationMenuExample />

  <Preview.Code>
    {`
      import {
        NavigationMenu,
        NavigationMenuContent,
        NavigationMenuItem,
        NavigationMenuLink,
        NavigationMenuList,
        NavigationMenuTrigger,
      } from "moduix";

      export function NavigationMenuDemo() {
        return (
          <NavigationMenu>
            <NavigationMenuList>
              <NavigationMenuItem>
                <NavigationMenuTrigger>Product</NavigationMenuTrigger>
                <NavigationMenuContent>
                  <ul className={styles.contentGrid}>{/* links */}</ul>
                </NavigationMenuContent>
              </NavigationMenuItem>

              <NavigationMenuItem>
                <NavigationMenuTrigger>Guides</NavigationMenuTrigger>
                <NavigationMenuContent>
                  <ul className={styles.contentList}>{/* links */}</ul>
                </NavigationMenuContent>
              </NavigationMenuItem>

              <NavigationMenuItem>
                <NavigationMenuLink href="#" closeOnClick>
                  Releases
                </NavigationMenuLink>
              </NavigationMenuItem>
            </NavigationMenuList>
          </NavigationMenu>
        );
      }
    `}

  </Preview.Code>

  <Preview.CSSProperties>
    {(context) => <NavigationMenuCssPropertiesPanel {...context} />}
  </Preview.CSSProperties>
  <Preview.CSSPlayground>
    {(context) => <NavigationMenuCssPlaygroundPanel {...context} />}
  </Preview.CSSPlayground>
</Preview>

## Anatomy

`NavigationMenu` is a styled wrapper around the Base UI primitive. The default path keeps popup
infrastructure internal and only asks for the visible parts.

```text
NavigationMenu
└─ NavigationMenuList
   ├─ NavigationMenuItem
   │  ├─ NavigationMenuTrigger
   │  └─ NavigationMenuContent
   └─ NavigationMenuItem
      └─ NavigationMenuLink
```

The root renders `Portal`, `Positioner`, `Popup`, `Arrow`, and `Viewport` automatically. Those
parts stay available as explicit building blocks when you need a different DOM structure, popup
layout, backdrop, or inline viewport.

## Composition

`NavigationMenu` forwards Base UI root props such as `value`, `defaultValue`, `onValueChange`,
`delay`, `closeDelay`, `orientation`, `actionsRef`, `render`, `className`, and `style`.

The high-level root also accepts a small popup contract:

| Prop                                                                                                                                                                         | Default                             | Purpose                                                                                                                        |
| ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------- | ------------------------------------------------------------------------------------------------------------------------------ |
| `showPopup`                                                                                                                                                                  | `true`                              | Disables the built-in popup composition so you can assemble `Portal`, `Positioner`, `Popup`, `Arrow`, and `Viewport` manually. |
| `showArrow`                                                                                                                                                                  | `true`                              | Toggles the built-in popup arrow in the default composition.                                                                   |
| `sideOffset`                                                                                                                                                                 | `10`                                | Default offset for the built-in `Positioner`.                                                                                  |
| `side`, `align`, `alignOffset`, `arrowPadding`, `anchor`, `collisionAvoidance`, `collisionBoundary`, `collisionPadding`, `sticky`, `positionMethod`, `disableAnchorTracking` | Base UI defaults unless noted above | Forwarded to the built-in `Positioner`.                                                                                        |

`NavigationMenuTrigger` keeps two small DX shortcuts:

| Prop       | Default                | Purpose                            |
| ---------- | ---------------------- | ---------------------------------- |
| `icon`     | `ChevronDownSmallIcon` | Replaces the default trigger icon. |
| `hideIcon` | `false`                | Removes the built-in trigger icon. |

When the default path is enough, style `NavigationMenu`, `NavigationMenuTrigger`,
`NavigationMenuContent`, and `NavigationMenuLink`. Switch to explicit parts only when structure or
popup behavior needs to change.

## Examples

### Nested Submenu

Use another `NavigationMenu` inside content when a second-level submenu should open in its own
popup.

<Preview>
  <NestedNavigationMenuExample />
</Preview>

### Nested Inline Submenu

When second-level content should stay inside the current panel, disable the built-in popup and
render `NavigationMenuViewport` inline.

<Preview>
  <NestedInlineNavigationMenuExample />

  <Preview.Code>
    {`
      import {
        NavigationMenu,
        NavigationMenuContent,
        NavigationMenuItem,
        NavigationMenuList,
        NavigationMenuTrigger,
        NavigationMenuViewport,
      } from "moduix";

      export function InlineNavigationMenuDemo() {
        return (
          <NavigationMenu>
            <NavigationMenuList>
              <NavigationMenuItem>
                <NavigationMenuTrigger>Product</NavigationMenuTrigger>
                <NavigationMenuContent>
                  <NavigationMenu
                    defaultValue="developers"
                    orientation="vertical"
                    showPopup={false}
                  >
                    <NavigationMenuList>{/* nested items */}</NavigationMenuList>
                    <NavigationMenuViewport className={styles.inlineViewport} />
                  </NavigationMenu>
                </NavigationMenuContent>
              </NavigationMenuItem>
            </NavigationMenuList>
          </NavigationMenu>
        );
      }
    `}

  </Preview.Code>
</Preview>

### Custom Composition

When you need a backdrop, custom popup structure, or direct styling hooks for the infrastructure
parts, disable the default popup and compose the low-level parts explicitly.

<Preview>
  <CustomCompositionNavigationMenuExample />

  <Preview.Code>
    {`
      import {
        NavigationMenu,
        NavigationMenuArrow,
        NavigationMenuBackdrop,
        NavigationMenuContent,
        NavigationMenuItem,
        NavigationMenuList,
        NavigationMenuPopup,
        NavigationMenuPortal,
        NavigationMenuPositioner,
        NavigationMenuTrigger,
        NavigationMenuViewport,
      } from "moduix";

      export function CustomNavigationMenuDemo() {
        return (
          <NavigationMenu showPopup={false}>
            <NavigationMenuList>
              <NavigationMenuItem>
                <NavigationMenuTrigger>Resources</NavigationMenuTrigger>
                <NavigationMenuContent>{/* content */}</NavigationMenuContent>
              </NavigationMenuItem>
            </NavigationMenuList>

            <NavigationMenuPortal>
              <NavigationMenuBackdrop />
              <NavigationMenuPositioner sideOffset={16}>
                <NavigationMenuPopup>
                  <NavigationMenuArrow />
                  <NavigationMenuViewport />
                </NavigationMenuPopup>
              </NavigationMenuPositioner>
            </NavigationMenuPortal>
          </NavigationMenu>
        );
      }
    `}

  </Preview.Code>
</Preview>

### Full Width Popup

The same low-level path covers viewport-width popups without adding a dedicated root prop.

<Preview>
  <FullWidthNavigationMenuExample />
</Preview>

### Custom Icon

Use `icon` for the common case. Use `hideIcon` when the trigger should not render one.

<Preview>
  <CustomIconNavigationMenuExample />
</Preview>