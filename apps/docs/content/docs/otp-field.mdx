---
title: OTP Field
description: A one-time password input composed of individual character slots.
icon: Component
---

import 'moduix/style.css';
import {
  OTPFieldCssPlaygroundPanel,
  OTPFieldCssPropertiesPanel,
  OTPFieldAlphanumericExample,
  OTPFieldAutoSubmitExample,
  OTPFieldCustomSanitizationExample,
  OTPFieldCustomSeparatorExample,
  OTPFieldExample,
  OTPFieldGroupedLayoutExample,
  OTPFieldMaskedExample,
  OTPFieldPlaceholderHintsExample,
  OTPFieldValidationExample,
  otpFieldPlaygroundCssProperties,
} from '@/components/examples/otp-field';

## API Reference

<BaseUIReference href="https://base-ui.com/react/components/otp-field" />

## Basic

`OTPField` is a thin wrapper over the Base UI root primitive. By default it renders one styled slot
per character in `length`, so the common path stays short. Drop down to `OTPFieldInput` and
`OTPFieldSeparator` only when the markup or per-slot styling needs to change.

<Preview cssProperties={otpFieldPlaygroundCssProperties}>
  <OTPFieldExample />

  <Preview.Code>
    {`
      import { Field, FieldLabel, OTPField } from "moduix";
      import { useId } from "react";

      export function OTPFieldDemo() {
        const id = useId();

        return (
          <Field>
            <FieldLabel htmlFor={id}>Verification code</FieldLabel>
            <OTPField id={id} length={6} />
          </Field>
        );
      }
    `}

  </Preview.Code>
  <Preview.CSSProperties>
    {(context) => <OTPFieldCssPropertiesPanel {...context} />}
  </Preview.CSSProperties>
  <Preview.CSSPlayground>
    {(context) => <OTPFieldCssPlaygroundPanel {...context} />}
  </Preview.CSSPlayground>
</Preview>

The generated first slot keeps the field label association from `htmlFor={id}`. The remaining slots
receive position-aware labels automatically in the default path.

## Anatomy

```text
OTPField[length]
├─ OTPFieldInput
├─ OTPFieldInput
├─ OTPFieldSeparator (optional)
└─ OTPFieldInput
```

| Part                | Role                                                                                         |
| ------------------- | -------------------------------------------------------------------------------------------- |
| `OTPField`          | Root state machine. Owns value, validation, completion, masking, and default slot rendering. |
| `OTPFieldInput`     | Visible character slot. Use it for grouped layouts, placeholders, or per-slot styling.       |
| `OTPFieldSeparator` | Optional visual separator between groups. Decorative by default.                             |

## Composition

Use the default path when every slot shares the same structure:

```tsx
<OTPField id={id} length={6} />
```

Compose it manually when the layout needs grouping, separators, or different props on specific
slots:

```tsx
<OTPField id={id} length={6}>
  <div className={styles.group}>
    <OTPFieldInput />
    <OTPFieldInput aria-label="Character 2 of 6" />
    <OTPFieldInput aria-label="Character 3 of 6" />
  </div>
  <OTPFieldSeparator>-</OTPFieldSeparator>
  <div className={styles.group}>
    <OTPFieldInput aria-label="Character 4 of 6" />
    <OTPFieldInput aria-label="Character 5 of 6" />
    <OTPFieldInput aria-label="Character 6 of 6" />
  </div>
</OTPField>
```

When composing manually, keep the first slot unlabeled so it inherits the field label, give the
remaining slots position-aware `aria-label` values, and still render exactly `length` inputs.

## Examples

### Alphanumeric

Use `validationType="alphanumeric"` for recovery, backup, or invite codes.

<Preview>
  <OTPFieldAlphanumericExample />

  <Preview.Code>
    {`
      import {
        Field,
        FieldDescription,
        FieldLabel,
        OTPField,
      } from "moduix";
      import { useId, useState } from "react";

      export function AlphanumericOTPField() {
        const id = useId();
        const [value, setValue] = useState("");

        return (
          <Field>
            <FieldLabel htmlFor={id}>Recovery code</FieldLabel>
            <FieldDescription>
              Letters and numbers are allowed, for example <code>A7C9XZ</code>.
            </FieldDescription>
            <OTPField
              id={id}
              length={6}
              value={value}
              validationType="alphanumeric"
              onValueChange={setValue}
            />
          </Field>
        );
      }
    `}

  </Preview.Code>
</Preview>

### Masked

Use `mask` when the code should stay obscured while it is being typed.

<Preview>
  <OTPFieldMaskedExample />

  <Preview.Code>
    {`
      import { Field, FieldLabel, OTPField } from "moduix";
      import { useId } from "react";

      export function MaskedOTPField() {
        const id = useId();

        return (
          <Field>
            <FieldLabel htmlFor={id}>PIN</FieldLabel>
            <OTPField id={id} length={4} mask />
          </Field>
        );
      }
    `}

  </Preview.Code>
</Preview>

### Field Validation

Wrap the field in `Field` when you want label, description, and validation messaging to stay in one
form primitive flow.

<Preview>
  <OTPFieldValidationExample />

  <Preview.Code>
    {`
      import {
        Field,
        FieldError,
        FieldLabel,
        OTPField,
      } from "moduix";
      import { useId } from "react";

      export function ValidatedOTPField() {
        const id = useId();

        return (
          <Field name="verificationCode" validationMode="onBlur">
            <FieldLabel htmlFor={id}>Verification code</FieldLabel>
            <OTPField id={id} length={6} required />
            <FieldError match="valueMissing">Please enter the verification code.</FieldError>
          </Field>
        );
      }
    `}

  </Preview.Code>
</Preview>

### Auto Submit

Use `autoSubmit` when a complete code should submit its owning form immediately.

<Preview>
  <OTPFieldAutoSubmitExample />

  <Preview.Code>
    {`
      import { Field, FieldDescription, FieldLabel, OTPField } from "moduix";
      import { useId, useState } from "react";
      import styles from "./otp-field.module.css";

      export function AutoSubmitOTPField() {
        const id = useId();
        const [completedValue, setCompletedValue] = useState("");
        const [submittedValue, setSubmittedValue] = useState("");

        return (
          <form
            className={styles.form}
            onSubmit={(event) => {
              event.preventDefault();
              const formData = new FormData(event.currentTarget);
              setSubmittedValue(String(formData.get("verificationCode") ?? ""));
            }}
          >
            <Field>
              <FieldLabel htmlFor={id}>Verification code</FieldLabel>
              <FieldDescription>
                Form submits automatically when all slots are filled.
              </FieldDescription>
              <OTPField
                id={id}
                name="verificationCode"
                length={6}
                autoSubmit
                onValueComplete={(value) => {
                  setCompletedValue(value);
                }}
              />
            </Field>
            <span className={styles.hint}>Last completed value: {completedValue || "empty"}</span>
            <span className={styles.hint}>Last submitted value: {submittedValue || "empty"}</span>
          </form>
        );
      }
    `}

  </Preview.Code>
</Preview>

### Custom Sanitization

Set `validationType="none"` with `normalizeValue` when typed or pasted input needs app-specific
normalization.

<Preview>
  <OTPFieldCustomSanitizationExample />

  <Preview.Code>
    {`
      import { Field, FieldDescription, FieldLabel, OTPField } from "moduix";
      import { useId, useState } from "react";
      import styles from "./otp-field.module.css";

      export function CustomSanitizationOTPField() {
        const id = useId();
        const [value, setValue] = useState("");
        const [invalidValue, setInvalidValue] = useState("");

        return (
          <div className={styles.stack}>
            <Field className={styles.field}>
              <FieldLabel htmlFor={id}>Invite code</FieldLabel>
              <FieldDescription>
                <code>validationType=&quot;none&quot;</code> with custom sanitization.
              </FieldDescription>
              <OTPField
                id={id}
                length={6}
                value={value}
                validationType="none"
                normalizeValue={(nextValue) => nextValue.toUpperCase().replace(/[^A-Z0-9]/g, "")}
                onValueChange={setValue}
                onValueInvalid={(nextValue) => {
                  setInvalidValue(nextValue);
                }}
              />
            </Field>
            <span className={styles.hint}>Current value: {value || "empty"}</span>
            <span className={styles.hint}>Last invalid attempt: {invalidValue || "none"}</span>
          </div>
        );
      }
    `}

  </Preview.Code>
</Preview>

## Custom Composition

Use explicit composition when the layout needs grouped slots, custom separators, or per-slot native
input props.

### Grouped Layout

<Preview>
  <OTPFieldGroupedLayoutExample />

  <Preview.Code>
    {`
      import {
        Field,
        FieldLabel,
        OTPField,
        OTPFieldInput,
        OTPFieldSeparator,
        SeparatorMarkIcon,
      } from "moduix";
      import { useId } from "react";
      import styles from "./otp-field.module.css";

      export function GroupedOTPField() {
        const id = useId();

        return (
          <Field>
            <FieldLabel htmlFor={id}>Auth code</FieldLabel>
            <OTPField id={id} length={6}>
              <div className={styles.group}>
                <OTPFieldInput />
                <OTPFieldInput aria-label="Character 2 of 6" />
                <OTPFieldInput aria-label="Character 3 of 6" />
              </div>
              <OTPFieldSeparator>
                <SeparatorMarkIcon />
              </OTPFieldSeparator>
              <div className={styles.group}>
                <OTPFieldInput aria-label="Character 4 of 6" />
                <OTPFieldInput aria-label="Character 5 of 6" />
                <OTPFieldInput aria-label="Character 6 of 6" />
              </div>
            </OTPField>
          </Field>
        );
      }
    `}

  </Preview.Code>
</Preview>

### Placeholder Hints

`OTPFieldInput` is a real input, so native `placeholder` props and CSS work as usual.

<Preview>
  <OTPFieldPlaceholderHintsExample />

  <Preview.Code>
    {`
      import {
        Field,
        FieldDescription,
        FieldLabel,
        OTPField,
        OTPFieldInput,
      } from "moduix";
      import { useId } from "react";
      import styles from "./otp-field.module.css";

      export function PlaceholderOTPField() {
        const id = useId();

        return (
          <Field>
            <FieldLabel htmlFor={id}>Verification code</FieldLabel>
            <FieldDescription>
              Placeholder hints stay visible until the active slot is focused.
            </FieldDescription>
            <OTPField id={id} length={6}>
              <OTPFieldInput className={styles.placeholderInput} placeholder="•" />
              <OTPFieldInput
                className={styles.placeholderInput}
                placeholder="•"
                aria-label="Character 2 of 6"
              />
              <OTPFieldInput
                className={styles.placeholderInput}
                placeholder="•"
                aria-label="Character 3 of 6"
              />
              <OTPFieldInput
                className={styles.placeholderInput}
                placeholder="•"
                aria-label="Character 4 of 6"
              />
              <OTPFieldInput
                className={styles.placeholderInput}
                placeholder="•"
                aria-label="Character 5 of 6"
              />
              <OTPFieldInput
                className={styles.placeholderInput}
                placeholder="•"
                aria-label="Character 6 of 6"
              />
            </OTPField>
          </Field>
        );
      }
    `}

  </Preview.Code>

  <Preview.CSS>
    {`
      .placeholderInput {
        &::placeholder {
          color: var(--color-muted-foreground);
        }

        &:focus::placeholder {
          color: transparent;
        }
      }
    `}

  </Preview.CSS>
</Preview>

### Styled Slots and Separator

<Preview>
  <OTPFieldCustomSeparatorExample />

  <Preview.Code>
    {`
      import {
        Field,
        FieldLabel,
        OTPField,
        OTPFieldInput,
        OTPFieldSeparator,
        SeparatorMarkIcon,
      } from "moduix";
      import { useId } from "react";
      import styles from "./otp-field.module.css";

      export function StyledOTPField() {
        const id = useId();

        return (
          <Field>
            <FieldLabel htmlFor={id}>Styled code</FieldLabel>
            <OTPField id={id} length={6} className={styles.customRoot}>
              <div className={styles.group}>
                <OTPFieldInput className={styles.customInput} />
                <OTPFieldInput className={styles.customInput} aria-label="Character 2 of 6" />
                <OTPFieldInput className={styles.customInput} aria-label="Character 3 of 6" />
              </div>
              <OTPFieldSeparator>
                <SeparatorMarkIcon />
              </OTPFieldSeparator>
              <div className={styles.group}>
                <OTPFieldInput className={styles.customInput} aria-label="Character 4 of 6" />
                <OTPFieldInput className={styles.customInput} aria-label="Character 5 of 6" />
                <OTPFieldInput className={styles.customInput} aria-label="Character 6 of 6" />
              </div>
            </OTPField>
          </Field>
        );
      }
    `}

  </Preview.Code>

  <Preview.CSS>
    {`
      .customInput {
        --otp-field-input-width: 3rem;
        --otp-field-input-height: 3rem;
        --otp-field-font-size: var(--text-xl);
        --otp-field-bg-filled: var(--color-muted);
      }

      .customRoot {
        --otp-field-separator-size: 1.5rem;
        --otp-field-separator-color: var(--color-primary);
      }

      .group {
        display: inline-flex;
        gap: inherit;
      }
    `}

  </Preview.CSS>
</Preview>