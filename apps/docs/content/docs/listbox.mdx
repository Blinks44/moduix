---
title: Listbox
description: Ark UI listbox composition for visible single, multiple, and extended selection.
icon: Component
---

import * as ListboxDocs from '@/components/examples/listbox';

## API Reference

<PrimitiveReference href="https://ark-ui.com/docs/components/listbox" />

## Basic

Use `Listbox` for a visible collection where users pick one or more items without opening a popup.
Pass a `collection`, render `Content`, and pass each item through `Listbox.Item item={item}`.

<Preview>
  <ListboxDocs.ListboxExample />

<Preview.Code>
  <include lang="tsx">./_snippets/listbox/basic.tsx#demo</include>
</Preview.Code>

  <Preview.CSS>
    {`
      [data-slot="listbox-content"] {
        max-height: var(--listbox-content-max-height);
      }
    `}

  </Preview.CSS>

</Preview>

## Install with shadcn

<ShadcnInstall packageName="react-listbox" />

## Anatomy

```text
Listbox / Listbox.Root
├─ Listbox.Label
├─ Listbox.Input
├─ Listbox.Content
│  ├─ Listbox.Empty
│  ├─ Listbox.ItemGroup
│  │  ├─ Listbox.ItemGroupLabel
│  │  └─ Listbox.Item[item]
│  │     ├─ Listbox.ItemText
│  │     └─ Listbox.ItemIndicator
│  └─ Listbox.Item[item]
│     ├─ Listbox.ItemText
│     └─ Listbox.ItemIndicator
└─ Listbox.ValueText
```

| Part                       | Role                                                          |
| -------------------------- | ------------------------------------------------------------- |
| `Listbox` / `Listbox.Root` | Owns collection, selected values, highlighted item, and mode. |
| `Listbox.RootProvider`     | Renders a listbox tree from state returned by `useListbox`.   |
| `Listbox.Label`            | Accessible label for the listbox content.                     |
| `Listbox.Input`            | Optional filter input connected to the same listbox state.    |
| `Listbox.Content`          | Focusable listbox surface with keyboard navigation.           |
| `Listbox.Empty`            | Empty-state content for filtered collections.                 |
| `Listbox.ItemGroup`        | Group wrapper for related collection items.                   |
| `Listbox.ItemGroupLabel`   | Label for a collection group.                                 |
| `Listbox.Item`             | Selectable row. Pass the collection item through `item`.      |
| `Listbox.ItemText`         | Text used for display and accessibility.                      |
| `Listbox.ItemIndicator`    | Selected-state indicator with the moduix default check icon.  |
| `Listbox.ValueText`        | Selected value summary.                                       |

## Composition

### Recommended composition

Render the Ark part tree directly: `Listbox` owns the collection, `Listbox.Content` owns focus and
keyboard navigation, and each `Listbox.Item` receives the original collection item through `item`.
Use `Listbox.ItemIndicator` when rows should reserve selected-state space.

### When to go custom

Use `asChild` only when a custom element can keep the required listbox semantics and receive Ark's
props. Use `ids` when listbox parts need stable IDs for composition with surrounding UI. Use moduix
`useListbox` with `Listbox.RootProvider` when state must be controlled outside the rendered tree;
do not render `Listbox.Root` for the same state instance.

### Default props

| Prop                      | Default    | Notes                                                                 |
| ------------------------- | ---------- | --------------------------------------------------------------------- |
| `collection`              | -          | Required Ark collection. Use `createListCollection` or grid variants. |
| `defaultValue`            | `[]`       | Initial selected values for uncontrolled state.                       |
| `defaultHighlightedValue` | -          | Initial highlighted item for uncontrolled highlight state.            |
| `deselectable`            | -          | Follows Ark's empty-selection behavior for deselecting values.        |
| `disabled`                | `false`    | Disables the whole listbox.                                           |
| `disallowSelectAll`       | `false`    | Disables `Cmd/Ctrl + A` selection.                                    |
| `loopFocus`               | `false`    | Keeps arrow navigation from wrapping by default.                      |
| `orientation`             | `vertical` | Controls layout and arrow-key direction.                              |
| `selectionMode`           | `single`   | Supports `single`, `multiple`, and `extended`.                        |
| `selectOnHighlight`       | `false`    | Selects items as keyboard highlight changes.                          |
| `typeahead`               | `true`     | Enables typeahead unless a filtering input owns text entry.           |

### Behavioral notes

`value`, `defaultValue`, and `onValueChange(details)` use string arrays. Read `details.value` for
selected keys and `details.items` for selected collection items.

`highlightedValue`, `defaultHighlightedValue`, and `onHighlightChange(details)` control active
descendant movement. `onSelect(details)` fires for item selection with the selected value.

`Listbox.Input` is only needed for filtering. Set `typeahead={false}` when text input owns search
so typeahead and filtering do not compete.

Ark owns the ARIA listbox pattern, active-descendant focus management, keyboard navigation,
typeahead, Home/End behavior, disabled item handling, and orientation-specific arrow keys. Keep
`Listbox.Label` in the tree when the listbox needs a visible label.

## Examples

### Controlled

Control the selected string array from React state and update it from `details.value`.

<Preview>
  <ListboxDocs.ControlledListboxExample />

  <Preview.Code>
  <include lang="tsx">./_snippets/listbox/controlled.tsx#demo</include>
</Preview.Code>
  <Preview.CSS>{`[data-slot="listbox-item"][data-selected] { color: var(--listbox-item-selected-color); }`}</Preview.CSS>
  
</Preview>

### Disabled Item

Disable individual items through the collection item `disabled` field or `isItemDisabled`.

<Preview>
  <ListboxDocs.DisabledItemListboxExample />

  <Preview.Code>
  <include lang="tsx">./_snippets/listbox/disabled-item.tsx#demo</include>
</Preview.Code>
  <Preview.CSS>{`[data-slot="listbox-item"][data-disabled] { opacity: var(--listbox-disabled-opacity); }`}</Preview.CSS>
  
</Preview>

### Extended Selection

Use `selectionMode="extended"` when users should select multiple items with platform modifier keys.

<Preview>
  <ListboxDocs.ExtendedSelectListboxExample />

  <Preview.Code>
  <include lang="tsx">./_snippets/listbox/extended-selection.tsx#demo</include>
</Preview.Code>
  <Preview.CSS>{`[data-slot="listbox-item"][data-highlighted] { background: var(--listbox-highlight-bg); }`}</Preview.CSS>
  
</Preview>

### Filtering

Use `Listbox.Input` with `useListCollection`. Turn off typeahead when text input owns search.

<Preview>
  <ListboxDocs.FilteringListboxExample />

  <Preview.Code>
  <include lang="tsx">./_snippets/listbox/filtering.tsx#demo</include>
</Preview.Code>
  <Preview.CSS>{`[data-slot="listbox-root"] { gap: var(--listbox-root-gap); }`}</Preview.CSS>
  
</Preview>

### Grid

Pass a grid collection to get grid keyboard navigation. Ark exposes `data-layout="grid"` and the
`--column-count` CSS variable on `Content`. Grid examples use selected background instead of an
inline check indicator to keep cells compact.

<Preview>
  <ListboxDocs.GridListboxExample />

  <Preview.Code>
  <include lang="tsx">./_snippets/listbox/grid.tsx#demo</include>
</Preview.Code>
  <Preview.CSS>{`[data-slot="listbox-content"][data-layout="grid"] { grid-template-columns: repeat(var(--column-count), minmax(0, 1fr)); }
[data-slot="listbox-content"][data-layout="grid"] [data-slot="listbox-item"] { justify-items: center; text-align: center; }
[data-slot="listbox-content"][data-layout="grid"] [data-selected] { background: var(--listbox-grid-selected-bg); }`}</Preview.CSS>
  
</Preview>

### Grouping

Use `groupBy` on the collection and render `ItemGroup` plus `ItemGroupLabel`.

<Preview>
  <ListboxDocs.GroupedListboxExample />

  <Preview.Code>
  <include lang="tsx">./_snippets/listbox/grouping.tsx#demo</include>
</Preview.Code>
  <Preview.CSS>{`[data-slot="listbox-item-group-label"] { color: var(--listbox-item-group-label-color); }`}</Preview.CSS>
  
</Preview>

### Horizontal

Set `orientation="horizontal"` for horizontal layout and left/right keyboard navigation.

<Preview>
  <ListboxDocs.HorizontalListboxExample />

  <Preview.Code>
  <include lang="tsx">./_snippets/listbox/horizontal.tsx#demo</include>
</Preview.Code>
  <Preview.CSS>
    {`
      .horizontalRoot {
        --listbox-width: min(100%, 34rem);
        --listbox-horizontal-content-max-height: none;
        --listbox-horizontal-item-width: 10rem;
      }

      .albumText {
        flex-direction: column;
        align-items: flex-start;
        gap: var(--spacing-1);
      }
    `}

  </Preview.CSS>
  
</Preview>

### Multiple

Set `selectionMode="multiple"` to toggle multiple values without modifier keys.

<Preview>
  <ListboxDocs.MultipleListboxExample />

  <Preview.Code>
  <include lang="tsx">./_snippets/listbox/multiple.tsx#demo</include>
</Preview.Code>
  <Preview.CSS>{`[data-slot="listbox-value-text"] { color: var(--listbox-value-text-color); }`}</Preview.CSS>
  
</Preview>

### Root Provider

Use moduix `useListbox` with `Listbox.RootProvider` when external controls need direct access to the
listbox API.

<Preview>
  <ListboxDocs.RootProviderListboxExample />

  <Preview.Code>
  <include lang="tsx">./_snippets/listbox/root-provider.tsx#demo</include>
</Preview.Code>
  <Preview.CSS>{`[data-slot="listbox-root"] { gap: var(--listbox-root-gap); }`}</Preview.CSS>
  
</Preview>

### Select All

Use moduix `useListboxContext` inside a child component to implement select-all controls.

<Preview>
  <ListboxDocs.SelectAllListboxExample />

  <Preview.Code>
  <include lang="tsx">./_snippets/listbox/select-all.tsx#demo</include>
</Preview.Code>
  <Preview.CSS>{`[data-slot="listbox-root"] { width: var(--listbox-width); }`}</Preview.CSS>
  
</Preview>

### Value Text

Render `ValueText` when the selected value summary should stay visible near the list.

<Preview>
  <ListboxDocs.ValueTextListboxExample />

  <Preview.Code>
  <include lang="tsx">./_snippets/listbox/value-text.tsx#demo</include>
</Preview.Code>
  <Preview.CSS>{`[data-slot="listbox-value-text"] { font-size: var(--listbox-value-text-font-size); }`}</Preview.CSS>
  
</Preview>

## Styling

Style `Listbox` through `className`, stable `data-slot="listbox-*"` hooks, Ark
`data-scope="listbox"` / `data-part`, and state attributes.

### CSS Properties

<div className="not-prose">
  <Tabs items={['CSS Variables']} className="mb-0 mt-3">
    <Tab className="max-h-150 overflow-auto">
      <ListboxDocs.ListboxCssPropertiesPanel />
    </Tab>
  </Tabs>
</div>

### Styling Hooks

- Ark anatomy hooks: `data-scope="listbox"` and `data-part` on root, label, input, content,
  item group, item group label, item, item text, item indicator, value text, and empty.
- State hooks: `data-orientation`, `data-disabled`, `data-empty`, `data-layout`,
  `data-activedescendant`, `data-value`, `data-selected`, `data-state`, and `data-highlighted`.
- Moduix hooks: `data-slot="listbox-root"`, `listbox-label`, `listbox-input`,
  `listbox-content`, `listbox-empty`, `listbox-item-group`, `listbox-item-group-label`,
  `listbox-item`, `listbox-item-text`, `listbox-item-indicator`, and `listbox-value-text`.