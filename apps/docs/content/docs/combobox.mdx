---
title: Combobox
description: A filterable input for selecting one or more values from an Ark UI collection.
icon: Component
---

import {
  AsyncSearchComboboxExample,
  AutoHighlightComboboxExample,
  ComboboxCssPropertiesPanel,
  ComboboxExample,
  ContextComboboxExample,
  ControlledComboboxExample,
  CreatableComboboxExample,
  DynamicComboboxExample,
  GroupedComboboxExample,
  InlineAutocompleteComboboxExample,
  LimitComboboxExample,
  LinksComboboxExample,
  MultipleComboboxExample,
  RehydrateValueComboboxExample,
  RootProviderComboboxExample,
  VirtualizedComboboxExample,
  comboboxExampleCss,
} from '@/components/examples/combobox';

## API Reference

<PrimitiveReference href="https://ark-ui.com/docs/components/combobox" />

## Choosing the right component

Use `Combobox` when users select from known values and typing helps filter the available options.
Use `Select` when the same predefined choice does not need an editable input.

## Basic

Create an Ark `ListCollection`, pass it to `Combobox.Root`, and keep the popup structure explicit.
`onInputValueChange` receives an Ark details object.

<Preview>
  <ComboboxExample />

  <Preview.Code>
    {`
      import { Combobox, Portal, useFilter, useListCollection } from "moduix";

      export function ComboboxDemo() {
        const { contains } = useFilter({ sensitivity: "base" });
        const { collection, filter } = useListCollection({
          initialItems: fruits,
          filter: contains,
        });

        return (
          <Combobox
            collection={collection}
            onInputValueChange={(details) => filter(details.inputValue)}
          >
            <Combobox.Label>Choose fruit</Combobox.Label>
            <Combobox.Control>
              <Combobox.Input placeholder="e.g. Mango" />
              <Combobox.ClearTrigger aria-label="Clear selection" />
              <Combobox.Trigger aria-label="Open options" />
            </Combobox.Control>
            <Portal>
              <Combobox.Positioner>
                <Combobox.Content>
                  <Combobox.Empty>No fruits found.</Combobox.Empty>
                  <Combobox.List>
                    {collection.items.map((item) => (
                      <Combobox.Item key={item.value} item={item}>
                        <Combobox.ItemText>{item.label}</Combobox.ItemText>
                        <Combobox.ItemIndicator />
                      </Combobox.Item>
                    ))}
                  </Combobox.List>
                </Combobox.Content>
              </Combobox.Positioner>
            </Portal>
          </Combobox>
        );
      }
    `}

  </Preview.Code>

<Preview.CSS>{comboboxExampleCss}</Preview.CSS>

  <Preview.Data>
    {`
      const fruits = [
        { label: "Apple", value: "apple" },
        { label: "Banana", value: "banana" },
        { label: "Mango", value: "mango" },
      ];
    `}

  </Preview.Data>
</Preview>

## Install with shadcn (optional)

Install the registry source when the component should live in your application:

<Tabs items={['npm', 'pnpm', 'bun', 'deno', 'aube']}>
  <Tab value="npm">

```bash
npx shadcn@latest add Blinks44/moduix/combobox
```

  </Tab>
  <Tab value="pnpm">

```bash
pnpm dlx shadcn@latest add Blinks44/moduix/combobox
```

  </Tab>
  <Tab value="bun">

```bash
bunx shadcn@latest add Blinks44/moduix/combobox
```

  </Tab>
  <Tab value="deno">

```bash
deno x -A npm:shadcn@latest add Blinks44/moduix/combobox
```

  </Tab>
  <Tab value="aube">

```bash
aubx shadcn@latest add Blinks44/moduix/combobox
```

  </Tab>
</Tabs>

## Anatomy

```text
Combobox.Root[collection]
├─ Combobox.Label
├─ Combobox.Control
│  ├─ Combobox.Input
│  ├─ Combobox.ClearTrigger
│  └─ Combobox.Trigger
└─ Portal
   └─ Combobox.Positioner
      └─ Combobox.Content
         ├─ Combobox.Empty
         ├─ Combobox.List
         │  └─ Combobox.Item[item]
         │     ├─ Combobox.ItemText
         │     └─ Combobox.ItemIndicator
         └─ Combobox.ItemGroup
            ├─ Combobox.ItemGroupLabel
            └─ Combobox.Item
```

| Part                         | Role                                                               |
| ---------------------------- | ------------------------------------------------------------------ |
| `Combobox` / `Combobox.Root` | Owns combobox state, filtering, highlighting, and selected values. |
| `Combobox.Control`           | Wrapper for input and trigger controls.                            |
| `Combobox.Input`             | Text input that drives search and selection.                       |
| `Combobox.Trigger`           | Opens and closes the popup list.                                   |
| `Combobox.ClearTrigger`      | Clears current value and/or input.                                 |
| `Combobox.Positioner`        | Floating positioner for popup content.                             |
| `Combobox.Content`           | Popup content container.                                           |
| `Combobox.List`              | Option list wrapper.                                               |
| `Combobox.Item`              | Selectable option row.                                             |
| `Combobox.ItemText`          | Text label slot inside an item.                                    |
| `Combobox.ItemIndicator`     | Selected/highlight indicator slot for an item.                     |

`Combobox.RootProvider`, `Combobox.Context`, and `Combobox.ItemContext` expose the corresponding
Ark provider and context paths.

## Composition

### Recommended composition

Use `createListCollection` for a static collection or `useListCollection` when filtering changes
the rendered items. Object collections should define `itemToString` and `itemToValue`.

Keep `Portal`, `Combobox.Positioner`, and `Combobox.Content` visible. `Content` is the actual Ark
content part; it does not create a portal or positioner.

### When to go custom

Use `Combobox.Context` when adjacent UI needs `selectedItems`, `valueAsString`, focus methods, or
other root state. Use `useCombobox` with `Combobox.RootProvider` when state must be created outside
the rendered subtree.

Multiple selection does not add a combobox-specific chip API. Render tags or another value summary
from `Combobox.Context`, then keep selection behavior on `Combobox.Root multiple`.

Use ordinary elements inside `Combobox.Content` for loading and error status. Ark does not expose a
combobox `Status` part.

### Default props

| Surface                            | Default               | Notes                                                                 |
| ---------------------------------- | --------------------- | --------------------------------------------------------------------- |
| `Combobox.Trigger`                 | moduix chevron icon   | Used when children are omitted.                                       |
| `Combobox.ClearTrigger`            | moduix close icon     | Used when children are omitted.                                       |
| `Combobox.ItemIndicator`           | moduix check icon     | Used when children are omitted.                                       |
| `Combobox` / `Combobox.Root` width | `16rem` (max `100%`)  | Styled root width defaults.                                           |
| `Combobox.Positioner` variables    | Ark runtime variables | Uses `--reference-width`, `--available-height`, `--transform-origin`. |

### Behavioral notes

- `collection` is required.
- `value` and `defaultValue` are string arrays, including single selection.
- `onValueChange(details)` reads the current values from `details.value`.
- `onInputValueChange(details)` reads input text from `details.inputValue` and its source from
  `details.reason`.
- `onOpenChange`, `onHighlightChange`, and `onSelect` keep their Ark detail shapes.
- All Ark DOM parts preserve `asChild`.
- `useComboboxContext` and `useComboboxItemContext` are exported from `moduix` alongside
  `Combobox.Context` and `Combobox.ItemContext`.
- Ark `Field.Root` state inheritance remains available when composing with `@ark-ui/react/field`.
  The current moduix `Field` component has not migrated to the Ark field contract yet and does not provide Ark field
  context.

## Examples

### Auto Highlight

Set `inputBehavior="autohighlight"` to highlight the first matching item while the user types, so
Enter can select it without an extra arrow-key press.

<Preview>
  <AutoHighlightComboboxExample />

  <Preview.Code>
    {`
      import { Combobox, Portal, useFilter, useListCollection } from "moduix";

      export function AutoHighlightComboboxDemo() {
        const { contains } = useFilter({ sensitivity: "base" });
        const { collection, filter } = useListCollection({
          initialItems: departments,
          filter: contains,
        });

        return (
          <Combobox
            collection={collection}
            inputBehavior="autohighlight"
            onInputValueChange={(details) => filter(details.inputValue)}
          >
            <Combobox.Label>Department</Combobox.Label>
            <Combobox.Control>
              <Combobox.Input placeholder="e.g. Engineering" />
              <Combobox.ClearTrigger aria-label="Clear selection" />
              <Combobox.Trigger aria-label="Open options" />
            </Combobox.Control>
            <Portal>
              <Combobox.Positioner>
                <Combobox.Content>
                  <Combobox.Empty>No departments found.</Combobox.Empty>
                  <Combobox.List>
                    {collection.items.map((item) => (
                      <Combobox.Item key={item.value} item={item}>
                        <Combobox.ItemText>{item.label}</Combobox.ItemText>
                        <Combobox.ItemIndicator />
                      </Combobox.Item>
                    ))}
                  </Combobox.List>
                </Combobox.Content>
              </Combobox.Positioner>
            </Portal>
          </Combobox>
        );
      }
    `}

  </Preview.Code>

<Preview.CSS>{comboboxExampleCss}</Preview.CSS>

  <Preview.Data>
    {`
      const departments = [
        { label: "Engineering", value: "engineering" },
        { label: "Marketing", value: "marketing" },
        { label: "Sales", value: "sales" },
        { label: "Finance", value: "finance" },
        { label: "Human Resources", value: "human-resources" },
        { label: "Operations", value: "operations" },
      ];
    `}

  </Preview.Data>
</Preview>

### Inline Autocomplete

Set `inputBehavior="autocomplete"` and filter with `startsWith` to complete the input from the
first matching item while preserving Ark keyboard navigation.

<Preview>
  <InlineAutocompleteComboboxExample />

  <Preview.Code>
    {`
      import { Combobox, Portal, useFilter, useListCollection } from "moduix";

      export function InlineAutocompleteComboboxDemo() {
        const { startsWith } = useFilter({ sensitivity: "base" });
        const { collection, filter } = useListCollection({
          initialItems: seaCreatures,
          filter: startsWith,
        });

        return (
          <Combobox
            collection={collection}
            inputBehavior="autocomplete"
            onInputValueChange={(details) => filter(details.inputValue)}
          >
            <Combobox.Label>Sea creature</Combobox.Label>
            <Combobox.Control>
              <Combobox.Input placeholder="e.g. Dolphin" />
              <Combobox.ClearTrigger aria-label="Clear selection" />
              <Combobox.Trigger aria-label="Open options" />
            </Combobox.Control>
            <Portal>
              <Combobox.Positioner>
                <Combobox.Content>
                  <Combobox.Empty>No creatures found.</Combobox.Empty>
                  <Combobox.List>
                    {collection.items.map((item) => (
                      <Combobox.Item key={item.value} item={item}>
                        <Combobox.ItemText>{item.label}</Combobox.ItemText>
                        <Combobox.ItemIndicator />
                      </Combobox.Item>
                    ))}
                  </Combobox.List>
                </Combobox.Content>
              </Combobox.Positioner>
            </Portal>
          </Combobox>
        );
      }
    `}

  </Preview.Code>

<Preview.CSS>{comboboxExampleCss}</Preview.CSS>

  <Preview.Data>
    {`
      const seaCreatures = [
        { label: "Whale", value: "whale" },
        { label: "Dolphin", value: "dolphin" },
        { label: "Shark", value: "shark" },
        { label: "Octopus", value: "octopus" },
        { label: "Jellyfish", value: "jellyfish" },
        { label: "Seahorse", value: "seahorse" },
      ];
    `}

  </Preview.Data>
</Preview>

### Links

Use `asChild` on `Combobox.Item` to preserve combobox keyboard behavior while rendering each result
as a native link. `selectionBehavior="preserve"` keeps the search text visible during navigation.

<Preview>
  <LinksComboboxExample />

  <Preview.Code>
    {`
      import { Combobox, Portal, useFilter, useListCollection } from "moduix";

      export function LinksComboboxDemo() {
        const { contains } = useFilter({ sensitivity: "base" });
        const { collection, filter } = useListCollection({
          initialItems: developerResources,
          filter: contains,
        });

        return (
          <Combobox
            collection={collection}
            selectionBehavior="preserve"
            onInputValueChange={(details) => filter(details.inputValue)}
          >
            <Combobox.Label>Developer resources</Combobox.Label>
            <Combobox.Control>
              <Combobox.Input placeholder="e.g. GitHub" />
              <Combobox.Trigger aria-label="Open options" />
            </Combobox.Control>
            <Portal>
              <Combobox.Positioner>
                <Combobox.Content>
                  <Combobox.List>
                    {collection.items.map((item) => (
                      <Combobox.Item key={item.value} item={item} asChild>
                        <a href={item.href} target="_blank" rel="noreferrer">
                          <Combobox.ItemText>{item.label}</Combobox.ItemText>
                          <Combobox.ItemIndicator />
                        </a>
                      </Combobox.Item>
                    ))}
                  </Combobox.List>
                </Combobox.Content>
              </Combobox.Positioner>
            </Portal>
          </Combobox>
        );
      }
    `}

  </Preview.Code>

<Preview.CSS>{comboboxExampleCss}</Preview.CSS>

  <Preview.Data>
    {`
      const developerResources = [
        { label: "GitHub", href: "https://github.com", value: "github" },
        {
          label: "Stack Overflow",
          href: "https://stackoverflow.com",
          value: "stack-overflow",
        },
        {
          label: "MDN Web Docs",
          href: "https://developer.mozilla.org",
          value: "mdn",
        },
        { label: "npm", href: "https://www.npmjs.com", value: "npm" },
        {
          label: "TypeScript",
          href: "https://www.typescriptlang.org",
          value: "typescript",
        },
      ];
    `}

  </Preview.Data>
</Preview>

### Dynamic

Build the collection from the current input when suggestions do not exist until the user starts
typing. Check `details.reason` to avoid rebuilding it after selection-driven input changes.

<Preview>
  <DynamicComboboxExample />

  <Preview.Code>
    {`
      import { Combobox, Portal, useListCollection } from "moduix";

      export function DynamicComboboxDemo() {
        const { collection, set } = useListCollection({
          initialItems: [] as string[],
        });

        return (
          <Combobox
            collection={collection}
            onInputValueChange={(details) => {
              if (details.reason !== "input-change") {
                return;
              }

              const name = details.inputValue.trim();
              set(
                name
                  ? domains.map((domain) => \`\${name}@\${domain}\`)
                  : [],
              );
            }}
          >
            <Combobox.Label>Email</Combobox.Label>
            <Combobox.Control>
              <Combobox.Input placeholder="e.g. alex" />
              <Combobox.Trigger aria-label="Open options" />
            </Combobox.Control>
            <Portal>
              <Combobox.Positioner>
                <Combobox.Content>
                  <Combobox.List>
                    {collection.items.map((item) => (
                      <Combobox.Item key={item} item={item}>
                        <Combobox.ItemText>{item}</Combobox.ItemText>
                        <Combobox.ItemIndicator />
                      </Combobox.Item>
                    ))}
                  </Combobox.List>
                </Combobox.Content>
              </Combobox.Positioner>
            </Portal>
          </Combobox>
        );
      }
    `}

  </Preview.Code>

<Preview.CSS>{comboboxExampleCss}</Preview.CSS>

  <Preview.Data>
    {`
      const domains = ["gmail.com", "outlook.com", "proton.me"];
    `}

  </Preview.Data>
</Preview>

### Creatable

Combine `allowCustomValue` with collection mutation to expose a temporary “Create” item, then
replace that sentinel value with the user-entered value after selection.

<Preview>
  <CreatableComboboxExample />

  <Preview.Code>
    {`
      import { Combobox, Portal, useFilter, useListCollection } from "moduix";
      import { useState } from "react";
      import { flushSync } from "react-dom";

      const createOptionValue = "__create-option__";

      export function CreatableComboboxDemo() {
        const { contains } = useFilter({ sensitivity: "base" });
        const { collection, filter, remove, update, upsert } =
          useListCollection({
            initialItems: issueLabels,
            filter: contains,
          });
        const [inputValue, setInputValue] = useState("");
        const [value, setValue] = useState([] as string[]);

        return (
          <Combobox
            collection={collection}
            inputValue={inputValue}
            value={value}
            allowCustomValue
            onInputValueChange={(details) => {
              const nextInputValue = details.inputValue;

              if (
                details.reason === "input-change" ||
                details.reason === "item-select"
              ) {
                const hasExactMatch = collection.items.some(
                  (item) =>
                    item.label.toLowerCase() === nextInputValue.toLowerCase(),
                );

                flushSync(() => {
                  if (nextInputValue.trim() && !hasExactMatch) {
                    upsert(createOptionValue, {
                      label: nextInputValue,
                      value: createOptionValue,
                    });
                  } else {
                    remove(createOptionValue);
                  }
                });
                filter(nextInputValue);
              }

              setInputValue(nextInputValue);
            }}
            onOpenChange={(details) => {
              if (details.reason === "trigger-click") {
                filter("");
              }
            }}
            onValueChange={(details) => {
              setValue(
                details.value.map((item) =>
                  item === createOptionValue ? inputValue : item,
                ),
              );

              if (details.value.includes(createOptionValue)) {
                update(createOptionValue, {
                  label: inputValue,
                  value: inputValue,
                  created: true,
                });
              }
            }}
          >
            <Combobox.Label>Issue label</Combobox.Label>
            <Combobox.Control>
              <Combobox.Input placeholder="e.g. Accessibility" />
              <Combobox.ClearTrigger aria-label="Clear selection" />
              <Combobox.Trigger aria-label="Open options" />
            </Combobox.Control>
            <Portal>
              <Combobox.Positioner>
                <Combobox.Content>
                  <Combobox.List>
                    {collection.items.map((item) => (
                      <Combobox.Item key={item.value} item={item}>
                        <Combobox.ItemText>
                          {item.value === createOptionValue
                            ? \`Create "\${item.label}"\`
                            : \`\${item.label}\${item.created ? " (new)" : ""}\`}
                        </Combobox.ItemText>
                        <Combobox.ItemIndicator />
                      </Combobox.Item>
                    ))}
                  </Combobox.List>
                </Combobox.Content>
              </Combobox.Positioner>
            </Portal>
          </Combobox>
        );
      }
    `}

  </Preview.Code>

<Preview.CSS>{comboboxExampleCss}</Preview.CSS>

  <Preview.Data>
    {`
      const issueLabels = [
        { label: "Bug", value: "bug" },
        { label: "Feature", value: "feature" },
        { label: "Enhancement", value: "enhancement" },
        { label: "Documentation", value: "documentation" },
      ] as Array<{
        label: string;
        value: string;
        created?: boolean;
      }>;
    `}

  </Preview.Data>
</Preview>

### Limit Results

Set `limit` on `useListCollection` when the full matching set is unnecessary or would make the
popup harder to scan. Filtering still runs against the complete initial collection.

<Preview>
  <LimitComboboxExample />

  <Preview.Code>
    {`
      import { Combobox, Portal, useFilter, useListCollection } from "moduix";

      export function LimitComboboxDemo() {
        const { contains } = useFilter({ sensitivity: "base" });
        const { collection, filter } = useListCollection({
          initialItems: cities,
          filter: contains,
          limit: 5,
        });

        return (
          <Combobox
            collection={collection}
            onInputValueChange={(details) => filter(details.inputValue)}
          >
            <Combobox.Label>City</Combobox.Label>
            <Combobox.Control>
              <Combobox.Input placeholder="e.g. San" />
              <Combobox.Trigger aria-label="Open options" />
            </Combobox.Control>
            <Portal>
              <Combobox.Positioner>
                <Combobox.Content>
                  <Combobox.Empty>No cities found.</Combobox.Empty>
                  <Combobox.List>
                    {collection.items.map((item) => (
                      <Combobox.Item key={item.value} item={item}>
                        <Combobox.ItemText>{item.label}</Combobox.ItemText>
                        <Combobox.ItemIndicator />
                      </Combobox.Item>
                    ))}
                  </Combobox.List>
                </Combobox.Content>
              </Combobox.Positioner>
            </Portal>
          </Combobox>
        );
      }
    `}

  </Preview.Code>

<Preview.CSS>{comboboxExampleCss}</Preview.CSS>

  <Preview.Data>
    {`
      const cities = [
        "New York",
        "Los Angeles",
        "Chicago",
        "Houston",
        "Phoenix",
        "Philadelphia",
        "San Antonio",
        "San Diego",
        "Dallas",
        "San Jose",
        "Austin",
        "Jacksonville",
      ].map((label) => ({
        label,
        value: label.toLowerCase().replaceAll(" ", "-"),
      }));
    `}

  </Preview.Data>
</Preview>

### Virtualized

For large collections, render only visible rows with TanStack Virtual and connect Ark keyboard
navigation through `scrollToIndexFn`. Keep `aria-setsize` and `aria-posinset` on virtual rows.

<Preview>
  <VirtualizedComboboxExample />

  <Preview.Code>
    {`
      import { useVirtualizer } from "@tanstack/react-virtual";
      import { Combobox, Portal, useFilter, useListCollection } from "moduix";
      import { useRef } from "react";

      export function VirtualizedComboboxDemo() {
        const scrollRef = useRef(null as HTMLDivElement | null);
        const { contains } = useFilter({ sensitivity: "base" });
        const { collection, filter, reset } = useListCollection({
          initialItems: results,
          filter: contains,
        });
        const virtualizer = useVirtualizer({
          count: collection.size,
          getScrollElement: () => scrollRef.current,
          estimateSize: () => 32,
          overscan: 8,
        });

        return (
          <Combobox
            collection={collection}
            onInputValueChange={(details) => filter(details.inputValue)}
            scrollToIndexFn={(details) => {
              virtualizer.scrollToIndex(details.index, { align: "center" });
            }}
          >
            <Combobox.Label>Large dataset</Combobox.Label>
            <Combobox.Control>
              <Combobox.Input placeholder="Search 1,000 results" />
              <Combobox.Trigger
                aria-label="Open options"
                onClick={reset}
              />
            </Combobox.Control>
            <Portal>
              <Combobox.Positioner>
                <Combobox.Content className="virtualContent">
                  <Combobox.Empty>No results found.</Combobox.Empty>
                  <div ref={scrollRef} className="virtualScroller">
                    <Combobox.List
                      className="virtualList"
                      style={{ height: virtualizer.getTotalSize() }}
                    >
                      {virtualizer.getVirtualItems().map((virtualItem) => {
                        const item = collection.items[virtualItem.index];

                        return (
                          <Combobox.Item
                            key={item.value}
                            item={item}
                            aria-setsize={collection.size}
                            aria-posinset={virtualItem.index + 1}
                            className="virtualItem"
                            style={{
                              height: virtualItem.size,
                              transform: \`translateY(\${virtualItem.start}px)\`,
                            }}
                          >
                            <Combobox.ItemText>{item.label}</Combobox.ItemText>
                            <Combobox.ItemIndicator />
                          </Combobox.Item>
                        );
                      })}
                    </Combobox.List>
                  </div>
                </Combobox.Content>
              </Combobox.Positioner>
            </Portal>
          </Combobox>
        );
      }
    `}

  </Preview.Code>

  <Preview.CSS>
    {`
      .virtualContent {
        overflow: hidden;
      }

      .virtualScroller {
        height: min(16rem, var(--available-height, 16rem));
        overflow: auto;
      }

      .virtualList {
        position: relative;
      }

      .virtualItem {
        position: absolute;
        inset: 0 0 auto;
      }
    `}

  </Preview.CSS>

  <Preview.Data>
    {`
      const results = Array.from({ length: 1000 }, (_, index) => ({
        label: \`Result \${String(index + 1).padStart(4, "0")}\`,
        value: \`result-\${index + 1}\`,
      }));
    `}

  </Preview.Data>
</Preview>

### Controlled

Control the selected string array and read the next value from `details.value`.

<Preview>
  <ControlledComboboxExample />

  <Preview.Code>
    {`
      import { Combobox, Portal, useFilter, useListCollection } from "moduix";
      import { useState } from "react";

      export function ControlledComboboxDemo() {
        const { contains } = useFilter({ sensitivity: "base" });
        const { collection, filter } = useListCollection({
          initialItems: fruits,
          filter: contains,
        });
        const [value, setValue] = useState(["mango"]);

        return (
          <div>
            <Combobox
              collection={collection}
              value={value}
              onInputValueChange={(details) => filter(details.inputValue)}
              onValueChange={(details) => setValue(details.value)}
            >
              <Combobox.Label>Choose fruit</Combobox.Label>
              <Combobox.Control>
                <Combobox.Input />
                <Combobox.ClearTrigger aria-label="Clear selection" />
                <Combobox.Trigger aria-label="Open options" />
              </Combobox.Control>
              <Portal>
                <Combobox.Positioner>
                  <Combobox.Content>
                    <Combobox.Empty>No fruits found.</Combobox.Empty>
                    <Combobox.List>
                      {collection.items.map((item) => (
                        <Combobox.Item key={item.value} item={item}>
                          <Combobox.ItemText>{item.label}</Combobox.ItemText>
                          <Combobox.ItemIndicator />
                        </Combobox.Item>
                      ))}
                    </Combobox.List>
                  </Combobox.Content>
                </Combobox.Positioner>
              </Portal>
            </Combobox>
            <span>Selected: {value[0] ?? "none"}</span>
          </div>
        );
      }
    `}

  </Preview.Code>

<Preview.CSS>{comboboxExampleCss}</Preview.CSS>

  <Preview.Data>
    {`
      const fruits = [
        { label: "Apple", value: "apple" },
        { label: "Banana", value: "banana" },
        { label: "Mango", value: "mango" },
        { label: "Orange", value: "orange" },
      ];
    `}

  </Preview.Data>
</Preview>

### Custom Objects and Grouping

Map object labels and values on the collection, then render Ark item groups from
`collection.group()`.

<Preview>
  <GroupedComboboxExample />

  <Preview.Code>
    {`
      import { Combobox, Portal, useFilter, useListCollection } from "moduix";

      export function GroupedComboboxDemo() {
        const { contains } = useFilter({ sensitivity: "base" });
        const { collection, filter } = useListCollection({
          initialItems: countries,
          itemToString: (item) => item.country,
          itemToValue: (item) => item.code,
          filter: contains,
          groupBy: (item) => item.continent,
        });

        return (
          <Combobox
            collection={collection}
            onInputValueChange={(details) => filter(details.inputValue)}
          >
            <Combobox.Label>Country</Combobox.Label>
            <Combobox.Control>
              <Combobox.Input placeholder="e.g. Canada" />
              <Combobox.ClearTrigger aria-label="Clear selection" />
              <Combobox.Trigger aria-label="Open options" />
            </Combobox.Control>
            <Portal>
              <Combobox.Positioner>
                <Combobox.Content>
                  <Combobox.Empty>No countries found.</Combobox.Empty>
                  {collection.group().map(([continent, items]) => (
                    <Combobox.ItemGroup key={continent}>
                      <Combobox.ItemGroupLabel>
                        {continent}
                      </Combobox.ItemGroupLabel>
                      {items.map((item) => (
                        <Combobox.Item key={item.code} item={item}>
                          <Combobox.ItemText>
                            {item.country}
                          </Combobox.ItemText>
                          <Combobox.ItemIndicator />
                        </Combobox.Item>
                      ))}
                    </Combobox.ItemGroup>
                  ))}
                </Combobox.Content>
              </Combobox.Positioner>
            </Portal>
          </Combobox>
        );
      }
    `}

  </Preview.Code>

<Preview.CSS>{comboboxExampleCss}</Preview.CSS>

  <Preview.Data>
    {`
      const countries = [
        { country: "Canada", code: "CA", continent: "North America" },
        {
          country: "United States",
          code: "US",
          continent: "North America",
        },
        { country: "Germany", code: "DE", continent: "Europe" },
        { country: "France", code: "FR", continent: "Europe" },
        { country: "Japan", code: "JP", continent: "Asia" },
        { country: "South Korea", code: "KR", continent: "Asia" },
      ];
    `}

  </Preview.Data>
</Preview>

### Context

Use `Combobox.Context` for inline access to selected values and root methods without creating a
separate state owner.

<Preview>
  <ContextComboboxExample />

  <Preview.Code>
    {`
      import type { UseComboboxContext } from "moduix";
      import { Combobox, Portal, useFilter, useListCollection } from "moduix";

      export function ContextComboboxDemo() {
        const { contains } = useFilter({ sensitivity: "base" });
        const { collection, filter } = useListCollection({
          initialItems: sizes,
          filter: contains,
        });

        return (
          <Combobox
            collection={collection}
            onInputValueChange={(details) => filter(details.inputValue)}
          >
            <Combobox.Context>
              {(context: UseComboboxContext<(typeof sizes)[number]>) => (
                <span>Selected: {context.valueAsString || "none"}</span>
              )}
            </Combobox.Context>
            <Combobox.Label>Size</Combobox.Label>
            <Combobox.Control>
              <Combobox.Input placeholder="e.g. Medium" />
              <Combobox.ClearTrigger aria-label="Clear selection" />
              <Combobox.Trigger aria-label="Open options" />
            </Combobox.Control>
            <Portal>
              <Combobox.Positioner>
                <Combobox.Content>
                  <Combobox.List>
                    {collection.items.map((item) => (
                      <Combobox.Item key={item.value} item={item}>
                        <Combobox.ItemText>{item.label}</Combobox.ItemText>
                        <Combobox.ItemIndicator />
                      </Combobox.Item>
                    ))}
                  </Combobox.List>
                </Combobox.Content>
              </Combobox.Positioner>
            </Portal>
          </Combobox>
        );
      }
    `}

  </Preview.Code>

<Preview.CSS>{comboboxExampleCss}</Preview.CSS>

  <Preview.Data>
    {`
      const sizes = [
        { label: "Small", value: "sm" },
        { label: "Medium", value: "md" },
        { label: "Large", value: "lg" },
        { label: "Extra Large", value: "xl" },
      ];
    `}

  </Preview.Data>
</Preview>

### Multiple

Enable `multiple` and render the selected objects from `Combobox.Context`. Tags are consumer
composition rather than combobox parts.

<Preview>
  <MultipleComboboxExample />

  <Preview.Code>
    {`
      import type { UseComboboxContext } from "moduix";
      import { Combobox, Portal, useFilter, useListCollection } from "moduix";

      export function MultipleComboboxDemo() {
        const { contains } = useFilter({ sensitivity: "base" });
        const { collection, filter } = useListCollection({
          initialItems: fruits,
          filter: contains,
        });

        return (
          <Combobox
            collection={collection}
            onInputValueChange={(details) => filter(details.inputValue)}
            multiple
          >
            <Combobox.Label>Fruits</Combobox.Label>
            <Combobox.Context>
              {(context: UseComboboxContext<(typeof fruits)[number]>) => (
                <div className="tags">
                  {context.selectedItems.length === 0 ? (
                    <span className="note">None selected</span>
                  ) : null}
                  {context.selectedItems.map((item) => (
                    <span key={item.value} className="tag">
                      {item.label}
                    </span>
                  ))}
                </div>
              )}
            </Combobox.Context>
            <Combobox.Control>
              <Combobox.Input placeholder="Search fruits" />
              <Combobox.Trigger aria-label="Open options" />
            </Combobox.Control>
            <Portal>
              <Combobox.Positioner>
                <Combobox.Content>
                  <Combobox.Empty>No fruits found.</Combobox.Empty>
                  <Combobox.List>
                    {collection.items.map((item) => (
                      <Combobox.Item key={item.value} item={item}>
                        <Combobox.ItemText>{item.label}</Combobox.ItemText>
                        <Combobox.ItemIndicator />
                      </Combobox.Item>
                    ))}
                  </Combobox.List>
                </Combobox.Content>
              </Combobox.Positioner>
            </Portal>
          </Combobox>
        );
      }
    `}

  </Preview.Code>

  <Preview.CSS>
    {`
      .tags {
        display: flex;
        min-height: 1.5rem;
        flex-wrap: wrap;
        gap: var(--spacing-1);
      }

      .tag {
        display: inline-flex;
        align-items: center;
        border-radius: var(--radius-sm);
        padding: var(--spacing-1) var(--spacing-2);
        background: var(--color-muted);
        font-size: var(--text-xs);
      }

      .note {
        color: var(--color-muted-foreground);
        font-size: var(--text-xs);
      }
    `}

  </Preview.CSS>

  <Preview.Data>
    {`
      const fruits = [
        { label: "Apple", value: "apple" },
        { label: "Banana", value: "banana" },
        { label: "Mango", value: "mango" },
        { label: "Orange", value: "orange" },
      ];
    `}

  </Preview.Data>
</Preview>

### Async Search

Replace collection items from an async result and use `details.reason` to distinguish typed input
from machine-driven input updates.

<Preview>
  <AsyncSearchComboboxExample />

  <Preview.Code>
    {`
      import { Combobox, Portal, createListCollection } from "moduix";
      import { useEffect, useMemo, useState } from "react";

      export function AsyncSearchComboboxDemo() {
        const [query, setQuery] = useState("");
        const [items, setItems] = useState([] as typeof fruits);
        const [loading, setLoading] = useState(false);

        useEffect(() => {
          if (!query) {
            setItems([]);
            setLoading(false);
            return;
          }

          setLoading(true);
          const timeout = window.setTimeout(() => {
            setItems(
              fruits.filter((item) =>
                item.label.toLowerCase().includes(query.toLowerCase()),
              ),
            );
            setLoading(false);
          }, 300);

          return () => window.clearTimeout(timeout);
        }, [query]);

        const collection = useMemo(
          () => createListCollection({ items }),
          [items],
        );

        return (
          <Combobox
            collection={collection}
            inputValue={query}
            onInputValueChange={(details) => {
              if (
                details.reason === "input-change" ||
                details.inputValue === ""
              ) {
                setQuery(details.inputValue);
              }
            }}
          >
            <Combobox.Label>Search fruit</Combobox.Label>
            <Combobox.Control>
              <Combobox.Input placeholder="Start typing" />
              <Combobox.ClearTrigger aria-label="Clear search" />
              <Combobox.Trigger aria-label="Open options" />
            </Combobox.Control>
            <Portal>
              <Combobox.Positioner>
                <Combobox.Content>
                  {loading ? <div>Searching…</div> : null}
                  {!loading && query && collection.items.length === 0 ? (
                    <div>No results found.</div>
                  ) : null}
                  <Combobox.List>
                    {collection.items.map((item) => (
                      <Combobox.Item key={item.value} item={item}>
                        <Combobox.ItemText>{item.label}</Combobox.ItemText>
                        <Combobox.ItemIndicator />
                      </Combobox.Item>
                    ))}
                  </Combobox.List>
                </Combobox.Content>
              </Combobox.Positioner>
            </Portal>
          </Combobox>
        );
      }
    `}

  </Preview.Code>

<Preview.CSS>{comboboxExampleCss}</Preview.CSS>

  <Preview.Data>
    {`
      const fruits = [
        { label: "Apple", value: "apple" },
        { label: "Banana", value: "banana" },
        { label: "Mango", value: "mango" },
        { label: "Orange", value: "orange" },
        { label: "Pineapple", value: "pineapple" },
        { label: "Strawberry", value: "strawberry" },
      ];
    `}

  </Preview.Data>
</Preview>

### Rehydrate Value

When an existing value arrives before its object collection, call
`useComboboxContext().syncSelectedItems()` after the collection loads so the input can resolve the
selected object.

<Preview>
  <RehydrateValueComboboxExample />

  <Preview.Code>
    {`
      import {
        Combobox,
        Portal,
        useCombobox,
        useComboboxContext,
        useListCollection,
      } from "moduix";
      import { useEffect, useRef } from "react";

      function RehydrateComboboxValue() {
        const combobox = useComboboxContext();
        const hydrated = useRef(false);

        useEffect(() => {
          if (
            combobox.value.length &&
            combobox.collection.size &&
            !hydrated.current
          ) {
            combobox.syncSelectedItems();
            hydrated.current = true;
          }
        }, [combobox]);

        return null;
      }

      export function RehydrateValueComboboxDemo() {
        const { collection, set } = useListCollection({
          initialItems: [],
          itemToString: (item) => item.name,
          itemToValue: (item) => item.name,
        });
        const combobox = useCombobox({
          collection,
          defaultValue: ["C-3PO"],
          placeholder: "e.g. Luke",
        });

        useEffect(() => {
          const timeout = window.setTimeout(() => set(characters), 400);
          return () => window.clearTimeout(timeout);
        }, [set]);

        return (
          <ComboboxProvider value={combobox}>
            <Combobox.Label>Character</Combobox.Label>
            <RehydrateComboboxValue />
            <Combobox.Control>
              <Combobox.Input />
              <Combobox.ClearTrigger aria-label="Clear selection" />
              <Combobox.Trigger aria-label="Open options" />
            </Combobox.Control>
            <Portal>
              <Combobox.Positioner>
                <Combobox.Content>
                  <Combobox.List>
                    {collection.items.map((item) => (
                      <Combobox.Item key={item.name} item={item}>
                        <Combobox.ItemText>
                          {item.name} · {item.height} cm / {item.mass} kg
                        </Combobox.ItemText>
                        <Combobox.ItemIndicator />
                      </Combobox.Item>
                    ))}
                  </Combobox.List>
                </Combobox.Content>
              </Combobox.Positioner>
            </Portal>
          </Combobox.RootProvider>
        );
      }
    `}

  </Preview.Code>

<Preview.CSS>{comboboxExampleCss}</Preview.CSS>

  <Preview.Data>
    {`
      const characters = [
        { name: "C-3PO", height: "167", mass: "75" },
        { name: "R2-D2", height: "96", mass: "32" },
        { name: "Luke Skywalker", height: "172", mass: "77" },
      ];
    `}

  </Preview.Data>
</Preview>

### Root Provider

Create the Ark machine with `useCombobox` when external controls need imperative methods or shared
state before the root renders.

<Preview>
  <RootProviderComboboxExample />

  <Preview.Code>
    {`
      import {
        Combobox,
        Portal,
        createListCollection,
        useCombobox,
      } from "moduix";

      const collection = createListCollection({ items: jobTitles });

      export function RootProviderComboboxDemo() {
        const combobox = useCombobox({ collection });

        return (
          <div>
            <button type="button" onClick={() => combobox.focus()}>
              Focus
            </button>
            <ComboboxProvider value={combobox}>
              <Combobox.Label>Job title</Combobox.Label>
              <Combobox.Control>
                <Combobox.Input />
                <Combobox.ClearTrigger aria-label="Clear selection" />
                <Combobox.Trigger aria-label="Open options" />
              </Combobox.Control>
              <Portal>
                <Combobox.Positioner>
                  <Combobox.Content>
                    <Combobox.List>
                      {collection.items.map((item) => (
                        <Combobox.Item key={item.value} item={item}>
                          <Combobox.ItemText>{item.label}</Combobox.ItemText>
                          <Combobox.ItemIndicator />
                        </Combobox.Item>
                      ))}
                    </Combobox.List>
                  </Combobox.Content>
                </Combobox.Positioner>
              </Portal>
            </Combobox.RootProvider>
          </div>
        );
      }
    `}

  </Preview.Code>

<Preview.CSS>{comboboxExampleCss}</Preview.CSS>

  <Preview.Data>
    {`
      const jobTitles = [
        { label: "Designer", value: "designer" },
        { label: "Developer", value: "developer" },
        { label: "Product Manager", value: "product-manager" },
      ];
    `}

  </Preview.Data>
</Preview>

## Styling

### CSS Properties

<div className="not-prose">
  <Tabs items={['CSS Variables']} className="mb-0 mt-3">
    <Tab className="max-h-150 overflow-auto">
      <ComboboxCssPropertiesPanel />
    </Tab>
  </Tabs>
</div>

### Styling hooks

- Stable slots include `combobox-root`, `combobox-label`, `combobox-control`, `combobox-input`,
  `combobox-clear-trigger`, `combobox-trigger`, `combobox-positioner`, `combobox-content`,
  `combobox-empty`, `combobox-list`, `combobox-item-group`, `combobox-item-group-label`,
  `combobox-item`, `combobox-item-text`, and `combobox-item-indicator`.
- Ark state hooks include `data-state="open|closed"`, `data-focus`, `data-invalid`,
  `data-disabled`, `data-highlighted`, `data-empty`, `data-placement`, and `data-side`.
- Ark positioning variables include `--reference-width`, `--available-width`,
  `--available-height`, `--transform-origin`, `--z-index`, and `--layer-index`.