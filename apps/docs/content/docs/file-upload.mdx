---
title: File Upload
description: Ark UI file upload composition for selecting, dropping, validating, and previewing files.
icon: Component
---

import * as FileUploadDocs from '@/components/examples/file-upload';

## API Reference

<PrimitiveReference href="https://ark-ui.com/docs/components/file-upload" />

## Basic

Use `FileUpload` as the root, render a `Label`, add either a `Trigger` or `Dropzone`, and list
accepted files with `Items`. The root renders its native form input automatically.

<Preview>
  <FileUploadDocs.FileUploadExample />

  <Preview.Code>
  <include lang="tsx">./_snippets/file-upload/basic.tsx#demo</include>
</Preview.Code>
  <Preview.CSS>{FileUploadDocs.fileUploadExampleCss}</Preview.CSS>
  
</Preview>

## Install with shadcn

<ShadcnInstall packageName="react-file-upload" />

## Anatomy

```text
FileUpload / FileUpload.Root
├─ FileUpload.Label
├─ FileUpload.Dropzone
│  ├─ FileUpload.DropzoneIcon
│  └─ FileUpload.Trigger
├─ FileUpload.ItemGroup[type]
│  ├─ FileUpload.Items
│  └─ FileUpload.Context
│     └─ FileUpload.Item[file]
│        ├─ FileUpload.ItemPreview[type]
│        │  └─ FileUpload.ItemPreviewImage
│        ├─ FileUpload.ItemName
│        ├─ FileUpload.ItemSizeText
│        └─ FileUpload.ItemDeleteTrigger
├─ FileUpload.ClearTrigger
└─ native input (automatic)
```

| Part                             | Role                                                                   |
| -------------------------------- | ---------------------------------------------------------------------- |
| `FileUpload` / `FileUpload.Root` | Owns file state, validation, drag/drop, paste, and form integration.   |
| `FileUpload.RootProvider`        | Renders a file upload tree from state returned by Ark `useFileUpload`. |
| `FileUpload.Label`               | Accessible label for the hidden input and upload controls.             |
| `FileUpload.Dropzone`            | Drag/drop target. Use `disableClick` with a nested trigger.            |
| `FileUpload.DropzoneIcon`        | Decorative upload icon helper for dropzone surfaces.                   |
| `FileUpload.Trigger`             | Button that opens the native file picker.                              |
| `FileUpload.ItemGroup`           | List wrapper for accepted or rejected file items.                      |
| `FileUpload.Items`               | Compact accepted-file rows with a name and delete control.             |
| `FileUpload.Context`             | Ark-compatible render-prop context for custom file rows.               |
| `FileUpload.Item`                | File row. Pass the current `file` object.                              |
| `FileUpload.ItemPreview`         | Conditional preview slot matched by MIME type.                         |
| `FileUpload.ItemPreviewImage`    | Image preview rendered inside `ItemPreview type="image/*"`.            |
| `FileUpload.ItemName`            | File name text.                                                        |
| `FileUpload.ItemSizeText`        | Localized file size text.                                              |
| `FileUpload.ItemDeleteTrigger`   | Removes one file. Defaults to the moduix trash icon.                   |
| `FileUpload.ClearTrigger`        | Clears selected files. Defaults to the moduix close icon.              |

## Composition

### Recommended composition

Keep the Ark part tree visible. Use `Trigger` for compact upload controls and `Dropzone` for
drag-and-drop surfaces. If `Trigger` is nested inside `Dropzone`, pass `disableClick` to `Dropzone`
so the native file picker is not opened twice.

The root renders the native form input whenever the upload participates in a form. Use `Items` for compact accepted-file rows. Read
accepted files from `FileUpload.Context.acceptedFiles` and rejected files from `FileUpload.Context.rejectedFiles`
when custom rows are needed.

Import `FileUpload.Context`, `useFileUpload()`, and `useFileUploadContext()` from `@moduix/react` when advanced
state access is needed. These are direct Ark re-exports, so callback details and state contracts stay unchanged.

### When to go custom

Use `Items` for simple attachment lists. Add `ItemPreview type="image/*"` and a fallback
`ItemPreview type=".*"` when a dropzone or media flow benefits from richer rows. Use
`asChild` only when the child is a single semantic element that can preserve the required button,
label, or input behavior.

### Default props

| Prop                  | Default | Notes                                                        |
| --------------------- | ------- | ------------------------------------------------------------ |
| `allowDrop`           | `true`  | Enables drag/drop on `Dropzone`.                             |
| `maxFiles`            | `1`     | Increase or remove it for multi-file and directory uploads.  |
| `minFileSize`         | `0`     | Minimum accepted file size in bytes.                         |
| `maxFileSize`         | `∞`     | Maximum accepted file size in bytes.                         |
| `preventDocumentDrop` | `true`  | Prevents accidental navigation when dropping outside target. |
| `locale`              | `en-US` | Used for file size formatting.                               |

### Behavioral notes

`acceptedFiles`, `defaultAcceptedFiles`, and `onFileChange(details)` preserve Ark controlled and
uncontrolled state. `onFileAccept(details)` and `onFileReject(details)` keep Ark detail objects.
`Field.Root` and `Fieldset.Root` can provide disabled, invalid, required, and read-only state.
Use `ids` when separate labels or descriptions need stable accessibility links.

## Examples

### Dropzone

Use `Dropzone` for drag-and-drop uploads. Keep a nested `Trigger` for keyboard and picker access.

<Preview>
  <FileUploadDocs.FileUploadDropzoneDemo />

  <Preview.Code>
  <include lang="tsx">./_snippets/file-upload/dropzone.tsx#demo</include>
</Preview.Code>
  <Preview.CSS>{FileUploadDocs.fileUploadExampleCss}</Preview.CSS>
  
</Preview>

### Accepted File Types

Restrict picker and drop validation with `accept`. MIME types and extensions are both supported by Ark.

<Preview>
  <FileUploadDocs.FileUploadAcceptedTypesExample />

  <Preview.Code>
  <include lang="tsx">./_snippets/file-upload/accepted-file-types.tsx#demo</include>
</Preview.Code>
  <Preview.CSS>{FileUploadDocs.fileUploadExampleCss}</Preview.CSS>
  
</Preview>

### Rejected Files

Render `rejectedFiles` in a separate `ItemGroup type="rejected"` when users need to see validation failures.

<Preview>
  <FileUploadDocs.FileUploadRejectedFilesExample />

  <Preview.Code>
  <include lang="tsx">./_snippets/file-upload/rejected-files.tsx#demo</include>
</Preview.Code>
  <Preview.CSS>{FileUploadDocs.fileUploadExampleCss}</Preview.CSS>
  
</Preview>

### Error Handling

Use `onFileReject(details)` for application feedback and keep validation constraints on the root.

<Preview>
  <FileUploadDocs.FileUploadErrorHandlingExample />

  <Preview.Code>
  <include lang="tsx">./_snippets/file-upload/error-handling.tsx#demo</include>
</Preview.Code>
  <Preview.CSS>{FileUploadDocs.fileUploadExampleCss}</Preview.CSS>
  
</Preview>

### Controlled

Control `acceptedFiles` when selected files need to coordinate with external state or multi-step flows.

<Preview>
  <FileUploadDocs.ControlledFileUploadExample />

  <Preview.Code>
  <include lang="tsx">./_snippets/file-upload/controlled.tsx#demo</include>
</Preview.Code>
  <Preview.CSS>{FileUploadDocs.fileUploadExampleCss}</Preview.CSS>
  
</Preview>

### Initial Files

Use `defaultAcceptedFiles` when the upload should start with existing files and then manage itself.

<Preview>
  <FileUploadDocs.FileUploadInitialFilesExample />

  <Preview.Code>
  <include lang="tsx">./_snippets/file-upload/initial-files.tsx#demo</include>
</Preview.Code>
  <Preview.CSS>{FileUploadDocs.fileUploadExampleCss}</Preview.CSS>
  
</Preview>

### Clear Trigger

Use `ClearTrigger` to clear accepted files at once.

<Preview>
  <FileUploadDocs.FileUploadClearTriggerExample />

  <Preview.Code>
  <include lang="tsx">./_snippets/file-upload/clear-trigger.tsx#demo</include>
</Preview.Code>
  <Preview.CSS>{FileUploadDocs.fileUploadExampleCss}</Preview.CSS>
  
</Preview>

### With Field

Wrap the upload in `Field.Root` when helper text, error text, required state, or invalid state belongs to the form field.

<Preview>
  <FileUploadDocs.FileUploadWithFieldExample />

  <Preview.Code>
  <include lang="tsx">./_snippets/file-upload/with-field.tsx#demo</include>
</Preview.Code>
  <Preview.CSS>{FileUploadDocs.fileUploadExampleCss}</Preview.CSS>
  
</Preview>

### Form Usage

Pass `name` to the root. It renders the native form input automatically, so native form submission
and reset include the file input.

<Preview>
  <FileUploadDocs.FileUploadFormExample />

  <Preview.Code>
  <include lang="tsx">./_snippets/file-upload/form-usage.tsx#demo</include>
</Preview.Code>
  <Preview.CSS>{FileUploadDocs.fileUploadExampleCss}</Preview.CSS>
  
</Preview>

### Root Provider And Paste

Use moduix `useFileUpload` with `RootProvider` when state or methods need to be owned outside the
rendered upload tree.

<Preview>
  <FileUploadDocs.FileUploadRootProviderExample />

  <Preview.Code>
  <include lang="tsx">./_snippets/file-upload/root-provider-and-paste.tsx#demo</include>
</Preview.Code>
  <Preview.CSS>{FileUploadDocs.fileUploadExampleCss}</Preview.CSS>
  
</Preview>

### Directory Upload

Use `directory` for folder upload. Browsers that support it expose relative paths on `file.webkitRelativePath`.

<Preview>
  <FileUploadDocs.FileUploadDirectoryExample />

  <Preview.Code>
  <include lang="tsx">./_snippets/file-upload/directory-upload.tsx#demo</include>
</Preview.Code>
  <Preview.CSS>{FileUploadDocs.fileUploadExampleCss}</Preview.CSS>
  
</Preview>

### Media Capture

Use `capture` with an image or video `accept` value to hint that mobile browsers should open a camera.

<Preview>
  <FileUploadDocs.FileUploadMediaCaptureExample />

  <Preview.Code>
  <include lang="tsx">./_snippets/file-upload/media-capture.tsx#demo</include>
</Preview.Code>
  <Preview.CSS>{FileUploadDocs.fileUploadExampleCss}</Preview.CSS>
  
</Preview>

### Transform Files

Use `transformFiles(files)` for async processing before files enter accepted state.

<Preview>
  <FileUploadDocs.FileUploadTransformFilesExample />

  <Preview.Code>
  <include lang="tsx">./_snippets/file-upload/transform-files.tsx#demo</include>
</Preview.Code>
  <Preview.CSS>{FileUploadDocs.fileUploadExampleCss}</Preview.CSS>
  
</Preview>

### Advanced Customization

Bypass `Items` when custom rows need file metadata, previews, or a different layout. The explicit
`FileUpload.Context` composition keeps Ark state visible and uses the same styled parts.

<Preview>
  <FileUploadDocs.FileUploadAdvancedCustomizationExample />

  <Preview.Code>
  <include lang="tsx">./_snippets/file-upload/advanced-customization.tsx#demo</include>
</Preview.Code>
  <Preview.CSS>{FileUploadDocs.fileUploadExampleCss}</Preview.CSS>
</Preview>

## Styling

### CSS Properties

<div className="not-prose">
  <Tabs items={['CSS Variables']}>
    <Tab value="CSS Variables">
      <div className="max-h-96 overflow-auto">
        <FileUploadDocs.FileUploadCssPropertiesPanel />
      </div>
    </Tab>
  </Tabs>
</div>

### Styling hooks

Target stable moduix slots such as `[data-slot="file-upload-dropzone"]`,
`[data-slot="file-upload-item"]`, and `[data-slot="file-upload-item-delete-trigger"]`.
Ark also exposes `data-scope="file-upload"`, part-specific `data-part`, and state attributes:
`data-disabled`, `data-readonly`, `data-invalid`, `data-required`, `data-dragging`, and `data-type`.