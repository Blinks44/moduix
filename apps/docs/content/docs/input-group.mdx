---
title: Input Group
description: A composable field shell for inline prefixes, suffixes, helper text, and compact actions.
icon: Component
---

import {
  CustomStylesInputGroupExample,
  DisabledInputGroupExample,
  InputGroupCssPlaygroundPanel,
  InputGroupCssPropertiesPanel,
  InputGroupExample,
  InputGroupFieldValidationExample,
  InputGroupPrefixSuffixExample,
  InputGroupSizesExample,
  InputGroupWithActionExample,
  inputGroupPlaygroundCssProperties,
} from '@/components/examples/input-group';

## API Reference

`InputGroup` does not wrap a dedicated Base UI primitive. It is a moduix composition helper built
from the local `Input` and `Button` components, so the public API lives on these exported parts:

- `InputGroup`
- `InputGroupInput`
- `InputGroupAddon`
- `InputGroupText`
- `InputGroupButton`

## Overview

`InputGroup` keeps the public surface short:

- `InputGroup` renders the shared field shell
- `InputGroupInput` renders the actual `Input`
- `InputGroupAddon`, `InputGroupText`, and `InputGroupButton` decorate the input inline

The group is composition-first. You place one `InputGroupInput` inside the shell, then add the
parts you need before or after it.

## Basic

<Preview cssProperties={inputGroupPlaygroundCssProperties} cssVariableScope="local">
  <InputGroupExample />

  <Preview.Code>
    {`
      import { Field, FieldLabel, InputGroup, InputGroupAddon, InputGroupInput } from "moduix";

      export function InputGroupDemo() {
        return (
          <Field className={styles.field}>
            <FieldLabel>Workspace</FieldLabel>
            <InputGroup>
              <InputGroupAddon>@</InputGroupAddon>
              <InputGroupInput placeholder="maps" />
            </InputGroup>
          </Field>
        );
      }
    `}

  </Preview.Code>

<Preview.CSS>
  {`
      .field {
        width: min(24rem, 100%);
      }
    `}
</Preview.CSS>

  <Preview.CSSProperties>{(context) => <InputGroupCssPropertiesPanel {...context} />}</Preview.CSSProperties>
  <Preview.CSSPlayground>{(context) => <InputGroupCssPlaygroundPanel {...context} />}</Preview.CSSPlayground>
</Preview>

## Anatomy

```text
InputGroup
├─ InputGroupAddon (optional)
├─ InputGroupInput
├─ InputGroupText (optional)
└─ InputGroupButton (optional)
```

| Part               | Role                                                     |
| ------------------ | -------------------------------------------------------- |
| `InputGroup`       | Shared shell for border, radius, focus ring, and sizing. |
| `InputGroupInput`  | The `Input` rendered inside the shell.                   |
| `InputGroupAddon`  | Filled prefix or suffix surface with a separator.        |
| `InputGroupText`   | Plain inline text without the filled addon background.   |
| `InputGroupButton` | Inline action based on `Button`.                         |

## Composition

Use one `InputGroupInput` per group. The shell click-to-focus behavior targets the first grouped
input, so multiple grouped inputs are not a supported composition.

Addons, helper text, and buttons can sit on either side of the input depending on the flow you
need.

Set `size` on `InputGroup` to keep the whole row aligned. `InputGroupInput` and
`InputGroupButton` inherit that size automatically, so the default path stays short. Pass an
explicit `size` to either part only when one child should intentionally differ from the group.

Clicking the shell focuses the input, which keeps prefix and suffix layouts feeling like one field
instead of separate controls. That focus redirect is skipped for `disabled` and `readOnly` grouped
inputs.

`InputGroupInput` accepts the same props as `Input`, including controlled values, native input
attributes, and Base UI field validation state.

`InputGroupButton` defaults to `variant="ghost"` and `type="button"`, so inline actions work
inside forms without submitting them accidentally.

For standalone widgets that combine an input and action into one logical unit, add `role="group"`
with `aria-label` or `aria-labelledby` on `InputGroup`. When the group lives inside `Field`, prefer
`FieldLabel` and keep `InputGroup` purely visual.

### Defaults

| Part / Prop                | Default              | Notes                                                     |
| -------------------------- | -------------------- | --------------------------------------------------------- |
| `InputGroup.size`          | `md`                 | Supports `xs`, `sm`, `md`, `lg`, and `xl`.                |
| `InputGroupInput.size`     | inherited from group | Override only when one input should intentionally differ. |
| `InputGroupButton.variant` | `ghost`              | Accepts the same variants as `Button`.                    |
| `InputGroupButton.size`    | inherited from group | Override for icon or mixed-density actions.               |
| `InputGroupButton.type`    | `button`             | Use `submit` or `reset` when the action should do that.   |

## Examples

### With Action

Use `InputGroupButton` for compact inline actions tied to the current field value.

<Preview>
  <InputGroupWithActionExample />

  <Preview.Code>
    {`
      import { Field, FieldLabel, InputGroup, InputGroupButton, InputGroupInput } from "moduix";
      import { useState } from "react";

      export function InputGroupWithActionDemo() {
        const [value, setValue] = useState("");

        return (
          <Field className={styles.field}>
            <FieldLabel>Invite by email</FieldLabel>
            <InputGroup>
              <InputGroupInput
                value={value}
                onValueChange={setValue}
                type="email"
                placeholder="name@example.com"
              />
              <InputGroupButton disabled={!value}>Send</InputGroupButton>
            </InputGroup>
          </Field>
        );
      }
    `}

  </Preview.Code>
</Preview>

### Prefix And Suffix

Mix `InputGroupAddon` and `InputGroupText` when one side should feel fixed and the other should
read as helper text.

<Preview>
  <InputGroupPrefixSuffixExample />

  <Preview.Code>
    {`
      import { Field, FieldLabel, InputGroup, InputGroupAddon, InputGroupInput, InputGroupText } from "moduix";

      export function PrefixSuffixInputGroupDemo() {
        return (
          <Field className={styles.field}>
            <FieldLabel>Monthly budget</FieldLabel>
            <InputGroup>
              <InputGroupAddon>$</InputGroupAddon>
              <InputGroupInput inputMode="decimal" placeholder="2500" />
              <InputGroupText>USD</InputGroupText>
            </InputGroup>
          </Field>
        );
      }
    `}

  </Preview.Code>
</Preview>

### Field Validation

`InputGroupInput` keeps the Base UI input state, so `Field` validation can style the whole group as
invalid.

<Preview>
  <InputGroupFieldValidationExample />

  <Preview.Code>
    {`
      import { Field, FieldError, FieldLabel, InputGroup, InputGroupInput, InputGroupText } from "moduix";

      export function InputGroupFieldValidationDemo() {
        return (
          <Field className={styles.field} validationMode="onBlur">
            <FieldLabel>Domain</FieldLabel>
            <InputGroup>
              <InputGroupInput required placeholder="company" />
              <InputGroupText>.test.com</InputGroupText>
            </InputGroup>
            <FieldError match="valueMissing">Please enter a domain.</FieldError>
          </Field>
        );
      }
    `}

  </Preview.Code>
</Preview>

### Disabled

When the input is disabled, the group shell dims with it.

<Preview>
  <DisabledInputGroupExample />

  <Preview.Code>
    {`
      import { InputGroup, InputGroupAddon, InputGroupButton, InputGroupInput } from "moduix";

      export function DisabledInputGroupDemo() {
        return (
          <InputGroup className={styles.group} role="group" aria-label="Workspace handle">
            <InputGroupAddon>@</InputGroupAddon>
            <InputGroupInput disabled value="maps" />
            <InputGroupButton disabled>Copy</InputGroupButton>
          </InputGroup>
        );
      }
    `}

  </Preview.Code>
</Preview>

### Sizes

Set `size` on the group instead of wiring the same size into each child.

<Preview>
  <InputGroupSizesExample />

  <Preview.Code>
    {`
      import { InputGroup, InputGroupAddon, InputGroupInput } from "moduix";

      export function InputGroupSizesDemo() {
        return (
          <div className={styles.stack}>
            <InputGroup size="xs">
              <InputGroupAddon>@</InputGroupAddon>
              <InputGroupInput placeholder="Extra-small group" />
            </InputGroup>
            <InputGroup size="sm">
              <InputGroupAddon>@</InputGroupAddon>
              <InputGroupInput placeholder="Small group" />
            </InputGroup>
            <InputGroup size="md">
              <InputGroupAddon>@</InputGroupAddon>
              <InputGroupInput placeholder="Medium group" />
            </InputGroup>
            <InputGroup size="lg">
              <InputGroupAddon>@</InputGroupAddon>
              <InputGroupInput placeholder="Large group" />
            </InputGroup>
            <InputGroup size="xl">
              <InputGroupAddon>@</InputGroupAddon>
              <InputGroupInput placeholder="Extra-large group" />
            </InputGroup>
          </div>
        );
      }
    `}

  </Preview.Code>
</Preview>

## Custom Styling

Use root CSS variables for theme-level changes and part-level `className` props for local styling.

<Preview>
  <CustomStylesInputGroupExample />

  <Preview.Code>
    {`
      import { InputGroup, InputGroupAddon, InputGroupButton, InputGroupInput } from "moduix";

      export function CustomInputGroupDemo() {
        return (
          <InputGroup className={styles.customGroup}>
            <InputGroupAddon className={styles.customAddon}>@</InputGroupAddon>
            <InputGroupInput placeholder="custom-group" />
            <InputGroupButton className={styles.customButton}>Check</InputGroupButton>
          </InputGroup>
        );
      }
    `}

  </Preview.Code>

  <Preview.CSS>
    {`
      .customGroup {
        --input-group-bg: var(--color-background);
        --input-group-border-color: color-mix(in oklab, var(--color-primary) 35%, transparent);
        --input-group-focus-ring-color: color-mix(in oklab, var(--color-primary) 50%, transparent);
        --input-group-radius: var(--radius-lg);
      }

      .customAddon {
        --input-group-addon-bg: color-mix(in oklab, var(--color-primary) 14%, var(--color-background));
        --input-group-addon-color: var(--color-primary);

        font-weight: var(--weight-semibold);
      }

      .customButton {
        --button-ghost-bg-hover: color-mix(in oklab, var(--color-primary) 18%, var(--color-background));
        --input-group-button-color: var(--color-primary);

        margin: var(--spacing-1);
        border: var(--border-width-sm) solid
          color-mix(in oklab, var(--color-primary) 28%, transparent);
        border-radius: var(--radius-sm);
        background-color: color-mix(in oklab, var(--color-primary) 10%, var(--color-background));
        font-weight: var(--weight-semibold);
      }
    `}

  </Preview.CSS>
</Preview>