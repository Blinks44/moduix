---
title: Dialog
description: A modal window that appears above the main content for focused workflows.
icon: Component
---

import {
  AlertDialogExample,
  ContextDialogExample,
  ControlledDialogExample,
  DialogCssPropertiesPanel,
  DialogExample,
  FinalFocusDialogExample,
  InitialFocusDialogExample,
  ConfirmationDialogExample,
  LazyMountDialogExample,
  MultipleTriggersDialogExample,
  NestedDialogExample,
  NonModalDialogExample,
  OpenFromMenuDialogExample,
  OutsideScrollDialogExample,
  RootProviderDialogExample,
  ScrollableDialogExample,
  dialogEmptyData,
  dialogExampleCss,
  dialogScrollData,
  dialogUsersData,
} from '@/components/examples/dialog';

## API Reference

<PrimitiveReference href="https://ark-ui.com/docs/components/dialog" />

## Choosing the right component

Use `Dialog` for focused forms, confirmations, details, and secondary workflows that stay in the
current page context. Use `Drawer` when the surface should remain attached to a viewport edge.

## Basic

Compose the explicit Ark UI structure: `Dialog.Root`, `Dialog.Backdrop`,
`Dialog.Positioner`, and `Dialog.Content`.

<Preview>
  <DialogExample />

  <Preview.Code>
    {`
      import { Button, Dialog } from "@moduix/react";

      export function DialogDemo() {
        return (
          <Dialog>
            <Dialog.Trigger asChild>
              <Button>View notifications</Button>
            </Dialog.Trigger>
            <Dialog.Backdrop />
            <Dialog.Positioner>
              <Dialog.Content>
                <Dialog.Header>
                  <Dialog.Title>Notifications</Dialog.Title>
                  <Dialog.CloseIcon />
                  <Dialog.Description>
                    You are all caught up. Good job!
                  </Dialog.Description>
                </Dialog.Header>
                <Dialog.Footer>
                  <Dialog.CloseTrigger asChild>
                    <Button variant="outline">Close</Button>
                  </Dialog.CloseTrigger>
                </Dialog.Footer>
              </Dialog.Content>
            </Dialog.Positioner>
          </Dialog>
        );
      }
    `}

  </Preview.Code>
  <Preview.CSS>{dialogExampleCss}</Preview.CSS>
  <Preview.Data>{dialogEmptyData}</Preview.Data>
</Preview>

## Install with shadcn

<Tabs items={['npm', 'pnpm', 'bun', 'deno', 'aube']}>
  <Tab value="npm">

```bash
npx shadcn@latest add Blinks44/moduix/react-dialog
```

  </Tab>
  <Tab value="pnpm">

```bash
pnpm dlx shadcn@latest add Blinks44/moduix/react-dialog
```

  </Tab>
  <Tab value="bun">

```bash
bunx shadcn@latest add Blinks44/moduix/react-dialog
```

  </Tab>
  <Tab value="deno">

```bash
deno x -A npm:shadcn@latest add Blinks44/moduix/react-dialog
```

  </Tab>
  <Tab value="aube">

```bash
aubx shadcn@latest add Blinks44/moduix/react-dialog
```

  </Tab>
</Tabs>

## Anatomy

```text
Dialog.Root
├─ Dialog.Trigger
└─ Overlay subtree (automatically portalled)
   ├─ Dialog.Backdrop
   └─ Dialog.Positioner
      └─ Dialog.Content
         ├─ Dialog.CloseTrigger or Dialog.CloseIcon
         ├─ Dialog.Title
         ├─ Dialog.Description
         ├─ Dialog.Header (moduix layout sugar)
         ├─ Dialog.Body (moduix layout sugar)
         └─ Dialog.Footer (moduix layout sugar)

Dialog.RootProvider
└─ the same automatically portalled content tree connected to Ark useDialog()
```

| Part                     | Role                                                             |
| ------------------------ | ---------------------------------------------------------------- |
| `Dialog` / `Dialog.Root` | Owns dialog open state, focus lifecycle, and dismissal behavior. |
| `Dialog.Trigger`         | Opens the dialog.                                                |
| `Dialog.Backdrop`        | Optional modal backdrop layer.                                   |
| `Dialog.Positioner`      | Floating positioner for content placement.                       |
| `Dialog.Content`         | Dialog surface container.                                        |
| `Dialog.Title`           | Accessible dialog title.                                         |
| `Dialog.Description`     | Accessible supporting description.                               |
| `Dialog.CloseTrigger`    | Closes the dialog.                                               |
| `Dialog.RootProvider`    | Connects parts to state created with Ark `useDialog()`.          |

Import Ark `useDialog()` and `useDialogContext()` directly from `@ark-ui/react/dialog` when dialog
state must live outside the rendered tree or be read inside reusable children.

## Composition

The root portals `Backdrop` and `Positioner` automatically. Use `portalled={false}` for inline rendering and `portalRef` for a custom container; structural parts remain explicit and styleable.

### Recommended composition

Keep `Backdrop`, `Positioner`, and `Content` explicit. This preserves structural override
points and matches Ark UI directly. Omit `Backdrop` for non-modal dialogs.

Use `Dialog.Trigger asChild` and `Dialog.CloseTrigger asChild` with one semantic button child.
`Dialog.CloseIcon` is moduix sugar that composes Ark `CloseTrigger` with the library close button.

### When to go custom

Use `open` with `onOpenChange(details)` for React-controlled state. The callback receives an Ark
detail object, so read `details.open`.

Use Ark `useDialog()` with `Dialog.RootProvider` when state and methods must live outside the rendered
dialog tree. Do not render `Dialog.Root` for the same store.

Use `ids` when separately composed parts need stable shared accessibility IDs. Use
`initialFocusEl` and `finalFocusEl` for explicit focus targets. `role="alertdialog"` models
destructive or critical confirmation flows.

Use `lazyMount` and `unmountOnExit` instead of conditionally rendering the root. Use `present` only
when JavaScript-driven exit animation needs to control mounted state.

### Default props

| Surface                  | Default | Notes                                                  |
| ------------------------ | ------- | ------------------------------------------------------ |
| `Dialog` / `Dialog.Root` | —       | No additional behavioral defaults; Ark defaults apply. |

### Behavioral notes

- Modal dialogs trap focus, prevent background scrolling, hide underlying content from assistive
  technology, and restore focus after close.
- `modal={false}` disables focus trapping and scroll prevention and should omit the backdrop.
- `closeOnEscape`, `closeOnInteractOutside`, `trapFocus`, `preventScroll`, `restoreFocus`,
  `persistentElements`, and the outside-interaction callbacks pass through to Ark UI.
- `Dialog.Title` or root `aria-label` is required for an accessible name.
- Nested content receives `data-has-nested` and `--nested-layer-count`. Moduix uses those hooks to
  scale the parent down and offset it downward so the active nested dialog still reads as layered.
- Open-from-menu and rapid state changes use the controlled `open` contract. Unsaved-change
  confirmation uses two Ark `useDialog()` stores and intercepts the parent store's `onOpenChange`.

## Examples

### Controlled

Use the Ark detail object when synchronizing dialog state with React.

<Preview>
  <ControlledDialogExample />
  <Preview.Code>
    {`
      import { Button, Dialog } from "@moduix/react";
      import { useState } from "react";

      export function ControlledDialogDemo() {
        const [open, setOpen] = useState(false);

        return (
          <Dialog open={open} onOpenChange={(details) => setOpen(details.open)}>
            <Dialog.Trigger asChild>
              <Button>Open controlled dialog</Button>
            </Dialog.Trigger>
            <Dialog.Backdrop />
            <Dialog.Positioner>
              <Dialog.Content>
                <Dialog.Title>Publish changes?</Dialog.Title>
                <Dialog.CloseIcon />
              </Dialog.Content>
            </Dialog.Positioner>
          </Dialog>
        );
      }
    `}

  </Preview.Code>
  <Preview.CSS>{dialogExampleCss}</Preview.CSS>
  <Preview.Data>{dialogEmptyData}</Preview.Data>
</Preview>

### Root Provider and Ark Context Hooks

Create state with Ark `useDialog()` when controls live outside the dialog, and read dialog state in
child components with Ark `useDialogContext()`.

<Preview>
  <div className="dialog-stack">
    <RootProviderDialogExample />
    <ContextDialogExample />
  </div>

  <Preview.Code>
    {`
      import { useDialog, useDialogContext } from "@ark-ui/react/dialog";
      import { Button, Dialog } from "@moduix/react";

      function DialogStatusText() {
        const dialog = useDialogContext();

        return <>Dialog is {dialog.open ? "open" : "closed"}</>;
      }

      export function DialogStateDemo() {
        const dialog = useDialog();

        return (
          <>
            <Button onClick={() => dialog.setOpen(true)}>
              Dialog is {dialog.open ? "open" : "closed"}
            </Button>
            <Dialog.RootProvider value={dialog}>
              <Dialog.Backdrop />
              <Dialog.Positioner>
                <Dialog.Content>
                  <Dialog.Title>Status</Dialog.Title>
                  <Dialog.Description>
                    <DialogStatusText />
                  </Dialog.Description>
                  <Dialog.CloseIcon />
                </Dialog.Content>
              </Dialog.Positioner>
            </Dialog.RootProvider>
          </>
        );
      }
    `}

  </Preview.Code>
  <Preview.CSS>{dialogExampleCss}</Preview.CSS>
  <Preview.Data>{dialogEmptyData}</Preview.Data>
</Preview>

### Alert Dialog

Use `role="alertdialog"` for critical decisions and keep the safest action available as a close
trigger.

<Preview>
  <AlertDialogExample />
  <Preview.Code>
    {`
      import { Button, Dialog } from "@moduix/react";

      export function DeleteAccountDialog() {
        return (
          <Dialog role="alertdialog">
            <Dialog.Trigger asChild>
              <Button>Delete account</Button>
            </Dialog.Trigger>
            <Dialog.Backdrop />
            <Dialog.Positioner>
              <Dialog.Content>
                <Dialog.Title>Are you absolutely sure?</Dialog.Title>
                <Dialog.Description>This action cannot be undone.</Dialog.Description>
                <Dialog.Footer>
                  <Dialog.CloseTrigger asChild>
                    <Button variant="outline">Cancel</Button>
                  </Dialog.CloseTrigger>
                  <Button>Delete account</Button>
                </Dialog.Footer>
              </Dialog.Content>
            </Dialog.Positioner>
          </Dialog>
        );
      }
    `}

  </Preview.Code>
  <Preview.CSS>{dialogExampleCss}</Preview.CSS>
  <Preview.Data>{dialogEmptyData}</Preview.Data>
</Preview>

### Mounting and Focus Lifecycle

Ark UI supports lazy mounting and explicit initial and final focus targets through root props.

<Preview>
  <div className="dialog-stack">
    <LazyMountDialogExample />
    <InitialFocusDialogExample />
    <FinalFocusDialogExample />
  </div>

  <Preview.Code>
    {`
      import { Button, Dialog } from "@moduix/react";
      import { useRef } from "react";

      export function DialogLifecycleDemo() {
        const inputRef = useRef(null as HTMLInputElement | null);
        const finalFocusRef = useRef(null as HTMLButtonElement | null);

        return (
          <>
            <Button ref={finalFocusRef}>Final focus target</Button>
            <Dialog
              lazyMount
              unmountOnExit
              initialFocusEl={() => inputRef.current}
              finalFocusEl={() => finalFocusRef.current}
            >
              <Dialog.Trigger asChild>
                <Button>Edit profile</Button>
              </Dialog.Trigger>
              <Dialog.Backdrop />
              <Dialog.Positioner>
                <Dialog.Content>
                  <Dialog.Title>Edit profile</Dialog.Title>
                  <input ref={inputRef} className="dialog-input" />
                  <Dialog.CloseIcon />
                </Dialog.Content>
              </Dialog.Positioner>
            </Dialog>
          </>
        );
      }
    `}

  </Preview.Code>
  <Preview.CSS>{dialogExampleCss}</Preview.CSS>
  <Preview.Data>{dialogEmptyData}</Preview.Data>
</Preview>

### Non-Modal

Set `modal={false}` and omit the backdrop when outside interaction should remain available.

<Preview>
  <NonModalDialogExample />
  <Preview.Code>
    {`
      import { Button, Dialog } from "@moduix/react";

      export function NonModalDialogDemo() {
        return (
          <Dialog modal={false}>
            <Dialog.Trigger asChild>
              <Button>Open non-modal dialog</Button>
            </Dialog.Trigger>
            <Dialog.Positioner>
              <Dialog.Content>
                <Dialog.Title>Non-modal dialog</Dialog.Title>
                <Dialog.Description>The page remains interactive.</Dialog.Description>
                <Dialog.CloseIcon />
              </Dialog.Content>
            </Dialog.Positioner>
          </Dialog>
        );
      }
    `}

  </Preview.Code>
  <Preview.CSS>{dialogExampleCss}</Preview.CSS>
  <Preview.Data>{dialogEmptyData}</Preview.Data>
</Preview>

### Inside and Outside Scroll

Scroll the body for a fixed header/footer layout, or scroll the positioner when content should
extend beyond the viewport.

<Preview>
  <div className="dialog-stack">
    <ScrollableDialogExample />
    <OutsideScrollDialogExample />
  </div>

  <Preview.Code>
    {`
      import { Button, Dialog, ScrollArea } from "@moduix/react";

      export function DialogScrollDemo() {
        return (
          <Dialog>
            <Dialog.Trigger asChild>
              <Button>Open terms</Button>
            </Dialog.Trigger>
            <Dialog.Backdrop />
            <Dialog.Positioner className="dialog-outside-positioner">
              <Dialog.Content className="dialog-inside-scroll">
                <Dialog.Title>Terms of service</Dialog.Title>
                <ScrollArea className="dialog-scroll-area">
                  <ScrollArea.Viewport>
                    <ScrollArea.Content>
                      <div className="dialog-scroll-content">
                        {sections.map((section) => (
                          <section key={section.title}>
                            <h3>{section.title}</h3>
                            <p>{section.body}</p>
                          </section>
                        ))}
                      </div>
                    </ScrollArea.Content>
                  </ScrollArea.Viewport>
                  <ScrollArea.Scrollbar>
                    <ScrollArea.Thumb />
                  </ScrollArea.Scrollbar>
                  <ScrollArea.Corner />
                </ScrollArea>
              </Dialog.Content>
            </Dialog.Positioner>
          </Dialog>
        );
      }
    `}

  </Preview.Code>
  <Preview.CSS>{dialogExampleCss}</Preview.CSS>
  <Preview.Data>{dialogScrollData}</Preview.Data>
</Preview>

### Nested

Use separate Ark `useDialog()` stores for nested layers. Ark UI provides stack-aware state and CSS
variables to the parent content; moduix uses them for a receding parent effect that stacks downward.

<Preview>
  <NestedDialogExample />
  <Preview.Code>
    {`
      import { useDialog } from "@ark-ui/react/dialog";
      import { Button, Dialog } from "@moduix/react";

      export function NestedDialogDemo() {
        const parent = useDialog();
        const child = useDialog();

        return (
          <>
            <Button onClick={() => parent.setOpen(true)}>Open parent</Button>
            <Dialog.RootProvider value={parent}>
              <Dialog.Backdrop />
              <Dialog.Positioner>
                <Dialog.Content>
                  <Dialog.Title>Parent dialog</Dialog.Title>
                  <Button onClick={() => child.setOpen(true)}>Open nested</Button>
                </Dialog.Content>
              </Dialog.Positioner>
            </Dialog.RootProvider>
            <Dialog.RootProvider value={child}>
              <Dialog.Backdrop />
              <Dialog.Positioner>
                <Dialog.Content>
                  <Dialog.Title>Nested dialog</Dialog.Title>
                  <Dialog.CloseIcon />
                </Dialog.Content>
              </Dialog.Positioner>
            </Dialog.RootProvider>
          </>
        );
      }
    `}

  </Preview.Code>
  <Preview.CSS>{dialogExampleCss}</Preview.CSS>
  <Preview.Data>{dialogEmptyData}</Preview.Data>
</Preview>

### Open from Menu

Control `open` from a menu item when a contextual action needs a confirmation dialog.

<Preview>
  <OpenFromMenuDialogExample />
  <Preview.Code>
    {`
      import { Button, Dialog, Menu } from "@moduix/react";
      import { useState } from "react";

      export function OpenFromMenuDialogDemo() {
        const [open, setOpen] = useState(false);

        return (
          <>
            <Menu>
              <Menu.Trigger asChild>
                <Button variant="outline">
                  Actions
                  <Menu.Indicator />
                </Button>
              </Menu.Trigger>
              <Menu.Positioner>
                <Menu.Content>
                  <Menu.Item value="edit">Edit</Menu.Item>
                  <Menu.Item value="duplicate">Duplicate</Menu.Item>
                  <Menu.Item value="delete" tone="destructive" onClick={() => setOpen(true)}>
                    Delete...
                  </Menu.Item>
                </Menu.Content>
              </Menu.Positioner>
            </Menu>

            <Dialog
              open={open}
              onOpenChange={(details) => setOpen(details.open)}
              role="alertdialog"
            >
              <Dialog.Backdrop />
              <Dialog.Positioner>
                <Dialog.Content>
                  <Dialog.Title>Confirm delete</Dialog.Title>
                  <Dialog.Description>This action cannot be undone.</Dialog.Description>
                  <Dialog.Footer>
                    <Dialog.CloseTrigger asChild>
                      <Button variant="outline">Cancel</Button>
                    </Dialog.CloseTrigger>
                    <Button>Delete</Button>
                  </Dialog.Footer>
                </Dialog.Content>
              </Dialog.Positioner>
            </Dialog>
          </>
        );
      }
    `}

  </Preview.Code>
  <Preview.CSS>{dialogExampleCss}</Preview.CSS>
  <Preview.Data>{dialogEmptyData}</Preview.Data>
</Preview>

### Multiple Triggers

Give each trigger a `value` and react to `onTriggerValueChange(details)` when one dialog edits
different records.

<Preview>
  <MultipleTriggersDialogExample />
  <Preview.Code>
    {`
      import { Button, Dialog } from "@moduix/react";
      import { useState } from "react";

      export function MultipleTriggersDialogDemo() {
        const [activeUser, setActiveUser] = useState(null as (typeof users)[number] | null);

        return (
          <Dialog
            onTriggerValueChange={(details) => {
              setActiveUser(users.find((user) => user.id === details.value) ?? null);
            }}
          >
            {users.map((user) => (
              <Dialog.Trigger key={user.id} value={user.id} asChild>
                <Button variant="outline">Edit {user.name}</Button>
              </Dialog.Trigger>
            ))}
            <Dialog.Backdrop />
            <Dialog.Positioner>
              <Dialog.Content>
                <Dialog.Title>Edit user</Dialog.Title>
                <Dialog.Description>{activeUser?.email}</Dialog.Description>
                <Dialog.CloseIcon />
              </Dialog.Content>
            </Dialog.Positioner>
          </Dialog>
        );
      }
    `}

  </Preview.Code>
  <Preview.CSS>{dialogExampleCss}</Preview.CSS>
  <Preview.Data>{dialogUsersData}</Preview.Data>
</Preview>

### Confirmation

Intercept the parent dialog close request when unsaved changes should be confirmed first.

<Preview>
  <ConfirmationDialogExample />
  <Preview.Code>
    {`
      import { useDialog } from "@ark-ui/react/dialog";
      import { Button, Dialog } from "@moduix/react";
      import { useState } from "react";

      export function ConfirmationDialogDemo() {
        const [formContent, setFormContent] = useState("");
        const [open, setOpen] = useState(false);
        const confirmDialog = useDialog();
        const parentDialog = useDialog({
          open,
          onOpenChange(details) {
            if (!details.open && formContent.trim()) {
              confirmDialog.setOpen(true);
              return;
            }

            setOpen(details.open);
          },
        });

        const handleDiscard = () => {
          setFormContent("");
          confirmDialog.setOpen(false);
          setOpen(false);
        };

        return (
          <>
            <Button onClick={() => setOpen(true)}>Open form</Button>

            <Dialog.RootProvider value={parentDialog}>
              <Dialog.Backdrop />
              <Dialog.Positioner>
                <Dialog.Content>
                  <Dialog.Title>Edit content</Dialog.Title>
                  <Dialog.CloseIcon />
                  <Dialog.Description>
                    Unsaved changes ask for confirmation before closing.
                  </Dialog.Description>
                  <Dialog.Body>
                    <textarea
                      className="dialog-input dialog-textarea"
                      value={formContent}
                      onChange={(event) => setFormContent(event.currentTarget.value)}
                      placeholder="Enter some text..."
                    />
                  </Dialog.Body>
                </Dialog.Content>
              </Dialog.Positioner>
            </Dialog.RootProvider>

            <Dialog.RootProvider value={confirmDialog}>
              <Dialog.Backdrop />
              <Dialog.Positioner>
                <Dialog.Content>
                  <Dialog.Title>Discard changes?</Dialog.Title>
                  <Dialog.Description>You have unsaved changes.</Dialog.Description>
                  <Dialog.Footer>
                    <Dialog.CloseTrigger asChild>
                      <Button variant="outline">Keep editing</Button>
                    </Dialog.CloseTrigger>
                    <Button onClick={handleDiscard}>Discard</Button>
                  </Dialog.Footer>
                </Dialog.Content>
              </Dialog.Positioner>
            </Dialog.RootProvider>
          </>
        );
      }
    `}

  </Preview.Code>
  <Preview.CSS>{dialogExampleCss}</Preview.CSS>
  <Preview.Data>{dialogEmptyData}</Preview.Data>
</Preview>

## Styling

### CSS Properties

<div className="not-prose">
  <Tabs items={['CSS Variables']} className="mb-0 mt-3">
    <Tab className="max-h-150 overflow-auto">
      <DialogCssPropertiesPanel />
    </Tab>
  </Tabs>
</div>

### Styling hooks

- Moduix slots: `dialog-trigger`, `dialog-backdrop`, `dialog-positioner`, `dialog-content`,
  `dialog-title`, `dialog-description`, `dialog-close-trigger`, `dialog-close-icon`,
  `dialog-header`, `dialog-body`, and `dialog-footer`.
- Ark anatomy: `data-scope="dialog"` with `data-part="trigger|backdrop|positioner|content|title|description|close-trigger"`.
- State: `data-state="open|closed"`, `data-has-nested`, `data-nested`, `data-current`, and
  `data-value`.
- Runtime variables: `--layer-index` and `--nested-layer-count`.
- `Dialog.CloseIcon` is absolutely positioned against `Dialog.Content`; its placement does not
  depend on `Dialog.Header`.