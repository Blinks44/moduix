---
title: Alert
description: Inline feedback for status, validation, and system messages.
icon: Component
---

import {
  AlertActionsExample,
  AlertCssPropertiesPanel,
  AlertErrorExample,
  AlertExample,
  AlertStatusesExample,
  AlertWithIconExample,
} from '@/components/examples/alert';

## API Reference

`Alert` uses an Ark-style compound API built with the Ark `ark` factory. Public parts are
`Alert.Root`, `Alert.Indicator`, `Alert.Content`, `Alert.Title`, and `Alert.Description`.

## Choosing the right component

Use `Alert` when the message should stay inline in the page layout and participate in the normal
document flow.

- Choose **`Alert`** for inline status, validation, and system messages inside forms, panels, or page content.
- Choose **`Toast`** for transient notifications that appear outside the document flow and dismiss on their own.

## Basic

Start with `Alert.Root`, `Alert.Content`, `Alert.Title`, and `Alert.Description`. Add
`Alert.Indicator` only when the message benefits from a leading visual cue.

<Preview>
  <AlertExample />

  <Preview.Code>
    {`
      import { Alert } from "moduix";

      export function AlertDemo() {
        return (
          <Alert.Root>
            <Alert.Content>
              <Alert.Title>Update available</Alert.Title>
              <Alert.Description>
                Install the latest version when your workflow allows it.
              </Alert.Description>
            </Alert.Content>
          </Alert.Root>
        );
      }
    `}

  </Preview.Code>
</Preview>

## Install with shadcn (optional)

If you want `Alert` to become part of your own project source instead of staying in
`node_modules`, install it from the moduix GitHub registry:

<Tabs items={['npm', 'pnpm', 'bun', 'deno', 'aube']}>
  <Tab value="npm">

```bash
npx shadcn@latest add Blinks44/moduix/alert
```

  </Tab>
  <Tab value="pnpm">

```bash
pnpm dlx shadcn@latest add Blinks44/moduix/alert
```

  </Tab>
  <Tab value="bun">

```bash
bunx shadcn@latest add Blinks44/moduix/alert
```

  </Tab>
  <Tab value="deno">

```bash
deno x -A npm:shadcn@latest add Blinks44/moduix/alert
```

  </Tab>
  <Tab value="aube">

```bash
aubx shadcn@latest add Blinks44/moduix/alert
```

  </Tab>
</Tabs>

This install path also pulls in the shared moduix foundation files that `Alert` depends on, such
as styles, icons, and small utilities.

## Anatomy

```text
Alert.Root
├─ Alert.Indicator (optional)
└─ Alert.Content
   ├─ Alert.Title
   └─ Alert.Description
```

| Part                | Role                                                         |
| ------------------- | ------------------------------------------------------------ |
| `Alert.Root`        | Root slot. Applies status styles and semantics.              |
| `Alert.Indicator`   | Optional indicator slot. Sizes child SVG icons consistently. |
| `Alert.Content`     | Text content wrapper for title and description.              |
| `Alert.Title`       | Alert heading slot. Renders `h3` by default.                 |
| `Alert.Description` | Supporting message slot.                                     |

## Composition

### Recommended composition

Stay with `Alert.Root`, `Alert.Content`, `Alert.Title`, and `Alert.Description` for most inline
feedback messages.

Use `status` for intent: `neutral`, `info`, `success`, `warning`, or `error`.

Keep text, links, and extra actions inside `Alert.Content`. The component intentionally avoids
special-purpose action props or slot bags.

### When to go custom

Switch to custom composition only when the alert needs extra inline controls, local dismiss state,
or a different rendered element through Ark `asChild`.

Use `Alert.Title asChild` when the document outline needs a different heading element:

```tsx
<Alert.Title asChild>
  <h2>Billing issue</h2>
</Alert.Title>
```

### Default props

| Prop     | Default   | Values / Notes                                    |
| -------- | --------- | ------------------------------------------------- |
| `status` | `neutral` | `neutral`, `info`, `success`, `warning`, `error`  |
| `role`   | Auto      | `status` by default, `alert` for `status="error"` |

### Behavioral notes

- `Alert.Root` defaults to `role="status"` and switches to `role="alert"` for `status="error"`.
- `Alert.Title` renders `h3` by default and uses Ark `asChild` for heading polymorphism.
- `Alert.Indicator` defaults to `aria-hidden="true"`, so essential meaning should stay in the
  title or description.
- The component is intentionally presentational. It does not manage dismiss state, focus, or
  keyboard behavior for embedded actions.

## Examples

### With Icon

Place a moduix icon or custom SVG inside `Alert.Indicator`.

<Preview>
  <AlertWithIconExample />

  <Preview.Code>
    {`
      import { Alert, InfoIcon } from "moduix";

      export function AlertWithIconDemo() {
        return (
          <Alert.Root status="info">
            <Alert.Indicator>
              <InfoIcon />
            </Alert.Indicator>
            <Alert.Content>
              <Alert.Title>Workspace sync is active</Alert.Title>
              <Alert.Description>
                Changes are being synced across all connected devices.
              </Alert.Description>
            </Alert.Content>
          </Alert.Root>
        );
      }
    `}

  </Preview.Code>
</Preview>

### Statuses

Use statuses for neutral, informational, success, warning, and error states.

<Preview>
  <AlertStatusesExample />

  <Preview.Code>
    {`
      import { Alert, CheckIcon, InfoIcon } from "moduix";

      export function AlertStatusesDemo() {
        return (
          <div className="stack">
            {statuses.map((status) => (
              <Alert.Root key={status} status={status}>
                <Alert.Indicator>
                  {status === "success" ? <CheckIcon /> : <InfoIcon />}
                </Alert.Indicator>
                <Alert.Content>
                  <Alert.Title>{status}</Alert.Title>
                  <Alert.Description>Use this alert for {status} feedback.</Alert.Description>
                </Alert.Content>
              </Alert.Root>
            ))}
          </div>
        );
      }
    `}

  </Preview.Code>

  <Preview.Data>
    {`      const statuses = ["neutral", "info", "success", "warning", "error"] as const;`}

  </Preview.Data>

  <Preview.CSS>
    {`
      .stack {
        display: grid;
        width: min(38rem, 100%);
        gap: var(--spacing-3);
      }
    `}

  </Preview.CSS>
</Preview>

### Error

Use `status="error"` when the message needs assertive announcement semantics and stronger visual
emphasis.

<Preview>
  <AlertErrorExample />

  <Preview.Code>
    {`
      import { Alert, InfoIcon } from "moduix";

      export function AlertErrorDemo() {
        return (
          <Alert.Root status="error">
            <Alert.Indicator>
              <InfoIcon />
            </Alert.Indicator>
            <Alert.Content>
              <Alert.Title>Payment failed</Alert.Title>
              <Alert.Description>
                Your payment could not be processed. Check the payment method and try again.
              </Alert.Description>
            </Alert.Content>
          </Alert.Root>
        );
      }
    `}

  </Preview.Code>
</Preview>

### Custom Composition

Compose extra controls inside `Alert.Content` when the message needs a recovery path or follow-up
action. Keep dismiss state in the consuming component when a local alert should be hidden.

<Preview>
  <AlertActionsExample />

  <Preview.Code>
    {`
      import { Alert, Button, InfoIcon } from "moduix";
      import { useState } from "react";

      export function AlertActionsDemo() {
        const [visible, setVisible] = useState(true);

        if (!visible) return null;

        return (
          <Alert.Root status="warning" className="customAlert">
            <Alert.Indicator>
              <InfoIcon />
            </Alert.Indicator>
            <Alert.Content>
              <Alert.Title>Storage is almost full</Alert.Title>
              <Alert.Description>
                You are using 92% of the available storage. Archive old uploads or upgrade the plan.
              </Alert.Description>
              <div className="actions">
                <Button size="sm">Review uploads</Button>
                <Button size="sm" variant="outline" onClick={() => setVisible(false)}>
                  Dismiss
                </Button>
              </div>
            </Alert.Content>
          </Alert.Root>
        );
      }
    `}

  </Preview.Code>

  <Preview.CSS>
    {`
      .customAlert {
        --alert-bg: color-mix(in oklab, var(--color-primary) 12%, var(--color-background));
        --alert-border-color: color-mix(in oklab, var(--color-primary) 38%, transparent);
        --alert-indicator-color: var(--color-primary);
        --alert-radius: var(--radius-md);
        --alert-shadow: var(--shadow-sm);

        max-width: 32rem;
      }

      .actions {
        display: flex;
        flex-wrap: wrap;
        margin-block-start: var(--spacing-2);
        gap: var(--spacing-2);
      }
    `}

  </Preview.CSS>
</Preview>

## Styling

Use `className` on the root or parts for local styles. Override `--alert-*` CSS variables on the
root when the alert needs local theming.

Built-in statuses also read shared palette tokens:

| Status    | Shared token          | Default                      |
| --------- | --------------------- | ---------------------------- |
| `success` | `--color-success`     | `oklch(0.627 0.194 149.214)` |
| `warning` | `--color-warning`     | `oklch(0.795 0.184 86.047)`  |
| `error`   | `--color-destructive` | theme destructive            |

### CSS Properties

<div className="not-prose">
  <Tabs items={['CSS Variables']} className="mb-0 mt-3">
    <Tab className="max-h-150 overflow-auto">
      <AlertCssPropertiesPanel />
    </Tab>
  </Tabs>
</div>

### Styling hooks

- Stable `data-slot` selectors: `alert-root`, `alert-indicator`, `alert-content`, `alert-title`,
  and `alert-description`
- `Alert.Root` writes the selected status to `data-status`