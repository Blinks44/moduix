---
title: Menu
description: A trigger-anchored list of actions, links, choices, and nested commands.
icon: Component
---

import * as MenuDocs from '@/components/examples/menu';

## API Reference

<PrimitiveReference href="https://ark-ui.com/docs/components/menu" />

## Choosing the right component

Use `Menu` for trigger-anchored command lists, context menus, lightweight selections, and nested
actions.

- Choose **`Menu`** for overflow actions, row actions, and command lists opened from a button or
  context trigger.

## Basic

Start with the Ark part tree: `Menu`, `Menu.Trigger`, `Menu.Positioner`, and `Menu.Content`.
Items use stable `value` identifiers for selection and typeahead.

<Preview>
  <MenuDocs.MenuExample />

  <Preview.Code>
  <include lang="tsx">./_snippets/menu/basic.tsx#demo</include>
</Preview.Code>
  <Preview.CSS>{MenuDocs.menuExampleCss}</Preview.CSS>
  
</Preview>

## Install with shadcn

<ShadcnInstall packageName="react-menu" />

## Anatomy

```text
Menu
├─ Menu.Trigger
│  └─ Menu.Indicator
├─ Menu.ContextTrigger
├─ Overlay subtree (automatically portalled)
│  └─ Menu.Positioner
│     └─ Menu.Content
│        ├─ Menu.Arrow
│        │  └─ Menu.ArrowTip
│        ├─ Menu.Item
│        ├─ Menu.CheckboxItem
│        │  ├─ Menu.ItemIndicator
│        │  └─ Menu.ItemText
│        ├─ Menu.RadioItemGroup
│        │  └─ Menu.RadioItem
│        ├─ Menu.ItemGroup
│        │  └─ Menu.ItemGroupLabel
│        └─ Menu
│           └─ Menu.TriggerItem
```

| Part                  | Role                                                        |
| --------------------- | ----------------------------------------------------------- |
| `Menu` / `Menu.Root`  | Ark root for open state, positioning, typeahead, dismissal. |
| `Menu.RootProvider`   | Renders a menu from a `useMenu` state instance.             |
| `Menu.Trigger`        | Button trigger; use `asChild` for a custom host.            |
| `Menu.Indicator`      | Ark state-aware trigger indicator.                          |
| `Menu.ContextTrigger` | Right-click and long-press trigger for context menus.       |
| `Menu.Positioner`     | Floating layer positioner.                                  |
| `Menu.Content`        | Popup surface with menu role and item collection.           |
| `Menu.Item`           | Command row.                                                |
| `Menu.CheckboxItem`   | Boolean option item.                                        |
| `Menu.RadioItemGroup` | Exclusive value group for radio items.                      |
| `Menu.RadioItem`      | Exclusive option item.                                      |
| `Menu.TriggerItem`    | Row that opens a nested `Menu`.                             |
| `Menu.ItemIndicator`  | Check indicator for checkbox and radio rows.                |

## Composition

The root portals `Positioner` automatically. Use `portalled={false}` for inline rendering and `portalRef` for a custom container; structural parts remain explicit and styleable.

### Recommended composition

Keep popup structure explicit. `Menu.Content` is the actual Ark content part; it no longer hides
`Menu.Positioner`.

```tsx
<Menu positioning={{ placement: 'bottom-start', gutter: 8 }}>
  <Menu.Trigger asChild>
    <Button>
      Actions
      <Menu.Indicator />
    </Button>
  </Menu.Trigger>
  <Menu.Positioner>
    <Menu.Content className="menu-content">
      <Menu.Item value="edit">Edit</Menu.Item>
    </Menu.Content>
  </Menu.Positioner>
</Menu>
```

### When to go custom

Use `asChild` when the trigger or item must render as another semantic element. For links, put
`asChild` on `Menu.Item` and make the anchor the single child so Ark can apply the correct ARIA,
focus, and keyboard behavior.

Nested menus are regular `Menu` roots inside the parent content. The nested root uses
`Menu.TriggerItem`, then its own `Menu.Positioner` and `Menu.Content`.

### Default props

| Surface                           | Default | Notes                                                  |
| --------------------------------- | ------- | ------------------------------------------------------ |
| `Menu` / `Menu.Root`              | —       | No additional behavioral defaults; Ark defaults apply. |
| `Menu.CheckboxItem` / `RadioItem` | `start` | `indicator` controls the indicator column position.    |

### Behavioral notes

- `open`, `defaultOpen`, `onOpenChange(details)`, `highlightedValue`,
  `onHighlightChange(details)`, `onSelect(details)`, `closeOnSelect`, `ids`, `lazyMount`,
  `unmountOnExit`, and `present` follow Ark directly.
- Use `value`, not a custom `id`, for menu item identity.
- `Menu.CheckboxItem.onCheckedChange` receives the next boolean checked state.
- `Menu.RadioItemGroup.onValueChange` receives `details.value`.
- `Menu.RootProvider` must be used with a `useMenu` instance instead of rendering a second `Menu`
  root for the same state.
- Roots portal `Menu.Positioner` automatically. Use `portalled={false}` to keep a menu within a
  dialog or scroll container, and pair it with `positioning={{ strategy: 'fixed', hideWhenDetached: true }}`
  when the container scrolls.
- `Menu.Indicator` mirrors Ark's indicator part. `Menu.TriggerIcon`, `Menu.TriggerItemIcon`,
  `Menu.ItemShortcut`, and item text helpers are moduix leaf sugar only.

## Examples

### Checkbox Items

Checkbox items keep boolean state while preserving menu navigation and typeahead.

<Preview>
  <MenuDocs.CheckboxItemsMenuExample />
  <Preview.Code>
    <include lang="tsx">./_snippets/menu/checkbox-items.tsx#demo</include>
  </Preview.Code>
  <Preview.CSS>{MenuDocs.menuExampleCss}</Preview.CSS>
</Preview>

### Context Menu

Use `Menu.ContextTrigger` for right-click and long-press context menus.

<Preview>
  <MenuDocs.ContextMenuExample />
  <Preview.Code>
    <include lang="tsx">./_snippets/menu/context-menu.tsx#demo</include>
  </Preview.Code>
  <Preview.CSS>{MenuDocs.menuExampleCss}</Preview.CSS>
</Preview>

### Controlled

Control `open` with `onOpenChange(details)` when another control needs to drive the menu.

<Preview>
  <MenuDocs.ControlledMenuExample />
  <Preview.Code>
    <include lang="tsx">./_snippets/menu/controlled.tsx#demo</include>
  </Preview.Code>
  <Preview.CSS>{MenuDocs.menuExampleCss}</Preview.CSS>
</Preview>

### Grouping

Use `Menu.ItemGroup` and `Menu.ItemGroupLabel` to organize related menu rows.

<Preview>
  <MenuDocs.GroupMenuExample />
  <Preview.Code>
    <include lang="tsx">./_snippets/menu/grouping.tsx#demo</include>
  </Preview.Code>
  <Preview.CSS>{MenuDocs.menuExampleCss}</Preview.CSS>
</Preview>

### Indicator Placement

moduix adds `indicator="start" | "end" | "none"` on checkbox and radio rows for layout control.

<Preview>
  <MenuDocs.IndicatorRightMenuExample />
  <Preview.Code>
    <include lang="tsx">./_snippets/menu/indicator-placement.tsx#demo</include>
  </Preview.Code>
  <Preview.CSS>{MenuDocs.menuExampleCss}</Preview.CSS>
</Preview>

### Item Context

Use `Menu.ItemContext` for inline access to item state such as `highlighted`.

<Preview>
  <MenuDocs.ItemContextMenuExample />
  <Preview.Code>
    <include lang="tsx">./_snippets/menu/item-context.tsx#demo</include>
  </Preview.Code>
  <Preview.CSS>{MenuDocs.menuExampleCss}</Preview.CSS>
</Preview>

### Lazy Mounted Context Menu

Use Ark `lazyMount` and `unmountOnExit` when context menu content should mount only while the menu
is present.

<Preview>
  <MenuDocs.ContextLazyMountMenuExample />
  <Preview.Code>
    <include lang="tsx">./_snippets/menu/lazy-mounted-context-menu.tsx#demo</include>
  </Preview.Code>
  <Preview.CSS>{MenuDocs.menuExampleCss}</Preview.CSS>
</Preview>

### Links

Use `Menu.Item asChild` for anchors so the link itself receives menu semantics.

<Preview>
  <MenuDocs.LinkItemsMenuExample />
  <Preview.Code>
    <include lang="tsx">./_snippets/menu/links.tsx#demo</include>
  </Preview.Code>
  <Preview.CSS>{MenuDocs.menuExampleCss}</Preview.CSS>
</Preview>

### Menu in Dialog

Keep the popup inside a dialog or scroll container with `portalled={false}`. Use fixed positioning
and `hideWhenDetached` when the container scrolls.

<Preview>
  <MenuDocs.MenuInDialogExample />
  <Preview.Code>
    <include lang="tsx">./_snippets/menu/menu-in-dialog.tsx#demo</include>
  </Preview.Code>
  <Preview.CSS>{MenuDocs.menuExampleCss}</Preview.CSS>
</Preview>

### Multiple Triggers

Pass a `value` to each `Menu.Trigger` when one menu is shared across multiple trigger elements.

<Preview>
  <MenuDocs.MultipleTriggersMenuExample />
  <Preview.Code>
    <include lang="tsx">./_snippets/menu/multiple-triggers.tsx#demo</include>
  </Preview.Code>
  <Preview.CSS>{MenuDocs.menuExampleCss}</Preview.CSS>
</Preview>

### Nested

Render a nested `Menu` and open it from `Menu.TriggerItem`.

<Preview>
  <MenuDocs.NestedMenuExample />
  <Preview.Code>
    <include lang="tsx">./_snippets/menu/nested.tsx#demo</include>
  </Preview.Code>
  <Preview.CSS>{MenuDocs.menuExampleCss}</Preview.CSS>
</Preview>

### Radio Items

Use `Menu.RadioItemGroup` for one selected value inside a menu.

<Preview>
  <MenuDocs.RadioItemsMenuExample />
  <Preview.Code>
    <include lang="tsx">./_snippets/menu/radio-items.tsx#demo</include>
  </Preview.Code>
  <Preview.CSS>{MenuDocs.menuExampleCss}</Preview.CSS>
</Preview>

### Root Provider

Use `useMenu` and `Menu.RootProvider` when code outside the rendered tree needs access to the menu API.

<Preview>
  <MenuDocs.RootProviderMenuExample />
  <Preview.Code>
    <include lang="tsx">./_snippets/menu/root-provider.tsx#demo</include>
  </Preview.Code>
  <Preview.CSS>{MenuDocs.menuExampleCss}</Preview.CSS>
</Preview>

### Select Event

Use root `onSelect(details)` when the menu should react to any item selection.

<Preview>
  <MenuDocs.SelectEventMenuExample />
  <Preview.Code>
    <include lang="tsx">./_snippets/menu/select-event.tsx#demo</include>
  </Preview.Code>
  <Preview.CSS>{MenuDocs.menuExampleCss}</Preview.CSS>
</Preview>

### Advanced Customization

Bypass the default indicator and leaf helpers when a menu needs fully custom item content, an arrow,
or a link host; the Ark structural parts stay explicit.

<Preview>
  <MenuDocs.AdvancedCustomizationMenuExample />
  <Preview.Code>
    <include lang="tsx">./_snippets/menu/advanced-customization.tsx#demo</include>
  </Preview.Code>
  <Preview.CSS>{MenuDocs.menuExampleCss}</Preview.CSS>
</Preview>

## Styling

### CSS Properties

<div className="not-prose">
  <Tabs items={['CSS Variables']}>
    <Tab value="CSS Variables">
      <div style={{ maxHeight: '28rem', overflow: 'auto' }}>
        <MenuDocs.MenuCssPropertiesPanel />
      </div>
    </Tab>
  </Tabs>
</div>

### Styling hooks

- Ark attributes: `[data-scope='menu']`, `[data-part='trigger']`, `[data-part='indicator']`,
  `[data-part='content']`, `[data-part='item']`, `[data-part='option-item']`,
  `[data-type='checkbox' | 'radio']`, `[data-state='open' | 'closed' | 'checked' | 'unchecked']`,
  `[data-highlighted]`, `[data-disabled]`, `[data-placement]`, and `[data-side]`.
- Ark variables: `--reference-width`, `--available-width`, `--available-height`,
  `--transform-origin`, `--layer-index`, `--arrow-size`, and `--arrow-background`.
- moduix hooks: `data-slot="menu-trigger"`, `menu-content`, `menu-item`, `menu-checkbox-item`,
  `menu-radio-item`, `menu-trigger-item`, `menu-item-indicator`, and `menu-item-shortcut`.