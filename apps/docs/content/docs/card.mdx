---
title: Card
description: A composable surface for content related to a single subject.
icon: Component
---

import {
  CardAsLinkExample,
  CardAvatarExample,
  CardCssPropertiesPanel,
  CardCustomStylingExample,
  CardExample,
  CardFormExample,
  CardHorizontalExample,
  CardLinkWithActionsExample,
  CardSizesExample,
  CardVariantsExample,
  CardWithActionExample,
  CardWithImageExample,
} from '@/components/examples/card';

## API Reference

Ark UI does not ship a dedicated card primitive. Moduix builds this component with the official
[Ark factory](https://ark-ui.com/docs/guides/composition#the-ark-factory), follows Ark's
[composition](https://ark-ui.com/docs/guides/composition), [styling](https://ark-ui.com/docs/guides/styling), and
[ref](https://ark-ui.com/docs/guides/ref) guidance.

## Basic

Use the callable `<Card />` root with namespaced parts for the standard card composition.

<Preview>
  <CardExample />

  <Preview.Code>
    {`
      import { Button, Card } from "@moduix/react";

      export function CardDemo() {
        return (
          <Card className="card">
            <Card.Header>
              <Card.Title>{content.title}</Card.Title>
              <Card.Description>{content.description}</Card.Description>
            </Card.Header>
            <Card.Body>{content.summary}</Card.Body>
            <Card.Footer>
              <Button variant="outline">View log</Button>
              <Button>Promote release</Button>
            </Card.Footer>
          </Card>
        );
      }
    `}

  </Preview.Code>

<Preview.CSS>
  {`
      .card {
        width: min(24rem, calc(100vw - var(--spacing-8)));
      }
    `}
</Preview.CSS>

  <Preview.Data>
    {`
      const content = {
        title: "Release health",
        description: "Summary for the current production rollout.",
        summary: "98.4% successful sessions · 12 checks passed",
      };
    `}
  </Preview.Data>
</Preview>

## Install with shadcn (optional)

<Tabs items={['npm', 'pnpm', 'bun', 'deno', 'aube']}>
  <Tab value="npm">

```bash
npx shadcn@latest add Blinks44/moduix/react-card
```

  </Tab>
  <Tab value="pnpm">

```bash
pnpm dlx shadcn@latest add Blinks44/moduix/react-card
```

  </Tab>
  <Tab value="bun">

```bash
bunx shadcn@latest add Blinks44/moduix/react-card
```

  </Tab>
  <Tab value="deno">

```bash
deno x -A npm:shadcn@latest add Blinks44/moduix/react-card
```

  </Tab>
  <Tab value="aube">

```bash
aubx shadcn@latest add Blinks44/moduix/react-card
```

  </Tab>
</Tabs>

## Anatomy

```text
Card.Root
├─ Card.Header
│  ├─ Card.Title
│  ├─ Card.Description
│  └─ Card.Action (optional moduix sugar)
├─ Card.Body
└─ Card.Footer (optional)
```

`Card` and `Card.Root` are the same root component.

| Part                 | Role                                                                          |
| -------------------- | ----------------------------------------------------------------------------- |
| `Card` / `Card.Root` | Root surface with `size`, `variant`, and `asChild`.                           |
| `Card.Header`        | Header layout for title, description, and an optional trailing action.        |
| `Card.Body`          | Main content area.                                                            |
| `Card.Footer`        | Wrapping action or metadata row.                                              |
| `Card.Title`         | Heading part. Defaults to `h3`; use `asChild` for another heading level.      |
| `Card.Description`   | Supporting text.                                                              |
| `Card.Action`        | moduix header-side layout slot.                                               |
| `Card.Link`          | moduix overlay link for a navigable card that also contains separate actions. |

## Composition

### Recommended composition

Use `<Card />`, `Card.Header`, `Card.Title`, `Card.Description`, `Card.Body`, and `Card.Footer`.
The root defaults to `size="md"` and `variant="outline"`.

### When to go custom

Use `asChild` when one semantic child should own a part's DOM node. Ark composition requires exactly
one child. A linked root must not contain nested buttons, inputs, or links.

Use `Card.Link` inside the title when the card needs an overlay navigation target and also contains
independent controls. `Card.Action` only provides layout and does not create ARIA relationships.

### Default props

| Part        | Prop      | Default     | Values                                |
| ----------- | --------- | ----------- | ------------------------------------- |
| `Card.Root` | `size`    | `'md'`      | `'sm' \| 'md' \| 'lg'`                |
| `Card.Root` | `variant` | `'outline'` | `'elevated' \| 'outline' \| 'subtle'` |

### Behavioral notes

- The root renders a presentational `div` by default and forwards its ref to the rendered element.
- All parts accept native props, `className`, `style`, and Ark factory `asChild`.
- `Card.Title` renders `h3` by default.
- Card has no managed state, callbacks, keyboard behavior, provider, context, or hidden input.
- `Card.Body` resets first and last direct-child margins.

## Examples

### Variants

Use the Chakra-aligned variants to communicate surface hierarchy.

<Preview>
  <CardVariantsExample />

  <Preview.Code>
    {`
      import { Card } from "@moduix/react";

      export function CardVariantsDemo() {
        return (
          <div className="cards">
            {variants.map((variant) => (
              <Card key={variant} className="card" variant={variant}>
                <Card.Header>
                  <Card.Title>{variant}</Card.Title>
                  <Card.Description>{descriptions[variant]}</Card.Description>
                </Card.Header>
                <Card.Body>Use variants to communicate surface hierarchy.</Card.Body>
              </Card>
            ))}
          </div>
        );
      }
    `}

  </Preview.Code>

<Preview.CSS>
  {`
      .cards {
        display: grid;
        gap: var(--spacing-4);
        width: min(28rem, calc(100vw - var(--spacing-8)));
      }

      .card {
        width: 100%;
      }
    `}

</Preview.CSS>

  <Preview.Data>
    {`
      const variants = ["elevated", "outline", "subtle"] as const;
      const descriptions = {
        elevated: "Raised above nearby content.",
        outline: "Separated with a visible border.",
        subtle: "Grouped with a muted background.",
      };
    `}
  </Preview.Data>
</Preview>

### Sizes

Use `sm`, `md`, and `lg` to change card padding and title scale.

<Preview>
  <CardSizesExample />

  <Preview.Code>
    {`
      import { Card } from "@moduix/react";

      export function CardSizesDemo() {
        return (
          <div className="cards">
            {sizes.map((size) => (
              <Card key={size} className="card" size={size}>
                <Card.Header>
                  <Card.Title>Card {size}</Card.Title>
                  <Card.Description>{descriptions[size]}</Card.Description>
                </Card.Header>
                <Card.Body>Shared content with size-specific density.</Card.Body>
              </Card>
            ))}
          </div>
        );
      }
    `}

  </Preview.Code>

<Preview.CSS>
  {`
      .cards {
        display: grid;
        gap: var(--spacing-4);
        width: min(28rem, calc(100vw - var(--spacing-8)));
      }

      .card {
        width: 100%;
      }
    `}

</Preview.CSS>

  <Preview.Data>
    {`
      const sizes = ["sm", "md", "lg"] as const;
      const descriptions = {
        sm: "Compact supporting content.",
        md: "Default product content.",
        lg: "Prominent standalone content.",
      };
    `}
  </Preview.Data>
</Preview>

### Within Form

Compose the root as a form when the card groups related fields and actions.

<Preview>
  <CardFormExample />

  <Preview.Code>
    {`
      import { Button, Card, Input } from "@moduix/react";

      export function CardFormDemo() {
        return (
          <Card asChild>
            <form>
              <Card.Header>
                <Card.Title>{copy.title}</Card.Title>
                <Card.Description>{copy.description}</Card.Description>
              </Card.Header>
              <Card.Body>
                {fields.map((field) => (
                  <label key={field.name}>
                    {field.label}
                    <Input name={field.name} />
                  </label>
                ))}
              </Card.Body>
              <Card.Footer>
                <Button type="reset" variant="outline">Cancel</Button>
                <Button type="submit">Create account</Button>
              </Card.Footer>
            </form>
          </Card>
        );
      }
    `}

  </Preview.Code>

  <Preview.CSS>
    {`
      form [data-slot="card-body"] {
        display: grid;
        gap: var(--spacing-3);
      }

      form label {
        display: grid;
        gap: var(--spacing-1);
      }
    `}

  </Preview.CSS>

  <Preview.Data>
    {`
      const copy = {
        title: "Create account",
        description: "Enter the contact details for the new member.",
      };
      const fields = [
        { label: "First name", name: "firstName" },
        { label: "Last name", name: "lastName" },
      ];
    `}
  </Preview.Data>
</Preview>

### With Image

Place media before the named parts when it should span the card width.

<Preview>
  <CardWithImageExample />

  <Preview.Code>
    {`
      import { Button, Card } from "@moduix/react";

      export function CardWithImageDemo() {
        return (
          <Card>
            <img className="image" src={product.image} alt={product.imageAlt} />
            <Card.Header>
              <Card.Title>{product.title}</Card.Title>
              <Card.Description>{product.description}</Card.Description>
            </Card.Header>
            <Card.Body>{product.capacity}% allocated</Card.Body>
            <Card.Footer>
              <Button variant="outline">Open report</Button>
            </Card.Footer>
          </Card>
        );
      }
    `}

  </Preview.Code>

<Preview.CSS>
  {`
      .image {
        aspect-ratio: 16 / 9;
        display: block;
        width: 100%;
        object-fit: cover;
      }
    `}
</Preview.CSS>

  <Preview.Data>
    {`
      const product = {
        title: "Warehouse capacity",
        description: "North region allocation for the next planning cycle.",
        capacity: 72,
        image: "/warehouse.jpg",
        imageAlt: "A warehouse with neatly stacked delivery boxes.",
      };
    `}
  </Preview.Data>
</Preview>

### Horizontal

Use consumer layout CSS to create a horizontal card without adding a presentation-specific prop.

<Preview>
  <CardHorizontalExample />

  <Preview.Code>
    {`
      import { Badge, Button, Card } from "@moduix/react";

      export function HorizontalCardDemo() {
        return (
          <Card className="horizontalCard">
            <img className="image" src={item.image} alt={item.imageAlt} />
            <div className="content">
              <Card.Header>
                <Card.Title>{item.title}</Card.Title>
                <Card.Description>{item.description}</Card.Description>
              </Card.Header>
              <Card.Body><Badge variant="secondary">{item.badge}</Badge></Card.Body>
              <Card.Footer><Button>{item.action}</Button></Card.Footer>
            </div>
          </Card>
        );
      }
    `}

  </Preview.Code>

  <Preview.CSS>
    {`
      .horizontalCard {
        display: grid;
        grid-template-columns: minmax(10rem, 0.8fr) minmax(0, 1.2fr);
        overflow: hidden;
      }

      .image {
        width: 100%;
        height: 100%;
        object-fit: cover;
      }

      .content {
        display: flex;
        min-width: 0;
        flex-direction: column;
      }
    `}

  </Preview.CSS>

  <Preview.Data>
    {`
      const item = {
        title: "The perfect latte",
        description: "Espresso balanced with steamed milk and a light foam.",
        badge: "Hot",
        action: "Buy latte",
        image: "/latte.jpg",
        imageAlt: "Caffè latte in a ceramic cup.",
      };
    `}
  </Preview.Data>
</Preview>

### With Avatar

Compose profile media inside the header when identity is the card's subject.

<Preview>
  <CardAvatarExample />

  <Preview.Code>
    {`
      import { Button, Card } from "@moduix/react";

      export function CardAvatarDemo() {
        return (
          <Card>
            <Card.Header>
              <div className="profile">
                <span aria-hidden="true" className="avatar">{person.initials}</span>
                <div>
                  <Card.Title>{person.name}</Card.Title>
                  <Card.Description>{person.handle}</Card.Description>
                </div>
              </div>
            </Card.Header>
            <Card.Body>{person.message}</Card.Body>
            <Card.Footer>
              <Button variant="outline">Decline</Button>
              <Button>Approve</Button>
            </Card.Footer>
          </Card>
        );
      }
    `}

  </Preview.Code>

  <Preview.CSS>
    {`
      .profile {
        display: flex;
        align-items: center;
        gap: var(--spacing-3);
      }

      .avatar {
        display: inline-grid;
        width: 3rem;
        height: 3rem;
        place-items: center;
        border-radius: var(--radius-full);
        background: var(--color-primary);
        color: var(--color-primary-foreground);
      }
    `}

  </Preview.CSS>

  <Preview.Data>
    {`
      const person = {
        initials: "NF",
        name: "Nate Foss",
        handle: "@natefoss",
        message: "Nate has requested to join your team.",
      };
    `}
  </Preview.Data>
</Preview>

### Header Action

`Card.Action` is moduix sugar for a compact trailing status or control.

<Preview>
  <CardWithActionExample />

  <Preview.Code>
    {`
      import { Badge, Card } from "@moduix/react";

      export function CardActionDemo() {
        return (
          <Card>
            <Card.Header>
              <Card.Title>{incident.title}</Card.Title>
              <Card.Description>{incident.description}</Card.Description>
              <Card.Action><Badge variant="secondary">{incident.status}</Badge></Card.Action>
            </Card.Header>
            <Card.Body>{incident.summary}</Card.Body>
          </Card>
        );
      }
    `}

  </Preview.Code>

<Preview.CSS>
  {`
      [data-slot="card-action"] {
        align-self: start;
      }
    `}
</Preview.CSS>

  <Preview.Data>
    {`
      const incident = {
        title: "Incident response",
        description: "Owner rotation and escalation readiness.",
        status: "Stable",
        summary: "18 min median response · 99.97% service uptime",
      };
    `}
  </Preview.Data>
</Preview>

### Root as Link

Use `asChild` when the entire card is one navigation target and contains no nested controls.

<Preview>
  <CardAsLinkExample />

  <Preview.Code>
    {`
      import { Card } from "@moduix/react";

      export function LinkedCardDemo() {
        return (
          <Card asChild>
            <a href={report.href}>
              <Card.Header>
                <Card.Title>{report.title}</Card.Title>
                <Card.Description>{report.description}</Card.Description>
              </Card.Header>
              <Card.Body>{report.summary}</Card.Body>
            </a>
          </Card>
        );
      }
    `}

  </Preview.Code>

<Preview.CSS>
  {`
      a[data-slot="card-root"] {
        text-decoration: none;
      }
    `}
</Preview.CSS>

  <Preview.Data>
    {`
      const report = {
        href: "/reports/release-health",
        title: "Release health",
        description: "Summary for the current production rollout.",
        summary: "98.4% successful sessions",
      };
    `}
  </Preview.Data>
</Preview>

### Overlay Link

Use `Card.Link` when navigation and independent actions must coexist.

<Preview>
  <CardLinkWithActionsExample />

  <Preview.Code>
    {`
      import { Button, Card } from "@moduix/react";

      export function OverlayLinkCardDemo() {
        return (
          <Card>
            <Card.Header>
              <Card.Title>
                <Card.Link href={incident.href}>{incident.title}</Card.Link>
              </Card.Title>
              <Card.Description>{incident.description}</Card.Description>
              <Card.Action>
                <Button variant="outline" size="sm">Acknowledge</Button>
              </Card.Action>
            </Card.Header>
            <Card.Body>{incident.summary}</Card.Body>
          </Card>
        );
      }
    `}

  </Preview.Code>

<Preview.CSS>
  {`
      [data-slot="card-link"]:focus-visible::after {
        outline-color: var(--color-ring);
      }
    `}
</Preview.CSS>

  <Preview.Data>
    {`
      const incident = {
        href: "/incidents/response",
        title: "Incident response",
        description: "Owner rotation and escalation readiness.",
        summary: "18 min median response",
      };
    `}
  </Preview.Data>
</Preview>

### Custom Styling

Override public variables or target stable part hooks without changing the composition.

<Preview>
  <CardCustomStylingExample />

  <Preview.Code>
    {`
      import { Card } from "@moduix/react";

      export function CustomCardDemo() {
        return (
          <Card className="customCard">
            <Card.Header>
              <Card.Title>{metric.title}</Card.Title>
              <Card.Description>{metric.description}</Card.Description>
            </Card.Header>
            <Card.Body>{metric.value}%</Card.Body>
          </Card>
        );
      }
    `}

  </Preview.Code>

<Preview.CSS>
  {`
      .customCard {
        --card-bg: color-mix(in oklab, var(--color-card) 94%, var(--color-primary));
        --card-border-color: color-mix(in oklab, var(--color-primary) 35%, var(--color-border));
        --card-radius: var(--radius-md);
        --card-shadow: var(--shadow-md);
      }
    `}
</Preview.CSS>

  <Preview.Data>
    {`
      const metric = {
        title: "System load",
        description: "Aggregated worker utilization across the current batch.",
        value: 64,
      };
    `}
  </Preview.Data>
</Preview>

## Styling

### CSS Properties

<div className="not-prose">
  <Tabs items={['CSS Variables']} className="mb-0 mt-3">
    <Tab className="max-h-150 overflow-auto">
      <CardCssPropertiesPanel />
    </Tab>
  </Tabs>
</div>

### Styling hooks

- Every part exposes `data-scope="card"` and a matching `data-part`.
- Stable moduix hooks are `data-slot="card-root"`, `card-header`, `card-body`, `card-footer`,
  `card-title`, `card-description`, `card-action`, and `card-link`.
- The root exposes `data-size="sm | md | lg"` and
  `data-variant="elevated | outline | subtle"`.
- Card has no interactive state attributes because it does not own state or behavior.