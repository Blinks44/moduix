---
title: Toggle
description: A two-state button for compact on/off actions and saved states.
icon: Component
---

import * as ToggleDocs from '@/components/examples/toggle';

## API Reference

<PrimitiveReference href="https://ark-ui.com/docs/components/toggle" />

## Choosing the right component

Use `Toggle` when the control is still a button that exposes a pressed state.

- Choose **`Toggle`** for favorites, saved states, formatting actions, and compact toolbar actions.
- Choose **`Switch`** for settings-like on/off controls that read as a preference row.
- Choose **`Checkbox`** for submitted list or form selections.
- Choose **`ToggleGroup`** when several toggles should share one selection model.

## Basic

Start with `Toggle` alone. It is the short form for `Toggle.Root` and keeps Ark pressed-state behavior intact.

<Preview>
  <ToggleDocs.ToggleExample />

<Preview.Code>
  <include lang="tsx">./_snippets/toggle/basic.tsx#demo</include>
</Preview.Code>

  <Preview.CSS>{ToggleDocs.toggleBasicCss}</Preview.CSS>
  
</Preview>

## Install with shadcn

<ShadcnInstall packageName="react-toggle" />

## Anatomy

```text
Toggle / Toggle.Root
└─ Toggle.Indicator (optional)
```

| Part                     | Role                                                                              |
| ------------------------ | --------------------------------------------------------------------------------- |
| `Toggle` / `Toggle.Root` | Two-state button root that handles pressed, focus, disabled, and `asChild` state. |
| `Toggle.Indicator`       | Renders pressed content and optional `fallback` content when the toggle is off.   |

## Composition

### Recommended composition

Use `Toggle` for the standard root. It forwards refs to the Ark button part and supports
`pressed`, `defaultPressed`, `onPressedChange(pressed)`, `disabled`, `asChild`, and regular button
attributes.

`Toggle.Indicator` is optional. Use it when the visible icon or label should change based on
whether the toggle is pressed.

### When to go custom

Use `asChild` when another semantic button must own the rendered element. Keep the child a single
button-like element so Ark can merge the required props and behavior onto it.

If a custom child part needs direct access to Ark toggle state, import `useToggleContext()` or
`useToggle()` from `@ark-ui/react/toggle`. moduix keeps the visual parts public and leaves the
advanced state helpers to Ark.

Use `ToggleGroup` instead of manually coordinating several standalone toggles when multiple pressed
buttons belong to one shared choice set.

### Default props

| Prop      | Default   | Notes                                                   |
| --------- | --------- | ------------------------------------------------------- |
| `variant` | `default` | moduix visual treatment: `default`, `outline`, `ghost`. |
| `size`    | `md`      | moduix spacing or icon-only geometry.                   |

### Behavioral notes

- Icon-only toggles need an accessible name such as `aria-label="Favorite"`.
- `disabled` keeps the current visual state but prevents interaction.
- Ark writes `data-state="on" | "off"`, `data-pressed`, and `data-disabled` for styling.
- Ark Toggle does not expose `RootProvider` or a hidden form input.
- legacy `render` and `nativeButton` are not part of the Ark-backed API; use `asChild`.

## Examples

### Controlled

Control `pressed` from React state when the toggle participates in app state or analytics.

<Preview>
  <ToggleDocs.ControlledToggleExample />

<Preview.Code>
  <include lang="tsx">./_snippets/toggle/controlled.tsx#demo</include>
</Preview.Code>

  <Preview.CSS>{ToggleDocs.toggleStackCss}</Preview.CSS>
  
</Preview>

### Disabled

Use `disabled` to freeze interaction while preserving the current pressed state.

<Preview>
  <ToggleDocs.ToggleDisabledExample />

<Preview.Code>
  <include lang="tsx">./_snippets/toggle/disabled.tsx#demo</include>
</Preview.Code>

  <Preview.CSS>{ToggleDocs.toggleRowCss}</Preview.CSS>
  
</Preview>

### Indicator

Use `Toggle.Indicator` when pressed and unpressed states need different inline content.

<Preview>
  <ToggleDocs.ToggleIndicatorExample />

<Preview.Code>
  <include lang="tsx">./_snippets/toggle/indicator.tsx#demo</include>
</Preview.Code>

  <Preview.CSS>{ToggleDocs.toggleBasicCss}</Preview.CSS>
  
</Preview>

### Variants

Use `variant` to match surrounding UI density and emphasis.

<Preview>
  <ToggleDocs.ToggleVariantsExample />

<Preview.Code>
  <include lang="tsx">./_snippets/toggle/variants.tsx#demo</include>
</Preview.Code>

  <Preview.CSS>{ToggleDocs.toggleRowCss}</Preview.CSS>
  
</Preview>

### Sizes

Use text sizes for labeled toggles and icon sizes for denser action rows.

<Preview>
  <ToggleDocs.ToggleSizesExample />

<Preview.Code>
  <include lang="tsx">./_snippets/toggle/sizes.tsx#demo</include>
</Preview.Code>

  <Preview.CSS>{ToggleDocs.toggleRowCss}</Preview.CSS>
  
</Preview>

### Icons

Pass icons as regular children. Use icon-only sizes for compact actions.

<Preview>
  <ToggleDocs.ToggleIconExample />

<Preview.Code>
  <include lang="tsx">./_snippets/toggle/icons.tsx#demo</include>
</Preview.Code>

  <Preview.CSS>{ToggleDocs.toggleRowCss}</Preview.CSS>
  
</Preview>

### asChild

Use `asChild` when another semantic button should receive the Ark toggle behavior.

<Preview>
  <ToggleDocs.ToggleAsChildExample />

<Preview.Code>
  <include lang="tsx">./_snippets/toggle/aschild.tsx#demo</include>
</Preview.Code>

  <Preview.CSS>{ToggleDocs.toggleAsChildCss}</Preview.CSS>
  
</Preview>

### Custom Styling

Layer custom visuals through `className` and `--toggle-*` variables when the structure stays the same.

<Preview>
  <ToggleDocs.CustomStylesToggleExample />

<Preview.Code>
  <include lang="tsx">./_snippets/toggle/custom-styling.tsx#demo</include>
</Preview.Code>

  <Preview.CSS>{ToggleDocs.toggleCustomCss}</Preview.CSS>
  
</Preview>

## Styling

`Toggle` exposes a styled root, an optional indicator part, and the full `--toggle-*` token surface
for variants, sizes, focus ring, spacing, and icon sizing.

### CSS Properties

<div className="not-prose">
  <Tabs items={['CSS Variables']} className="mb-0 mt-3">
    <Tab className="max-h-150 overflow-auto">
      <ToggleDocs.ToggleCssPropertiesPanel />
    </Tab>
  </Tabs>
</div>

### Styling hooks

- `data-slot="toggle-root"` is the stable selector on the root.
- `data-slot="toggle-indicator"` is the stable selector on `Toggle.Indicator`.
- `data-scope="toggle"` and `data-part="root" | "indicator"` come from Ark UI.
- `data-state="on" | "off"` reflects pressed state on root and indicator.
- `data-pressed` is present when the toggle is pressed.
- `data-variant` reflects the moduix visual variant.
- `data-size` reflects the moduix size, including `icon-sm`, `icon-md`, and `icon-lg`.
- `data-disabled` drives disabled styles; Ark also preserves the native `disabled` attribute.
- Descendant `svg` elements are sized through `--toggle-icon-size`.