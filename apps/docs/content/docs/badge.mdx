---
title: Badge
description: A compact status label for short metadata such as state, category, and count.
icon: Component
---

import {
  BadgeAsChildExample,
  BadgeCssPropertiesPanel,
  BadgeExample,
  BadgeTruncatedExample,
  BadgeVariantsExample,
  BadgeWithDotExample,
  BadgeWithIconExample,
  CustomBadgeExample,
} from '@/components/examples/badge';

## API Reference

Ark UI does not ship a dedicated badge primitive. Moduix builds this component with the official
[Ark factory](https://ark-ui.com/docs/guides/composition#the-ark-factory), follows Ark's
[composition](https://ark-ui.com/docs/guides/composition), [styling](https://ark-ui.com/docs/guides/styling), and
[ref](https://ark-ui.com/docs/guides/ref) guidance.

## Choosing the right component

Use `Badge` when the UI is showing metadata about something else rather than the selected value
itself.

- Choose **`Badge`** for short metadata such as status, category, version, or count.
- Choose **`Tag`** when the UI is showing selected values, filters, assignments, or labels that belong to the current state of the interface.

## Basic

Start with `Badge` only. Add `variant` when the default tone is not enough, and add `Badge.Dot`
only when the status needs a grouped indicator.

<Preview>
  <BadgeExample />

  <Preview.Code>
    {`
      import { Badge } from "@moduix/react";

      export function BadgeDemo() {
        return (
          <div className="badge-demo-basic">
            <Badge>{label}</Badge>
          </div>
        );
      }
    `}

  </Preview.Code>

  <Preview.CSS>
    {`
      .badge-demo-basic {
        display: inline-flex;
      }
    `}

  </Preview.CSS>

  <Preview.Data>
    {`
      const label = "New";
    `}

  </Preview.Data>
</Preview>

## Install with shadcn (optional)

If you want `Badge` to become part of your own project source instead of staying in
`node_modules`, install it from the moduix GitHub registry:

<Tabs items={['npm', 'pnpm', 'bun', 'deno', 'aube']}>
  <Tab value="npm">

```bash
npx shadcn@latest add Blinks44/moduix/react-badge
```

  </Tab>
  <Tab value="pnpm">

```bash
pnpm dlx shadcn@latest add Blinks44/moduix/react-badge
```

  </Tab>
  <Tab value="bun">

```bash
bunx shadcn@latest add Blinks44/moduix/react-badge
```

  </Tab>
  <Tab value="deno">

```bash
deno x -A npm:shadcn@latest add Blinks44/moduix/react-badge
```

  </Tab>
  <Tab value="aube">

```bash
aubx shadcn@latest add Blinks44/moduix/react-badge
```

  </Tab>
</Tabs>

This install path also pulls in the shared moduix foundation files that `Badge` depends on, such
as styles, icons, and small utilities.

## Anatomy

`Badge` keeps the public surface small and exposes only the visible parts.

```text
Badge.Root
├─ content
├─ Badge.Dot (optional)
└─ svg icon (optional)
```

| Part                   | Role                                                                                 |
| ---------------------- | ------------------------------------------------------------------------------------ |
| `Badge` / `Badge.Root` | Root slot. Applies variant, border, spacing, and text truncation.                    |
| `Badge.Dot`            | Optional indicator slot. Renders a circular marker that inherits current text color. |

## Composition

### Recommended composition

Stay with `Badge` directly for most status labels, categories, version pills, and short
counters.

Use `variant` for the built-in tones: `default`, `secondary`, `destructive`, `outline`, `ghost`.

Place `Badge.Dot` or a direct child icon next to the label when a badge needs an extra visual cue.

`Badge.Root` and `Badge.Dot` forward refs to their rendered DOM elements.

### When to go custom

Use `Badge asChild` when another semantic element must own the DOM node. Ark composition
requires exactly one child. Interactive children keep their native behavior and must retain a
visible focus treatment.

Switch to local CSS variables or `className` overrides when the surface needs custom sizing or a
different tone.

### Default props

| Prop      | Default   | Values / Notes                                            |
| --------- | --------- | --------------------------------------------------------- |
| `variant` | `default` | `default`, `secondary`, `destructive`, `outline`, `ghost` |
| `asChild` | `false`   | Ark factory composition with exactly one child            |

### Behavioral notes

- `Badge.Root` is presentational by default and renders a `span`.
- `Badge.Dot` is decorative and hidden from assistive technology, so the visible text must carry the status meaning.
- Long labels stay on one line and are truncated by the root; add `title` when the full value should remain discoverable.
- Direct child `svg` icons inherit `currentColor` and use `--badge-icon-size`.
- Numeric content uses tabular figures, and badge text is not selectable by default.
- Chakra's size recipe is intentionally represented by public CSS variables instead of a `size`
  prop.

## Examples

### Variants

Use the built-in variants for the common visual tones.

<Preview>
  <BadgeVariantsExample />

  <Preview.Code>
    {`
      import { Badge } from "@moduix/react";

      export function BadgeVariantsDemo() {
        return (
          <div className="badge-demo-row">
            {variants.map((variant) => (
              <Badge key={variant} variant={variant}>
                {variant}
              </Badge>
            ))}
          </div>
        );
      }
    `}

  </Preview.Code>

  <Preview.CSS>
    {`
      .badge-demo-row {
        display: flex;
        flex-wrap: wrap;
        align-items: center;
        gap: var(--spacing-2);
      }
    `}

  </Preview.CSS>

  <Preview.Data>
    {`
      const variants = ["default", "secondary", "destructive", "outline", "ghost"] as const;
    `}

  </Preview.Data>
</Preview>

### With Dot

Add `Badge.Dot` when label and status indicator should stay visually grouped.

<Preview>
  <BadgeWithDotExample />

  <Preview.Code>
    {`
      import { Badge } from "@moduix/react";

      export function BadgeWithDotDemo() {
        return (
          <div className="badge-demo-row">
            {statuses.map((status) => (
              <Badge key={status.label} variant={status.variant}>
                <Badge.Dot />
                {status.label}
              </Badge>
            ))}
          </div>
        );
      }
    `}

  </Preview.Code>

  <Preview.CSS>
    {`
      .badge-demo-row {
        display: flex;
        flex-wrap: wrap;
        align-items: center;
        gap: var(--spacing-2);
      }
    `}

  </Preview.CSS>

  <Preview.Data>
    {`
      const statuses = [
        { label: "Online", variant: "default" },
        { label: "Draft", variant: "secondary" },
        { label: "Failed", variant: "destructive" },
      ] as const;
    `}

  </Preview.Data>
</Preview>

### With Icon

Place an inline icon next to the label when the metadata needs a second visual cue.

<Preview>
  <BadgeWithIconExample />

  <Preview.Code>
    {`
      import { Badge, ChevronRightIcon } from "@moduix/react";

      export function BadgeWithIconDemo() {
        return (
          <div className="badge-demo-row">
            <Badge variant="default">
              {labels.release}
              <ChevronRightIcon />
            </Badge>
            <Badge variant="secondary">
              {labels.details}
              <ChevronRightIcon />
            </Badge>
            <Badge variant="outline">
              {labels.more}
              <ChevronRightIcon />
            </Badge>
          </div>
        );
      }
    `}

  </Preview.Code>

  <Preview.CSS>
    {`
      .badge-demo-row {
        display: flex;
        flex-wrap: wrap;
        align-items: center;
        gap: var(--spacing-2);
      }
    `}

  </Preview.CSS>

  <Preview.Data>
    {`
      const labels = {
        release: "Release",
        details: "Details",
        more: "Read more",
      };
    `}

  </Preview.Data>
</Preview>

### Custom Composition

Use `asChild` when a single semantic child should receive the badge props and styling. Keep native
interaction behavior and provide visible hover and keyboard-focus states.

<Preview>
  <BadgeAsChildExample />

  <Preview.Code>
    {`
      import { Badge } from "@moduix/react";

      export function BadgeAsChildDemo() {
        return (
          <Badge asChild variant="outline">
            <a className="badge-demo-link" href={link.href}>
              {link.label}
            </a>
          </Badge>
        );
      }
    `}

  </Preview.Code>

  <Preview.CSS>
    {`
      .badge-demo-link {
        &:hover {
          --badge-border-color: var(--color-primary);
          --badge-color: var(--color-primary);
        }

        &:focus-visible {
          outline: var(--border-width-md) solid var(--color-ring);
          outline-offset: var(--spacing-1);
        }
      }
    `}

  </Preview.CSS>

  <Preview.Data>
    {`
      const link = {
        href: "#styling",
        label: "Badge styling guidance",
      };
    `}

  </Preview.Data>
</Preview>

### Long Labels

Badges truncate long labels by default. Add `title` when the full text should still be available.

<Preview>
  <BadgeTruncatedExample />

  <Preview.Code>
    {`
      import { Badge } from "@moduix/react";

      export function BadgeTruncatedDemo() {
        return (
          <Badge
            className="badge-demo-constrained"
            title={label}
          >
            {label}
          </Badge>
        );
      }
    `}

  </Preview.Code>

  <Preview.CSS>
    {`
      .badge-demo-constrained {
        max-width: 14rem;
      }
    `}

  </Preview.CSS>

  <Preview.Data>
    {`
      const label = "Ready for stakeholder review after legal approval";
    `}

  </Preview.Data>
</Preview>

### Custom Styling

Use local CSS variables when a surface needs custom sizing or a different tone.

<Preview>
  <CustomBadgeExample />

  <Preview.Code>
    {`
      import { Badge } from "@moduix/react";

      export function CustomBadgeDemo() {
        return (
          <div className="badge-demo-row">
            <Badge className="badge-demo-small">{labels.small}</Badge>
            <Badge>{labels.default}</Badge>
            <Badge className="badge-demo-large">{labels.large}</Badge>
            <Badge className="badge-demo-custom">
              <Badge.Dot />
              {labels.custom}
            </Badge>
          </div>
        );
      }
    `}

  </Preview.Code>

  <Preview.CSS>
    {`
      .badge-demo-row {
        display: flex;
        flex-wrap: wrap;
        align-items: center;
        gap: var(--spacing-2);
      }

      .badge-demo-custom {
        --badge-bg: color-mix(in oklab, var(--color-primary) 20%, var(--color-background));
        --badge-border-color: color-mix(in oklab, var(--color-primary) 35%, transparent);
        --badge-color: var(--color-primary);
        --badge-dot-size: 0.5rem;
        --badge-height: 1.5rem;
        --badge-padding-x: var(--spacing-3);
        --badge-radius: var(--radius-sm);
      }

      .badge-demo-small {
        --badge-height: 1rem;
        --badge-font-size: var(--text-xs);
        --badge-icon-size: 0.75rem;
        --badge-line-height: var(--line-height-text-xs);
        --badge-padding-x: 0.5rem;
      }

      .badge-demo-large {
        --badge-height: 2rem;
        --badge-font-size: var(--text-sm);
        --badge-icon-size: 1rem;
        --badge-line-height: var(--line-height-text-sm);
        --badge-padding-x: 1rem;
      }
    `}

  </Preview.CSS>

  <Preview.Data>
    {`
      const labels = {
        small: "Small",
        default: "Default",
        large: "Large",
        custom: "Priority",
      };
    `}

  </Preview.Data>
</Preview>

## Styling

Prefer built-in variants first, then local CSS variables for one-off sizing and tone changes.

### CSS Properties

<div className="not-prose">
  <Tabs items={['CSS Variables']} className="mb-0 mt-3">
    <Tab className="max-h-150 overflow-auto">
      <BadgeCssPropertiesPanel />
    </Tab>
  </Tabs>
</div>

### Styling hooks

- `data-slot="badge-root"` and `data-slot="badge-dot"` are stable selectors on the exported parts.
- `Badge.Root` also exposes `data-scope="badge"` and `data-part="root"`. `Badge.Dot` exposes `data-part="dot"`.
- `Badge.Root` writes the selected tone to `data-variant`.
- Direct child `svg` icons inherit `currentColor`, do not receive pointer events, and are sized with `--badge-icon-size`.
- There are no Ark-managed states or runtime CSS variables because Badge has no dedicated Ark
  primitive or behavior machine.