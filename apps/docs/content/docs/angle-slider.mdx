---
title: Angle Slider
description: A circular control for selecting an angle from 0 to 360 degrees.
icon: Component
---

import {
  AngleSliderCssPropertiesPanel,
  AngleSliderExample,
  ContextAngleSliderExample,
  ControlledAngleSliderExample,
  CustomStylingAngleSliderExample,
  DisabledAngleSliderExample,
  ReadOnlyAngleSliderExample,
  RootProviderAngleSliderExample,
  SteppedAngleSliderExample,
  angleSliderBasicCss,
  angleSliderContextCss,
  angleSliderControlledCss,
  angleSliderCustomStylingCss,
  angleSliderDisabledCss,
  angleSliderReadOnlyCss,
  angleSliderRootProviderCss,
  angleSliderStepCss,
} from '@/components/examples/angle-slider';

## API Reference

<PrimitiveReference href="https://ark-ui.com/docs/components/angle-slider" />

## Choosing the right component

Use `AngleSlider` when users should manipulate a direction or rotation directly on a circular dial.

- Choose **`AngleSlider`** for rotation, heading, spotlight direction, or other angular values.
- Choose **`Slider`** when the value is better understood on a linear range.
- Choose **`NumberInput`** when exact typed degree entry matters more than direct manipulation.

## Basic

Compose the Ark parts explicitly. Markers provide reference points, `Thumb` owns keyboard focus and
the active angle indicator, and `HiddenInput` enables native form submission.

<Preview>
  <AngleSliderExample />

  <Preview.Code>
    {`
      import { AngleSlider } from "moduix";

      export function AngleSliderDemo() {
        return (
          <AngleSlider
            defaultValue={135}
            aria-label="Rotation"
            className="docs-angle-slider-basic"
          >
            <AngleSlider.Label>Rotation</AngleSlider.Label>
            <AngleSlider.Control>
              <AngleSlider.MarkerGroup>
                {markerValues.map((value) => (
                  <AngleSlider.Marker key={value} value={value} />
                ))}
              </AngleSlider.MarkerGroup>
              <AngleSlider.Thumb />
            </AngleSlider.Control>
            <AngleSlider.ValueText />
            <AngleSlider.HiddenInput />
          </AngleSlider>
        );
      }
    `}

  </Preview.Code>

<Preview.CSS>{angleSliderBasicCss}</Preview.CSS>

  <Preview.Data>
    {`
      const markerValues = [0, 45, 90, 135, 180, 225, 270, 315];
    `}

  </Preview.Data>
</Preview>

## Install with shadcn (optional)

If you want `AngleSlider` in your project source instead of `node_modules`, install it from the
moduix GitHub registry:

<Tabs items={['npm', 'pnpm', 'bun', 'deno', 'aube']}>
  <Tab value="npm">

```bash
npx shadcn@latest add Blinks44/moduix/angle-slider
```

  </Tab>
  <Tab value="pnpm">

```bash
pnpm dlx shadcn@latest add Blinks44/moduix/angle-slider
```

  </Tab>
  <Tab value="bun">

```bash
bunx shadcn@latest add Blinks44/moduix/angle-slider
```

  </Tab>
  <Tab value="deno">

```bash
deno x -A npm:shadcn@latest add Blinks44/moduix/angle-slider
```

  </Tab>
  <Tab value="aube">

```bash
aubx shadcn@latest add Blinks44/moduix/angle-slider
```

  </Tab>
</Tabs>

## Anatomy

```text
AngleSlider.Root
├─ AngleSlider.Label
├─ AngleSlider.Control
│  ├─ AngleSlider.MarkerGroup
│  │  └─ AngleSlider.Marker[value]
│  └─ AngleSlider.Thumb
├─ AngleSlider.ValueText
└─ AngleSlider.HiddenInput
```

| Part                               | Role                                                                  |
| ---------------------------------- | --------------------------------------------------------------------- |
| `AngleSlider` / `AngleSlider.Root` | Owns uncontrolled or controlled slider state.                         |
| `AngleSlider.RootProvider`         | Connects externally owned state from `useAngleSlider`.                |
| `AngleSlider.Context`              | Exposes `value`, `valueAsDegree`, `setValue`, and `dragging`.         |
| `AngleSlider.Label`                | Associates a visible label with the slider thumb.                     |
| `AngleSlider.Control`              | Circular pointer interaction surface.                                 |
| `AngleSlider.MarkerGroup`          | Contains optional reference markers.                                  |
| `AngleSlider.Marker`               | Positions a numeric marker and exposes its relation to current value. |
| `AngleSlider.Thumb`                | Focusable slider handle and rotating angle indicator.                 |
| `AngleSlider.ValueText`            | Renders the current value as degree text.                             |
| `AngleSlider.HiddenInput`          | Participates in native form submission and reset.                     |

## Composition

### Recommended composition

Use the full part tree shown above. `MarkerGroup` is optional, but `Label`, `Thumb`, and
`HiddenInput` should remain when their accessibility and form behavior are needed.

### When to go custom

Use `AngleSlider.Context` for inline state-dependent rendering. Use `useAngleSlider` with
`AngleSlider.RootProvider` when state must be controlled outside the rendered slider subtree.
Do not render `Root` and `RootProvider` for the same state instance.

Every DOM part supports Ark `asChild`. Supply one semantic child that can preserve the part's
keyboard, focus, ARIA, and pointer behavior. Use `ids` on `Root` when composed elements need stable
shared IDs. Refs are forwarded to the DOM element rendered by each wrapped Ark part.

### Default props

| Prop           | Default | Notes                                      |
| -------------- | ------- | ------------------------------------------ |
| `defaultValue` | `0`     | Initial uncontrolled angle                 |
| `step`         | `1`     | Keyboard and pointer value increment       |
| `disabled`     | `false` | Prevents focus and interaction             |
| `readOnly`     | `false` | Keeps the value visible but non-editable   |
| `invalid`      | `false` | Exposes invalid state for ARIA and styling |

### Behavioral notes

- `value` is a single number; `onValueChange(details)` and `onValueChangeEnd(details)` preserve Ark
  callback objects with `details.value` and `details.valueAsDegree`.
- The thumb supports Ark slider keyboard behavior and pointer dragging around the circular control.
- Set `name` and render `HiddenInput` for native form submission and reset synchronization.
- `ValueText` renders Ark's degree string when no custom children are provided.
- `data-disabled`, `data-invalid`, `data-readonly`, `data-focus`, marker `data-state`, `--value`,
  `--angle`, `--marker-value`, and `--marker-display-value` remain available for styling.

## Examples

### Controlled

Use controlled state when the angle must stay synchronized with application state.

<Preview>
  <ControlledAngleSliderExample />

  <Preview.Code>
    {`
      import { AngleSlider } from "moduix";
      import { useState } from "react";

      export function ControlledAngleSliderDemo() {
        const [value, setValue] = useState(initialValue);

        return (
          <AngleSlider
            value={value}
            aria-label="Heading"
            className="docs-angle-slider-controlled"
            onValueChange={(details) => setValue(details.value)}
          >
            <AngleSlider.Label>Heading</AngleSlider.Label>
            <AngleSlider.Control>
              <AngleSlider.MarkerGroup>
                {markerValues.map((markerValue) => (
                  <AngleSlider.Marker key={markerValue} value={markerValue} />
                ))}
              </AngleSlider.MarkerGroup>
              <AngleSlider.Thumb />
            </AngleSlider.Control>
            <AngleSlider.ValueText />
            <AngleSlider.HiddenInput />
          </AngleSlider>
        );
      }
    `}

  </Preview.Code>

<Preview.CSS>{angleSliderControlledCss}</Preview.CSS>

  <Preview.Data>
    {`
      const initialValue = 210;
      const markerValues = [0, 45, 90, 135, 180, 225, 270, 315];
    `}

  </Preview.Data>
</Preview>

### Steps

Use `step` to snap pointer and keyboard interaction to a discrete angular increment.

<Preview>
  <SteppedAngleSliderExample />

  <Preview.Code>
    {`
      import { AngleSlider } from "moduix";

      export function SteppedAngleSliderDemo() {
        return (
          <AngleSlider
            defaultValue={initialValue}
            step={step}
            aria-label="Snap angle"
            className="docs-angle-slider-step"
          >
            <AngleSlider.Label>15 Step</AngleSlider.Label>
            <AngleSlider.Control>
              <AngleSlider.MarkerGroup>
                {markerValues.map((value) => (
                  <AngleSlider.Marker key={value} value={value} />
                ))}
              </AngleSlider.MarkerGroup>
              <AngleSlider.Thumb />
            </AngleSlider.Control>
            <AngleSlider.ValueText />
            <AngleSlider.HiddenInput />
          </AngleSlider>
        );
      }
    `}

  </Preview.Code>

<Preview.CSS>{angleSliderStepCss}</Preview.CSS>

  <Preview.Data>
    {`
      const initialValue = 60;
      const step = 15;
      const markerValues = [0, 45, 90, 135, 180, 225, 270, 315];
    `}

  </Preview.Data>
</Preview>

### Disabled

Use `disabled` when the control must be unavailable and excluded from interaction.

<Preview>
  <DisabledAngleSliderExample />

  <Preview.Code>
    {`
      import { AngleSlider } from "moduix";

      export function DisabledAngleSliderDemo() {
        return (
          <AngleSlider
            defaultValue={initialValue}
            disabled
            aria-label="Disabled rotation"
            className="docs-angle-slider-disabled"
          >
            <AngleSlider.Label>Rotation</AngleSlider.Label>
            <AngleSlider.Control>
              <AngleSlider.MarkerGroup>
                {markerValues.map((value) => (
                  <AngleSlider.Marker key={value} value={value} />
                ))}
              </AngleSlider.MarkerGroup>
              <AngleSlider.Thumb />
            </AngleSlider.Control>
            <AngleSlider.ValueText />
            <AngleSlider.HiddenInput />
          </AngleSlider>
        );
      }
    `}

  </Preview.Code>

<Preview.CSS>{angleSliderDisabledCss}</Preview.CSS>

  <Preview.Data>
    {`
      const initialValue = 45;
      const markerValues = [0, 45, 90, 135, 180, 225, 270, 315];
    `}

  </Preview.Data>
</Preview>

### Context

Use `AngleSlider.Context` for inline rendering that depends on the current Ark state.

<Preview>
  <ContextAngleSliderExample />

  <Preview.Code>
    {`
      import { AngleSlider } from "moduix";

      export function ContextAngleSliderDemo() {
        return (
          <AngleSlider
            defaultValue={initialValue}
            aria-label="Rotation"
            className="docs-angle-slider-context"
          >
            <AngleSlider.Context>
              {(context) => (
                <AngleSlider.Label>{context.value} degrees</AngleSlider.Label>
              )}
            </AngleSlider.Context>
            <AngleSlider.Control>
              <AngleSlider.MarkerGroup>
                {markerValues.map((value) => (
                  <AngleSlider.Marker key={value} value={value} />
                ))}
              </AngleSlider.MarkerGroup>
              <AngleSlider.Thumb />
            </AngleSlider.Control>
            <AngleSlider.ValueText />
            <AngleSlider.HiddenInput />
          </AngleSlider>
        );
      }
    `}

  </Preview.Code>

<Preview.CSS>{angleSliderContextCss}</Preview.CSS>

  <Preview.Data>
    {`
      const initialValue = 90;
      const markerValues = [0, 45, 90, 135, 180, 225, 270, 315];
    `}

  </Preview.Data>
</Preview>

### Root Provider

Use `useAngleSlider` and `RootProvider` when another control needs direct access to the same state
machine.

<Preview>
  <RootProviderAngleSliderExample />

  <Preview.Code>
    {`
      import { AngleSlider, useAngleSlider } from "moduix";

      export function RootProviderAngleSliderDemo() {
        const angleSlider = useAngleSlider({
          defaultValue: initialValue,
          "aria-label": "Rotation",
        });

        return (
          <div className="docs-angle-slider-provider-layout">
            <AngleSliderProvider
              value={angleSlider}
              className="docs-angle-slider-provider"
            >
              <AngleSlider.Label>Rotation</AngleSlider.Label>
              <AngleSlider.Control>
                <AngleSlider.MarkerGroup>
                  {markerValues.map((value) => (
                    <AngleSlider.Marker key={value} value={value} />
                  ))}
                </AngleSlider.MarkerGroup>
                <AngleSlider.Thumb />
              </AngleSlider.Control>
              <AngleSlider.ValueText />
              <AngleSlider.HiddenInput />
            </AngleSlider.RootProvider>
            <button
              type="button"
              className="docs-angle-slider-provider-button"
              onClick={() => angleSlider.setValue(targetValue)}
            >
              Set to 90°
            </button>
          </div>
        );
      }
    `}

  </Preview.Code>

<Preview.CSS>{angleSliderRootProviderCss}</Preview.CSS>

  <Preview.Data>
    {`
      const initialValue = 45;
      const targetValue = 90;
      const markerValues = [0, 45, 90, 135, 180, 225, 270, 315];
    `}

  </Preview.Data>
</Preview>

### Read Only

Use `readOnly` when the current value should remain focusable and visible without allowing edits.

<Preview>
  <ReadOnlyAngleSliderExample />

  <Preview.Code>
    {`
      import { AngleSlider } from "moduix";

      export function ReadOnlyAngleSliderDemo() {
        return (
          <AngleSlider
            defaultValue={initialValue}
            readOnly
            aria-label="Locked angle"
            className="docs-angle-slider-readonly"
          >
            <AngleSlider.Label>Locked angle</AngleSlider.Label>
            <AngleSlider.Control>
              <AngleSlider.MarkerGroup>
                {markerValues.map((value) => (
                  <AngleSlider.Marker key={value} value={value} />
                ))}
              </AngleSlider.MarkerGroup>
              <AngleSlider.Thumb />
            </AngleSlider.Control>
            <AngleSlider.ValueText />
            <AngleSlider.HiddenInput />
          </AngleSlider>
        );
      }
    `}

  </Preview.Code>

<Preview.CSS>{angleSliderReadOnlyCss}</Preview.CSS>

  <Preview.Data>
    {`
      const initialValue = 300;
      const markerValues = [0, 45, 90, 135, 180, 225, 270, 315];
    `}

  </Preview.Data>
</Preview>

### Custom Styling

Override public CSS variables to change the visual treatment without changing Ark composition or
state behavior.

<Preview>
  <CustomStylingAngleSliderExample />

  <Preview.Code>
    {`
      import { AngleSlider } from "moduix";

      export function AngleSliderStylingDemo() {
        return (
          <AngleSlider
            defaultValue={initialValue}
            aria-label="Spotlight direction"
            className="docs-angle-slider-spotlight"
          >
            <AngleSlider.Label>Spotlight</AngleSlider.Label>
            <AngleSlider.Control>
              <AngleSlider.MarkerGroup>
                {markerValues.map((value) => (
                  <AngleSlider.Marker key={value} value={value} />
                ))}
              </AngleSlider.MarkerGroup>
              <AngleSlider.Thumb />
            </AngleSlider.Control>
            <AngleSlider.ValueText />
            <AngleSlider.HiddenInput />
          </AngleSlider>
        );
      }
    `}

  </Preview.Code>

<Preview.CSS>{angleSliderCustomStylingCss}</Preview.CSS>

  <Preview.Data>
    {`
      const initialValue = 315;
      const markerValues = [0, 45, 90, 135, 180, 225, 270, 315];
    `}

  </Preview.Data>
</Preview>

## Styling

Style the component through `className`, stable `data-slot="angle-slider-*"` hooks, Ark
`data-scope="angle-slider"` / `data-part` attributes, state attributes, and the public CSS
variables below.

### CSS Properties

<div className="not-prose">
  <Tabs items={['CSS Variables']} className="mb-0 mt-3">
    <Tab className="max-h-150 overflow-auto">
      <AngleSliderCssPropertiesPanel />
    </Tab>
  </Tabs>
</div>

### Styling hooks

- `data-slot="angle-slider-root"` and `data-slot="angle-slider-root-provider"` own shared layout.
- `data-slot="angle-slider-control"` owns the dial and receives `data-focus`, `data-disabled`,
  `data-invalid`, and `data-readonly`.
- `data-slot="angle-slider-thumb"` owns the focusable rotating indicator.
- `data-slot="angle-slider-marker"` exposes `data-value` and
  `data-state="under-value | at-value | over-value"`.
- Ark root variables `--value` and `--angle` and marker variables `--marker-value` and
  `--marker-display-value` are preserved.