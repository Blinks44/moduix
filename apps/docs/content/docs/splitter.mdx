---
title: Splitter
description: A resizable layout primitive for dividing an interface into panels.
icon: Component
---

import {
  BasicSplitterExample,
  CollapsibleSplitterExample,
  ControlledSplitterExample,
  CustomStylingSplitterExample,
  DynamicCollapsibleSplitterExample,
  MultiplePanelsSplitterExample,
  NestedSplitterExample,
  ResizeIndicatorSplitterExample,
  RootProviderSplitterExample,
  SplitterCssPropertiesPanel,
  VerticalSplitterExample,
  splitterBasicCode,
  splitterCollapsibleCode,
  splitterCollapsibleData,
  splitterControlledCode,
  splitterCustomStylingCode,
  splitterCustomStylingCss,
  splitterDynamicCollapsibleCode,
  splitterExampleCss,
  splitterMultipleCode,
  splitterNestedCode,
  splitterNestedCss,
  splitterNestedData,
  splitterNoData,
  splitterPanelsData,
  splitterResizeIndicatorCode,
  splitterRootProviderCode,
  splitterStackCss,
  splitterThreePanelsData,
  splitterVerticalCode,
  splitterVerticalCss,
} from '@/components/examples/splitter';

## API Reference

<PrimitiveReference href="https://ark-ui.com/docs/components/splitter" />

## Basic

Use Ark-style parts: `Splitter.Root`, `Splitter.Panel`, `Splitter.ResizeTrigger`, and
`Splitter.ResizeTriggerIndicator`. `panels` defines ids and constraints; rendered panel ids must
match that array.

<Preview>
  <BasicSplitterExample />

  <Preview.Code>{splitterBasicCode}</Preview.Code>
  <Preview.CSS>{splitterExampleCss}</Preview.CSS>
  <Preview.Data>{splitterPanelsData}</Preview.Data>
</Preview>

## Install with shadcn

If you want `Splitter` to become part of your own project source instead of staying in
`node_modules`, install it from the moduix GitHub registry:

<Tabs items={['npm', 'pnpm', 'bun', 'deno', 'aube']}>
  <Tab value="npm">

```bash
npx shadcn@latest add Blinks44/moduix/react-splitter
```

  </Tab>
  <Tab value="pnpm">

```bash
pnpm dlx shadcn@latest add Blinks44/moduix/react-splitter
```

  </Tab>
  <Tab value="bun">

```bash
bunx shadcn@latest add Blinks44/moduix/react-splitter
```

  </Tab>
  <Tab value="deno">

```bash
deno x -A npm:shadcn@latest add Blinks44/moduix/react-splitter
```

  </Tab>
  <Tab value="aube">

```bash
aubx shadcn@latest add Blinks44/moduix/react-splitter
```

  </Tab>
</Tabs>

## Anatomy

```text
Splitter.Root
├─ Splitter.Panel[id]
├─ Splitter.ResizeTrigger[id="panel-a:panel-b"]
│  └─ Splitter.ResizeTriggerIndicator (optional)
└─ Splitter.Panel[id]
```

| Part                              | Role                                                                                   |
| --------------------------------- | -------------------------------------------------------------------------------------- |
| `Splitter` / `Splitter.Root`      | Owns panel constraints, sizes, orientation, callbacks, and the splitter state machine. |
| `Splitter.Panel`                  | Resizable region. Its `id` must match one item in the `panels` prop.                   |
| `Splitter.ResizeTrigger`          | Accessible handle button between adjacent panels.                                      |
| `Splitter.ResizeTriggerIndicator` | Optional visual indicator inside a resize trigger.                                     |
| `Splitter.RootProvider`           | Renders the same part tree from state created with `useSplitter()`.                    |

## Composition

### Recommended composition

Render panels and handles explicitly in the same order they appear visually. Use trigger ids in the
`"left:right"` form so Ark can connect the two adjacent panel ids.

### When to go custom

Use controlled `size` with `onResize(details)` when panel sizes must be mirrored into app state.
For advanced imperative access, import `Splitter as ArkSplitter`, `useSplitter()`,
`useSplitterContext()`, or `createSplitterRegistry()` directly from `@ark-ui/react/splitter` and
pair them with the moduix visual parts. `Splitter.RootProvider` stays available for externally owned
Ark state.

Use `asChild` only when another element must own the rendered node. Custom resize triggers must keep
button semantics, focus behavior, and keyboard interaction intact.

### Default props

| Prop               | Default        | Notes                                                                 |
| ------------------ | -------------- | --------------------------------------------------------------------- |
| `orientation`      | `"horizontal"` | Use `"vertical"` for stacked panels.                                  |
| `defaultSize`      | Ark-calculated | Pass percentage values totaling `100` for predictable initial sizing. |
| `keyboardResizeBy` | Ark default    | Controls arrow-key resize distance.                                   |
| `size`             | uncontrolled   | Pass with `onResize(details)` for controlled panel sizes.             |
| `disabled`         | `false`        | Set on `ResizeTrigger` to disable a handle.                           |

### Behavioral notes

`panels` is required and defines constraints such as `minSize`, `maxSize`, `collapsible`, and
`collapsedSize`. `defaultSize` and controlled `size` are percentage arrays. Ark preserves the WAI-ARIA
Window Splitter pattern, keyboard resizing, focus state, and pointer dragging.

Zag 1.41.2 currently renders a collapsed panel at `minSize` when `collapsedSize` is smaller. The
upstream [issue](https://github.com/chakra-ui/zag/issues/3179) and
[fix](https://github.com/chakra-ui/zag/pull/3180) cover this regression; moduix does not add a local
layout override while the fix is pending.

Callbacks keep Ark detail objects: `onResize(details)`, `onResizeEnd(details)`, `onCollapse(details)`,
and `onExpand(details)`. `onResizeStart` is called when a resize begins.

## Examples

### Controlled

Control panel sizes from React state and update that state from `details.size`.

<Preview>
  <ControlledSplitterExample />

  <Preview.Code>{splitterControlledCode}</Preview.Code>
  <Preview.CSS>{splitterStackCss}</Preview.CSS>
  <Preview.Data>{splitterPanelsData}</Preview.Data>
</Preview>

### Vertical

Set `orientation="vertical"` and give the root a stable height so stacked panels have room to resize.

<Preview>
  <VerticalSplitterExample />

  <Preview.Code>{splitterVerticalCode}</Preview.Code>
  <Preview.CSS>{splitterVerticalCss}</Preview.CSS>
  <Preview.Data>{splitterPanelsData}</Preview.Data>
</Preview>

### Collapsible Panels

Set `collapsible` and `collapsedSize` on a panel definition when a panel can shrink to a collapsed
rail.

<Preview>
  <CollapsibleSplitterExample />

  <Preview.Code>{splitterCollapsibleCode}</Preview.Code>
  <Preview.CSS>{splitterExampleCss}</Preview.CSS>
  <Preview.Data>{splitterCollapsibleData}</Preview.Data>
</Preview>

### Multiple Panels

Add another `Panel` and another adjacent `ResizeTrigger` for three-panel layouts.

<Preview>
  <MultiplePanelsSplitterExample />

  <Preview.Code>{splitterMultipleCode}</Preview.Code>
  <Preview.CSS>{splitterExampleCss}</Preview.CSS>
  <Preview.Data>{splitterThreePanelsData}</Preview.Data>
</Preview>

### Root Provider

Use `useSplitter()` from Ark with `Splitter.RootProvider` when controls outside the root need access
to the same splitter API.

<Preview>
  <RootProviderSplitterExample />

  <Preview.Code>{splitterRootProviderCode}</Preview.Code>
  <Preview.CSS>{splitterStackCss}</Preview.CSS>
  <Preview.Data>{splitterPanelsData}</Preview.Data>
</Preview>

### Resize Indicator

Place `Splitter.ResizeTriggerIndicator` inside the trigger when the handle needs a stronger visual
affordance.

<Preview>
  <ResizeIndicatorSplitterExample />

  <Preview.Code>{splitterResizeIndicatorCode}</Preview.Code>
  <Preview.CSS>{splitterExampleCss}</Preview.CSS>
  <Preview.Data>{splitterPanelsData}</Preview.Data>
</Preview>

### Dynamic Collapsible

Programmatic collapse and expand methods work well for responsive sidebars that react to measured
available width.

<Preview>
  <DynamicCollapsibleSplitterExample />

  <Preview.Code>{splitterDynamicCollapsibleCode}</Preview.Code>
  <Preview.CSS>{splitterExampleCss}</Preview.CSS>
  <Preview.Data>{splitterNoData}</Preview.Data>
</Preview>

### Nested

Share a registry between nested splitters to support multi-drag behavior at horizontal and vertical
handle intersections.

<Preview>
  <NestedSplitterExample />

  <Preview.Code>{splitterNestedCode}</Preview.Code>
  <Preview.CSS>{splitterNestedCss}</Preview.CSS>
  <Preview.Data>{splitterNestedData}</Preview.Data>
</Preview>

### Custom Styling

Customize visuals with CSS variables, `className`, `data-slot` hooks, and Ark state attributes.

<Preview>
  <CustomStylingSplitterExample />

  <Preview.Code>{splitterCustomStylingCode}</Preview.Code>
  <Preview.CSS>{splitterCustomStylingCss}</Preview.CSS>
  <Preview.Data>{splitterPanelsData}</Preview.Data>
</Preview>

## Styling

Style `Splitter` through `className`, Ark attributes, stable `data-slot="splitter-*"` hooks, and the
public Moduix CSS variables below. `Splitter.Root` and `Splitter.RootProvider` apply
`--splitter-width` and `--splitter-height` through their default inline style so those variables can
override Ark's root layout style. The default surface lives on the root, while panels stay flat and
flush inside it.

The resize trigger contributes only a `1px` separator to layout, with a transparent hit area and
`Splitter.ResizeTriggerIndicator` rendered as the raised rounded handle on top of that line. The
indicator is centered over the separator for both orientations. The separator line and handle stay
neutral on hover, focus, and drag by default, while drag-specific CSS variables let consumers opt
into a different active color.

### CSS Properties

<div className="not-prose">
  <Tabs items={['CSS Variables']} className="mb-0 mt-3">
    <Tab className="max-h-150 overflow-auto">
      <SplitterCssPropertiesPanel />
    </Tab>
  </Tabs>
</div>

### Styling hooks

Ark writes `data-scope="splitter"` and `data-part` to rendered parts. Moduix also writes
`data-slot` values: `splitter-root`, `splitter-root-provider`, `splitter-panel`,
`splitter-resize-trigger`, and `splitter-resize-trigger-indicator`.

Important Ark state hooks include `data-orientation`, `data-dragging`, trigger `data-focus`,
trigger `data-disabled`, panel `data-id`, and panel `data-index`.