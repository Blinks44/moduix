---
title: Fieldset
description: Group related controls with Ark UI fieldset semantics, descriptions, errors, and shared state.
icon: Component
---

import {
  CustomStylesFieldsetExample,
  FieldsetCheckboxExample,
  FieldsetCssPropertiesPanel,
  FieldsetDisabledExample,
  FieldsetExample,
  FieldsetInvalidExample,
  FieldsetPhoneInputExample,
  FieldsetRootProviderExample,
  FieldsetWithFieldExample,
  fieldsetCountryCodesData,
  fieldsetCustomStylingCss,
  fieldsetDisabledData,
  fieldsetEmptyData,
  fieldsetExampleCss,
  fieldsetInvalidData,
  fieldsetPhoneInputCss,
  fieldsetPreferencesData,
} from '@/components/examples/fieldset';

## API Reference

<PrimitiveReference href="https://ark-ui.com/docs/components/fieldset" />

## Choosing the right component

Use `Fieldset` for multiple related controls with one legend. Use `Field` for one control with its
own label, description, and validation message.

## Basic

Compose `Fieldset.Root`, `Fieldset.Legend`, optional helper or error text, and the related controls.
The short `<Fieldset>` form is equivalent to `<Fieldset.Root>`.

<Preview>
  <FieldsetExample />

  <Preview.Code>
    {`
      import { Field, Fieldset } from "@moduix/react";

      export function ContactDetails() {
        return (
          <Fieldset className="fieldset">
            <Fieldset.Legend>Contact details</Fieldset.Legend>
            <Field>
              <Field.Label>Name</Field.Label>
              <Field.Input name="name" />
            </Field>
            <Field>
              <Field.Label>Email</Field.Label>
              <Field.Input name="email" type="email" />
            </Field>
            <Fieldset.HelperText>
              We only use these details to contact you.
            </Fieldset.HelperText>
          </Fieldset>
        );
      }
    `}

  </Preview.Code>
  <Preview.CSS>{fieldsetExampleCss}</Preview.CSS>
  <Preview.Data>{fieldsetEmptyData}</Preview.Data>
</Preview>

## Install with shadcn (optional)

<Tabs items={['npm', 'pnpm', 'bun', 'deno', 'aube']}>
  <Tab value="npm">

```bash
npx shadcn@latest add Blinks44/moduix/react-fieldset
```

  </Tab>
  <Tab value="pnpm">

```bash
pnpm dlx shadcn@latest add Blinks44/moduix/react-fieldset
```

  </Tab>
  <Tab value="bun">

```bash
bunx shadcn@latest add Blinks44/moduix/react-fieldset
```

  </Tab>
  <Tab value="deno">

```bash
deno x -A npm:shadcn@latest add Blinks44/moduix/react-fieldset
```

  </Tab>
  <Tab value="aube">

```bash
aubx shadcn@latest add Blinks44/moduix/react-fieldset
```

  </Tab>
</Tabs>

## Anatomy

```text
Fieldset.Root | Fieldset.RootProvider
├─ Fieldset.Legend
├─ grouped controls
├─ Fieldset.HelperText (optional)
├─ Fieldset.ErrorText (optional)
└─ Fieldset.Context (optional)
```

| Part                         | Role                                                                  |
| ---------------------------- | --------------------------------------------------------------------- |
| `Fieldset` / `Fieldset.Root` | Creates state and renders a native `fieldset`.                        |
| `Fieldset.RootProvider`      | Renders externally created `useFieldset` state.                       |
| `Fieldset.Legend`            | Native `legend` and accessible group name.                            |
| `Fieldset.HelperText`        | Description linked to the root through `aria-describedby`.            |
| `Fieldset.ErrorText`         | Polite-live validation message rendered only while `invalid` is true. |
| `Fieldset.Context`           | Inline access to IDs, state, refs, and prop getters.                  |

## Composition

### Recommended composition

Keep one visible `Fieldset.Legend` inside the root. Pass `disabled`, `invalid`, and `id` directly to
the root. Ark links the legend, mounted helper text, and active error text to the fieldset.

Root, provider, legend, helper text, and error text forward refs to their rendered DOM elements.
Use `asChild` with one semantic child when a custom host must own a part.

### When to go custom

Use `Fieldset.Context` for an inline state read and `useFieldsetContext` in reusable descendants.
Use `useFieldset` with `Fieldset.RootProvider` when state is owned outside the rendered tree. Do not
wrap a provider in `Fieldset.Root` for the same state instance.

### Default props

| Surface         | Prop       | Default |
| --------------- | ---------- | ------- |
| `Fieldset.Root` | `disabled` | `false` |
| `Fieldset.Root` | `invalid`  | `false` |

### Behavioral notes

- `Fieldset.Legend` renders a native `legend`, unlike the removed legacy wrapper.
- Native controls inside a disabled fieldset are disabled by the browser.
- Ark `Field` descendants inherit disabled state from the fieldset. Set `invalid` on nested `Field`
  components when individual controls need invalid state.
- `Fieldset.ErrorText` is absent from the DOM until `invalid` is true.
- Fieldset owns no value, keyboard navigation, callback, or `HiddenInput`.
- Legacy `render` and flat exports such as `FieldsetLegend` are not supported; use `asChild` and the
  namespace parts.

## Examples

### Field

Nest `Field` components when each control also needs an individual label or description.

<Preview>
  <FieldsetWithFieldExample />

  <Preview.Code>
    {`
      import { Field, Fieldset } from "@moduix/react";

      export function PersonalInformation() {
        return (
          <Fieldset className="fieldset">
            <Fieldset.Legend>Personal information</Fieldset.Legend>
            <Field>
              <Field.Label>First name</Field.Label>
              <Field.Input />
              <Field.HelperText>As it appears on your ID.</Field.HelperText>
            </Field>
            <Field>
              <Field.Label>Last name</Field.Label>
              <Field.Input />
            </Field>
          </Fieldset>
        );
      }
    `}

  </Preview.Code>
  <Preview.CSS>{fieldsetExampleCss}</Preview.CSS>
  <Preview.Data>{fieldsetEmptyData}</Preview.Data>
</Preview>

### Disabled

Set `disabled` on the root to disable native descendants and pass disabled state to Ark-aware
controls that consume fieldset context.

<Preview>
  <FieldsetDisabledExample />

  <Preview.Code>
    {`
      import { Field, Fieldset } from "@moduix/react";

      export function DisabledFieldset() {
        return (
          <Fieldset className="fieldset" disabled>
            <Fieldset.Legend>Shipping address</Fieldset.Legend>
            <Fieldset.HelperText>
              Your address cannot be changed after order confirmation.
            </Fieldset.HelperText>
            <Field>
              <Field.Label>Street</Field.Label>
              <Field.Input defaultValue="123 Main St" />
            </Field>
            <Field>
              <Field.Label>City</Field.Label>
              <Field.Input defaultValue="San Francisco" />
            </Field>
          </Fieldset>
        );
      }
    `}

  </Preview.Code>
  <Preview.CSS>{fieldsetExampleCss}</Preview.CSS>
  <Preview.Data>{fieldsetDisabledData}</Preview.Data>
</Preview>

### Invalid

Set `invalid` on the fieldset for group-level validation. Set `invalid` on nested `Field`
components when individual controls also need invalid styling and ARIA state.

<Preview>
  <FieldsetInvalidExample />

  <Preview.Code>
    {`
      import { Field, Fieldset } from "@moduix/react";

      export function InvalidFieldset() {
        return (
          <Fieldset className="fieldset" invalid>
            <Fieldset.Legend>Account information</Fieldset.Legend>
            <Fieldset.ErrorText>Please fix the errors below to continue.</Fieldset.ErrorText>
            <Field invalid>
              <Field.Label>Username</Field.Label>
              <Field.Input defaultValue="jo" />
              <Field.ErrorText>Username must be at least 3 characters.</Field.ErrorText>
            </Field>
            <Field invalid>
              <Field.Label>Email</Field.Label>
              <Field.Input type="email" defaultValue="invalid-email" />
              <Field.ErrorText>Enter a valid email address.</Field.ErrorText>
            </Field>
          </Fieldset>
        );
      }
    `}

  </Preview.Code>
  <Preview.CSS>{fieldsetExampleCss}</Preview.CSS>
  <Preview.Data>{fieldsetInvalidData}</Preview.Data>
</Preview>

### Checkbox

Use the fieldset legend as the shared name for a related set of Ark checkbox controls. Keep each
`Checkbox.HiddenInput` for native form submission and reset behavior.

<Preview>
  <FieldsetCheckboxExample />

  <Preview.Code>
    {`
      import { Checkbox, Fieldset } from "@moduix/react";

      export function EmailPreferences() {
        return (
          <Fieldset className="fieldset">
            <Fieldset.Legend>Email preferences</Fieldset.Legend>
            {preferences.map((preference) => (
              <Checkbox key={preference.value} value={preference.value}>
                <Checkbox.Control>
                  <Checkbox.Indicator />
                </Checkbox.Control>
                <Checkbox.Label>{preference.label}</Checkbox.Label>
                <Checkbox.HiddenInput />
              </Checkbox>
            ))}
          </Fieldset>
        );
      }
    `}

  </Preview.Code>
  <Preview.CSS>{fieldsetExampleCss}</Preview.CSS>
  <Preview.Data>{fieldsetPreferencesData}</Preview.Data>
</Preview>

### Root Provider

Create fieldset state outside the rendered tree when sibling controls or reusable descendants need
to coordinate with it.

<Preview>
  <FieldsetRootProviderExample />

  <Preview.Code>
    {`
      import { Field, Fieldset, useFieldset } from "@moduix/react";

      export function ExternalFieldsetState() {
        const fieldset = useFieldset({ invalid: true });

        return (
          <Fieldset.RootProvider value={fieldset} className="fieldset">
            <Fieldset.Legend>Contact details</Fieldset.Legend>
            <Field>
              <Field.Label>Email</Field.Label>
              <Field.Input type="email" defaultValue="invalid-address" />
            </Field>
            <Fieldset.ErrorText>Enter a valid email address.</Fieldset.ErrorText>
            <Fieldset.Context>
              {(context) => <output>Invalid: {String(context.invalid)}</output>}
            </Fieldset.Context>
          </Fieldset.RootProvider>
        );
      }
    `}

  </Preview.Code>
  <Preview.CSS>{fieldsetExampleCss}</Preview.CSS>
  <Preview.Data>{fieldsetEmptyData}</Preview.Data>
</Preview>

### Input with Select

Group a country-code selector and phone input under one legend when both controls represent one
logical value.

<Preview>
  <FieldsetPhoneInputExample />

  <Preview.Code>
    {`
      import { createListCollection } from "@ark-ui/react/collection";
      import { Field, Fieldset, Select } from "@moduix/react";

      const countryCodes = createListCollection({
        items: [
          { label: "+1", value: "+1" },
          { label: "+44", value: "+44" },
          { label: "+49", value: "+49" },
          { label: "+41", value: "+41" },
        ],
      });

      export function PhoneInput() {
        return (
          <Fieldset className="fieldset">
            <Fieldset.Legend>Mobile number</Fieldset.Legend>
            <div className="phone-input">
              <Select
                className="country-code"
                collection={countryCodes}
                defaultValue={["+1"]}
              >
                <Select.Label>Code</Select.Label>
                <Select.Control>
                  <Select.Trigger>
                    <Select.ValueText />
                  </Select.Trigger>
                  <Select.Indicators>
                    <Select.Indicator />
                  </Select.Indicators>
                </Select.Control>
                <Select.Positioner>
                  <Select.Content>
                    {countryCodes.items.map((item) => (
                      <Select.Item key={item.value} item={item}>
                        <Select.ItemText>{item.label}</Select.ItemText>
                        <Select.ItemIndicator />
                      </Select.Item>
                    ))}
                  </Select.Content>
                </Select.Positioner>
                <Select.HiddenSelect name="countryCode" />
              </Select>
              <Field>
                <Field.Label>Phone</Field.Label>
                <Field.Input type="tel" aria-label="Phone number" />
              </Field>
            </div>
            <Fieldset.HelperText>Include the area code.</Fieldset.HelperText>
          </Fieldset>
        );
      }
    `}

  </Preview.Code>
  <Preview.CSS>{fieldsetPhoneInputCss}</Preview.CSS>
  <Preview.Data>{fieldsetCountryCodesData}</Preview.Data>
</Preview>

### Custom Styling

Override the public CSS variables or target Ark `data-scope`, `data-part`, `data-disabled`, and
`data-invalid` attributes for state-aware styling.

<Preview>
  <CustomStylesFieldsetExample />

  <Preview.Code>
    {`
      import { Field, Fieldset } from "@moduix/react";

      export function StyledFieldset() {
        return (
          <Fieldset className="fieldset custom-fieldset">
            <Fieldset.Legend>Styled fieldset</Fieldset.Legend>
            <Field>
              <Field.Label>Project name</Field.Label>
              <Field.Input placeholder="Maps Platform" />
            </Field>
            <Fieldset.HelperText>Visible to project members.</Fieldset.HelperText>
          </Fieldset>
        );
      }
    `}

  </Preview.Code>
  <Preview.CSS>{fieldsetCustomStylingCss}</Preview.CSS>
  <Preview.Data>{fieldsetEmptyData}</Preview.Data>
</Preview>

## Styling

### CSS Properties

<div className="not-prose">
  <Tabs items={['CSS Variables']} className="mb-0 mt-3">
    <Tab className="max-h-150 overflow-auto">
      <FieldsetCssPropertiesPanel />
    </Tab>
  </Tabs>
</div>

### Styling hooks

Every DOM part has Ark `data-scope="fieldset"` and a `data-part` matching the anatomy. Root and
legend expose `data-disabled` and `data-invalid`; moduix also adds `data-slot="fieldset-*"` hooks.
Use `className` for local overrides and `--fieldset-*` variables for theme-level customization,
especially legend spacing and root border tokens.