---
title: Field
description: Ark UI field context for labels, helper text, errors, and form-control state.
icon: Component
---

import * as FieldDocs from '@/components/examples/field';

## API Reference

<PrimitiveReference href="https://ark-ui.com/docs/components/field" />

## Choosing the right component

Use `Field` when one control needs a visible label, helper text, error text, or shared
`disabled`, `invalid`, `required`, and `readOnly` state.

- Choose **`Field`** for one control and its label or descriptive text.
- Choose **`Fieldset`** when several related fields need one shared legend.
- Use a standalone control when no field-level ARIA wiring or shared state is needed.

## Basic

Start with Ark parts: `Field`, `Field.Label`, one control part, and optional helper or error text.

<Preview>
  <FieldDocs.FieldExample />

<Preview.Code>
  <include lang="tsx">./_snippets/field/basic.tsx#demo</include>
</Preview.Code>

  <Preview.CSS>{`.field { width: fit-content; max-width: min(20rem, 100%); }`}</Preview.CSS>
  
</Preview>

## Install with shadcn

<ShadcnInstall packageName="react-field" />

## Anatomy

`Field` follows Ark UI's field parts and keeps their names in the public moduix API.

```text
Field
├─ Field.Label
├─ Field.Input | Field.Textarea | Field.Select | custom control
├─ Field.HelperText
├─ Field.ErrorText
└─ Field.RequiredIndicator
```

| Part                      | Role                                                            |
| ------------------------- | --------------------------------------------------------------- |
| `Field` / `Field.Root`    | Root context and default `<div role="group">`.                  |
| `Field.RootProvider`      | Root powered by a `useField()` instance owned outside the tree. |
| `Field.Label`             | Label linked to the current control id.                         |
| `Field.Input`             | Native input wired with field ARIA and state props.             |
| `Field.Textarea`          | Native textarea; supports Ark `autoresize`.                     |
| `Field.Select`            | Native select wired with field ARIA and state props.            |
| `Field.HelperText`        | Helper text included in `aria-describedby`.                     |
| `Field.ErrorText`         | Error text rendered only when `Field` is `invalid`.             |
| `Field.RequiredIndicator` | Visual required marker with optional `fallback`.                |
| `Field.Item`              | Item-scoped field context for grouped controls via `value`.     |

## Composition

### Recommended composition

Keep one visible label in the root, render exactly one primary control for the field, and add helper
or error text when the control needs description or validation feedback.

### When to go custom

Use `Field.Item value` with root `target` when a field contains multiple native controls and the
label should point to one of them. Use `asChild` when one semantic input element must own the
rendered node.

### Default props

| Surface                | Default | Notes                                                  |
| ---------------------- | ------- | ------------------------------------------------------ |
| `Field` / `Field.Root` | —       | No additional behavioral defaults; Ark defaults apply. |

### Behavioral notes

- `Field.ErrorText` is absent from the DOM until the root is `invalid`.
- `Field.RootProvider` must receive state from `useField()` imported from `@moduix/react`
  and should not be nested inside `Field.Root` for the same field instance.
- `Field` does not render a hidden input; composed controls such as `Checkbox` and `RadioGroup` own
  their own native form parts.

## Examples

### Checkbox

Use `Field` with `Checkbox` when the field owns shared helper text, error text, or required state
around a single checkbox control.

<Preview>
  <FieldDocs.FieldCheckboxExample />

<Preview.Code>
  <include lang="tsx">./_snippets/field/checkbox.tsx#demo</include>
</Preview.Code>

  <Preview.CSS>{`.field { width: fit-content; max-width: min(20rem, 100%); }`}</Preview.CSS>
  
</Preview>

### Controlled Invalid

Derive `invalid` from local state when the field should update as the user types.

<Preview>
  <FieldDocs.FieldControlledInvalidExample />

<Preview.Code>
  <include lang="tsx">./_snippets/field/controlled-invalid.tsx#demo</include>
</Preview.Code>
</Preview>

### Disabled

Set `disabled` on the root to propagate disabled state to Ark field parts.

<Preview>
  <FieldDocs.FieldDisabledExample />

<Preview.Code>
  <include lang="tsx">./_snippets/field/disabled.tsx#demo</include>
</Preview.Code>
</Preview>

### Invalid

Set `invalid` on the root when validation is owned by application state or a form library.

<Preview>
  <FieldDocs.FieldInvalidExample />

<Preview.Code>
  <include lang="tsx">./_snippets/field/invalid.tsx#demo</include>
</Preview.Code>
</Preview>

### Native Select

Use `NativeSelect` for a browser-native select with the field's label, state, and description wiring.

<Preview>
  <FieldDocs.FieldNativeSelectExample />

<Preview.Code>
  <include lang="tsx">./_snippets/field/native-select.tsx#demo</include>
</Preview.Code>
</Preview>

### RadioGroup

Use `Field` with `RadioGroup` when one field label, helper text, or error message should describe
the whole choice set instead of individual native radio inputs.

<Preview>
  <FieldDocs.FieldRadioGroupExample />

<Preview.Code>
  <include lang="tsx">./_snippets/field/radiogroup.tsx#demo</include>
</Preview.Code>
</Preview>

### Read Only

Set `readOnly` on the root when the control value can be copied but not edited.

<Preview>
  <FieldDocs.FieldReadOnlyExample />

<Preview.Code>
  <include lang="tsx">./_snippets/field/read-only.tsx#demo</include>
</Preview.Code>
</Preview>

### Select

Use the composed `Select` when the native control is not enough and you need searchable or custom option UI.

<Preview>
  <FieldDocs.FieldSelectExample />

<Preview.Code>
  <include lang="tsx">./_snippets/field/select.tsx#demo</include>
</Preview.Code>
</Preview>

### Textarea

Use `Field.Textarea` when the field owns a multiline native textarea.

<Preview>
  <FieldDocs.FieldTextareaExample />

<Preview.Code>
  <include lang="tsx">./_snippets/field/textarea.tsx#demo</include>
</Preview.Code>
</Preview>

### Textarea Autoresize

Pass `autoresize` to let Ark keep the textarea height in sync with its content.

<Preview>
  <FieldDocs.FieldTextareaAutoresizeExample />

<Preview.Code>
  <include lang="tsx">./_snippets/field/textarea-autoresize.tsx#demo</include>
</Preview.Code>
</Preview>

### Advanced Customization

Use `Field.Item` with root `target` when one label should point to a control inside a grouped row.

<Preview>
  <FieldDocs.FieldItemExample />

<Preview.Code>
  <include lang="tsx">./_snippets/field/item-target.tsx#demo</include>
</Preview.Code>

  <Preview.CSS>{`.inlineControls { display: grid; grid-template-columns: minmax(6rem, max-content) minmax(0, 1fr); gap: var(--spacing-2); }`}</Preview.CSS>
</Preview>

Use `useField` with `Field.RootProvider` when state must be owned outside the rendered tree.

<Preview>
  <FieldDocs.FieldRootProviderExample />

<Preview.Code>
  <include lang="tsx">./_snippets/field/root-provider.tsx#demo</include>
</Preview.Code>
</Preview>

## Styling

Style `Field` through `className`, Ark `data-scope="field"` / `data-part="..."` attributes,
moduix `data-slot="field-*"` hooks, and the public `--field-*` CSS variables shown below.

### CSS Properties

<div className="not-prose">
  <Tabs items={['CSS Variables']} className="mb-0 mt-3">
    <Tab className="max-h-150 overflow-auto">
      <FieldDocs.FieldCssPropertiesPanel />
    </Tab>
  </Tabs>
</div>

### Styling hooks

Ark adds `data-disabled`, `data-invalid`, `data-readonly`, and `data-required` to the relevant
parts. `Field.ErrorText` is only rendered while the current field context is invalid, and helper or
error text ids are included in the control's `aria-describedby` when present. Every DOM part also
keeps a stable `data-slot="field-*"` hook.