---
title: Checkbox Group
description: A composable group for managing shared state across multiple checkboxes.
icon: Component
---

import {
  CheckboxGroupCssPlaygroundPanel,
  CheckboxGroupCssPropertiesPanel,
  CheckboxGroupExample,
  CheckboxGroupFieldCompositionExample,
  CheckboxGroupFormIntegrationExample,
  CheckboxGroupSiblingLabelNativeButtonExample,
  CheckboxGroupSizesExample,
  ControlledCheckboxGroupExample,
  CustomIconCheckboxGroupExample,
  CustomCompositionCheckboxGroupExample,
  DisabledCheckboxGroupExample,
  ParentCheckboxGroupExample,
  ReadOnlyItemCheckboxGroupExample,
  checkboxGroupPlaygroundCssProperties,
} from '@/components/examples/checkbox-group';

## API Reference

<BaseUIReference href="https://base-ui.com/react/components/checkbox-group" />

## Basic

Use `CheckboxGroup` for shared selection state and the `CheckboxGroupItem*` parts for the default
row layout. `CheckboxGroupItem` and `CheckboxGroupItemLabel` are thin convenience wrappers over
`CheckboxField` and `CheckboxLabel`.

<Preview cssProperties={checkboxGroupPlaygroundCssProperties}>
  <CheckboxGroupExample />

  <Preview.Code>
    {`
      import {
        CheckboxGroup,
        CheckboxGroupItem,
        CheckboxGroupItemControl,
        CheckboxGroupItemLabel,
        CheckboxGroupLabel,
        CheckboxGroupList,
      } from "moduix";
      import { useId } from "react";

      export function CheckboxGroupDemo() {
        const labelId = useId();

        return (
          <CheckboxGroup defaultValue={["email"]} aria-labelledby={labelId}>
            <CheckboxGroupLabel id={labelId}>
              Notification Channels
            </CheckboxGroupLabel>
            <CheckboxGroupList>
              {options.map((option) => (
                <CheckboxGroupItem key={option.value}>
                  <CheckboxGroupItemControl value={option.value} name="notifications" />
                  <CheckboxGroupItemLabel>{option.label}</CheckboxGroupItemLabel>
                </CheckboxGroupItem>
              ))}
            </CheckboxGroupList>
          </CheckboxGroup>
        );
      }
    `}

  </Preview.Code>

  <Preview.Data>
    {`      const options = [
        { value: "email", label: "Email updates" },
        { value: "push", label: "Push notifications" },
        { value: "sms", label: "SMS alerts" },
      ];`}

  </Preview.Data>

  <Preview.CSSProperties>
    {(context) => <CheckboxGroupCssPropertiesPanel {...context} />}
  </Preview.CSSProperties>
  <Preview.CSSPlayground>
    {(context) => <CheckboxGroupCssPlaygroundPanel {...context} />}
  </Preview.CSSPlayground>
</Preview>

## Install with shadcn (optional)

If you want `Checkbox Group` to become part of your own project source instead of staying in
`node_modules`, install it from the moduix GitHub registry:

<Tabs items={['npm', 'pnpm', 'bun']}>
  <Tab value="npm">

```bash
npx shadcn@latest add Blinks44/moduix/checkbox-group
```

  </Tab>
  <Tab value="pnpm">

```bash
pnpm dlx shadcn@latest add Blinks44/moduix/checkbox-group
```

  </Tab>
  <Tab value="bun">

```bash
bunx shadcn@latest add Blinks44/moduix/checkbox-group
```

  </Tab>
</Tabs>

This install path also pulls in the shared moduix foundation files that `Checkbox Group` depends on, such
as styles, icons, and small utilities.

## Anatomy

Checkbox Group keeps one shared root state and a short default composition.
Use `aria-labelledby` on `CheckboxGroup` when `CheckboxGroupLabel` is the visible group label.
Keep `CheckboxGroupItemControl` and `CheckboxGroupItemLabel` inside `CheckboxGroupItem` for the
default enclosing-label row.

```text
CheckboxGroup
├─ CheckboxGroupLabel
└─ CheckboxGroupList
   └─ CheckboxGroupItem
      ├─ CheckboxGroupItemControl
      │  └─ CheckboxIndicator
      │     └─ CheckboxIndicatorIcon
      └─ CheckboxGroupItemLabel
```

```tsx
<CheckboxGroup defaultValue={['email']} aria-labelledby="notification-channels">
  <CheckboxGroupLabel id="notification-channels">Notification Channels</CheckboxGroupLabel>
  <CheckboxGroupList>
    <CheckboxGroupItem>
      <CheckboxGroupItemControl value="email" name="notifications" />
      <CheckboxGroupItemLabel>Email updates</CheckboxGroupItemLabel>
    </CheckboxGroupItem>
  </CheckboxGroupList>
</CheckboxGroup>
```

| Part                       | Role                                                                                |
| -------------------------- | ----------------------------------------------------------------------------------- |
| `CheckboxGroup`            | Root state. Manages selected values, `disabled`, `allValues`, and change callbacks. |
| `CheckboxGroupLabel`       | Optional group heading. Use its `id` with `aria-labelledby` on `CheckboxGroup`.     |
| `CheckboxGroupList`        | Optional list wrapper for checkbox rows.                                            |
| `CheckboxGroupItem`        | Default row wrapper for one control + one text label. Built on `CheckboxField`.     |
| `CheckboxGroupItemControl` | Interactive checkbox control for an item.                                           |
| `CheckboxGroupItemLabel`   | Default item label. Built on `CheckboxLabel`.                                       |

Customize visible parts with `className`, explicit composition, and CSS variables.

## Composition

Use `CheckboxGroup` for shared state (`value`, `defaultValue`, `onValueChange`) and group behavior
(`allValues`, `disabled`). Use `CheckboxGroupItem`, `CheckboxGroupItemControl`, and
`CheckboxGroupItemLabel` for the common row layout. Drop down to `CheckboxField`, `Checkbox`, and
`CheckboxLabel` when the row structure needs to change. Use `allValues` with
`CheckboxGroupItemControl parent` for select-all behavior.

| Prop                                    | Default     | Values                                                                  |
| --------------------------------------- | ----------- | ----------------------------------------------------------------------- |
| `defaultValue`                          | `[]`        | `string[]`                                                              |
| `disabled`                              | `false`     | `boolean`                                                               |
| `value`                                 | `undefined` | `string[]` (controlled mode, with `onValueChange`)                      |
| `allValues`                             | `undefined` | `string[]` (required for parent/select-all checkbox patterns)           |
| `CheckboxGroupItemControl.size`         | `md`        | `xs`, `sm`, `md`, `lg`, `xl`                                            |
| `CheckboxGroupItemControl.parent`       | `false`     | `boolean` (enables parent checkbox behavior when group has `allValues`) |
| `CheckboxGroupItemControl.nativeButton` | `false`     | `boolean`                                                               |
| `CheckboxGroupItemControl.readOnly`     | `false`     | `boolean`                                                               |

`CheckboxGroupLabel`, `CheckboxGroupList`, `CheckboxGroupItem`, and `CheckboxGroupItemLabel`
accept `className`. `CheckboxGroupItemControl` composes `Checkbox`, so it also supports the same
checkbox composition patterns, refs, and form props described on the Checkbox page. Inside a
group, prefer letting `CheckboxGroup` own checked state rather than mixing in per-item `checked`
or `defaultChecked`.

For sibling labels or custom row layouts, compose `CheckboxGroupItemControl` with
your own wrapper element instead of replacing `CheckboxGroupItem`.

Use `className` for the checkbox root. Compose `CheckboxIndicator` when you need
indicator-specific styling or behavior:

```tsx
<CheckboxGroupItemControl className={styles.customControl}>
  <CheckboxIndicator className={styles.customIndicator} keepMounted />
</CheckboxGroupItemControl>
```

## Examples

### Sizes

Use `size` on `CheckboxGroupItemControl` to align the controls with different form densities.

<Preview>
  <CheckboxGroupSizesExample />

  <Preview.Code>
    {`
      import {
        CheckboxGroup,
        CheckboxGroupItem,
        CheckboxGroupItemControl,
        CheckboxGroupItemLabel,
        CheckboxGroupLabel,
        CheckboxGroupList,
      } from "moduix";

      export function CheckboxGroupSizesDemo() {
        return (
          <CheckboxGroup defaultValue={["md"]} aria-labelledby="control-size">
            <CheckboxGroupLabel id="control-size">Control Size</CheckboxGroupLabel>
            <CheckboxGroupList>
              {options.map((option) => (
                <CheckboxGroupItem key={option.value}>
                  <CheckboxGroupItemControl value={option.value} size={option.value} />
                  <CheckboxGroupItemLabel>{option.label}</CheckboxGroupItemLabel>
                </CheckboxGroupItem>
              ))}
            </CheckboxGroupList>
          </CheckboxGroup>
        );
      }
    `}

  </Preview.Code>

  <Preview.Data>
    {`
      const options = [
        { value: "xs", label: "Extra-small" },
        { value: "sm", label: "Small" },
        { value: "md", label: "Medium" },
        { value: "lg", label: "Large" },
        { value: "xl", label: "Extra-large" },
      ] as const;
    `}

  </Preview.Data>
</Preview>

### Controlled

Control `value` from React state when the selected options need to coordinate with other UI.

<Preview>
  <ControlledCheckboxGroupExample />

  <Preview.Code>
    {`
      import {
        CheckboxGroup,
        CheckboxGroupItem,
        CheckboxGroupItemControl,
        CheckboxGroupItemLabel,
        CheckboxGroupLabel,
        CheckboxGroupList,
      } from "moduix";
      import { useState } from "react";

      export function ControlledCheckboxGroupDemo() {
        const [value, setValue] = useState(["push"] as string[]);

        return (
          <div className={styles.wrapper}>
            <CheckboxGroup
              value={value}
              onValueChange={setValue}
              aria-labelledby="active-alerts"
            >
              <CheckboxGroupLabel id="active-alerts">Active Alerts</CheckboxGroupLabel>
              <CheckboxGroupList>
                {options.map((option) => (
                  <CheckboxGroupItem key={option.value}>
                    <CheckboxGroupItemControl value={option.value} name="alerts" />
                    <CheckboxGroupItemLabel>{option.label}</CheckboxGroupItemLabel>
                  </CheckboxGroupItem>
                ))}
              </CheckboxGroupList>
            </CheckboxGroup>
            <span className={styles.hint}>Current value: {value.join(", ") || "none"}</span>
          </div>
        );
      }
    `}

  </Preview.Code>

  <Preview.CSS>
    {`
      .wrapper {
        display: flex;
        flex-direction: column;
        align-items: flex-start;
        gap: var(--spacing-2);
      }

      .hint {
        color: var(--color-muted-foreground);
        font-size: var(--text-xs);
        line-height: var(--line-height-text-xs);
      }
    `}

  </Preview.CSS>

  <Preview.Data>
    {`
      const options = [
        { value: "email", label: "Email updates" },
        { value: "push", label: "Push notifications" },
        { value: "sms", label: "SMS alerts" },
      ];
    `}

  </Preview.Data>
</Preview>

### Disabled

Use `disabled` on the group to prevent interaction with every checkbox in the group.

<Preview>
  <DisabledCheckboxGroupExample />

  <Preview.Code>
    {`
      import {
        CheckboxGroup,
        CheckboxGroupItem,
        CheckboxGroupItemControl,
        CheckboxGroupItemLabel,
        CheckboxGroupLabel,
        CheckboxGroupList,
      } from "moduix";

      export function DisabledCheckboxGroupDemo() {
        return (
          <CheckboxGroup
            defaultValue={["push"]}
            disabled
            aria-labelledby="disabled-settings"
          >
            <CheckboxGroupLabel id="disabled-settings">Disabled Settings</CheckboxGroupLabel>
            <CheckboxGroupList>
              {options.map((option) => (
                <CheckboxGroupItem key={option.value}>
                  <CheckboxGroupItemControl value={option.value} name="disabled-settings" />
                  <CheckboxGroupItemLabel>{option.label}</CheckboxGroupItemLabel>
                </CheckboxGroupItem>
              ))}
            </CheckboxGroupList>
          </CheckboxGroup>
        );
      }
    `}

  </Preview.Code>

  <Preview.Data>
    {`
      const options = [
        { value: "email", label: "Email updates" },
        { value: "push", label: "Push notifications" },
        { value: "sms", label: "SMS alerts" },
      ];
    `}

  </Preview.Data>
</Preview>

### Read-only Item

Use `readOnly` on an item control when that checkbox should stay focusable and visible but must not
change. The group root supports `disabled`, but not a group-level `readOnly` prop.

<Preview>
  <ReadOnlyItemCheckboxGroupExample />

  <Preview.Code>
    {`
      import {
        CheckboxGroup,
        CheckboxGroupItem,
        CheckboxGroupItemControl,
        CheckboxGroupItemLabel,
        CheckboxGroupLabel,
        CheckboxGroupList,
      } from "moduix";
      import { useId } from "react";

      export function ReadOnlyItemCheckboxGroupDemo() {
        const labelId = useId();

        return (
          <CheckboxGroup defaultValue={["push"]} aria-labelledby={labelId}>
            <CheckboxGroupLabel id={labelId}>Partially Locked Settings</CheckboxGroupLabel>
            <CheckboxGroupList>
              {options.map((option) => (
                <CheckboxGroupItem key={option.value}>
                  <CheckboxGroupItemControl
                    value={option.value}
                    name="partially-locked-settings"
                    readOnly={option.value === "push"}
                  />
                  <CheckboxGroupItemLabel>{option.label}</CheckboxGroupItemLabel>
                </CheckboxGroupItem>
              ))}
            </CheckboxGroupList>
          </CheckboxGroup>
        );
      }
    `}

  </Preview.Code>

  <Preview.Data>
    {`      const options = [
        { value: "email", label: "Email updates" },
        { value: "push", label: "Push notifications" },
        { value: "sms", label: "SMS alerts" },
      ];`}

  </Preview.Data>
</Preview>

### Parent Checkbox

Use `allValues` with a `parent` checkbox to create a select-all control that enters the
indeterminate state when only some child options are checked.

<Preview>
  <ParentCheckboxGroupExample />

  <Preview.Code>
    {`
      import {
        CheckboxGroup,
        CheckboxGroupItem,
        CheckboxGroupItemControl,
        CheckboxGroupItemLabel,
        CheckboxGroupLabel,
        CheckboxGroupList,
      } from "moduix";
      import { useState } from "react";

      export function ParentCheckboxGroupDemo() {
        const [value, setValue] = useState([] as string[]);

        return (
          <CheckboxGroup
            value={value}
            onValueChange={setValue}
            allValues={fruitValues}
            aria-labelledby="fruits"
          >
            <CheckboxGroupLabel id="fruits">Fruits</CheckboxGroupLabel>
            <CheckboxGroupList>
              <CheckboxGroupItem>
                <CheckboxGroupItemControl parent />
                <CheckboxGroupItemLabel>Select all</CheckboxGroupItemLabel>
              </CheckboxGroupItem>

              {options.map((option) => (
                <CheckboxGroupItem key={option.value}>
                  <CheckboxGroupItemControl value={option.value} />
                  <CheckboxGroupItemLabel>{option.label}</CheckboxGroupItemLabel>
                </CheckboxGroupItem>
              ))}
            </CheckboxGroupList>
          </CheckboxGroup>
        );
      }
    `}

  </Preview.Code>

  <Preview.Data>
    {`
      const options = [
        { value: "apple", label: "Apple" },
        { value: "orange", label: "Orange" },
        { value: "pear", label: "Pear" },
      ];

      const fruitValues = options.map((option) => option.value);
    `}

  </Preview.Data>
</Preview>

### Custom Icon

Compose `CheckboxIndicator` inside `CheckboxGroupItemControl` to use any icon from your
application or icon library.

<Preview>
  <CustomIconCheckboxGroupExample />

  <Preview.Code>
    {`
      import {
        CheckboxGroup,
        CheckboxGroupItem,
        CheckboxGroupItemControl,
        CheckboxGroupItemLabel,
        CheckboxGroupLabel,
        CheckboxGroupList,
        CheckboxIndicator,
      } from "moduix";

      function CustomPlusIcon() {
        return (
          <svg viewBox="0 0 10 10" fill="none" aria-hidden="true" focusable="false">
            <path
              d="M5 1.5V8.5M1.5 5H8.5"
              stroke="currentColor"
              strokeWidth="1.8"
              strokeLinecap="round"
            />
          </svg>
        );
      }

      export function CustomIconCheckboxGroupDemo() {
        return (
          <CheckboxGroup defaultValue={["email"]} aria-labelledby="custom-indicators">
            <CheckboxGroupLabel id="custom-indicators">
              Custom Indicators
            </CheckboxGroupLabel>
            <CheckboxGroupList>
              {options.map((option) => (
                <CheckboxGroupItem key={option.value}>
                  <CheckboxGroupItemControl value={option.value} name="custom-indicators">
                    <CheckboxIndicator>
                      <CustomPlusIcon className={styles.customIndicatorIcon} />
                    </CheckboxIndicator>
                  </CheckboxGroupItemControl>
                  <CheckboxGroupItemLabel>{option.label}</CheckboxGroupItemLabel>
                </CheckboxGroupItem>
              ))}
            </CheckboxGroupList>
          </CheckboxGroup>
        );
      }
    `}

  </Preview.Code>

  <Preview.Data>
    {`
      const options = [
        { value: "email", label: "Email updates" },
        { value: "push", label: "Push notifications" },
        { value: "sms", label: "SMS alerts" },
      ];
    `}

  </Preview.Data>
</Preview>

## Custom Composition

Use the higher-level parts by default. Drop to the lower-level composition shown here only when the
row layout, indicator markup, or form structure needs to change.

### Slot Styling

Pass `className` to the group, label, list, item, control, and item label slots. Use
composition on `CheckboxGroupItemControl` when the indicator needs its own selector.

<Preview>
  <CustomCompositionCheckboxGroupExample />

  <Preview.Code>
    {`
      import {
        CheckboxGroup,
        CheckboxGroupItem,
        CheckboxGroupItemControl,
        CheckboxGroupItemLabel,
        CheckboxGroupLabel,
        CheckboxGroupList,
        CheckboxIndicator,
      } from "moduix";

      export function CustomCompositionCheckboxGroupDemo() {
        return (
          <CheckboxGroup
            defaultValue={["email"]}
            className={styles.customGroup}
            aria-labelledby="styled-channels"
          >
            <CheckboxGroupLabel id="styled-channels" className={styles.customLabel}>
              Styled Channels
            </CheckboxGroupLabel>
            <CheckboxGroupList className={styles.customList}>
              {options.map((option) => (
                <CheckboxGroupItem key={option.value} className={styles.customItem}>
                  <CheckboxGroupItemControl
                    value={option.value}
                    name="styled-notifications"
                    className={styles.customControl}
                  >
                    <CheckboxIndicator className={styles.customIndicator} />
                  </CheckboxGroupItemControl>
                  <CheckboxGroupItemLabel className={styles.customItemLabel}>
                    {option.label}
                  </CheckboxGroupItemLabel>
                </CheckboxGroupItem>
              ))}
            </CheckboxGroupList>
          </CheckboxGroup>
        );
      }
    `}

  </Preview.Code>

  <Preview.CSS>
    {`
      .customGroup,
      .customList,
      .customItem {
        gap: var(--spacing-3);
      }

      .customLabel,
      .customItemLabel {
        color: var(--color-primary);
      }

      .customControl {
        border-color: var(--color-primary);

        &[data-checked] {
          background-color: var(--color-primary);
        }
      }

      .customIndicator {
        color: var(--color-primary-foreground);
      }
    `}

  </Preview.CSS>

  <Preview.Data>
    {`
      const options = [
        { value: "email", label: "Email updates" },
        { value: "push", label: "Push notifications" },
        { value: "sms", label: "SMS alerts" },
      ];
    `}

  </Preview.Data>
</Preview>

### CheckboxField Composition

Use `CheckboxField`, `Checkbox`, and `CheckboxLabel` directly inside `CheckboxGroup` when you
want a compact enclosing-label composition.

<Preview>
  <CheckboxGroupFieldCompositionExample />

  <Preview.Code>
    {`
      import {
        Checkbox,
        CheckboxField,
        CheckboxGroup,
        CheckboxGroupLabel,
        CheckboxGroupList,
        CheckboxLabel,
      } from "moduix";

      export function CheckboxGroupFieldCompositionDemo() {
        return (
          <CheckboxGroup defaultValue={["email"]} aria-labelledby="channels">
            <CheckboxGroupLabel id="channels">Channels</CheckboxGroupLabel>
            <CheckboxGroupList>
              {options.map((option) => (
                <CheckboxField key={option.value}>
                  <Checkbox value={option.value} name="field-composition" />
                  <CheckboxLabel>{option.label}</CheckboxLabel>
                </CheckboxField>
              ))}
            </CheckboxGroupList>
          </CheckboxGroup>
        );
      }
    `}

  </Preview.Code>

  <Preview.Data>
    {`
      const options = [
        { value: "email", label: "Email updates" },
        { value: "push", label: "Push notifications" },
        { value: "sms", label: "SMS alerts" },
      ];
    `}

  </Preview.Data>
</Preview>

### Sibling Label

Use `nativeButton` with `render={<button />}` for the sibling `label` pattern with `htmlFor`
and `id`.

<Preview>
  <CheckboxGroupSiblingLabelNativeButtonExample />

  <Preview.Code>
    {`
      import { CheckboxGroup, CheckboxGroupItemControl } from "moduix";
      import { useId } from "react";

      export function SiblingLabelCheckboxGroupDemo() {
        const id = useId();
        const labelId = useId();

        return (
          <div className={styles.siblingRow}>
            <div id={labelId} className={styles.hint}>
              Channels
            </div>
            <CheckboxGroup defaultValue={["email"]} aria-labelledby={labelId}>
              <CheckboxGroupItemControl
                nativeButton
                render={<button />}
                id={id}
                value="email"
                name="sibling-notifications"
              />
            </CheckboxGroup>
            <label htmlFor={id} className={styles.label}>
              Email updates
            </label>
          </div>
        );
      }
    `}

  </Preview.Code>

  <Preview.CSS>
    {`
      .siblingRow {
        display: flex;
        align-items: center;
        gap: var(--checkbox-gap, var(--spacing-2));
      }

      .hint {
        color: var(--color-muted-foreground);
        font-size: var(--text-xs);
        line-height: var(--line-height-text-xs);
      }

      .label {
        color: var(--checkbox-label-color, var(--color-foreground));
        font-size: var(--checkbox-label-font-size, var(--text-sm));
        font-weight: var(--checkbox-label-font-weight, var(--weight-medium));
        line-height: var(--checkbox-label-line-height, var(--line-height-text-sm));
      }
    `}

  </Preview.CSS>
</Preview>

### Form Integration

Use `Field` and `Fieldset` when the group needs form naming, validation state, or shared
labeling.

<Preview>
  <CheckboxGroupFormIntegrationExample />

  <Preview.Code>
    {`
      import {
        Checkbox,
        CheckboxGroup,
        CheckboxLabel,
        Field,
        FieldItem,
        FieldLabel,
        Fieldset,
        FieldsetLegend,
      } from "moduix";

      export function CheckboxGroupFormIntegrationDemo() {
        return (
          <Field name="notificationChannels">
            <Fieldset render={<CheckboxGroup defaultValue={["email"]} />}>
              <FieldsetLegend>Notification Channels</FieldsetLegend>
              {options.map((option) => (
                <FieldItem key={option.value}>
                  <FieldLabel>
                    <Checkbox value={option.value} />
                    <CheckboxLabel>{option.label}</CheckboxLabel>
                  </FieldLabel>
                </FieldItem>
              ))}
            </Fieldset>
          </Field>
        );
      }
    `}

  </Preview.Code>

  <Preview.Data>
    {`
      const options = [
        { value: "email", label: "Email updates" },
        { value: "push", label: "Push notifications" },
        { value: "sms", label: "SMS alerts" },
      ];
    `}

  </Preview.Data>
</Preview>