---
title: List
description: A semantic list wrapper built with Ark-style composition, moduix spacing tokens, and native marker styling.
icon: Component
---

import {
  CustomCompositionListExample,
  customStyleItemsData,
  ListCssPropertiesPanel,
  listCustomStylingCss,
  ListExample,
  listItemsData,
  listNoopCss,
  listToneExampleCss,
  MarkerlessListExample,
  markerlessListItemsData,
  NativeItemsListExample,
  nativeListItemsData,
  OrderedListExample,
  orderedListItemsData,
  ToneListExample,
  toneListItemsData,
} from '@/components/examples/list';

## API Reference

Ark UI does not ship a dedicated list primitive. Moduix builds this component with the official
[Ark factory](https://ark-ui.com/docs/guides/composition#the-ark-factory), follows Ark's
[composition](https://ark-ui.com/docs/guides/composition), [styling](https://ark-ui.com/docs/guides/styling), and
[ref](https://ark-ui.com/docs/guides/ref) guidance.

## Basic

Start with `List` and `List.Item`. This is the recommended path when you want native list semantics with moduix spacing, typography, and marker tokens.

<Preview>
  <ListExample />

  <Preview.Code>
    {`
      import { List } from "@moduix/react";

      export function ListDemo() {
        return (
          <List>
            <List.Item>Use semantic list markup for grouped content.</List.Item>
            <List.Item>Keep spacing and typography on the library scale.</List.Item>
            <List.Item>Style markers with CSS variables or native ::marker selectors.</List.Item>
          </List>
        );
      }
    `}

  </Preview.Code>

  <Preview.CSS>{listNoopCss}</Preview.CSS>
  <Preview.Data>{listItemsData}</Preview.Data>
</Preview>

## Install with shadcn

If you want `List` to become part of your own project source instead of staying in `node_modules`, install it from the moduix GitHub registry:

<Tabs items={['npm', 'pnpm', 'bun', 'deno', 'aube']}>
  <Tab value="npm">

```bash
npx shadcn@latest add Blinks44/moduix/react-list
```

  </Tab>
  <Tab value="pnpm">

```bash
pnpm dlx shadcn@latest add Blinks44/moduix/react-list
```

  </Tab>
  <Tab value="bun">

```bash
bunx shadcn@latest add Blinks44/moduix/react-list
```

  </Tab>
  <Tab value="deno">

```bash
deno x -A npm:shadcn@latest add Blinks44/moduix/react-list
```

  </Tab>
  <Tab value="aube">

```bash
aubx shadcn@latest add Blinks44/moduix/react-list
```

  </Tab>
</Tabs>

This install path also pulls in the shared moduix foundation files that `List` depends on, such as styles and small utilities.

## Anatomy

The normal consumer path is short:

```text
List / List.Root
└─ List.Item | li
```

| Part                 | Role                                                               |
| -------------------- | ------------------------------------------------------------------ |
| `List` / `List.Root` | Semantic list root that controls marker, spacing, size, and tone.  |
| `List.Item`          | Optional Ark-style item part for a stable `li` slot and `asChild`. |

## Composition

### Recommended composition

Stay with `<List>` for unordered content and use `as="ol"` when you need ordered semantics. Use `asChild` only when the caller needs to own a custom semantic host.

### When to go custom

`List` does not expose an internal state machine, nested structure helpers, or a marker render API. When you need a custom host element or per-item markup, use `asChild`, plain `li` children, and native CSS such as `li::marker`.

Keep items as direct `li` descendants. The built-in spacing and marker styling apply only to direct list items.

### Default props

| Prop      | Default         | Notes                                                                |
| --------- | --------------- | -------------------------------------------------------------------- |
| `gap`     | `sm`            | Spacing between direct list items.                                   |
| `size`    | `md`            | Typography size token.                                               |
| `tone`    | `default`       | Semantic color path for text and markers.                            |
| `marker`  | semantic `auto` | `disc` on `ul`, browser-provided ordered markers on `ol`, or `none`. |
| `as`      | `ul`            | Switches the native list host between `ul` and `ol`.                 |
| `asChild` | `false`         | Replaces the semantic host with a single caller-owned child element. |

### Behavioral notes

- Ordered-list props such as `start`, `reversed`, and `type` are available when `as="ol"` is used.
- Use `marker="none"` to hide markers while keeping list semantics. `List` adds `role="list"` on that path unless you explicitly override `role`.
- `List.Item` is optional. Use plain `li` elements when the exported item slot adds no value.

## Examples

### Ordered

Use `as="ol"` to render an ordered list while keeping the moduix root styling contract.

<Preview>
  <OrderedListExample />

  <Preview.Code>
    {`
      import { List } from "@moduix/react";

      export function OrderedListDemo() {
        return (
          <List as="ol" start={3}>
            <List.Item>Prepare the release notes.</List.Item>
            <List.Item>Publish the package.</List.Item>
            <List.Item>Announce the release.</List.Item>
          </List>
        );
      }
    `}

  </Preview.Code>

  <Preview.CSS>{listNoopCss}</Preview.CSS>
  <Preview.Data>{orderedListItemsData}</Preview.Data>
</Preview>

### Markerless

Use `marker="none"` when you need list semantics without visible markers.

<Preview>
  <MarkerlessListExample />

  <Preview.Code>
    {`
      import { List } from "@moduix/react";

      export function MarkerlessListDemo() {
        return (
          <List marker="none">
            <List.Item>Semantics stay intact without visible markers.</List.Item>
            <List.Item>Useful for grouped metadata or key-value blocks.</List.Item>
            <List.Item>Spacing and text tokens still come from the root.</List.Item>
          </List>
        );
      }
    `}

  </Preview.Code>

  <Preview.CSS>{listNoopCss}</Preview.CSS>
  <Preview.Data>{markerlessListItemsData}</Preview.Data>
</Preview>

### Native Items

Use native `li` elements when you do not need the exported `List.Item` slot.

<Preview>
  <NativeItemsListExample />

  <Preview.Code>
    {`
      import { List } from "@moduix/react";

      export function NativeItemsListDemo() {
        return (
          <List>
            <li>Use native li elements when a wrapper component is unnecessary.</li>
            <li>The root still controls spacing, marker style, size, and tone.</li>
            <li>Reach for List.Item when you want the stable item slot.</li>
          </List>
        );
      }
    `}

  </Preview.Code>

  <Preview.CSS>{listNoopCss}</Preview.CSS>
  <Preview.Data>{nativeListItemsData}</Preview.Data>
</Preview>

### Tone

Use `tone` when the list should inherit a semantic text color.

<Preview>
  <ToneListExample />

  <Preview.Code>
    {`
      import { List } from "@moduix/react";

      export function ListToneDemo() {
        return (
          <div className="list-demo-tones">
            <List tone="default">
              <List.Item>Default list tone</List.Item>
            </List>
            <List tone="muted">
              <List.Item>Muted list tone</List.Item>
            </List>
            <List tone="subtle">
              <List.Item>Subtle list tone</List.Item>
            </List>
            <List tone="primary">
              <List.Item>Primary list tone</List.Item>
            </List>
            <List tone="destructive">
              <List.Item>Destructive list tone</List.Item>
            </List>
          </div>
        );
      }
    `}

  </Preview.Code>

  <Preview.CSS>{listToneExampleCss}</Preview.CSS>
  <Preview.Data>{toneListItemsData}</Preview.Data>
</Preview>

### Custom Composition

Use `List.Item asChild` when the item slot should stay public but the caller needs to own the rendered `li`.

<Preview>
  <CustomCompositionListExample />

  <Preview.Code>
    {`
      import { List } from "@moduix/react";

      export function CustomCompositionListDemo() {
        return (
          <List className="list-demo-accent">
            <List.Item asChild>
              <li className="list-demo-accent-item">Native markers stay available for per-item styling.</li>
            </List.Item>
            <List.Item asChild>
              <li className="list-demo-accent-item">Root CSS variables still control spacing and indentation.</li>
            </List.Item>
            <List.Item asChild>
              <li className="list-demo-accent-item">asChild keeps the slot contract while handing markup to the caller.</li>
            </List.Item>
          </List>
        );
      }
    `}

  </Preview.Code>

  <Preview.CSS>{listCustomStylingCss}</Preview.CSS>
  <Preview.Data>{customStyleItemsData}</Preview.Data>
</Preview>

## Styling

Style `List` through `className`, the exported Ark-style `data-*` hooks, native `li::marker`, and the public `--list-*` CSS variables shown below. The variable list here was checked against `packages/react/src/lib/moduix/styles/theme.css`.

### CSS Properties

<div className="not-prose">
  <Tabs items={['CSS Variables']} className="mb-0 mt-3">
    <Tab className="max-h-150 overflow-auto">
      <ListCssPropertiesPanel />
    </Tab>
  </Tabs>
</div>

### Styling hooks

`List` exposes a small public styling surface:

- root: `data-scope="list"`, `data-part="root"`, `data-slot="list-root"`
- item: `data-scope="list"`, `data-part="item"`, `data-slot="list-item"`
- `data-gap="xs" | "sm" | "md" | "lg" | "xl" | "2xl"` for spacing-specific styling
- `data-marker="auto" | "disc" | "decimal" | "none"` for marker-specific styling
- `data-size="xs" | "sm" | "md" | "lg" | "xl"` for typography size styling
- `data-tone="default" | "muted" | "subtle" | "primary" | "destructive"` for semantic color paths
- native `li::marker` targeting for marker-specific color or weight overrides