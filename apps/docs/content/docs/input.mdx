---
title: Input
description: An Ark Field input wrapper with native events, form semantics, and moduix styling.
icon: Component
---

import {
  ControlledInputExample,
  CustomStylesInputExample,
  DisabledAndReadOnlyInputExample,
  InputAsChildExample,
  InputCssPropertiesPanel,
  InputExample,
  InputFieldValidationExample,
  InputNativeAttributesExample,
  InputSizesExample,
  StandaloneInputExample,
} from '@/components/examples/input';

## API Reference

Ark UI does not ship a dedicated input primitive. Moduix builds this component with the official
[Ark factory](https://ark-ui.com/docs/guides/composition#the-ark-factory), follows Ark's
[composition](https://ark-ui.com/docs/guides/composition), [styling](https://ark-ui.com/docs/guides/styling), and
[ref](https://ark-ui.com/docs/guides/ref) guidance, and delegates field behavior to
[Ark Field.Input](https://ark-ui.com/docs/components/field).

## Choosing the right component

- Choose **`Input`** for a plain single-line native input.
- Choose **`InputGroup`** for inline addons, text, or actions.
- Choose **`PasswordInput`** for the existing visibility-toggle wrapper.
- Choose **`NumberInput`** for numeric parsing and stepping.
- Choose **`Textarea`** for multi-line content.
- Choose **`Editable`** for preview-first text that switches into edit mode.

## Basic

Use `Input` inside `Field` so Ark can connect the label, helper text, ids, and form state.

<Preview>
  <InputExample />

  <Preview.Code>
    {`
      import { Field, Input } from "@moduix/react";

      export function InputDemo() {
        return (
          <Field className="input-demo-field">
            <Field.Label>Name</Field.Label>
            <Field.HelperText>Used in your public workspace profile.</Field.HelperText>
            <Input name="name" placeholder="Enter your name" />
          </Field>
        );
      }
    `}

  </Preview.Code>

<Preview.CSS>
  {`
      .input-demo-field {
        width: min(14rem, 100%);
      }
    `}
</Preview.CSS>

  <Preview.Data>
    {`
      const profileField = {
        name: "name",
        placeholder: "Enter your name",
      };
    `}
  </Preview.Data>
</Preview>

## Install with shadcn

<Tabs items={['npm', 'pnpm', 'bun', 'deno', 'aube']}>
  <Tab value="npm">

```bash
npx shadcn@latest add Blinks44/moduix/react-input
```

  </Tab>
  <Tab value="pnpm">

```bash
pnpm dlx shadcn@latest add Blinks44/moduix/react-input
```

  </Tab>
  <Tab value="bun">

```bash
bunx shadcn@latest add Blinks44/moduix/react-input
```

  </Tab>
  <Tab value="deno">

```bash
deno x -A npm:shadcn@latest add Blinks44/moduix/react-input
```

  </Tab>
  <Tab value="aube">

```bash
aubx shadcn@latest add Blinks44/moduix/react-input
```

  </Tab>
</Tabs>

## Anatomy

```text
Field.Root (optional)
└─ Input
```

| Part                   | Role                                                                  |
| ---------------------- | --------------------------------------------------------------------- |
| `Input` / `Input.Root` | Styled Ark `Field.Input` that renders the native single-line control. |

## Composition

### Recommended composition

Render `Input` inside `Field` for labels, helper/error text, ids, and shared `disabled`, `invalid`,
`required`, or `readOnly` state. `Fieldset` can provide disabled state to nested fields.

Use native input props and events. Controlled inputs use `value` and `onChange(event)`;
uncontrolled inputs use `defaultValue`.

### When to go custom

Use `asChild` when one semantic input element must own the rendered node. Ark merges the wrapper
props, field props, class, and ref into that single child.

For a control that is not input-compatible, use `Field.Context` and `context.getInputProps()`
instead of forcing it through `Input`.

### Default props

| Prop       | Default | Notes                                                  |
| ---------- | ------- | ------------------------------------------------------ |
| `size`     | `md`    | Visual size: `xs`, `sm`, `md`, `lg`, or `xl`.          |
| `htmlSize` | —       | Writes the native character-based `input[size]` value. |
| `asChild`  | `false` | Merges props into one semantic child through Ark Slot. |

### Behavioral notes

- The ref points to the real `HTMLInputElement`.
- No `HiddenInput` is needed because `Input` is the native form control.
- Ark `Field` supplies `aria-describedby`, `aria-invalid`, ids, and state props.
- `Input` does not manage value, validity, focus, or keyboard navigation.

## Examples

### Controlled

Use the native change event when React owns the current value.

<Preview>
  <ControlledInputExample />

  <Preview.Code>
    {`
      import { Field, Input } from "@moduix/react";
      import { useState } from "react";

      export function ControlledInputDemo() {
        const [value, setValue] = useState("");

        return (
          <Field className="input-demo-field">
            <Field.Label>Username</Field.Label>
            <Input
              value={value}
              onChange={(event) => setValue(event.currentTarget.value)}
              placeholder="Type to control value"
            />
          </Field>
        );
      }
    `}

  </Preview.Code>

<Preview.CSS>
  {`
      .input-demo-field {
        width: min(14rem, 100%);
      }
    `}
</Preview.CSS>

  <Preview.Data>
    {`
      const initialValue = "";
    `}
  </Preview.Data>
</Preview>

### Field State

Put invalid state on `Field`; Ark applies `aria-invalid` and `data-invalid` to the input and
connects the error text.

<Preview>
  <InputFieldValidationExample />

  <Preview.Code>
    {`
      import { Field, Input } from "@moduix/react";

      export function InvalidInputDemo() {
        return (
          <Field className="input-demo-field" invalid>
            <Field.Label>Email</Field.Label>
            <Input type="email" placeholder="name@example.com" />
            <Field.ErrorText>Enter a valid email address.</Field.ErrorText>
          </Field>
        );
      }
    `}

  </Preview.Code>

<Preview.CSS>
  {`
      .input-demo-field {
        width: min(14rem, 100%);
      }
    `}
</Preview.CSS>

  <Preview.Data>
    {`
      const invalidEmail = "not-an-email";
    `}
  </Preview.Data>
</Preview>

### Disabled and Readonly

Use native props directly or place them on `Field.Root` when the full field should share state.

<Preview>
  <DisabledAndReadOnlyInputExample />

  <Preview.Code>
    {`
      import { Input } from "@moduix/react";

      export function InputStatesDemo() {
        return (
          <div className="input-demo-stack">
            <Input disabled aria-label="Disabled input" placeholder="Disabled input" />
            <Input readOnly aria-label="Read-only workspace" value="Assigned workspace" />
          </div>
        );
      }
    `}

  </Preview.Code>

<Preview.CSS>
  {`
      .input-demo-stack {
        display: grid;
        gap: var(--spacing-3);
        width: min(14rem, 100%);
      }
    `}
</Preview.CSS>

  <Preview.Data>
    {`
      const assignedWorkspace = "Assigned workspace";
    `}
  </Preview.Data>
</Preview>

### Native Attributes

Native attributes continue to control browser validation, keyboard, autocomplete, and form
submission. Use `htmlSize` only for character-based width.

<Preview>
  <InputNativeAttributesExample />

  <Preview.Code>
    {`
      import { Field, Input } from "@moduix/react";

      export function SecurityCodeDemo() {
        return (
          <Field className="input-demo-field">
            <Field.Label>Security code</Field.Label>
            <Input
              htmlSize={8}
              inputMode="numeric"
              maxLength={6}
              name="security-code"
              autoComplete="one-time-code"
              placeholder="000000"
            />
          </Field>
        );
      }
    `}

  </Preview.Code>

<Preview.CSS>
  {`
      .input-demo-field {
        width: min(14rem, 100%);
      }
    `}
</Preview.CSS>

  <Preview.Data>
    {`
      const securityCodeLength = 6;
    `}
  </Preview.Data>
</Preview>

### As Child

Use `asChild` for a custom input host while preserving one semantic, focusable input element.

<Preview>
  <InputAsChildExample />

  <Preview.Code>
    {`
      import { Field, Input } from "@moduix/react";

      export function InputAsChildDemo() {
        return (
          <Field className="input-demo-field">
            <Field.Label>Repository</Field.Label>
            <Input asChild>
              <input name="repository" placeholder="owner/project" />
            </Input>
          </Field>
        );
      }
    `}

  </Preview.Code>

<Preview.CSS>
  {`
      .input-demo-field {
        width: min(14rem, 100%);
      }
    `}
</Preview.CSS>

  <Preview.Data>
    {`
      const repositoryPlaceholder = "owner/project";
    `}
  </Preview.Data>
</Preview>

### Sizes

The moduix `size` prop changes visual density without replacing the native `size` attribute.

<Preview>
  <InputSizesExample />

  <Preview.Code>
    {`
      import { Input } from "@moduix/react";

      export function InputSizesDemo() {
        return (
          <div className="input-demo-stack">
            <Input size="xs" aria-label="Extra-small input" placeholder="Extra-small input" />
            <Input size="sm" aria-label="Small input" placeholder="Small input" />
            <Input size="md" aria-label="Medium input" placeholder="Medium input" />
            <Input size="lg" aria-label="Large input" placeholder="Large input" />
            <Input size="xl" aria-label="Extra-large input" placeholder="Extra-large input" />
          </div>
        );
      }
    `}

  </Preview.Code>

<Preview.CSS>
  {`
      .input-demo-stack {
        display: grid;
        gap: var(--spacing-3);
        width: min(14rem, 100%);
      }
    `}
</Preview.CSS>

  <Preview.Data>
    {`
      const inputSizes = ["xs", "sm", "md", "lg", "xl"] as const;
    `}
  </Preview.Data>
</Preview>

### Standalone Input

Outside `Field`, provide an accessible name explicitly. Native state props still work, but there is
no field context to supply ids or helper/error relationships.

<Preview>
  <StandaloneInputExample />

  <Preview.Code>
    {`
      import { Input } from "@moduix/react";

      export function StandaloneInputDemo() {
        return (
          <Input
            className="input-demo-standalone"
            aria-label="Search projects"
            placeholder="Search projects"
          />
        );
      }
    `}

  </Preview.Code>

<Preview.CSS>
  {`
      .input-demo-standalone {
        max-width: 14rem;
      }
    `}
</Preview.CSS>

  <Preview.Data>
    {`
      const accessibleName = "Search projects";
    `}
  </Preview.Data>
</Preview>

### Custom Styling

Use `className`, public CSS variables, Ark anatomy attributes, and native state selectors.

<Preview>
  <CustomStylesInputExample />

  <Preview.Code>
    {`
      import { Field, Input } from "@moduix/react";

      export function CustomInputDemo() {
        return (
          <Field className="input-demo-field">
            <Field.Label>Project key</Field.Label>
            <Input placeholder="MAPS" className="input-demo-custom-input" />
          </Field>
        );
      }
    `}

  </Preview.Code>

  <Preview.CSS>
    {`
      .input-demo-custom-input {
        --input-border-color: color-mix(in srgb, var(--color-primary) 40%, transparent);
        --input-bg: color-mix(in srgb, var(--color-primary) 5%, transparent);
        --input-focus-ring-color: var(--color-primary);

        color: var(--color-primary);
        text-transform: uppercase;
      }
    `}

  </Preview.CSS>

  <Preview.Data>
    {`
      const projectKey = "MAPS";
    `}
  </Preview.Data>
</Preview>

## Styling

### CSS Properties

<div className="not-prose">
  <Tabs items={['CSS Variables']} className="mb-0 mt-3">
    <Tab className="max-h-150 overflow-auto">
      <InputCssPropertiesPanel />
    </Tab>
  </Tabs>
</div>

### Styling hooks

- `data-slot="input-root"`
- `data-size="xs" | "sm" | "md" | "lg" | "xl"`
- `data-scope="field"` and `data-part="input"`
- Ark `data-invalid`, `data-required`, and `data-readonly` inside `Field`
- native `:disabled`, `:read-only`, `:focus-visible`, and `[aria-invalid="true"]`