---
title: Skeleton
description: A loading placeholder block for text, cards, media objects, and custom layouts.
icon: Component
---

import {
  SkeletonAsChildExample,
  SkeletonCardExample,
  SkeletonClassNameExample,
  SkeletonCompositionExample,
  SkeletonCssPropertiesPanel,
  SkeletonExample,
  SkeletonLoadedExample,
  SkeletonMediaObjectExample,
  SkeletonStaticExample,
  SkeletonVariantsExample,
  skeletonAsChildData,
  skeletonCardData,
  skeletonCompositionData,
  skeletonCustomStylingCss,
  skeletonExampleData,
  skeletonLoadedData,
  skeletonMediaObjectData,
  skeletonStaticData,
  skeletonVariantsData,
} from '@/components/examples/skeleton';

## API Reference

Ark UI does not ship a dedicated `Skeleton` primitive. moduix implements `Skeleton` as an Ark-style
factory wrapper over a native element with `asChild`, `data-scope`, `data-part`, `data-state`, and
`data-variant` hooks. Chakra UI's `loading` and `variant` model informs the public API.

## Basic

Use a few line skeletons for the common text-loading case.

<Preview>
  <SkeletonExample />

  <Preview.Code>
    {`
      import { Skeleton, Stack } from "@moduix/react";

      export function SkeletonDemo() {
        return (
          <Stack gap={10} className="skeleton-stack">
            {lines.map((line) => (
              <Skeleton key={line.width} width={line.width} height={line.height} />
            ))}
          </Stack>
        );
      }
    `}

  </Preview.Code>

<Preview.CSS>
  {`
      .skeleton-stack {
        width: min(20rem, calc(100vw - var(--spacing-8)));
      }
    `}
</Preview.CSS>

  <Preview.Data>{skeletonExampleData}</Preview.Data>
</Preview>

## Install with shadcn (optional)

If you want `Skeleton` to become part of your own project source instead of staying in
`node_modules`, install it from the moduix GitHub registry:

<Tabs items={['npm', 'pnpm', 'bun', 'deno', 'aube']}>
  <Tab value="npm">

```bash
npx shadcn@latest add Blinks44/moduix/react-skeleton
```

  </Tab>
  <Tab value="pnpm">

```bash
pnpm dlx shadcn@latest add Blinks44/moduix/react-skeleton
```

  </Tab>
  <Tab value="bun">

```bash
bunx shadcn@latest add Blinks44/moduix/react-skeleton
```

  </Tab>
  <Tab value="deno">

```bash
deno x -A npm:shadcn@latest add Blinks44/moduix/react-skeleton
```

  </Tab>
  <Tab value="aube">

```bash
aubx shadcn@latest add Blinks44/moduix/react-skeleton
```

  </Tab>
</Tabs>

## Anatomy

`Skeleton` is a single visual part. Layout stays in the surrounding markup.

```text
Skeleton.Root
└─ children (optional, revealed when loading={false})
```

| Part                         | Role                                                                                        |
| ---------------------------- | ------------------------------------------------------------------------------------------- |
| `Skeleton` / `Skeleton.Root` | Placeholder surface. Renders an Ark factory element and accepts `asChild` for custom hosts. |

## Composition

### Recommended composition

Use `Skeleton` directly for text lines, cards, avatars, and media placeholders. It renders one
decorative block while `loading` is true and reveals children when `loading` is false.

`variant="pulse"` is the default. Use `variant="none"` when motion should stay quiet.
Reduced-motion users always get a static surface.

### When to go custom

Use `asChild` when another semantic element must own the rendered node. Keep the child single and
semantic, matching Ark's composition guidance.

Use `width`, `height`, `boxSize`, and `borderRadius` for common dimensions. For more specialized
surfaces, apply `className`, `style`, or the public CSS variables.

### Default props

| Prop           | Default                         | Notes                                                                          |
| -------------- | ------------------------------- | ------------------------------------------------------------------------------ |
| `loading`      | `true`                          | Controls whether placeholder styles are active or children are shown normally. |
| `variant`      | `pulse`                         | Supports `pulse` and `none`.                                                   |
| `height`       | `1rem`                          | CSS sets the default root height. Numeric prop values become `px`.             |
| `width`        | `100%`                          | CSS sets the default root width. Numeric prop values become `px`.              |
| `boxSize`      | -                               | Sets width and height together when that axis is not provided.                 |
| `borderRadius` | `var(--skeleton-border-radius)` | Numeric prop values become `px`; style overrides still win.                    |

### Behavioral notes

- `Skeleton.Root` is the same component as `Skeleton`, provided for namespace consistency.
- `loading={true}` sets `aria-hidden`, `data-state="loading"`, and `data-loading`.
- `loading={false}` sets `data-state="loaded"` and removes placeholder-only pointer and color styles.
- `variant` is styling-only; no internal timers or state management are added.
- `asChild` comes from Ark factory composition and replaces legacy render-prop composition.

## Examples

### Card

Use a larger block plus shorter text lines to reserve card layout while content is loading.

<Preview>
  <SkeletonCardExample />

  <Preview.Code>
    {`
      import { Skeleton, Stack } from "@moduix/react";
      import styles from "./skeleton-card-demo.module.css";

      export function SkeletonCardDemo() {
        return (
          <Stack gap={16} className={styles.card}>
            <Skeleton height={cardSkeleton.mediaHeight} borderRadius="var(--radius-lg)" />
            <Stack gap={12}>
              <Skeleton width={cardSkeleton.titleWidth} height={20} />
              <Skeleton height={14} />
              <Skeleton width={cardSkeleton.bodyWidth} height={14} />
            </Stack>
          </Stack>
        );
      }
    `}

  </Preview.Code>

<Preview.CSS>
  {`
      .card {
        width: min(20rem, calc(100vw - var(--spacing-8)));
      }
    `}
</Preview.CSS>

  <Preview.Data>{skeletonCardData}</Preview.Data>
</Preview>

### Media Object

Use `boxSize` with a full radius for avatar placeholders, then compose text lines beside it.

<Preview>
  <SkeletonMediaObjectExample />

  <Preview.Code>
    {`
      import { Skeleton, Stack } from "@moduix/react";
      import styles from "./skeleton-media-object-demo.module.css";

      export function SkeletonMediaObjectDemo() {
        return (
          <Stack direction="row" align="center" gap={12} className={styles.mediaObject}>
            <Skeleton boxSize={userRowSkeleton.avatarSize} borderRadius="var(--radius-full)" />
            <Stack gap={8} fill>
              <Skeleton width={userRowSkeleton.titleWidth} height={16} />
              <Skeleton height={14} />
              <Skeleton width={userRowSkeleton.bodyWidth} height={14} />
            </Stack>
          </Stack>
        );
      }
    `}

  </Preview.Code>

<Preview.CSS>
  {`
      .mediaObject {
        width: min(24rem, calc(100vw - var(--spacing-8)));
      }
    `}
</Preview.CSS>

  <Preview.Data>{skeletonMediaObjectData}</Preview.Data>
</Preview>

### Responsive Layout

Keep responsive layout logic in wrapper markup while each `Skeleton` stays a visual building block.

<Preview>
  <SkeletonCompositionExample />

  <Preview.Code>
    {`
      import { Skeleton, Stack } from "@moduix/react";
      import styles from "./skeleton-layout-demo.module.css";

      export function SkeletonCompositionDemo() {
        return (
          <Stack gap={12} className={styles.layoutExample}>
            {announcements.map((item) => (
              <Stack key={item.titleWidth} direction={{ mobile: "column", desktop: "row" }} gap={12}>
                <Skeleton width={72} height={48} />
                <Stack gap={8} fill>
                  <Skeleton width={item.titleWidth} height={14} />
                  <Skeleton height={14} />
                </Stack>
              </Stack>
            ))}
          </Stack>
        );
      }
    `}

  </Preview.Code>

<Preview.CSS>
  {`
      .layoutExample {
        width: min(24rem, calc(100vw - var(--spacing-8)));
        padding-block: 4px;
      }
    `}
</Preview.CSS>

  <Preview.Data>{skeletonCompositionData}</Preview.Data>
</Preview>

### Variants

Use `variant` to choose between pulse and static placeholder rendering.

<Preview>
  <SkeletonVariantsExample />

  <Preview.Code>
    {`
      import { Skeleton, Stack } from "@moduix/react";
      import styles from "./skeleton-variants-demo.module.css";

      export function SkeletonVariantsDemo() {
        return (
          <Stack gap={12} className={styles.stack}>
            {variants.map((variant) => (
              <Skeleton key={variant} height={18} variant={variant} />
            ))}
          </Stack>
        );
      }
    `}

  </Preview.Code>

<Preview.CSS>
  {`
      .stack {
        width: min(20rem, calc(100vw - var(--spacing-8)));
      }
    `}
</Preview.CSS>

  <Preview.Data>{skeletonVariantsData}</Preview.Data>
</Preview>

### Static

Pass `variant="none"` when the loading state is dense or motion should stay quieter.

<Preview>
  <SkeletonStaticExample />

  <Preview.Code>
    {`
      import { Skeleton } from "@moduix/react";
      import styles from "./static-skeleton-demo.module.css";

      export function StaticSkeletonDemo() {
        return (
          <Skeleton
            width={staticSkeleton.width}
            height={staticSkeleton.height}
            variant={staticSkeleton.variant}
            className={styles.staticSkeleton}
          />
        );
      }
    `}

  </Preview.Code>

<Preview.CSS>
  {`
      .staticSkeleton {
        max-width: calc(100vw - var(--spacing-8));
      }
    `}
</Preview.CSS>

  <Preview.Data>{skeletonStaticData}</Preview.Data>
</Preview>

### Loaded Content

Use `loading` to keep the same host in place while switching from placeholder to real content.

<Preview>
  <SkeletonLoadedExample />

  <Preview.Code>
    {`
      import { Skeleton } from "@moduix/react";
      import styles from "./loaded-skeleton-demo.module.css";

      export function LoadedSkeletonDemo() {
        return (
          <div className={styles.loadedGrid}>
            <Skeleton loading={states.loading} className={styles.loadedContent}>
              <strong>{profile.name}</strong>
              <span>{profile.role}</span>
            </Skeleton>
            <Skeleton loading={states.loaded} className={styles.loadedContent}>
              <strong>{profile.name}</strong>
              <span>{profile.role}</span>
            </Skeleton>
          </div>
        );
      }
    `}

  </Preview.Code>

<Preview.CSS>
  {`
      .loadedContent {
        display: grid;
        width: min(20rem, calc(100vw - var(--spacing-8)));
        min-height: 4.25rem;
        gap: var(--spacing-1);
        padding: var(--spacing-4);
        border: var(--border-width-sm) solid var(--color-border);
      }

      .loadedGrid {
        display: grid;
        gap: var(--spacing-3);
      }
    `}

</Preview.CSS>

  <Preview.Data>{skeletonLoadedData}</Preview.Data>
</Preview>

### Custom Host

Use `asChild` when a semantic element such as `section` should own the DOM node.

<Preview>
  <SkeletonAsChildExample />

  <Preview.Code>
    {`
      import { Skeleton } from "@moduix/react";
      import styles from "./skeleton-as-child-demo.module.css";

      export function SkeletonAsChildDemo() {
        return (
          <Skeleton asChild height={72} borderRadius="var(--radius-lg)" className={styles.asChild}>
            <section aria-label={loadingRegion.label} />
          </Skeleton>
        );
      }
    `}

  </Preview.Code>

<Preview.CSS>
  {`
      .asChild {
        width: min(20rem, calc(100vw - var(--spacing-8)));
      }
    `}
</Preview.CSS>

  <Preview.Data>{skeletonAsChildData}</Preview.Data>
</Preview>

### Custom Styling

Use `className` and the public CSS variables when the surface needs local styling overrides.

<Preview>
  <SkeletonClassNameExample />

  <Preview.Code>
    {`
      import { Skeleton, Stack } from "@moduix/react";
      import styles from "./custom-skeleton-demo.module.css";

      export function CustomSkeletonDemo() {
        return (
          <Stack gap={10} className={styles.customBlock}>
            <Skeleton className={styles.customSkeleton} height={18} />
            <Skeleton className={styles.customSkeleton} width="78%" height={18} />
            <Skeleton className={styles.customSkeleton} width="52%" height={18} />
          </Stack>
        );
      }
    `}

  </Preview.Code>

<Preview.CSS>{skeletonCustomStylingCss}</Preview.CSS>

  <Preview.Data>{skeletonExampleData}</Preview.Data>
</Preview>

## Styling

`Skeleton` exposes Ark-style part and state hooks plus public `--skeleton-*` variables for color,
radius, and motion.

### CSS Properties

<div className="not-prose">
  <Tabs items={['CSS Variables']} className="mb-0 mt-3">
    <Tab className="max-h-150 overflow-auto">
      <SkeletonCssPropertiesPanel />
    </Tab>
  </Tabs>
</div>

### Styling hooks

- `[data-scope="skeleton"][data-part="root"]` targets the exported root.
- `data-slot="skeleton-root"` is the stable moduix selector for the root.
- `data-state="loading"` and `data-state="loaded"` reflect the `loading` prop.
- `data-variant="pulse"` and `data-variant="none"` reflect the variant.
- `data-loading` is present only while placeholder styles are active.