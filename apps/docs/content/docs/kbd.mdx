---
title: Kbd
description: A compact keycap for keyboard shortcuts and command hints.
icon: Component
---

import {
  CustomCompositionKbdExample,
  KbdCssPlaygroundPanel,
  KbdCssPropertiesPanel,
  KbdExample,
  KbdGroupExample,
  KbdSizesExample,
  KbdVariantsExample,
  kbdPlaygroundCssProperties,
} from '@/components/examples/kbd';

## API Reference

`Kbd` is a standalone moduix component and does not wrap a Base UI primitive.
Use it for single keys, shortcuts, and compact command hints.

## Basic

<Preview cssProperties={kbdPlaygroundCssProperties} cssVariableScope="local">
  <KbdExample />

  <Preview.Code>
    {`
      import { Kbd } from "moduix";

      export function KbdDemo() {
        return <Kbd>Cmd K</Kbd>;
      }
    `}

  </Preview.Code>
  <Preview.CSSProperties>{(context) => <KbdCssPropertiesPanel {...context} />}</Preview.CSSProperties>
  <Preview.CSSPlayground>{(context) => <KbdCssPlaygroundPanel {...context} />}</Preview.CSSPlayground>
</Preview>

## Anatomy

Use `Kbd` for a single key label. Use `KbdGroup` to keep multi-key shortcuts aligned.

```text
KbdGroup
└─ Kbd
└─ Kbd
```

```tsx
<KbdGroup>
  <Kbd>Cmd</Kbd>
  <Kbd>K</Kbd>
</KbdGroup>
```

| Part       | Role                                                       |
| ---------- | ---------------------------------------------------------- |
| `Kbd`      | Root keycap slot. Applies size, variant, border, and tone. |
| `KbdGroup` | Inline wrapper for keyboard shortcut combinations.         |

## Composition

Pass `className` to `Kbd` or `KbdGroup` for targeted overrides. `Kbd` accepts native `kbd` props,
and `KbdGroup` accepts native `span` props.

### Defaults

| Part       | Prop      | Default   | Values                        |
| ---------- | --------- | --------- | ----------------------------- |
| `Kbd`      | `variant` | `default` | `default`, `outline`, `ghost` |
| `Kbd`      | `size`    | `md`      | `sm`, `md`, `lg`              |
| `KbdGroup` | `size`    | `md`      | `sm`, `md`, `lg`              |

## Examples

### Variants

Use `default` for primary hints, `outline` for quieter surfaces, and `ghost` for dense menus.

<Preview>
  <KbdVariantsExample />

  <Preview.Code>
    {`
      import { Kbd } from "moduix";

      export function KbdVariantsDemo() {
        return (
          <>
            <Kbd variant="default">default</Kbd>
            <Kbd variant="outline">outline</Kbd>
            <Kbd variant="ghost">ghost</Kbd>
          </>
        );
      }
    `}

  </Preview.Code>
</Preview>

### Sizes

Switch `size` between `sm`, `md`, and `lg` to match the surrounding UI density.

<Preview>
  <KbdSizesExample />

  <Preview.Code>
    {`
      import { Kbd } from "moduix";

      export function KbdSizesDemo() {
        return (
          <>
            <Kbd size="sm">Esc</Kbd>
            <Kbd size="md">Cmd K</Kbd>
            <Kbd size="lg">Enter</Kbd>
          </>
        );
      }
    `}

  </Preview.Code>
</Preview>

### Shortcut Group

Use `KbdGroup` to keep multi-key shortcuts together.

<Preview>
  <KbdGroupExample />

  <Preview.Code>
    {`
      import { Kbd, KbdGroup } from "moduix";

      export function KbdGroupDemo() {
        return (
          <KbdGroup>
            <Kbd>Cmd</Kbd>
            <Kbd>K</Kbd>
          </KbdGroup>
        );
      }
    `}

  </Preview.Code>
</Preview>

### Custom Composition

Use `className` and CSS variables for product-specific styling.

<Preview>
  <CustomCompositionKbdExample />

  <Preview.Code>
    {`
      import { Kbd, KbdGroup } from "moduix";
      import styles from "./custom-kbd-demo.module.css";

      export function CustomKbdDemo() {
        return (
          <KbdGroup className={styles.customGroup}>
            <Kbd className={styles.customKbd}>Cmd</Kbd>
            <Kbd className={styles.customKbd}>K</Kbd>
          </KbdGroup>
        );
      }
    `}

  </Preview.Code>

  <Preview.CSS>
    {`
      .customGroup {
        --kbd-group-gap: 0.375rem;
      }

      .customKbd {
        --kbd-bg: color-mix(in oklab, var(--color-primary) 12%, var(--color-background));
        --kbd-border-color: color-mix(in oklab, var(--color-primary) 32%, transparent);
        --kbd-color: var(--color-primary);
        --kbd-height: 1.625rem;
        --kbd-min-width: 1.625rem;
        --kbd-radius: var(--radius-md);
      }
    `}

  </Preview.CSS>
</Preview>