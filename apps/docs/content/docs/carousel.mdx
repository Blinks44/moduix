---
title: Carousel
description: A paged carousel with Ark UI composition, autoplay, provider state, and variable-size slides.
icon: Component
---

import {
  AutoplayCarouselExample,
  CarouselCssPropertiesPanel,
  CarouselExample,
  ControlledCarouselExample,
  DynamicSlidesCarouselExample,
  LoopCarouselExample,
  MouseDragCarouselExample,
  PauseOnHoverCarouselExample,
  RootProviderCarouselExample,
  ScrollToCarouselExample,
  SlidesPerPageCarouselExample,
  SpacingCarouselExample,
  ThumbnailIndicatorCarouselExample,
  VariableSizeCarouselExample,
  VerticalCarouselExample,
  carouselExampleCss,
  carouselSlidesData,
  carouselVariableSlidesData,
} from '@/components/examples/carousel';

## API Reference

<PrimitiveReference href="https://ark-ui.com/docs/components/carousel" />

## Basic

Use the Ark composition directly: `Carousel.Root`, `Carousel.Control`, `Carousel.ItemGroup`,
`Carousel.Item`, `Carousel.PrevTrigger`, `Carousel.NextTrigger`, `Carousel.IndicatorGroup`, and
`Carousel.Indicator`.

<Preview>
  <CarouselExample />

  <Preview.Code>
    {`
      import { Carousel } from "@moduix/react";

      export function TravelCarousel() {
        return (
          <Carousel slideCount={slides.length}>
            <Carousel.Control>
              <Carousel.PrevTrigger />
              <Carousel.ItemGroup aria-label="Travel gallery">
                {slides.map((slide, index) => (
                  <Carousel.Item key={slide.id} index={index}>
                    <img src={slide.src} alt={slide.alt} />
                  </Carousel.Item>
                ))}
              </Carousel.ItemGroup>
              <Carousel.NextTrigger />
            </Carousel.Control>

            <Carousel.IndicatorGroup>
              {slides.map((_, index) => (
                <Carousel.Indicator key={index} index={index} />
              ))}
            </Carousel.IndicatorGroup>

            <Carousel.ProgressText />
          </Carousel>
        );
      }
    `}

  </Preview.Code>

  <Preview.CSS>{carouselExampleCss}</Preview.CSS>
  <Preview.Data>{carouselSlidesData}</Preview.Data>
</Preview>

## Install with shadcn

If you want `Carousel` to become part of your own project source instead of staying in
`node_modules`, install it from the moduix GitHub registry:

<Tabs items={['npm', 'pnpm', 'bun', 'deno', 'aube']}>
  <Tab value="npm">

```bash
npx shadcn@latest add Blinks44/moduix/react-carousel
```

  </Tab>
  <Tab value="pnpm">

```bash
pnpm dlx shadcn@latest add Blinks44/moduix/react-carousel
```

  </Tab>
  <Tab value="bun">

```bash
bunx shadcn@latest add Blinks44/moduix/react-carousel
```

  </Tab>
  <Tab value="deno">

```bash
deno x -A npm:shadcn@latest add Blinks44/moduix/react-carousel
```

  </Tab>
  <Tab value="aube">

```bash
aubx shadcn@latest add Blinks44/moduix/react-carousel
```

  </Tab>
</Tabs>

## Anatomy

```text
Carousel.Root
├─ Carousel.Control
│  ├─ Carousel.PrevTrigger
│  ├─ Carousel.ItemGroup
│  │  └─ Carousel.Item[index]
│  └─ Carousel.NextTrigger
├─ Carousel.IndicatorGroup
│  └─ Carousel.Indicator[index]
└─ Carousel.ProgressText
```

| Part                         | Role                                                           |
| ---------------------------- | -------------------------------------------------------------- |
| `Carousel` / `Carousel.Root` | Owns carousel state, navigation behavior, and item collection. |
| `Carousel.Control`           | Layout wrapper for triggers and item group.                    |
| `Carousel.ItemGroup`         | Slide track that owns `Carousel.Item` parts.                   |
| `Carousel.Item`              | Individual slide at a stable `index`.                          |
| `Carousel.PrevTrigger`       | Previous-page trigger.                                         |
| `Carousel.NextTrigger`       | Next-page trigger.                                             |
| `Carousel.IndicatorGroup`    | Group wrapper for page indicators.                             |
| `Carousel.Indicator`         | Page indicator trigger for a specific slide/page index.        |
| `Carousel.ProgressText`      | Current page and total progress text.                          |

Additional Ark parts stay available for advanced flows:

- `Carousel.AutoplayTrigger`
- `Carousel.AutoplayIndicator`
- `Carousel.Context`
- `Carousel.RootProvider`
- `useCarousel()`
- `useCarouselContext()`

## Composition

### Recommended composition

Stay with `Carousel.Root`, `Carousel.Control`, `Carousel.ItemGroup`, `Carousel.Item`,
`Carousel.PrevTrigger`, `Carousel.NextTrigger`, `Carousel.IndicatorGroup`, and
`Carousel.Indicator` for the standard Ark carousel path.

### When to go custom

Use `Carousel.Context` when indicator count depends on runtime `pageSnapPoints`, especially with
`autoSize`, `slidesPerPage > 1`, and programmatic scrolling.

Use `Carousel.RootProvider` and `useCarousel()` when carousel state must be created outside the
rendered subtree.

Use `useCarouselContext()` inside custom child components that need the carousel API without a
render prop.

Use `Carousel.AutoplayTrigger` and `Carousel.AutoplayIndicator` when autoplay controls should be
part of the visible UI instead of only a root prop.

### Default props

| Prop         | Default | Notes                                     |
| ------------ | ------- | ----------------------------------------- |
| `slideCount` | —       | Required on `Carousel` / `Carousel.Root`. |

### Behavioral notes

- `onPageChange(details)` follows Ark shape and exposes the active page as `details.page`.
- `spacing`, `padding`, `slidesPerPage`, `slidesPerMove`, and `autoSize` follow Ark sizing and
  snap-point behavior directly.
- Vertical carousels stay on the same public surface. Default triggers rotate automatically when the
  component uses moduix-provided chevrons.

## Examples

### Controlled

Control the current page when carousel state must stay in sync with external navigation, filters, or analytics.

<Preview>
  <ControlledCarouselExample />

  <Preview.Code>
    {`
      import { Carousel } from "@moduix/react";
      import { useState } from "react";

      export function ControlledCarousel() {
        const [page, setPage] = useState(1);

        return (
          <Carousel
            slideCount={slides.length}
            page={page}
            onPageChange={(details) => setPage(details.page)}
          >
            <Carousel.Control>
              <Carousel.PrevTrigger />
              <Carousel.ItemGroup aria-label="Controlled gallery">
                {slides.map((slide, index) => (
                  <Carousel.Item key={slide.id} index={index}>
                    <img src={slide.src} alt={slide.alt} />
                  </Carousel.Item>
                ))}
              </Carousel.ItemGroup>
              <Carousel.NextTrigger />
            </Carousel.Control>
            <Carousel.IndicatorGroup>
              {slides.map((_, index) => (
                <Carousel.Indicator key={index} index={index} />
              ))}
            </Carousel.IndicatorGroup>
          </Carousel>
        );
      }
    `}

  </Preview.Code>

  <Preview.CSS>{carouselExampleCss}</Preview.CSS>
  <Preview.Data>{carouselSlidesData}</Preview.Data>
</Preview>

### Dynamic

Update `slideCount` alongside the rendered items when slides can be added or removed after the initial render.

<Preview>
  <DynamicSlidesCarouselExample />

  <Preview.Code>
    {`
      import { Button, Carousel, PlusIcon } from "@moduix/react";
      import { useState } from "react";

      export function DynamicSlidesCarousel() {
        const [count, setCount] = useState(4);
        const [page, setPage] = useState(0);
        const visibleSlides = slides.slice(0, count);

        return (
          <>
            <Carousel
              slideCount={visibleSlides.length}
              page={page}
              onPageChange={(details) => setPage(details.page)}
            >
              <Carousel.ItemGroup aria-label="Dynamic gallery">
                {visibleSlides.map((slide, index) => (
                  <Carousel.Item key={slide.id} index={index}>
                    <img src={slide.src} alt={slide.alt} />
                  </Carousel.Item>
                ))}
              </Carousel.ItemGroup>
              <Carousel.Control>
                <Carousel.PrevTrigger />
                <Carousel.IndicatorGroup>
                  {visibleSlides.map((_, index) => (
                    <Carousel.Indicator key={index} index={index} />
                  ))}
                </Carousel.IndicatorGroup>
                <Carousel.NextTrigger />
              </Carousel.Control>
            </Carousel>

            <Button
              variant="outline"
              onClick={() => setCount((currentCount) => Math.min(slides.length, currentCount + 1))}
            >
              <PlusIcon />
              Add slide
            </Button>
          </>
        );
      }
    `}

  </Preview.Code>

  <Preview.CSS>{carouselExampleCss}</Preview.CSS>
  <Preview.Data>{carouselSlidesData}</Preview.Data>
</Preview>

### Pause On Hover

Pause autoplay on pointer hover when readers need time to inspect slide content without competing motion.

<Preview>
  <PauseOnHoverCarouselExample />

  <Preview.Code>
    {`
      import { Carousel } from "@moduix/react";

      export function PauseOnHoverCarousel() {
        return (
          <Carousel autoplay loop slideCount={slides.length}>
            <Carousel.Context>
              {({ isPlaying }) => <p>Autoplay: {isPlaying ? "playing" : "paused"}</p>}
            </Carousel.Context>

            <Carousel.Context>
              {(api) => (
                <Carousel.ItemGroup
                  aria-label="Pause on hover gallery"
                  onPointerOver={() => api.pause()}
                  onPointerLeave={() => api.play()}
                >
                  {slides.map((slide, index) => (
                    <Carousel.Item key={slide.id} index={index}>
                      <img src={slide.src} alt={slide.alt} />
                    </Carousel.Item>
                  ))}
                </Carousel.ItemGroup>
              )}
            </Carousel.Context>

            <Carousel.IndicatorGroup>
              {slides.map((_, index) => (
                <Carousel.Indicator key={index} index={index} />
              ))}
            </Carousel.IndicatorGroup>
          </Carousel>
        );
      }
    `}

  </Preview.Code>

  <Preview.CSS>{carouselExampleCss}</Preview.CSS>
  <Preview.Data>{carouselSlidesData}</Preview.Data>
</Preview>

### Root Provider

Use `Carousel.RootProvider` when the carousel store should be created outside the rendered subtree and reused across composition boundaries.

<Preview>
  <RootProviderCarouselExample />

  <Preview.Code>
    {`
      import { Carousel, useCarousel } from "@moduix/react";

      export function RootProviderCarousel() {
        const carousel = useCarousel({ slideCount: slides.length });

        return (
          <Carousel.RootProvider value={carousel}>
            <Carousel.Control>
              <Carousel.PrevTrigger />
              <Carousel.ItemGroup aria-label="Root provider gallery">
                {slides.map((slide, index) => (
                  <Carousel.Item key={slide.id} index={index}>
                    <img src={slide.src} alt={slide.alt} />
                  </Carousel.Item>
                ))}
              </Carousel.ItemGroup>
              <Carousel.NextTrigger />
            </Carousel.Control>
            <Carousel.IndicatorGroup>
              {slides.map((_, index) => (
                <Carousel.Indicator key={index} index={index} />
              ))}
            </Carousel.IndicatorGroup>
          </Carousel.RootProvider>
        );
      }
    `}

  </Preview.Code>

  <Preview.CSS>{carouselExampleCss}</Preview.CSS>
  <Preview.Data>{carouselSlidesData}</Preview.Data>
</Preview>

### Scroll to Slide

Call the carousel API from context when another control should jump directly to a specific slide.

<Preview>
  <ScrollToCarouselExample />

  <Preview.Code>
    {`
      import { Button, Carousel } from "@moduix/react";

      export function ScrollToCarousel() {
        return (
          <Carousel slideCount={slides.length}>
            <Carousel.Context>
              {(api) => (
                <Button variant="outline" onClick={() => api.scrollToIndex(3)}>
                  Go to slide 4
                </Button>
              )}
            </Carousel.Context>

            <Carousel.ItemGroup aria-label="Scroll to gallery">
              {slides.map((slide, index) => (
                <Carousel.Item key={slide.id} index={index}>
                  <img src={slide.src} alt={slide.alt} />
                </Carousel.Item>
              ))}
            </Carousel.ItemGroup>

            <Carousel.Control>
              <Carousel.PrevTrigger />
              <Carousel.NextTrigger />
            </Carousel.Control>
          </Carousel>
        );
      }
    `}

  </Preview.Code>

  <Preview.CSS>{carouselExampleCss}</Preview.CSS>
  <Preview.Data>{carouselSlidesData}</Preview.Data>
</Preview>

### Slides Per Page

Increase `slidesPerPage` when the layout should preview multiple items at once instead of presenting one full-width panel.

<Preview>
  <SlidesPerPageCarouselExample />

  <Preview.Code>
    {`
      import { Carousel } from "@moduix/react";

      export function SlidesPerPageCarousel() {
        return (
          <Carousel slideCount={slides.length} slidesPerPage={2} spacing="var(--spacing-3)">
            <Carousel.Control>
              <Carousel.PrevTrigger />
              <Carousel.NextTrigger />
            </Carousel.Control>

            <Carousel.ItemGroup aria-label="Two-up gallery">
              {slides.map((slide, index) => (
                <Carousel.Item key={slide.id} index={index}>
                  <img src={slide.src} alt={slide.alt} />
                </Carousel.Item>
              ))}
            </Carousel.ItemGroup>

            <Carousel.Context>
              {(api) => (
                <Carousel.IndicatorGroup>
                  {api.pageSnapPoints.map((_, index) => (
                    <Carousel.Indicator key={index} index={index} />
                  ))}
                </Carousel.IndicatorGroup>
              )}
            </Carousel.Context>
          </Carousel>
        );
      }
    `}

  </Preview.Code>

  <Preview.CSS>{carouselExampleCss}</Preview.CSS>
  <Preview.Data>{carouselSlidesData}</Preview.Data>
</Preview>

### Spacing

Tune `spacing` when adjacent slides need more separation for editorial layouts, product rails, or branded rhythm.

<Preview>
  <SpacingCarouselExample />

  <Preview.Code>
    {`
      import { Carousel } from "@moduix/react";

      export function SpacingCarousel() {
        return (
          <Carousel slideCount={slides.length} slidesPerPage={1.5} spacing="3rem">
            <Carousel.ItemGroup aria-label="Spaced gallery">
              {slides.map((slide, index) => (
                <Carousel.Item key={slide.id} index={index}>
                  <img src={slide.src} alt={slide.alt} />
                </Carousel.Item>
              ))}
            </Carousel.ItemGroup>

            <Carousel.Control>
              <Carousel.PrevTrigger />
              <Carousel.Context>
                {(api) => (
                  <Carousel.IndicatorGroup>
                    {api.pageSnapPoints.map((_, index) => (
                      <Carousel.Indicator key={index} index={index} />
                    ))}
                  </Carousel.IndicatorGroup>
                )}
              </Carousel.Context>
              <Carousel.NextTrigger />
            </Carousel.Control>
          </Carousel>
        );
      }
    `}

  </Preview.Code>

  <Preview.CSS>{carouselExampleCss}</Preview.CSS>
  <Preview.Data>{carouselSlidesData}</Preview.Data>
</Preview>

### Thumbnail Indicators

Render custom indicator content when the pager itself should preview the slides instead of using plain dots.

<Preview>
  <ThumbnailIndicatorCarouselExample />

  <Preview.Code>
    {`
      import { Carousel } from "@moduix/react";

      export function ThumbnailIndicatorCarousel() {
        return (
          <Carousel slideCount={slides.length}>
            <Carousel.Control>
              <Carousel.PrevTrigger />
              <Carousel.ItemGroup aria-label="Gallery with thumbnails">
                {slides.map((slide, index) => (
                  <Carousel.Item key={slide.id} index={index}>
                    <img src={slide.src} alt={slide.alt} />
                  </Carousel.Item>
                ))}
              </Carousel.ItemGroup>
              <Carousel.NextTrigger />
            </Carousel.Control>

            <Carousel.IndicatorGroup>
              {slides.map((slide, index) => (
                <Carousel.Indicator key={slide.id} index={index}>
                  <img src={slide.src} alt={slide.alt} />
                </Carousel.Indicator>
              ))}
            </Carousel.IndicatorGroup>
          </Carousel>
        );
      }
    `}

  </Preview.Code>

  <Preview.CSS>{carouselExampleCss}</Preview.CSS>
  <Preview.Data>{carouselSlidesData}</Preview.Data>
</Preview>

### Variable Sizes

Enable `autoSize` when slide widths should follow their content rather than a fixed page grid.

<Preview>
  <VariableSizeCarouselExample />

  <Preview.Code>
    {`
      import { Carousel } from "@moduix/react";

      export function VariableSizeCarousel() {
        return (
          <Carousel
            autoSize
            slideCount={slides.length}
            spacing="var(--spacing-3)"
            padding="var(--spacing-4)"
          >
            <Carousel.Control>
              <Carousel.PrevTrigger />
              <Carousel.NextTrigger />
            </Carousel.Control>

            <Carousel.ItemGroup aria-label="Variable size gallery">
              {slides.map((slide, index) => (
                <Carousel.Item key={slide.id} index={index} snapAlign="center">
                  <img src={slide.src} alt={slide.alt} style={{ width: slide.width }} />
                </Carousel.Item>
              ))}
            </Carousel.ItemGroup>

            <Carousel.Context>
              {(api) => (
                <Carousel.IndicatorGroup>
                  {api.pageSnapPoints.map((_, index) => (
                    <Carousel.Indicator key={index} index={index} />
                  ))}
                </Carousel.IndicatorGroup>
              )}
            </Carousel.Context>
          </Carousel>
        );
      }
    `}

  </Preview.Code>

  <Preview.CSS>{carouselExampleCss}</Preview.CSS>
  <Preview.Data>{carouselVariableSlidesData}</Preview.Data>
</Preview>

### Autoplay

Use autoplay for passive galleries and pair it with visible controls so users can pause motion when needed.

<Preview>
  <AutoplayCarouselExample />

  <Preview.Code>
    {`
      import { Carousel } from "@moduix/react";

      export function AutoplayCarousel() {
        return (
          <Carousel autoplay={{ delay: 3500 }} loop slideCount={slides.length}>
            <Carousel.ItemGroup aria-label="Autoplay gallery">
              {slides.map((slide, index) => (
                <Carousel.Item key={slide.id} index={index}>
                  <img src={slide.src} alt={slide.alt} />
                </Carousel.Item>
              ))}
            </Carousel.ItemGroup>

            <Carousel.Control>
              <Carousel.PrevTrigger />
              <Carousel.AutoplayTrigger>
                <Carousel.AutoplayIndicator fallback="Play">Pause</Carousel.AutoplayIndicator>
              </Carousel.AutoplayTrigger>
              <Carousel.NextTrigger />
            </Carousel.Control>
          </Carousel>
        );
      }
    `}

  </Preview.Code>

  <Preview.CSS>{carouselExampleCss}</Preview.CSS>
  <Preview.Data>{carouselSlidesData}</Preview.Data>
</Preview>

### Loop

Turn on `loop` when the carousel should wrap seamlessly from the last slide back to the first.

<Preview>
  <LoopCarouselExample />

  <Preview.Code>
    {`
      import { Carousel } from "@moduix/react";

      export function LoopCarousel() {
        return (
          <Carousel loop slideCount={slides.length}>
            <Carousel.Control>
              <Carousel.PrevTrigger />
              <Carousel.ItemGroup aria-label="Loop gallery">
                {slides.map((slide, index) => (
                  <Carousel.Item key={slide.id} index={index}>
                    <img src={slide.src} alt={slide.alt} />
                  </Carousel.Item>
                ))}
              </Carousel.ItemGroup>
              <Carousel.NextTrigger />
            </Carousel.Control>

            <Carousel.IndicatorGroup>
              {slides.map((_, index) => (
                <Carousel.Indicator key={index} index={index} />
              ))}
            </Carousel.IndicatorGroup>

            <Carousel.ProgressText />
          </Carousel>
        );
      }
    `}

  </Preview.Code>

  <Preview.CSS>{carouselExampleCss}</Preview.CSS>
  <Preview.Data>{carouselSlidesData}</Preview.Data>
</Preview>

### Mouse Drag

Allow mouse drag when desktop users should be able to scrub through slides with the same gesture model as touch input.

<Preview>
  <MouseDragCarouselExample />

  <Preview.Code>
    {`
      import { Carousel } from "@moduix/react";

      export function MouseDragCarousel() {
        return (
          <Carousel allowMouseDrag slideCount={slides.length}>
            <Carousel.Control>
              <Carousel.PrevTrigger />
              <Carousel.ItemGroup aria-label="Mouse draggable gallery">
                {slides.map((slide, index) => (
                  <Carousel.Item key={slide.id} index={index}>
                    <img src={slide.src} alt={slide.alt} />
                  </Carousel.Item>
                ))}
              </Carousel.ItemGroup>
              <Carousel.NextTrigger />
            </Carousel.Control>

            <Carousel.IndicatorGroup>
              {slides.map((_, index) => (
                <Carousel.Indicator key={index} index={index} />
              ))}
            </Carousel.IndicatorGroup>

            <Carousel.ProgressText />
          </Carousel>
        );
      }
    `}

  </Preview.Code>

  <Preview.CSS>{carouselExampleCss}</Preview.CSS>
  <Preview.Data>{carouselSlidesData}</Preview.Data>
</Preview>

### Vertical

Switch to a vertical orientation when the carousel should stack movement and controls on the block axis.

<Preview>
  <VerticalCarouselExample />

  <Preview.Code>
    {`
      import { Carousel } from "@moduix/react";

      export function VerticalCarousel() {
        return (
          <Carousel orientation="vertical" slideCount={slides.length}>
            <Carousel.ItemGroup aria-label="Vertical gallery">
              {slides.map((slide, index) => (
                <Carousel.Item key={slide.id} index={index}>
                  <img src={slide.src} alt={slide.alt} />
                </Carousel.Item>
              ))}
            </Carousel.ItemGroup>

            <Carousel.Control>
              <Carousel.PrevTrigger />
              <Carousel.IndicatorGroup>
                {slides.map((_, index) => (
                  <Carousel.Indicator key={index} index={index} />
                ))}
              </Carousel.IndicatorGroup>
              <Carousel.NextTrigger />
            </Carousel.Control>
          </Carousel>
        );
      }
    `}

  </Preview.Code>

  <Preview.CSS>{carouselExampleCss}</Preview.CSS>
  <Preview.Data>{carouselSlidesData}</Preview.Data>
</Preview>

## Styling

Use `className` on exported parts for structural styling changes. For state styling, prefer Ark
data attributes (`data-part`, `data-orientation`, `data-current`, `data-dragging`) and moduix
`data-slot` hooks.

### CSS Properties

<div className="not-prose">
  <Tabs items={['CSS Variables']} className="mb-0 mt-3">
    <Tab className="max-h-150 overflow-auto">
      <CarouselCssPropertiesPanel />
    </Tab>
  </Tabs>
</div>

### Styling hooks

- `data-slot="carousel-root"`, `carousel-control`, `carousel-item-group`, `carousel-item`,
  `carousel-prev-trigger`, `carousel-next-trigger`, `carousel-indicator-group`,
  `carousel-indicator`, `carousel-autoplay-trigger`, `carousel-autoplay-indicator`, and
  `carousel-progress-text` are stable selectors on the exported parts.
- Ark also exposes `data-scope="carousel"` and `data-part` names on the rendered nodes.
- `data-orientation="horizontal" | "vertical"` is available on root and structural parts.
- `Carousel.ItemGroup` exposes `data-dragging` while drag interaction is active.
- `Carousel.Indicator` exposes `data-current`, and `Carousel.Item` exposes `data-index` and
  `data-inview`.
- `Carousel.Indicator` exposes `data-readonly`, and `Carousel.AutoplayTrigger` exposes
  `data-pressed`.