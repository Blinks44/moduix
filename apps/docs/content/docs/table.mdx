---
title: Table
description: A native table primitive with small sugar for bordered overflow shells, numeric columns, captions, and empty states.
icon: Component
---

import {
  ColumnGroupTableExample,
  CustomCompositionTableExample,
  EmptyTableExample,
  NumericTableExample,
  StickyTableExample,
  StripedTableExample,
  TableCaptionExample,
  TableCssPropertiesPanel,
  TableExample,
  TableInCardExample,
  TableRowActionsExample,
  TableSizesAndVariantsExample,
} from '@/components/examples/table';

## API Reference

`Table` does not wrap a dedicated Ark UI primitive; Ark UI v5 has no table component. Moduix owns
this native semantic table family using Ark's [factory/composition model](https://ark-ui.com/docs/guides/composition),
Ark-style `data-scope` / `data-part` styling hooks, and the current
[Chakra Table recipe](https://chakra-ui.com/docs/components/table) as the naming and feature reference.

## Basic

Start with `Table.ScrollArea`, `Table`, `Table.Header`, `Table.Body`, and the native row and cell
parts. This is the recommended path for invoices, summaries, and admin-style read-only datasets.

<Preview>
  <TableExample />

  <Preview.Code>
    {`
      import {
        Badge,
        Table,
      } from "moduix";

      export function TableDemo() {
        return (
          <Table.ScrollArea className={styles.shell}>
            <Table className={styles.table}>
              <Table.Header>
                <Table.Row>
                  <Table.ColumnHeader>Invoice</Table.ColumnHeader>
                  <Table.ColumnHeader>Status</Table.ColumnHeader>
                  <Table.ColumnHeader>Method</Table.ColumnHeader>
                  <Table.ColumnHeader numeric>Amount</Table.ColumnHeader>
                </Table.Row>
              </Table.Header>
              <Table.Body>
                {invoices.map((invoice) => (
                  <Table.Row key={invoice.id}>
                    <Table.Cell>{invoice.id}</Table.Cell>
                    <Table.Cell>
                      <Badge variant={invoice.status === "Paid" ? "secondary" : "outline"}>
                        {invoice.status}
                      </Badge>
                    </Table.Cell>
                    <Table.Cell>{invoice.method}</Table.Cell>
                    <Table.Cell numeric>{invoice.amount}</Table.Cell>
                  </Table.Row>
                ))}
              </Table.Body>
            </Table>
          </Table.ScrollArea>
        );
      }
    `}

  </Preview.Code>

  <Preview.Data>
    {`
      const invoices = [
        { id: "INV001", status: "Paid", method: "Credit Card", amount: "$250.00" },
        { id: "INV002", status: "Pending", method: "PayPal", amount: "$150.00" },
        { id: "INV003", status: "Unpaid", method: "Bank Transfer", amount: "$350.00" },
        { id: "INV004", status: "Paid", method: "Credit Card", amount: "$450.00" },
      ];
    `}

  </Preview.Data>

  <Preview.CSS>
    {`
      .shell {
        width: min(44rem, calc(100vw - var(--spacing-8)));
      }

      .table {
        min-width: 40rem;
      }
    `}

  </Preview.CSS>
</Preview>

## Install with shadcn (optional)

If you want `Table` to become part of your own project source instead of staying in
`node_modules`, install it from the moduix GitHub registry:

<Tabs items={['npm', 'pnpm', 'bun', 'deno', 'aube']}>
  <Tab value="npm">

```bash
npx shadcn@latest add Blinks44/moduix/table
```

  </Tab>
  <Tab value="pnpm">

```bash
pnpm dlx shadcn@latest add Blinks44/moduix/table
```

  </Tab>
  <Tab value="bun">

```bash
bunx shadcn@latest add Blinks44/moduix/table
```

  </Tab>
  <Tab value="deno">

```bash
deno x -A npm:shadcn@latest add Blinks44/moduix/table
```

  </Tab>
  <Tab value="aube">

```bash
aubx shadcn@latest add Blinks44/moduix/table
```

  </Tab>
</Tabs>

This install path also pulls in the shared moduix foundation files that `Table` depends on, such
as styles, icons, and small utilities.

## Anatomy

The normal consumer path stays close to native table markup:

```text
Table.ScrollArea (optional)
└─ Table
   ├─ Table.ColumnGroup (optional)
   │  └─ Table.Column
   ├─ Table.Caption (optional)
   ├─ Table.Header
   │  └─ Table.Row
   │     └─ Table.ColumnHeader
   ├─ Table.Body
   │  ├─ Table.Row
   │  │  └─ Table.Cell
   │  └─ Table.Empty (optional)
   └─ Table.Footer (optional)
```

| Part                   | Role                                                                       |
| ---------------------- | -------------------------------------------------------------------------- |
| `Table` / `Table.Root` | Native `table` root. Owns typography, dividers, and state attributes.      |
| `Table.ScrollArea`     | Optional bordered overflow shell for the common “table in a surface” case. |
| `Table.ColumnGroup`    | Native `colgroup` wrapper for column width distribution.                   |
| `Table.Column`         | Native `col` wrapper. Supports `htmlWidth`.                                |
| `Table.Caption`        | Optional label above or below the table via `side`.                        |
| `Table.Header`         | Native `thead` wrapper for column headers.                                 |
| `Table.Body`           | Native `tbody` wrapper for body rows.                                      |
| `Table.Footer`         | Native `tfoot` wrapper for summary rows.                                   |
| `Table.Row`            | Native `tr` wrapper with divider behavior.                                 |
| `Table.ColumnHeader`   | Native `th` wrapper. Supports `numeric`.                                   |
| `Table.Cell`           | Native `td` wrapper. Supports `numeric`.                                   |
| `Table.Empty`          | Convenience empty-state row inside `Table.Body`.                           |

## Composition

### Recommended composition

Stay with `Table.ScrollArea` plus the semantic table parts for most read-only data views.

Use `Table.ScrollArea` when you need the common bordered horizontal-scroll shell. Use `numeric` on
`Table.ColumnHeader` and `Table.Cell` for amounts, counts, percentages, and latency columns. Use
`Table.Caption` when the table needs a short label or summary, and `Table.Footer` when the last row
represents totals or rollups. Use `striped`, `interactive`, `showColumnBorder`, `stickyHeader`,
and `size` / `variant` when the table needs Chakra-style visual modes without adding data-grid
state.

### When to go custom

Skip `Table.ScrollArea` when the surrounding layout already owns scrolling, padding, border, or
surface styling. In that case, keep `Table` itself and move the wrapper concerns into your own
layout component or shell markup.

`Table` intentionally does not own sorting, filtering, selection, pagination, or row action state.
Treat it as the visual and semantic primitive that higher-level table abstractions can build on.

### Default props

All parts accept `className` and their matching native element props.

| Component            | Prop               | Default    | Notes                                                  |
| -------------------- | ------------------ | ---------- | ------------------------------------------------------ |
| `Table`              | `interactive`      | `false`    | Enables body-row hover highlighting.                   |
| `Table`              | `showColumnBorder` | `false`    | Adds borders between visible columns.                  |
| `Table`              | `size`             | `'md'`     | Supports `'sm'`, `'md'`, and `'lg'`.                   |
| `Table`              | `stickyHeader`     | `false`    | Makes `Table.Header` sticky inside a scroll container. |
| `Table`              | `striped`          | `false`    | Zebra-stripes even body rows, excluding empty rows.    |
| `Table`              | `variant`          | `'line'`   | Supports `'line'` and `'outline'`.                     |
| `Table.Caption`      | `side`             | `'bottom'` | Supports `'top'` and `'bottom'`.                       |
| `Table.Column`       | `htmlWidth`        | —          | Maps to the native `width` attribute on `col`.         |
| `Table.ColumnHeader` | `numeric`          | `false`    | Right-aligns content and uses tabular nums.            |
| `Table.Cell`         | `numeric`          | `false`    | Right-aligns content and uses tabular nums.            |
| `Table.Empty`        | `colSpan`          | required   | Must match the current body column count.              |

### Behavioral notes

- `Table.Empty` renders a full table row for the empty state, so pass the body column count through
  `colSpan`.
- Header and body rows render a shared bottom divider. The last body row drops its border.
- Body rows get a subtle hover background only when `interactive` is set.
- `Table.Caption` inherits muted typography and adds inline padding so it does not sit flush against
  the table edges.

## Examples

### Caption and Footer

Use `Table.Caption side="top"` when the label reads better before the data, and `Table.Footer` for
totals or summaries.

<Preview>
  <TableCaptionExample />

  <Preview.Code>
    {`
      import {
        Table,
      } from "moduix";

      <Table.ScrollArea className={styles.shell}>
        <Table className={styles.table}>
          <Table.Caption side="top">
            A list of recent invoices for the current billing cycle.
          </Table.Caption>
          <Table.Header>
            <Table.Row>
              <Table.ColumnHeader>Invoice</Table.ColumnHeader>
              <Table.ColumnHeader>Status</Table.ColumnHeader>
              <Table.ColumnHeader>Method</Table.ColumnHeader>
              <Table.ColumnHeader numeric>Amount</Table.ColumnHeader>
            </Table.Row>
          </Table.Header>
          <Table.Body>
            {invoices.map((invoice) => (
              <Table.Row key={invoice.id}>
                <Table.Cell>{invoice.id}</Table.Cell>
                <Table.Cell>{invoice.status}</Table.Cell>
                <Table.Cell>{invoice.method}</Table.Cell>
                <Table.Cell numeric>{invoice.amount}</Table.Cell>
              </Table.Row>
            ))}
          </Table.Body>
          <Table.Footer>
            <Table.Row>
              <Table.Cell colSpan={3}>Total</Table.Cell>
              <Table.Cell numeric>$1,200.00</Table.Cell>
            </Table.Row>
          </Table.Footer>
        </Table>
      </Table.ScrollArea>
    `}

  </Preview.Code>

  <Preview.Data>
    {`
      const invoices = [
        { id: "INV001", status: "Paid", method: "Credit Card", amount: "$250.00" },
        { id: "INV002", status: "Pending", method: "PayPal", amount: "$150.00" },
        { id: "INV003", status: "Unpaid", method: "Bank Transfer", amount: "$350.00" },
        { id: "INV004", status: "Paid", method: "Credit Card", amount: "$450.00" },
      ];
    `}

  </Preview.Data>

  <Preview.CSS>
    {`
      .shell {
        width: min(44rem, calc(100vw - var(--spacing-8)));
      }

      .table {
        min-width: 40rem;
      }
    `}

  </Preview.CSS>
</Preview>

### Numeric Columns

Use `numeric` on both the header and the cells for the common aligned-metrics case.

<Preview>
  <NumericTableExample />

  <Preview.Code>
    {`
      import {
        Table,
      } from "moduix";

      <Table.ScrollArea className={styles.shell}>
        <Table className={styles.table}>
          <Table.Header>
            <Table.Row>
              <Table.ColumnHeader>Service</Table.ColumnHeader>
              <Table.ColumnHeader numeric>Requests</Table.ColumnHeader>
              <Table.ColumnHeader numeric>Error rate</Table.ColumnHeader>
              <Table.ColumnHeader numeric>Latency</Table.ColumnHeader>
            </Table.Row>
          </Table.Header>
          <Table.Body>
            {metrics.map((metric) => (
              <Table.Row key={metric.service}>
                <Table.Cell>{metric.service}</Table.Cell>
                <Table.Cell numeric>{metric.requests}</Table.Cell>
                <Table.Cell numeric>{metric.errorRate}</Table.Cell>
                <Table.Cell numeric>{metric.latency}</Table.Cell>
              </Table.Row>
            ))}
          </Table.Body>
        </Table>
      </Table.ScrollArea>
    `}

  </Preview.Code>

  <Preview.Data>
    {`
      const metrics = [
        { service: "Search API", requests: "1,204,122", errorRate: "0.12%", latency: "128 ms" },
        { service: "Billing API", requests: "248,421", errorRate: "0.04%", latency: "96 ms" },
        { service: "Docs site", requests: "82,304", errorRate: "0.00%", latency: "42 ms" },
      ];
    `}

  </Preview.Data>

  <Preview.CSS>
    {`
      .shell {
        width: min(44rem, calc(100vw - var(--spacing-8)));
      }

      .table {
        min-width: 40rem;
      }
    `}

  </Preview.CSS>
</Preview>

### Striped Rows

Use `striped` when long read-only bodies need stronger row separation without switching to a more
opinionated table abstraction.

<Preview>
  <StripedTableExample />

  <Preview.Code>
    {`
      import {
        Table,
      } from "moduix";

      <Table.ScrollArea className={styles.shell}>
        <Table striped interactive className={styles.table}>
          <Table.Header>
            <Table.Row>
              <Table.ColumnHeader>Project</Table.ColumnHeader>
              <Table.ColumnHeader>Owner</Table.ColumnHeader>
              <Table.ColumnHeader>Updated</Table.ColumnHeader>
              <Table.ColumnHeader numeric>Open issues</Table.ColumnHeader>
            </Table.Row>
          </Table.Header>
          <Table.Body>
            {projects.map((project, index) => (
              <Table.Row key={project.name}>
                <Table.Cell>{project.name}</Table.Cell>
                <Table.Cell>{project.owner}</Table.Cell>
                <Table.Cell>{project.updated}</Table.Cell>
                <Table.Cell numeric>{index + 1}</Table.Cell>
              </Table.Row>
            ))}
          </Table.Body>
        </Table>
      </Table.ScrollArea>
    `}

  </Preview.Code>

  <Preview.Data>
    {`
      const projects = [
        { name: "Docs redesign", owner: "Product Design", updated: "2 hours ago" },
        { name: "Billing migration", owner: "Growth", updated: "Yesterday" },
        { name: "Command palette", owner: "Platform", updated: "Today" },
      ];
    `}

  </Preview.Data>

  <Preview.CSS>
    {`
      .shell {
        width: min(44rem, calc(100vw - var(--spacing-8)));
      }

      .table {
        min-width: 40rem;
      }
    `}

  </Preview.CSS>
</Preview>

### Sizes and Variants

Use `size` for density and `variant` for divider style. `Table` keeps these as visual modes on the
native root instead of adding extra state.

<Preview>
  <TableSizesAndVariantsExample />

  <Preview.Code>
    {`
      import { Table } from "moduix";

      {(["sm", "md", "lg"] as const).map((size) => (
        <Table.ScrollArea key={size} className={styles.shell}>
          <Table size={size} variant={size === "md" ? "outline" : "line"}>
            <Table.Caption side="top">{size.toUpperCase()} project summary</Table.Caption>
            <Table.Header>
              <Table.Row>
                <Table.ColumnHeader>Project</Table.ColumnHeader>
                <Table.ColumnHeader>Owner</Table.ColumnHeader>
                <Table.ColumnHeader numeric>Open issues</Table.ColumnHeader>
              </Table.Row>
            </Table.Header>
            <Table.Body>
              {projects.map((project, index) => (
                <Table.Row key={project.name}>
                  <Table.Cell>{project.name}</Table.Cell>
                  <Table.Cell>{project.owner}</Table.Cell>
                  <Table.Cell numeric>{index + 1}</Table.Cell>
                </Table.Row>
              ))}
            </Table.Body>
          </Table>
        </Table.ScrollArea>
      ))}
    `}

  </Preview.Code>

  <Preview.Data>
    {`
      const projects = [
        { name: "Docs redesign", owner: "Product Design" },
        { name: "Billing migration", owner: "Growth" },
      ];
    `}

  </Preview.Data>

  <Preview.CSS>
    {`
      .shell {
        width: min(44rem, calc(100vw - var(--spacing-8)));
      }
    `}

  </Preview.CSS>
</Preview>

### Column Group and Borders

Use `Table.ColumnGroup` and `Table.Column` for native column widths, and `showColumnBorder` when
column separation needs to be visible.

<Preview>
  <ColumnGroupTableExample />

  <Preview.Code>
    {`
      import { Table } from "moduix";

      <Table.ScrollArea className={styles.shell}>
        <Table showColumnBorder className={styles.table}>
          <Table.ColumnGroup>
            <Table.Column htmlWidth="42%" />
            <Table.Column htmlWidth="28%" />
            <Table.Column htmlWidth="30%" />
          </Table.ColumnGroup>
          <Table.Header>
            <Table.Row>
              <Table.ColumnHeader>Product</Table.ColumnHeader>
              <Table.ColumnHeader>Category</Table.ColumnHeader>
              <Table.ColumnHeader numeric>Price</Table.ColumnHeader>
            </Table.Row>
          </Table.Header>
          <Table.Body>
            {products.map((product) => (
              <Table.Row key={product.name}>
                <Table.Cell>{product.name}</Table.Cell>
                <Table.Cell>{product.category}</Table.Cell>
                <Table.Cell numeric>{product.price}</Table.Cell>
              </Table.Row>
            ))}
          </Table.Body>
        </Table>
      </Table.ScrollArea>
    `}

  </Preview.Code>

  <Preview.Data>
    {`
      const products = [
        { name: "Laptop", category: "Electronics", price: "$999.99" },
        { name: "Coffee Maker", category: "Home Appliances", price: "$49.99" },
        { name: "Desk Chair", category: "Furniture", price: "$150.00" },
      ];
    `}

  </Preview.Data>

  <Preview.CSS>
    {`
      .shell {
        width: min(44rem, calc(100vw - var(--spacing-8)));
      }

      .table {
        min-width: 40rem;
      }
    `}

  </Preview.CSS>
</Preview>

### Sticky Header and Column

Use `stickyHeader` on the root and `data-sticky="start" | "end"` on cells when a horizontally or
vertically scrolling table needs persistent context.

<Preview>
  <StickyTableExample />

  <Preview.Code>
    {`
      import { Table } from "moduix";

      <Table.ScrollArea className={styles.stickyShell}>
        <Table stickyHeader interactive className={styles.wideTable}>
          <Table.Header>
            <Table.Row>
              <Table.ColumnHeader data-sticky="start">Project</Table.ColumnHeader>
              <Table.ColumnHeader>Owner</Table.ColumnHeader>
              <Table.ColumnHeader>Environment</Table.ColumnHeader>
              <Table.ColumnHeader>Updated</Table.ColumnHeader>
              <Table.ColumnHeader numeric>Open issues</Table.ColumnHeader>
            </Table.Row>
          </Table.Header>
          <Table.Body>
            {rows.map((row, index) => (
              <Table.Row key={row.name}>
                <Table.Cell data-sticky="start">{row.name}</Table.Cell>
                <Table.Cell>{row.owner}</Table.Cell>
                <Table.Cell>{row.environment}</Table.Cell>
                <Table.Cell>{row.updated}</Table.Cell>
                <Table.Cell numeric>{index + 1}</Table.Cell>
              </Table.Row>
            ))}
          </Table.Body>
        </Table>
      </Table.ScrollArea>
    `}

  </Preview.Code>

  <Preview.Data>
    {`
      const rows = [
        { name: "Docs redesign", owner: "Product Design", environment: "Production", updated: "2 hours ago" },
        { name: "Billing migration", owner: "Growth", environment: "Staging", updated: "Yesterday" },
        { name: "Command palette", owner: "Platform", environment: "Preview", updated: "Today" },
      ];
    `}

  </Preview.Data>

  <Preview.CSS>
    {`
      .stickyShell {
        width: min(44rem, calc(100vw - var(--spacing-8)));
        max-height: 18rem;
      }

      .wideTable {
        min-width: 56rem;
      }
    `}

  </Preview.CSS>
</Preview>

### Empty State

Use `Table.Empty` when a body would otherwise render no rows.

<Preview>
  <EmptyTableExample />

  <Preview.Code>
    {`
      import {
        Table,
      } from "moduix";

      export function EmptyStateTableDemo() {
        return (
          <Table.ScrollArea className={styles.shell}>
            <Table>
              <Table.Caption side="top">
                Projects filtered by the current workspace query.
              </Table.Caption>
              <Table.Header>
                <Table.Row>
                  <Table.ColumnHeader>Project</Table.ColumnHeader>
                  <Table.ColumnHeader>Owner</Table.ColumnHeader>
                  <Table.ColumnHeader>Updated</Table.ColumnHeader>
                  <Table.ColumnHeader numeric>Open issues</Table.ColumnHeader>
                </Table.Row>
              </Table.Header>
              <Table.Body>
                <Table.Empty colSpan={columnCount}>
                  No projects matched the current filters.
                </Table.Empty>
              </Table.Body>
            </Table>
          </Table.ScrollArea>
        );
      }
    `}

  </Preview.Code>

  <Preview.Data>
    {`
      const columnCount = 4;
    `}

  </Preview.Data>

  <Preview.CSS>
    {`
      .shell {
        width: min(44rem, calc(100vw - var(--spacing-8)));
      }

      .table {
        min-width: 40rem;
      }
    `}

  </Preview.CSS>
</Preview>

### Inside Card

`Table.ScrollArea` fits naturally inside `Card.Body` when the table should live on a card surface.

<Preview>
  <TableInCardExample />

  <Preview.Code>
    {`
      import { Card, Table } from "moduix";

      <Card>
        <Card.Header>
          <Card.Title>Projects</Card.Title>
        </Card.Header>
        <Card.Body className={styles.cardContent}>
          <Table.ScrollArea>
            <Table className={styles.table}>
              <Table.Header>
                <Table.Row>
                  <Table.ColumnHeader>Project</Table.ColumnHeader>
                  <Table.ColumnHeader>Owner</Table.ColumnHeader>
                  <Table.ColumnHeader>Updated</Table.ColumnHeader>
                  <Table.ColumnHeader>Health</Table.ColumnHeader>
                </Table.Row>
              </Table.Header>
              <Table.Body>
                {projects.map((project) => (
                  <Table.Row key={project.name}>
                    <Table.Cell>{project.name}</Table.Cell>
                    <Table.Cell>{project.owner}</Table.Cell>
                    <Table.Cell>{project.updated}</Table.Cell>
                    <Table.Cell>{project.issues}</Table.Cell>
                  </Table.Row>
                ))}
              </Table.Body>
            </Table>
          </Table.ScrollArea>
        </Card.Body>
      </Card>
    `}

  </Preview.Code>

  <Preview.Data>
    {`
      const projects = [
        { name: "Docs redesign", owner: "Product Design", updated: "2 hours ago", issues: "3 open" },
        { name: "Billing migration", owner: "Growth", updated: "Yesterday", issues: "1 blocked" },
        { name: "Command palette", owner: "Platform", updated: "Today", issues: "Healthy" },
      ];
    `}

  </Preview.Data>

  <Preview.CSS>
    {`
      .card {
        width: min(44rem, calc(100vw - var(--spacing-8)));
      }

      .cardContent {
        min-width: 0;
        padding-top: 0;
      }

      .table {
        min-width: 40rem;
      }
    `}

  </Preview.CSS>
</Preview>

### Row Actions With Menu

Use the last column for row-specific actions when the dataset needs lightweight overflow commands.
`Menu` fits this case cleanly without changing the native table structure.

<Preview>
  <TableRowActionsExample />

  <Preview.Code>
    {`
      import {
        Button,
        Menu,
        Portal,
        Table,
      } from "moduix";

      function RowActionsMenu({ itemName }) {
        return (
          <Menu positioning={{ placement: "bottom-end" }}>
            <Menu.Trigger asChild>
                <Button
                  variant="ghost"
                  size="icon-sm"
                  className={styles.actionTrigger}
                  aria-label={\`Open actions for \${itemName}\`}
                >
                  <span aria-hidden className={styles.actionEllipsis}>...</span>
                </Button>
            </Menu.Trigger>
            <Portal>
              <Menu.Positioner>
                <Menu.Content>
                  <Menu.Item value="open">Open project</Menu.Item>
                  <Menu.Item value="copy-link">Copy link</Menu.Item>
                  <Menu.Item value="duplicate">Duplicate</Menu.Item>
                  <Menu.Separator />
                  <Menu.Item value="archive" tone="destructive">
                    Archive
                  </Menu.Item>
                </Menu.Content>
              </Menu.Positioner>
            </Portal>
          </Menu>
        );
      }

      export function TableRowActionsDemo() {
        return (
          <Table.ScrollArea className={styles.shell}>
            <Table className={styles.table}>
              <Table.Header>
                <Table.Row>
                  <Table.ColumnHeader>Project</Table.ColumnHeader>
                  <Table.ColumnHeader>Owner</Table.ColumnHeader>
                  <Table.ColumnHeader>Environment</Table.ColumnHeader>
                  <Table.ColumnHeader>Updated</Table.ColumnHeader>
                  <Table.ColumnHeader className={styles.actionsHead}>Actions</Table.ColumnHeader>
                </Table.Row>
              </Table.Header>
              <Table.Body>
                {rows.map((row) => (
                  <Table.Row key={row.name}>
                    <Table.Cell>{row.name}</Table.Cell>
                    <Table.Cell>{row.owner}</Table.Cell>
                    <Table.Cell>{row.environment}</Table.Cell>
                    <Table.Cell>{row.updated}</Table.Cell>
                    <Table.Cell className={styles.actionsCell}>
                      <RowActionsMenu itemName={row.name} />
                    </Table.Cell>
                  </Table.Row>
                ))}
              </Table.Body>
            </Table>
          </Table.ScrollArea>
        );
      }
    `}

  </Preview.Code>

  <Preview.Data>
    {`
      const rows = [
        { name: "Docs redesign", owner: "Product Design", environment: "Production", updated: "2 hours ago" },
        { name: "Billing migration", owner: "Growth", environment: "Staging", updated: "Yesterday" },
        { name: "Command palette", owner: "Platform", environment: "Preview", updated: "Today" },
      ];
    `}

  </Preview.Data>

  <Preview.CSS>
    {`
      .shell {
        width: min(44rem, calc(100vw - var(--spacing-8)));
      }

      .table {
        min-width: 44rem;
      }

      .actionsHead,
      .actionsCell {
        width: 1%;
        text-align: right;
        white-space: nowrap;
      }

      .actionTrigger {
        --button-size-icon-sm: 1.25rem;
        --button-focus-ring-width: var(--border-width-sm);

        margin-inline-start: auto;
        border-radius: var(--radius-sm);
      }

      .actionEllipsis {
        font-size: 0.8125rem;
        font-weight: var(--weight-medium);
        line-height: 1;
      }
    `}

  </Preview.CSS>
</Preview>

### Custom Composition

Skip `Table.ScrollArea` when the surrounding layout already owns the shell or scrolling behavior.

<Preview>
  <CustomCompositionTableExample />

  <Preview.Code>
    {`
      import {
        Table,
      } from "moduix";

      <div className={styles.shell}>
        <Table className={styles.table}>
          <Table.Caption side="bottom">
            This version uses a plain wrapper instead of Table.ScrollArea.
          </Table.Caption>
          <Table.Header>
            <Table.Row>
              <Table.ColumnHeader>Plan</Table.ColumnHeader>
              <Table.ColumnHeader>Seats</Table.ColumnHeader>
              <Table.ColumnHeader numeric>Monthly cost</Table.ColumnHeader>
            </Table.Row>
          </Table.Header>
          <Table.Body>
            {plans.map((plan) => (
              <Table.Row key={plan.name}>
                <Table.Cell>{plan.name}</Table.Cell>
                <Table.Cell>{plan.seats}</Table.Cell>
                <Table.Cell numeric>{plan.cost}</Table.Cell>
              </Table.Row>
            ))}
          </Table.Body>
        </Table>
      </div>
    `}

  </Preview.Code>

  <Preview.Data>
    {`
      const plans = [
        { name: "Starter", seats: "Up to 5", cost: "$19" },
        { name: "Team", seats: "Up to 25", cost: "$79" },
        { name: "Enterprise", seats: "Custom", cost: "Contact sales" },
      ];
    `}

  </Preview.Data>

  <Preview.CSS>
    {`
      .shell {
        width: min(44rem, calc(100vw - var(--spacing-8)));
        overflow-x: auto;
        padding: var(--spacing-3);
        border: var(--border-width-sm) dashed var(--color-border);
        border-radius: var(--radius-lg);
        background-color: color-mix(in srgb, var(--color-muted) 35%, transparent);
      }

      .table {
        min-width: 34rem;
      }
    `}

  </Preview.CSS>
</Preview>

## Styling

Style `Table` through `className`, the public `data-slot` hooks, `data-side` and `data-numeric`
attributes, and the `--table-*` CSS variables shown below.

### CSS Properties

<div className="not-prose">
  <Tabs items={['CSS Variables']} className="mb-0 mt-3">
    <Tab className="max-h-150 overflow-auto">
      <TableCssPropertiesPanel />
    </Tab>
  </Tabs>
</div>

### Styling hooks

Use the part that owns the visual concern:

- `className` on `Table.ScrollArea` for shell-wide border, radius, and surface overrides
- `className` on `Table` for table typography and width constraints
- `data-slot="table-root"`, `table-scroll-area`, `table-column-group`, `table-column`,
  `table-caption`, `table-header`, `table-body`, `table-footer`, `table-row`,
  `table-column-header`, `table-cell`, and `table-empty` for part-level targeting
- `data-side="top" | "bottom"` on `Table.Caption` for caption placement styling
- `data-numeric` on `Table.ColumnHeader` and `Table.Cell` for numeric-column targeting
- `data-size`, `data-variant`, `data-striped`, `data-interactive`, `data-sticky-header`, and
  `data-show-column-border` on `Table` for root-level state styling
- `data-sticky="start" | "end"` on `Table.ColumnHeader` and `Table.Cell` for sticky columns