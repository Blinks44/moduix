---
title: Collapsible
description: A disclosure control that opens and closes a single content region.
icon: Component
---

import {
  CollapsibleCssPropertiesPanel,
  CollapsibleExample,
  ControlledCollapsibleExample,
  CustomCompositionCollapsibleExample,
  DisabledCollapsibleExample,
  LazyMountCollapsibleExample,
  NestedCollapsibleExample,
  PartialCollapseCollapsibleExample,
  RootProviderCollapsibleExample,
} from '@/components/examples/collapsible';

## API Reference

<BaseUIReference href="https://ark-ui.com/docs/components/collapsible" />

## Basic

Use the Ark-shaped `Root`, `Trigger`, `Indicator`, and `Content` parts for one inline disclosure.

<Preview>
  <CollapsibleExample />

  <Preview.Code>
    {`
      import { Collapsible } from "moduix";

      export function CollapsibleDemo() {
        return (
          <Collapsible.Root>
            <Collapsible.Trigger>
              Recovery keys
              <Collapsible.Indicator />
            </Collapsible.Trigger>
            <Collapsible.Content>
              <ul>
                <li>alien-bean-pasta</li>
                <li>wild-irish-burrito</li>
                <li>horse-battery-staple</li>
              </ul>
            </Collapsible.Content>
          </Collapsible.Root>
        );
      }
    `}

  </Preview.Code>
</Preview>

## Install with shadcn (optional)

Install the component source from the moduix GitHub registry:

<Tabs items={['npm', 'pnpm', 'bun', 'deno', 'aube']}>
  <Tab value="npm">

```bash
npx shadcn@latest add Blinks44/moduix/collapsible
```

  </Tab>
  <Tab value="pnpm">

```bash
pnpm dlx shadcn@latest add Blinks44/moduix/collapsible
```

  </Tab>
  <Tab value="bun">

```bash
bunx shadcn@latest add Blinks44/moduix/collapsible
```

  </Tab>
  <Tab value="deno">

```bash
deno x -A npm:shadcn@latest add Blinks44/moduix/collapsible
```

  </Tab>
  <Tab value="aube">

```bash
aubx shadcn@latest add Blinks44/moduix/collapsible
```

  </Tab>
</Tabs>

## Anatomy

```text
Collapsible.Root
├─ Collapsible.Trigger
│  ├─ label
│  └─ Collapsible.Indicator (optional)
└─ Collapsible.Content
   └─ content
```

| Part                       | Role                                                                |
| -------------------------- | ------------------------------------------------------------------- |
| `Collapsible.Root`         | Owns controlled or uncontrolled open state.                         |
| `Collapsible.RootProvider` | Connects parts to state created with `useCollapsible`.              |
| `Collapsible.Trigger`      | Accessible button that toggles the content.                         |
| `Collapsible.Indicator`    | Optional state cue with a default moduix chevron.                   |
| `Collapsible.Content`      | Collapsible region measured through Ark CSS variables.              |
| `Collapsible.Context`      | Exposes Ark state to a render function without another state layer. |

## Composition

### Recommended composition

Use `Collapsible.Root`, `Collapsible.Trigger`, optional `Collapsible.Indicator`, and
`Collapsible.Content`. Root props include `open`, `defaultOpen`, `onOpenChange(details)`,
`disabled`, `collapsedHeight`, `collapsedWidth`, `lazyMount`, `unmountOnExit`, `ids`, and
`onExitComplete`.

Keep padding and surface styles on an inner element inside `Collapsible.Content`. Ark measures the
content and exposes `--height`, `--width`, `--collapsed-height`, and `--collapsed-width`.

### When to go custom

Use `asChild` when another element must own the rendered node. This is the Ark replacement for the
removed Base UI `render` and `nativeButton` contract.

Use `useCollapsible` with `Collapsible.RootProvider` when state must be created outside the part
tree. Use `Collapsible.Context` when descendants need the current Ark state.

`Collapsible.Indicator` is optional. It renders the moduix right chevron by default and accepts
custom children.

### Default props

| Part                    | Default          | Notes                                              |
| ----------------------- | ---------------- | -------------------------------------------------- |
| `Collapsible.Root`      | closed           | `defaultOpen={false}`, mounted content             |
| `Collapsible.Trigger`   | `button`         | Supports Ark `asChild`                             |
| `Collapsible.Indicator` | right chevron    | Rotates from `data-state="open"`                   |
| `Collapsible.Content`   | collapsed region | Uses Ark measurement variables and render strategy |

### Behavioral notes

- `onOpenChange` receives an Ark details object. Read the next state from `details.open`.
- Keep nested interactive controls outside `Collapsible.Trigger`.
- Use `Accordion` instead when multiple disclosures need coordinated keyboard or value behavior.

## Examples

### Default Open

Set `defaultOpen` for an uncontrolled disclosure that starts expanded.

<Preview>
  <CollapsibleExample defaultOpen />

  <Preview.Code>
    {`
      <Collapsible.Root defaultOpen>
        <Collapsible.Trigger>
          Recovery keys
          <Collapsible.Indicator />
        </Collapsible.Trigger>
        <Collapsible.Content>Content</Collapsible.Content>
      </Collapsible.Root>
    `}

  </Preview.Code>
</Preview>

### Controlled

Use `open` with the Ark `onOpenChange(details)` callback when surrounding UI owns the state.

<Preview>
  <ControlledCollapsibleExample />

  <Preview.Code>
    {`
      import { Collapsible } from "moduix";
      import { useState } from "react";

      export function ControlledCollapsibleDemo() {
        const [open, setOpen] = useState(false);

        return (
          <Collapsible.Root
            open={open}
            onOpenChange={(details) => setOpen(details.open)}
          >
            <Collapsible.Trigger>
              Recovery keys
              <Collapsible.Indicator />
            </Collapsible.Trigger>
            <Collapsible.Content>Content</Collapsible.Content>
          </Collapsible.Root>
        );
      }
    `}

  </Preview.Code>
</Preview>

### Disabled

Set `disabled` on the root to keep the disclosure visible but unavailable.

<Preview>
  <DisabledCollapsibleExample />

  <Preview.Code>
    {`
      <Collapsible.Root disabled>
        <Collapsible.Trigger>
          Recovery keys
          <Collapsible.Indicator />
        </Collapsible.Trigger>
        <Collapsible.Content>Content</Collapsible.Content>
      </Collapsible.Root>
    `}

  </Preview.Code>
</Preview>

### Lazy Mount

Combine `lazyMount` and `unmountOnExit` when closed content should not remain in the DOM.

<Preview>
  <LazyMountCollapsibleExample />

  <Preview.Code>
    {`
      <Collapsible.Root lazyMount unmountOnExit>
        <Collapsible.Trigger>
          Recovery keys
          <Collapsible.Indicator />
        </Collapsible.Trigger>
        <Collapsible.Content>Content</Collapsible.Content>
      </Collapsible.Root>
    `}

  </Preview.Code>
</Preview>

### Partial Collapse

Use `collapsedHeight` or `collapsedWidth` to leave part of the content visible while closed.

<Preview>
  <PartialCollapseCollapsibleExample />

  <Preview.Code>
    {`
      <Collapsible.Root collapsedHeight="3rem">
        <Collapsible.Trigger>
          Recovery keys
          <Collapsible.Indicator />
        </Collapsible.Trigger>
        <Collapsible.Content>Content</Collapsible.Content>
      </Collapsible.Root>
    `}

  </Preview.Code>
</Preview>

### Nested

Nest independent roots when a disclosed section contains another optional detail.

<Preview>
  <NestedCollapsibleExample />

  <Preview.Code>
    {`
      <Collapsible.Root>
        <Collapsible.Trigger>
          Account security
          <Collapsible.Indicator />
        </Collapsible.Trigger>
        <Collapsible.Content>
          <Collapsible.Root>
            <Collapsible.Trigger>
              Recovery keys
              <Collapsible.Indicator />
            </Collapsible.Trigger>
            <Collapsible.Content>Nested content</Collapsible.Content>
          </Collapsible.Root>
        </Collapsible.Content>
      </Collapsible.Root>
    `}

  </Preview.Code>
</Preview>

### Root Provider

Create the state machine with `useCollapsible` when code outside the rendered root needs its state.

<Preview>
  <RootProviderCollapsibleExample />

  <Preview.Code>
    {`
      import { Collapsible, useCollapsible } from "moduix";

      export function RootProviderCollapsibleDemo() {
        const collapsible = useCollapsible();

        return (
          <Collapsible.RootProvider value={collapsible}>
            <Collapsible.Trigger>
              Recovery keys
              <Collapsible.Indicator />
            </Collapsible.Trigger>
            <Collapsible.Content>Content</Collapsible.Content>
          </Collapsible.RootProvider>
        );
      }
    `}

  </Preview.Code>
</Preview>

### Custom Composition

Use `asChild` to compose the trigger behavior onto a consumer-owned button.

<Preview>
  <CustomCompositionCollapsibleExample />

  <Preview.Code>
    {`
      import { ChevronDownIcon, Collapsible } from "moduix";

      export function CustomCompositionCollapsibleDemo() {
        return (
          <Collapsible.Root>
            <Collapsible.Trigger asChild>
              <button type="button" className="custom-trigger">
                Styled recovery keys
                <Collapsible.Indicator className="custom-indicator">
                  <ChevronDownIcon />
                </Collapsible.Indicator>
              </button>
            </Collapsible.Trigger>
            <Collapsible.Content>
              <div className="custom-content">Content</div>
            </Collapsible.Content>
          </Collapsible.Root>
        );
      }
    `}

  </Preview.Code>

  <Preview.CSS>
    {`
      .custom-trigger {
        display: flex;
        width: 100%;
        align-items: center;
        justify-content: space-between;
        padding: var(--spacing-2) var(--spacing-3);
        border-radius: var(--radius-md);
        background-color: var(--color-muted);
      }

      .custom-indicator {
        --collapsible-indicator-open-transform: rotate(180deg);
        color: var(--color-primary);
      }

      .custom-content {
        margin-top: var(--spacing-1);
        padding: var(--spacing-2) var(--spacing-3);
        border-radius: var(--radius-md);
        background-color: var(--color-muted);
      }
    `}

  </Preview.CSS>
</Preview>

## Styling

### CSS Properties

<div className="not-prose">
  <Tabs items={['CSS Variables']} className="mb-0 mt-3">
    <Tab className="max-h-150 overflow-auto">
      <CollapsibleCssPropertiesPanel />
    </Tab>
  </Tabs>
</div>

### Styling hooks

- DOM parts expose `data-slot="collapsible-root"`, `collapsible-root-provider`,
  `collapsible-trigger`, `collapsible-indicator`, and `collapsible-content`.
- Ark exposes `data-state="open" | "closed"` on root, trigger, indicator, and content.
- `data-disabled` marks disabled trigger, indicator, and content parts.
- `data-has-collapsed-size` marks content configured with `collapsedHeight` or `collapsedWidth`.
- Content animation uses Ark `--height`, `--width`, `--collapsed-height`, and `--collapsed-width`.