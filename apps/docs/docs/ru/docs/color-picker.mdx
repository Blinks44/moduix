---
title: Color Picker
component: color-picker
description: Скомпонованный Color Picker из Ark UI для цветовых полей, элементов управления каналами и образцов.
---

import * as ColorPickerDocs from '@/components/examples/color-picker';

## Справочник API

<PrimitiveReference href="https://ark-ui.com/docs/components/color-picker" />

## Выбор подходящего компонента

Используйте `ColorPicker`, когда пользователям нужно выбрать или точно настроить цвет. Используйте `Input`, когда пользователям достаточно
вставить известное значение цвета и прямое управление цветом не нужно.

## Основы

Начните с компактного поля: корневого компонента, одного поля канала, стандартного образца в триггере, явного всплывающего
содержимого, стандартных внутренних частей picker и скрытого поля ввода для форм.

<style>{ColorPickerDocs.colorPickerExampleCss}</style>

<PreviewFrame maxWidth="sm">
  ```tsx preview file="./_snippets/color-picker/basic.tsx"

```
</PreviewFrame>

## Установка с shadcn

<ShadcnInstall packageName="color-picker" />

This install path also pulls in the shared moduix foundation files that `ColorPicker` depends on,
such as styles, icons, and small utilities.

## Анатомия

```text
ColorPicker
├─ ColorPicker.Label
├─ ColorPicker.Control
│  ├─ ColorPicker.ChannelInput[channel]
│  └─ ColorPicker.Trigger
│     ├─ ColorPicker.TransparencyGrid
│     └─ ColorPicker.ValueSwatch
├─ Поддерево оверлея (автоматически портируется)
│  └─ ColorPicker.Positioner
│     └─ ColorPicker.Content
│        ├─ ColorPicker.Area
│        │  ├─ ColorPicker.AreaBackground
│        │  └─ ColorPicker.AreaThumb
│        ├─ ColorPicker.EyeDropperTrigger
│        ├─ ColorPicker.ChannelSlider[channel]
│        │  ├─ ColorPicker.TransparencyGrid
│        │  ├─ ColorPicker.ChannelSliderTrack
│        │  └─ ColorPicker.ChannelSliderThumb
│        ├─ ColorPicker.SwatchGroup
│        │  └─ ColorPicker.SwatchTrigger[value]
│        │     └─ ColorPicker.Swatch[value]
│        │        └─ ColorPicker.SwatchIndicator
│        └─ ColorPicker.View[format]
│           └─ ColorPicker.ChannelInput[channel]
└─ нативное поле ввода (автоматически)
```

| Part                               | Role                                                                   |
| ---------------------------------- | ---------------------------------------------------------------------- |
| `ColorPicker`                      | Owns color value, format, popup state, and form integration.           |
| `ColorPicker.Control`              | Groups visible inputs and trigger.                                     |
| `ColorPicker.Trigger`              | Button that opens the popup and renders the current swatch by default. |
| `ColorPicker.Content`              | Actual Ark popup content part.                                         |
| `ColorPicker.Area`                 | Two-dimensional color interaction surface.                             |
| `ColorPicker.ChannelSlider`        | One-dimensional channel control for hue, alpha, or other channels.     |
| `ColorPicker.Sliders`              | Styled hue and alpha slider pair.                                      |
| `ColorPicker.ChannelInput`         | Text or numeric input for a specific color channel.                    |
| `ColorPicker.SwatchTrigger`        | Selectable preset color button.                                        |
| `ColorPicker.ValueSwatch`          | Visual swatch for the current value.                                   |

`ColorPicker.RootProvider`, `parseColor`, `useColorPicker`, and `useColorPickerContext` cover the
normal advanced state path. Import Ark-only type helpers directly when needed as an escape hatch.

## Композиция

The root portals `Positioner` automatically. Use `portalled={false}` for inline rendering and `portalRef` for a custom container; structural parts remain explicit and styleable.

### Рекомендуемая композиция

Keep `ColorPicker.Positioner` and `ColorPicker.Content` visible for popup pickers. `Content` is the
actual Ark part; the root only automates the portal boundary.

Omit children on `Trigger`, `Area`, `ChannelSlider`, and `SwatchTrigger` for the default visual
composition. Use `ColorPicker.Sliders` for the usual hue and alpha pair. Style its channels through
`data-channel` or CSS variables; pass children only when a part needs custom structure. Add
`data-fit-content` when the trigger should size to custom content such as a swatch plus `ValueText`.

For native form submission and reset behavior, pass `name` to `ColorPicker`. It automatically
renders and synchronizes its native input; `FormData` and `form.reset()` need no extra setup.

### Когда нужна кастомизация

Use `inline` on `ColorPicker` when the area, sliders, inputs, and swatches should render as a
permanent panel instead of a popup. Use `ColorPicker.View` to show inputs for one format at a time.

Use `useColorPicker` with `ColorPicker.RootProvider` when the state must be owned outside the
rendered subtree. Do not render `Root` and `RootProvider` for the same state instance.

Use `asChild` on exported Ark parts when another semantic element must own the DOM node. The child
must stay a single element and preserve the required focus and interaction behavior.

### Значения по умолчанию

The portalled overlay content lazily mounts and unmounts after its exit animation by default. Set
`unmountOnExit={false}` to retain it after first open; set both lifecycle props to `false` only when
eager initial rendering is needed.

| Surface / Prop                           | Default                 | Notes                                         |
| ---------------------------------------- | ----------------------- | --------------------------------------------- |
| `defaultValue`                           | `parseColor("#000000")` | Ark default color value.                      |
| `defaultFormat`                          | `"rgba"`                | Ark default format.                           |
| `ColorPicker.Content` width              | `16rem`                 | Prevents collapse to trigger swatch width.    |
| `ColorPicker.EyeDropperTrigger`          | moduix pipette icon     | Used when children are omitted.               |
| `ColorPicker.Trigger` children           | current swatch          | Renders `TransparencyGrid` and `ValueSwatch`. |
| `ColorPicker.Area` children              | background and thumb    | Used when children are omitted.               |
| `ColorPicker.ChannelSlider` children     | track and thumb         | Alpha sliders also render `TransparencyGrid`. |
| `ColorPicker.Sliders`                    | hue and alpha pair      | Style with `data-channel` or CSS variables.   |
| `ColorPicker.SwatchTrigger` children     | swatch and indicator    | Uses the trigger `value` for the swatch.      |
| `ColorPicker.SwatchIndicator`            | moduix check icon       | Used when children are omitted.               |
| `ColorPicker` width                      | `16rem` (max `100%`)    | Styled root width defaults.                   |

### Особенности поведения

- `value` and `defaultValue` use Ark `Color` objects. Use `parseColor()` for string defaults.
- `onValueChange(details)` and `onValueChangeEnd(details)` preserve Ark detail objects.
- `onFormatChange(details)` and `onOpenChange(details)` preserve Ark detail objects.
- `ids` is forwarded to Ark for shared accessibility IDs across custom compositions.
- Field and fieldset state can flow through Ark-compatible `disabled`, `invalid`, `required`, and
  `readOnly` root props.
- Use `positioning`, `initialFocusEl`, and `openAutoFocus` when popup placement or focus needs to
  follow a surrounding workflow.
- `lazyMount` and `unmountOnExit` default to `true`; `present` and `onExitComplete` preserve Ark's
  presence lifecycle for custom mount or exit-animation control.
- All Ark DOM parts preserve `asChild`.

## Примеры

### Компактный триггер

Use `data-fit-content` on `ColorPicker.Trigger` when a content-sized button reads better than a
square swatch, for example during migration from button-based color fields.

<style>{ColorPickerDocs.colorPickerInlineCss}</style>

<PreviewFrame contentWidth="fit-content" maxWidth="sm">
  ```tsx preview file="./_snippets/color-picker/compact-trigger.tsx"

```

</PreviewFrame>

### Управляемый режим

Используйте управляемое состояние, когда выбранный цвет должен синхронизироваться с состоянием приложения.

<style>{ColorPickerDocs.colorPickerExampleCss}</style>

<PreviewFrame maxWidth="sm">
  ```tsx preview file="./_snippets/color-picker/controlled.tsx"

```

</PreviewFrame>

### Отключённое состояние

Use `disabled` on the root when every color picker part should become non-interactive while keeping
the current color visible.

<style>{ColorPickerDocs.colorPickerExampleCss}</style>

<PreviewFrame maxWidth="sm">
  ```tsx preview file="./_snippets/color-picker/disabled.tsx"

```

</PreviewFrame>

### Состояние поля

Используйте пропы состояния формы корневого компонента, такие как `required` и `invalid`, когда picker отрисовывается внутри
контейнера поля и должен отображать то же состояние через атрибуты данных Ark.

<PreviewFrame contentWidth="fit-content" maxWidth="sm">
  ```tsx preview file="./_snippets/color-picker/field-state.tsx"

```

</PreviewFrame>

### Использование в форме

Use `name` on `ColorPicker`; it renders its native form input automatically.

<PreviewFrame maxWidth="sm">
  ```tsx preview file="./_snippets/color-picker/form-usage.tsx"

```

</PreviewFrame>

### Представления формата

Используйте `format`, `onFormatChange`, `FormatSelect` и `View`, когда пользователи должны переключаться между
группами полей каналов без изменения структуры picker.

<style>{ColorPickerDocs.colorPickerInlineCss}</style>

<PreviewFrame maxWidth="sm">
  ```tsx preview file="./_snippets/color-picker/format-views.tsx"

```

</PreviewFrame>

### Встроенный

Use `inline` for settings panels where the color tools should stay visible without a popup trigger.

<style>{ColorPickerDocs.colorPickerInlineCss}</style>

<PreviewFrame maxWidth="sm">
  ```tsx preview file="./_snippets/color-picker/inline.tsx"

```

</PreviewFrame>

### Только ввод

Используйте поля каналов с пассивным образцом значения, когда важен ввод цвета с клавиатуры, а всплывающее окно было бы
слишком громоздким для интерфейса.

<style>{ColorPickerDocs.colorPickerInlineCss}</style>

<PreviewFrame maxWidth="sm">
  ```tsx preview file="./_snippets/color-picker/input-only.tsx"

```

</PreviewFrame>

### Внутри Dialog

When a picker lives inside a modal dialog, render its popup without a portal so it shares the
dialog's stacking context and remains interactive above the dialog surface.

<style>{ColorPickerDocs.colorPickerDialogCss}</style>

<PreviewFrame maxWidth="sm">
  ```tsx preview file="./_snippets/color-picker/inside-dialog.tsx"

```

</PreviewFrame>

### Управляемое открытие

Используйте управляемое состояние `open`, когда другой элемент управления или сценарий определяет видимость всплывающего окна.

<style>{ColorPickerDocs.colorPickerExampleCss}</style>

<PreviewFrame maxWidth="sm">
  ```tsx preview file="./_snippets/color-picker/open-controlled.tsx"

```

</PreviewFrame>

### Root Provider

Use moduix `useColorPicker` with `RootProvider` when the color picker state must be created outside
the rendered tree and shared with nearby controls.

<style>{ColorPickerDocs.colorPickerExampleCss}</style>

<PreviewFrame maxWidth="sm">
  ```tsx preview file="./_snippets/color-picker/root-provider.tsx"

```

</PreviewFrame>

### Только слайдер

Используйте композицию только со слайдером, когда важно прямое управление, но ввод канала с клавиатуры не нужен.

<style>{ColorPickerDocs.colorPickerInlineCss}</style>

<PreviewFrame maxWidth="sm">
  ```tsx preview file="./_snippets/color-picker/slider-only.tsx"

```

</PreviewFrame>

### Только образец

Use swatch-only composition when a preset palette is enough and the area, sliders, and inputs would
be unnecessary.

<style>{ColorPickerDocs.colorPickerInlineCss}</style>

<PreviewFrame contentWidth="fit-content" maxWidth="sm">
  ```tsx preview file="./_snippets/color-picker/swatch-only.tsx"

```

</PreviewFrame>

### Образцы цветов

Используйте образцы во всплывающем окне, когда picker должен предлагать отобранный набор цветов в дополнение к свободному
редактированию. `closeOnSelect` соответствует поведению Ark и закрывает всплывающее окно после выбора образца.

<style>{ColorPickerDocs.colorPickerExampleCss}</style>

<PreviewFrame maxWidth="sm">
  ```tsx preview file="./_snippets/color-picker/swatches.tsx"

```

</PreviewFrame>

### Образец значения

Use `ValueSwatch` and `ValueText` when adjacent UI needs to show the selected color without opening
the full picker.

<style>{ColorPickerDocs.colorPickerValueSwatchCss}</style>

<PreviewFrame contentWidth="fit-content" maxWidth="sm">
  ```tsx preview file="./_snippets/color-picker/value-swatch.tsx"

```

</PreviewFrame>

### Расширенная кастомизация

Используйте явные части и дочерние элементы, когда стандартной композиции нужна другая визуальная структура.

<style>{ColorPickerDocs.colorPickerExampleCss}</style>

<PreviewFrame maxWidth="sm">
  ```tsx preview file="./_snippets/color-picker/advanced-customization.tsx"

```

</PreviewFrame>

## Стилизация

Style `ColorPicker` through `className`, stable `data-slot="color-picker-*"` hooks, Ark
`data-scope` / `data-part` attributes, and the public `--moduix-color-picker-*` CSS variables shown
below.

### CSS-свойства

<CssPropertiesSection properties={ColorPickerDocs.colorPickerOverrideCssProperties} />

### Хуки стилизации

Use the part that owns the visual concern:

- `ColorPicker` for width, gap, shared color, and disabled opacity
- `ColorPicker.Control`, `ChannelInput`, `Trigger`, `FormatSelect`, and `FormatTrigger` for the
  visible field controls
- `ColorPicker.Positioner` and `ColorPicker.Content` for popup layering, width, padding, radius,
  shadow, and state animations
- `ColorPicker.Area`, `AreaBackground`, and `AreaThumb` for the two-dimensional color surface
- `ColorPicker.ChannelSlider`, `ChannelSliderTrack`, `ChannelSliderThumb`, and `Sliders` for hue,
  alpha, and other channel sliders
- `ColorPicker.SwatchGroup`, `SwatchTrigger`, `Swatch`, and `SwatchIndicator` for preset color
  grids
- `ColorPicker.ValueSwatch`, `ValueText`, `View`, and `TransparencyGrid` for value display and
  format-specific layouts

The main state hooks are `data-state`, `data-focus`, `data-invalid`, `data-disabled`,
`data-readonly`, `data-required`, `data-channel`, `data-orientation`, `data-value`,
`data-placement`, and `data-side`. Ark runtime variables include `--color`, `--value`,
`--available-width`, `--available-height`, and `--transform-origin`.
