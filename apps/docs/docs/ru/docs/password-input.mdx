---
title: Password Input
компонент: ввод пароля
description: Поле пароля Ark UI с переключателем видимости защищённого текста.
---

import * as PasswordInputDocs from '@/components/examples/password-input';

## Справочник API

<PrimitiveReference href="https://ark-ui.com/docs/components/password-input" />

## Выбор подходящего компонента

Используйте `PasswordInput`, когда одно безопасное значение должно оставаться замаскированным по умолчанию, но при этом разрешается
действие раскрытия, контролируемое пользователем.

- Выберите **`Input`** для полей в виде простого текста без переключателя видимости.
- Выберите **`PasswordInput`** для паролей, ключей API и других защищенных строк.
- Выберите **`InputGroup`**, если для поля требуется настраиваемое встроенное действие, не связанное с видимостью пароля.

## Базовый пример

Используйте `PasswordInput.Field` для стандартного ввода и триггера видимости. Он включает в себя moduix по умолчанию
значки, сохраняя при этом состояние Ark и поведение доступности.

<PreviewFrame maxWidth="sm">
  ```tsx preview file="../../en/docs/_snippets/password-input/basic.tsx"

  ```
</PreviewFrame>

## Установка через shadcn

<ShadcnInstall packageName="password-input" />

## Анатомия

```tsx
<PasswordInput>
  <PasswordInput.Label />
  <PasswordInput.Field />
</PasswordInput>
```

Для расширенной настройки замените `PasswordInput.Field` его явными частями Ark:

```tsx
<PasswordInput>
  <PasswordInput.Label />
  <PasswordInput.Control>
    <PasswordInput.Input />
    <PasswordInput.VisibilityTrigger>
      <PasswordInput.Indicator />
    </PasswordInput.VisibilityTrigger>
  </PasswordInput.Control>
</PasswordInput>
```

|Часть|Роль|
| -------------------------------------- | ------------------------------------------------------------------------------ |
|`PasswordInput` / `PasswordInput.Root`|Владеет состоянием видимости пароля и состоянием поля.|
|`PasswordInput.RootProvider`|Отрисовывает ту же корневую часть из объекта состояния `usePasswordInput`.|
|`PasswordInput.Label`|Маркирует входные данные и получает состояние «обязательное», «отключенное», «недопустимое» и «только для чтения».|
|`PasswordInput.Field`|Отрисовывает элемент управления по умолчанию, ввод, триггер видимости и индикатор.|
|`PasswordInput.Control`|Располагает триггер ввода и видимости вместе.|
|`PasswordInput.Input`|Собственная входная часть, `type` которой переключается между `password` и `text`.|
|`PasswordInput.VisibilityTrigger`|Кнопка, которая переключает состояние видимости.|
|`PasswordInput.Indicator`|Показывает видимый/скрытый контент; moduix предоставляет значки по умолчанию.|

## Композиция

### Рекомендуемая композиция

Используйте `PasswordInput.Field` для элемента управления по умолчанию, ввода, триггера видимости и индикатора. Проходить
видимость root, состояние формы, идентификаторы, переводы и реквизиты менеджера паролей для `PasswordInput`.

### Когда нужна собственная композиция

Используйте `Control`, `Input`, `VisibilityTrigger` и `Indicator` непосредственно, когда структура, значки или
стиль детали должен отличаться от `PasswordInput.Field`. Используйте `asChild` только тогда, когда деталь должна отрисовываться.
пользовательский семантический элемент.

### Свойства по умолчанию

|Опора|По умолчанию|Примечания|
| -------------------------------- | -------------------- | ------------------------------------------------------ |
|`autoComplete`|`"current-password"`|Ставим на вход через корневую машину.|
|`defaultVisible`|`false`|Начальное состояние неконтролируемой видимости.|
|`visible`|неконтролируемый|Состояние контролируемой видимости.|
|`PasswordInput.Field`| —                    |Отрисовывает видимые части по умолчанию и пересылает управляющие реквизиты.|
|`onVisibilityChange`| -                    |Получает детали Ковчега; прочитайте `details.visible`.|
|`translations.visibilityTrigger`|Ковчег по умолчанию|Функция, используемая для локализованных меток триггера видимости.|
|`ignorePasswordManagers`|`false`|Добавляет подсказки по игнорированию поддерживаемого менеджера паролей.|

### Особенности поведения

- Управляйте видимостью с помощью `visible` и `onVisibilityChange(details)`.
- Управляйте значением поля непосредственно на `PasswordInput.Input` с помощью встроенных `value` и `onChange`.
- Используйте корневые свойства, такие как `name`, `required`, `disabled`, `readOnly`, `invalid`, `ids` и
`translations` прямо из пользовательского интерфейса Ark.
- Используйте `usePasswordInput` с `PasswordInput.RootProvider`, когда то же самое требуется другому компоненту.
состояние видимости за пределами визуализированного дерева.
- `PasswordInput.Input`, `PasswordInput.VisibilityTrigger` и `PasswordInput.Indicator` открывают
`data-state="visible" | "hidden"` для стайлинга.

## Примеры

### Автозаполнение

Установите `autoComplete="new-password"` для создания и сброса потоков. По умолчанию остается
`"current-password"` для форм входа.

<PreviewFrame maxWidth="sm">
  ```tsx preview file="../../en/docs/_snippets/password-input/autocomplete.tsx"

  ```
</PreviewFrame>

### Контролируемая видимость

Используйте объект детализации видимости Ark, когда состояние раскрытия принадлежит состоянию React.

<PreviewFrame maxWidth="sm">
  ```tsx preview file="../../en/docs/_snippets/password-input/controlled-visibility.tsx"

  ```
</PreviewFrame>

### Игнорирование менеджеров паролей

Используйте `ignorePasswordManagers` для ключей API или сгенерированных секретов, где накладываются браузер и расширения.
должен оставаться вне поля.

<PreviewFrame maxWidth="sm">
  ```tsx preview file="../../en/docs/_snippets/password-input/ignore-password-managers.tsx"

  ```
</PreviewFrame>

### Root Provider

Используйте `usePasswordInput` и `RootProvider`, когда родственному элементу управления или дисплею состояния требуется доступ к
тот же объект состояния Ковчег.

<PreviewFrame maxWidth="sm">
  ```tsx preview file="../../en/docs/_snippets/password-input/root-provider.tsx"

  ```
</PreviewFrame>

### Индикатор надёжности

Сохраните состояние значения пароля на `PasswordInput.Input` и создайте дополнительный пользовательский интерфейс после элемента управления.

<style>{`
      .password-input-demo-strength-meter {
        display: grid;
        gap: var(--moduix-spacing-1);
        margin-top: var(--moduix-spacing-2);
      }

      .password-input-demo-strength-bar {
        height: var(--moduix-spacing-2);
        overflow: hidden;
        background: var(--moduix-color-muted);
        border-radius: var(--moduix-radius-full);
      }

      .password-input-demo-strength-fill[data-strength="medium"] {
        width: 60%;
        height: 100%;
        background: var(--moduix-color-warning);
      }

      .password-input-demo-strength-fill[data-strength="weak"] {
        width: 30%;
        height: 100%;
        background: var(--moduix-color-destructive);
      }

      .password-input-demo-strength-fill[data-strength="strong"] {
        width: 100%;
        height: 100%;
        background: var(--moduix-color-success);
      }

      .password-input-demo-strength-label {
        color: var(--moduix-color-muted-foreground);
        font-size: var(--moduix-text-xs);
        line-height: var(--moduix-line-height-text-xs);
        text-transform: capitalize;
      }
    `}</style>

<PreviewFrame maxWidth="sm">
  ```tsx preview file="../../en/docs/_snippets/password-input/strength-meter.tsx"

  ```
</PreviewFrame>

### С Field

Оберните `PasswordInput` в `Field`, если вспомогательный текст, текст ошибки или общее недопустимое состояние принадлежат
поле формы большего размера.

<PreviewFrame maxWidth="sm">
  ```tsx preview file="../../en/docs/_snippets/password-input/with-field.tsx"

  ```
</PreviewFrame>

### С валидацией

Используйте `Field.ErrorText`, чтобы соединить сообщение проверки с входными данными, пока Ark распространяет недействительные данные.
состояние для частей метки, ввода, управления и индикатора.

<PreviewFrame maxWidth="sm">
  ```tsx preview file="../../en/docs/_snippets/password-input/with-validation.tsx"

  ```
</PreviewFrame>

### Расширенная настройка

Обходите `PasswordInput.Field` и компонуйте части Ark напрямую при входе, триггере или индикаторе.
должна отличаться от структуры по умолчанию.

<PreviewFrame maxWidth="sm">
  ```tsx preview file="../../en/docs/_snippets/password-input/advanced-customization.tsx"

  ```
</PreviewFrame>

## Стилизация

`PasswordInput` настраивает оболочку управления, область ввода текста и триггер видимости с помощью
Токены `--moduix-password-input-*`, наложенные на контракт общего ввода.

### CSS-свойства

<div className="docs-reference">
  <Tabs
    values={[{ label: 'CSS-переменные', value: 'CSS Variables' }]}
    className="docs-reference-tabs"
    contentItemClassName="docs-reference-panel"
  >
    <Tab>
      <PasswordInputDocs.PasswordInputCssPropertiesPanel />
    </Tab>
  </Tabs>
</div>

### Хуки стилизации

- Корень и корневой провайдер предоставляют `data-slot="password-input-root"` и
`data-slot="password-input-root-provider"`.
- Детали Ark сохраняют значения `data-scope="password-input"` и `data-part` для `root`, `label`,
`control`, `input`, `visibility-trigger` и `indicator`.
- Стилизация состояния использует `data-state="visible" | "hidden"` на входе, триггере и индикаторе, а также
`data-disabled`, `data-invalid`, `data-readonly` и `data-required`, где их предоставляет Ark.
- Ark не предоставляет переменные runtime CSS-переменные, специфичные для ввода пароля; moduix добавляет
Переменные `--moduix-password-input-*` для визуальных настроек по умолчанию.
