---
title: App header
description: A copy-paste workspace header with calm navigation, notifications, and account actions.
---

import { AppHeaderPreview } from '@/components/recipes/app-header-preview';

# App header

Add a calm workspace header with a location switcher, compact navigation, unread notifications, and
account actions. The menus are fully interactive; replace the example anchors and menu items with
your routes and data.

<ExampleFrame clientOnly>
  <AppHeaderPreview />
</ExampleFrame>

<div className="docs-reference">
  <Tabs values={[{ label: 'Component', value: 'Component' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="Component">

```tsx file="../../../src/components/recipes/app-header.tsx"

```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/recipes/app-header.module.css"

```

    </Tab>
  </Tabs>
</div>

## Install

<ShadcnInstall packageName={['avatar', 'badge', 'button', 'menu', 'separator']} itemLabel="recipe" copiedSource dependencies={['lucide-react']} />

## Use in a route

Keep the header copy-owned: replace the workspace name, navigation anchors, notification data, and
menu actions with your routes and application state. The recipe works independently or above the
Sidebar dashboard.
