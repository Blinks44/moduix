---
title: Progress Circular
component: progress-circular
description: A circular progressbar for determinate or indeterminate progress.
---

import * as ProgressCircularDocs from '@/components/examples/progress-circular';

## API Reference

<PrimitiveReference href="https://ark-ui.com/docs/components/progress-circular" />

## Choosing the right component

Choose `ProgressCircular` for compact inline status, cards, toolbar badges, and small dashboards.
Choose `ProgressLinear` when horizontal comparison against a full track matters.

## Basic

Use `ProgressCircular.Ring` for the standard SVG circle subtree and
`ProgressCircular.ValueText` for visible progress.

<style>{ProgressCircularDocs.progressCircularBasicExampleCss}</style>

```tsx preview file="./_snippets/progress-circular/basic.tsx"

```

## Install with shadcn

<ShadcnInstall packageName="progress-circular" />

## Anatomy

```text
ProgressCircular.Root
├─ ProgressCircular.Label
├─ ProgressCircular.Ring
│  └─ fixed Circle, CircleTrack, and CircleRange subtree
├─ ProgressCircular.Circle
│  ├─ ProgressCircular.CircleTrack
│  └─ ProgressCircular.CircleRange
└─ ProgressCircular.ValueText

ProgressCircular.RootProvider
└─ same part tree connected to ProgressCircular.useProgress()
```

| Part                                         | Role                                                                   |
| -------------------------------------------- | ---------------------------------------------------------------------- |
| `ProgressCircular` / `ProgressCircular.Root` | Owns progress state, formatting, and ids.                              |
| `ProgressCircular.RootProvider`              | Connects parts to state created with `ProgressCircular.useProgress()`. |
| `ProgressCircular.Context`                   | Render-prop access to the current progress state.                      |
| `ProgressCircular.Label`                     | Accessible label for the progressbar.                                  |
| `ProgressCircular.Ring`                      | Default circle, track, and range convenience subtree.                  |
| `ProgressCircular.Circle`                    | SVG progressbar surface with ARIA value.                               |
| `ProgressCircular.CircleTrack`               | Background circle.                                                     |
| `ProgressCircular.CircleRange`               | Foreground circle that reflects the current percent/state.             |
| `ProgressCircular.ValueText`                 | Visible value text generated from Ark translations.                    |
| `ProgressCircular.View`                      | Conditional view for `indeterminate`, `loading`, or `complete`.        |

## Composition

### Recommended composition

`ProgressCircular` is the short root form. It is equivalent to `ProgressCircular.Root`.
`ProgressCircular.Ring` is the recommended fixed SVG circle, track, and range subtree; it accepts
`className` and the same CSS variables as `ProgressCircular.Circle`.

Use `defaultValue` for uncontrolled progress, `value` with `onValueChange(details)` for controlled
progress, and `defaultValue={null}` or `value={null}` for indeterminate progress.

### When to go custom

Use `translations.value(details)` for custom visible and accessible value text. Wrap the circle and
value text in your own layout element when the number should sit in the center.

Use `Circle`, `CircleTrack`, and `CircleRange` when their structure or Ark `asChild` composition
needs customization. Use `ids` when the label, circle, or surrounding layout need stable explicit
IDs across a custom tree. Use `ProgressCircular.useProgress()` with `RootProvider` when state must
exist outside the rendered tree, and `ProgressCircular.useProgressContext()` below a root/provider
when a custom component needs the current state. Direct Ark imports remain an escape hatch for
surfaces intentionally outside the moduix API.

### Default props

| Prop            | Default                | Notes                                   |
| --------------- | ---------------------- | --------------------------------------- |
| `defaultValue`  | `50`                   | Initial uncontrolled value.             |
| `min`           | `0`                    | Lower bound for percent and ARIA value. |
| `max`           | `100`                  | Upper bound for percent and ARIA value. |
| `formatOptions` | `{ style: 'percent' }` | Controls `ValueText` formatting.        |
| `locale`        | `'en-US'`              | Locale used by `Intl.NumberFormat`.     |

### Behavioral notes

`ProgressCircular.Circle` receives the progressbar role and ARIA value attributes from Ark. Ark
also owns the SVG variables for radius, circumference, percent, and stroke offset.

All Ark-backed parts except the fixed `Ring` support `asChild` through polymorphic composition.
Keep replacement children semantic and preserve the progressbar semantics on the circle.

`ProgressCircular.ValueText` announces changes with `aria-live="polite"`. Keep the visible label in
the tree, or provide `aria-label`/`aria-labelledby` when composing without `ProgressCircular.Label`.

## Examples

### Controlled

Keep `value` in React state when circular progress mirrors another control or external task state.

<style>{ProgressCircularDocs.progressCircularControlledExampleCss}</style>

```tsx preview file="./_snippets/progress-circular/controlled.tsx"

```

### Indeterminate

Pass `defaultValue={null}` or `value={null}` when progress is running but completion is unknown.

<style>{ProgressCircularDocs.progressCircularBasicExampleCss}</style>

```tsx preview file="./_snippets/progress-circular/indeterminate.tsx"

```

### Initial Value

Use `defaultValue` to set the initial uncontrolled progress value.

<style>{ProgressCircularDocs.progressCircularBasicExampleCss}</style>

```tsx preview file="./_snippets/progress-circular/initial-value.tsx"

```

### Min Max

Use `min` and `max` when the value does not use the default `0` to `100` range.

<style>{ProgressCircularDocs.progressCircularBasicExampleCss}</style>

```tsx preview file="./_snippets/progress-circular/min-max.tsx"

```

### Value Text

Use `translations.value(details)` when the formatted percent is not the wording users need.

<style>{ProgressCircularDocs.progressCircularBasicExampleCss}</style>

```tsx preview file="./_snippets/progress-circular/value-text.tsx"

```

### Advanced Customization

Bypass `ProgressCircular.Ring` when you need to alter the Ark circle tree or create the progress
store outside the rendered subtree.

#### Root Provider

Create the progress store with `ProgressCircular.useProgress()` outside the rendered tree when
another component needs to own state.

<style>{ProgressCircularDocs.progressCircularBasicExampleCss}</style>

```tsx preview file="./_snippets/progress-circular/root-provider.tsx"

```

#### State Views

Use `ProgressCircular.View` when supporting text or UI should switch with the Ark progress state
instead of duplicating state checks outside the component.

<style>{ProgressCircularDocs.progressCircularStateViewsExampleCss}</style>

```tsx preview file="./_snippets/progress-circular/state-views.tsx"

```

## Styling

Use `className` on exported parts for structural styling changes. For state styling, prefer Ark
data attributes, Ark SVG variables, and moduix `data-slot` hooks.

### CSS Properties

<div className="docs-reference">
  <Tabs
    values={[{ label: 'CSS Variables', value: 'CSS Variables' }]}
    className="docs-reference-tabs"
    contentItemClassName="docs-reference-panel"
  >
    <Tab>
      <ProgressCircularDocs.ProgressCircularCssPropertiesPanel />
    </Tab>
  </Tabs>
</div>

### Styling hooks

- `data-slot="progress-circular-root"`, `progress-circular-root-provider`,
  `progress-circular-label`, `progress-circular-circle`, `progress-circular-circle-track`,
  `progress-circular-circle-range`, `progress-circular-value-text`, and `progress-circular-view`.
- `ProgressCircular.Ring` shares `ProgressCircular.Circle`'s `data-slot`, `className`, ref, and
  `--moduix-progress-circular-*` size and thickness variables.
- Ark hooks: `data-scope="progress"`, `data-part`, `data-state="indeterminate|loading|complete"`,
  `data-orientation`, `data-value`, and `data-max`.
- Ark circle variables: `--size`, `--thickness`, `--moduix-radius`, `--circumference`, `--percent`, and
  stroke offset values generated by Ark.