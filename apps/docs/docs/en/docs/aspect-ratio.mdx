---
title: Aspect Ratio
component: aspect-ratio
description: Constrains media and embed content inside a responsive box with a fixed aspect ratio.
---

import * as AspectRatioDocs from '@/components/examples/aspect-ratio';

## API Reference

Ark UI does not ship a dedicated `aspect-ratio` primitive. Moduix builds this component with the official
[Ark factory](https://ark-ui.com/docs/guides/composition#the-ark-factory), follows Ark's
[composition](https://ark-ui.com/docs/guides/composition), [styling](https://ark-ui.com/docs/guides/styling), and
[ref](https://ark-ui.com/docs/guides/ref) guidance.

## Basic

Start with `AspectRatio`, put width or max-width on the root itself, and pass a numeric `ratio`.
Direct `img`, `video`, `iframe`, `canvas`, and `svg` children fill the frame automatically.

<style>{AspectRatioDocs.aspectRatioBasicCss}</style>

<PreviewFrame maxWidth="lg">
  ```tsx preview file="./_snippets/aspect-ratio/basic.tsx"

````
</PreviewFrame>

## Install with shadcn

<ShadcnInstall packageName="aspect-ratio" />

## Anatomy

```text
AspectRatio / AspectRatio.Root
└─ media | iframe | canvas | svg | custom content
````

| Part                               | Role                            |
| ---------------------------------- | ------------------------------- |
| `AspectRatio` / `AspectRatio.Root` | Responsive frame with clipping. |

## Composition

### Recommended composition

Use `AspectRatio` for most media frames. Put sizing, radius, and layout classes on the root itself;
`AspectRatio.Root` is the same root exposed for namespace consistency.

- `ratio` is required and numeric; pass a finite value greater than zero for valid CSS behavior
- `asChild` uses Ark factory composition and requires exactly one React element
- direct `img`, `video`, `iframe`, `canvas`, and `svg` children fill the frame automatically
- `className` styles and sizes the root directly
- `--moduix-aspect-ratio-radius` controls the default rounded frame

### When to go custom

Wrap `AspectRatio` when shadows, focus rings, or layout decoration need to escape the clipped
frame. Keep the clipping on the root itself, and override the root `aspect-ratio` in your own CSS
when a breakpoint needs a different frame.

### Default props

| Entry                          | Default                   | Values / Notes                       |
| ------------------------------ | ------------------------- | ------------------------------------ |
| `ratio`                        | -                         | Required positive finite `number`    |
| `className`                    | `undefined`               | Styles and sizes the root            |
| `style`                        | `undefined`               | Can override `aspect-ratio` directly |
| `asChild`                      | `undefined`               | Ark factory composition              |
| `--moduix-aspect-ratio-radius` | `var(--moduix-radius-md)` | CSS length or token value            |

### Behavioral notes

- The root renders with `data-scope="aspect-ratio"`, `data-part="root"`, and
  `data-slot="aspect-ratio-root"`.
- `position: relative` on the root is intentional and supports absolute-fill child patterns such
  as Next.js `Image fill`.
- The previous preset strings (`square`, `video`, `portrait`, `photo`) were removed during the Ark
  migration. Use numbers such as `1`, `16 / 9`, `9 / 16`, and `4 / 3`.

## Examples

### Card layouts

Keep repeated cards visually aligned by wrapping their media in the same aspect ratio even when the
source assets differ.

<style>{AspectRatioDocs.aspectRatioGridCss}</style>

```tsx preview file="./_snippets/aspect-ratio/card-grid.tsx"

```

### Embedded content

Wrap video or other embeds in `AspectRatio` to keep the frame stable before the external content
finishes loading.

<style>{AspectRatioDocs.aspectRatioEmbedCss}</style>

<PreviewFrame maxWidth="lg">
  ```tsx preview file="./_snippets/aspect-ratio/embedded-content.tsx"

````

</PreviewFrame>

### Migrating from shadcn

If you are coming from shadcn, move width and radius classes onto `AspectRatio` itself. The same
root pattern works with `next/image fill` in Next.js.

<style>{AspectRatioDocs.aspectRatioMigrationCss}</style>

<PreviewFrame maxWidth="lg">
  ```tsx preview file="./_snippets/aspect-ratio/migrating-from-shadcn.tsx"

````

</PreviewFrame>

### Advanced Customization

Use `asChild` when you need to bypass the default root host and let a semantic element such as
`figure` own the frame. Ark merges the component props into the single child element.

<style>{AspectRatioDocs.aspectRatioBasicCss}</style>

<PreviewFrame maxWidth="lg">
  ```tsx preview file="./_snippets/aspect-ratio/advanced-customization.tsx"

```

</PreviewFrame>

## Styling

### CSS Properties

<CssPropertiesSection properties={AspectRatioDocs.aspectRatioCssProperties} />

### Styling hooks

- `data-scope="aspect-ratio"` and `data-part="root"` are the Ark-aligned root hooks.
- `data-slot="aspect-ratio-root"` is the stable moduix root hook.
- Put width, max-width, and layout styles on the root itself. Override `aspect-ratio` from your own
  CSS when a breakpoint needs a different ratio.
- `overflow: hidden` is intentional. If visible decoration must escape the frame, put that
  decoration on an outer wrapper instead of removing root clipping.
```