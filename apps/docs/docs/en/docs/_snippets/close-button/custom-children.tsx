import { CloseButton } from '@moduix/react';

export default function CloseButtonCustomChildrenDemo() {
  return (
    <CloseButton className="customChildrenButton">
      <span>Dismiss</span>
    </CloseButton>
  );
}