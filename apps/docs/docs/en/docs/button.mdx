---
title: Button
component: button
description: A styled action control built with the Ark factory composition model.
---

import * as ButtonDocs from '@/components/examples/button';

## API Reference

Ark UI does not ship a dedicated `button` primitive. Moduix builds this component with the official
[Ark factory](https://ark-ui.com/docs/guides/composition#the-ark-factory), follows Ark's
[composition](https://ark-ui.com/docs/guides/composition), [styling](https://ark-ui.com/docs/guides/styling), and
[ref](https://ark-ui.com/docs/guides/ref) guidance.

## Basic

Use the short `<Button />` form for normal actions. `Button.Root` is the equivalent namespaced
form.

```tsx preview file="./_snippets/button/basic.tsx"

```

## Install with shadcn

<ShadcnInstall packageName="button" />

## Anatomy

`Button` is a single-part component.

```text
Button.Root
└─ root[data-scope="button"][data-part="root"][data-slot="button-root"]
   └─ children
```

| Part                     | Role                                          |
| ------------------------ | --------------------------------------------- |
| `Button` / `Button.Root` | Root interactive surface with moduix styling. |
| `Button`                 | Callable alias of `Button.Root`.              |

## Composition

### Recommended composition

Use a native root for actions. It defaults to `type="button"` to avoid accidental form submission;
set `type="submit"` explicitly for a submit action.

### Default props

| Prop      | Default   | Values                                                                                   |
| --------- | --------- | ---------------------------------------------------------------------------------------- |
| `loading` | `false`   | `boolean`                                                                                |
| `variant` | `default` | `default`, `outline`, `secondary`, `destructive`, `destructive-outline`, `ghost`, `link` |
| `size`    | `md`      | `xs`, `sm`, `md`, `lg`, `xl`, `icon-sm`, `icon-md`, `icon-lg`                            |
| `type`    | `button`  | Native roots only                                                                        |

### When to go custom

Use `asChild` when one semantic child should own the DOM node. Ark composition requires exactly one
child. The default `type="button"` is not injected into an `asChild` target.

When `Dialog.Trigger`, `Menu.Trigger`, or another Ark part composes Button as its child, the parent
part's `data-scope` and `data-part` take precedence on the shared DOM node. Button retains its
recipe classes and stable `data-slot`, and Ark continues to merge the trigger handlers and refs.

Use a real anchor for navigation. On an `asChild` host, `disabled`, `loading`, and
`aria-disabled="true"` expose `aria-disabled` plus the disabled styling state and prevent click
activation. They do not add a native `disabled` attribute to the custom host.

### Behavioral notes

- Native attributes, `className`, and `style` pass through without callback translation. Clicks on
  disabled roots are prevented.
- The forwarded ref targets the rendered root.
- Native `disabled`, `aria-disabled="true"`, and `loading` expose `data-disabled`.
- `loading` is narrow sugar: it sets `data-loading`, `aria-busy`, and the disabled styling path
  while the spinner and label stay fully compositional.
- Direct child `svg` elements inherit `currentColor` and use `--moduix-button-icon-size`.
- Icon-only buttons require an accessible name.
- Chakra-only `colorPalette`, responsive recipe props, `ButtonGroup`, and managed loading props are
  intentionally outside this root-only moduix contract.

## Examples

### AsChild Link

Use `asChild` for navigation so the anchor retains link semantics while receiving button styling.

```tsx preview file="./_snippets/button/as-child-link.tsx"

```

### Disabled

Use `disabled` for native buttons and `asChild` hosts.

```tsx preview file="./_snippets/button/disabled.tsx"

```

### Icons

Icons can appear before or after text; their JSX order and the Button root's gap determine layout.

```tsx preview file="./_snippets/button/icons.tsx"

```

### Pending State

Use `loading` for the state wiring while keeping the label, spinner, and async lifecycle explicit
in product code. On `asChild` hosts it also prevents click activation.

```tsx preview file="./_snippets/button/pending-state.tsx"

```

### Ref

The forwarded ref points at the rendered root and can be used for imperative focus or measurement.

```tsx preview file="./_snippets/button/ref.tsx"

```

### Sizes

Text and icon-only sizes share the same root API.

```tsx preview file="./_snippets/button/sizes.tsx"

```

### Variants

Use variants to communicate action priority and intent.

```tsx preview file="./_snippets/button/variants.tsx"

```

## Styling

### CSS Properties

<CssPropertiesSection properties={ButtonDocs.buttonCssProperties} />

### Styling hooks

- `data-slot="button-root"` is the stable moduix selector.
- Standalone roots default to `data-scope="button"` and `data-part="root"`. A composing Ark parent
  may replace them with its own anatomy values.
- `data-variant` and `data-size` reflect the active visual recipe.
- `data-disabled` is present for native `disabled`, `aria-disabled="true"`, and `loading`.
- `data-loading` is present when `loading={true}`.
- `[data-disabled]` drives disabled visuals for native `disabled`, `aria-disabled="true"`, and `loading`.
- Enabled button variants use a subtle press movement; `link` keeps opacity feedback only, and reduced-motion
  preferences omit the movement.
