---
title: Spinner
component: spinner
description: A compact loading indicator with status semantics, size control, and optional custom indicator content.
---

import * as SpinnerDocs from '@/components/examples/spinner';

## API Reference

Ark UI does not ship a dedicated spinner primitive. Moduix builds this component with the official
[Ark factory](https://ark-ui.com/docs/guides/composition#the-ark-factory), follows Ark's
[composition](https://ark-ui.com/docs/guides/composition), [styling](https://ark-ui.com/docs/guides/styling), and
[ref](https://ark-ui.com/docs/guides/ref) guidance.

## Basic

Start with `Spinner` for compact pending states inside buttons, inline status rows, and small empty
states. The default path announces loading automatically with `role="status"` and a fallback
`aria-label="Loading"`.

<PreviewFrame maxWidth="sm">
  ```tsx preview file="./_snippets/spinner/basic.tsx"

  ```
</PreviewFrame>

## Install with shadcn

<ShadcnInstall packageName="spinner" />

## Anatomy

The normal consumer path is short:

```text
Spinner
└─ spinner-indicator
   └─ spinner-ring (default indicator only)
```

| Part                       | Role                                                                                  |
| -------------------------- | ------------------------------------------------------------------------------------- |
| `Spinner`                  | Root status element. Supports `size`, `decorative`, `asChild`, and native span props. |
| `spinner-indicator`        | Inner rotating wrapper that keeps sizing and spin behavior.                           |
| `spinner-ring`             | Default ring indicator. Renders only when custom `children` are absent.               |

## Composition

### Recommended composition

Stay with the default ring when a compact loading indicator is enough.

- Use `size` for density changes.
- Use `decorative` when adjacent visible text already communicates the loading state.
- Use `aria-label` or `aria-labelledby` when the status name should be more specific than
  `"Loading"`.

### When to go custom

`Spinner` is a root-only component. Use `children` to replace the default ring while keeping the
default root, sizing, and rotating indicator wrapper.

Use `asChild` when the root element itself must be provided by the consumer. In that mode, Ark merges
root props into the single child, so the child owns its visual contents.

Keep the `asChild` child non-interactive. It is a visual host for the spinner, not a button or link.

### Default props

| Prop         | Default     | Notes                                                    |
| ------------ | ----------- | -------------------------------------------------------- |
| `size`       | `'md'`      | Accepts `'inherit' \| 'xs' \| 'sm' \| 'md' \| 'lg' \| 'xl'`. `inherit` uses `1em`. |
| `decorative` | `false`     | Hides the default root from assistive technology; with `asChild`, preserves the child's semantics. |
| `asChild`    | `false`     | Merges root props into the single child element.         |
| `aria-label` | `'Loading'` | Applied by default when the spinner is not decorative.   |

### Behavioral notes

- The default root renders as a `span` with `data-scope="spinner"`, `data-part="root"`,
  `data-slot="spinner-root"`, and `data-size`.
- `Spinner.Root` is a compatibility alias for `Spinner`; use `Spinner` in new code.
- Non-decorative spinners render with `role="status"`.
- `decorative={true}` hides the default root from assistive technology. With `asChild`, it instead
  removes Spinner's status and labelling props while preserving the child semantics.
- `Spinner` owns these semantics and its `data-scope`, `data-part`, `data-slot`, and `data-size`
  hooks, so native props cannot override them.
- The rotating wrapper is `data-scope="spinner"`, `data-part="indicator"`, and
  `data-slot="spinner-indicator"`.
- Custom `children` replace only the default ring. The wrapper still owns sizing and rotation.
- With `asChild`, the consumer-provided child becomes the root and must provide any visual indicator
  content it needs.
- Spinner keeps its loading motion under `prefers-reduced-motion`; the motion is the visual loading cue.

## Examples

### Custom Indicator

Pass custom `children` when the default ring should be replaced but the size and status behavior
should stay the same.

<PreviewFrame maxWidth="sm">
  ```tsx preview file="./_snippets/spinner/custom-indicator.tsx"

  ```
</PreviewFrame>

### Inline Loading Text

Use `decorative` when visible text next to the spinner already says what is happening.

<PreviewFrame maxWidth="sm">
  ```tsx preview file="./_snippets/spinner/inline-loading-text.tsx"

  ```
</PreviewFrame>

### Sizes

Use smaller sizes inside compact controls and larger sizes for standalone loading states.

<PreviewFrame maxWidth="sm">
  ```tsx preview file="./_snippets/spinner/sizes.tsx"

  ```
</PreviewFrame>

### In a Button

Compose a decorative spinner with the visible action label. The preview status is centered below the
button and is documentation-only feedback.

<PreviewFrame contentWidth="fit-content" maxWidth="sm">
  ```tsx preview file="./_snippets/spinner/button.tsx"

  ```
</PreviewFrame>

### Advanced Customization

Use `asChild` only when the root host itself must change. This bypasses the default root host, so the
single child receives its attributes and class name and must render the visual indicator itself.

<PreviewFrame maxWidth="sm">
  ```tsx preview file="./_snippets/spinner/as-child.tsx"

  ```
</PreviewFrame>

## Styling

Style `Spinner` through `className`, Ark `data-scope` / `data-part` selectors, stable `data-slot`
hooks, `data-size`, and the public `--moduix-spinner-*` variables documented by the CSS API.

### CSS Properties

<div className="docs-reference">
  <Tabs
    values={[{ label: 'CSS Variables', value: 'CSS Variables' }]}
    className="docs-reference-tabs"
    contentItemClassName="docs-reference-panel"
  >
    <Tab>
      <SpinnerDocs.SpinnerCssPropertiesPanel />
    </Tab>
  </Tabs>
</div>

### Styling hooks

- `[data-scope="spinner"][data-part="root"]` targets the Ark-style root.
- `[data-scope="spinner"][data-part="indicator"]` targets the rotating inner wrapper.
- `[data-scope="spinner"][data-part="ring"]` targets the default ring only.
- `data-slot="spinner-root"` is the stable moduix selector for the exported root.
- `data-slot="spinner-indicator"` is the stable moduix selector for the rotating inner wrapper.
- `data-slot="spinner-ring"` targets the default ring only.
- `Spinner` writes `data-size="inherit" | "xs" | "sm" | "md" | "lg" | "xl"` on the root.
- `className` applies only to the root. Target the indicator or ring with descendant selectors from
  that root when needed.
