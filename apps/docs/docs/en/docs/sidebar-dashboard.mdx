---
title: Sidebar dashboard
description: A copy-owned desktop dashboard shell with collapsible and resizable moduix navigation.
---

import { SidebarDashboardPreview } from '@/components/recipes/sidebar-dashboard-preview';

# Sidebar dashboard

Start a desktop workspace with a resizable navigation sidebar and a simple dashboard overview.
`Sidebar` owns the collapse, keyboard resize, and collapsed-nav tooltips. The Projects item also
demonstrates nested navigation, and the footer includes a workspace menu.

<ExampleFrame clientOnly>
  <SidebarDashboardPreview />
</ExampleFrame>

<div className="docs-reference">
  <Tabs values={[{ label: 'Component', value: 'Component' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="Component">

```tsx file="../../../src/components/recipes/sidebar-dashboard.tsx"

```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/recipes/sidebar-dashboard.module.css"

```

    </Tab>
  </Tabs>
</div>

## Install

<ShadcnInstall packageName={['button', 'card', 'collapsible', 'menu', 'sidebar']} itemLabel="recipe" copiedSource dependencies={['lucide-react']} />

## Use in a route

Replace the example anchors, labels, and dashboard content with your routes and data. This recipe is
desktop-first: account menus, notifications, and mobile drawer navigation remain separate recipes so
you can compose them only when your application needs them.
