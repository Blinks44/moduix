---
title: Sign up
description: A copy-owned account creation form built with moduix React components.
---

import { SignUpPreview } from '@/components/recipes/sign-up-preview';

# Sign up

Create an account with native validation for name, email, passwords, and terms acceptance. The
recipe deliberately leaves authentication and matching-password validation to your `onSubmit`
handler.

<ExampleFrame clientOnly>
  <SignUpPreview />
</ExampleFrame>

<div className="docs-reference">
  <Tabs values={[{ label: 'Component', value: 'Component' }, { label: 'Styles', value: 'Styles' }]} className="docs-tabs">
    <Tab value="Component">

```tsx file="../../../src/components/recipes/sign-up.tsx"

```

    </Tab>
    <Tab value="Styles">

```css file="../../../src/components/recipes/sign-up.module.css"

```

    </Tab>
  </Tabs>
</div>

## Install

<ShadcnInstall packageName={['button', 'card', 'checkbox', 'field', 'input']} itemLabel="recipe" copiedSource />

## Use in a route

Render `SignUp` in the route that owns registration and pass its real submit handler. Replace the
default `/terms`, `/privacy`, and `/sign-in` links with your application routes.
