---
title: Splitter
component: splitter
description: A resizable layout primitive for dividing an interface into panels.
---

import * as SplitterDocs from '@/components/examples/splitter';

## API Reference

<PrimitiveReference href="https://ark-ui.com/docs/components/splitter" />

## Basic

Use Ark-style parts: `Splitter`, `Splitter.Panel`, and `Splitter.ResizeTrigger`. The trigger
shows the moduix resize indicator by default. `panels` defines ids and constraints; rendered panel
ids must match that array.

<style>{SplitterDocs.splitterExampleCss}</style>

<PreviewFrame>
  ```tsx preview file="./_snippets/splitter/basic.tsx"

  ```
</PreviewFrame>

## Install with shadcn

<ShadcnInstall packageName="splitter" />

## Anatomy

```text
Splitter
├─ Splitter.Panel[id]
├─ Splitter.ResizeTrigger[id="panel-a:panel-b"]
│  └─ Splitter.ResizeTriggerIndicator (default, customizable)
└─ Splitter.Panel[id]
```

| Part                              | Role                                                                                   |
| --------------------------------- | -------------------------------------------------------------------------------------- |
| `Splitter` / `Splitter.Root`      | Owns panel constraints, sizes, orientation, callbacks, and the splitter state machine. |
| `Splitter.Panel`                  | Resizable region. Its `id` must match one item in the `panels` prop.                   |
| `Splitter.ResizeTrigger`          | Accessible handle button between adjacent panels.                                      |
| `Splitter.ResizeTriggerIndicator` | Default visual indicator; pass custom trigger children to replace it.                  |
| `Splitter.RootProvider`           | Renders the same part tree from state created with `useSplitter()`.                    |

## Composition

### Recommended composition

Render panels and handles explicitly in the same order they appear visually. Use trigger ids in the
`"left:right"` form so Ark can connect the two adjacent panel ids. Omit trigger children for the
default resize indicator; pass children only when its content needs customization.

### When to go custom

Use controlled `size` with `onResize(details)` when panel sizes must be mirrored into app state.
Import `useSplitter`, `useSplitterContext`, and `createSplitterRegistry` from `@moduix/react` for
imperative controls, externally-owned state, and nested multi-drag layouts. Pair `useSplitter()`
with `Splitter.RootProvider` when state must be created outside the rendered part tree.

Use `asChild` only when another element must own the rendered node. Custom resize triggers must keep
button semantics, focus behavior, and keyboard interaction intact. With `asChild`, provide exactly one
interactive trigger child; it does not receive the default resize indicator.

### Default props

| Prop               | Default        | Notes                                                     |
| ------------------ | -------------- | --------------------------------------------------------- |
| `orientation`      | `"horizontal"` | Use `"vertical"` for stacked panels.                      |
| `defaultSize`      | Ark-calculated | Use percentages for stable server-rendered sizing.        |
| `keyboardResizeBy` | Ark default    | Controls arrow-key resize distance.                       |
| `size`             | uncontrolled   | Pass with `onResize(details)` for controlled panel sizes. |
| `disabled`         | `false`        | Set on `ResizeTrigger` to disable a handle.               |

### Behavioral notes

`panels` is required and defines constraints such as `minSize`, `maxSize`, `collapsible`, and
`collapsedSize`. Numeric size values are percentages; Ark also accepts CSS lengths for initial and
constraint sizes, which are measured on the client and can shift after hydration.

Callbacks keep Ark detail objects: `onResize(details)`, `onResizeEnd(details)`, `onCollapse(details)`,
and `onExpand(details)`. `onResizeStart` is called when a resize begins.

## Examples

### Collapsible Panels

Set `collapsible` and `collapsedSize` on a panel definition when a panel can shrink to a collapsed
rail. To keep pointer resizing continuous, set `minSize` to the same value as `collapsedSize`; a
larger `minSize` intentionally makes Zag snap between the expanded minimum and the rail.

<style>{SplitterDocs.splitterExampleCss}</style>

<PreviewFrame>
  ```tsx preview file="./_snippets/splitter/collapsible-panels.tsx"

  ```
</PreviewFrame>

### Controlled

Control panel sizes from React state and update that state from `details.size`.

<style>{SplitterDocs.splitterStackCss}</style>

<PreviewFrame>
  ```tsx preview file="./_snippets/splitter/controlled.tsx"

  ```
</PreviewFrame>

### Dynamic Collapsible

Programmatic collapse and expand methods work well for responsive sidebars that react to measured
available width.

<style>{SplitterDocs.splitterExampleCss}</style>

<PreviewFrame>
  ```tsx preview file="./_snippets/splitter/dynamic-collapsible.tsx"

  ```
</PreviewFrame>

### Multiple Panels

Add another `Panel` and another adjacent `ResizeTrigger` for three-panel layouts.

<style>{SplitterDocs.splitterExampleCss}</style>

<PreviewFrame>
  ```tsx preview file="./_snippets/splitter/multiple-panels.tsx"

  ```
</PreviewFrame>

### Nested

Share a registry between nested splitters to support multi-drag behavior at horizontal and vertical
handle intersections.

<style>{SplitterDocs.splitterNestedCss}</style>

<PreviewFrame>
  ```tsx preview file="./_snippets/splitter/nested.tsx"

  ```
</PreviewFrame>

### Root Provider

Use `useSplitter()` with `Splitter.RootProvider` when controls outside the root need access to the
same splitter API.

<style>{SplitterDocs.splitterStackCss}</style>

<PreviewFrame>
  ```tsx preview file="./_snippets/splitter/root-provider.tsx"

  ```
</PreviewFrame>

### Vertical

Set `orientation="vertical"` and give the root a stable height so stacked panels have room to resize.

<style>{SplitterDocs.splitterVerticalCss}</style>

<PreviewFrame>
  ```tsx preview file="./_snippets/splitter/vertical.tsx"

  ```
</PreviewFrame>

## Styling

Style `Splitter` through `className`, Ark attributes, stable `data-slot="splitter-*"` hooks, and the
public Moduix CSS variables below. `Splitter.Root` and `Splitter.RootProvider` apply
`--moduix-splitter-width` and `--moduix-splitter-height` through their default inline style so those variables can
override Ark's root layout style. The default surface lives on the root, while panels stay flat and
flush inside it.

The resize trigger contributes a `1px` separator to layout with a visually thinner `0.5px` line, a
transparent hit area, and a raised rounded indicator on top. The indicator is centered over the
separator for both orientations. Hover strengthens its border and shadow, while dragging adds a
slight scale and stronger shadow. Releasing the pointer returns the trigger to its idle appearance.
Override the corresponding `--moduix-splitter-resize-trigger-*` variables when the surrounding theme needs
a different treatment.

### CSS Properties

<div className="docs-reference">
  <Tabs
    values={[{ label: 'CSS Variables', value: 'CSS Variables' }]}
    className="docs-reference-tabs"
    contentItemClassName="docs-reference-panel"
  >
    <Tab>
      <SplitterDocs.SplitterCssPropertiesPanel />
    </Tab>
  </Tabs>
</div>

### Styling hooks

Ark writes `data-scope="splitter"` and `data-part` to rendered parts. Moduix also writes
`data-slot` values: `splitter-root`, `splitter-root-provider`, `splitter-panel`,
`splitter-resize-trigger`, and `splitter-resize-trigger-indicator`.

Important Ark state hooks include `data-orientation`, `data-dragging`, trigger `data-focus`, trigger
`data-disabled`, panel `data-id`, and panel `data-index`.
